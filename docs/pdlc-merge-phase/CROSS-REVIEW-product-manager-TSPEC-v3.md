# Cross-Review: product-manager — TSPEC (round 3)

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-merge-phase/TSPEC-pdlc-merge-phase.md` (v1.2, commit `8144608`)
**Date:** 2026-08-02
**Iteration:** 3
**Scope:** Confirmation pass over the v1.1 → v1.2 delta only (§2.2/§2.3, §4.1, §4.7, §11.3, §13, §15.2/§15.3) against FSPEC v1.3 and REQ v1.1 — whether the targeted changes preserve my round-2 APPROVED, whether my low finding M-01 is closed, and specifically whether the author's judgment that E-4 needs no FSPEC edit is sound. No unchanged section was re-litigated; technical design and test mechanics remain outside this lens.

## Delta assessment

Both changes land cleanly and neither touches a behaviour, a safety property, or a finding resolution I confirmed in round 2. The `_ghRun` transport gaining a third field is, from the product lens, not merely a testability fix but the closure of a real operator-facing gap: FSPEC §6.3 requires the exhaustion reason to name each attempted method **and its failure**, and a two-field `{ ok, stdout }` transport could not produce the second half — §4.7 now derives `detail` from the first line of the transport's `stderr` via the existing `firstLine` helper, pins the fixed token `"merge not confirmed"` for the zero-exit-but-unconfirmed case so the field is never empty or `undefined`, and joins the per-attempt rows into §11 row 17's reason line, so an operator reading a `deferred` run learns *why* each method failed rather than only that it did; §11.3's adapter arm preserving `stderr` on the `{"ok":false}` reply (with the assertion that a two-field implementation reds) closes the same gap at the seam, and `defaultGhRun` adopting `defaultGit`'s exact `{ ok, stdout, stderr }` contract and `catch` shape keeps it a house idiom rather than a new one. On E-4 and my M-01, the paperwork I asked for is done — §15.2 records the `0…10` tightening as a distinct class ("declared downstream tightening", not an unresolved divergence), §15.3's closing row no longer claims that nothing diverges, and §3.1 plus §13.2's `10`/`11` boundary pair keep the reasoning and the proof where a reader will stand. I judge the author's "no FSPEC edit requested" call **sound but second-best, and not worth a round**: the narrowing genuinely runs in FSPEC §10.3's own fallback direction (out-of-domain takes the default), the literal transcription is demonstrably the unsafe reading since an unbounded count converts FSPEC §11 row 13 from `deferred` into `refused, row: "internal"`, and the operator-visible surface is retry counts above 10 — so nothing user-facing is at risk. My only residual preference is that REQ §7 and FSPEC §10.3 are the documents an operator actually reads, and §15.2 already writes the one-line edit that would state the domain once, upstream; I would take that edit the next time the FSPEC is versioned for any other reason, and I explicitly do not want it triggering one. Everything I verified in round 2 survives: the §11 row-identity rule, the §8.2 ahead-of-remote notice with its three suppression cases, `mergeableRetryDelay`, the `squash` enum scoping, and the four safety properties (no guard override, squash absent by default, no precondition-bypassing mode, `merged` never downgraded). Approval holds.

## Verdict

VERDICT: APPROVED
{"high": 0, "medium": 0, "low": 0}
