# Cross-Review: product-manager — PROPERTIES (round 2)

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-merge-phase/PROPERTIES-pdlc-merge-phase.md` (v1.1, commit `1c3daab`)
**Date:** 2026-08-02
**Iteration:** 2
**Scope:** Delta re-review against my round-1 cross-review only — disposition of the two blocking findings and six advisories, plus a check that the revision (including the `D_core` re-derivation and the domain-size corrections it brought with it) broke nothing in my lens: no property contradicting REQ v1.1 / FSPEC v1.3 / TSPEC v1.2, every property still traced to an AC and an owning PLAN task, no safety commitment newly orphaned. Harness mechanics and generator design remain the TE/SE lens.

## Disposition

| ID | Round-1 severity | Disposition | Evidence in v1.1 |
|----|------------------|-------------|------------------|
| 1 | **blocking** | **Resolved twice over, which is the right amount** | `mergeRequiresCi` now enters `D_core` **at the CI level** (§2's derivation table crosses it only where it can change an outcome), *and* **PROP-M-21** gives the rule its own exhaustive `ci ∈ 5 × mergeRequiresCi ∈ 2` sub-domain on a fixture built to reach guard 11 — `O5` clear so the guard cannot preempt it, `state: OPEN` — with the oracle transcribed column-by-column from FSPEC §5's table. The mutant-killer is stated explicitly as the differential: for every `ci ≠ "none"` the two settings are deep-equal, so a rule widened to `ci === "none" \|\| ci === "pending"` reds at `(pending, false)`. That is precisely the mutant that survived v1.0's entire suite. §8.4 goes further and corrects its own attribution — "the CI rule's relaxed cell (**M-21, *not* M-03** — v1.0 named a property that could not kill it)" — which was the heart of the finding: a mutation-target list that names a property unable to kill the mutant is worse than no list. AC-4.1/4.2/4.3/4.4 are all traced to PROP-M-21 in §7. |
| 2 | **blocking** | **Resolved exactly as specified** | PROP-M-18's oracle is now split by resolving row: **row 18** ⇒ ≥ 1 and ≤ 3 merge commands; **row 3** ⇒ **exactly zero**, stated positively as "the already-merged path attempts no merge and evaluates no guard, and asserting that positively is what pins NFR-5"; every non-`merged` row ⇒ zero merge commands, zero `push`/`checkout`/`rebase`/`merge` verbs, no `_recordQueueRow` call. "Exactly one `_recordQueueRow` call" is retained across **both** merged rows, which is the AC-5.2 recovery conjunct and was correct in v1.0. The property now agrees with FSPEC §2.5 and TSPEC §13.3 rather than contradicting them, and the row-3 arm has become a positive assertion of idempotence rather than a hole. |
| 3 | advisory | **Resolved** | §7's exclusion list now carries AC-4.0 with the structural argument named: "carried *structurally* by **PROP-M-02**: `decideMerge` is a function of `(record, config)` alone and `record.ci` can only come from `O2`". The completeness argument is now on the page for the DoD phase to read instead of in a reviewer's head. |
| 4 | advisory | **Resolved** | PROP-M-06's oracle counts **escalations**, not notices — "exactly one line beginning `MERGE ESCALATION: `" — **plus** FSPEC §9.4's merge-deferred note, "which every `refused` run emits, and nothing else". Asserting the §9.4 note on the guard path turns its presence into a fact rather than an accident, which is more than I asked for. |
| 5 | advisory | **Resolved** | PROP-M-19's domain is now PROP-M-16's 34 + PROP-M-17's 29 + **two named fixtures** (a `merge` section present but not an object; a merged run whose `prUrl` neither `parsePrRef` nor `O1.number` resolves) = 65, and the third previously-unreachable member — the §2.5 non-overwrite note — is accounted for as arriving with PROP-M-16's overlays. The every-member coverage conjunct is now satisfiable, so the catalogue-closure assertion no longer risks being weakened under pressure to make a correct build green. |
| 6 | advisory | **Resolved** | The three uncited carriers are cited — AC-2.1/AC-2.2 → PROP-M-11 (with "filtered by capability **in that order**" added to the oracle so the citation is earned), AC-5.1 → PROP-M-14 and PROP-M-15, AC-7.2 → PROP-M-09 — and the four structural ACs are declared: AC-1.1 as TSPEC §10.4's call site asserted by `pipelineWiring`, AC-1.2 as the union of guards PROP-M-01/-03/-21 quantify, AC-2.5 as inside PROP-M-11's candidate-chain oracle, AC-4.0 per finding 3. AC-4.1 also picked up a real carrier rather than a declaration. |
| 7 | advisory | **Resolved by quantifying, which is what I preferred** | AC-1.2a has left the exclusion list — §7 says so in place — and is now a conjunct on PROP-M-01's retry sub-path: for every `mergeableRetries = R ∈ 0…10` an exhausting run demands `O1` exactly `1 + R` times and the reason line interpolates that same count, `after 1 observations` at `R = 0` included, with counter and sentence read by one oracle so they cannot drift. Eleven enumerated values where TSPEC §13.2 samples three. |
| 8 | advisory (record) | **Still holds, and the strengthening is real** | The four safety commitments I called out as well-pinned are unchanged in substance and two got stronger: PROP-M-06's domain is corrected from a wrong 360 to an asserted 1 080 (2 × 2 × 5 × 3 × 3 × 6 — I recomputed it), and PROP-M-11's chain-order conjunct now carries AC-2.1/AC-2.2. No property here is an example test wearing a quantifier. |
| — | new check | **Nothing broke** | I re-derived the arithmetic the revision introduced: `D_core` = 1 (`mergeMode: off`) + 2 modes × 209 = **419** ✓; PROP-M-03 = 120 row-18 cases × 5 degradations + 2 exceptions = **602** ✓; PROP-M-04 = the 209-case subtree decided twice ✓ (correctly excluding `off`, since the claim is `gated` ≡ `on`); PROP-M-19 = 34 + 29 + 2 = **65** ✓. Every property still names an owning PLAN task and a file present in PLAN §4's manifest, and PROP-M-21 lands on A4 → `mergeDecision.test.js`, a task and file that already exist — **no new task, no new file, no batch change**. Two items I am recording as watch items rather than findings, because both are correctly routed and neither is mine to decide: PROP-M-19 §3 hands **A7** the job of unifying TSPEC §7.1's standalone `AHEAD_OF_REMOTE_NOTE(…)` with §10.2's `MERGE_NOTES` catalogue (a naming unification with no behavioural content — but it should surface in A7's PLAN row when the PLAN next takes an edit), and §1.2's generator widening of F1's scope still needs the one-line entry in PLAN §4's ownership manifest that §8 already routes there. |

## Recommendation

**Approved**

Both blocking findings are closed at the level the specs require — the CI relaxation is now falsifiable by a differential that kills the exact surviving mutant, and PROP-M-18 agrees with FSPEC §2.5 on the already-merged path — and all six advisories are addressed, two of them by strengthening rather than by the minimum edit. The `D_core` re-derivation that arrived in the same revision introduces no contradiction with the approved specs in my lens, and every arithmetic claim I could check reconciles.

## Verdict

VERDICT: APPROVED
{"high": 0, "medium": 0, "low": 0}
