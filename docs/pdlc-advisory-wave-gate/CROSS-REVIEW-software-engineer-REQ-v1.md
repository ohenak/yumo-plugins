# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-advisory-wave-gate/REQ-pdlc-advisory-wave-gate.md` (v1.0, 2026-08-09)
**Date:** 2026-08-18
**Iteration:** 1
**Scope:** Technical lens — feasibility, implementability, integration risk, existing-code claim verification. Reviewed against branch `feat-pdlc-advisory-wave-gate` @ `7b2c3879` and `origin/main` @ `1efb9a3b`.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Process | **The document under review is superseded on the default branch.** `origin/main` carries this REQ at **v1.3 / 2026-08-13** (`git show origin/main:docs/pdlc-advisory-wave-gate/REQ-pdlc-advisory-wave-gate.md`, header row `| pdlc | draft | Claude | 1.3 | 2026-08-13 |`), reached through `5218deb0` (v1.1), `093a6a29` and `2ca2335a` (operator adjudications). The branch under review is at v1.0 with merge-base `b88c693b`, i.e. ~202 lines behind main for this one file. Main's v1.3 records operator answers to the very questions this draft leaves open — **Q-1 answered: `advisory.waveBudgetPerRun` = `1`, "proposal 2 superseded"**, plus Q-2…Q-5, a new `D-AWG-06`, and `O-7`. Approving this text would re-open settled decisions and ship §5 C-2 / R-3 / Q-1 with a default the operator has already rejected. Rebase `feat-pdlc-advisory-wave-gate` onto `origin/main` and re-issue the review against v1.3. | Whole document; header, §5 C-2, §7 R-3, §8 Q-1 |
| F-02 | High | Local | **An E-6 repair can never be committed, so a green gate would certify a tree state the history never contains.** AC-3.1 E-6 permits changing files in a **later** task's owned-path set. But the wave commit loop is pathspec-scoped to the *current* wave's tasks only — `for (const task of wave) { const paths = task.files … commitPaths({paths, …}) }` (`pdlc/workflows/orchestrate-dev.js:10334-10352`), plus the configured `postWavePathspecs` build outputs (`:10353-10362`). AC-4.2 explicitly keeps that path as the sole writer. So an E-6 repair passes the re-gate, is never staged, and survives only as an uncommitted edit in the shared tree until the later task overwrites or contradicts it. REQ must state the observable outcome for E-6 repair persistence (who commits it, or that the wave is escalated instead), or defer E-6 entirely. | §6 AC-3.1 (E-6) vs AC-4.2 |
| F-03 | High | Local | **AC-4.4's re-gate is under-specified against the shipped build-then-gate order, and in this very repo it re-reds by construction.** The shipped order is post-wave command first, test gate second (`orchestrate-dev.js:10307-10329`), and the comment at `:10301-10306` states why: "a wave that edits workflow sources leaves the generated artifacts stale, and the suite itself asserts their freshness … gating first would red every source-editing wave for its own unbuilt outputs." An A6 repair under E-5/E-6 edits sources *after* the build has already run. AC-4.4 says only that "the configured gate command re-runs". With this repo's own `.claude/pdlc.config.json` (`testCommand` = the jest suite, which includes `runtimeBundle.test.js`'s `bundle freshness` describe at `:537-556`; `postWaveCommand` = `node pdlc/workflows/build-runtime.mjs`), re-running the test command alone reds on stale artifacts, so A6 could never resolve a source-touching wave here. AC-4.4 must say whether "re-gate" means the test command alone or the post-wave command **then** the test command — and if the latter, AC-4.3's prohibition on touching implementation configuration needs to be read against it. | §6 AC-4.4, AC-1.2 |
| F-04 | High | Local | **The closed refusal-reason set has no member for a diagnosis-only outcome, and AC-3.4 forbids adding one.** AC-2.2 states `environmental` and `unclassified` "authorise no action"; AC-3.4 binds refusals to the tier's existing closed, ordered set and says this REQ has no licence to add a ninth. But every one of the tier's eight triggers presupposes a proposal, a budget overrun, or a malformed verdict (`docs/pdlc-advisory-tier/REQ-pdlc-advisory-tier.md:148-165`): `prohibited-action`, `revert-on-test-touch`, `out-of-envelope` ("any other out-of-envelope **proposal** or reverted diff"), `post-action-verification-failed`, `record-write-failed`, `malformed-verdict`, `low-confidence`, `budget-exhausted`. A well-formed, high-confidence `environmental` diagnosis that proposes nothing matches none of them, yet tier AC-3.6 requires every refusal to carry exactly one. Name the mapping in this REQ (an AC, not prose) or the seam has undefined behaviour on its most likely outcome (R-2 predicts these dominate). | §6 AC-2.2, AC-3.4 |
| F-05 | High | Local | **Blast-radius enumeration (BL-06) is incomplete: it covers the seam catalogue but not the envelope catalogue this REQ also extends.** BL-06 and M-WG-8 enumerate only the `ADVISORY_SEAMS` transcriptions (`orchestrate-dev.js:1669`; `advisoryEnvelope.test.js:317`). AC-3.1 additionally extends the shipped default envelope with E-5/E-6, and `ENVELOPE_DEFAULTS` (`orchestrate-dev.js:1660`) is asserted by set-equality at `advisoryEnvelope.test.js:284` and transcribed literally at `advisoryDisabled.test.js:129` and `:616`, `advisoryHarvest.test.js:196`, with partial-envelope fixtures at `advisoryConfig.test.js:274-286`. Two consequences the REQ must own: (i) BL-06's site list must include the envelope-default transcriptions; (ii) two of them are **disabled-tier** fixtures, so AC-1.4/NFR-2's "identical to the pre-A6 baseline" must be scoped explicitly to created files and phase outcomes and must state that the shipped config *defaults* do change — otherwise AC-1.4 and AC-3.1 contradict each other as written. | §6 AC-3.1, AC-1.4, NFR-2; §9 BL-06 |
| F-06 | Medium | Local | **BL-05's resolution form does not match repository state.** BL-05 gives the resolution form as "PR merged (queue row 2)", but `docs/_queue/QUEUE.md` row 2 reads `| 2 | halted | pdlc-consolidation-agent | … |` on this branch, and on `origin/main` the feature's docs have been archived to `docs/completed/pdlc-consolidation-agent/` (visible in the `origin/main` diff to `docs/_constraints/pdlc-advisory-corpus-baseline.md`, whose "Cited by" now points at `docs/completed/…`). A prerequisite whose stated gating evidence contradicts the queue is not checkable at FSPEC entry. State the observable form actually used (archive location and/or merge commit), not the row status. | §9 BL-05 |
| F-07 | Medium | Cross-Feature | **The deferral bindings may be routed to the wrong successor.** All five deferrals bind to `pdlc-engineering-loop` (queue row 6), including D-AWG-03 (Phase I POSTMORTEM lifecycle / approval skip — M-WG-5, M-WG-6, i.e. re-invocation cost). Queue row 20 `pdlc-wave-resume` was added the same day, depends on this feature, and its acceptance criteria are precisely about re-entering a wave (`docs/pdlc-wave-resume/REQ-pdlc-wave-resume.md`, REQ-WVR-01 "…wave", REQ-WVR-05 "self-clearing lifecycle", REQ-WVR-07 "unattended parity"). Reconcile which feature owns D-AWG-03 before FSPEC, or two rows will both claim the same gap. (v1.3 on main may already have moved this — see F-01.) | §10 D-AWG-03; §4 Non-Goals |
| F-08 | Medium | Local | **AC-3.1's envelope table omits the `Seam` column of the table it extends.** The shipped envelope enumeration carries four columns — `# | Permitted | Decidable rule | Seam` (`docs/pdlc-advisory-tier/REQ-pdlc-advisory-tier.md:127-132`); AC-3.1's E-5/E-6 rows carry three. An extension of an enumerated contract that will be checked by set-equality should join it at the same shape, otherwise the "closed set" spans two schemas and the completeness assertion cannot be written over one table. | §6 AC-3.1 |
| F-09 | Low | Local | **Budget scope is ambiguous between AC-2.4 and NFR-4.** AC-2.4 spends `seamBudgetMinutes` as "working time on one wave"; NFR-4 measures it per invocation, dispatch→verdict, less gate-command time. With `attemptBudget` 3 and one gate re-run per attempt these give different halt points. Say whether the minute budget is per-invocation or cumulative per-wave. | §6 AC-2.4, NFR-4 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | AC-5.1 requires reverting the repair while leaving the wave agents' *uncommitted* work intact. At that point the tree holds uncommitted output from every task in the wave and nothing distinguishes it by author. What observable does the REQ intend — "the repair's paths are restored to their pre-A6 content" (per-path, decidable) or "the tree is restored" (needs a snapshot taken before A6 acts)? O-1 defers the mechanism; the *observable* is REQ-level. |
| Q-02 | If an E-6 repair does land (see F-02) and the later wave's task then runs with its owned files already partially written, is that task expected to be idempotent over A6's edit, or is the later task's dispatch expected to be told what A6 did? Nothing in §6 covers the hand-off. |
| Q-03 | AC-1.5 says inapplicability is "named once in the run report". Is that a new report key, or a line in an existing surface? AC-1.1 already commits to the per-seam rows going from five to six; if the A6 row itself can carry `not-applicable`, AC-1.5 may cost nothing new. |
| Q-04 | AC-2.2 class 1 `plan-ordering-defect` is decided from gate output naming "a symbol, file or artifact that the PLAN itself schedules for a later task". Deciding that requires parsing the PLAN's task table from within the seam — while AC-3.3 (f) forbids A6 changing the PLAN, reading it is presumably intended. Is PLAN read access in scope for A6, and is `parsePlanTasks` the intended reader? |

## Positive Observations

- Every measured-fact citation I sampled from `docs/_constraints/pdlc-wave-gate-baseline.md` holds at HEAD: `evaluateWaveDispatch` declared `:8283` / called `:10299` (M-WG-1), the build-then-gate block `:10301-10329` (M-WG-2, M-WG-3), the pathspec-scoped commit loop `:10334-10364` (M-WG-4), `FORCE_PHASE_TOKENS` as a six-member set with no `I` (`:4585`, M-WG-6), the `status: "halted"` queue write (`:10805`, M-WG-7), and `ADVISORY_SEAMS` frozen at five with the set-equality assertion at `advisoryEnvelope.test.js:317` (M-WG-8). Holding these in a cited baseline instead of restating them in the REQ is the right call and it survived verification.
- §5 C-2's config claims match the shipped defaults exactly: `ADVISORY_DEFAULTS` = `{enabled: false, attemptBudget: 3, seamBudgetMinutes: 10, envelope: ENVELOPE_DEFAULTS}` (`orchestrate-dev.js:1660-1668`).
- NFR-6 and BL-02 are correct and well-aimed: `resolveAdvisoryRung` really is exported (`orchestrate-dev.js:1833`) and really is the one ladder (doc comment `:1800-1802`), so "reuse the resolver, do not restate the ladder" is enforceable rather than aspirational.
- AC-6.4's "honest limit" on resolution counts is accurate — `pdlc-advisory-corpus-baseline.md` §4 says exactly that escalations are durable and resolutions are observable only as absence. A REQ that names the metric it *cannot* deliver and bounds it in an obligation (O-2) is the behaviour I want to see more of.
- AC-1.3's V-wave carve-out matches the code's own reasoning: the V-wave has no manifest row and is the one dispatch that commits its own work, with the gate running "as verification rather than as permission" (`orchestrate-dev.js:10383-10397`). Excluding it is right, and for the reason stated.
- Every tier AC id cited (AC-2.2, AC-3.4, AC-3.6, AC-9.2, NFR-1, BL-01) exists in `REQ-pdlc-advisory-tier.md` and says what this REQ says it says. Cross-feature citations to nonexistent authorities have shipped here before; these are clean.

## Recommendation

**Needs revision**

The design intent is sound and the altitude is right — the split between "diagnosing is delegable, authorising is not" is the correct extension of the tier, and the gate stays the only thing that can declare a wave green. Five High findings block it.

F-01 is the cheapest to clear and should be cleared first: the branch is reviewing a v1.0 draft that the default branch has already carried to v1.3 with operator adjudications, including an answer to Q-1 that contradicts this text. Rebase onto `origin/main`, re-issue at v1.3, and F-06/F-07/F-09 may resolve in the process.

F-02, F-03 and F-04 are the substantive ones and survive any rebase unless v1.3 addressed them: an E-6 repair that no commit step owns (F-02), a re-gate whose meaning is under-specified against the shipped build-then-gate order and which re-reds by construction under this repo's own config (F-03), and a refusal-reason set with no member for the seam's most likely outcome (F-04). Each needs an acceptance criterion stating the observable outcome; none needs a mechanism, so all three stay inside REQ altitude. F-05 needs BL-06 widened to the envelope-default transcription sites and AC-1.4/NFR-2 scoped so they stop contradicting AC-3.1.

Noting the REQ's own DC-09 stopping rule: these are not oracle-deferrable implementability gaps. F-01 contests the document's currency, and F-02/F-03/F-04 contest observable outcomes the REQ commits to — all four are REQ-level content, not TSPEC entry obligations.

## Verdict

VERDICT: Needs revision
{"high": 5, "medium": 3, "low": 1}
