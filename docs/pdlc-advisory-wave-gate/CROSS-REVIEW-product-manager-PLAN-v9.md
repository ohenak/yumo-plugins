# Cross-Review: product-manager — PLAN (delta re-review, round 9)

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-advisory-wave-gate/PLAN-pdlc-advisory-wave-gate.md` (v1.8)
**Date:** 2026-08-19
**Iteration:** 9

**Scope:** Delta re-review of `dccfbfea..HEAD` (v1.7 → v1.8, seven commits `0bb9d279`…`52885fb1`, +83/−29 lines). Round-8 findings F-01…F-05 checked for closure; only the sections those commits touched were scanned for new defects; every repository claim in the changed text was re-measured on a clean tree at HEAD.

## Round-8 findings disposition

| Round-8 finding | Landed? | Evidence |
|---|---|---|
| F-01 Medium — residual arithmetic (26 measured, not 28; class 3 was 8, not 10) | **Yes, and now measured true** | The round did not change `28`; it *dated* it ("28 paths as of the 2026-08-19 measurement") and gave it a growth rule ("+1 per *committed* cross-review file"), which is the durable fix. Re-measured at HEAD by replaying the oracle's own logic (`git ls-files` → `grep -rln` over L-2's seven terms → minus A-1's 15 globs, `documentOracles.test.js:538-575`): residual is **28** — 14 `.bak` blobs, 4 `.claude/workflows/` runtime artifacts, and **10** feature documents (`TSPEC`, `PLAN`, `DECISIONS`, `PROPERTIES`, plus six `CROSS-REVIEW-*`: pm-PLAN-v7, pm-PLAN-v8, se-PROPERTIES-v1, te-PLAN-v6, te-PLAN-v7, te-PLAN-v8). My round-8 count of 26/8 plus this round's two committed cross-review files is exactly 28/10, so the growth rule the plan wrote down is the one the repository obeys. |
| F-02 Medium — Overview file count vs manifest | **Yes** | Overview now reads "**Twelve** test-side files … **all twelve of which exist at HEAD**", names `documentOracles.test.js` as the twelfth and the shared fixture `__tests__/helpers/advisoryDoubles.js` as the thirteenth path. Counted the manifest myself: twelve distinct `*.test.js` paths under `pdlc/workflows/__tests__` (advisoryWaveGate, documentOracles, advisoryEnvelope, advisoryConfig, advisoryDriver, advisoryRecord, advisoryHarvest, consolidationProperties, advisoryDisabled, advisoryQueueSeams, advisoryEscalationLog, waveExecution) plus the fixture. All thirteen exist on disk. Overview and manifest now reconcile. |
| F-03 Low — A6-21's "unconditional throw" | **Yes** | Both the Overview and the red-before-green row now anchor by halt literal: the wave-loop arm whose literal is `` Error: Wave ${waveNum} test gate failed … ``, *not* the V-wave's `` Error: V-wave ${vWaveNum} PROPERTIES test gate failed ``, and "unconditional" is withdrawn in favour of "falls straight through `if (!gate \|\| gate.ok !== true)` to `throw haltError(…)` with no repair path". Measured at `pdlc/workflows/orchestrate-dev.js`: the two literals sit at distinct sites, the wave-loop arm is `if (scriptGate) { const gate = await runCommandFn(…); if (!gate \|\| gate.ok !== true) { throw haltError(…) } }`. The plan's sentence is now a byte-for-byte description of that arm. |
| F-04 Low — `.gitignore` absent from "Where it lands" | **Yes** | "Where it lands" now names "one repo-root file: `.gitignore`", with the reason (live write target of the workflow sync path) and the note that it is a tree-wide edit rather than an advisory-tier one. |
| F-05 Low Process — completeness gate supplies PLAN headings on the cross-review invocation | **No** (not this document's to fix) | Recurs unchanged in round 9's invocation; re-filed below for harvest routing. |

Beyond the routed items, the round's own headline fix is machine-checkable and checks out. v1.8 rejoined A6-00's task row into one physical line because blank lines inside it terminated the markdown task table. Verified by running the shipped parsers over the document at HEAD: `parsePlanTasks` returns **11 tasks** (`A6-00, A6-01, A6-04, A6-05, A6-06, A6-08, A6-10, A6-12, A6-14, A6-18, A6-21`), `validatePlanContract` returns `ok: true` with no violations, and `computeWaves` returns **7 waves** — exactly what the changelog row claims. A plan that parses to one task and eleven unknown-id manifest rows would have failed Phase I at the first gate, so this was the right thing to catch and it is genuinely closed.

## Findings

One High, one Medium, two Lows. The High is new: it was introduced by this round's own fix for TE v8 F-03, and it sits inside the Definition of Done that Phase I is judged against.

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **The DoD's new set-equality leg calls the expected growth of the residual a regression, so the DoD contradicts itself and cannot be satisfied at the moment it is evaluated.** The `test:coverage` leg now ends: *"the residual it prints set-equals the enumerated inherited paths of the Overview's HEAD-drift note — class 2's four … plus class 3's `docs/pdlc-advisory-wave-gate/**` documents and this feature's `CROSS-REVIEW-*` files … **A residual containing a fifteenth class member is a regression even though the test was already red.**"* Four bullets lower, the same DoD says of those very paths: *"every committed cross-review file adds one more."* Both cannot hold. Fourteen is today's inherited count (4 runtime artifacts + 10 documents, re-measured this round); this review's own two cross-review files, once committed, make it sixteen — expected by one bullet, a "regression" by the other. Phase I ends after several more review rounds, so the leg is guaranteed to be evaluated in the state it declares failing. This is the same failure mode as PM v7 F-01 (a DoD unsatisfiable by construction), re-entering through the fix for the leniency problem, and it is worse than the leniency it replaced: a mechanical criterion that mis-fires produces a false halt at the ship boundary rather than a soft read. **Fix:** state class 2 as the closed enumeration it is (the four named `.claude/workflows/` artifacts, set-equality) and class 3 as a *membership* predicate, not an enumeration — every remaining residual path matches `docs/pdlc-advisory-wave-gate/**` and none is a `.pdlc-backups/*.bak` blob. That still fails on any genuinely new residual (a tracked `.gitignore` carrying an L-2 term, a new `.claude/workflows/` artifact) and no longer fails on a cross-review file the pipeline is designed to produce. Drop the "fifteenth class member" sentence or re-scope it to class 2. | AC-1.2 (coupled sweep); PLAN Definition of Done |
| F-02 | Medium | Local | **The DoD bullet's `28` / `14` counts stayed undated while the Overview's identical counts were dated this round.** The Overview's HEAD-drift note now says "28 paths as of the 2026-08-19 measurement" and carries the growth rule in its class-3 row; the DoD bullet three sections below still reads flatly "closes 14 of its 28 residual paths; the other 14 — …". The round fixed the rot at one of the two sites that carry the number. By the time the DoD is read, 28 and 14 will both be stale for exactly the reason the same bullet's closing clause names, and a verifier reading the DoD alone has no signal that the figure is a dated measurement rather than an invariant. **Fix:** carry the Overview's dating into the DoD bullet in one clause — "14 of the 28 residual paths measured 2026-08-19; the rest grow by one per committed cross-review file" — or replace the DoD bullet's numbers with a pointer to the note that owns them. | PLAN Definition of Done; Overview HEAD-drift note |
| F-03 | Low | Local | **The class-2 provenance evidence recipe names a command whose output contradicts the conclusion for two of the four paths.** The Overview says class 2 is branch-introduced because *"`git ls-tree` at the merge-base `1efb9a3b` returns empty for all four, and `git log --diff-filter=A` names `e3b9d5a3`"*. The conclusion is correct — I verified `git ls-tree 1efb9a3b -- .claude/workflows/` is empty, so all four are absent at the merge-base. But `git log --diff-filter=A` on either `.bundle.js` prints **two** adding commits, and the older one, `3991b4d5` (2026-07-27), is an ancestor of the merge-base: those paths were added, deleted and re-added. A reader re-running the cited command and reading its last line concludes "pre-existing" and reverses the finding. **Fix:** name the `ls-tree`-at-merge-base leg as the deciding measurement and mark `--diff-filter=A` as corroborating-with-a-caveat (the bundles carry an earlier, superseded add). | DEC-DOC-01; PLAN Overview HEAD-drift note |
| F-04 | Low | Process | **The completeness gate still supplies PLAN headings on the cross-review invocation — sixth consecutive round.** This invocation again supplies `## Overview` / `## Batches` / `## Dependencies` / `## Verification` as the accepted top-level section set. Those are the reviewed document's headings, not the cross-review's (`## Findings` / `## Questions` / `## Positive Observations` / `## Recommendation` / `## Verdict`). Flagged in v5, v6, v7, v8 and here; five routings without a landing confirm it is a wiring defect in the dispatcher, not a PLAN defect. | — |

Tagging note: F-01 stays `Local`. The defect is arithmetic inside this plan's own DoD, and the durable lesson — "a DoD criterion stated as set-equality over a set the pipeline is designed to grow will mis-fire" — is worth a LEARNINGS line at harvest, but no sibling feature is implicated and no `DOMAIN-CONSTRAINT` is restated, so promoting it now would over-tag. F-04 is `Process` for the sixth time and is the only cross-feature-shaped item here.

## Questions

| ID | Question |
|----|---------|
| Q-01 | Carried from v8, still non-blocking and now partly answered: the DoD's full-suite leg no longer re-derives a count, it names two test titles. I checked both against the source and they are verbatim transcriptions, not paraphrases (`documentOracles.test.js:75` and `:572`). Does the plan want to say so explicitly — "these titles are transcribed, so a title rename upstream fails this leg and that is intended"? Today a rename in the coupled sweep's file would fail the DoD leg for a reason a verifier could mistake for an A6 regression. |
| Q-02 | Carried from v7-v8: A6-00's untrack step is a non-file act (`git rm --cached`), while the wave commit arm iterates exactly `task.files`. `.gitignore` is now in A6-00's owned set, so the ignore half lands cleanly — but does the un-tracking of the 14 tracked paths land in that commit, given none of the `.bak` paths appear in the task's `files` list? If not, it is worth marking as a manual step the implementer performs before the wave's commit arm runs. |
| Q-03 | Carried from v3-v8: the shipped example commits `{"enabled": false, "waveBudgetPerRun": 1}` and no README row is in scope, so the feature ships with no operator-facing discoverability beyond the example file itself. Upstream's explicit decision, correctly followed here; still worth one line in the feature's LEARNINGS so a future operator-documentation pass picks it up. |

## Positive Observations

- **The round's headline fix is verifiable by running the shipped parsers, and it passes.** A6-00's row was rejoined into one physical line because blank lines inside it terminated the task table. I ran `parsePlanTasks`, `validatePlanContract` and `computeWaves` over the document at HEAD: 11 tasks, `ok: true`, 7 waves — the changelog's exact claim. This class of defect (a document that reads fine to a human and parses to nonsense) is the one worth catching before Phase I, and it was caught by measurement rather than by reading.
- **The `.gitignore` rule's form is now derived from two measured constraints, not chosen by taste.** A6-00's Edit 1 no longer says "add the path"; it says add the bare literal `.pdlc-backups/` and gives both reasons: T21's first assertion is an unscoped substring check (`expect(gitignore).not.toEqual(expect.stringContaining(".claude/workflows/"))`, verified at `documentOracles.test.js:357`), and `.gitignore` is itself tracked, so any rule carrying an L-2 term mints a fresh sweep residual. I checked both: `.gitignore` is tracked, contains no `.claude/workflows/` today, and `.pdlc-backups/` carries neither the substring nor any of L-2's seven terms. An implementer following the anchored spelling would have reddened a currently-green oracle; the plan now makes that impossible to do by accident.
- **The residual counts were dated instead of re-argued, which is the fix that survives the next round.** My round-8 finding measured 26 where the document said 28. The response was not to write 26 — it was to date 28 and state the growth rule. Two committed cross-review files later the document reads true again, for the stated reason. That is the difference between correcting a number and correcting the thing that makes numbers rot.
- **The set-equality reflex is right even though this instance mis-fires.** TE v8 F-03's objection — that "no red outside the named inherited set" is an absence-shaped condition a human cannot fail — is correct, and replacing it with a two-directional check on failing test titles is the right shape of answer. F-01 is about the class-3 side of that check being enumerated where it should be a predicate; the leg itself is a real improvement over what it replaced.

## Recommendation

**Needs revision.**

Round 8's two Mediums and two Lows all landed, and the round's own parser fix is verified green (11 tasks, 7 waves, contract ok). But the fix for TE v8 F-03 introduced a High in the Definition of Done, and a DoD defect blocks because the DoD is what Phase I is judged against.

Exactly what to change:

1. **DoD full-suite leg (F-01, High).** The residual check must not call expected growth a regression. Keep the two-directional set-equality on failing *test titles*. For the residual's contents, split the check: set-equality on class 2's four named `.claude/workflows/` artifacts, and a *membership* predicate on the rest — every remaining path matches `docs/pdlc-advisory-wave-gate/**`, and no `.pdlc-backups/*.bak` blob appears. Then remove or re-scope the sentence "A residual containing a fifteenth class member is a regression", which as written fails the DoD on the pipeline's own cross-review output.
2. **DoD residual bullet (F-02, Medium).** Date the `28` / `14` in the DoD bullet the way the Overview's note now dates them, or point at the note instead of restating the numbers.

Two Lows worth folding in the same pass: mark `ls-tree` at the merge-base as the deciding provenance measurement and note that `--diff-filter=A` prints a superseded earlier add for the two bundles (F-03); F-04 is the cross-review dispatcher's heading defect and is not this document's to fix.

## Verdict

VERDICT: Needs revision
{"high": 1, "medium": 1, "low": 2}
