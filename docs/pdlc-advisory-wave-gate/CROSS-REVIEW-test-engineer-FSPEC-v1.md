# Cross-Review: test-engineer — FSPEC

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-advisory-wave-gate/FSPEC-pdlc-advisory-wave-gate.md`
**Date:** 2026-08-18
**Iteration:** 1
**Scope:** Testing lens only — testability, edge-case completeness, oracle falsifiability, acceptance-test implementability. Not product strategy, not architecture, not code style.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **AT-07-1 is not an implementable test.** It reads "*Given* each boundary in §4. *When* an agent is prompted to violate it. *Then* the violation is refused by the workflow script." Three problems compound. (a) "Each boundary in §4" is BR-1…BR-16, and several are not agent-proposable at all — BR-13 (record-write ordering), BR-14 (halt control flow), BR-16 (which is the meta-rule this AT is trying to prove) have no "proposal" an agent could submit. (b) "When an agent is prompted to violate it" implies a live model in the workflows suite; every other AT in §6 uses the injected-transport doubles the tier already ships. (c) "with no reliance on the prompt" is a claim about what is *absent* from a prompt string — unfalsifiable as stated. A test author cannot write one line of this today. Restate as: an enumerated list of agent-proposable boundaries (BR-4/E-5, BR-4/E-6, BR-5 exclusions, BR-6 (f)–(i), BR-8), each driven by a **stub agent double returning the violating proposal directly** (no prompt involved), each asserting the shipped refusal reason and that the working tree is unchanged. That form is falsifiable and it is the form AT-03-5 already uses. | §6.7 AT-07-1, BR-16, NFR-1 |
| F-02 | High | Local | **Six §5 rows carry specified behaviour and no acceptance test.** §5 states "None of these is left to judgement at run time", but §6 falsifies only 26 of the 32 rows. Uncovered: **E-11** (`environmental`/`unclassified` **with high confidence** still authorises nothing) — this is the single rule most likely to be got wrong, because the tier's shipped gate is `in-envelope AND high-confidence` and A6 adds a third conjunct that the existing gate has no slot for; AT-02-2 tests unclassified escalation but never with high confidence, so a "confidence promotes" defect ships green. **E-28** (restoration itself fails ⇒ the wave halts) — the FSPEC's own words are "A6 must never leave a tree it can neither repair nor restore"; routing the *mechanism* to TSPEC O-1 is right, but the *observable* (halt, never proceed-to-commit) is this document's and needs an AT, otherwise the worst failure mode in the feature has no falsifying test. **E-30** (escalation-log write fails ⇒ escalation still happens, halt unchanged, never upgraded to a resolution) — a safety claim with no test; contrast E-29, which has AT-06-2. **E-32** (tier enabled but A6 never fires ⇒ six-row all-zero summary, key **present**) — the deliberate counterpart to AT-01-4's disabled-tier key-**absent** assertion; the pair is what catches the classic present-and-undefined bug, and only half of it is tested. **E-02** (no script-owned gate ⇒ degrades to legacy self-report gate as today) and **E-06** (legacy worktree path) — AT-01-5 asserts the notice cardinality but never that the degraded path itself behaves as today. Add one AT per row, or state in §5 which rows are deliberately example-only and why. | §5.1–§5.5 vs §6; E-11, E-28, E-30, E-32, E-02, E-06 |
| F-03 | High | Local | **The advisory config key set — the surface this feature actually changes — has no acceptance test with a transcribed oracle.** AT-07-2 is the only clause that names it, and it is prose, not an oracle: "*Then* each has been updated for A6 … a run in which a surface was not re-checked is a defect" has no assertable predicate — "has been updated" cannot fail. Grounded in HEAD: `ADVISORY_DEFAULTS` (`pdlc/workflows/orchestrate-dev.js:1940-1945`) carries exactly `enabled`, `attemptBudget`, `seamBudgetMinutes`, `envelope` and no `waveBudgetPerRun`; and the existing key-set assertion (`pdlc/workflows/__tests__/advisoryConfig.test.js:321`) compares parse output against a **test double** (`pdlc/workflows/__tests__/helpers/advisoryDoubles.js:245`), not against a transcribed literal — so it proves parse/default agreement, not catalogue content. Consequence: the FSPEC ships a new config key (C-2, BR-11) whose **key-set membership and shipped default value `1`** are asserted nowhere. BR-11 and AT-02-6 both say "the shipped default of `1`" in prose only. Add an AT in the shape AT-02-1/AT-03-1/AT-03-7 already use: the advisory config key set compared by set-equality against its transcribed literal including `waveBudgetPerRun`, plus the literal default `1` — expected values transcribed from this spec, never read back from the module under test. | §6.7 AT-07-2; BR-11; REQ C-2 |
| F-04 | High | Local | **AT-07-3 is absence-only on both conjuncts.** "*Then* no advisory agent was dispatched for it and no measurable wall-clock cost was added." Neither conjunct asserts anything positive on the same path, so the test passes vacuously whenever the green-wave path did not execute at all — a fixture that errors before the gate satisfies it. Additionally "no measurable wall-clock cost" is a timing assertion in a unit suite: either flaky or, if the threshold is loose enough not to flake, incapable of failing. Restate with a positive conjunct on the same path — the green wave reached its post-gate commit step and the per-task commit ran — and replace the timing half with the structural claim NFR-5 actually rests on: the advisory dispatch count for a green-gated wave is exactly `0` while the same run's dispatch count for a red-gated wave is ≥ 1, so the oracle can distinguish "not reachable" from "nothing ran". | §6.7 AT-07-3, NFR-5 |
| F-05 | High | Local | **AT-04-1's oracle is an unbounded existential negative.** "*Then* the wave is not treated as gated, and **no path exists** by which the verdict substitutes for the gate result." No test can assert "no path exists"; and "not treated as gated" is itself absence-shaped, so a run that halted for an unrelated reason satisfies the whole AT. This is the load-bearing safety property of the feature — a false green here is exactly the incident A6 is meant not to cause. Restate as three positive conjuncts on one run: given a verdict asserting the wave is fixed at highest confidence and a re-gate that returns non-zero, the terminal disposition equals `escalated`, the halt reason string equals the reason the pre-A6 pipeline emits for the same gate failure (the oracle AT-05-3 already names), and the wave's commit count is exactly `0`. Note the same unfalsifiable phrasing originates upstream in REQ AC-4.1 and is raised as an erratum. | §6.4 AT-04-1, BR-7, AC-4.1 |
| F-06 | Medium | Local | **The attempt-budget check has no place in the flow, so the per-wave dispatch count is underivable.** §3.2 step 3 is explicit that it checks only the wave budget and that "attempt and time budgets are checked inside the invocation, not here", yet step 8b and §3.3's row-7 red branch both route a consumed attempt "back to step 3". Step 9 mentions "a red re-gate with no attempt left" as an escalation cause, so the behaviour is inferable — but a test author asking "with `attemptBudget` at 1 and a red re-gate, how many A6 dispatches occur for this wave?" must reconstruct it rather than read it. No AT counts dispatches per wave: AT-04-4's given is "a red re-gate that exhausts the budget" without stating the count. Add the attempt check as its own numbered step between 8b and 3, and give E-24 an AT asserting the exact dispatch count (`attemptBudget = 1` ⇒ exactly 1 dispatch, then escalate; `= 2` ⇒ exactly 2). | §3.2 steps 3/8b/9, §3.3, BR-11, E-24 |
| F-07 | Medium | Local | **BR-6 declares "four further prohibitions, as a closed set" but nothing asserts the set.** Every other closed catalogue in this FSPEC gets a set-equality AT — classifications (AT-02-1), envelope members (AT-03-1), refusal reasons (AT-03-7). BR-6 gets AT-03-5, which enumerates nine *operations* and tests each individually: that is containment, not set-equality, so a deleted prohibition or a silently added tenth passes. Also AT-03-5's enumeration drops one member of (f): it lists "PLAN edit, manifest edit" but (f) names "the PLAN, **its task table**, or its file-ownership manifest" as three surfaces. Either add a set-equality over the prohibition ids `(f)`…`(i)` (matching how E-1…E-6 are asserted), or drop the words "closed set" from BR-6 so the doc does not promise a completeness bar the tests do not meet. | §4 BR-6, §6.3 AT-03-5 |
| F-08 | Medium | Local | **BR-5's "the exclusion set holds unchanged" has no regression oracle, and the shipped catalogue is ordered.** AT-03-2 and AT-03-3 test two exclusion clauses by example (test artifacts, guard paths), which is good, but nothing asserts the catalogue itself is unmodified — the way AT-03-7 does for refusal reasons. This matters more than the symmetry suggests: at HEAD `ADVISORY_EXCLUSIONS` is `["X-a", "X-e", "X-d", "X-b", "X-c"]` (`pdlc/workflows/orchestrate-dev.js:2311`) and its **order is load-bearing** — the comment at `:2312-2313` and `classifyEnvelope` iterate it in order to decide which refusal wins, exactly as `ADVISORY_REFUSAL_REASONS` (`:2297-2306`) does. So the oracle must be an **ordered sequence equality**, not a set equality: a reordering that silently changes which reason A6 reports would pass a set assertion. Add it as a companion to AT-03-7. | §4 BR-5, §6.3 |
| F-09 | Medium | Local | **"Observably identical" is not yet an oracle, and the discriminating case in AT-05-2 depends on which one is chosen.** AT-05-1 and AT-05-2 both rest on the tree being "observably identical" to the pre-A6 tree without naming the comparison surface. If a test author reaches for `git status --porcelain`, AT-05-2 stops discriminating: a per-path restore of the repair's paths leaves the *same set* of modified paths as a whole-tree restore whenever the post-wave command rewrote paths that were already dirty from the wave's own work — status-level output matches, contents differ, and the case the FSPEC explicitly designed to fail a per-path restore passes it. Name the oracle at content level: the map from path to content hash over tracked and untracked files, including generated outputs, equals the pre-A6 map. State it once in BR-9 so both ATs inherit it. | §6.5 AT-05-1/AT-05-2, BR-9 |
| F-10 | Medium | Local | **AT-07-5 is a human review, not an acceptance test, and both its conjuncts are negative.** Its *Who* is "a security-minded reviewer" and its *When* is "its capabilities are enumerated" — no automated suite runs that. The *Then* — "holds no credential the pipeline does not already hold and reaches no network surface Phase I does not already reach" — is two absence claims with no positive companion. Every other AT in §6 names a suite or an operator observing a run artifact. Either give it a mechanical oracle (A6's dispatch options — tool grants, transport, environment — compared by equality against the options the tier's existing seams are dispatched with, which is a positive assertion and reuses NFR-6's resolver argument) or move it out of §6 into a review obligation in §7, so §6 stays a set of runnable tests. | §6.7 AT-07-5, NFR-3 |
| F-11 | Medium | Local | **BR-12 is the feature's one genuine gap against shipped scope and its single AT has no falsifying companion.** §7.1 says so itself: "a TSPEC that treats it as already covered will ship an E-6 resolution whose repair is stranded uncommitted", and M-WG-12 is confirmed at HEAD — the wave commit loop commits only paths owned by tasks *in that wave* (`pdlc/workflows/orchestrate-dev.js:14347-14368` gate, per-task pathspec commits following). AT-04-5 asserts the positive outcome on a resolved E-6 wave, but nothing pins that the assertion **reds against today's commit scope**: if the fixture's later-task paths happen to fall inside a configured post-wave pathspec, AT-04-5 passes with no new behaviour at all. Per "does the fix itself have a falsifying test", add the companion: the same scenario with the later task's paths outside every post-wave pathspec, asserted to leave the repair uncommitted **before** the fix and committed after. | §4 BR-12, §6.4 AT-04-5, M-WG-12 |
| F-12 | Low | Local | **AT-01-3's "byte-comparable to today's" halt is a stronger oracle than the document elsewhere allows.** NFR-2 already concedes that run text "varies with timestamp" and states equality on *named artifacts* instead; AT-05-3 correctly uses "the halt reason equals the reason the pre-A6 pipeline emits". AT-01-3 should use the same wording rather than "byte-comparable", which a literal-minded implementer will either weaken silently or make flaky. | §6.1 AT-01-3 |
| F-13 | Low | Local | **E-19 has no AT, and the obvious one would be absence-only.** "An escalation with no refusal reason" tested as `reason == null` is a field-absence oracle that any unrelated malformed outcome satisfies. If it is tested, pair it with the positive: the terminal disposition is `escalated`, the root-cause class is `environmental` or `unclassified`, and the escalation-log entry carries that class. | §5.3 E-19, BR-15 |
| F-14 | Low | Local | **AT-06-1 asserts the record entry's fields by containment.** "*Then* an entry names the wave, the root-cause class, the envelope determination, the action taken or refused, and the gate-output citation" — five required fields, with no statement of whether the entry's field set is closed. Say which: if the entry shape is the tier's and A6 only adds the class, say containment is deliberate and A6's addition is what set-equality covers; otherwise a field can be dropped from the shipped record without a test noticing. | §6.6 AT-06-1, BR-13 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | BR-2 says the receiving side is total — an absent or out-of-set classification reads as `unclassified` — and E-08 says that escalates *without* consuming an attempt. E-07 says a malformed verdict escalates *consuming* one. Is a verdict whose classification field is present but of the wrong **type** (a number, an array) malformed under E-07 or out-of-set under E-08? E-09 resolves the both-apply case only when the verdict is independently malformed. A test author writing the E-08 table needs the type case pinned. |
| Q-02 | BR-11 measures `seamBudgetMinutes` "excluding time spent running the gate command", and AT-02-7 wants a companion case with a slow gate and fast working time that does **not** escalate. On a red re-gate the invocation runs the gate sequence up to three more times. Is *every* gate invocation excluded, including the re-gate passes, or only the first? The AT is only falsifiable once that is fixed. |
| Q-03 | Step 8a says an E-6 resolution tells "the later task's dispatch that the promotion already exists". If that later task sits in a wave the run never reaches — because a subsequent wave halts first — is the advisory record entry alone the operator's evidence, or is there a further observable? AT-04-5 assumes the wave's commit step completes and does not cover the halt-before-later-wave case. |
| Q-04 | E-04 requires "exactly one" A6-authored inapplicability notice per run, carried by the shipped once-per-run notices. AT-01-5's oracle scans "the whole notice surface" — is that surface the run report object, the emitted notice stream, or both? A oracle over one but not the other can count one while the operator sees two. |

## Positive Observations

- **§4's oracle discipline is unusually strong for an FSPEC.** BR-7 does not merely say "the gate re-runs" — it names the observable as an **ordered sequence**, gives three worked expected sequences (`[post-wave, test, post-wave, test]`, the truncated `[post-wave, test, post-wave]`, the no-post-wave `[test, test]`), and states in the spec itself why set equality is the wrong unit. AT-04-2 then transcribes those literals rather than deriving them. That is exactly the shape a test author can implement without a clarifying question, and I verified the ordering claim holds at HEAD: the post-wave command runs before the test gate (`pdlc/workflows/orchestrate-dev.js:14347-14368`).
- **AT-02-6 is built as a discriminating pair.** "Two oracles that a 'counts every invocation' reading would make disagree" — two escalated waves leave the budget untouched, one resolved wave exhausts it — is a test designed to fail against the plausible wrong implementation rather than to confirm the right one. Same for AT-03-4's both-halves-of-E-6 companion and AT-05-2's whole-tree-vs-per-path case.
- **AT-04-4 is explicitly the positive companion to the negative assertions.** The FSPEC names the reason in the AT itself: "a positive assertion on all three, so the negative assertions of AT-04-1 and AT-04-3 cannot be satisfied by accident." That the author reached for this unprompted is why F-04 and F-05 are narrow fixes rather than a structural rewrite of §6.
- **The closed catalogues are asserted by set-equality against transcribed literals, not against the module under test.** AT-02-1 spells the four classes out; AT-03-1 spells `E-1`…`E-6`; AT-03-7 pins the refusal catalogue at eight in shipped order. I confirmed the shipped starting points (`ADVISORY_SEAMS` five members at `pdlc/workflows/orchestrate-dev.js:1947`, `ENVELOPE_DEFAULTS` four at `:1938`, `ADVISORY_REFUSAL_REASONS` eight at `:2297`), so the transcriptions are honest and the deltas are the ones §6 claims.
- **§5 is genuinely exhaustive on the hard cases.** E-09's precedence tie-break, E-16's no-partial-application, E-20's truncated sequence as an admitted form, E-27's escalations-don't-consume-wave-budget, and E-31's honest limit on resolution counts are all cases a weaker spec would have left to run-time judgement. F-02 asks for tests, not for more rows.
- **Non-goals are bound to the tests.** §1's "restated from REQ §4 because they bound this spec's tests" and §7.2's deferral table with named owners mean a reviewer who spots a gap finds it already routed. That saved several findings here.

## Recommendation

**Needs revision**

Five High findings, and the shape of them is consistent: §4's business rules are testable, and §6 does not yet finish the job of turning three of them into oracles. The document is close. Nothing here asks for new behaviour, a new rule, or a re-architecture — every fix is a rewrite of an acceptance test into a form a suite can run and fail.

Concretely, to reach Approved:

1. **F-01** — replace AT-07-1 with an enumerated list of agent-proposable boundaries, each driven by a stub agent double returning the violating proposal, each asserting a shipped refusal reason and an unchanged tree. Drop "prompted to violate" and "with no reliance on the prompt" as oracles; they are the rationale, not the assertion.
2. **F-02** — add one AT each for E-11, E-28, E-30 and E-32, and either cover E-02/E-06 or say in §5 that they are inherited-behaviour rows with no new observable. E-11 and E-28 are the two that matter: the first is the rule an implementer is most likely to collapse into the tier's existing two-conjunct gate, the second is the feature's worst failure mode.
3. **F-03** — add a config-key-set AT with a transcribed literal including `waveBudgetPerRun` and its default `1`. Today that key's existence and default are asserted nowhere, and AT-07-2's "has been updated" cannot fail.
4. **F-04, F-05** — give AT-07-3 and AT-04-1 positive conjuncts on the same path. AT-04-4 already shows the pattern the author intended; these two just have not been brought up to it.

The Medium findings (F-06…F-11) are worth taking in the same pass because they are cheap: F-07 and F-08 are one set-equality assertion each, F-09 and F-12 are a sentence naming the comparison surface, F-06 is one numbered step. F-11 is the one Medium I would not defer — BR-12 is the feature's real gap against shipped scope, and a test that cannot red against today's behaviour will not protect it.

What is already right is worth saying plainly: BR-7's ordered-sequence oracle, AT-02-6's discriminating pair, and AT-04-4's deliberate positive companion are better than most specs reach at this phase, and they are why this review is a list of local repairs rather than a request to rethink §6.

## Verdict

VERDICT: Needs revision
{"high": 5, "medium": 6, "low": 3}
