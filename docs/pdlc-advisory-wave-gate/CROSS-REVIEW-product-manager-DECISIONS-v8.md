# Cross-Review: product-manager — DECISIONS

**Reviewer:** product-manager
**Document reviewed:** docs/pdlc-advisory-wave-gate/DECISIONS-pdlc-advisory-wave-gate.md (v1.6)
**Date:** 2026-08-19
**Iteration:** 8 (delta re-review)

## Scope

Delta re-review of the four commits landed since the bytes I reviewed at `6e80e476`: `33b18eac`
(withdraw the false split-schedule claim, fix the `PROP-CFG-02` citation), `7705d16f` (re-derive
column (2) by running the suites), `bf06364e` (correct column (3) membership, add the re-derivation
recipe and the `dist/` exclusion) and `8412569e` (metadata bump to v1.6, round-7 cross-review rows).
`git diff 6e80e476 HEAD` on the document is +92/-23, confined to the metadata header and the same
"Consequences / sizing" bullet block reviewed in v5, v6 and v7. The four decisions (DEC-A6-01 …
DEC-A6-04) are byte-identical to the round I approved on substance and are not re-litigated.

Method, per the standing rule that this block's numbers are measurements: I re-ran
`npm test -- __tests__/advisory` in `pdlc/workflows` at HEAD, attributed all 24 failures to the
document's two populations by name, read every newly enumerated prose site in context, and re-ran
the record's own re-derivation recipe over the surface the recipe names (the advisory suites **and**
`orchestrate-dev.js`). My v7 blocking finding is resolved, and resolved by measurement rather than
argument. The blocking finding this round is in material the same commits added: the parenthetical
that reconciles the "seven sites" bullet with column (2)'s new ten asserts a subset relation that
the two numbers refute.

## Prior-Round Disposition

| ID | Finding | Disposition at HEAD |
|----|---------|---------------------|
| v7 F-01 (High) | Column (2) named two oracles; five were red at HEAD | **Resolved, and past what I asked.** Column (2) is now ten sites / fourteen failures, re-derived by running the suite. `npm test -- __tests__/advisory` at HEAD is 24 failed / 386 passed / 410 total across 15 suites (7 failed, 8 passed) — the document's headline figure reproduces exactly. I attributed every failure: the fourteen column-(2) failures and the ten non-members (`ADVISORY_ROOT_CAUSES`, `A6_PROHIBITIONS`, the seven `nonNegativeInt` arms, `P-1`) partition the 24 with none left over. The round also found `advisoryHarvest.test.js:733`'s `T-08-8` row count, which no review named. |
| v7 F-02 (Medium) | Column (3)'s "seventeen" not set-equal; `advisoryDriver` sites missing | **Partially resolved.** The two `advisoryDriver` sites I named (`:31`, `:230`) are now counted and the total is restated as twenty. But the sweep still stops short of its own recipe — see F-02 below. |
| v7 F-03 (Medium) | `PROP-CFG-02` citation resolves to the wrong property in PROPERTIES | **Resolved.** The record now cites `advisoryConfig.test.js`'s `describe("PROP-CFG-02 — absent/unreadable/malformed input yields ADVISORY_DEFAULTS (T-01-1)")` explicitly, flags the id collision with PROPERTIES' `waveBudgetPerRun`-through-`nonNegativeInt` property, notes `PROP-CFG-01` collides the same way, and adds the bench-level consequence. Verified at `advisoryConfig.test.js:103` and `:127`. |
| v7 F-04 (Low) | `A-17` is not a PLAN task id | **Resolved with evidence I re-checked.** `grep -c "A-17"` over PLAN is `0`; the id survives only at `pdlc/workflows/__tests__/helpers/advisoryDoubles.js:313`, exactly as the record now says. |
| v7 Q-02 (`dist/pdlc-cli.mjs`) | Carried since v5 | **Answered.** The new "`dist/pdlc-cli.mjs` is in no column" clause is correct: `pdlc/workflows/dist/pdlc-cli.mjs:1957` carries the same `envelope: ENVELOPE_DEFAULTS, // the four-member literal above` comment as `orchestrate-dev.js:1948`, and CLAUDE.md's DEC-08 makes it generated-and-regenerated by `postWaveCommand`. |
| v7 F-05 (Low, Process) | Recurring half-re-derived enumerations | **Not actioned as a document edit** (correctly — harvest note). The pattern recurs this round in column (3); restated as F-05. |

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **The new "seven and ten count different populations" parenthetical asserts a subset relation the two numbers refute: ten cannot be a subset of seven. The seven-site bullet is itself incomplete under its own stated population — it should be twelve.** The bullet's population is stated as "*sites at the post-A6 value*, oracles and inputs alike"; column (2)'s population is stated as the subset of those "*oracles that are red at HEAD*". A subset cannot exceed its superset, so the parenthetical cannot be doing the reconciling work it claims. Measured at HEAD, the superset is twelve: column (2)'s ten sites, **all** of which already carry the post-A6 value and need no edit — `advisoryEnvelope.test.js`'s `ENVELOPE_DEFAULTS` and `ADVISORY_SEAMS` equalities, `advisoryConfig.test.js`'s `PROP-CFG-02` deep-equal and `PROP-CFG-01` key-set, `advisoryDriver.test.js`'s `PROP-GATE-06` (registry row `A6` present at `:227`), `advisoryHarvest.test.js`'s `T-08-6` (`:578`) and `T-08-8` (`:733`), `advisoryDisabled.test.js`'s `T-10-5 / PROP-DIS-05` (`:629`), `advisoryQueueSeams.test.js`'s row-list assertion (`:634`), `advisoryRecord.test.js`'s `PROP-SUM-02` `test.each` — **plus** the two green inputs the parenthetical correctly names (the `consolidationProperties` generator pick and `helpers/advisoryDoubles.js`'s `SEAMS`). All four `toHaveLength` sites read `6` today; I checked each. The five sites in column (2) but absent from the seven — `PROP-CFG-01`, `PROP-GATE-06`, `T-08-8`, `PROP-DIS-05`, `advisoryQueueSeams`'s row list — are exactly the ones an implementer reading "seven sites … need no edit" would budget an edit for, and PLAN A6-05's red step already says the opposite ("At HEAD most of this step is **verification, not editing**"). The cost is the mirror of v7 F-01: last round the record under-promised which suites go green for free, this round it under-lists which sites are already done. Fix by re-deriving the bullet to twelve from the same run that produced column (2), and either dropping the parenthetical or restating it as the true relation (column (2) = the seven-bullet's oracles; the residue is the two green inputs). | TSPEC §1.3; PLAN A6-05 |
| F-02 | Medium | Local | **Column (3)'s "twenty" is falsified by the record's own re-derivation recipe, which the round published but did not run over the whole surface it names. At least twenty-five.** The recipe says to grep the advisory suites *and* `orchestrate-dev.js` for `five` and `01…05`-style range restatements, read each in context, and keep those whose referent is `ADVISORY_SEAMS`, `ENVELOPE_DEFAULTS` or `ADVISORY_DEFAULTS`. Run as written it returns five members the enumeration omits: (a) `advisoryDriver.test.js:238` and `:280`, the two generated `it()` titles that restate "(PROP-GATE-01…05)" — the same restatement, in the same file, as the two sites the round *did* add at `:31` and `:230`, and worse than either, because after `A6` joins the registry the A6 case prints a title naming a five-member range; (b) `orchestrate-dev.js:2978` ("`ADVISORY_SEAMS` drives the row list (S-1), so five rows always appear"), `:13688` ("still carries five zero rows rather than `undefined` (T-10-5)") and `:15036` ("S-1: an enabled tier always reports its five rows") — three production-side prose sites in the very file A6-05 edits, in a file from which the enumeration counts exactly one site (`:1948`). I read all five in context and confirmed the referent; I also confirmed the correctly-excluded near-misses (`orchestrate-dev.js:5570`'s "all five are total" = record parsers, `:6320`'s "when all five are satisfied" = LEARNINGS sections). Column (3) is explicitly de-weighted by the record itself, which is why this is Medium and not High — but a number shipped one paragraph above its own recipe should survive that recipe. | TSPEC §1.3 |
| F-03 | Low | Local | **PLAN is cited as v1.3 for claims verified against PLAN v1.4.** The block says "PLAN v1.3's **Green step (A6-05 proper)**" and "a task id that does not exist in PLAN v1.3". Both claims hold at HEAD — PLAN's own A6-05 row (`PLAN:209`) carries the red-and-green-in-one-task wording, `PLAN:316` puts the whole advisory constants block in A6-05, and `A-17` matches zero times — but PLAN's revision table now runs to **1.4** (erratum round 4, which rewrote A6-05's red steps into "verification plus one genuine transcription"). Cite the version the claim was measured against, or cite the task row without a version; a v1.3 citation invites the next reader to check a superseded document. | PLAN §, revision table |
| F-04 | Low | Local | **The `advisoryQueueSeams` column-(2) member is cited by an inline comment's id, not by the test that fails.** The record names it "`advisoryQueueSeams.test.js`'s `ADVISORY_SEAMS drives the row list (S-1)`" — that string is the trailing comment on `:634`, inside `test("S-5 — buildQueueReport's advisory summary counts an A1 escalation on the A1 row only")` at `:615`, which is the name the runner prints. An implementer matching the red run's output against this column finds no `S-1` in it. Add the test id (`S-5`) beside the assertion quote. | — |
| F-05 | Low | Process | **Fourth consecutive round in which this block's headline enumeration is complete on the half most recently challenged and short on the half carried over.** v4 F-03 (envelope sites), v5 F-01 (seam sites), v6 F-01 (envelope oracles), v7 F-01 (seam oracles), and now F-01/F-02: column (2) was re-derived beautifully by running the suite, while the seven-site bullet it was reconciled against, and column (3)'s tree sweep, were not re-run from the same evidence. The generalisable rule for harvest is narrower and more testable than "re-derive": **when a round re-measures one population, it must re-measure every population its edit puts in the same sentence** — a reconciliation clause between two counts is a claim about both, and inherits the staleness of whichever one was not re-run. The recipe this round added is the right artifact; the gap is that it was published rather than executed. | — |

## Questions

| ID | Question |
|----|---------|
| Q-01 | With F-01 fixed, the seven-site bullet and column (2) become the same enumeration read two ways (twelve sites at the post-A6 value; the ten of them that are oracles are red today). Is the bullet still earning its place as a separate bullet, or would the block read better with it folded into column (2) and the two green inputs named there as the residue? That is an editorial call and not a finding — but three rounds of findings have now landed on the seam between these two counts, and a seam that keeps failing is worth removing rather than re-welding. |
| Q-02 | Column (2) closes with "a member still red at that boundary is an incomplete green step, not a false alarm", which is the corrected reading of TE v7 F-01 and reads true against PLAN's wave-gate arm. Would it be worth one clause naming the *other* direction — a member of column (2) that goes green **before** `A6-05`'s green step would mean production moved outside the task, which is drift to escalate? The record now tells an implementer what a red means; the symmetric signal is free to state and is the one the wave gate cannot catch. |

## Positive Observations

- **Column (2) was closed by running the suite, and the run reproduces exactly.** "24 failed / 386 passed across 15 suites" is what I measured at HEAD, to the test. More than that, the fourteen-in / ten-out attribution is a genuine partition — I checked every one of the 24 failures against the document's two lists and found no member unaccounted for and no member in both. The distinction the round drew to make that partition work — production lacking a symbol A6 *creates* (`ADVISORY_ROOT_CAUSES`, `A6_PROHIBITIONS`, `nonNegativeInt`, `P-1`) versus a member it *grows* — is the right axis, and it is the reason the count is now stable under re-measurement rather than under re-reading.
- **`T-08-8` was found by the run and by nothing else.** `advisoryHarvest.test.js:733` had survived four rounds of review, mine included, because every reviewer including me was reading the file around `T-08-6`. The record says so in-line ("which no review named and only the run surfaced"). Naming what the method caught that reading missed is what makes the method transferable.
- **The `A-17` retraction is unusually clean.** It states the false claim, names where the record picked the id up (`advisoryDoubles.js`'s hand-sync comment), gives the falsifying measurement (`0` matches in PLAN), corrects the substance (A6-02 is a red *step* of A6-05, not a task that lands production), and draws the operational consequence — a still-red `advisoryConfig` at A6-05's boundary is an incomplete green step, and the gate has no expected-red channel. I verified every link of that chain. A retraction that ends by inverting the operative advice is worth more than the original claim was.
- **The `PROP-CFG-02` collision note goes past the fix.** I asked for the citation to resolve; the round also flagged that a red `PROP-CFG-02` names either property at the bench. That is the version of the finding that helps someone at 2am.
- **Column (3)'s exclusions are right, including the one it removed.** `advisoryRecord.test.js:436`'s "the table stays exactly five rows regardless of the injected newline" really does count the diagnosis table's `| Field | Value |` rows — the regex on `:431` matches exactly `Seam|Confidence|Envelope|Disposition|Model`, and `:32` documents the same five as the worked example. Dropping a member while raising a total, and saying so in-line, is harder and more honest than letting the number drift upward unexamined.
- **`dist/pdlc-cli.mjs` is now handled, and handled as an instruction rather than a footnote.** "An implementer who greps the constant repo-wide will find it; the instruction is to leave it alone and let the gate rebuild it" is exactly the sentence the grep-first reader needs.

## Recommendation

**Needs revision** — one High finding (F-01).

My v7 blocking finding is fully resolved: column (2)'s ten sites, fourteen failures, the 24/386/15 headline and the in/out partition all reproduce at HEAD exactly as written, and the round found a member (`T-08-8`) that four rounds of reading had missed. The four decisions remain a faithful compression of TSPEC v1.10. What blocks is this round's own new material.

Exactly what to change:

1. **F-01 (blocking)** — the "Seven sites … need no edit" bullet and its new parenthetical. Ten cannot be a subset of seven. Re-derive the bullet from the same run that produced column (2): the population is **twelve** — column (2)'s ten oracles plus the two green inputs. Then either drop the parenthetical or restate the true relation: column (2) *is* the oracle part of that bullet, and the residue is the `consolidationProperties` generator pick and `advisoryDoubles.js`'s `SEAMS`, green because nothing compares them to production.
2. **F-02, F-03, F-04 (non-blocking)** — run the published recipe over the surface it names and raise column (3) to at least twenty-five (`advisoryDriver.test.js:238`, `:280`; `orchestrate-dev.js:2978`, `:13688`, `:15036`); cite PLAN at v1.4 or by task row; add `S-5` beside the `advisoryQueueSeams` quote.
3. **F-05** — harvest note only, no document edit required.

Nothing else in the changed block needs to move. The metadata bump, the `PROP-CFG-02` citation and collision note, the `A-17` retraction and its wave-boundary consequence, the twelve seam prose sites and four exclusions I read in context, the `dist/` clause, and the closing "column (1)'s four" all verify clean at HEAD.

## Verdict

VERDICT: Needs revision
{"high": 1, "medium": 1, "low": 3}
