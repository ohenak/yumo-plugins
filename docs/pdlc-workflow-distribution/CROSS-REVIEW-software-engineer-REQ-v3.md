# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-workflow-distribution/REQ-pdlc-workflow-distribution.md` (labelled v3.0)
**Date:** 2026-07-27
**Iteration:** 3
**Scope:** Delta re-review against `CROSS-REVIEW-software-engineer-REQ-v2.md`. The orchestrator labelled this "iteration 1"; v1 and v2 SE reviews exist on this branch, so the delta protocol was applied. Every existing-code claim in the document was re-verified in a single pass against the working tree at `f897bff` + uncommitted REQ edit.

## Prior-finding disposition (v2)

`git diff e719fe7 -- docs/pdlc-workflow-distribution/REQ-…md` is **2 lines**: the version cell `2.0 → 3.0` and the Cross-Reviews link row. The body is byte-identical to v2.

| v2 ID | Status | Evidence |
|---|---|---|
| F-01 §0 not reproducible | **Resolved** (by the tree, not by an edit) — re-measured today: A `2cfc66c1`, A′ `ae2e586f`/`b2dd0f74`, B/C `b8815b28`/`192974cd`. All five §0 rows match exactly; fact 7 is now true (`.claude/workflows/orchestrate-dev.js` `b8815b28` ≠ `pdlc/workflows/orchestrate-dev.js` `2cfc66c1`) |
| F-02 bundle toolchain absent | **Resolved externally** — `3991b4d` landed `pdlc/workflows/build-runtime.mjs`, `runtime-adapter.js`, `__tests__/runtimeBundle.test.js`, tracked `.claude/workflows/*.bundle.js`, and the `CLAUDE.md` §"Workflow scripts and the runtime build" |
| F-03 CI does not exist | **Open** — `.github/` still absent → F-07 |
| F-04 session-start freshness | **Open, unedited** → F-06 |
| F-05 false `export const meta` claim | **Open, and worse than v2 assessed** → F-02 |
| F-06 drift-state writer | **Open, unedited** → F-05 |
| F-07 AC-4.3 second read | **Open, unedited** → F-10 |
| F-08 dogfooding / builder writes node C | **Open, unedited** → F-09 |
| F-09 NFR-5 POSIX / JSON tool | **Open, unedited** → F-11 |
| F-10 A′ path inconsistency | **Open, unedited** → F-13 |
| F-11 AC-6.4 `CLAUDE.md` | Superseded — `CLAUDE.md:54-63` now *does* document the convention, but three of the other four list entries have flipped → F-14 |
| F-12 `{id}` safety / AC-1.8 axes | **Open, unedited** → F-15 |
| F-13 split review chain | Resolved — REQ, v1 and v2 reviews are all on `feat-pdlc-workflow-distribution` |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Process | **The document was version-bumped to 3.0 without being revised.** `git diff e719fe7 -- docs/pdlc-workflow-distribution/REQ-pdlc-workflow-distribution.md` returns exactly two changed lines — the `2.0 → 3.0` cell and the Cross-Reviews link row. Ten of the twelve v2 findings (F-03…F-12) are verbatim untouched, including four Highs. Two v2 Highs closed only because the *repository* changed under the document (`3991b4d` landed the bundle toolchain; the tree now reproduces §0). A version bump is a claim that findings were addressed; here it is unbacked. The optimizer loop must not treat a metadata edit as a revision round. | header table, whole document |
| F-02 | High | Local | **AC-5.1's stated mechanism is the exact opposite of what the builder does, and the value it stamps does not exist.** AC-5.1: "each canonical module declares its artifact version as `meta.version` (`export const meta` is already the mandatory first statement and only export), and `build-runtime.mjs` propagates that value into the emitted bundle's `meta` literal". Verified: (i) in `pdlc/workflows/orchestrate-dev.js`, `export const meta` is at **line 43**, preceded by `import * as fs from "fs"` (line 15), and is one of **20** `export` statements (`orchestrate-queue.js`: line 42, 11 exports, plus `import realMain from "./orchestrate-dev.js"` at line 39) — the first-statement/only-export rule constrains the *bundle*, never the module, which is the distinction §0 fact 2 draws correctly elsewhere; (ii) `build-runtime.mjs:84-85` states the opposite of "propagates" in a comment — "`meta` must be a pure literal and the first statement, so each bundle carries its own **hand-written copy rather than re-exporting the module's**" — `DEV_META` and `QUEUE_META` are string constants inside the builder; (iii) neither `meta` literal contains a `version` key. So AC-5.1 requires three unscoped changes (add `meta.version` to both modules, replace the builder's hand-written META with a propagating one *without* breaking the pure-literal/first-statement constraint the runtime enforces, and give the shell drift check a way to extract a JS object field from a 92 KB bundle). None appears in §6 In-scope or §7. Either scope those changes with ACs or drop version stamping to a manifest-side field the builder can emit as data. | AC-5.1, AC-5.3, §6, §7 |
| F-03 | High | Local | **The only artifact that is actually stale today is excluded from the managed set by construction, and nothing retires it.** §0 fact 7 — the REQ's sole in-repo demonstration of the problem — is drift on `.claude/workflows/orchestrate-dev.js` (`b8815b28` vs source `2cfc66c1`). AC-0.2 restricts the manifest to the two `*.bundle.js` rows and forbids "**no** ES module source" entries; AC-1.5 then classifies any `.claude/workflows/` file without a manifest row as `not-managed`, "never read for comparison, never overwritten, and never deleted". Verified: `.claude/workflows/orchestrate-dev.js` and `orchestrate-queue.js` exist on disk right now (untracked), sit alongside the bundles, and would be permanently `not-managed`. The feature therefore ships unable to detect, sync, or retire the file whose staleness motivates it, and a consumer keeps a stale legacy `.js` next to a fresh `.bundle.js` with no AC saying which the runtime resolves. Add a migration AC (retire/quarantine the legacy `.js` consumer copies as part of first sync) and state the resolution precedence when both names are present. | AC-0.2, AC-1.5, §0 fact 7 |
| F-04 | High | Local | **AC-0.5's repo-root resolution has a well-defined wrong answer that writes into the user's home config directory.** It resolves the consumer repo root "by walking upward to the nearest directory containing `.claude/`, falling back to `git rev-parse --show-toplevel`". `~/.claude/` always exists on any machine running Claude Code (it is where `plugins/cache/yumo-plugins/…` lives — the very path AC-0.3 depends on). So for any consumer repo that does not itself contain `.claude/` — precisely AC-3.8's "fresh consumer, no `.claude/workflows/` directory at all" case — the upward walk terminates at `$HOME`, and sync then creates `$HOME/.claude/workflows/`, `$HOME/.claude/workflows/.pdlc-sync-manifest.json` and `$HOME/.claude/workflows/.pdlc-backups/`, polluting global config. The two resolution strategies are in the wrong order and the walk needs a stop condition. Invert to git-toplevel first, bound the walk at the git root, and make "no repo root inside the git boundary" an `unknown`, not a fallback to `$HOME`. | AC-0.5, AC-3.8 |
| F-05 | High | Local | **The drift-state-file ownership contradiction (v2 F-06) is unresolved and it breaks the operator's remediation loop, not just the schema.** AC-3.3: `--check` "writes nothing except the drift state file". AC-2.6 and the §4 table: drift state file "written by hook only". Beyond the double-writer race v2 flagged, the consequence is now concrete: AC-4.1 blocks the queue on any `stale` row read from `.pdlc-drift-state.json`, and that file is only refreshed at `SessionStart`. An operator who follows AC-2.1's remediation *within the session* — run `sync-workflows.sh`, which per AC-3.1 does not write the drift state — re-runs the queue and is still `blocked` by the pre-sync snapshot, with no in-session escape. Either sync must rewrite the drift state after every copy (and §4 must say so), or AC-4.1 must be able to invalidate a snapshot it knows is superseded. Pick one and write it. | AC-3.3, AC-2.6, AC-3.1, AC-4.1, §4 |
| F-06 | High | Local | **AC-4.1's "older than the current session start" is still unimplementable and still unedited.** The queue executes in the restricted runtime — no `process`, no `fs`, no guaranteed clock; the only file seam is the injected `_readFile` (`pdlc/workflows/orchestrate-queue.js:483,493`; `runtime-adapter.js` `rtReadFile`), which yields contents, not stat metadata. AC-2.6's `generatedAtUtc` supplies one operand; nothing in the REQ produces or persists "the current session start" in any form the queue's single read can reach, and AC-1.8(iii) forbids clock dependence in the classifier. Name the session-start token — what writes it, to which file, read by which of the queue's reads — or delete the clause and rely on absence/unparseability (plus F-05's post-sync rewrite) alone. | AC-4.1, AC-2.6, AC-1.8 |
| F-07 | High | Local | **Three P0 ACs are addressed to a CI system that does not exist, and the one preventing A→A′ drift has no host.** Verified again: `.github/` is absent and there is no other CI configuration in the repo. AC-3.3 is "Who: CI"; AC-6.3 is "Who: CI on `yumo-plugins` … When CI runs, Then `build-runtime.mjs --check` fails unless the committed `dist/` bundles were rebuilt in the same commit"; AC-6.2's "byte-identical to those built from that release's sources" is checkable only by a release job, since consumers get neither sources nor builder (§6 Out of scope). A real surface does exist — `cd pdlc/workflows && npm test`, with `__tests__/runtimeBundle.test.js` already asserting bundle freshness — so either restate these ACs against it, or make standing up the CI/release workflow a scoped deliverable or a `BL-` prerequisite. As written AC-6.3 is unverifiable and AC-6.2 is unfalsifiable. | AC-3.3, AC-6.2, AC-6.3, §7 |
| F-08 | Medium | Local | **AC-6.1 creates a second tracked copy of the same generated bundle without saying which is canonical.** `.claude/workflows/orchestrate-dev.bundle.js` and `orchestrate-queue.bundle.js` are already **git-tracked** (`git ls-files`), and `build-runtime.mjs:29` writes only there (`OUT_DIR = resolve(REPO_ROOT, ".claude", "workflows")`). AC-6.1 requires emitting to `pdlc/workflows/dist/*.bundle.js` "in addition to keeping this repo's own `.claude/workflows/` consumer copies working" — two tracked byte-identical generated trees. The REQ does not say which one `build-runtime.mjs --check` compares (AC-6.3 names only `dist/`), what happens when they diverge, or why the repo needs both rather than making `.claude/workflows/` a sync target of `dist/` like every other consumer. | AC-6.1, AC-6.3 |
| F-09 | Medium | Local | **The maintainer-repo dogfooding contradiction (v2 F-08) is unresolved and, with the toolchain now landed, immediately harmful.** AC-0.3 makes this repo "an ordinary consumer of the installed plugin"; the installed plugin at `~/.claude/plugins/cache/yumo-plugins/pdlc/0.10.0/workflows/` contains **no** `dist/` and no `*.bundle.js` (verified). So on day one every managed row here is `missing` → AC-2.1 warns at every session start, AC-4.1 `blocked`s the queue, and AC-3.1 would copy from a `pluginPath` that does not exist. Once the plugin does ship bundles, sync overwrites this repo's freshly built, git-tracked bundles with whatever the last published release contained. State the exemption (maintainer repo is not a managed consumer / the builder also writes the sync manifest / sync refuses when `build-runtime.mjs` is present) — it cannot be left to FSPEC. | AC-0.3, AC-6.1, AC-1.7, AC-2.5, AC-3.1 |
| F-10 | Medium | Local | **AC-4.3 still requires a second injected read that AC-4.1 and NFR-1 forbid.** AC-4.1: the queue "performs **one** injected file read of `.claude/workflows/.pdlc-drift-state.json`"; NFR-1 rests on that count. AC-4.3 requires reading `.claude/pdlc.config.json` *before* AC-4.1 to decide whether it runs. Unedited since v2. Reconcile explicitly: allow two reads of deterministic JSON, or have the hook resolve `distribution.checkEnabled` and encode the result into the drift-state file (which also makes the flag readable at the surface that owns classification). | AC-4.1, AC-4.3, NFR-1 |
| F-11 | Medium | Cross-Feature | **NFR-5 still misdescribes the sibling scripts and still leaves the JSON tool undeclared.** Verified at HEAD: all three shipped hooks are `#!/usr/bin/env bash`, not POSIX `sh`, and each ships the same Python-interpreter discovery loop (`nudge-consolidation.sh:10-14`, `check-scope-field.sh:10-14`, `guard-harvest-before-delete.sh:11-15`). This feature must parse two JSON files and write two more (AC-1.6, AC-2.6, AC-4.3) and AC-2.4 requires distinguishing *malformed* JSON — not safely doable in POSIX `sh`, and hand-rolling it reinvents a cross-cutting mechanism the siblings already ship. Add the JSON tool to the §4 table with location, default, owner and absence behaviour (as was correctly done for `shasum`/`sha1sum`), and mandate reuse of the existing discovery pattern rather than a new one. | NFR-5, §4, AC-1.6, AC-2.6, AC-2.4 |
| F-12 | Medium | Local | **Two more capabilities rest on the undeclared shell toolchain.** AC-5.1 requires semver *ordering* ("ordered by semver comparison") and AC-3.4 requires "the newest 5 per `id` are retained and older ones pruned" — i.e. a total order over timestamped filenames plus a bounded delete. Neither `sort -V` nor a semver comparator is in the tool set NFR-5 declares, and the pruning delete is the one operation in this feature that removes consumer files, so its selection rule needs to be specified exactly (lexicographic over the compact-ISO8601 stamp is safe; anything mtime-based contradicts AC-1.3). Name the mechanism and the tie-break. | AC-5.1, AC-3.4, NFR-5 |
| F-13 | Low | Local | **§0 and §1 still give node A′ two different paths, and §1's is not today's.** §0 row A′ says `.claude/workflows/{name}.bundle.js` — verified correct today. §1's diagram, captioned as the chain that exists ("Today node A′ is untracked"), says `pdlc/workflows/dist/*.bundle.js`, which is AC-6.1's *target*, not the present state; A′ is also no longer untracked. Redraw §1 with today's path and show `dist/` as the change REQ-DIST-06 makes. | §0 table, §1 diagram, AC-6.1 |
| F-14 | Low | Local | **AC-6.4's five-document list is now stale in the other direction.** Verified: `pdlc/skills/orchestrate-dev/SKILL.md:89-94` and `pdlc/skills/orchestrate-queue/SKILL.md:174-177` already state the *bundle* convention, and `CLAUDE.md:54-63` already documents the build; only `docs/_queue/QUEUE.md:35` ("manual copy (`.claude/workflows/*.js`)") and the module header `pdlc/workflows/orchestrate-dev.js:4-5` ("Consumer runtime copy: `.claude/workflows/orchestrate-dev.js`") still state the superseded form. Re-derive the list, or restate the AC as "no document states a convention contradicting the manifest after this lands". | AC-6.4 |
| F-15 | Low | Local | **Two under-specifications in the state machine remain unedited from v2.** (i) AC-3.4 interpolates manifest `{id}` into a backup filename with no constraint that ids be filename-safe — an id containing `/` writes outside `.pdlc-backups/`; constrain `id` in AC-0.1. (ii) AC-1.8's totality axes are all conditioned on "Given a manifest row", so none can produce `not-managed`; totality over the stated axes covers six of seven states. Scope AC-1.8 to the six manifest-row states or add the enumeration axis. | AC-3.4, AC-0.1, AC-1.8 |
| F-16 | Low | Process | **The orchestrator dispatched this review as "iteration 1" though v1 and v2 SE cross-reviews are committed on this branch (`6ac59cf`, `46d988f`).** Taken at face value the delta protocol would have been skipped and ten open findings re-derived from scratch or missed. Iteration number should be derived from the highest existing `CROSS-REVIEW-{role}-{doc}-v{N}` on the branch, not passed in. | orchestration |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Was v3.0 intended to be a content revision? If the v2 findings were considered addressed elsewhere, where? Nothing in the branch history touches the REQ body after `e719fe7`. |
| Q-02 | Given `build-runtime.mjs:84-85` deliberately hand-writes each bundle's `meta` because the runtime requires a pure first-statement literal, how is `meta.version` supposed to flow from module to bundle without violating that constraint — and how does a shell script read it back out of the bundle? |
| Q-03 | When both `.claude/workflows/orchestrate-dev.js` and `.claude/workflows/orchestrate-dev.bundle.js` exist, which does the workflow runtime load? The answer decides whether F-03 is a cleanup or a correctness bug. |
| Q-04 | Is `pdlc/workflows/dist/` meant to *replace* `.claude/workflows/` as the builder's output (making this repo a real sync consumer), or to duplicate it? |
| Q-05 | Which single component owns `.pdlc-drift-state.json` — hook, `--check`, or both with a stated merge rule (F-05)? |
| Q-06 | Is there any CI or release automation on `yumo-plugins` today, on any host? AC-3.3, AC-6.2 and AC-6.3 all assume one. |

## Positive Observations

- §0's grounding table is now **fully reproducible**. I re-measured all five distinct artifacts independently and every hash matches the document: A `2cfc66c1`, A′ `ae2e586f`/`b2dd0f74`, B/C `b8815b28`/`192974cd`, with `0.9.0` and `0.10.0` still byte-identical. Fact 7's "live drift exists in this repo today" is now demonstrably true. v2's F-01 is closed on the evidence.
- BL-04's premise checks out at HEAD: `_readFile` is documented as "async (path) → string|null" (`orchestrate-queue.js:483`) and `runtime-adapter.js`'s `rtReadFile` honours that, so absence is distinguishable from empty without a new seam. The prerequisite can be discharged by citation rather than a spike.
- The AC-2.6 → AC-4.1 split (hook computes, queue performs one injected read) remains the right architecture for NFR-1, and it survives the toolchain landing unchanged — the adapter's injection points are exactly where the REQ assumed they were.
- AC-5.2's "content hash authoritative, version reporting-only" and AC-3.5's restore-is-byte-identical oracle are both still the correct shape and should be preserved verbatim through any revision.

## Recommendation

**Needs revision**

Must change before FSPEC:

1. **Actually revise the document.** v3.0 is v2 with a version bump (F-01). Ten open findings, four of them High, are untouched.
2. **F-02 / AC-5.1** — the builder's design contradicts the stated mechanism and no `meta.version` exists. Either scope the module + builder changes with ACs, or move version stamping to builder-emitted manifest data.
3. **F-03** — bring the legacy `.claude/workflows/*.js` consumer copies into a migration AC, or the feature ships blind to the exact drift §0 fact 7 points at.
4. **F-04** — fix AC-0.5's resolution order and bound the upward walk; as written a fresh consumer writes into `$HOME/.claude/`.
5. **F-05 / F-06** — name the single owner of `.pdlc-drift-state.json` and make the post-sync in-session path work; specify or delete AC-4.1's session-start freshness clause.
6. **F-07** — name a CI/release surface that exists (`npm test` in `pdlc/workflows` plus a release job) or scope standing one up.
7. **F-08–F-12** — resolve the duplicate bundle location, state the maintainer-repo exemption, reconcile AC-4.3's read with AC-4.1's "one", and declare the JSON / sort / semver tooling in §4 with reuse of the sibling scripts' interpreter-discovery pattern.
