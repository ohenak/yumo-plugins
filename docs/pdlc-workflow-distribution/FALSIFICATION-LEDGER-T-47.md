## T-47

| Property | Subject | Mutation (named before run) | Red observed | Revert verified | Task |
|---|---|---|---|---|---|
| PROP-BSL-06 | `pdlc/hooks/scripts/sync-workflows.sh` (no-write-target guard, step 1) | Re-key the no-write-target guard from the E1 evidence to the *selected* reason — replace `if [[ "${PDLC_EVIDENCE_REPO_ROOT:-}" == "holds" ]]; then` with `if [[ "${PDLC_BASELINE_REASON:-}" == "repo-root-unresolved" ]]; then` at `sync-workflows.sh:215` (the documented v2.0 defect FSPEC §2.1 guards against: keying the guard on the reported reason instead of the E1 evidence). | 10 of 35 tests failed. First failure: `PROP-BSL-06 … › E3=unset, E2=holds (json-tool-absent outranks plugin-root-unset) › \`--check\` reports the precedence-selected reason and creates nothing` — `driftHarness.expectRepoRootUnresolved: expected status 3, got 4` (every vector whose selected reason outranks `repo-root-unresolved` while E1 still holds now falls through the guard, hits later write logic, and exits 4 instead of the documented 3). | `git checkout -- pdlc/hooks/scripts/sync-workflows.sh`; `git diff --exit-code pdlc/hooks/scripts/sync-workflows.sh` → clean (exit 0). | T-47 |
| PROP-NEG-02 | `pdlc/hooks/scripts/lib/pdlc-drift.sh` (`pdlc_resolve_repo_root`, walk branch) | Remove the $HOME-rejection entirely: delete the loop's early `[[ -n "$homeNorm" && "$dir" == "$homeNorm" ]] && break` (line 549) and the final post-loop `homeNorm2`/`candidate == $HOME` rejection block (lines 564-568), so a walk that starts at (or reaches) `$HOME` and finds `.claude/` there is accepted as a resolved repo root. | 3 of 35 tests failed — all three PROP-NEG-02 "adversarial fixture 1" cases (check/sync/hook). First failure: `PROP-NEG-02 … › adversarial fixture 1 — .claude/ sits directly at $HOME (TSPEC §8.4) › check: HOME's tree is unchanged, the reason is exactly repo-root-unresolved, and the exit code is the surface's own` — `assertTreeUnchanged: unexpected mutation outside .git/ at: .claude/workflows/.pdlc-drift-state.json` (the run resolved `$HOME` itself as the repo root and wrote the drift-state file, sync-manifest, and workflow bundles directly under `$HOME`). | `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code pdlc/hooks/scripts/lib/pdlc-drift.sh pdlc/hooks/scripts/sync-workflows.sh pdlc/hooks/scripts/check-workflow-drift.sh` → clean (exit 0). | T-47 |
| (pre-existing) `rejects $HOME` — de-vacuified during collection | `pdlc_resolve_repo_root` — BOTH `$HOME` guards: the walk-loop break (`pdlc-drift.sh:551`) and the final candidate check's `$candidate == $homeNorm2` disjunct (`:567`) | T-47 reported that `buildNonGitClaudeAtHome()` set `cwd: root` while `.claude/` sat at `home`, and `makeConsumerTree` makes those SIBLINGS — so the walk never became a descendant of `$HOME` and the test passed via chain-exhaustion, not via the guard. Fixture given a `cwdAtHome` option and the test switched to `cwdAtHome: true`; then both guards removed (one alone is defence-in-depth and does not falsify). | 1 failing case — `expect(resolveResult.fields[0]).toBe("1")`: `Expected: "1"`, `Received: "0"`. Under the OLD `cwd: root` fixture the same mutation stayed green — which is the proof the test was vacuous. | Restored from backup; `git diff --exit-code pdlc/hooks/scripts/lib/pdlc-drift.sh` clean; re-run green (35/35) | T-47 (orchestrator) |

## Residuals

None — both PROP-BSL-06 and PROP-NEG-02 were falsified with a nameable, targeted mutation and the corresponding tests observed red before being reverted.
