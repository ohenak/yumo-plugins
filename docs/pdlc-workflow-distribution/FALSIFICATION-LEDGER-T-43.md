## T-43

| Property | Subject | Mutation (named before run) | Red observed | Revert verified | Task |
|---|---|---|---|---|---|
| PROP-BSL-07 | C1 (`pdlc/hooks/scripts/lib/pdlc-drift.sh`) | Insert `PDLC_ROWS_ID+=("row-1")` into the malformed-manifest branch (the `case` arm handling an unparsable manifest), so a classify record is emitted even when the baseline is unresolved. | 1 failing case: `"an unresolved baseline (malformed manifest) produces zero classify records"` — `expect(received).toEqual([])`, received a non-empty classify-record array (1 record for `row-1`). | `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean (0). | T-43 |
| PROP-CLS-04 (trace half) | C1 (`pdlc_classify_all`'s row loop) | Change the loop bound `for ((i = 0; i < n; i++))` to `n - 1`, dropping the classification of the manifest's last row. | Failing at `n=2,3,5` (all three row-count cases): the as-found classify-id set was missing the final row id (e.g. `row-3` absent from a 3-row manifest's as-found set) — `expect([...asFoundIds].sort()).toEqual([...expectedIds].sort())` mismatch. | `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean (0). | T-43 |
| PROP-MTM-03 (trace half) | C3 (`pdlc/hooks/scripts/sync-workflows.sh`, line 344) | `stale | missing) _pdlc_c3_should_sync=1 ;;` → `stale | missing | in-sync) _pdlc_c3_should_sync=1 ;;`, making an already-in-sync row eligible for copy. | The in-sync row (`row-1`) acquired a spurious `copy` trace record — `expect(row1CopyOrBackup).toEqual([])` failed, received one `copy` record for `row-1`. | `git checkout -- pdlc/hooks/scripts/sync-workflows.sh`; `git diff --exit-code` clean (0). | T-43 |
| PROP-SEAM-05 (trace-file half) | C1 (`pdlc_trace()`) | Insert an unconditional stderr dump of the trace line inside `pdlc_trace()`, so tracing being on/off changes observable stderr even though the change is meant to be trace-file-only. | `stderr` diverged between the `trace:true` and `trace:false` runs — byte-identical-stderr assertion failed (`trace:true` run's stderr contained the dumped trace lines, `trace:false` run's did not). | `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean (0). | T-43 |
| PROP-SEAM-07 | C1 (`pdlc_percent_encode`'s safe-range check) | `if ((ord >= 32 && ord <= 126)); then` → `if ((ord >= 32 && ord <= 127)); then`, letting DEL (0x7F) pass through unescaped instead of being percent-encoded. | The driver's `0x7F`-containing case round-tripped to a value outside `[0x20, 0x7e]` in the encoded text — `expect(encodedByte).toBeGreaterThanOrEqual(0x20)` / `toBeLessThanOrEqual(0x7e)` failed for that case (raw `0x7f` byte present in output instead of `%7F`). | `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean (0). | T-43 |
| PROP-SEAM-08 | C1 (`pdlc_trace()`'s sequence counter) | `_PDLC_TRACE_SEQ=$((${_PDLC_TRACE_SEQ:-0} + 1))` → `+ 2`, introducing a gap in `seq` on every trace record after the first. | Red surfaced from inside the harness's own oracle: `parseTrace()` threw on its internal gapless-seq self-check (rather than from a top-level `expect` in the test body) — valid, legitimate red: the mutation broke gaplessness and the harness's own invariant caught it before the test's explicit assertions ran. | `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean (0). | T-43 |

## Residuals

- **PROP-DET-03 — Environment-order independence.** No nameable, effective falsification mutation exists against the subjects (C1/C2/C3). Every real production entrypoint is invoked by the test harness as `runScript` → `spawnFn("bash", [script, ...argv], spawnOpts)` (`driftHarness.js`) — i.e. always `bash <script>`, never a raw `execve` of the script file itself. Empirically confirmed via a standalone experiment chain (`env -i ... A=1 B=1 C=1 bash -c 'python3 -c "...os.environ..."'` vs the same with keys inserted in reverse order): bash's own process, when it starts and imports its inherited `environ` into its internal shell-variable table, destroys/reorders the original insertion order — this happens at bash's own startup, before any script code in C1/C2/C3 runs, and before any child process is exec'd from within that bash instance. By contrast, a single-execve hop (`node -e spawnSync("env", [], {env})` or `env -i ... python3 -c '...os.environ...'`, i.e. no intermediate `bash -c` hop) *does* preserve insertion order end-to-end. Consequence: by the time any C1/C2/C3 code runs, the environment's insertion-order information has already been erased by bash's own startup import, so no mutation added to that code (e.g. a variable-order-sensitive dump or comparison) can ever be made order-sensitive — the order information genuinely no longer exists at that point. The property is therefore true by construction/upstream enforcement (bash's own environment-import behavior), not by any property of C1/C2/C3's logic, and is not falsifiable via subject mutation. The test itself (`"PROP-DET-03 — production behavior is independent of environment-variable insertion order"` in `driftOrdering.test.js`) is left in place and passing, since it still correctly asserts the true (structurally-guaranteed) invariant — it is just not a green ledger entry with a mutation/red/revert triple.
