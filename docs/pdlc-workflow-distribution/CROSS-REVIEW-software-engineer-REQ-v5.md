# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-workflow-distribution/REQ-pdlc-workflow-distribution.md` (labelled v5.0, commit `05f2524`)
**Date:** 2026-07-27
**Iteration:** 5
**Scope:** Delta re-review against `CROSS-REVIEW-software-engineer-REQ-v4.md` (`3dab335`), over
`git diff ef4d402 05f2524 -- docs/pdlc-workflow-distribution/REQ-…md` (746 diff lines). Every
existing-code claim introduced or changed by v5 — §0 facts 7, 10, 12, 13, 14, the `build-runtime.mjs`
citations, the AC-6.4 covered/exempt enumeration and all eight of its line citations, the `.gitignore`
claim in AC-6.2, and `git ls-files .claude/` — was re-verified in a single pass against a clean
working tree at HEAD.

> **Filename / iteration note.** The orchestrator dispatched this as "iteration 3", told me to read
> `…-REQ-v2.md` and to write `…-REQ-v3.md`. SE cross-reviews v1–v4 are already committed on this
> branch and the REQ is at v5.0 (`05f2524`); writing `-v3` would have overwritten a committed
> review. This is **v5**, delta'd against **v4**. This is the third consecutive round the
> iteration index has been wrong (v4 F-11, v3 F-16) — see F-10.

## Prior-finding disposition (v4)

v5 is a genuine content revision (~530 changed lines), not a version bump.

| v4 ID | Sev | Status | Evidence |
|---|---|---|---|
| F-01 AC-6.4 enumeration wrong by 6× | High | **Resolved** | §0 fact 14 + AC-6.4 rewritten as a normative covered set / normative exemption set. I re-derived and verified **every** citation: `git grep -l -E '\.claude/workflows/orchestrate-(dev\|queue)\.js'` at `3dab335` returns exactly the 20 listed paths; `docs/_queue/QUEUE.md:41-42`, `docs/design/MASTER-PLAN-engineering-loop.md:82-83`, `docs/PLAN-pdlc-integration-boundary-gates.md:134` and `:184`, `pdlc/workflows/orchestrate-dev.js:5`, `pdlc/workflows/orchestrate-queue.js:5` all state the superseded form at the cited lines; `CLAUDE.md:58-59`, `pdlc/skills/orchestrate-dev/SKILL.md:90`, `pdlc/skills/orchestrate-queue/SKILL.md:175` all state `.bundle.js` correctly at the cited lines. Residual defects are presentational (F-06) and mechanical (F-03, F-04). |
| F-02 two manifest paths | High | **Resolved** | One path everywhere. `grep -n distribution-manifest.json` over the REQ returns 20 occurrences and **every** one carries the `dist/` segment (AC-0.1, AC-0.2, AC-0.3a, AC-0.3b, AC-5.1, AC-6.1, AC-6.2(d), AC-6.2a, §4, §1 diagram, §10). §0 fact 12 now states the install transform explicitly. |
| F-03 maintainer manifest source unreachable | High | **Resolved** | AC-0.3a restated as a plugin-root substitution (`<pluginRoot> := <repoRoot>/pdlc`) so the manifest source moves with the baseline and `pluginPath` joins unchanged; new AC-0.3b specifies the pre-manifest consumer end to end and names plugin update (not sync) as the remediation, with AC-4.2 amended to match. |
| F-04 AC-6.1 self-contradiction | Medium | **Resolved** | "writes `pdlc/workflows/dist/` and nothing else… no bundle, no sync-manifest entry, no drift state". The in-builder sync is dropped from AC-0.3a, AC-1.6 and §6. §0 fact 13 verified in substance (`build-runtime.mjs:29` is a single `OUT_DIR`; the loop at `:172-186` is the only emit) — citation nit at F-07. |
| F-05 AC-2.7 writer list omitted the builder | Medium | **Resolved** | Follows from F-04; AC-2.7 now states the list is exhaustive and why. New wording introduces F-02 below. |
| F-06 AC-3.9 had no VCS story | Medium | **Resolved** | Two rules: a one-time maintainer `git rm` + gitignore (in scope, §6), and "sync never runs a VCS command — it prints a manual action". AC-3.7 caveated. `git ls-files .claude/` re-verified: all four paths tracked today. This fix is what creates F-01 and F-05 below. |
| F-07 §0 fact 7 mechanism wrong | Low | **Resolved** | Restated as committed drift; re-measured: `pdlc/workflows/orchestrate-dev.js` = `2cfc66c1…`, `.claude/workflows/orchestrate-dev.js` = `b8815b28…`, `git status --porcelain -uall` empty at HEAD. |
| F-08 §0 fact 10 miscount | Low | **Resolved** | `ls pdlc/workflows/__tests__/*.test.js \| wc -l` = **15**, plus `fixtures/` and `helpers/`. Correct. |
| F-09 three nits | Low | **Resolved** | (i) AC-2.6's Who is the shared writer routine; (ii) AC-0.5 names `repo-root-unresolved`; (iii) NFR-2's bound labelled a deliberate ~4× headroom margin. |
| F-10 `artifactVersion` noise | Low | **Resolved** | AC-5.3 labels both version lines "not a drift signal", prints the two `sha1`s as the discriminating evidence, permits dropping it from the report. |
| F-11 iteration index | Low | **Not resolved** (correctly routed, not landed) | §10 routes it to `pdlc/skills/orchestrate-dev/SKILL.md`; nothing has landed there. See F-10 below. |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **v5 removes every way this repo can produce its own runtime artifacts, and the documented remediation command is unrunnable in the repo that develops the feature.** Three v5 changes compose into a bootstrap hole: (i) AC-3.9 rule 1 `git rm`s and gitignores `.claude/workflows/` — verified `git ls-files .claude/` returns `orchestrate-{dev,queue}.js` **and** `orchestrate-{dev,queue}.bundle.js` today, i.e. the four artifacts the workflow runtime actually loads; (ii) AC-6.1 forbids `build-runtime.mjs` from writing anything under `.claude/workflows/` (v4's builder-writes-consumer-copy path is deleted); (iii) the REQ-DIST-03 **Delivery vehicle** paragraph pins the sole remaining writer to `${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync-workflows.sh`, while v5's AC-0.4 states that when AC-0.3a's maintainer marker is present "`${CLAUDE_PLUGIN_ROOT}` is not consulted at all and its being unset is not an error". Consequences: a fresh clone of `yumo-plugins` on a machine with no installed pdlc plugin has no `.claude/workflows/*.bundle.js` and no reachable sync script, so `/pdlc:orchestrate-dev` and `/pdlc:orchestrate-queue` cannot run in the repo that develops them; and AC-2.1/AC-2.5/AC-4.2 each require the warning to name "the exact remediation command", which under AC-0.4 has no defined expansion in the maintainer repo. AC-0.3a's advertised green path is "build → sync", but no AC says which `sync-workflows.sh` the maintainer runs. Fix: state that `<pluginRoot>/hooks/scripts/sync-workflows.sh` is the invocation path — the same substitution AC-0.3a already defines, so the maintainer runs `pdlc/hooks/scripts/sync-workflows.sh` — and reconcile the Delivery-vehicle paragraph and AC-0.4 with it. This is v1 F-05's bootstrap paradox reappearing one layer down. | AC-3.9, AC-6.1, AC-0.4, REQ-DIST-03 preamble, AC-2.1/2.5/4.2 |
| F-02 | Medium | Local | **"`sync-workflows.sh` — the only writer of `.claude/workflows/`" is false, and the sentence immediately following it in AC-6.1 says so.** AC-1.6's new Who reads "the only writer of `.claude/workflows/`, in every repo including the maintainer's"; AC-6.1 restates it and then, in the same sentence, adds "exactly two writers of the drift state file (AC-2.7)" — and that file is `.claude/workflows/.pdlc-drift-state.json`. The SessionStart hook writes it (AC-2.6, AC-2.7, §4), and sync also writes `.claude/workflows/.pdlc-backups/` (AC-3.4). So there are at least two processes writing into that directory, which also makes AC-2.7's forward rule ("any future change that gives a **third** process write access to `.claude/workflows/`") count wrongly from the start. This is not pedantry: the natural implementer reading is "the hook must not write under `.claude/workflows/`", which breaks AC-2.6, and a test asserting it would be red on the specified design. Restate as "the only writer of **managed artifacts** into `.claude/workflows/`" in AC-1.6 and AC-6.1, and scope AC-2.7's forward rule to the drift state file. | AC-1.6, AC-6.1, AC-2.6, AC-2.7, §4 |
| F-03 | Medium | Local | **AC-6.4 mandates a grep-asserted test whose second pattern is not mechanically expressible.** The AC requires "a test asserts it by grepping for the superseded form (`.claude/workflows/orchestrate-{dev,queue}.js`, and the phrase 'managed manually' **in a distribution context**) over that set", and separately requires a two-directional RED/GREEN falsifying case. "In a distribution context" cannot be written as a pattern; an implementer must either drop it (changing what the test matches) or invent a heuristic, which is precisely the "narrow the pattern until it passes" failure the AC's own falsifying-case paragraph exists to prevent. Measured at HEAD, `git grep -n "managed manually"` returns five files: `docs/_queue/QUEUE.md:42`, `docs/design/MASTER-PLAN-engineering-loop.md:83`, `docs/orchestrate-dev-workflow/TSPEC-orchestrate-dev-workflow.md:30`, and two of this feature's own documents — the TSPEC and the feature docs are already excluded **by the exemption set**, so the qualifier separates nothing that the exemption set does not already separate. Delete the qualifier and pin the two literal patterns (`.claude/workflows/orchestrate-dev.js`, `.claude/workflows/orchestrate-queue.js`, `.claude/workflows/*.js`, `managed manually`), or the P1 AC has no implementable oracle. | AC-6.4 |
| F-04 | Medium | Local | **The new tracked generated tree `pdlc/workflows/dist/**` is in neither AC-6.4 set, while the exemption set declares itself exhaustive.** AC-6.4's covered set names "`pdlc/workflows/*.js` header comments" — which does not match `pdlc/workflows/dist/*.bundle.js` or `dist/distribution-manifest.json`. The exemption table is prefaced "normative and **exhaustive**; nothing else is exempt" and lists `.claude/workflows/**` but not `dist/`. AC-6.1 creates `dist/` as a *tracked, committed* generated tree in the same revision, and the bundles inline the module headers AC-6.4 is correcting — so on day one `dist/*.bundle.js` will contain the superseded string until a rebuild, and the mandated RED/GREEN test has no defined expectation for that path (covered ⇒ RED by the exhaustiveness clause; not-in-covered-set ⇒ GREEN by the AC's Then). Add `pdlc/workflows/dist/**` to the exemption set for the same reason `.claude/workflows/**` is there (generated, not documentation). | AC-6.4, AC-6.1 |
| F-05 | Medium | Local | **AC-3.9's untracking of `.claude/workflows/` turns every git worktree into a permanently-warning, queue-blocking consumer, and the REQ never mentions worktrees.** `pdlc/workflows/orchestrate-dev.js:1869` dispatches Phase-I implementation agents with `{ isolation: "worktree" }` and `:1882` merges `feat-{feature}-{task}-worktree` branches back — linked worktrees are a first-class part of this pipeline. AC-0.5 resolves the consumer repo root with `git rev-parse --show-toplevel`, which in a linked worktree returns the *worktree* path. After AC-3.9's landing step `.claude/workflows/` is untracked and gitignored, so it does not exist in a fresh worktree: every managed row is `missing` (AC-3.8), AC-2.1 warns on every implementation-agent session, AC-3.3 exits `1`, and AC-4.1 blocks anywhere the queue is evaluated there. Nothing in §0, AC-0.5 or §6 acknowledges this. State whether a linked worktree is a distinct consumer (and if so, who syncs it) or whether root resolution should prefer the main worktree (`git rev-parse --path-format=absolute --git-common-dir`, then its parent). | AC-0.5, AC-3.9, AC-2.1, AC-3.3, AC-4.1 |
| F-06 | Low | Local | **§0 fact 14 presents one grep as producing a table that requires two, and the table classifies 25 files while claiming to classify 20.** Verified: `git grep -l -E '\.claude/workflows/orchestrate-(dev\|queue)\.js'` at `3dab335` returns exactly the 20 paths described — but `docs/_queue/QUEUE.md`, `docs/design/MASTER-PLAN-engineering-loop.md`, `CLAUDE.md`, `pdlc/skills/orchestrate-dev/SKILL.md` and `pdlc/skills/orchestrate-queue/SKILL.md` are **not** among them (they write `.claude/workflows/*.js` / `*.bundle.js`, not the per-file names), yet all five appear in the table's first two rows. The classification's *substance* is correct — I verified all eight line citations individually — but a reader told "the list is re-derivable by the grep the test itself runs" who runs the grep printed in fact 14 will miss `QUEUE.md` and `MASTER-PLAN`, i.e. two of the five files §6 puts in scope. Print both patterns in fact 14, or label the table as classifying the union of the AC-6.4 patterns rather than the output of the one shown. | §0 fact 14, AC-6.4, §6 |
| F-07 | Low | Local | **§0 fact 13's write citation is off by eleven lines.** `build-runtime.mjs:29` is indeed the single `OUT_DIR` ✓, but `:170` is `mkdirSync(OUT_DIR, …)` and `:173` is a `resolve()`; the only content write is `writeFileSync(path, contents, "utf8")` at **`:184`**, inside the `for (const { file, contents } of bundles)` loop at `:172-186`. The fact's conclusion (one output directory, one write site) is correct; only the line range is wrong. Cite `:29`, `:170` and `:184`. | §0 fact 13, AC-6.1, §6 |
| F-08 | Low | Local | **AC-6.2's packaged-set parenthetical misstates `.gitignore`.** It says "`.gitignore` ignores only `node_modules/`". The repo root `.gitignore` ignores `.tokensave/`, `.claude/settings.local.json`, `.claude/.headroom_wrap_marker.json` **and** `node_modules/`; `pdlc/workflows/.gitignore` adds `node_modules/`. None of the extra rules touch the `pdlc/` subtree, so AC-6.2's conclusion (build inputs are in the packaged set — verified: `git ls-files pdlc/workflows/` returns `build-runtime.mjs`, `runtime-adapter.js`, `package.json`, `package-lock.json`, `__tests__/`) holds. But AC-3.9's own landing step adds a fifth root rule in this same feature, so a test written against the literal claim will drift. Say "no ignore rule under `pdlc/` other than `node_modules/`". | AC-6.2, AC-3.9 |
| F-09 | Low | Local | **AC-6.2a's "exactly the bundles" is now falsified by v5's manifest consolidation.** AC-6.2a: "When `${CLAUDE_PLUGIN_ROOT}/workflows/dist/` is listed, Then it contains **exactly** the bundles named in the distribution manifest." AC-0.1/AC-5.1/AC-6.1 place `distribution-manifest.json` in that same directory, so a literal listing always contains one more entry than the assertion allows. AC-6.2a was unchanged in v5 but its premise moved. Restate as "exactly the bundles named in the manifest, plus the manifest itself". | AC-6.2a, AC-0.1, AC-6.1 |
| F-10 | Low | Process | **The review iteration index is wrong for the third consecutive round, and the routing v5 chose has not landed.** This review was dispatched as "iteration 3", instructed to read `CROSS-REVIEW-software-engineer-REQ-v2.md` and write `-v3.md`, while v1–v4 are committed on this branch and the REQ is at v5.0 (`05f2524`). Following the dispatch literally would have overwritten two committed reviews and delta'd against a three-revision-stale baseline. §10 correctly routes the fix to `pdlc/skills/orchestrate-dev/SKILL.md`'s review-loop dispatch (index := highest `CROSS-REVIEW-{role}-{doc}-v{N}` on the branch), but nothing has landed there — `git log` shows no change to that SKILL between `ef4d402` and HEAD. Until it lands, the delta protocol is protected only by each reviewer noticing. Land the derivation in the SKILL (or in `pdlc/workflows/orchestrate-dev.js`'s `reviewLoop`) rather than restating it in a feature REQ. | §10, orchestration |

## Questions

| ID | Question |
|---|---|
| Q-01 | Which `sync-workflows.sh` does a maintainer with no pdlc plugin installed run, and what does the hook print as "the exact remediation command" when `${CLAUDE_PLUGIN_ROOT}` is unset under AC-0.3a's marker (F-01)? |
| Q-02 | Is a linked git worktree a distinct consumer for this feature, or should AC-0.5 resolve the main worktree (F-05)? Phase I creates one worktree per implementation task. |
| Q-03 | Is `pdlc/workflows/dist/**` covered by AC-6.4, exempt, or neither (F-04)? The exemption set claims exhaustiveness, so "neither" is not currently a legal answer. |
| Q-04 | What literal pattern does "the phrase 'managed manually' **in a distribution context**" compile to (F-03)? |

## Positive Observations

- **The two Highs that mattered are genuinely closed, and closed the right way.** The manifest path is now one string with the `dist/` segment, and I verified all 20 occurrences in the document agree — the AC-0.1 paragraph declaring every other occurrence a restatement ("Any AC, table row or fixture that writes it without the `dist/` segment is wrong") is exactly the construct that keeps it that way. AC-0.3a's re-expression as a **single variable binding** (`<pluginRoot> := <repoRoot>/pdlc`) rather than a baseline-directory swap eliminates the double-nesting class of bug outright, and recording *why* the v4 phrasing failed inside the AC means it cannot be re-derived.
- **AC-0.3b is the best-shaped AC in the document.** It takes the least convenient fact in the feature — no cached plugin version ships a manifest, so at rollout *every* consumer is in the degraded state — names the exact behaviour of all five surfaces, names the only real remediation (plugin update, not sync), names the interim escape, and instructs the suite to test it as a first-class path rather than an error case. That is how a rollout state should be specified.
- **AC-6.4's rewrite is the correct structural answer to a defect that recurred three times**: replacing a hand-maintained file list with a normative covered set, a normative exemption set, and a mandated two-directional falsifying case. I re-derived the enumeration independently and every one of the eight file:line citations is accurate — the first time this list has been right. The archived-spec exemption ("rewriting them falsifies history") is the right rule and is stated as a rule, not a carve-out.
- **§0's discipline improved markedly.** Facts 7, 10, 12 and 13 are each individually reproducible against a clean tree, and fact 12's extension ("the `pdlc/` segment is dropped, the `dist/` segment is **not**") is the sentence that makes the whole path story checkable rather than assertable.
- **AC-2.8 + AC-2.2's exhaustiveness clause** ("The warning ACs below are exhaustive over the conditions that break silence… There is no silent non-silent state") closes the silent-`else` hole by construction rather than by hoping a test catches it. Preserve that phrasing verbatim.

## Recommendation

**Needs revision**

Must change before FSPEC:

1. **F-01 — the bootstrap hole is the blocking one.** v5 simultaneously untracks `.claude/workflows/`, forbids the builder from writing it, and pins the only writer to a path AC-0.4 says is not consulted in this repo. Name `<pluginRoot>/hooks/scripts/sync-workflows.sh` as the invocation path and reconcile the REQ-DIST-03 delivery-vehicle paragraph with AC-0.3a/AC-0.4, or the repo that develops the pipeline cannot run it.
2. **F-02** — restate AC-1.6/AC-6.1 as "the only writer of *managed artifacts* into `.claude/workflows/`"; as written the claim contradicts AC-2.6/AC-2.7/§4 in the adjacent sentence.
3. **F-03 / F-04** — give AC-6.4 an implementable oracle: literal patterns with no prose qualifier, and a defined disposition for `pdlc/workflows/dist/**`.
4. **F-05** — state the worktree story for AC-0.5, or Phase I's per-task worktrees each become a permanently-warning consumer.
5. **F-06–F-09** — correct §0 fact 14's grep framing, fact 13's line citation, AC-6.2's `.gitignore` claim, and AC-6.2a's "exactly the bundles".
