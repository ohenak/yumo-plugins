## T-46

| Property | Subject | Mutation (named before run) | Red observed | Revert verified | Task |
|---|---|---|---|---|---|
| PROP-NEG-03 (converse half, all 6 generated compositions) | `pdlc/hooks/scripts/sync-workflows.sh` | `sync-workflows.sh:355-359`: remove the `continue` after a failed `pdlc_backup` call in the copy loop, so a failed/unverified backup no longer blocks the destroying operation — replace `if ! pdlc_backup "$_pdlc_c3_consumer_abs" "$_pdlc_c3_row_id"; then continue; fi` with `pdlc_backup "$_pdlc_c3_consumer_abs" "$_pdlc_c3_row_id" \|\| true` (still nested inside the existing `state != missing && -e` guard) | 6 failed, 14 skipped, 0 passed (`npm test -- __tests__/driftWriteFailure.test.js -t "converse half"`). First failure: `PROP-NEG-03 (converse half) — a failed/unverified backup destroys nothing, exit 4 › case 0 (rowState=stale, entrypoint=sync, fault=backup) — byte-identical target, reported skipped, exactly one writeFailures entry, exit 4` — `expect(postRunBytes.equals(preRunBytes)).toBe(true)` → `Expected: true, Received: false` (`__tests__/driftWriteFailure.test.js:668`). Cases 1-5 failed identically | Reverted via `git checkout -- pdlc/hooks/scripts/sync-workflows.sh`; `git diff --exit-code` clean; `npm test -- __tests__/driftWriteFailure.test.js` → 20/20 passed | T-46 |
| PROP-NEG-03's missing-row exception | `pdlc/hooks/scripts/sync-workflows.sh` | `sync-workflows.sh:355-359`: drop the `"$_pdlc_c3_state" != "missing" && -e "$_pdlc_c3_consumer_abs"` guard entirely, so `pdlc_backup` is attempted unconditionally for every syncing row, including a `missing` row that has nothing to back up — replace the guarded block with a bare `if ! pdlc_backup "$_pdlc_c3_consumer_abs" "$_pdlc_c3_row_id"; then continue; fi` | 2 failed, 18 passed (`npm test -- __tests__/driftWriteFailure.test.js`, full file — the mutation is broad enough to also collaterally red AT-17, recorded for completeness though not this property's own oracle). This property's own failure: `PROP-NEG-03's missing-row exception — no backup record for a row that was missing › a missing row is copied with no backup record at all, and lands in-sync` — `expect(run.status).toBe(0)` → `Expected: 0, Received: 4` (`__tests__/driftWriteFailure.test.js:703`) — `pdlc_backup` attempted a copy of the (absent) consumer path, wrote a `backup` trace record via `pdlc_trace "run" "backup" …` before its own existence check, then failed and recorded a `writeFailures` entry, so both the "no backup record" and the exit-0 happy-path conjuncts went red together | Reverted via `git checkout -- pdlc/hooks/scripts/sync-workflows.sh`; `git diff --exit-code` clean; `npm test -- __tests__/driftWriteFailure.test.js` → 20/20 passed | T-46 |

## Residuals

| Property | Reason no mutation is nameable | Task |
|---|---|---|
| *(none)* | | |
