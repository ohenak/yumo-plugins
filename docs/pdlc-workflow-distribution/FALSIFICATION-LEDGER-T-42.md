## T-42

| Property | Subject | Mutation (named before run) | Red observed | Revert verified | Task |
|---|---|---|---|---|---|
| PROP-BSL-01, PROP-BSL-02 | C1 (`pdlc-drift.sh`) | In `pdlc_resolve_baseline`'s resolved-fallthrough branch, change `PDLC_BASELINE_REASON=""` to `PDLC_BASELINE_REASON="unexpected-reason"`. | 1 failing case — `vector E1=does-not-hold E2=does-not-hold E3=ok E4=does-not-hold E5=does-not-hold E6=does-not-hold -> resolved (git-routed)`: `expect(received).toBeNull() // Received: "unexpected-reason"`. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-BSL-01"` green (22 passed, 37 skipped, 0 failed). | T-42 |
| PROP-BSL-03 | C1 (`pdlc-drift.sh`) | In `pdlc_resolve_baseline`, swap the rank of the `json-tool-absent` and `manifest-absent` check blocks (`manifest-absent` now ranks above `manifest-malformed`/`json-tool-absent`, instead of below both). | 1 failing case (of several affected) — `vector E1=holds E2=holds E3=ok E4=holds E5=indeterminate E6=indeterminate -> json-tool-absent (walk-routed)`: `expect(received).toBe(expected) // Expected: "json-tool-absent" // Received: "manifest-absent"`. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-BSL-01"` green (22 passed, 37 skipped, 0 failed). | T-42 |
| PROP-BSL-04 | C1 (`pdlc-drift.sh`) | In `pdlc_resolve_baseline`, broaden the `manifest-malformed` guard from `[[ "$PDLC_EVIDENCE_MANIFEST" == "manifest-malformed" ]]` to also match the "not yet computed" sentinel: `|| "$PDLC_EVIDENCE_MANIFEST" == "indeterminate"`. | 4 failing cases — first: `vector E1=holds E2=does-not-hold E3=unset E4=indeterminate E5=indeterminate E6=indeterminate -> repo-root-unresolved (walk-routed)`: `expect(received).toBe(expected) // Expected: "repo-root-unresolved" // Received: "manifest-malformed"`. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-BSL-01"` green (22 passed, 37 skipped, 0 failed). | T-42 |
| PROP-BSL-05 (record half) | C1 (`pdlc-drift.sh`) | In `pdlc_emit_printf_record`'s unresolved-record serialiser (line 1517), change the hardcoded `"retiredPresent":[],...,"rows":[]` literals to `"retiredPresent":["mutated"],...,"rows":["mutated"]`. | 4 failing cases (all `json-tool-absent` vectors, the only baseline reason routed through this function) — first: `vector E1=does-not-hold E2=holds E3=unset E4=indeterminate E5=indeterminate E6=indeterminate -> json-tool-absent (git-routed)`: `expect(state.rows).toEqual([]) // - Array [] // + Array ["mutated"]`. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-BSL-01"` green (22 passed, 37 skipped, 0 failed). | T-42 |
| PROP-BSL-08 | C1 (`pdlc-drift.sh`) | In `pdlc_resolve_check_enabled`'s `status == 0` case-statement, change the `false) PDLC_CHECK_ENABLED="false" ;;` arm to `false) PDLC_CHECK_ENABLED="true" ;;` (an explicit `checkEnabled:false` is silently coerced to `true`). | 2 failing cases — `explicit-false config on the resolved vector`: `expect(received).toBe(expected) // Expected: false // Received: true`; same message for the manifest-absent vector. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-BSL-08"` green (10 passed, 49 skipped, 0 failed). | T-42 |
| PROP-DET-06 | C2 (`check-workflow-drift.sh`) | Change the hook entrypoint's hardcoded record literal from `"generatedBy":"hook"` to `"generatedBy":"check"` (line 241), collapsing the two entrypoints' `generatedBy` values. | 1 failing case — `identical rows and baselineReason across --check and hook, differing only in generatedBy/generatedAtUtc`: `expect(afterHook.generatedBy).toBe("hook") // Expected: "hook" // Received: "check"`. | Reverted via `git checkout -- pdlc/hooks/scripts/check-workflow-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-DET-06"` green (1 passed, 58 skipped, 0 failed). | T-42 |
| PROP-MTM-02 (`--check` half) | C3 (`sync-workflows.sh`) | Remove the `&& ((! _pdlc_c3_check))` guard from the steps-4-9 gate (line 330), so the post-copy/post-run reclassify passes also run under `--check`. | 1 failing case — `--check over a generated tree yields exactly one classify pass, labelled as-found (O-20(b))`: `assertPhaseOrder: the collapsed classify-phase sequence [as-found, post-run] is not a prefix of [as-found, post-copy, post-run]`. | Reverted via `git checkout -- pdlc/hooks/scripts/sync-workflows.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-MTM-02"` green (1 passed, 58 skipped, 0 failed). | T-42 |
| PROP-RSN-05 (baseline half) | C1 (`pdlc-drift.sh`) | In `pdlc_resolve_baseline`'s `manifest-absent` branch, change `PDLC_BASELINE_REASON="manifest-absent"` to the row-reason literal `PDLC_BASELINE_REASON="hash-tool-absent"`, colliding the baseline-reason and row-reason vocabularies. | 3 failing cases — first: `vector E1=holds E2=does-not-hold E3=ok E4=holds E5=indeterminate E6=indeterminate -> manifest-absent (walk-routed)`: `expect(received).toBe(expected) // Expected: "manifest-absent" // Received: "hash-tool-absent"`. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-BSL-01"` green (22 passed, 37 skipped, 0 failed). | T-42 |
| PROP-NEG-07 (M10 half) | C1 (`pdlc-drift.sh`) | In the manifest-load python heredoc's M10 validator, remove the no-nesting clause `if remainder == "" or "/" in remainder: fail("M10")` (leaving only the prefix and `.pdlc-`-basename checks). | 1 failing case — `--check rejects a manifest nested one level under .claude/workflows/ (M10): ...`: `expect(state.baselineStatus).toBe("unresolved") // Expected: "unresolved" // Received: "resolved"`. | Reverted via `git checkout -- pdlc/hooks/scripts/lib/pdlc-drift.sh`; `git diff --exit-code` clean; re-run of `-t "PROP-NEG-07"` green (3 passed, 56 skipped, 0 failed). | T-42 |
