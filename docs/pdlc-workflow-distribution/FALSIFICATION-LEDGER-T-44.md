## T-44

| Property | Subject | Mutation (named before run) | Red observed | Revert verified |
|---|---|---|---|---|
| PROP-MTM-01 | C3 (`sync-workflows.sh` L53) | `_pdlc_c3_generated_by="sync"` → `"check"` | 1 failed: `Expected: "sync" / Received: "check"` (driftSync.test.js:723) | Yes; `git diff --exit-code` clean; targeted test green |
| PROP-MTM-03 | C3 (`sync-workflows.sh` L345-346) | removed the `((_pdlc_c3_force)) &&` gate on `local-edit \| unverified` so plain sync always overwrites | 1 failed ("plain sync declines..."): `expect(...).toBe(true)` → `Received: false` (row-1 bytes overwritten without `--force`) | Yes; diff clean; PROP-MTM-03 green (2/2) |
| PROP-MTM-04 conjunct 1 (agreement) | C3 (`sync-workflows.sh` L419) | `_PDLC_C3_RETIRED_STATE+=("$_pdlc_c3_pc_state")` → `+=("$_pdlc_c3_row_id")` (corrupt the recorded value) | 1 failed (R "local-edit"): `Expected: "local-edit" / Received: "row-1"` | Yes; diff clean; conjunct 1 green (5/5) |
| PROP-MTM-04 conjunct 2 (post-copy narrow) | C3 (`sync-workflows.sh` L376) | `if ((_pdlc_c3_has_retires)); then` → `if true; then` (classify non-retiring rows post-copy too) | 1 failed: `assertPostCopyNarrow` discriminated correctly, breaking the `.not.toThrow()` expectation | Yes; diff clean; conjunct 2 green |
| PROP-MTM-04 conjunct 3 (predicted disagreement) | C1 (`pdlc-drift.sh` L1649) | `if pdlc_fault_active "artifact-copy-corrupt" "$rowId"; then` → `if false && ...; then` (disable the corrupt-copy fault) | 2 failed (both sub-cases): post-copy no longer classifies "local-edit" as predicted | Yes; diff clean; both sub-cases green |
| PROP-MTM-05 (sync half) | C3 (`sync-workflows.sh` L344) | `stale \| missing) _pdlc_c3_should_sync=1 ;;` → `missing) _pdlc_c3_should_sync=1 ;;` (stop syncing stale rows) | 1 failed: `Expected: "in-sync" / Received: "stale"` | Yes; diff clean; test green |
| PROP-MTM-06 | C3 (`sync-workflows.sh` L227) | `pdlc_classify_all "as-found"` → `pdlc_classify_all "post-run"` (mislabel the first classify pass) | 1 failed (sync sub-case): `assertRecordedPassIs("as-found")` no longer throws as required | Yes; diff clean; both sub-cases green |
| PROP-MTM-07 | C3 (`sync-workflows.sh` L344) | added `in-sync` to the sync-trigger case, forcing every row to resync every run | 1 failed: `Expected: true / Received: false` (byte-for-byte no-op broken) | Yes; diff clean; test green |
| PROP-NEG-03 (forward, stale-row ordering + byte-fidelity) | C3 (`sync-workflows.sh` L354-361) | swapped the backup/copy call order (copy before backup) | 1 failed: backup index no longer precedes copy index | Yes; diff clean; test green |
| PROP-NEG-03 (forward, retirement ordering) | C1 (`pdlc-drift.sh` `pdlc_retire`) | reordered `pdlc_retire` to delete-then-backup instead of backup-then-delete | 1 failed: backup index no longer precedes delete index | Yes; diff clean; test green |
| PROP-NEG-03 (forward, missing-row-no-backup) | C3 (`sync-workflows.sh` L354) + C1 (`pdlc-drift.sh` `pdlc_backup`) | removed the `missing`/`-e` guard around the backup call, and made `pdlc_backup` fabricate a phantom backup file (with a bypassed verify step) when the source is absent | 1 failed: `Expected: 0 / Received: 1` backup record for the missing row | Yes; diff clean; test green |
| PROP-NEG-06 Run A/B/C (retire gated on post-copy in-sync) | C3 (`sync-workflows.sh` L409) | `if [[ "$_pdlc_c3_pc_state" == "in-sync" ]]; then` → `if true; then` | 3 failed (Runs A, B, C): retained rows incorrectly deleted | Yes; diff clean; Runs A-F green (6/6) |
| PROP-NEG-06 Run E/F (retire's own backup/delete failure still gates) | C3 (`sync-workflows.sh`, after the `pdlc_retire` call) | added an unconditional `rm -f -- "$_pdlc_c3_target_abs"` regardless of `pdlc_retire`'s own return value | 2 failed (Runs E, F): path deleted despite the retirement's own backup-verify/delete failure | Yes; diff clean; Runs A-F green (6/6) |
| PROP-NEG-06 Run D (hash-tool-absent, machine-wide) | C1 (`pdlc-drift.sh` L1262) | `if [[ -z "${PDLC_HASH_BIN:-}" ]]; then` → `if false && [[ -z ... ]]; then` (disable the hash-tool-absent rung) | 1 failed (Run D): row no longer classified `unknown`/`hash-tool-absent` | Yes; diff clean; Runs A-F green (6/6) |
| PROP-NEG-07 (blast-radius, two sibling-tree tests) | C3 (`sync-workflows.sh`, after L263 mkdir block) | added an unconditional stray write: `printf ... > "${PDLC_REPO_ROOT}/pdlc-blast-radius-falsification-marker.txt"` | 2 failed: both diff-based blast-radius assertions caught the out-of-scope write | Yes; diff clean; both tests green |
| PROP-NEG-07 (blast-radius, git-never-invoked) | C3 (`sync-workflows.sh`, top of file) | added an unconditional `git --version >/dev/null 2>&1 \|\| exit 9` | 1 failed: `Expected: 0 / Received: 9` (git absent from sandboxed PATH) | Yes; diff clean; test green |

## Residuals

- **`retiredPresent[].supersedingState` does not track the post-run pass in one narrow fault composition — `pdlc/hooks/scripts/sync-workflows.sh` lines ~406-419.** The retirement/W-6 block populates `_PDLC_C3_RETIRED_STATE` (which becomes `retiredPresent[].supersedingState`) from `_pdlc_c3_pc_state` (post-copy state, captured at line 400) at line 419 — this happens *before* step 6's sync-manifest rewrite (lines 424-511) and step 7's post-run reclassify (line 514), and nothing after line 419 re-touches it. For every row tested in PROP-MTM-04 conjunct 1's "agreement" cases (R ∈ in-sync/stale/missing/local-edit/unverified without the corrupt-copy fault) this is harmless because post-copy trivially equals post-run there. It genuinely diverges only in the two fault-composition sub-cases exercised by conjunct 3 (R "stale" + `artifact-copy-corrupt`, and R "local-edit" + `--force` + `artifact-copy-corrupt`): post-copy classifies the row "local-edit" (stale sync-manifest entry vs. corrupted bytes), step 6 then removes that manifest entry because the copy failed verification, and step 7's post-run reclassify — with no manifest entry — produces "unverified". So `retiredPresent[].supersedingState` reports "local-edit" for a row whose recorded (post-run) state is actually "unverified". PROP-MTM-04 conjunct 3 was tested by asserting the disagreement directly against the trace's post-copy classify record and the drift-state's row (deliberately bypassing the defective field), which is why this is filed as a residual rather than fixed here (T-44 owns only `driftSync.test.js` and this ledger; production fixes are the orchestrator's call).
