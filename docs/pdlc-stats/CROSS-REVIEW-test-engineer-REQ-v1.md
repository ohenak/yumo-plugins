# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-stats/REQ-pdlc-stats.md`
**Date:** 2026-08-30
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **The round-count oracle names a quantity the driver does not produce.** C-5 and REQ-STATS-03 define the reported count as "the round window the review-loop driver itself would derive from the same basenames". `deriveRoundWindow` (`pdlc/workflows/orchestrate-dev.js:10180-10241`) returns `{startIndex, endIndex, present, skipped}` — a *forward budget* window, where `startIndex = max(indices) + 1` (`:10236`) and `endIndex = startIndex + MAX_REVIEW_ROUNDS - 1` (`:10256`). Taken literally, a document with 5 rounds on disk has a "round window" of 6..10, so the AC as written asserts the wrong number. A count is derivable — the driver documents the identity itself: "`startIndex` is the round the next dispatch WOULD open, so `startIndex - 1` is the number of rounds already on disk" (`:10262-10264`) — but the REQ never picks it. Nor does it pick an aggregation: `present` is `Map<role, round[]>` (`:10216-10218`), so `test-engineer` at v5 and `product-manager` at v3 for one doc type give three defensible answers (5, 3, or per-role rows). A test author cannot write one assertion for REQ-STATS-03 without asking. | C-5, REQ-STATS-03 |
| F-02 | High | Cross-Feature | **The headline metric silently reads 0 for the exact corpus the baseline protocol targets.** `harvest-learnings` deletes `CROSS-REVIEW-*` once `LEARNINGS-{feature}.md` exists (CLAUDE.md hook table; `orchestrate-dev.js:12059-12063`). On disk today, 11 of 13 features under `docs/completed/` have **zero** `CROSS-REVIEW-*` files (e.g. `docs/completed/pdlc-wave-resume/`, `docs/completed/pdlc-loop-economics/`, `docs/completed/pdlc-plugin-retirement/`), while `docs/completed/pdlc-advisory-wave-gate/` has 62. So REQ-STATS-03 will report `0 rounds` for a harvested feature and `0 rounds` for a feature never reviewed — indistinguishable. PROPOSAL §5's protocol ("capture these three series for the last 3 completed features as the pre-change baseline") therefore returns zeros, and DESIGN §5.2's regression comparison ("rounds up, ratio up") compares a harvested feature against an unharvested one. NG-6 disclaims *recovery*, which is fine, but no AC requires *disclosure*, which is what makes the number safe to read. The distinguishing signal is already mechanical and on disk: `LEARNINGS-{feature}.md` present + zero cross-reviews ⇒ harvested. Add an AC requiring a harvested/unknown state distinct from a measured `0`, in both modes. Same argument applies to REQ-STATS-06's process-bytes numerator. | REQ-STATS-03, REQ-STATS-06, NG-6 |
| F-03 | High | Local | **REQ-STATS-05's resolution oracle contradicts the pipeline's own parser on two inputs.** The AC says resolved iff "a `RESOLVED: yes` line is present", open when the line "says anything other than `yes`". `parseResolvedMarker` (`orchestrate-dev.js:7602-7614`) (a) lowercases before comparing (`const value = values[0].toLowerCase()`), so `RESOLVED: YES` is **resolved** to the engine and **open** under the AC's literal text; and (b) returns `{ok:false, reason:"duplicated"}` when two markers appear (`:7610`), which §5.8 maps to unresolved — so a file with two `RESOLVED: yes` lines is **open** to the engine and **resolved** under the AC ("a `RESOLVED: yes` line is present"). C-5 pins fidelity for cross-review basenames only; nothing pins it for the post-mortem marker, so this divergence is unmarked. Either extend C-5 to cover the `RESOLVED:` marker by reference (preferred — it removes the restatement), or state the case-insensitivity and duplicate-marker rules explicitly. | REQ-STATS-05, C-5 |
| F-04 | High | Local | **REQ-STATS-08 is an absence-only acceptance criterion.** All three of its Then-conjuncts are negative — no file created/modified/deleted, no network request, no `git` write. Every one of them is satisfied by a binary that prints nothing and exits, by a stubbed no-op, and by a crash on line 1; the Given even admits "on failure". A test written from this AC can pass while the feature does not exist. Pair the negatives with a positive conjunct asserted on the **same** invocation: the run exits 0 *and* emits REQ-STATS-01's metric set (or, on the REQ-STATS-09 path, exits non-zero *and* emits the not-found report), while a before/after snapshot of the working tree compares **set-equal** (paths and mtimes), not merely "no new file". | REQ-STATS-08 |
| F-05 | High | Local | **Fleet discovery's exclusion list omits `docs/completed/` itself, and the repo contains the file that makes that bite.** REQ-STATS-07 excludes `docs/_queue/`, `docs/_constraints/`, `docs/_decisions/`, `docs/design/`, `docs/requirements/`, `docs/ideas/`, `docs/discarded/` — but `docs/completed/` is a *container of* features, not a feature, and it is not excluded. It also contains `docs/completed/REQ-completed.md`, an archive marker whose own header says "Not a feature REQ" (line 1) and which exists solely so `coveredViolations` sees `docs/<X>/REQ-<X>.md`. So a discovery pass over `docs/*/` yields a phantom feature `completed` that even passes a "has a REQ" heuristic. State that `docs/completed/` is traversed for children and never itself reported, and give the exclusion set as a fixed enumeration a test can assert **set-equal** against, so a later directory addition fails the test rather than silently joining the report. | REQ-STATS-07, O-1 |
| F-06 | Medium | Local | **"Reported separately as malformed" inherits a classification that includes ordinary spec files.** REQ-STATS-03 says any basename failing the cross-review grammar is reported as malformed. The driver's `skipped` array (`orchestrate-dev.js:10205-10208`) is populated from `parseReviewFilename`, whose first branch returns `{ok:false, reason:"not_cross_review"}` for any name lacking the `CROSS-REVIEW-` prefix (`:10135-10137`) — i.e. `REQ-*.md`, `LEARNINGS-*.md`, `POSTMORTEM-*.md` all land in `skipped`. Under the AC's literal reading, every feature reports its own REQ as malformed. Scope the malformed report to basenames that start `CROSS-REVIEW-` but fail the grammar (`bad_role`, `bad_doc_type`, `bad_round`, `trailing_junk` — `:10143-10163`). | REQ-STATS-03 |
| F-07 | Medium | Local | **No AC covers the driver's own refusal outcome.** `deriveRoundWindow` returns `{ok:false, reason:"malformed_round_one_duplicate"}` for a whole document type when one role has both `CROSS-REVIEW-{role}-{DOC}.md` and `-v1.md` (`orchestrate-dev.js:10219-10231`) — the un-suffixed form *is* round 1 (`:10125-10127`). If `pdlc stats` honours C-5 it inherits that refusal, and the REQ says nothing about what a single-feature run reports for that document type. REQ-STATS-07 covers a malformed *feature*; nothing covers a malformed *document type* inside an otherwise healthy feature. Add the case (report the doc type as unmeasurable, name the colliding role) so it is testable and does not become an implementation choice. | REQ-STATS-03, REQ-STATS-07 |
| F-08 | Medium | Local | **"Stable field names" carries no falsifiable oracle.** REQ-STATS-02 requires the JSON to use "stable field names", while O-1 defers the field names to FSPEC/TSPEC. Nothing at this altitude can fail. Two constructions make it assertable without importing schema detail into the REQ: (i) require the JSON's top-level key set to be **set-equal** to the metric set REQ-STATS-01 prints, so adding a human-mode metric without adding the JSON field fails; (ii) require an explicit schema-version field, which is what makes "stable" observable to the NG-1 consumer later. R-5 identifies exactly this risk and then mitigates it only by deferral, which leaves the risk unmitigated at every level. | REQ-STATS-02, R-5, O-1 |
| F-09 | Medium | Local | **A metric named by the cited upstream is neither delivered nor excluded.** `DESIGN-pdlc-minimal-loop-2026-08-30.md:106` specifies `pdlc stats [feature]` as measuring "rounds, dispatches, payload bytes, halts", and §5.2 (`:130`) appends "rounds per document, dispatch count, process-to-spec byte ratio" to LEARNINGS. NG-3 excludes payload size only. **Dispatch count** appears in no goal, no AC, and no non-goal — it falls through the traceability net, so no reviewer can tell whether its absence is a decision or an omission. Either add it or add an NG naming it. (§1's phrasing "erratum events/halts" has the same shape: PROPOSAL §5 counts erratum events from "POSTMORTEM files **and the erratum changelog rows**"; only halts are specified.) | §1, §2, NG-3 |
| F-10 | Medium | Local | **REQ-STATS-04 mixes two different quantities and drops the malformed case.** "The reported count equals the highest version `N` found on disk" is the *last round index*, not the round count; the two diverge whenever a version is absent — which is not hypothetical, since harvest deletes `CODE_REVIEW-*` too (`orchestrate-dev.js:12063`), and `docs/completed/pdlc-loop-economics/` retains v1+v2 while most completed features retain none. State which is intended and why. Separately, the engine's DoD version pattern is `^CODE_REVIEW-{feature}-v(\d+)\.md$` (`:12387`), which accepts `v0` and `v01`, whereas cross-review round tokens forbid leading zeros by design (`[1-9][0-9]*`, `:10095`). REQ-STATS-04 gives no malformed-basename clause at all, unlike its REQ-STATS-03 sibling, so the two counters behave differently on the same class of input with no stated reason. | REQ-STATS-04 |
| F-11 | Low | Local | **The ratio has no precision or spelling contract, so tests will echo the implementation.** REQ-STATS-06 says "process bytes divided by spec bytes" with no rounding, no precision, and no stated rendering, and "reports the ratio as not-available" without fixing the token in either mode (`null`? `"n/a"`? omitted key?). Any test written now must read the implementation to learn the expected value — the exact implementation-echo this pipeline forbids. Fix the precision and the not-available token (or explicitly route both to FSPEC under O-1, which at least makes the deferral visible). | REQ-STATS-06, O-1 |
| F-12 | Low | Process | **A constraint cites its own dispatch as authority.** C-2 grounds the `docs/completed/{feature}/` lookup in "this REQ's own dispatch input", which is not an artifact a later reader or a test can open — and CLAUDE.md's Artifact convention section names only `docs/{feature-name}/`, not the archive location. The verifiable authorities exist: `docs/completed/` holds 13 archived feature directories, and `docs/completed/REQ-completed.md` documents the archive convention and its `coveredViolations` interaction. Cite those instead; a citation that cannot be opened has shipped in this repo before. | C-2 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | For a document type where `test-engineer` reached v5 and `product-manager` reached v3, what single number does REQ-STATS-03 report — 5, 3, or a per-role breakdown? (F-01) |
| Q-02 | Should a harvested feature's round count read `0`, or a distinct "harvested" state? The `LEARNINGS-{feature}.md`-present signal makes either answer implementable; only one of them is honest to read. (F-02) |
| Q-03 | Is `pdlc stats` expected to be runnable on a repo where `docs/completed/` does not exist at all (a fresh consuming repo)? C-2's fallback and REQ-STATS-07's discovery both assume it, but no AC states the absent-directory outcome. |

## Positive Observations

- **C-5 is the single best decision in this document.** Defining round-count fidelity by reference to the driver's own derivation, and explicitly refusing to define a second parsing rule, forecloses the entire class of drift bug where a reporting tool and its subject disagree about the same filenames. F-01 asks it to name the quantity more precisely; it does not ask it to change shape.
- **REQ-STATS-06's zero-denominator clause is exactly the right altitude.** It names the failure mode (no spec document yet on disk, mid-authoring), states the observable outcome (not-available), and rules out both alternatives (divide-by-zero, crash) — testable as written, once F-11's token is fixed.
- **R-2 and C-2 together close the mid-archival double-count.** A feature present in both locations during an archive move is a real transient state in this repo, and the fixed preference order with an explicit "never both summed" is directly assertable with a two-directory fixture.
- **REQ-STATS-09 refuses the silent-empty failure mode** and pins it in *both* modes, including "an error object rather than a truncated or partial success document" — that last clause is what stops a JSON consumer from mistaking a failure for an empty result.
- **The non-goals do real work.** NG-3 through NG-7 close off the adjacent mechanisms (`decisionLedger`, `cascade.pinCheck`, `review.derivativeStop`, the `RESOLVED:` lifecycle, the erratum channel) rather than leaving them ambiguous, which is what keeps this feature's blast radius genuinely read-only.

## Recommendation

**Needs revision**

Five High findings. The two that most affect testability are F-01 (the round-count oracle names the driver's forward *budget* window, not a historical count, so REQ-STATS-03 — the feature's headline metric — cannot be asserted without a clarifying question) and F-02 (that metric reads `0` for 11 of the 13 features under `docs/completed/`, which is precisely the corpus PROPOSAL §5's baseline protocol and DESIGN §5.2's regression comparison are aimed at). F-03 and F-05 are each a one-clause fix with an on-disk counterexample already in the repo; F-04 asks REQ-STATS-08 to pair its three negative conjuncts with a positive one on the same invocation.

To reach Approved: name the counted quantity and its cross-role aggregation in C-5/REQ-STATS-03 (F-01); add a harvested-vs-measured distinction to REQ-STATS-03/06 (F-02); pin the `RESOLVED:` marker to the engine's parser rather than restating it (F-03); add a positive success conjunct to REQ-STATS-08 (F-04); and exclude `docs/completed/` as a container in REQ-STATS-07 (F-05).


## Verdict

VERDICT: Needs revision
{"high": 5, "medium": 5, "low": 2}
