# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-stats/FSPEC-pdlc-stats.md` (v1.0)
**Date:** 2026-08-31
**Iteration:** 1

## Verification performed

Every existing-code and repository-path claim in the FSPEC was checked against HEAD in one pass.

| FSPEC claim | Where checked | Result |
|---|---|---|
| `parseReviewFilename`, `deriveRoundWindow`, `deriveDodRoundIndex`, `parseResolvedMarker` all live in `pdlc/workflows/orchestrate-dev.js` (§1 fidelity anchor) | `orchestrate-dev.js:10134`, `:10192`, `:12384`, `:7601` | Confirmed — all four exported from that module |
| The un-suffixed `CROSS-REVIEW-{role}-{DOC}.md` form is round 1, per the driver (BR-05, AT-08) | `orchestrate-dev.js:10148` (`round: n === undefined ? 1 : Number(n)`) and the docstring at `:10126` | Confirmed |
| A role carrying both the plain and the `-v1` form for one doc type is a collision the convention refuses, naming the role (BR-07, EC-06) | `orchestrate-dev.js:10216-10225` (`malformed_round_one_duplicate`, carries `role`) | Confirmed |
| The DoD derivation returns highest-plus-one, so reporting it unchanged is off by one (BR-10, D-2) | `deriveDodRoundIndex`, `orchestrate-dev.js:12384` ff. — returns `max + 1`, and `1` for an empty listing | Confirmed |
| An absent or unparseable `RESOLVED:` marker classifies fail-closed (BR-12, EC-14) | `parseResolvedMarker`, `orchestrate-dev.js:7601-7615`; docstring at `:7593` maps `absent`/`duplicated` onto `unresolved` | Confirmed |
| The closed-flag surface behaves as the existing commands do (BR-01) | `FLAGS_BY_COMMAND` `pdlc/engine/bin/cli.mjs:168`; `validateFlags` `:198` — unknown flag and value-flag-without-value both yield a usage-error string | Confirmed |
| Exit `2` is reserved by the existing CLI for a pipeline halt (BR-29) | `pdlc/engine/bin/cli.mjs:20-22` (exit-code header) and `:693` (`outcome === "halted" \|\| "blocked"` → 2) | Confirmed |
| BR-25's exclusion set is set-equal to the non-feature directories at the `docs/` root | Directory listing of `docs/`: `_constraints`, `_decisions`, `_queue`, `completed`, `design`, `discarded`, `ideas`, `requirements` — eight, matching BR-25's eight | Confirmed |
| `docs/PLAN-pdlc-integration-boundary-gates.md` and `docs/completed/REQ-completed.md` are both present and neither is a feature (BR-25, AT-18) | Both present | Confirmed. `docs/completed/QUEUE-HISTORY-rows-0-1.md` is a third loose file the FSPEC does not name; harmless, same treatment |
| `docs/completed/pdlc-loop-economics/` carries `_evidence/` (BR-03) and `CODE_REVIEW-…-v1.md` and `-v2.md` (AT-11) | Directory listing | Confirmed — AT-11's expected `2` is correct |
| `docs/completed/pdlc-headless-engine/` has `LEARNINGS`, one surviving TSPEC cross-review, none for the other five types (AT-10) | Directory listing — the survivor is `CROSS-REVIEW-software-engineer-TSPEC-v13.md` | Confirmed; the measured index is `13` (see F-05) |
| `docs/completed/pdlc-wave-resume/` carries `POSTMORTEM-PR-pdlc-wave-resume.md` (AT-13) | Present; exactly one line-leading `RESOLVED:` marker, value `yes` | Confirmed; the classification is `resolved` (see F-05) |
| `docs/pdlc-halt-hardening/` carries only a PLAN (EC-17) | Directory listing — `PLAN-pdlc-halt-hardening.md` only | Confirmed |
| The post-mortem **basename grammar** is the pipeline's own (BR-12, EC-15) | `orchestrate-dev.js:8618`, `:9402`, `:15293` — the driver **constructs** `docs/{feature}/POSTMORTEM-{phase}-{feature}.md` and probes it per phase; it never parses a `POSTMORTEM-*` listing | **Not confirmed** — see F-03 |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **An empty feature directory has two contradictory behaviours.** §3.2 step B5 sends a feature whose computation produced no metric set — parenthesised as "unreadable directory, **no artifacts at all**" — to a gap row, and BR-27 says "a feature whose artifacts are **missing** or cannot be read is reported by name with a reason". EC-03 says the opposite for the same tree: an empty directory reports all-zero states and is "**not** a gap and not an error". In fleet mode an implementer must pick one, and the two produce different rows, a different `features` value shape in JSON (metric object vs `{gap}`), and a different human row. Resolve by narrowing B5/BR-27 to *unreadable*, and stating that emptiness is a measurable state, not a gap — EC-03's reading is the one the metric vocabulary supports. | §3.2 B5, BR-27, EC-03 |
| F-02 | High | Local | **An unclassified directory has no representation in the fleet JSON document.** BR-26 and EC-10 require an unexpected `docs/`-root directory to surface "as an unclassified entry in the report", and AT-19 asserts it. But BR-23 fixes the fleet document at **exactly two** top-level keys (`schemaVersion`, `features`), and `features` maps *discovered feature names* to either a four-metric object or a `{gap}` object. An unclassified entry is by construction not a discovered feature, so it fits neither value shape and cannot become a third top-level key. As written, AT-19 is unimplementable in `--json` mode. Decide the carrier: either a third top-level key (and amend BR-23's count), or a distinct `{unclassified}` value inside `features` (and say so, so a consumer's discriminant stays key-presence per BR-23). | BR-23, BR-26, EC-10, AT-19 |
| F-03 | High | Cross-Feature | **The halts metric asserts a driver classification that does not exist, and then forbids inventing one.** §1's fidelity anchor claims every classification this command re-reads "is already made in one place in the shipped pipeline", naming four functions; BR-12 adds "the basename grammar admits one post-mortem per phase per feature". There is no post-mortem basename grammar in the driver: it **constructs** `docs/{feature}/POSTMORTEM-{phase}-{feature}.md` from a known phase id and probes that path (`pdlc/workflows/orchestrate-dev.js:8618`, `:9402`, `:15293`), and never parses a `POSTMORTEM-*` directory listing. Only `parseResolvedMarker` (`:7601`) is a real anchor, and it classifies *contents*, not basenames. So BR-12's "one entry per distinct phase that has a `POSTMORTEM-{phase}-{feature}.md` file" requires the stats command to discover phases by parsing basenames — a new rule — while EC-15 refuses a malformed bucket precisely "because inventing one here would be an independent parsing rule (REQ C-5)". The document forbids what it requires. Nor is there a phase-id catalogue to lean on: `FORCE_PHASE_TOKENS` (`:7416`) is `R,F,T,P,D,PR` and omits `I`, yet `POSTMORTEM-I-pdlc-headless-engine.md` exists on disk. State plainly that halts discovery is a *this-command* basename match, cite what it matches, and say why that is not a C-5 divergence (there is nothing to diverge from). An upstream erratum is raised against REQ C-5 separately; this finding is the FSPEC's own unverified existing-code claim. | §1 fidelity anchor, BR-12, EC-15 |
| F-04 | Medium | Local | **BR-20's unconditional promise is contradicted by EC-08.** BR-20: "In `--json` mode, stdout is exactly one well-formed JSON document" — stated over the mode, not over the success path — and its stated purpose is that "a caller can parse stdout unconditionally". EC-08 and AT-24 then require **empty** stdout for a usage error under `--json`, which no caller can parse. BR-30 makes the opposite choice for the adjacent failure (unknown feature → error object on stdout). Both choices are defensible; the document must not make both. Scope BR-20 explicitly ("on every path that produces a report or a not-found result"), or extend the error object to usage errors. | BR-20, BR-30, EC-08, AT-24 |
| F-05 | Medium | Process | **Two acceptance tests derive their expected value from the code under test instead of transcribing it.** AT-10's Then is "the TSPEC row carries **the measured index derived from the surviving file**"; AT-13's is "tagged with **the resolution the pipeline's own `RESOLVED:` rule yields** for that file's bytes". Both instruct the test to compute the expectation by running the mechanism under test, so an implementation that mis-derives the index or mis-reads the marker passes. Both fixtures are real, stable, archived paths whose values are checkable now: `docs/completed/pdlc-headless-engine/` yields **13** (`CROSS-REVIEW-software-engineer-TSPEC-v13.md`), and `docs/completed/pdlc-wave-resume/POSTMORTEM-PR-pdlc-wave-resume.md` carries exactly one line-leading `RESOLVED: yes` and therefore yields **`resolved`**. AT-11 already sets the right precedent by pinning the literal `2`. Transcribe both literals. | AT-10, AT-13 |
| F-06 | Medium | Local | **Fleet human mode silently drops information the JSON document carries.** BR-18's row is "the feature name, one column per BR-09 document type, then DoD rounds, halt count, and the ratio" — no malformed list (BR-06 requires malformed basenames be "reported separately", and the single-feature layout BR-17 renders them), and no place for F-02's unclassified entry. An operator scanning the fleet — BR-18's stated audience — never sees a malformed cross-review at all, which is the one state that means a round count is *wrong* rather than merely absent. Give BR-18 a rendering for both. | BR-18, BR-06, BR-17 |
| F-07 | Medium | Local | **The `unmeasurable` state has no acceptance test.** §6.10 lists BR-07 as covered by "AT-01 (row rendering), EC-06" — but AT-01's Given constructs no round-1 collision, and EC-06 is an edge-case row, not a test; an edge case is not an oracle and cannot be cited as coverage. Consequently the `unmeasurable` token fixed by BR-19, and the "naming the colliding role" requirement (and its `collidingRole` JSON field, BR-22), are asserted nowhere. Add an AT that builds the collision — the plain and `-v1` forms for one role and doc type — and asserts the token, the named role, that the other five rows still carry their measured values, and exit 0. | §6.10, BR-07, BR-19, BR-22 |
| F-08 | Medium | Local | **AT-15 does not check BR-14's enumeration by set-equality, so a dropped member passes.** Its Given is "a directory holding spec documents, cross-reviews, a post-mortem, a `CODE_REVIEW`" — it never requires all six spec document types or all three process grammars to be present. An implementation that omits `DECISIONS-{feature}.md` from the spec side, or `POSTMORTEM-*` from the process side, still satisfies "the totals equal the on-disk sizes of exactly the BR-14 members present" on a fixture that lacks them. BR-14 is an enumerated catalogue and needs a fixture pinned to the full enumeration, so deleting a case fails. Require the fixture to carry all six spec types and all three process families, each with a distinct size. | AT-15, BR-14 |
| F-09 | Medium | Local | **Eight of twenty-one edge cases have no acceptance test, and §6 has no EC-to-test traceability table.** EC-03, EC-11, EC-15, EC-16, EC-17, EC-18, EC-19 and EC-20 are cited by no AT. §6.10 traces rules to tests only, which is why F-01's contradiction survived authoring: EC-03 has no test that would have forced the question. EC-11 matters most — it is the one row whose exit code differs by mode (D-6's decision, 0 in fleet and 1 in single-feature) and nothing asserts either half. Add an EC-to-test column or table, and cover at least EC-03, EC-11 and EC-16. | §5, §6.10 |
| F-10 | Low | Local | **The review-round off-by-one is left unguarded while the identical DoD one is guarded.** BR-10 explicitly rules out reporting `deriveDodRoundIndex`'s return value because it is highest-plus-one. `deriveRoundWindow` has exactly the same shape — `startIndex` is `Math.max(...indices) + 1` and is `1` for an empty listing (`orchestrate-dev.js:10229-10231`) — so an implementer reusing it for BR-05 reports `1` for every never-reviewed document type, colliding with BR-08's `0`/`harvested` distinction. BR-05 is behaviourally unambiguous, so this is a near-miss note, not a contract gap; state it as BR-10 does, since BR-05 must read `present`, not `startIndex`. | BR-05, BR-10 |
| F-11 | Low | Local | **EC-05's malformed enumeration omits one of the driver's four rejection reasons.** `parseReviewFilename` returns `bad_role`, `bad_round`, `trailing_junk` **and `bad_doc_type`** (`orchestrate-dev.js:10145`) — a grammatical basename whose doc type is outside the six-type catalogue. EC-05 lists only the first three. Since BR-09 fixes the row set at six types, a `CROSS-REVIEW-software-engineer-GLOSSARY.md` has no row and must land in the malformed list; say so, so the implementer does not silently drop it. | EC-05, BR-06, BR-09 |
| F-12 | Low | Local | **EC-19 does not decide `lstat` versus `stat` for a symbolic link.** "Its byte size is the size the filesystem reports for the path as read" is readable either way, and the two differ by orders of magnitude. Name one. | EC-19 |
| F-13 | Low | Local | **A missing `docs/` root has two spellings in single-feature mode.** §3.1 step A2 routes an unreadable root to EC-09; EC-09 says that in single-feature mode a merely-absent root "presents as EC-01's not-found". Both exit 1, so only the operator-visible message differs — but the message is the observable, and two rules assign two different ones. | §3.1 A2, EC-09 |
| F-14 | Low | Local | **BR-13's halt ordering names no collation.** "Ordered by phase identifier, ascending" — BR-18 says *lexicographically* for feature names, BR-13 does not, and the phase ids on disk (`D`, `F`, `I`, `PR`, `T`) include a two-character id whose position depends on the choice. Say lexicographic, so BR-13's stability claim is checkable. | BR-13 |

## Questions

## Positive Observations

## Recommendation

## Verdict
