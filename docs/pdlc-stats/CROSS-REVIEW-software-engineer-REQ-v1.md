# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-stats/REQ-pdlc-stats.md`
**Date:** 2026-08-30
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | REQ-STATS-03/C-5 define the headline metric as equal to "the round window the review-loop driver would derive". A *window* is not a *count*. `deriveRoundWindow` (`pdlc/workflows/orchestrate-dev.js:10192`) returns `{startIndex, endIndex}` where `startIndex = max(existing rounds) + 1` (`:10236`) and `endIndex = startIndex + MAX_REVIEW_ROUNDS - 1` (`:10254`) — i.e. the *upcoming* round budget, not rounds already completed. Implemented literally, the P0 metric reports off-by-one, or a two-number range where a count is required. Contrast REQ-STATS-04, which correctly names an outcome ("the highest version `N` found on disk"). Fix: state the outcome directly — the count is the highest round index present for that document type (equivalently `startIndex - 1`) — and keep C-5's fidelity clause as the *no-independent-parsing* rule it is. | REQ-STATS-03, C-5 |
| F-02 | High | Local | Nonexistent-authority citation. C-2 states the two lookup locations "mirror the two locations already named in this repo's `CLAUDE.md` Artifact convention section". `CLAUDE.md:93` names only `docs/{feature-name}/`; the string `completed` does not appear anywhere in `CLAUDE.md` (0 occurrences). The only file that names the archived location is `pdlc/OPERATIONS.md:146` ("`docs/*/LEARNINGS-*.md` and `docs/completed/*/LEARNINGS-*.md`"), and it names it for learnings-harvest sourcing, not as a general artifact-lookup convention. The constraint itself is sound; its cited authority is false. Fix: cite `pdlc/OPERATIONS.md:146`, or state C-2 as this REQ's own decision rather than as an inherited convention. | C-2 |
| F-03 | High | Local | Second nonexistent-authority citation. REQ-STATS-07 excludes seven directories "this repo's `CLAUDE.md` and `pdlc/OPERATIONS.md` already name". Three of the seven are named in neither file: `docs/design/` (0 hits in both), `docs/requirements/` (0 hits in both), `docs/ideas/` (0 hits in both). `docs/discarded/` appears only in `pdlc/OPERATIONS.md`, not in `CLAUDE.md`. Only `docs/_queue/`, `docs/_constraints/` and `docs/_decisions/` are actually named by `CLAUDE.md:93`. The exclusion list is a new decision being made here while presented as a citation. Fix: present it as this REQ's enumeration, or delete the parenthetical and let O-1 own the list (O-1 already claims the precise list is FSPEC/TSPEC material — see F-04). | REQ-STATS-07 |
| F-04 | High | Local | The REQ-STATS-07 exclusion list omits `docs/completed/` itself, so fleet discovery over `docs/*` enumerates `completed` as a feature. This is not hypothetical in this repo: `docs/completed/` is a directory under `docs/` and contains `docs/completed/REQ-completed.md`, which matches the `REQ-{feature}.md` shape for a feature literally named `completed` — so the phantom row would look like a *legitimate* feature, not a flagged gap, defeating G-3's whole point. `docs/completed/QUEUE-HISTORY-rows-0-1.md` sits in the same directory. Fix: exclude `docs/completed/` from the `docs/*` sweep (it is a container of features, not a feature), and note that discovery must consider directories only — `docs/PLAN-pdlc-integration-boundary-gates.md` is a loose file at `docs/` root today. | REQ-STATS-07, G-3 |
| F-05 | Medium | Local | REQ-STATS-05 restates the POSTMORTEM resolution rule as "a `RESOLVED: yes` line is present", dropping the qualifier the documented convention carries: `pdlc/OPERATIONS.md:297` requires the line be "outside any fenced block". A post-mortem that shows `RESOLVED: yes` inside a fenced example — a realistic thing for a doc explaining its own lifecycle — would be reported *resolved* by `pdlc stats` while the driver still refuses the phase as *open*. That is exactly the reader-divergence C-5 forbids for round counts, but C-5's scope is round counts only. Fix: either widen C-5 to cover every artifact-parsing rule this command re-reads, or restate REQ-STATS-05 with the fenced-block qualifier. | REQ-STATS-05, C-5 |
| F-06 | Medium | Local | REQ-STATS-03 asks for a "review-round count **per document type**", but rounds are tracked per *(role, docType)* pair: `deriveRoundWindow` builds `present` as a `Map<role, round[]>` (`pdlc/workflows/orchestrate-dev.js:10200`, `:10213-10217`), and one round of REQ review normally produces several files (this feature will have `CROSS-REVIEW-software-engineer-REQ-v1.md` alongside its pm/te siblings). With 3 roles reviewing 2 rounds, the REQ does not say whether the answer is `2` (distinct rounds) or `6` (cross-review files). Both are defensible readings of the current text and they diverge on every real feature. Fix: name which quantity the operator gets. | REQ-STATS-03 |
| F-07 | Medium | Local | Unspecified behaviour in the one state where the reference implementation refuses to produce a count. When one role has both `CROSS-REVIEW-{role}-{DOC}.md` and `CROSS-REVIEW-{role}-{DOC}-v1.md` on disk, `deriveRoundWindow` returns `{ok: false, reason: "malformed_round_one_duplicate"}` (`pdlc/workflows/orchestrate-dev.js:10225-10231`) — no round window at all, because picking either file "is a coin flip" (`:10180`). C-5 binds `pdlc stats` to never diverge from that driver, yet REQ-STATS-03 has no outcome for the case where the driver yields nothing. G-3's malformed-not-dropped stance suggests the intent, but it is not stated for the single-feature path. Fix: state that this state is reported as malformed for that document type rather than as a count. | REQ-STATS-03, C-5, G-3 |
| F-08 | Low | Local | REQ-STATS-03 says "any basename that fails the already-documented well-formed cross-review grammar is excluded from the count and reported separately as malformed". Applied literally to a feature directory, that sweeps in every ordinary artifact: `parseReviewFilename` returns `{ok: false, reason: "not_cross_review"}` for `REQ-pdlc-stats.md` and friends (`pdlc/workflows/orchestrate-dev.js:10135-10137`), which is a distinct outcome from the four genuine grammar rejections (`bad_role`, `bad_doc_type`, `bad_round`, `trailing_junk`, `:10143-10163`). Reporting spec documents as "malformed cross-reviews" would be noise. Fix: scope the malformed report to files that claim the `CROSS-REVIEW-` prefix but fail the grammar. | REQ-STATS-03, R-1 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | O-2 leaves reuse-vs-reimplement to TSPEC. Reuse is feasible — `parseReviewFilename`, `deriveRoundWindow` and `deriveDodRoundIndex` are all `export`ed (`pdlc/workflows/orchestrate-dev.js:10133`, `:10192`, `:12384`) — but they live in an ~18k-line module whose other exports drive dispatch. Is importing that module from a read-only path acceptable, given C-1's no-writes-on-any-path stance? If TSPEC picks reuse, it should confirm the module is import-side-effect-free. |
| Q-02 | O-3 leaves subcommand-vs-script to TSPEC. Note the engine CLI gates every command through a per-command flag table — `case "doctor"`/`case "decide"`/`case "dev"`/`case "queue"` each call `checkFlags(rest, cmd)` (`pdlc/engine/bin/cli.mjs:1268-1279`), and `stats` is absent today. If `--json` (REQ-STATS-02) is to be accepted, `stats` and its flag set need registering there. Is that in scope for this REQ's PLAN? |
| Q-03 | REQ-STATS-09 requires exit non-zero for an unknown feature. Does fleet mode (REQ-STATS-07) also exit non-zero when *some* feature is malformed, or is a malformed feature a reported row with a zero exit? The two ACs do not resolve each other, and an automated caller (US-02) will branch on it. |

## Positive Observations

- The `[-v{N}]` optional-suffix bracket in C-4 and REQ-STATS-03 is exactly right, and it is not the obvious choice: the un-suffixed `CROSS-REVIEW-{role}-{DOC}.md` form **is** round 1 in this codebase (`pdlc/workflows/orchestrate-dev.js:10125-10130`, `:10148`), so a spec that had written `-v{N}` as mandatory would have silently under-counted every historical review. The REQ got this right without being told.
- REQ-STATS-04 is a model of the altitude this document should hold throughout: "the highest version `N` found on disk, or `0` when no such file is present" is a pure observable, and it maps cleanly onto the shipped `deriveDodRoundIndex`, which returns `max + 1` (`pdlc/workflows/orchestrate-dev.js:12384-12396`). The `- 1` is an implementation detail the REQ correctly declines to state. F-01 asks only that REQ-STATS-03 be written to this same standard.
- C-5's "no independent parsing rules" is the right constraint to have written down, and it is the single highest-value line in the document: an independent basename parser is precisely how a measurement tool drifts from the thing it measures.
- NG-3's citation checks out — `report.learningsInjection` is real (`pdlc/workflows/orchestrate-dev.js:15068`), so the deferred payload-size series is a genuine, locatable follow-on rather than a vague gesture.
- G-4/C-1/REQ-STATS-08's read-only stance is stated three times at three altitudes and is mechanically testable as written (no writes, no network, no `git` write verbs, on success *and* error paths). That is a testable non-functional requirement, which is rarer than it should be.
- R-2 and R-4 anticipate the two failure modes that actually bite: the mid-archival double-count and the zero denominator. Both are mitigated by a named constraint rather than by hope.

## Recommendation

## Verdict
