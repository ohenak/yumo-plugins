# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v1.3, 2026-08-01)
**Date:** 2026-08-01
**Iteration:** 3
**Scope:** every finding below carries its own Scope tag in the findings table.
**Protocol:** delta re-review. The baseline reviewed at v2 was the REQ as of `fa83925`; this review
covers `fa83925..94e2137` on that file (80 insertions, 69 deletions). Sections unchanged since v2
were not re-litigated.

## 1. Disposition of the v2 findings

All six v2 findings are **closed**. Each was checked against the revised text and, where it made a
claim about the shipped code or a shared file, against that file at HEAD.

| v2 id | Sev | Status | Where it closed, and what was checked |
|---|---|---|---|
| **F-01** | Medium | ✅ closed | O-10 now carries the clause I asked for, and it is stronger than the ask: *"**the refusal's own operator strings** (§6) — the ❌ text `Refused — reset region corrupt at {path} ({reason})` and a recovery text naming **that reason's** sanctioned repair, both asserted character for character, with step G's shipped strings (`Refused — unresolved POSTMORTEM at …` and `set the row back to pending, then re-run the queue`) as **negative controls**, plus `postmortemStatus` asserted `resolved`-or-unset and **never** `unresolved`."* Naming the two shipped strings as explicit negative controls is what makes the fix falsifiable — the exact implementation I described in v2 F-01 (reuse `orchestrate-dev.js:4246` and `:4926`) now fails a stated leg rather than passing every one. |
| **F-02** | Medium | ✅ closed | §6's preamble now reads *"**Four rows below sit deliberately outside baseline §3's scope and are not that defect:** `budget-exhausted:` is a render fixed by catalogue §2, and the three refusal-render rows are non-catalogue operator strings this REQ alone owns. Both have a registered authority — the catalogue, or this table."* That is the second of the two resolutions I offered, taken cleanly: the preamble no longer asserts a rule its own table breaks, and each out-of-scope row now names where its authority actually lives. The arithmetic checks: six baseline-§3 rows owned + four scoped-out owned + one read-only row = the table as written. |
| **F-03** | Low | ✅ closed | §4's preamble now reads *"owns **six** catalogue ids and **reads** two"*, matching its table (S-12, S-13, S-14, S-15, S-16, S-4 owned; S-11, S-3 read). |
| **F-04** | Low | ✅ closed | AC-1.5(4) now states the positive: *"`postmortemStatus` reads `resolved` — the operator did clear it — or is unset; **never** `unresolved`"*, and O-10 asserts exactly that disjunction. §6's *Refusal phase-row text* row keeps the negative as a gloss, which is fine now the positive is stated where the behaviour is. |
| **F-05** | Low | ✅ closed | AC-1.2 gained *"**`W` is `windowEnd`'s sole production argument:** the dormant `windowEnd(startIndex)` parameter defaults (`reviewLoop`'s `endIndex`, `checkConverged`'s fallback) compute a *wider* window whenever `W ≠ startIndex`, so they must be removed or made unreachable."* Both sites are named, and the obligation is imperative rather than descriptive. |
| **F-06** | Low/Process | ✅ observed, and the constraint held | The revision is net **+436 bytes** (60,892 → 61,328) against a 61,440-byte ceiling, achieved by compressing eleven justification paragraphs to pay for the four substantive additions — exactly the shape I asked for. See F-05 below: the headroom is now 112 bytes, which is a live constraint on *this* round's revision rather than a finding against the content. |

## 2. Disposition of the v2 questions

All three v2 questions were explicitly marked *"no REQ change needed"* or *"I am not filing it"*, so
none of them gates this round. Recording where each stands so the next reader does not re-open them:

| v2 id | Status | Note |
|---|---|---|
| **Q-01** (does the `W` resolver share `deriveRoundWindow`'s doc-type-scoped derivation, or re-implement it?) | Still unstated, deliberately | AC-1.5(4) step 2's *"and it is the doc-type-scoped one, because a window is a property of a document"* is unchanged, and O-12 still covers only the forward direction (`W` reaching `deriveRoundWindow` as a resolved value). I said a TSPEC-level answer suffices and I hold to that — but the FSPEC/TSPEC reviewer should read this as an open obligation: **one** doc-type filter, not two. A second implementation that drifts from `parseReviewFilename`'s round-1 spelling would validate against a different listing than the loop windows over. |
| **Q-02** (both a value fault and a counts fault present) | Answered by the unchanged text | Step 4's *"the **first failing line** in document order, and `counts-mismatch` only when every line passes step 2"* settles it deterministically in favour of the value reason, which is the reason with the non-destructive repair. Two operator round-trips, and that is the right order. Nothing to change. |
| **Q-03** (reporting-only notice on the `{ skip: true }` path) | Not taken, as I expected | The REQ keeps deliberate silence on the skip path. I lean to that disposition and am not filing it in this round either. |

## 3. Findings

Scanned the changed sections only. **One High**, on the one genuinely new behaviour this revision
introduced — the unconfirmable-append render. Fixing v2 F-01/F-02 required minting a fourth
dispatch-less entry class, and that class was registered in §6 (which v2 F-02 forced) but **not** in
the shared catalogue whose row-B definition it contradicts.

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | **High** | Cross-Feature | **The unconfirmable-append entry is specified as emitting "row B", and that contradicts the shared catalogue's definition of row B — in the file the catalogue itself says must be amended first.** `docs/_constraints/pdlc-rcv-catalogue.md` §3 states: *"**Three rows have no dispatch behind them** … **row B**, the row of an entry whose reset region **failed validation**, in `pdlc-rcv-budget-stop` AC-1.5(4) … Rows B and C are mutually exclusive: B's entry takes no halt, C's takes one, so **B carries S-16 alone** and C carries S-4."* AC-1.5(4)'s new clause says the unconfirmable-append entry *"emits **row B with an empty `notice`** — no S-16, no S-4"*. Three separate contradictions, all in the same sentence: **(a)** its region **passed** steps 1–3, so by the catalogue's own definition it is not a row-B entry at all; **(b)** *"B carries S-16 alone"* is now false, unamended; **(c)** *"Three rows have no dispatch behind them"* is now four, and the REQ's own AC-1.5(1) still calls row C *"the **third** dispatch-less row"*. The REQ's own row-B cell spec breaks too: §5 line 404 fixes `notice` = **S-16 alone** and derives the `round` cell from the listing *"**not** from `W`, which is 1 on this path **by construction**"* — but on the unconfirmable path `W` is not 1 by construction; the region validated and the entry was about to grant a window at `N`. The numbers may coincide; the stated justification does not. This is load-bearing, not cosmetic: the catalogue's stated bar is *"a test author must be able to derive the exact cell, character for character, from these documents alone"*, and the catalogue's preamble sets the amendment protocol — *"A REQ may not add an `S-*` id without amending §2 of this file"* — i.e. shared-schema changes are made **in the catalogue**, by the REQ that needs them. A PROPERTIES author reading catalogue §3 writes *"every row B carries S-16"*; O-10 simultaneously demands *"the unconfirmable-append entry emitting §6's Unconfirmed-append text with an **empty** `notice`"*. One of those two tests must fail whatever the implementation does. **Resolve by picking one and stating it end-to-end:** either (i) amend catalogue §3 — row B becomes *"an entry that opens no round on step 4's path"*, its `notice` becomes *"S-16, or empty on the unconfirmable-append variant"*, and the count becomes four; or (ii) do not reuse row B — mint **row D** for the unconfirmable-append entry and state it cell by cell as the catalogue requires of the REQ owning the condition. Whichever is chosen, the consequential edits are AC-1.5(1) (*"third dispatch-less row"*), §5 row B's `notice` and `round` cells, §2's *"the two no-round rows"*, and O-10's *"**row B** asserted character for character with **no** S-4 reason"*. This is the same nonexistent-/contradicted-authority shape v2 F-02 filed; the fix registered the new **string** in §6 and left the new **row** unregistered. | AC-1.5(4) *"The answering line is written, and confirmed"*; §5 *Row B*; AC-1.5(1) row C; §6 row *Unconfirmed-append text*; `docs/_constraints/pdlc-rcv-catalogue.md` §3 |
| F-02 | Medium | Local | **The unconfirmable-append recovery text is neither pinned nor complete, and has no O-10 oracle — reopening on the new path exactly the gap v2 F-01 closed on the other two.** §6's new row gives the recovery as the bare word *"recovery text *re-run*"*, and AC-1.5(4) as *"its recovery text is *re-run*, there being nothing to repair."* Two problems. **Unpinned:** the other two refusal strings in the same table are given character for character (`Refused — reset region corrupt at {path} ({reason})`) or by a stated derivation rule (*"names the sanctioned repair for `{reason}`"*), and O-10 asserts both with negative controls. *"re-run"* is neither a literal nor a rule; nothing in O-10 touches it, so any string containing those characters passes — including the shipped generic one this REQ elsewhere forbids. **Incomplete:** the same clause routes this entry down *step 4's path*, and step 4's path is stated to write *"the feature's `docs/_queue/QUEUE.md` row … to `halted` and committed"*. An operator told only *"re-run"* cannot re-run — the queue row is `halted` and the queue gate will not pick the feature up until a human moves it. The shipped generic string (`set the ${featureName} row in docs/_queue/QUEUE.md back to pending, then re-run the queue`, `orchestrate-dev.js:4926`) is, on **this** path only, the correct and complete instruction — unlike on the corrupt-region path, where §6 rightly forbids it because re-running reproduces the refusal. If that is the intent, say so: it is a deliberate and slightly surprising asymmetry between two rows of the same table, and it needs to survive an implementer who reads only §6. Required: pin the string (or state that the shipped generic recovery text is deliberately reused here and why), and add it to O-10 alongside the ❌ text it already asserts. | §6 row *Unconfirmed-append text*; AC-1.5(4); O-10 |
| F-03 | Medium | Local | **"Safe both ways" enumerates two outcomes of an unconfirmable append, in a paragraph that has just admitted a third.** The clause states *"the file is **not** byte-unchanged (a partial append may have landed — that is what unconfirmable means)"*, then concludes *"Safe both ways — if the line landed, `A = H` and the next entry grants nothing; if not, the next entry re-observes the same clearance and grants the one window paid for, never two."* A **torn** write is neither: a truncated `WINDOW-START` or a value written without its newline leaves a line that is present but does not parse, which step 2 reads as a value fault ⇒ S-16 `invalid-window-start` ⇒ the *corrupt-region* refusal, with its own ❌ text, its own recovery text and a required operator hand-edit. That is fail-closed and therefore acceptable — but it is a third outcome, with a different operator experience and a different set of strings, reached from a transient IO fault. As written a reader concludes an unconfirmable append never needs operator action; on this branch it always does. It also interacts with F-02: the recovery text this entry emits (*"re-run"*) is wrong for the operator whose *next* invocation will refuse on a corrupt region. One sentence closes it: name the torn-write case, say it lands in step 2's fail-closed refusal on the next entry, and confirm the sanctioned in-place **correction** or line deletion applies to it like any other invalid value. | AC-1.5(4) *"The answering line is written, and confirmed"* |
| F-04 | Low | Local | **§6's new row asserts a negative about the S-16 enum that belongs in AC-1.5(4), where the enum's closure is argued.** The *Unconfirmed-append text* row carries *"**Not a catalogue id**: the region is not corrupt, so no S-16 reason binds"* — a behavioural justification, in the shared-threshold table, for a decision AC-1.5(4) already makes and argues (*"It mints **no new catalogue id and no new S-16 reason** (that enum is closed at three): an unconfirmable write is an IO fault of the loop, not a state of the region"*). Duplicated reasoning in two places drifts; the family has already paid for that once (§10's own account of the parent). Prefer the §6 row citing AC-1.5(4) rather than restating it — and it buys bytes, which F-05 says you need. | §6 row *Unconfirmed-append text* |
| F-05 | Low | Process | **112 bytes of headroom.** 508 lines / **61,328 bytes** against `check-req-size.sh`'s 700-line / 61,440-byte ceiling — **99.8%** of the byte budget, down from 548 bytes at v2. The v1.3 revision paid for its four additions by compressing eleven justification paragraphs, and that lever is close to spent. F-01's fix is the problem: option (ii) (mint row D, stated cell by cell) costs several hundred bytes this document does not have, and option (i) (amend the catalogue) puts most of the new text in `pdlc-rcv-catalogue.md` instead — which is both cheaper here and, per that file's own amendment protocol, the more correct home. That is a reason to prefer (i) beyond the byte count, but the byte count is now a hard constraint on the choice and should be acknowledged when it is made. If the revision cannot land inside the ceiling, a further split at a stated seam is the only remaining lever; do not drop a criterion to fit. | whole document; §10 |

## 4. Questions

| ID | Question |
|----|---------|
| Q-01 | The unconfirmable-append entry ends the invocation with the queue row `halted`, on a fault that is by construction **transient** (a failed re-read, a filesystem hiccup) rather than one needing a human. Every other step-4 exit halts on a state only an operator can repair; this one halts on an IO fault whose repair is "try again". Was a **single in-entry retry of the confirmation read** considered and rejected? I am not filing it — halting is fail-closed and consistent with the surrounding design, and a retry adds a loop where the document has been careful to have none — but the cost is one unattended queue stopped by a transient read, and I would rather the choice were recorded than inferred. If it stands, R-11's *"a real cost, accepted deliberately"* gloss is the natural place for one clause. |
| Q-02 | AC-1.5(4) says the unconfirmable entry keeps `W` at *"its prior value"*. On the branch this fires on, the prior value is whatever step 5 would have read **before** the answering line — which, on a granting entry, is the greatest `WINDOW-START:` from the *previous* window, or 1 if there is none. Since the entry dispatches nothing and returns, `W` is never used, so this is inert today. But `pdlc-rcv-fixed-point-stop` states both its tests over `W`, and a successor that reads `W` from a report or a phase record on a refusing entry would read a value that is neither the old window's nor the granted one's. Is `W` guaranteed **not** to be emitted on any refusing entry — i.e. is *"keeps its prior value"* an internal statement only? A half-sentence saying `W` appears in no operator-visible or downstream-visible surface on a refusal would close it for the successor's author. |
| Q-03 | §6's *Unconfirmed-append text* pins `Refused — answering line unconfirmed at {path}`, where `{path}` is the post-mortem's path. Step G's shipped string and this REQ's corrupt-region string both also carry `{path}` as the post-mortem. But the *unconfirmed* fault is about a **write**, and an operator meeting this row's most useful next action is to look at what actually landed in that file. Is `{path}` here deliberately the same repo-root-relative post-mortem path as the S-16 render fixes (catalogue §2), so all three refusal strings quote the same thing in the same form? I read it as yes; stating "the same `{path}` S-16 fixes" in the §6 row would make it checkable without the reader having to infer it. |

## 5. Positive Observations

- **O-10's negative controls are the strongest single edit in this revision.** v2 F-01 asked for an
  assertion; what landed asserts the two strings *and* names the two shipped strings an
  implementation would otherwise reach for as **negative controls**, by their exact text. That
  converts a leg that could be satisfied by accident into one that fails for the specific,
  identified reason it exists to catch. Very few review findings get closed with a test that names
  the wrong answer as well as the right one.
- **The revision paid for its additions instead of growing.** Four substantive additions landed for
  a net +436 bytes, funded by compressing eleven justification paragraphs — and I checked the
  compressions, not just the byte count: none of them dropped an obligation. *"the only two
  reachable states of the alternative, and opposite failures"*, *"closed but **absorbing**, and no
  clearance repairs it"*, *"both counts stay true"* all preserve their arguments in fewer words.
  The one place I looked hardest — AC-1.4 clause 2's disjoint-sets argument — kept both quantifiers
  and dropped only the restatement.
- **The unconfirmable-append case was recognised as needing its own render at all.** The easy
  revision was to leave step 4's existing strings covering it by implication. Instead the REQ
  noticed that *"step 4's own renders all take an S-16 reason this entry has not got"* — which is
  exactly right and is the kind of thing that is normally discovered in implementation. F-01 is
  about where that new row is registered, **not** about the observation, which is correct and was
  the document's own to make.
- **The two step-4 invariants that do not hold on the new path are named rather than left to
  collide.** *"the file is not byte-unchanged"* and *"the ratchet's *same reason next entry* has no
  reason to be stable"* are both scoped explicitly to the validation-failure path. A document that
  adds a branch and does not go back to check which of its existing invariants that branch breaks
  is how contradictory test suites get written; this one went back and checked. F-03 is that the
  check found two and stopped at two.
- **§6's scope statement resolves v2 F-02 by making the authority explicit per row.** *"Both have a
  registered authority — the catalogue, or this table"* is the sentence that makes the family-level
  consistency check possible: every row now points somewhere, so a future reviewer of the baseline
  can enumerate what is deliberately absent from it rather than inferring it. That principle is
  precisely what F-01 says has not yet been applied to the new **row**, as opposed to the new
  string.

## 6. Recommendation

## Verdict
