# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v2.8, 404 lines / 61,198 bytes)
**Date:** 2026-08-01
**Iteration:** 1 (fresh Phase R window after the operator reset at `e9f3264`; the v1..v10 round files were removed, so this is a first review of v2.8, not a delta re-review)
**Scope:** Technical lens only — feasibility, implementability, integration risk, threshold declaration, existing-code claim verification. Not product strategy, not test-pyramid choices, not fixture construction.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **AC-1.2's "exactly one executable declaration repo-wide" is decidably false the moment the change ships, because O-11 requires the generated bundles rebuilt in the same commit.** `pdlc/workflows/dist/orchestrate-dev.bundle.js:949` and `pdlc/workflows/dist/orchestrate-queue.bundle.js:924` each contain `const MAX_REVIEW_ROUNDS = 5;` — both **tracked**, both **executable** (the queue bundle inlines orchestrate-dev; the copy the runtime loads is derived from them byte-for-byte), and `build-runtime.mjs --check` plus the CI job *Generated artifacts are in sync* force them to be regenerated in the same commit. So after the change there are **three** declarations of the budget's value in executable code, not one, and no implementation can make it one. O-13(b)'s four classification buckets (*the declaration* / *derived from it* / *prose updated in the same commit* / *pinned non-budget literal*) have no bucket that both admits a generated copy and preserves AC-1.2's normative sentence "*any code needing it — pipeline or test — reads that declaration, not restating it*": a bundle copy restates it. This is not deferrable to FSPEC/TSPEC — §8 and O-14 state that no downstream document amends an AC, and the enumeration is this criterion's own declared observable. **Cheap fix, one clause:** exclude generated artifacts from the quantifier by name (e.g. "*excluding artifacts under `pdlc/workflows/dist/` and the untracked consumer copy under `.claude/workflows/`, which `build-runtime.mjs` derives from the declaration*"), or add a fifth O-13(b) class *generated copy, rebuilt by O-11* and say it is outside AC-1.2's count. | AC-1.2, O-11, O-13(b), §6 `MAX_REVIEW_ROUNDS` row |
| F-02 | Medium | Local | **The zero-round *creating* halt still dispatches a full authoring agent, and the REQ never says what that agent is supposed to write.** AC-1.4 exempts only a halt that **finds an existing post-mortem**; a halt finding none authors one. AC-1.5(1) makes the zero-round halt the commonest new case, and R-13 makes it the **first** thing every in-flight branch does at the landing commit. At the citation baseline the halt path dispatches the optimizer with a prompt demanding *Phase, Iterations, **Reviewers**, **Pattern of Disagreement**, **Best-Guess Root Cause**, Recommendation* and instructing it to read every cross-review file for the phase (M-1c's second site, `9486c81:1727`). On a zero-round entry no reviewer was dispatched and no round of *this* window exists, so the agent is required to produce a disagreement analysis and a root cause over rounds it never observed, from cross-reviews belonging to the **previous** window. That document is exactly the artifact the operator must read before writing the `RESOLVED: yes` that AC-1.5(3)'s escape hatch depends on, and §2's "the cheapest entry stays cheap" cost claim is stated **only** for the existing-post-mortem branch, so it does not cover the commonest first occurrence. AC-1.3 fixes only the Iterations render. State the disposition: either the zero-round creating halt authors as today and the vacuity is accepted as a named `NB-*` non-goal, or AC-1.4/O-14 say what the authored sections contain when `rounds run` is `0`. | AC-1.4, AC-1.5(1), AC-1.3, §2, R-13 |
| F-03 | Medium | Local | **AC-1.4 clause 3 and O-14 disagree about who renders the Iterations section on the *creating* halt, and the disagreement decides whether O-10's equality leg asserts anything.** Clause 3 says "*a creating halt's **authored** section carries it*" — i.e. an LLM following the post-mortem prompt must emit §6's byte-exact render — while AC-1.3 says "*O-14 produces it on **both** the creating halt and the re-halt*" and describes it as "*a two-integer render the loop computes*" for the re-halt only. Under the first reading, the production render comes from an agent, so O-10's mandated **equality** leg on the creating-halt fixture is green only because a test double returns canned text: the assertion is unfalsifiable with respect to production, which is exactly what DC-03 forbids for a load-bearing assertion, and the operator-visible string AC-1.3 exists to pin is not in fact pinned. Under the second reading (the loop writes or overwrites that one section after the authoring dispatch is confirmed, on both paths) the assertion is load-bearing and the criterion is implementable. Say which; the two ship differently. | AC-1.3, AC-1.4 clause 3, O-14, O-10 leg (i) |
| F-04 | Medium | Local | **A zero-round halt returns reviewer verdicts derived from reviewers that were never dispatched, and no criterion names them.** AC-1.3 protects three quantities (the Iterations section, the phase record, the returned `iterations`) and row C fixes the run-report row's cells, but the halt return also carries a per-reviewer verdict list built from the last round's reviewer results (`9486c81:1766`–`:1769`, the `lastResults` array immediately above `iterations: MAX_REVIEW_ROUNDS` at `:1773`). This path is **unreachable at HEAD** — the loop-top cap check cannot fire on the first pass because `endIndex ≥ startIndex` always — and AC-1.5(1) is precisely what makes it reachable, with both reviewer results still unset. So the new commonest halt reports two named reviewers with verdicts parsed from nothing, in the same report as row C's deliberately empty cells. That is the same conflation-at-the-operator's-eye defect AC-1.3 was written to prevent, on the path this REQ creates. Name the quantity (add it to AC-1.3's list, or to row C's cell-by-cell statement, or hand it to O-14 explicitly) rather than leaving it to the implementer to discover. | AC-1.3, AC-1.5(1) row C, O-14 |
| F-05 | Medium | Cross-Feature | **The document is 242 bytes under the hard ceiling, so this round's fixes cannot be appended.** 61,198 of 61,440 bytes (99.6%), past `check-req-size.sh`'s 90% soft threshold (55,296 B) — the class-D signature the predecessor post-mortem's root cause 2 identifies, where proximity to the ceiling silently determines *how* every finding may be fixed and one compression pass had already deleted a reason rather than a restatement. Filing it Low again would repeat the recorded mistake. Two relocation targets are named by the document itself and cost nothing: **§4.1's two-row table** is explicitly a subset of baseline §3.2 ("*the full map is baseline §3.2, keeping here the two rows…*"), and **§6's `WINDOW-START:` / `WINDOW-RESUMED:` rows** are explicitly restatements of baseline §3/§3.1 kept only to assert in-force status — both can become a one-line citation plus an in-force sentence, freeing >1.5 KB. Per R-5 / pm-author rule 5e, relocate **before** addressing F-01…F-04, not after. | §4.1, §6, R-5, root cause 2 |
| F-06 | Low | Local | **§3.1's queue-driver claim is an existing-code claim with no citation and no `M-*` home.** "*The driver picks the lowest `Order` among `pending` rows and defers a dependency **absent from the table** to readiness triage, so row 13 is pickable and row 17 precedes row 18*" is a control-flow claim about `orchestrate-queue.js`, which NB-4's "cite only by `M-*` id" discipline does not cover (it is scoped to `orchestrate-dev.js`). **I verified it and it holds** — `precheckDependencies` blocks only on a dependency *present* in the queue with a non-`done` status and explicitly falls through to triage for one that is absent or `done`, the candidate walk is in `Order` order, and rows 6/7/9 are `blocked` rather than `pending` so they do not precede row 10 — but the next reader has to re-derive it, which is the tax the `M-*` convention exists to remove. Either cite the function by name or add a baseline row. | §3.1, R-14 |
| F-07 | Low | Local | **§6's "exactly one operator string" and §4's ownership of S-4 read inconsistently.** §6 states this REQ mints "*exactly **one** operator string: AC-1.3's Iterations render*", while §4 lists **S-4** `budget-exhausted: …` as **owned**, and catalogue §2 fixes its render as a three-slot operator-facing string that row C puts in the `notice` cell and the operator reads on every budget halt. The intended reading is presumably "one string *not already rendered by the catalogue*"; as written, a reviewer of the successor pair cannot tell whether S-4's render may be amended here. One qualifier settles it. | §6 preamble, §4 S-4 row |

## Questions

## Positive Observations

## Recommendation
