# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v2.6)
**Date:** 2026-08-01
**Iteration:** 1
**Scope:** REQ-RCV-01 — the round window, the budget constant, the halt path's reset region, and the clearance accounting. Technical lens only.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **AC-1.2's "one constant, one budget" is already false at HEAD, and AC-1.3's "over the constant, never the literal" is not achievable.** `MAX_REVIEW_ROUNDS` is deliberately **not exported** (`pdlc/workflows/__tests__/roundDerivation.test.js:57`, *"The constant is deliberately **not exported**"*), so the test suite carries its own copy: `pdlc/workflows/__tests__/pacingWrapper.test.js:77` declares `const MAX_REVIEW_ROUNDS = 5;`. That is a **second declared budget**, which AC-1.2 says must not exist and which will silently disagree with the module the moment 5 becomes 3. AC-1.3 then requires the budget and `iterations` to be *"asserted **over the constant**, never the literal `3`"* — unachievable from `__tests__/` without either exporting the constant (contradicting the cited deliberate non-export) or keeping the duplicate (contradicting AC-1.2). §8 assigns neither decision: O-11 covers only rebuilding `dist/`. **The blast radius is also unenumerated.** A repo-wide grep of `pdlc/workflows/__tests__/` finds width-5 assumptions in three files that a 5→3 change reds: `reviewLoop.test.js:140, 171, 173, 174, 214, 238, 510, 511, 512` (`iterations: 5`, `reviewerPairCount`/`optimizerCount` `toBe(5)`); `pacingWrapper.test.js:77, 1455, 1489, 1501` — including an AT whose **title** encodes the width (`RLH-AT-54: … admits rounds 4 through 8 … no message claims 'after 5 iterations'`); `roundDerivation.test.js:57, 556` (the window invariant `endIndex === startIndex + MAX_REVIEW_ROUNDS - 1`). The REQ needs an obligation that owns (a) the export-vs-duplicate decision and (b) the enumeration, or the first implementer discovers both mid-PLAN. | AC-1.2, AC-1.3, §8 (O-11) |
| F-02 | Medium | Local | **AC-1.5(1)'s `forcePhases` clause states a mechanism its own AC-1.4 falsifies.** The text reads *"a second force changes nothing, because at `A = H` a marker grants nothing"*. Trace the stated sequence: the first forced entry halts on the budget path, so AC-1.4 clause 1 leaves `H = 1` and clause 2 **strips** the `RESOLVED:` line. At the second force the region is therefore `H = 1, A = 0` — i.e. `A < H`, **not** `A = H`. What actually stops the second force is the shipped step-G refusal on a now-unresolved post-mortem (M-7a), which `forcePhases` does not override (the REQ says so two lines earlier). `A = H` is a *different*, later state (after a clearance has been granted and consumed). O-10 owes a test *"a second force changing nothing"*; a test author deriving the oracle from the stated reason will assert the wrong region state and pin a behaviour the design does not have. Fix the causal clause, not the outcome — the outcome is right. | AC-1.5(1), AC-1.4 clauses 1–2, O-10 |
| F-03 | Medium | Cross-Feature | **The queue-distance premise under X-06/R-14 is falsified by the shipped queue driver, and it is a paired-edge fact both halves must agree on.** §3.1 and R-14 rest on *"`docs/_queue/QUEUE.md`'s stated net pickup order is **10 → 12 → 18**"* and *"a whole intervening feature"*. `selectNextPending` (`pdlc/workflows/orchestrate-queue.js:382`) picks the **lowest `Order` among `pending`**, and `precheckDependencies` (`:429`) blocks only on a dependency **present in the table** and not `done` — a dependency **absent** from the table is explicitly *"inconclusive here; defer to triage"*. Two consequences: (i) `pdlc-workflow-distribution` was removed from QUEUE.md as merged, so row **13** `pdlc-merge-phase` (`ready: true`, its only dep absent-and-merged) is pickable immediately after row 12; (ii) row **17** `pdlc-rcv-fixed-point-stop` depends on row 10 alone and `17 < 18`, so it is picked **before** row 18 — QUEUE.md's own gloss *"with 18 ahead of 17 by `Order`"* inverts the comparison. Real net order after row 10 is **12 → 13 → 17 → 18**. This matters to the requirement, not just to the prose: row 17 is the REQ that **emits S-11** (X-05), so AC-1.5(5)'s `WINDOW-RESUMED:` answering line — a machine write into the reset region — becomes reachable **while the validation conjunct is still unwired**, which R-14's *"operator-caused and no wider than HEAD's"* does not cover. Per `pdlc-rcv-split.md` §5 both ends must agree on *"the queue distance between the two rows"*; `REQ-RCV-07` R-16 carries the identical `10 → 12 → 18` and must be revised in the **same revision**. | §3.1 (X-06 row, *Consequence for sequencing*), R-14; paired `REQ-RCV-07` X-07/R-16 |
| F-04 | Medium | Local | **AC-1.3's second quantity has no downstream owner.** AC-1.3 requires the post-mortem's Iterations section to state *"the **rounds this entry ran** — `0` there"* alongside the effective budget. At HEAD that section is a fixed prompt literal, `` `Iterations (${MAX_REVIEW_ROUNDS} — limit reached)` `` (M-1c), with no slot for a second number and a *"limit reached"* phrase that is false on a zero-round entry. §8's table assigns O-5 (region survival), O-9 (the prompt's reset-region clause only — explicitly *"Not the mechanism"*), O-12 (`W` resolution), O-10 (properties) and O-11 (`dist/`). **None owns producing this figure.** §8's own preamble promises that a *"no oracle / no seam"* finding is answered by an obligation; this AC has an O-10 assertion with no producer behind it. | AC-1.3, §8 |
| F-05 | Medium | Local | **The commonest new halt spends an authoring dispatch and overwrites the operator's own post-mortem, and nothing states what it should say.** AC-1.5(1) makes the zero-round budget halt *"the commonest new case"*; AC-1.4 requires it to halt *"the way it halts today"*, which at HEAD is an agent dispatch carrying a bare `Write ${postmortemPath}.` plus a section list (M-7e) at a **fixed, unversioned** path the REQ itself flags as written twice. So on the cheapest possible entry — zero reviewers dispatched, zero rounds — the pipeline pays a full Opus authoring dispatch to write `Reviewers`, `Pattern of Disagreement` and `Best-Guess Root Cause` sections for which this entry has **no** content, and in doing so replaces the `Recommendation` the operator just acted on. AC-1.4 preserves only the reset region; nothing else in the file is protected, and §2's cost claim does not account for the dispatch. Either state that the zero-round halt reuses/appends rather than re-authoring, or state what the body must say and add the obligation that owns it. | AC-1.5(1), AC-1.4, §2 |
| F-06 | Low | Local | **A shipped-table claim that its cited `M-*` fact does not carry, and that the next paragraph contradicts.** AC-1.1 scopes itself to *"the phases of `PHASE_DISPATCH`: R/REQ, F/FSPEC, T/TSPEC, D/DECISIONS, P/PLAN, PR/PROPERTIES (M-1d)"*. `PHASE_DISPATCH` (`pdlc/workflows/orchestrate-dev.js:108`) also contains **`CR`**, which the immediately following *Scope* paragraph excludes — so the enumeration is `PHASE_DISPATCH` **minus CR**, not *"the phases of `PHASE_DISPATCH`"*. M-1d is a fact about `deriveRoundWindow` and does not establish that table's membership. NB-4 forbids exactly this shape (a claim about shipped code not backed by an `M-*`); the fix is one qualifier plus either a new baseline fact or a citation that carries it. | AC-1.1, NB-4 |
| F-07 | Low | Cross-Feature | **§4.1 claims a durable home without naming its documented deleter.** `POSTMORTEM-{phase}-{feature}.md` is the durable home of `W`, `H` and `A` — but `harvest-learnings` **deletes** `POSTMORTEM-*` files once `LEARNINGS-{feature}.md` exists (`CLAUDE.md`, *Artifact convention*; the `guard-harvest-before-delete` hook permits precisely that). Benign inside one feature, since Phase H runs after every review phase — but the claim is stated unqualified, and a post-harvest `forcePhases` re-entry silently restarts at `W = 1`, `H = A = 0` with no reset region and no S-16. One sentence in §4.1 (or an NB-* row) closes it. | §4.1, §7 |
| F-08 | Low | Local | **AC-1.2's observable is quantified over entries its own scope paragraph exempts.** The observable reads *"on **every production entry** the admitted window runs from the window's origin `W` for exactly the budget's number of rounds"*, but AC-1.1's *Scope* paragraph exempts `docType: null` loops (Phase CR, Phase DOD), for which **no `W` exists** — their window is one-past-highest, per invocation. As written the observable is either false or undefined on those entries. Add the scope qualifier, or a second clause stating the untyped observable (window width = the constant, origin unconstrained). | AC-1.2, AC-1.1 *Scope* |

## Questions

| ID | Question |
|----|---------|
| Q-01 | AC-1.3 demands assertions *over the constant*; `MAX_REVIEW_ROUNDS` is deliberately unexported and `pacingWrapper.test.js:77` keeps a private copy. Which resolution does the REQ intend — export the constant, or accept a second declaration and drop AC-1.2's "one constant" claim? Whichever it is, which obligation owns it (F-01)? |
| Q-02 | On the zero-round budget halt (AC-1.5(1)), is the existing post-mortem re-authored, appended to, or left alone apart from the region maintenance AC-1.4 mandates? The path is fixed and unversioned, so the three answers are operator-visibly different (F-05). |
| Q-03 | Given the real pickup order `10 → 12 → 13 → 17 → 18` (F-03), is the interim still acceptable once row 17 ships — i.e. once `WINDOW-RESUMED:` lines are machine-written into regions that nothing validates? If yes, R-14's residual needs restating over that composition; if no, row 18 needs to move ahead of row 17 by `Order`. |
| Q-04 | `MAX_REVIEW_ROUNDS` 5→3 also narrows Phase CR's per-invocation budget (AC-1.1 *Scope*). Is three rounds of Final Codebase Review a deliberate product decision, or a side effect the REQ is accepting because the constant is shared? |

## Positive Observations

- The `M-*`-only citation discipline (NB-4) works: apart from F-06 the document makes no line claim, and the facts it does lean on — `MAX_REVIEW_ROUNDS` at `orchestrate-dev.js:52`, `deriveRoundWindow`'s `Math.max(...indices) + 1` at `:2432`, `windowEnd` at `:2450`, `parseResolvedMarker` at `:1105`, the bare `Write ${postmortemPath}` prompt at `:1936`, the step-G refusal string at `:4246` — all verify at HEAD, with only the line drift the baseline header already warns about.
- N-7's `docType: null` characterisation is **accurate**, not a guess: Phase CR's `reviewLoop` call passes `docType: null` literally (`orchestrate-dev.js:4724`), so AC-1.1's scope boundary rests on a real discriminator the implementation can branch on.
- §4.1's *"there is no in-process-only row"* is the right invariant for this codebase and is actually satisfied — every quantity the gate reads has a branch-derived home, which is what makes the criteria decidable on a resumed invocation (M-1d, M-2f).
- Stating *the region validates* as a named, total, single-valued predicate with a fixed **meaning and failure disposition**, while delegating only the decision procedure, is a clean seam: an implementer can build the two-conjunct interim gate today and add a third conjunct later without re-reading the requirement.
- The counting rule *by line prefix, whatever the value* (AC-1.5(4) clause 4) is the detail that makes `H`/`A` implementable without a parser, and the accompanying argument for why a malformed value must still answer a halt is correct.
- AC-1.5(4)'s ordering argument — record the answering line **before** dispatching, because losing a record of a window *already used* is worse than losing an unused one — is the right fail direction and is justified rather than asserted.

## Recommendation

**Needs revision**

Blocking, in priority order:

1. **F-01** — give AC-1.2/AC-1.3 an owner for the export-vs-duplicate decision and enumerate the width-5 test surface (`reviewLoop.test.js`, `pacingWrapper.test.js`, `roundDerivation.test.js`), or the "one constant, one budget" claim ships false.
2. **F-03** — correct the pickup-order premise in §3.1 and R-14 to the order the driver actually produces, restate R-14's residual over the row-17-before-row-18 composition, and carry the same words into `REQ-RCV-07` X-07/R-16 **within this revision**.
3. **F-02** — replace AC-1.5(1)'s `A = H` justification with the step-G refusal that actually blocks the second force, so O-10's test asserts the real state.
4. **F-04** — add the obligation that owns the post-mortem's "rounds this entry ran" figure.
5. **F-05** — say what the zero-round halt writes, and whether it re-authors the file.

F-06, F-07 and F-08 are one-sentence corrections and are not individually blocking.

## Verdict

VERDICT: Needs revision

