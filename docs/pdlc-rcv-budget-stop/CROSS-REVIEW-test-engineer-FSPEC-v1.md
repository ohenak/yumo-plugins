# Cross-Review: test-engineer — FSPEC

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/FSPEC-pdlc-rcv-budget-stop.md` (v1.0)
**Date:** 2026-08-02
**Iteration:** 1
**Scope:** Local — testability of the FSPEC's named branches and of §11's acceptance tests; fixture derivability; oracle falsifiability. Not reviewed: product framing, architecture, altitude complaints §1.1/§13.2 already route.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **AT-BUD-03 is a test that cannot fail.** At HEAD `pdlc/workflows/orchestrate-dev.js:25` declares `DOD_MAX_ITERATIONS = 3` and `:52` `MAX_REVIEW_ROUNDS = 5`. After this feature ships both values are **3**, so "Phase DOD's admitted round count is unchanged" is satisfied identically whether Phase DOD reads its own declaration or wrongly reads `BUDGET` — the oracle cannot discriminate the defect it exists to catch. Its `When` ("the budget changes from 5 to 3") is also not an executable stimulus post-ship: the transition happens once, in the diff, not in a test. B-BUD-3/BR-15 therefore have **no** falsifiable coverage. Fix at FSPEC altitude: state the discriminating observable — Phase DOD's bound resolves from a declaration that is **not** the budget declaration (B-BUD-5 already gives it a class: *deliberately pinned non-budget literal, and says so at its site*), so the assertion is structural (the two declarations are distinct and DOD's admitted count tracks **its own**), or behavioural under a **varied** `BUDGET` — which is only writable if the declaration is reachable from test code (O-13). Either way the current Given/When/Then must be replaced, not merely rephrased | §11.1 AT-BUD-03; §3.1 B-BUD-3; BR-15 |
| F-02 | Medium | Local | **§12's answer (a) is a false universal, and B-CLR-2 is the counterexample.** "A granting entry can never immediately re-halt on the budget: the granting line carries `N = max(D, W)`, which becomes the new `W`" holds for B-CLR-1 and B-CLR-3 only. B-CLR-2 writes `WINDOW-RESUMED: {W}` and leaves `W` **unchanged** (§6.1, §5.2 — `W` reads `WINDOW-START:` values only). An S-11 halt taken on round `E` — the last round of the window — therefore resolves `D = E + 1 > E` on the clearing entry: the clearance is **consumed** (`A = H`), zero rounds are admitted, the entry takes B-WIN-2's budget halt, appends a second `HALT-REASON:` and strips the operator's marker. One hand-written `RESOLVED: yes` buys nothing and costs a second. This is target state (S-11 unreachable at this ship, §6.1), but the FSPEC states the answer unconditionally and `pdlc-rcv-fixed-point-stop` will inherit it. Either scope answer (a) to the two `WINDOW-START:` branches and disposition the resumed-into-exhausted-window case explicitly, or state that the resume is refused/no-ops when `D > E`. AT-CLR-02 needs the corresponding leg either way — its current `Then` ("rounds already spent stay spent") passes on the pathological path | §12 answered-(a); §6.1 B-CLR-2; §11.4 AT-CLR-02 |
| F-03 | Medium | Local | **E-1's migration disposition is incomplete for the case that is on disk in this repo today, and no AT covers it.** E-1 covers a document at 4–5 rounds and says "the escape is the ordinary clearance". But a **surviving pre-feature post-mortem** — `## Iterations (5 — limit reached)`, a readable unfenced `RESOLVED: yes`, and **no** `## Reset Region` — reads `H = A = 0` (B-REG-1), so the gate is closed by `A < H` being false: B-CLR-4 grants **nothing**, the entry proceeds to §4, admits zero rounds, budget-halts, creates the region with `H = 1` and **strips the marker**. The operator must write `RESOLVED: yes` **twice** to get one window. `docs/pdlc-rcv-budget-stop/POSTMORTEM-R-pdlc-rcv-budget-stop.md` is exactly this shape at HEAD, so this is the ordinary migration path, not a corner. §7.3's "Accepted cost, stated" is scoped to *a creating halt whose clause 1-and-2 update fails* (E-14) and does not reach it. Ask: state the disposition in E-1 (accepted, or the marker is honoured on a region-less post-mortem) and add one acceptance test for it | §10 E-1; §6.1 B-CLR-4; §5.3 B-REG-1 |
| F-04 | Medium | Local | **AT-REG-07's zero-consultation oracle does not falsify what §5.4/B-REG-7 says it falsifies.** The claim is that a count of **0 consultations** — as distinct from an absence of effect — is what rules out "wired with an ad-hoc interim procedure". At this ship there is no *region validates* callable to instrument (it is `REQ-RCV-07` AC-7.1's), and an ad-hoc inline interim procedure would not call it either — so a 0-count over a symbol that does not exist is satisfied by every implementation, including the one it is meant to exclude. As written the test degenerates into "no `reset-region-corrupt` notice and no refusal", an **absence-only** oracle. A falsifiable substitute exists at FSPEC altitude and should be named: over a family of regions that **would** fail validation at target state, positively assert that the entry takes the *same* branch as an equivalent well-formed region (grants, dispatches ≥ 1, writes the same answering line) — a positive-presence conjunct — plus a structural assertion that the enumeration of consultation sites is empty. Keep the fixture-construction to PROPERTIES (O-10); what must change here is the claimed observable | §5.4 B-REG-7; §11.3 AT-REG-07 |
| F-05 | Medium | Local | **AT-CLR-04's fixture is underspecified in a way that changes which branch it observes.** The Given pins `A = H = 1` and a readable marker but not `W`, `D` or that the window is open. If the window is exhausted, the *first* entry budget-halts and strips the marker (§7.2 clause 2), so the "entered again" leg meets the **shipped step-G refusal** (B-CLR-5), not B-CLR-4 — and "nothing appended" is then false, because the first entry appended a `HALT-REASON:`. I cannot write this test without asking which. Pin the window open in the Given (e.g. `W = 1`, highest existing round 1) so both entries reach the gate and the "one clearance grants exactly one window" claim is the thing under test | §11.4 AT-CLR-04 |
| F-06 | Low | Local | **§8.3 does not say what run-report row, if any, the shipped step-G refusal produces**, and row B is closed at three sources plus the target-state variant. AT-CLR-05 and AT-WIN-07 therefore have no stated report-row conjunct: a test author cannot derive whether to assert row B, or the absence of any row. One sentence in §8.3 ("the shipped step-G refusal emits no row; it is not row B") closes it | §8.3 B-RPT-6; §11.4 AT-CLR-05; §11.2 AT-WIN-07 |
| F-07 | Low | Local | **AT-RPT-06 discriminates two of the three `unconfirmable-append` sources.** It runs AT-HALT-04's and AT-HALT-05's fixtures; AT-CLR-07's answering-line source is asserted only in its own row. Since §8.3's whole claim is that the three are distinguished by the ❌ text and **never** by `notice` (empty on all three), the three-way comparison should be one row's `Then`, so a rendering collapse between `answering line` and `halt line` is caught by a test whose purpose is the discrimination | §11.6 AT-RPT-06; §8.3 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | **Row C is one row — but is it the only row the report carries for that entry?** §8.2 fixes row C cell by cell, and catalogue §3 says the report carries *one row per round*. A zero-round entry dispatched nothing, so row C's `round` cell (`S`) names a round that does not exist on the branch. Does the report for that entry carry **only** row C, or does it also re-render rows for the rounds earlier invocations ran (which are on disk and derivable)? AT-RPT-04 asserts the cells of one row and is silent on the row count, so a fixture author has to guess. A stated default is enough. |
| Q-02 | **Clause 3 locates the *first* heading beginning `Iterations` (B-RPT-2). What happens to a second one?** A pre-feature post-mortem plus an agent-written heading can leave two. The equality read-back confirms the located heading only, so a stale second heading survives and the operator can read `## Iterations (5 — limit reached)` below the fresh render. Is that accepted (and therefore something AT-HALT-03's sibling should pin as *byte-unchanged elsewhere*), or should the render be unique? |
| Q-03 | **AT-HALT-02 asserts byte-equality "with clauses 1, 2 and 3's edits applied".** Is the expected file constructed by the test author (a checked-in golden) or derived in-test by applying the three edits? The first is what makes the oracle falsifiable against a re-authoring regression; the second re-implements the production transform in the test and cannot fail. I read the FSPEC as intending the first — worth one word, since PROPERTIES will inherit the choice. |

## Positive Observations

- **§11 discharges DC-05 mechanically, and I checked it rather than trusting it.** Every `B-*` id named anywhere in the document has at least one row in §11: B-BUD-1…5, B-WIN-1…7, B-REG-1…7, B-CLR-1…7, B-HALT-1…9, B-RPT-1…6, B-PMT-1…3. No orphan branch, no orphan test.
- **The Iterations oracle is an equality on a single line, and the FSPEC says why.** §8.1's "one line — the heading's own text, not a heading plus a body line — so an oracle over it has a single target and can be an **equality**, not a substring match that any rendering satisfies" is precisely the property that makes AT-RPT-01/02 falsifiable, and B-RPT-1's "it is the loop's output, not an agent's" is what makes it falsifiable *against production* rather than against a prompt.
- **The refusal legs carry positive conjuncts, not absence-only ones.** AT-CLR-07, AT-HALT-04 and AT-HALT-05 each assert an exact ❌ string, an exact empty `notice`, both counts unmoved, and — where it applies — *nothing stripped* and *this entry's render present*. That is the four-conjunct shape I asked for at REQ v3/v4, carried into FSPEC without dilution.
- **Confirmations are content reads, and §7.3 states why an existence check is insufficient** ("on a re-halt the file always exists, so such a check passes whether or not the line landed — and that is the path that matters"). That is the builder-not-wired failure mode argued at the right altitude.
- **The one-update rule over clauses 1 and 2 removes a state instead of dispositioning it**, and BR-12 states the invariant as an unreachable state. An oracle can attack it directly by fault-injecting the strip and asserting the halt line is also absent.
- **Fixtures are largely derivable today.** AT-REG-01…06, AT-CLR-01…03/05…07, AT-HALT-01…09 and AT-RPT-01…05 each pin the region contents, the marker, the highest existing round and the expected observable well enough that I could write the fixture now without a clarifying question. F-05 is the one exception.

## Recommendation

## Verdict
