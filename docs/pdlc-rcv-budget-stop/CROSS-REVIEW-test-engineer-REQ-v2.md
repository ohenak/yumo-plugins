# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v2.9)
**Date:** 2026-08-01
**Iteration:** 2 (delta re-review of v2.8 → v2.9; baseline `993b34b`, the commit carrying my v1)
**Scope:** resolution of my v1 findings, plus the changed sections only — AC-1.1 through AC-1.5(1), §2, §3.1, §4.1, §6, NB-3/NB-6, O-10, O-13, O-14, R-14, §10, and the material relocated to `pdlc-rcv-split.md` §5.4/§5.6 and `pdlc-rcv-baseline.md` §3.1/§3.2. Unchanged sections I approved in v1 are not re-litigated.

## Prior findings — disposition

| v1 | Severity | Status | Evidence in v2.9 |
|----|----------|--------|------------------|
| F-01 | High | **Resolved** | AC-1.3 now says the render is *"the loop's, not an agent's"* and *"the loop computes and writes it on every halt in scope, creating halt and re-halt alike"*; AC-1.4 clause 3 carries the same rule with *"Not the agent's"* stated explicitly, and O-14 threads the quantities to the loop's post-write step. O-10 leg (i) is now honest: the equality is over production's output on all three fixtures, not a double's canned text. |
| F-02 | Medium | **Resolved** | AC-1.4 clause 3 declares the anchor — *"the first top-level heading whose text begins `Iterations`, case-sensitively, outside any fenced block"* — which matches the shipped shape (`## Iterations (5 — limit reached)`, `orchestrate-dev.js:1938`), so R-13's migration fixture is located. The not-found disposition appends rather than fails, and fixes the position (above `## Reset Region` if that is last) so region parsing is unaffected. |
| F-03 | Medium | **Resolved** | AC-1.1's `Then` is now *"round 3 counted from the window's origin `W`"* with `W = 1` as the no-reset default, and the `W = 4` ⇒ halt-on-7 case stated inline. AC-1.1 and AC-1.5(1) now give one predicate; O-10 leg (4) is consistent with both. |
| F-04 | Medium | **Resolved in kind** — see F-01 below for what the fix opened | AC-1.4 clause 1 now carries a confirmation obligation, correctly shaped (*"a confirmation that this halt's line is present in the region, not that the file exists"* — the distinction that mattered on the re-halt path), a fail-closed disposition, and the terminal-failure argument spelled out. O-10 gains leg (iii). |
| F-05 | Low | **Resolved** | AC-1.2 now requires the enumeration be *"compared against a repo scan by machine, not by a human reading a checklist"*, and O-13(b) and §6's row repeat it. The added generated-artifact exclusion is the right call: those cannot disagree with the declaration. |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **The fix for v1's F-04 created a third refusal path with no declared operator surface, and O-10's leg for it asserts a notice the paired REQ declares empty.** AC-1.4 clause 1's new fail-closed disposition — *"the halt is not recorded as taken, the entry refuses the phase and reports it"* — is a **refusing entry**, and this family states the report of a refusing entry as **row B**, whose variants are enumerated at `REQ-RCV-07` AC-7.6 as exactly two: the **validation-failure** variant (AC-7.1 step 4) and the **unconfirmable-append** variant (AC-7.5, the *answering* line). A lost `HALT-REASON:` append is neither, so at HEAD it has **no `notice` value, no ❌ phase-row text and no recovery text**. Three things break at once, and only the third is cosmetic: (a) split §5.4 leg (iii) asserts the entry emits *"exactly one notice"*, which is **unwritable** — S-16 is explicitly not it (*"an IO fault of the loop, not a state of the region … that enum stays closed at three"*, catalogue §4), the catalogue is closed to FSPEC (§4), and the nearest declared analogue, AC-7.6's unconfirmable-append variant, sets `notice` **empty**; a test written from §5.4 today would assert the opposite of what the paired document says; (b) AC-1.5(1)'s complementarity claim — *"B's entry takes no halt, C's takes one, so B never carries S-4 and C never carries S-16"* — no longer covers every refusing entry, so the two rows are not exhaustive and an implementer has no row to emit; (c) §10 asserts *"v2.9 carries no change to that edge"*, which is false: AC-1.4 clause 1 now refuses, and `REQ-RCV-07` AC-7.6's *Given* still reads *"an entry that refuses under AC-7.1 step 4 or AC-7.5"*. Not deferrable — this is which surface the operator reads, and the deferral NB-3 offers (*"which strings a refusing entry emits"*) points at a REQ that does not know this entry exists. **Fix, one clause plus its paired edit:** either state that this refusal emits AC-7.5's unconfirmable-append variant with `{path}` the post-mortem (and correct §5.4 leg (iii) to `notice` **empty**), or declare a third variant here and carry it into `REQ-RCV-07` AC-7.6 in the same revision, per the paired-edge rule. | AC-1.4 clause 1, AC-1.5(1) row C, `REQ-RCV-07` AC-7.6, catalogue §4, split §5.4 leg (iii), §10 |
| F-02 | Medium | Local | **The Iterations write is the only one of the three loop-written outputs with no confirmation obligation and no failure disposition, and its failure silently reproduces exactly the conflation AC-1.3 exists to prohibit.** v2.9 gave the region append one (AC-1.4 clause 1) and AC-1.5(4)'s answering line already had one; clause 3 has neither. Its only near-miss is *"after the write it confirms"*, which reads as an ordering statement about the **post-mortem** write (AC-1.4's opening), not a confirmation of the section — and clause 3 goes on to make a categorical promise it cannot keep on that path: *"the operator always reads this halt's two integers, never a stale or absent line."* On a **re-halt** a failed section rewrite leaves the **previous** halt's `rounds run {k}`, `k > 0`, on an entry that dispatched zero rounds — the precise defect AC-1.3 was written for — and nothing reports it. NB-6 raises the stakes rather than lowering them: it accepts thin authored sections *because* the operator still gets *"the two things the `RESOLVED: yes` decision needs, both loop-written and pinned"*, and this is one of the two. O-10 has a leg for the lost append (iii) and none here, so the failure is neither dispositioned nor asserted. State the disposition at the same altitude as clause 1's — including whether it refuses (the recoverable choice, given clause 1 already refuses on the same halt) or reports and proceeds — and give O-10 the leg. | AC-1.4 clause 3, AC-1.3, NB-6, O-10, O-14 |
| F-03 | Low | Local | **Whether §6's render is the heading's own text or a line beneath a `## Iterations` heading is under-determined, so leg (i)'s equality has two possible targets across its three fixtures.** The found case is located by *"the first top-level heading whose text begins `Iterations`"*, and at HEAD that heading **is** the literal (`## Iterations (5 — limit reached)`), so the natural reading is that the render replaces the heading text. The not-found case then says the loop appends *"a new top-level `## Iterations` heading **carrying** §6's render"* — which reads the other way, heading plus body. AC-1.3 deliberately raised the oracle from substring to **equality**, so the target line's identity is load-bearing: a test writer must ask which shape before writing leg (i)'s third fixture or leg (ii)'s expected file. Filed **Low** because O-14 already owns producing the render and can fix placement without varying what the operator reads — but O-14 currently declares itself *"mechanism only: this obligation does not vary the outcome"*, so route it explicitly rather than leaving it to be inferred. | AC-1.4 clause 3, AC-1.3, §6 Iterations row, O-14, split §5.4 leg (i) |

## Questions

## Positive Observations

## Recommendation

## Verdict
