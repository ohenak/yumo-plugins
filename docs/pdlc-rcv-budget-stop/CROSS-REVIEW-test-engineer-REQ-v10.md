# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v2.4, 442 lines / 55,273 bytes)
**Scope:** Testing lens only, **delta re-review**. Verification that each v9 finding is closed, plus a scan of the text added or rewritten since v9 (`26d886f..HEAD`, 11 commits touching the REQ) for new issues. Sections unchanged since v1…v9 and already approved are not re-litigated. Product strategy, architecture and prose style remain out of scope.
**Reviewed range:** `26d886f..HEAD` — +51 / −64 lines
**Date:** 2026-08-01
**Iteration:** 10

## Disposition of v9 findings

Both are **closed**, and F-41 is closed in the exact form I proposed, at the site I named. I checked
the closure against the fixture arithmetic rather than the wording, and I checked the round-9
relocations against their four destinations. F-42 is re-filed once, as F-46, because the headroom it
tracks fell again.

| v9 | Severity | Status | Where it was answered |
|----|----------|--------|----------------------|
| F-41 | Medium | **Closed as proposed, with the ordering half settled by the same clause** | O-10's contract leg now reads *"the interim composition calling it **exactly 0 times** — **asserted on *leg 1's* fixture**, the only one that defeats the two earlier conjuncts and therefore the only one a wired implementation would answer differently (on leg 2's region `A < H` is false, so a wired gate short-circuits and never reaches the seam either — 0 calls under every wiring, an oracle that cannot fail). Naming leg 1 also settles the evaluation-order half: on that fixture every conjunct is reached under any order."* I re-derived it: leg 1 is `H = 1`, `A = 0`, `RESOLVED: yes` readable ⇒ conjuncts 1 and 2 both hold, so the gate reaches the third and a wired implementation calls the seam ≥ 1 against the interim's 0 — the assertion inverts at row 18 and is falsifiable today. The parenthetical also records *why* leg 2 was rejected, which is what stops a later pass from "simplifying" the leg back onto the vacuous fixture. |
| F-42 | Low / Process | **Acknowledged in substance and acted on — four relocations landed — but the headroom fell again; re-filed once as F-46** | The round moved four blocks out (AC-1.5(4)'s conjunct arithmetic → split §5.2; the refusal-is-not-a-halt argument → split §5.3; §6's row notes → baseline §3.1; §4.1's durability map → baseline §3.2), including the ~800-byte block I nominated as the next relocation. I read all four destinations. Net effect on the gate this finding tracks: 55,238 → 55,273 bytes — headroom **58 → 23**, the sixth consecutive round to finish inside a few hundred bytes of the size gate and the second running in which the file **grew** while shedding several KB. |

## Findings

Four, of which one is High and one Medium. Both blocking findings are in **text added this round**,
and both are about the same thing from opposite ends: the two new normative statements about the
*validate* seam and about malformed `WINDOW-START:` values each say something a test author would
derive a test from, and each is contradicted by another statement in the same revision. Nothing
carried over from v1…v9 is reopened.

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-43 | High | Local | **O-12's newly pinned seam contract — *arity 2, boolean return* — cannot produce AC-1.5(4)'s stated failure disposition, and O-10 asserts that signature as a stable contract leg, so the one seam property this REQ keeps pins a signature row 18 must change.** O-12 now says the TSPEC must fix *"the **injected *validate* seam** … its parameter name … its **arity 2** and its **boolean return** (AC-1.5(4) fixes the predicate as total and single-valued over the region and the branch listing)"*, and O-10's contract leg asserts *"the injected function's signature (O-12)"* on the production path, alongside the claim that legs 1 and 2 *"stay true after row 18 wires the conjunct — so neither is deleted at that commit"*. Derive the target state from AC-1.5(4)'s false bullet (`:298`–`:304`): when the predicate is false, *"the run report emits exactly one `reset-region-corrupt: {reason}` notice (S-16, rendered per catalogue §2)"*, and `{reason}` is one of the three the sibling enumerates (`REQ-RCV-07` §6 — `invalid-window-start`, and its two siblings), selected by AC-7.1 step 4. **A boolean cannot carry `{reason}`.** Take either reading of who emits: (a) the **caller** builds the run report — then a `false` tells it the region is corrupt but not which reason to render, so the notice is unrenderable and the caller would have to re-derive the reason, contradicting *"its sole emitter and its `{reason}` selection are `REQ-RCV-07` AC-7.1 step 4's"*; or (b) the **seam** emits — then arity 2 (region, branch listing) gives it no report sink to emit into, and the return type is beside the point. Under both, the row-18 implementation needs a wider signature than the one this REQ pins, so the contract leg asserted today **breaks** at row 18 rather than inverting — and O-10 explicitly promises the opposite. Note this is the failure mode leg 3 was carefully protected against: leg 3 *is* marked interim-only, *"so that commit replaces it rather than deleting an assertion it cannot explain"*. The signature leg is not marked, and it is the one that will not survive. **Repair, either of two, both ≲ 60 bytes:** (i) state the return as a small result carrying the verdict and, on failure, AC-7.1 step 4's `{reason}` from the closed set of three — which is what the caller needs to render S-16 and is single-valued exactly as DC-01 requires; or (ii) keep the boolean and mark the contract leg **interim-only, inverting at row 18** in leg 3's own words, so the assertion is replaced rather than silently broken. (i) is the better repair because it makes the target state reachable without a second signature change; (ii) is the honest one if the signature is genuinely undecided here. Whichever is chosen, the signature is a **cross-document constraint on `REQ-RCV-07`'s implementation** and that REQ's O-12 does not carry it (I checked HEAD: it specifies the append-confirmation obligation only) — the paired-edge rule at §10 covers X-06/R-14 ↔ X-07/R-16 and does not reach O-12, so this one has to be carried deliberately or it will be discovered at row 18. | §8 O-12 (*"its arity 2 and its boolean return"*), §8 O-10 (*"the injected function's signature (O-12)"*, *"stay true after row 18"*), §5 AC-1.5(4) *when it is **false*** bullet (`:298`–`:304`), `REQ-RCV-07` §6 reason table and O-12 |
| F-44 | Medium | Local | **§6's new S-13 grammar sentence and O-10 leg 3's counting rule give opposite values for `A` on leg 3's own fixture, and one of the two readings is fail-open.** §6 now states, of `WINDOW-START: {N}`: *"a line whose value is not one **is not a `WINDOW-START: {N}` line** and contributes no value"*. Leg 3 states of the same fixture: *"the malformed line **still counts toward `A`** (the counts are over line prefixes, not values), so `A = H = 1` and no clearance is observed"*. Run the fixture (`H = 1`, one `WINDOW-START: abc`, `RESOLVED: yes` readable) under each: under leg 3's rule `A = 1`, `A < H` is false, the gate grants nothing and the asserted oracle is *no answering line, counts unmoved*. Under §6's sentence read plainly the malformed line is **not** a `WINDOW-START:` line at all, so `A = 0 < H = 1`, all decidable conjuncts hold and the interim gate **grants** — an answering line is appended and both counts move. **The two readings produce mutually exclusive oracles on the same fixture, and the §6 reading is the fail-open one** (a hand-edit that corrupts one value buys a fresh window — precisely R-10's threat model). The two can be reconciled only by noticing that §6 negates the `` `WINDOW-START: {N}` `` form while AC-1.5(4) clause 4 and §4.1's `A` row count the bare `` `WINDOW-START:` `` prefix — a distinction resting entirely on whether `{N}` appears inside a backticked token, which is nowhere stated as a convention. The resolving rule is stated **once, parenthetically, inside a PROPERTIES obligation**; the normative sites (AC-1.5(4) clause 4 `:250`–`:251`, §4.1's `A` row) do not state it, and §6 — the section that speaks about malformed values and asserts it is *in force at this ship* — reads against it. A test author working from §5/§6, which is where a REQ's behaviour is supposed to live, writes the opposite test. **Repair, ~30 bytes net:** put the rule at the normative site — in AC-1.5(4) clause 4 after *"`A`, the number of `WINDOW-START:` **plus** `WINDOW-RESUMED:` lines"*, add *"counted **by line prefix, whatever the value** — a malformed value contributes no origin but still answers a halt"* — and strike the redundant *"is not a `WINDOW-START: {N}` line and"* from §6, whose *"contributes no value"* half already says everything §4.1's `W` row needs. That is a net **reduction**, which matters given F-46. | §6 `WINDOW-START: {N}` row (`:364`), §8 O-10 leg 3, §5 AC-1.5(4) clause 4 (`:250`–`:251`), §4.1 `A` row (`:136`) |
| F-45 | Low | Local | **Leg 3's fixture omits the branch's highest round — the quantity that decides both of its own outcome conjuncts — where legs 1 and 2 both state it.** Leg 1 pins *"highest round on the branch = `windowEnd(1)` = **3***"; leg 2 pins *"highest round below `windowEnd(1)`"*. Leg 3 states the region (`H = 1`, one `WINDOW-START: abc`, `RESOLVED: yes`) and then asserts *"The ordinary window `[1, 3]` opens — … **≥ 1** dispatch"*, but never says how many rounds are on the branch. On the same region with the highest round at 3 the window is exhausted and the entry halts with **0** dispatches — the opposite of the asserted oracle. It is recoverable (the asserted outcome uniquely forces highest round ≤ 2), which is why this is Low and not Medium, but a test author should not have to solve for a fixture cell from the expectation; that is how leg 1's v8 vacuity arose. **Repair, ~28 bytes:** after *"and a readable `RESOLVED: yes`"* add *", highest round below `windowEnd(1)`"* — leg 2's own phrase, so the three legs read as a set. | §8 O-10 leg 3 |
| F-46 | Low | Process | **Sixth consecutive round inside a few hundred bytes of the size gate, and the second running in which the file grew while shedding several KB: headroom is now 23 bytes.** 55,273 bytes against `SOFT_BYTE_LIMIT = 55296` (`pdlc/hooks/scripts/check-req-size.sh:47`–`:48`), 442/630 lines. Four relocations landed this round — including the ~800-byte AC-1.5(4) arithmetic block I nominated in F-42, plus split §5.3, baseline §3.1 and §3.2 — and the round still ended 35 bytes *heavier*, because X-06, §4.1's `W` row, §6, O-12, O-10 leg 3 and §10 all took new prose. The trajectory across rounds is 9 → 171 → 58 → 23 bytes of headroom. As in F-42 I am not criticising any individual insertion: every one was a finding's repair and both blocking findings this round arise from text that was *added to close v9*. The durable signal is unchanged and now well-evidenced: **a REQ owning a live forward edge accretes prose at that edge faster than relocation can clear it, and relocation has nearly run out of targets** — the four largest shared blocks are gone. F-44's repair is a net reduction and F-45's is ~28 bytes, so the two together are roughly neutral; **F-43's is not affordable without a further move**. The remaining candidates, in order: O-10's leg-1/leg-2/leg-3 fixture prose (~1.4 KB) belongs in the PROPERTIES document the obligation names, not in the REQ; and X-06's *what is not deferred* paragraph now duplicates `REQ-RCV-07` X-07 almost verbatim (I diffed them), so one end could hold it and the other cite it — but that is a paired-edge change and must be agreed at both ends. | Document size, `pdlc/hooks/scripts/check-req-size.sh:47`–`:48`, §3.1 X-06, §8 O-10 |

## Questions

## Positive Observations

## Recommendation

## Verdict
