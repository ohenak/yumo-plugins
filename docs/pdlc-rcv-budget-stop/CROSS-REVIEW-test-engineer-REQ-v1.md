# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v2.6)
**Date:** 2026-08-01
**Iteration:** 1
**Scope:** testability, edge-case completeness, oracle falsifiability, property coverage. Reviewed at
HEAD of `feat-pdlc-rcv-budget-stop`, against `docs/_constraints/pdlc-rcv-baseline.md`,
`docs/_constraints/pdlc-rcv-catalogue.md`, `docs/_constraints/pdlc-rcv-split.md`,
`docs/_constraints/DOMAIN-CONSTRAINTS.md`, and both sibling REQs
(`docs/pdlc-rcv-reset-region/`, `docs/pdlc-rcv-fixed-point-stop/`).

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **The paired edge is desynced at HEAD, and the desync removes the interim ship's only falsifiable oracle.** Four clauses in the other half cite content of this REQ that v2.6 deleted: (a) `REQ-RCV-07` O-12 says the validate seam "keeps the signature **`REQ-RCV-01` O-12 fixes** — arity 2 (the region, the branch listing) …", while this REQ's O-12 says "the seam contract itself is `REQ-RCV-07` O-12's" — **circular; no document fixes the seam signature**; (b) `REQ-RCV-07` X-07 cites "the interim composition calls it **0 times** (`REQ-RCV-01` O-10)"; (c) `REQ-RCV-07` O-10 says "Row 10 keeps only a contract leg asserting its interim composition calls the seam **0 times**, asserted on **its leg 1's** fixture", and "Row 10 also keeps one **interim-only** leg" (the malformed-`WINDOW-START:` one); (d) `pdlc-rcv-split.md` §5.4 states "`REQ-RCV-01` O-10 states which legs it owes and why (the 0-call contract leg is asserted on leg 1)". **This REQ's O-10 contains none of it** — no 0-call leg, no seam, no interim legs, and no citation of split §5.4 at all. Testing consequence: with the validation conjunct unwired (X-06), the *only* assertion that could distinguish "conjunct deliberately not consulted" from "conjunct wired with an ad-hoc interim procedure" is the 0-call oracle, and it is now owed by nobody. Every leg O-10 *does* list passes under both the refusing and the granting horn that `split.md` §5.1 rejects. Split §5's own rule ("revise all four within the same revision, in the same words; the reviewer checks both ends **at HEAD**") is violated. | §8 O-10, O-12; §3.1 X-06; §9 R-14 |
| F-02 | High | Local | **Undefined behaviour when the derived window start falls below the origin `W`, and it is reachable by a documented operator act.** AC-1.5(2) fixes the start as one past the highest existing round of the document type; AC-1.5(1) and (4) fix the window as `{W … W+2}` and state "rounds below `W` are outside the window". No clause says which rule wins when `highest + 1 < W`. This arises whenever cross-review files are removed after a grant while the post-mortem (and its region) survives — exactly the operator reset performed on this branch at `e9f3264` ("remove REQ cross-review rounds v1..v10 for a fresh Phase R window"), with `POSTMORTEM-R-pdlc-rcv-budget-stop.md` still on disk. The consistency check that would catch it (highest-round validation) is deferred to `REQ-RCV-07` AC-7.1 and **not in force at this ship** (X-06), so the interim has no guard either. A test author cannot decide whether the entry dispatches round 1, opens no round and halts on S-4, or refuses — nor what S-4 renders. O-10 has no leg for it, §7 does not name it a non-goal, and §9 does not accept it as a risk. | AC-1.5(1), (2), (4); §4.1 `W` row |
| F-03 | Medium | Local | **AC-1.3's second reported quantity has no declared render, and contradicts §6.** AC-1.3 requires the Iterations section to *additionally* state "the rounds this entry ran — `0` there", and O-10 requires it asserted ("with rounds-run `0` on the zero-round halt"). No row in baseline §3/§3.1, catalogue §2 or this §6 fixes that text, while §6 asserts "this REQ now mints no operator string of its own" — one of the two statements is false. Baseline §3's own rule is that "a threshold used by a child REQ and not in this table … is a defect". HEAD's literal is `` `Iterations (${MAX_REVIEW_ROUNDS} — limit reached)` `` (M-1c); nothing states what the augmented section looks like, so the O-10 leg can only be written as a fuzzy substring match — an oracle that passes on any rendering, including one that reports the two quantities in a way an operator conflates, which is the exact failure AC-1.3 exists to prevent. | AC-1.3; §6; §8 O-10 |
| F-04 | Medium | Cross-Feature | **O-10 obliges example fixtures only, for a component that is plainly parameterisable.** The window-origin/counts resolution is a parser-plus-validator: `(ordered region line sequence, values, highest round on branch) → (W, H, A, grant?)`. O-10 enumerates hand-picked fixtures and nothing else; split §5.4 adds three more hand-built ones. The REQ already states the invariants a property test would pin — `W` is always a decimal integer ≥ 1 and never exceeds the greatest well-formed `WINDOW-START:` (§4.1, §6); "no absent or malformed value ever widens the window" (§4.1); one clearance grants exactly one window (AC-1.5(4)); `H − A ∈ {0, 1}`. Require ≥ 1 property-based obligation over generated line sequences asserting the fail-closed direction, not only the enumerated points; the enumerated set cannot cover the interleaving of `HALT-REASON:`/`WINDOW-START:`/`WINDOW-RESUMED:` lines that document order makes significant. | §8 O-10 |
| F-05 | Medium | Cross-Feature | **DC-03 is not routed by any obligation.** `docs/_constraints/DOMAIN-CONSTRAINTS.md` DC-03 requires every load-bearing assertion to pass the falsification cycle and be recorded in the feature's `FALSIFICATION-LEDGER.md`, and names non-vacuity as "the dominant defect class in this codebase". O-10 and O-11 name no ledger, no mutation obligation and no artifact; no sibling in the family does either (`grep -rl FALSIFICATION-LEDGER docs/` returns no `pdlc-rcv-*` file). The assertions this REQ turns on are precisely DC-03's domain — the assertion over the constant (AC-1.2/AC-1.3), the 0-dispatch count on row C, the one-shot clearance (AC-1.5(4)) — each of which is the *only* signal of its defect. Note DC-10 additionally requires a lifecycle disposition for the ledger, which Phase H does not delete. | §8 O-10, O-11 |
| F-06 | Medium | Process | **DC-09's stopping rule is not pasted into the artifact under review.** DC-09 states with evidence that a stopping rule "living only in a constraints file or a method document nobody loads does nothing", and that pasting it into the REQ "changed both reviewers' behaviour immediately". This document is the one that most needs it: R-1 records that it is reviewed by the loop it is changing and disposes of the risk as "Accepted and unenforceable — the operator watches the trajectory and halts by hand", which is the advisory shape §1 P-2 identifies as having failed on three features; the document has already exhausted one Phase R window (`POSTMORTEM-R-pdlc-rcv-budget-stop.md` on disk, ten cross-review rounds removed by operator reset). Paste DC-09's four stopping conditions into §9 R-1 (or a preamble) so this window's reviewers can cite them by name to route findings downstream instead of blocking. | §9 R-1 |
| F-07 | Low | Cross-Feature | **Dangling obligation citation into this REQ.** `pdlc-rcv-catalogue.md` §4's Recovery-text row says the generic queue-reset line's suppression is "owned by `pdlc-rcv-budget-stop` O-6"; this REQ has no O-6 (split §2 moved it to `REQ-RCV-07` O-6). Split §6's blanket delegation is scoped to references to "AC-1.5(4)'s algorithm, its numbered steps, or the refusal renders it produces" and to §3's `§6` pointer — it does not reach an `O-*` id. Either widen split §6's rule to obligation ids or record the correction; a test author reading catalogue §4 for the suppression contract lands on a non-existent authority. | §8; `pdlc-rcv-catalogue.md` §4 |

## Questions

## Positive Observations

## Recommendation
