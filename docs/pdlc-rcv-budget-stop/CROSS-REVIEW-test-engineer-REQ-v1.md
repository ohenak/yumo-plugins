# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v1.1, 410 lines / 48,175 bytes)
**Scope:** Testing lens only — testability of the acceptance criteria, completeness of the edge/negative cases, falsifiability of the oracles the REQ names, and verification of the citations it rests on. Product strategy, architecture choice and prose style are out of scope.
**Date:** 2026-08-01
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **Phase CR is inside the mechanism this REQ changes and outside every criterion that governs it — and one reachable path is an absorbing permanent refusal.** Phase CR calls the same `reviewLoop` through `phaseWindow(null)` (`pdlc/workflows/orchestrate-dev.js:4720-4721`, `docType: null`), so it consumes `MAX_REVIEW_ROUNDS` and `deriveRoundWindow` exactly as R/F/T/D/P/PR do, and its halts write `POSTMORTEM-CR-{feature}.md`. AC-1.2 forces a single module-scope constant, so CR's budget silently becomes 3; AC-1.4 says the reset region is maintained on **every** halt "without exception", so a CR halt creates `## Reset Region`; but N-7 (baseline §4) says the round-window mechanism does **not** apply to Phase CR. Those three cannot all be true, and the document never resolves them. The consequence is not cosmetic: with `docType: null` no basename ever matches, so `deriveRoundWindow` always returns `startIndex = 1` for CR. If AC-1.5(4) runs there, the first CR clearance writes `WINDOW-START: 1`; a **second** CR clearance computes `N = 1` again, which fails step 2's *"strictly greater than every `WINDOW-START:` value before it"* ⇒ S-16 ⇒ phase refused ⇒ and AC-1.5(4) itself notes the failure is **absorbing** (the region is preserved verbatim by every later halt, and no clearance repairs it). Phase CR would be permanently unenterable for that feature. A test author cannot write a single CR assertion from this document, and the fix is a scope statement, not an oracle. State explicitly, in AC-1.1 and AC-1.4, whether `docType: null` loops are in or out — and if out, say what bounds them, since they still read the one constant AC-1.2 owns. | AC-1.1, AC-1.2, AC-1.4, §7 N-7 |
| F-02 | High | Local | **The most common halt this REQ creates has no run-report row, so its own operator-visible surface is unspecified.** §2 names "the budget in the run report" as a delivered surface, and AC-1.5(1) states the headline case — a re-entered branch whose highest round is ≥ 3 is *admitted no rounds* and halts immediately on the budget path, emitting S-4. Catalogue §3 carries **one row per round**; an entry that opens zero rounds produces zero rows, so the S-4 string has nowhere to be rendered. The catalogue anticipates exactly two dispatch-less rows and names them both — **row A** (zero-delta, owned by `pdlc-rcv-fixed-point-stop`) and **row B** (reset-region refusal, owned here, AC-1.5(4)) — and row B is stated to carry **S-16 alone with no S-4 reason**. There is no row C. AC-1.1's own primary scenario therefore has no report oracle: a test author reading AC-1.1 + catalogue §3 cannot derive whether the report is empty, carries a row, or which cells it fills. This is not deferrable to TSPEC — catalogue §3 states that a dispatch-less row is *"stated cell by cell in the REQ that owns the condition"*, and this REQ owns the budget condition. State row C cell by cell as AC-1.5(4) states row B. | AC-1.5(1), §2 Value, catalogue §3 |
| F-03 | High | Local | **Step 2's range check reads "the highest round on the branch" without saying which listing, and the two readings give opposite refuse/grant outcomes on a constructible fixture.** `deriveRoundWindow(basenames, docType)` filters by document type (`if (result.docType !== docType) continue;`), but a feature directory holds cross-reviews for **several** document types at once. Fixture: Phase F, `POSTMORTEM-F` region carrying `WINDOW-START: 4`, branch holding `CROSS-REVIEW-*-FSPEC-v1..v2` and `CROSS-REVIEW-*-REQ-v1..v5`. Doc-type-scoped reading: one past highest FSPEC round = 3, so `4 > 3` ⇒ invalid ⇒ S-16 ⇒ **phase refused, permanently** (AC-1.5(4)'s absorbing failure). Whole-listing reading: one past highest = 6, so `4 ≤ 6` ⇒ valid ⇒ `W = 4` ⇒ **window granted**. Same bytes, opposite operator outcome, no way to choose from the document. The same ambiguity infects AC-1.5(4)'s `N` ("one past the highest round then on the branch"), AC-1.5(2) and row B's `round` cell. One clause fixes it ("…of the document type under review"), but it must be in the REQ because it decides a refusal. | AC-1.5(4) step 2, AC-1.5(2), §4.1 |
| F-04 | Medium | Local | **The answering-line write has no confirmation obligation, so the one-shot property fails open on exactly the failure mode AC-1.4 already defends against on the halt path.** AC-1.4 requires the halt to "confirm the write rather than trusting the agent's reply", and O-5 extends that to the region re-apply. AC-1.5(4)'s `WINDOW-START:`/`WINDOW-RESUMED:` append — the write that makes `A = H` and is the *sole* mechanism preventing a `RESOLVED: yes` from re-granting a window on every subsequent invocation — carries no such obligation anywhere in AC-1.5, §6 or §8. If that append is lost, the clearance stays unconsumed and the loop hands out a fresh three-round window per invocation: the per-invocation budget AC-1.1 exists to abolish, restored silently and fail-**open** (the document's own words for the failure it means to prevent, §4.1 and AC-1.5(4)). There is also no stated oracle for "the append failed", so the defect is untestable as specified. Require confirmation of the answering-line write and state the behaviour when it cannot be confirmed (fail-closed: treat the clearance as consumed, or refuse). | AC-1.5(4), AC-1.4, O-5 |
| F-05 | Medium | Cross-Feature | **Two of §7's three non-goal rows cite ids that do not exist in the authority §7 names, and the family is minting colliding ids in a shared namespace.** §7 opens *"The shared list is baseline §4"*, then tables **N-14** and **N-11**. Baseline §4 defines **N-1 … N-10 only** — verified by reading `docs/_constraints/pdlc-rcv-baseline.md` §4. Worse, the family has quietly started minting per-REQ `N-*` ids in that shared namespace and they already collide: `N-13` means *"Re-specifying the round budget, the window origin `W`…"* in `pdlc-rcv-fixed-point-stop` §7 and *"Deciding, mechanically, whether a reviewer applied AC-5.1's test correctly"* in `pdlc-rcv-finding-quality` §7. The catalogue's own rule is that shared ids are stable across the family and never renumbered. A reviewer or test author told "this absence is intentional per N-14" cannot resolve N-14 anywhere. Also internally inconsistent: the prose says *"**Two** are worth pointing at"* immediately above a **three**-row table. Either add the new ids to baseline §4 (with family-unique numbers) or renumber them into a per-REQ namespace that cannot be mistaken for the shared one. | §7 |
| F-06 | Medium | Local | **O-10's two headline negative legs are absence-only oracles and cannot fail.** The mid-window positive control is stated as *"**no round-3 cross-review file is written**"*, and AC-1's Observability as *"a fourth round never dispatches a reviewer"*. Both are satisfied by an implementation that dispatches reviewers and by one that does nothing at all — a test double whose `_agent`/`_parallel` writes no file passes the first, and "never dispatches" has no positive conjunct at all. These are precisely the legs that distinguish honouring step 4 from falling back to `W = 1`, so they are load-bearing (DC-03) and currently unfalsifiable. O-10 must name the positive conjunct: a **call-count oracle on the reviewer dispatch seam** — 0 reviewer dispatches on the refusing/exhausted entry, and ≥ 1 on the control entry that *does* open round 3 under the fallback — asserted alongside the file-absence check, not instead of it. | O-10, AC-1 Observability |
| F-07 | Medium | Local | **The refusal's "positive control" is unreachable at this REQ's own ship time, which contradicts §3.1's completeness claim and makes R-11's accepted cost unrealisable.** AC-1.5(4) step 4 and R-11 justify the mid-window refusal by saying it is reachable "at highest round **2**, since `pdlc-rcv-fixed-point-stop` AC-2.1 can fire on the (1, 2) pair and its AC-2.8 can halt at round 2, either of which creates the region with `H = 1`, `A = 0`". Both cited halts belong to a **successor that has not shipped** — X-05 already records that no halt path emits S-11 until it does, and AC-2.1's fixed-point halt is equally absent. When this REQ ships alone, the *only* halt that can create a region is the budget halt, which by construction fires only once the window is full; so `H = 1, A = 0` always coexists with an **exhausted** branch, and the document's own analysis says refusal and fallback are then *indistinguishable*. The consequence for testing is concrete: the mid-window fixture O-10 mandates is **synthetic** — hand-built files that no shipped code path can produce — and the REQ should say so, rather than presenting it as a reachable operating state. It also weakens §3.1's *"AC-1 is complete in itself and its behaviour is fully determined without any successor"*: the one branch that distinguishes AC-1.5(4)'s central design choice from its fallback is not. | AC-1.5(4) step 4, R-11, §3.1 X-05, O-10 |
| F-08 | Medium | Cross-Feature | **`forcePhases` is a shipped, documented operator override whose behaviour this REQ silently changes, and no criterion states the new behaviour.** `orchestrate-dev` accepts `forcePhases` to re-run a phase whose approval is already recorded (`CLAUDE.md`, *Artifact convention* → *Entry (single feature)*). Today a forced Phase R on a branch with three rounds is admitted rounds 4…6 (M-1d). Under AC-1.5(1) with `W = 1` it is admitted **no rounds** and halts immediately — so the documented override now yields a post-mortem instead of a review, and the operator's only way to actually re-review is to write `RESOLVED: yes` into a post-mortem the halt has just created. That may well be the intended trade, but it is a behavioural change to an operator-facing feature that the REQ neither states nor lists as a non-goal, and there is therefore no acceptance test for it. State it (as an AC clause or as an explicit non-goal), so the force path has an oracle. | AC-1.5(1), §7 |
| F-09 | Low | Local | **Two ACs pin literals that AC-1.2 and §6 declare configurable, so their oracles contradict each other.** S-4's grammar is `rounds {first}..{last} of {MAX_REVIEW_ROUNDS}` and §6 says *"a clause that hard-codes `rounds 1..3 of 3` is a defect"* — yet AC-1.5(1) renders it as `rounds {W}..{W+2} of 3`, hard-coding **both** the constant slot and the width arithmetic that AC-1.2 says lives only in `windowEnd`. AC-1.3 likewise requires the post-mortem to "state **three**". A test written from AC-1.5(1)/AC-1.3 pins `3`, and the AC-1.2 test that changes the constant then goes red for the wrong reason. State both over the constant. | AC-1.5(1), AC-1.3, §6 |
| F-10 | Low | Process | **§10 cites files that harvest deletes, as the surviving record.** *"The nine review rounds … live in `docs/discarded/pdlc-review-convergence/CROSS-REVIEW-*-REQ-v{1..9}.md` … those files remain the record."* That directory contains exactly two files — `REQ-pdlc-review-convergence.md` and `LEARNINGS-pdlc-review-convergence.md`. `harvest-learnings` deletes `CROSS-REVIEW-*` once LEARNINGS is written, so this citation form is *structurally* wrong for any harvested feature, not just this one. Cite the LEARNINGS file, which is what survives. | §10 |
| F-11 | Low | Local | **`iterations` is unspecified on the zero-round entry.** AC-1.3 requires the returned `iterations` field to be "consistent with" the post-mortem's Iterations section, but on AC-1.5(1)'s immediate-halt entry no round ran in that invocation while the shipped site returns `iterations: MAX_REVIEW_ROUNDS` (M-1c). Say which quantity `iterations` reports — rounds the invocation ran, or rounds the window holds — so the assertion is derivable. | AC-1.3, AC-1.5(1) |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Is the answering line written **before** or **after** the round dispatches of the entry that grants the window? O-6 assigns the *placement* to TSPEC, but the choice is observable: written first, an invocation that dies before round `W` has spent the operator's one escape hatch for nothing; written last, a converging invocation may never write it at all. Which of those two is the accepted outcome is a REQ-level decision, and PROPERTIES needs it to write the crash-fixture assertion. |
| Q-02 | Does the S-16 refusal produce a `POSTMORTEM` **phase record** distinguishable from the step-G refusal it borrows its shape from? AC-1.5(4) says a ❌ phase row is recorded and the queue row becomes `halted`; if the recorded reason text is identical to step G's *"Refused — unresolved POSTMORTEM at …"* (M-7a), the operator is told the wrong thing (the marker **is** resolved — that is why step 4 ran) and the two branches are indistinguishable in the phase record. |
| Q-03 | AC-1.4 clause 2 strips every unfenced `RESOLVED:` line on every halt. What happens to a post-mortem whose `RESOLVED: yes` sits inside the `## Reset Region` heading's span? The strip and the region-preserve rule are stated independently and both claim that span. |

## Positive Observations

- **Every quantity the criteria read is given a durable on-branch home, and the table is explicit that an in-process-only row would be a defect (§4.1).** This is the failure mode that killed the predecessor, and answering it structurally rather than per-criterion is the right shape. It also makes almost every AC fixture-constructible from files alone: no clock, no process identity, no runtime fact anyone has to measure.
- **AC-1.5(4)'s receive side is stated as an ordered algorithm rather than a table of independent rows**, with an explicit totality argument and a stated tie-break for multi-fault regions ("exactly one S-16 notice, first failing line in document order"). That is directly executable as a test oracle, and it removes the usual source of two-reviewers-two-readings.
- **The `H`/`A` counting scheme is a genuinely good design for the one-shot property**, and the REQ does the hard part: it enumerates both directions of hand-edit corruption, shows the arithmetic for each repair in a table, and explains *why* deleting an answering line is unsafe at both reachable values of `H − A`. The `WINDOW-RESUMED:` line existing purely to give the S-11 path a **positive artifact** to assert on — rather than an absence — is exactly the oracle discipline DC-03 asks for, and is called out as such in the text.
- **O-10 is unusually good as a downstream obligation**: it names the negative cases, the mutation pairs (counts-mismatch refusal ↔ recovery leg), the ratchet test, and the byte-unchanged assertion. F-06 is a gap in two of its legs, not a criticism of its structure.
- **The append-not-prepend rule is justified by a stated failure** (a `WINDOW-RESUMED: 4` landing ahead of the `WINDOW-START: 4` it answers), and O-10 requires it asserted **positionally**. That is a property most specs leave implicit and most implementations then get wrong.
- Citations spot-checked at HEAD: `MAX_REVIEW_ROUNDS` (:52), `windowEnd` (:2450-2451), `deriveRoundWindow` (:2386), `parseResolvedMarker` (:1105), `checkPostmortem` (:2695), `scanLines` (:721), `recordHaltFn` (:4907), the bare `Write ${postmortemPath}.` prompt (:1937). All present; line numbers have drifted from the baseline commit as the header predicts, and the symbol-plus-literal convention made every one of them findable in one grep.

## Recommendation

**Needs revision**

Three High findings, all of them contesting **scope or a delivered operator-visible surface** rather than an oracle that could be routed downstream under DC-09:

1. **F-01** — say whether Phase CR (`docType: null`) is inside or outside AC-1.1/AC-1.4/AC-1.5. It shares the constant AC-1.2 owns and the halt path AC-1.4 quantifies over "every halt", while N-7 excludes it; as written, one reachable CR sequence is a permanent, unrepairable phase refusal.
2. **F-02** — state, cell by cell, the run-report row for the entry that admits **no rounds and halts on the budget**. Catalogue §3 has rows A and B only, so AC-1.1's headline scenario currently cannot be rendered in the report §2 sells as a deliverable.
3. **F-03** — say which listing "the highest round on the branch" is taken over. Doc-type-scoped and whole-directory readings give *refuse permanently* vs *grant the window* on the same bytes.

The Mediums are all one-clause fixes and none of them requires new mechanism: confirm the answering-line write (F-04), resolve the `N-*` id namespace (F-05), add the dispatch-count conjunct to O-10's two absence-only legs (F-06), mark the mid-window control's fixture synthetic and reconcile it with §3.1 (F-07), state the `forcePhases` outcome (F-08).

Explicitly **not** filed, per §8 and DC-09: the absence of fixtures, seams, oracle wiring or test-level assignment for AC-1.5(4)'s algorithm — that is O-10's and TSPEC's, and the REQ routes it correctly. Nothing in this review contests user need, priority or phasing.

## Verdict

VERDICT: Needs revision

{"high": 3, "medium": 5, "low": 3}

