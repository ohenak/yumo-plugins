# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-rcv-budget-stop/REQ-pdlc-rcv-budget-stop.md` (v2.8)
**Date:** 2026-08-01
**Iteration:** 1 (first round of the fresh Phase R window opened by `e9f3264`)
**Scope:** testability of REQ-RCV-01's acceptance criteria and of the test obligations O-10 states over them. Product strategy, technical architecture and the sibling REQs' contents are out of scope.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **The creating halt's Iterations render is agent-authored, so O-10's equality leg on it cannot be a deterministic oracle.** AC-1.4 clause 3 says *"A creating halt's authored section carries it"*, and at HEAD that section is produced by an authoring dispatch from a prose prompt (M-7e), whose only confirmation is that the file exists (M-1c's literal lives in that prompt string). O-10 (i) nonetheless requires the Iterations section to be asserted as an **equality on §6's declared render** on **two named fixtures**, the creating halt being one. That leg has only two implementations, and both are defects: fake the authoring agent and the test asserts a string the double was told to write (proves nothing about production — the string an operator reads is still whatever the real agent emits), or drive a real agent and the assertion is nondeterministic. The re-halt half is safe precisely because AC-1.4 clause 3 makes it *"a loop-computed render, not an authoring dispatch"* — the creating half is left on the agent. The REQ must decide, at outcome altitude, **that the loop owns the Iterations section on every halt in scope** (or explicitly downgrade AC-1.3's creating-halt obligation from an exact render to something an agent can be held to). O-14 cannot absorb this: it declares itself *"mechanism only … this obligation does not vary the outcome"*, and who guarantees the string **is** the outcome. | AC-1.3, AC-1.4 clause 3, §6 Iterations row, O-10 leg (i), O-14 |
| F-02 | Medium | Local | **The Iterations section has no anchor grammar and no not-found disposition, so O-10's re-halt byte-equality leg has no expected file on its reachable fixtures.** AC-1.4 clause 3 requires a re-halt to rewrite *"that one section and nothing around it"*, and O-10 (ii) states the oracle as **byte-equality against the prior bytes with clauses 1–3's edits applied**. Neither the ACs nor §6 say how the section is located in a file whose structure was never verified (the halt path confirms existence only, M-7e), nor what the halt does when no such section is found. Two reachable fixtures need that answer before the leg is writable: (a) R-13's migration branch, whose post-mortem was authored at HEAD and carries the baseline literal `Iterations (5 — limit reached)` rather than §6's render; (b) a post-mortem the agent wrote without the section at all — nothing gates its presence. Fixing outcome, not procedure: state the located anchor (e.g. a heading whose text begins `Iterations`) and the fail-closed disposition when it is absent. | AC-1.3, AC-1.4 clause 3, O-10 leg (ii), R-13 |
| F-03 | Medium | Local | **AC-1.1's `Then` is false in a reachable state, so a test written from it fails on every post-clearance fixture.** AC-1.1 states unconditionally that *"the window ends at round 3 counted from round 1 of that document, and the loop halts on entering round 4 — whatever invocation opened the earlier rounds"*, with no origin qualifier. AC-1.5(1) states the end as round 3 counted from the **origin `W`**, and O-10's own leg (4) exercises `W = 4`, where the halt is on entering round **7**, not 4. Two ACs therefore give different predicates for the same observable, and only cross-reading resolves which wins. This is not an oracle deferral §8 can receive — it is an ambiguity about what the requirement **is**. Qualify AC-1.1's `Then` by the origin (`W = 1` absent a reset) so the criterion is true on every path, or state explicitly that AC-1.5(1) supersedes it. | AC-1.1, AC-1.5(1), O-10 leg (4) |
| F-04 | Medium | Local | **The `HALT-REASON:` append carries no confirmation obligation, and its failure has no declared outcome — the mirror image of a clause the REQ states carefully for the answering line.** AC-1.5(4) gives the answering line *"the same confirmation obligation AC-1.4 puts on the post-mortem write, fail-closed on failure: no window, no dispatch, the entry refuses the phase."* AC-1.4 clause 1's append gets nothing equivalent, and the obligation it would inherit is vacuous on exactly the path that matters: on a **re-halt** the file already exists, so an existence-shaped confirmation (the shipped shape, M-7e) is satisfied whether or not the line landed. The resulting state is unrecoverable and no clause names it: a lost `HALT-REASON:` under-counts `H`, so `A = H` holds forever, no clearance is **ever** unconsumed (AC-1.5(4)'s gate), the operator's `RESOLVED: yes` grants nothing, and every subsequent entry is a zero-round budget halt. Note this is not covered by NB-3, which defers only *"what a partially-written **answering** line leaves behind"* to `REQ-RCV-07` AC-7.5. O-5's *"a region lost or unwritable is reported fail-closed"* names a report, not the operator's route out. Also leaves O-10 with no leg for the case. | AC-1.4 clause 1, AC-1.5(4), O-5, NB-3 |
| F-05 | Low | Local | **AC-1.2's "decidable observable" is dischargeable by a human checklist, i.e. by no test at all.** AC-1.2 fixes the observable as O-13(b)'s closed enumeration — *"a site absent from that list, or a second executable declaration, is the violation"* — but O-13(b) then leaves it open whether *"a document-drift oracle (`coveredViolations`) or a checklist line carries that"*. A checklist line cannot detect a site absent from the list, so under that branch AC-1.2's prose-site half ships unfalsifiable, which is the same class of defect AC-1.2 exists to prevent (a green suite disagreeing with the pipeline). Recommend AC-1.2 require the enumeration be **machine-compared against a repo scan**, leaving only *which* mechanism to TSPEC. Filed Low because §9's stopping rule routes oracle-selection to TSPEC and O-13 already receives it. | AC-1.2, O-13(b), §6 `MAX_REVIEW_ROUNDS` row |

## Questions

| ID | Question |
|----|---------|
| Q-01 | On the creating halt, is the Iterations section intended to be the loop's output or the authoring agent's? F-01 assumes the REQ meant the loop (that is the only reading under which AC-1.3's *"asserted over the constant"* and O-10's equality leg are simultaneously satisfiable), but AC-1.4 clause 3's word *"authored"* says the opposite. |
| Q-02 | O-10 leg (4)'s granting fixture has `W = 4` with every cross-review file of that document type removed. Is the fixture's post-mortem region `[WINDOW-START: 4, HALT-REASON: …]` with `A = H`, i.e. is the leg asserting `W` read from a **durable** region rather than granted on that entry? The text says *"clearance already spent (`A = H`, so this entry grants nothing and `W` is durable)"* — stating the region's exact line sequence would remove the last ambiguity for whoever builds it. |

## Positive Observations

- **The zero-dispatch conjunct is stated positively.** O-10 requires row C be asserted with *"a dispatch count of `0` alongside the absence of any new cross-review file, since a double that writes nothing satisfies absence either way"* — an absence-only oracle caught and closed at REQ altitude, which is where it is cheapest.
- **The re-halt oracle is stated as byte-equality against a constructed expected file, and the weaker alternative is refuted in writing** (*"a byte comparison with the region span excised" would be unsatisfiable on every reachable fixture*). Modulo F-02's missing anchor, that is the right shape.
- **The control in O-10 leg (4) discriminates.** `highest = 6` is chosen over `highest = 5` with the reason given — round 6 is the window's last slot, so a `5` control would dispatch and prove nothing. Re-derived: `W = 4` ⇒ window `{4,5,6}`; `highest = 6` ⇒ derived start 7 > 6 ⇒ halt, no dispatch. Correct.
- **The property obligation is total on the empty draw.** *"`W` never exceeds the greatest well-formed `WINDOW-START:` present, **or is `1` when none is present**"* closes the max-over-empty-set hole that would otherwise make the property vacuous on exactly the malformed-region draws it exists to cover.
- **The Phase CR leg is explicitly defended as non-vacuous** via M-7f (untyped phases reach the same halt path and do write a post-mortem), so the leg asserts a file that exists lacks the section rather than passing on a file that is not there.
- **DC-03 routing is named per assertion**, with the residual case (*"any assertion with no nameable mutation filed as a Residual"*) handled rather than assumed away.
- Citations spot-checked against their sources: M-1a–M-1e and M-7a–M-7f resolve in `pdlc-rcv-baseline.md` §2; S-4/S-15's renders resolve in `pdlc-rcv-catalogue.md` §2; split §§5.1–5.6 and §6 all exist; `REQ-RCV-07` AC-7.1–AC-7.6 and O-6/O-12 exist. `MAX_REVIEW_ROUNDS = 5` is at `pdlc/workflows/orchestrate-dev.js:52` with duplicate declarations in the test suite, exactly as O-13(a) reports.

## Recommendation

**Needs revision**

Three things must change, all at outcome altitude and all small:

1. **F-01** — decide, in AC-1.3/AC-1.4, that the loop writes the Iterations section on **every** halt in scope (creating and re-halt alike). Without it O-10's named leg (i) has no honest implementation, and AC-1.3's guarantee to the operator rests on an authoring agent's compliance with a prompt.
2. **F-02** — state how the Iterations section is located, and the fail-closed disposition when it is absent, so O-10 leg (ii)'s expected file is constructible on the migration fixture and on a post-mortem that never carried the section.
3. **F-03** — qualify AC-1.1's `Then` by the origin `W`, or state that AC-1.5(1) supersedes it, so the two criteria do not give different halt rounds for the same fixture.
4. **F-04** — give AC-1.4 clause 1's region append the same confirmation obligation and fail-closed disposition AC-1.5(4) gives the answering line, and name what the operator does when it fails. As written the failure is silent and terminal.

F-05 is Low and closable by routing to O-13.

## Verdict

VERDICT: Needs revision
