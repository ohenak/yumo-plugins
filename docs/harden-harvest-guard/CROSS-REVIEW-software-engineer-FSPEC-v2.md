# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** docs/harden-harvest-guard/FSPEC-harden-harvest-guard.md (Version 1.1; REQ v1.4)
**Date:** 2026-07-02
**Iteration:** 2

## Review Focus

Iteration-2 SE pass: (1) verify each of my seven iteration-1 findings (3 Medium, 4 Low) plus Q-01 against the actual v1.1 section text — not the disposition table's own summary; (2) hostile check of the v1.1 additions (eager `NO_REPO` early exit, over-cap-opaque D1-allow, reordered `mv` flow, degraded token pins, deterministic git-error code, M67–M79/S07 promotion into REQ v1.4) for **new** defects only. No re-litigation of settled decisions.

## Existing-Code Claim Verification (single pass, v1.1-new claims only)

| Claim | Verified against | Result |
|---|---|---|
| M78 "supersedes the current script's coerce-to-empty-and-allow at `guard-harvest-before-delete.sh:32`" (FSPEC-GUARD-04 DG-DP2) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh:32` — `cmd = (data.get("tool_input", {}) or {}).get("command", "") or ""` (absent/`null` → `""` → allow) | Accurate |
| "All fifteen rows are hermetically writable in the existing jest harness (spawn cwd, stdin content, restricted `PATH`, `CLAUDE_PROJECT_DIR`, and fixture-repo git state … all controllable)" (§ Acceptance Tests) | `pdlc/workflows/__tests__/hookCompatibility.test.js:47-52` — `runHookScript` passes `input:`, `env: {...process.env, ...opts.env}`, and `cwd:` to `spawnSync`; fixture repos are tmpdirs whose `.git/refs/remotes/origin/{branch}` file is an ordinary writable file (M77 corruption feasible) | Accurate |
| Iteration-1 claims (probe order, fail-open lines 21/30, `runHookScript` knobs) | Re-checked; script unchanged | Still accurate |

## Prior-Finding Disposition Verification (SE v1 → FSPEC v1.1)

Each resolution was checked against the actual section text and the REQ v1.4 matrix, not the FSPEC's Reviewer Findings Disposition table:

| v1 finding | Claimed resolution | Verified? |
|---|---|---|
| F-01 (M — `mv` flow evaluated indeterminacy before the static-source D2 test) | FSPEC-GUARD-02 steps reordered: step 2 is now the static-source D2 test, explicitly "evaluated before any indeterminacy jump"; BR-01-1 restated to bind the `mv` flow; REQ row M74 pins `mv docs/f "$DEST"` → `NOT_COMMITTED`; M74 in the M33 G6 re-run set (correctly ALLOW under G6 — traced through the flow) | **Resolved** — but the rewrite of step 3 introduced a new, narrower condition; see new finding F2-01 |
| F-02 (M — G1 eager vs lazy; GUARD-06 double-match) | Eager chosen and stated in three mutually consistent places (GUARD-01 step 8, GUARD-03 G-DP1, BR-01-1); BR-03-3 rescoped to in-repo; GUARD-06 table now has an eager `NO_REPO` row that precedes and exhausts G1, and the D3/D4 row is annotated repo-only — the double-match is gone; rows M75 (unguarded target in non-repo) and M76 (D3-shaped in non-repo → `NO_REPO`, not `INDETERMINATE`) pin both divergence cases I named | **Resolved** (residual Lows F2-02/F2-03 on the definition of "cwd" and the un-amended REQ prose) |
| F-03 (M — recursion-cap latitude licensed a D3 false block on verb-free over-cap payloads) | Edge case inverted: over-cap payload is **opaque → D1 ALLOW**, never deletion-shaped, with the NFR-01 rationale stated inline; REQ v1.4 RR-3 extended to register the over-cap bypass surface (verified in REQ text); depth-2 row M73 pins minimum recursion and joins the G6 re-run set | **Resolved** — correct direction (bypass surface accepted as residual risk rather than false-block surface accepted silently) |
| F-04 (L — union rule stated only for segment 1) | Step 6: "The initial effective cwd (the union of both candidate roots) applies to **every** segment — not only segment 1 — until a `cd` segment updates it", M18 cited | **Resolved** |
| F-05 (L — fetch unformable in the states G-DP3 detects) | G-DP2 conditioned on formability (an `origin` remote and a current branch both exist); skipped in no-remote/detached states | **Resolved** |
| F-06 (L — "TSPEC picks the nearest G-state" non-deterministic) | Deterministic: git query failure after G-DP1 passes → `NOT_COMMITTED` ("not a TSPEC choice"); repo-detection failure → `NO_REPO`; GUARD-06 row added; row M77 | **Resolved** (residual Low F2-04 on the failure/absence boundary and M77's discriminating power) |
| F-07 (L — parseable stdin without `tool_input.command` undecided) | DG-DP2: absent/`null` → `PARSE_ERROR` (M78, superseding the script's coerce-to-empty — citation verified); empty-string `command` → well-formed, zero segments → D1 ALLOW; REQ-GUARD-06 case 2 aligned in v1.4 (verified) | **Resolved** |
| Q-01 (degraded "iff" freezes the over-match surface) | Answered: intentional; over-match surface normative for oracle stability; narrowing is a REQ change. Allow rows M43/M69/M72 and block rows M42/M68/M70 pin both sides | **Answered** — coherent |

## Matrix Partition Check (v1.4 extension)

Row assignments in the Linked Requirements table were re-summed over the new rows: M73 (GUARD-01) + M67, M74, M79 (GUARD-02) + M75–M77 (GUARD-03) + M68–M72, M78 (GUARD-04) + S07 (GUARD-05) cover M67–M79 and S07 exactly once — the partition over M01–M79/S01–S07 remains exact, no gaps, no double-ownership. The M33 G6 re-run extension (M66, M67, M73, M74, M79) was traced: each row genuinely flips to ALLOW under a Verified `docs/f` (M74 traced through the reordered `mv` flow: step 2 misses on a Verified source, steps 3–5 miss, fall-through ALLOW). The exclusions (M68–M72 degraded, M75–M76 non-repo, M77–M78 contract) are correctly non-G6-constructible or git-state-independent.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F2-01 | Medium | Local | **The v1.1 `mv`-flow rewrite narrows the destination-indeterminate → D3 routing below both the REQ and the FSPEC's own general D3, silently reopening an `mv`-overwrite vector.** Step 3 now reads: source indeterminate, "**or** the destination is indeterminate **while a static source is/globs a guarded file in an unverified guarded directory**" → D3. The REQ v1.4 `mv` table row is unconditional ("Indeterminate source or destination → Decision rule D3 applies"), and FSPEC-GUARD-01's general D3 ("a deletion verb with at least one indeterminate operand … blocked iff docs-referencing …") carries no source-guardedness qualifier either. The readings diverge observably on a static **unguarded** source with an indeterminate docs-referencing destination: `D=docs/f; mv /tmp/notes.md "$D"/CROSS-REVIEW-x.md` — general D3 / REQ: BLOCK `INDETERMINATE` (indeterminate operand + `docs/` via the assignment-RHS dataflow channel + unverified dir exists); the `mv` flow as written: step 2 misses (source not guarded), step 3 misses (source static and not a guarded file), steps 4–5 don't apply → fall-through ALLOW — while the command overwrites (destroys) a guarded file. No matrix row pins the case (M74's source is a guarded dir; no `mv`+indeterminate row has an unguarded source). The qualifier is also unnecessary for the F-01 fix: with step 2 (static-source D2 test) now evaluated first, restoring step 3 to the REQ's breadth ("source **or destination** indeterminate → D3") preserves M74 and every existing `mv` row (M09–M13, M52, M79 all traced — none regress). Fix: drop the "while a static source is/globs a guarded file…" qualifier from step 3, and pin the overwrite case with a matrix row (e.g. `D=docs/f; mv /tmp/notes.md "$D"/CROSS-REVIEW-x.md` → BLOCK `INDETERMINATE`). | FSPEC-GUARD-02 `mv` flow step 3 vs REQ v1.4 `mv` table ("Indeterminate source or destination") and FSPEC-GUARD-01 D3 |
| F2-02 | Low | Local | **G-DP1 tests "the cwd" for repo-ness, but the spec distinguishes three cwd notions and never says which one G-DP1 consults.** Step 6 defines roots (A) (stdin `cwd`, else `CLAUDE_PROJECT_DIR`) and (B) (hook process cwd); step 8 anchors guarded-dir enumeration at "`CLAUDE_PROJECT_DIR` when set, else `git rev-parse --show-toplevel`". In a mixed state (e.g. process cwd in a non-repo tmpdir, `CLAUDE_PROJECT_DIR` pointing at a real repo — or the converse), the readings diverge observably: eager `NO_REPO` vs normal D2 evaluation. M37/M75/M76 fixtures are all-roots-non-repo, so no row discriminates. One-sentence fix: define G-DP1 as testing the same anchor the enumeration uses (repo-ness of `CLAUDE_PROJECT_DIR` when set, else whether `git rev-parse --show-toplevel` succeeds from the process cwd) — which also makes step 8's two halves one mechanism. | FSPEC-GUARD-01 step 8; FSPEC-GUARD-03 G-DP1 |
| F2-03 | Low | Local | **REQ-GUARD-01's normative decision-rule list was not amended for the eager exit, so REQ v1.4 now contradicts its own matrix — and the "no requirement semantics changed" framing is inaccurate.** REQ-GUARD-01 still reads "Decision rules (in order; first match wins): D1 → D2 → D3 → D4 → allowed" with no repo-check step; a verbatim reading yields ALLOW for M75 (`rm /tmp/scratch.log`, non-repo: D2/D3/D4 all miss → fall-through), while matrix row M75 — in the same document — says BLOCK `NO_REPO`. M75 also flips a v1.3-ALLOW command class to BLOCK, so both documents' claim that v1.4 "changed no requirement semantics" is wrong for M75/M76. Behavior is unambiguous (matrix is the declared oracle; FSPEC BR-01-1 states D1 → G-DP1 → D2 → D3 → D4), so this is doc-consistency, not implementability — but the FSPEC corrected the REQ explicitly when it superseded M62's `>&` phrasing (CF-1), and the same treatment is owed here. Fix: one clause in REQ-GUARD-01's decision-rule preamble naming the eager repo check between D1 and D2 (or an explicit supersession note in FSPEC step 8/BR-01-1 mirroring CF-1's). | FSPEC-GUARD-01 step 8 / BR-01-1 vs REQ v1.4 REQ-GUARD-01 decision rules and rows M75–M76 |
| F2-04 | Low | Local | **The boundary between "git query failure" (→ deterministic `NOT_COMMITTED`, M77) and "`origin/{branch}` ref does not exist" (→ G4/G5) is not operationally defined, and M77's fixture cannot discriminate the rule it was added to pin.** A corrupt `.git/refs/remotes/origin/{branch}` surfaces to most git queries the same way as an absent ref (`unknown revision`), so an implementation with no error branch at all routes M77's fixture through G5 — which also emits `NOT_COMMITTED` in the disk-only default state — and passes the row. The rule's determinism becomes observable only in the unpinned corrupt-ref × LEARNINGS-committed cell, where the two routings diverge (`NOT_COMMITTED` vs G4's `NOT_PUSHED`). Both routings block with exit 2, so the essential contract holds either way. One-sentence fix: define query-failure as any git invocation error not cleanly reporting ref-absence, or state that G4/G5 routing of an indistinguishable corrupt ref is conformant (then M77's expected code is pinned by coincidence, which the row should say). | FSPEC-GUARD-03 error scenarios / G-DP3 vs row M77 |

## Questions

None. (Q-01 from iteration 1 is answered in the disposition table; the answer is coherent and pinned on both sides by matrix rows.)

## Positive Observations

- All seven iteration-1 findings are genuinely discharged in section text, not just in the disposition table — and the two hardest ones (F-02 eager/lazy, F-03 over-cap) were resolved in the right direction: the over-cap case now trades a registered bypass surface (RR-3 extension) for the P0 no-false-block guarantee instead of the reverse.
- The TE F-09 process rule was actually applied: every behavior-changing resolution in v1.1 landed with a same-commit matrix row in the REQ (M67–M79, S07), keeping the oracle and the prose in one place of truth. The partition over M01–M79/S01–S07 remains exact.
- M77 was strengthened beyond what F-06 asked for (deterministic code, not just a stable prefix), and M78's supersession of the current script's coerce-to-empty behavior cites the exact line, which verifies.
- The degraded token table's allow-side negatives (M69 bare-`clean`, M72 empty-stdin) pin the over-match ceiling as tightly as the block side pins the floor — the "iff" surface is now genuinely testable in both directions.

## Recommendation

**Needs revision**

One Medium must be resolved before TSPEC authoring: F2-01 — restore FSPEC-GUARD-02 step 3 to the REQ's breadth ("source or destination indeterminate → D3"; the narrowing qualifier is unnecessary now that step 2 runs first) and pin the unguarded-source/indeterminate-destination overwrite case with a matrix row. The three Lows are each one-sentence fixes (F2-02: name G-DP1's directory; F2-03: one clause in REQ-GUARD-01's rule list or an explicit supersession note; F2-04: define the query-failure boundary) and may be carried forward with back-references if the optimizer prefers. Everything else in v1.1 holds the line: the prior Mediums are correctly and completely fixed.
