# Cross-Review: test-engineer — TSPEC

**Reviewer:** test-engineer
**Document reviewed:** docs/harden-harvest-guard/TSPEC-harden-harvest-guard.md (v1.1)
**Date:** 2026-07-02
**Iteration:** 2

Upstream context read: REQ v1.7 (Canonical Matrix M01–M90 / S01–S07), FSPEC v1.4. All eleven iteration-1 findings (6 Medium, 5 Low) were verified resolved against the actual v1.1 sections — none re-opened (see verification table). The two load-bearing empirical claims backing the fixes were **independently re-verified in this environment**: **C17** (Node `spawnSync("bash", …)` under a restricted child `PATH` fails `ENOENT`/`status: null`; the § 6.1 `BASH_ABS` absolute-path spawn succeeds and the child script's own `command -v python3` probe correctly reports no interpreter) and **C18** (git 2.50.1: corrupting the loose object behind `origin/feat-f` leaves `show-ref --verify --quiet` exit 0 and `symbolic-ref`/`rev-parse` unaffected, while `git ls-tree --name-only origin/feat-f -- <path>` exits 128 with `error: inflate: …` diagnostics — a true § 3.6 query failure). The rebuilt § 6.3 allocation invariant was re-derived arithmetically: the nine base groups sum to 89 ids and a row-by-row walk of M01–M90 minus M33 places every id in exactly one group (M17 → cwd-union only, M37 → non-repo only, M46 → D3 only; M74/M84/M90 bound in D2 statics); the M33 re-run enumeration matches REQ v1.7 row M33 verbatim; the FSPEC v1.4 step-2b/3/4 wording matches § 3.5.5. Five new Low findings on v1.1 material only.

## Iteration-1 finding verification

| v1.0 finding | Status in v1.1 |
|---|---|
| F-01 (M) M74/M84 unallocated at base state | **Resolved** — bound in the D2-statics group; allocation invariant re-derived: exact partition of {M01–M32, M34–M90}, no duplicates (length-vs-set meta-check added) |
| F-02 (M) degraded rows cannot spawn bash | **Resolved** — § 6.1 `BASH_ABS` resolved once under the parent env; re-verified empirically (relative spawn ENOENT, absolute spawn exit 0, child probe sees empty PATH) |
| F-03 (M) step-2b hoist / T-rows outside the matrix | **Resolved upstream** — REQ v1.7 `mv`-table carve-out + M87–M90; FSPEC v1.4 step 2b verified present and aligned with § 3.5.5; no TSPEC-owned row namespace remains |
| F-04 (M) no unit/property strategy for the parser | **Resolved** — § 6.6 `--self-test` unit mode (DEC-01) + P-D1/P-QUOTE black-box tables; the table-driven-over-generative justification (stdlib-only floor, enumerable corner space) is sound and matches the remedy iteration 1 offered |
| F-05 (M) substring oracle under-implemented | **Substantially resolved** — § 6.3 contract table delivers every substring iteration 1 enumerated (harvest instruction, `git push` per state, operand naming, `git clean`, `python3`); residual secondary conjuncts → new F2-01 (Low) |
| F-06 (M) M77 corrupt-ref fixture pins nothing | **Resolved** — corrupt-loose-object fixture (C18) re-verified empirically here; builder self-check makes the loose-object assumption falsifiable against git/backend drift |
| F-07 (L) self-audit gaps (S-rows, M33 source) | **Resolved** — shared `guardRowIds.js`; `S_CASES` audited against `S_ROWS`; `M33_RERUN_IDS` named as the single maintained copy (the accept-and-document option iteration 1 offered) |
| F-08 (L) C8 path-substring filter | **Resolved** — § 5 change 1 anchored basename `case`; C8 claim corrected; S07 non-discrimination honestly noted |
| F-09 (L) `spawnCwd` default unspecified | **Resolved** — § 6.2: `fixture.repoDir` default, mkdtemp scratch for `fixture === null`, never `process.cwd()` |
| F-10 (L) catch-all `PARSE_ERROR` off-catalog | **Resolved** — § 3.3 two-layer discipline; the top-level handler emits the exact § 3.7 template (required substring always present); git-query exceptions route to `NO_REPO`/query-failure `NOT_COMMITTED` |
| F-11 (L) nonexistent "CODE_REVIEW twin" citation | **Resolved** — § 6.5 corrected to the M06+M01 pair under uniform fnmatch handling; reasoning verified against § 3.5's single `enumerate_guarded` pattern pair |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F2-01 | Low | Local | **Residual REQ-GUARD-07 conjuncts missing from the § 6.3 substring table.** The REQ's required-content column also demands: NOT_COMMITTED — the instruction to "…and **commit**" (assert a `commit` substring alongside `LEARNINGS-f.md` + `/pdlc:harvest-learnings`); INDETERMINATE — "the fail-closed rationale" (assert e.g. `ailing closed`, case-tolerant, alongside the operand / `git clean` scope); DEGRADED — "the missing-interpreter cause" (assert e.g. `interpreter` alongside `python3`). The § 3.7 templates all carry these; only the assertions omit them, so a template regression dropping the commit instruction or the rationale sentence would still pass. Since substrings are assembled mechanically per reason, adding the three conjuncts is a one-line change per row of the contract table. | § 6.3 oracle table vs REQ-GUARD-07 |
| F2-02 | Low | Local | **M21's D3 `{operand}` interpolation is unspecified, so its substring cell is not mechanically assemblable.** The § 3.7 D3 template "names the unresolvable operand `{operand}`", and § 6.3 lists M21 among the rows asserting "the row's unresolvable-operand raw text" — but M21 (`ls docs/f \| xargs rm`) has no operand token: § 3.5.2 defines the xargs-wrapped verb's operands as the piped input stream. What `emit_block` prints for the piped-stream case (the producer segment's text? a fixed `piped input` marker?) is nowhere stated, so an implementer cannot fill M21's MATRIX `substrings` entry without a clarifying decision — contradicting § 6.3's "assembled mechanically, never hand-tuned". Specify the interpolation for stream-indeterminate operands (one sentence in § 3.7). | § 3.7 D3 template, § 6.3 oracle table (M21) |
| F2-03 | Low | Local | **The `--self-test` conjunction gate is externally satisfiable, and § 3.1's justifying claim is inaccurate.** Step 0 asserts both sentinels are "settable only by this wrapper branch" — but argv[1] on the production path *is* the raw hook stdin (externally chosen), and `GUARD_SELF_TEST` is an ordinary env var a user can export. Hook stdin that is literally `--self-test` while `GUARD_SELF_TEST=1` happens to be exported satisfies the conjunction: the Python runs the self-test and exits 0 — an allow (fail-open) on stdin the contract routes to `PARSE_ERROR`. The trigger is admittedly pathological (broken harness stdin of one exact spelling × a stray export), but the fix is free and unforgeable: gate on **argc** — the wrapper's self-test branch passes two argv entries (`--self-test` plus a second sentinel), the production path always passes exactly one (raw stdin occupies a single argv slot and can never manufacture a second), and the Python requires `len(sys.argv) == 3`. At minimum, correct the "settable only by this wrapper branch" prose. | § 3.1 step 0, § 6.6 |
| F2-04 | Low | Local | **The self-test jest binding asserts exit 0 only — an emptied `SELF_TEST_CASES` table passes silently.** § 6.6 specifies that success prints the case count on stdout, but the binding ("spawns the entrypoint with `--self-test` and asserts exit 0") never reads it. A refactor that guts the case table (or a gate bug that exits 0 without running cases — distinguishable from the intact path only by the count) keeps the suite green while the entire parser-unit layer silently disappears. Have the binding parse the stdout count and assert it ≥ a named floor (even `>= 1` closes the empty-table hole; a floor near the enumerated coverage areas is better). | § 6.6 jest binding |
| F2-05 | Low | Local | **§ 6.3's "dedicated `it()` blocks for rows needing bespoke orchestration" is in unresolved tension with self-audit meta-test 1.** Meta-test 1 requires the `MATRIX` id list to deep-equal `M_ROWS` minus M33 — so a bespoke row moved out of `MATRIX` into a dedicated block fails the audit (loud, safe), but a row kept in `MATRIX` *and* given a dedicated block is executed by both `it.each` and the block: two asserting tests, breaking the REQ-GUARD-05 exactly-one invariant the table claims to discharge. The implementer must invent the reconciliation. Specify it: bespoke rows keep their `MATRIX` entry with a `bespoke: true` marker; the `it.each` parameterization filters marked entries out; each dedicated block carries the row-id title and consumes the entry's config — keeping both the audit and exactly-one intact. | § 6.3 (structure paragraph + self-audit meta-test 1) |

## Questions

| ID | Question |
|----|---------|
| — | None — all ambiguities are filed as findings. |

## Positive Observations

- **Both empirical fixes hold up under independent reproduction**: C17 (ENOENT on relative spawn; `BASH_ABS` reaches the script with the restricted env intact) and C18 (corrupt loose object → `ls-tree` exit 128 with inflate diagnostics while `show-ref`/`symbolic-ref`/`rev-parse` stay clean) reproduced exactly as § 6.1/§ 6.2 specify. The M77 **builder self-check** (throw unless the probe query actually fails) is precisely the falsifiability discipline a version-sensitive fixture needs.
- **The § 6.3 allocation invariant is now arithmetically airtight**: nine groups, 89 ids, exact partition verified by independent re-derivation; the length-vs-set check closes the duplication hole set-equality alone leaves; the M33 re-run enumeration is byte-identical to REQ v1.7 row M33.
- **The T→M promotion is complete and consistent across all three documents**: REQ v1.7 `mv` table + M87–M90, FSPEC v1.4 step 2b/3/4, TSPEC § 3.5.5 — one durable oracle, no TSPEC-owned behavior, and the FSPEC's step-3 "never masks a G-state code" argument is correct (steps 2/2b exhaust static certainty first).
- **§ 6.6 is an honest resolution of the property-testing obligation**: the single-file constraint is respected via the argv-gated self-test, the generative-vs-table trade-off is argued rather than asserted, and P-QUOTE deliberately produces *variant* commands so the exactly-one row obligation is untouched.
- **The § 3.3 two-layer exception discipline** keeps the reason-code catalog closed while making every emission satisfy its substring oracle — including routing `verify_feature` exceptions into the § 3.6 query-failure class M77 now genuinely exercises.

## Recommendation

**Approved with minor changes**

All six iteration-1 Medium findings and all five Low findings are verified resolved; both empirical claims reproduce; the allocation arithmetic checks out; every matrix row's test is writable as specified (the sole wobble, M21's substring cell — F2-02 — leaves that row's exit-code and prefix assertions fully writable). The five Low findings are tightening work an implementer can absorb without re-review: three missing substring conjuncts, one one-sentence interpolation rule, one gate-prose/argc correction, one assert-the-count line, and one `bespoke` marker convention.
