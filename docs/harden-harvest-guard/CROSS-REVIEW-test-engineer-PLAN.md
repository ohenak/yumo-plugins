# Cross-Review: test-engineer — PLAN

**Reviewer:** test-engineer
**Document reviewed:** docs/harden-harvest-guard/PLAN-harden-harvest-guard.md (v1.0)
**Date:** 2026-07-02
**Iteration:** 1

## Verification performed (mechanical re-derivation, not prose trust)

- **Batch-DAG check:** re-derived every task's batch from its `Depends on` column (`batch == max(dep batch) + 1`): TASK-01/02→B1, TASK-03/04→B2 (deps B1), TASK-05 (dep 03)→B3, TASK-06 (dep 03)→B3, TASK-07 (dep 04)→B3, TASK-08→B4, …, TASK-12 (deps 11,05: max(B7,B3)+1)→B8, TASK-13→B9, TASK-14→B10. **All 14 rows match the Batch column; graph acyclic; ids unique; every dep resolves.** No desync.
- **Same-batch same-file writer check:** B1, B2 target disjoint files; B3 writers are guardMatrix.test.js (TASK-05), guard script (TASK-06), check-scope-field.sh (TASK-07) — disjoint. B4–B10 serial. No writer collision. (But see F-02 for a non-writer race.)
- **Partition check:** re-derived the union of the nine TSPEC § 6.3 base groups the PLAN binds to — it is exactly `{M01–M32, M34–M90}`, disjoint, with M33 discharged by the re-run parameterization. Complete.
- **`M33_RERUN_IDS`:** TASK-01's enumeration (M01, M02, M04–M24, M44–M54, M60–M64, M66–M67, M73–M74, M79–M80, M82, M84–M90) is character-identical to REQ v1.7 row M33.
- **Cited baselines verified in the working tree:** `hookCompatibility.test.js` — `runHookScript` at lines 47–59 (C10) and the PROP-COMPAT-05 block occupying ~155–244 through EOF (C12) both check out; `pdlc/workflows/package.json` `testPathIgnorePatterns` already ignores `/__tests__/helpers/` (C14).
- **TASK-06 degraded checkpoint achievability (the key TDD-ordering question):** verified against REQ rows M42–M43/M68–M72 and TSPEC §§ 3.1–3.2 — every row in the checkpoint runs under `degradedEnv()` (empty child `PATH`), so the interpreter probe fails and the flow terminates in the pure-bash coarse matcher **before any Python executes**. M71's DEGRADED-over-PARSE_ERROR precedence is structural (the matcher runs before any parse attempt) and M72's empty stdin matches no token → exit 0. The builtin-only stdin read (C4) and `BASH_ABS` spawning (C17) close the two ways the fixture could fail at spawn. **Yes — these rows can genuinely go green at TASK-06 with the Python engine still a fail-closed placeholder.**
- **Fail-closed ordering of the PROP-COMPAT-05 retirement:** TASK-04 (B2) deletes the disk-only-LEARNINGS-allows test *before* TASK-06 (B3) makes the guard fail closed — without this ordering the placeholder would turn a shipped green test red mid-chain. Correctly sequenced.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | Medium | Local | **TASK-09 and TASK-10 have no green checkpoint — their per-task exit criterion is unfalsifiable.** Every row in TASK-09's Test File column (M04–M08, M17–M25, …) and TASK-10's (M34–M40, M57–M59, M77) is annotated "green at TASK-11", and none can pass earlier because every matrix row traverses the decision engine that only TASK-11 builds. So two consecutive serial-chain tasks ship substantial engine code (verb identification, operand classification, union-cwd, `verify_feature`, message catalog) with a done-state of "code written; nothing newly green; nothing previously-green red" — a single se-implement agent running strict red→green→refactor has no green gate to discharge, and a subtly wrong TASK-09 classification surfaces only as a cascade of TASK-11 failures two batches later. Note 1 ("Named ATs per implementation task are the matrix row IDs listed in its Test File column") and note 2 ("intermediate chain tasks (TASK-08–TASK-10) assert their named intake/degraded rows green") are also internally inconsistent for these two tasks — TASK-09/10 have no intake/degraded rows to assert. **Fix (either):** (a) land the `--self-test` mechanism incrementally — TASK-08 installs the gate + tokenizer/segmenter/heredoc `SELF_TEST_CASES`, TASK-09 appends the verb-identifier/classification/cwd cases (§ 6.6 calls the § 3.5 functions directly, no git/fs needed, so these ARE runnable at those tasks), TASK-12 completes coverage + the jest binding — giving every chain task a directly invocable green checkpoint; or (b) state each task's exit criterion explicitly as a falsifiable check (at minimum: previously-green set enumerated and re-run, plus a named intermediate oracle). Option (a) is preferred; TASK-10's `verify_feature` (git-dependent, self-test-excluded) would then be the only checkpoint-less task and should say so explicitly. | Task table TASK-09/TASK-10; Dependency notes 1–2 |
| F-02 | Medium | Local | **B3 has execute-while-write races across its parallel tasks.** The same-batch rule as written covers only *write* targets, but in B3 the read/execute edges cross: TASK-05 red-verifies its new tests by *spawning the guard script* that TASK-06 is concurrently rewriting (a half-written wrapper gives arbitrary exit codes, so "🔴 for the right reason" is verified against a moving target — e.g. the CFD-8 pin and P-D1 rows), and TASK-06's green checkpoint *runs jest on guardMatrix.test.js* while TASK-05 is appending to it (a mid-write file is a syntax error → the whole suite, including the degraded rows, fails spuriously). Both directions produce nondeterministic checkpoint outcomes under parallel dispatch. **Fix:** serialize with one edge — make TASK-06 depend on TASK-05 (B3 = {TASK-05, TASK-07}, chain starts at B4; costs one batch) — or equivalent. The prose note that TASK-05 is "serialized after TASK-03" solves the write-write case only, not these read/execute races. | Task table B3; Batch overview |
| F-03 | Low | Local | TASK-03's claim "Entire suite is 🔴 against the current script" overclaims: ALLOW rows (e.g. M03, M43, M69, M72 and most of the D1 group) can pass vacuously against the *current* guard, and some legitimately never transition red→green. The red-verification instruction should be stated per row class — every BLOCK row red, every behavior-inverted row red, ALLOW rows may pre-pass (they pin non-regression) — so se-implement doesn't stall or force artificial redness on rows that are green by construction. | TASK-03, last sentence |
| F-04 | Low | Local | The self-test coverage floor is vacuous as bound: TASK-05 declares `SELF_TEST_MIN_CASES` with only "never below 1 — CFD-6" as a constraint, so a floor of 1 satisfies the letter while an emptied-to-one-case `SELF_TEST_CASES` table passes silently — the exact failure CFD-6 exists to catch. The PLAN should bind the floor to the § 6.6 enumeration it sits next to (e.g. ≥ the count of enumerated corner classes across the four function families), making the constant meaningful rather than merely nonzero. | TASK-05, TASK-12; CFD-6 |
| F-05 | Low | Local | Meta-test accounting is inconsistent between representations: TASK-03 folds the CFD-7 dispatch-map key-equality into meta-test 1 (as its third conjunct), while the DoD and TASK-14 count "the four self-audit meta-tests" (base allocation, re-run integrity, S-row coverage, CFD-7 key equality) as four distinct tests. CFD-7 permits either shape ("fourth conjunct of meta-test 1, or a meta-test 4") — pick one and use it consistently so the DoD checkbox is mechanically checkable by counting test titles. | TASK-03, DoD, TASK-14 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | TASK-08's green checkpoint includes M81 (ALLOW), which requires the intake to exit 0 for a zero-segment command while everything else stays placeholder-fail-closed. This interim "zero segments → allow, else exit 2" scaffold is derivable but never stated — should TASK-08 spell it out so se-implement doesn't ship a fail-closed-everything intake that leaves M81 red? |
| Q-02 | TSPEC § 3.1 step 0 gives the wrapper a no-interpreter self-test branch (`self-test requires python3`, exit 1) that no test pins (the jest binding is python3-gated). Accepted as untested wrapper surface, or worth one degraded-env supplementary case? |

## Positive Observations

- The batch column, dependency edges, and § 6.3 nine-group partition all survive mechanical re-derivation — no desync anywhere in the 14-task table.
- The [Fake first] convention is properly observed: TASK-01/02 precede all production tasks, and every implementation task (TASK-06–12) has preceding red-test rows (TASK-03/04/05) in the same test file with named row-ID ATs.
- The central TDD-ordering question checks out: TASK-06's degraded checkpoint (M42–M43, M68–M72) is genuinely achievable wrapper-only — the empty-`PATH` fixture guarantees the Python placeholder is never reached, and the C4 builtin-stdin-read and C17 `BASH_ABS` details that make this true are both carried into the task text.
- TASK-04 retiring PROP-COMPAT-05 in B2, one batch before the guard turns fail-closed in B3, is exactly the right sequencing to honor "a chain task must never make a previously-green row red".
- The self-audit meta-tests + dispatch-map rule (CFD-7) make the exactly-one-test-per-row obligation machine-checked rather than review-trusted; `M33_RERUN_IDS` matches the REQ v1.7 enumeration verbatim and is consumed by both the parameterization and the audit, so intra-suite drift is impossible.
- Single-writer discipline on `hookCompatibility.test.js` (note 6) and the hermeticity DoD items (no network, `mkdtempSync` + `afterEach`, local bare remotes) are carried through from TSPEC § 6.2 without loss.

## Recommendation

**Needs revision**

Two Medium findings must be addressed: give TASK-09/TASK-10 falsifiable green checkpoints (F-01 — incremental `--self-test` landing is the natural fix given the § 6.6 architecture) and eliminate the B3 execute-while-write races with a serialization edge (F-02). The Low findings are wording/binding tightenings that can land in the same revision.
