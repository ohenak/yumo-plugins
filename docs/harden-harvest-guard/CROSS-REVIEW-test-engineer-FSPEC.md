# Cross-Review: test-engineer — FSPEC

**Reviewer:** test-engineer
**Document reviewed:** docs/harden-harvest-guard/FSPEC-harden-harvest-guard.md (Version 1.0)
**Date:** 2026-07-02
**Iteration:** 1
**Scope:** Testing lens — decidability and hermetic testability of every behavioral flow (FSPEC-GUARD-01–06), 1:1 mapping of decision points onto Canonical Matrix rows (the FSPEC's own declared oracle: "the REQ's Canonical Block/Allow Matrix … is the acceptance-test suite for every flow below"), fixture constructibility for degraded-mode and git-state rows, and verification that CF-3's M66 closes my REQ-v4 finding F4-01.

## Prior-Finding Disposition Verification (F4-01 → CF-3/M66)

| REQ-v4 finding | Status in FSPEC v1.0 | Evidence |
|---|---|---|
| F4-01 (Low) — union rule's root-(B) disjunct had no positively pinned block row | **Resolved — correctly** | § Acceptance Tests adds M66 exactly as requested: no `cwd` in stdin, hook **process** cwd = `docs/f`, `rm *.md` → BLOCK `NOT_COMMITTED`, with a full Who/Given/When/Then. The row is genuinely discriminating: with no stdin `cwd`, candidate root (A) falls back to `CLAUDE_PROJECT_DIR` (repo root), where `*.md` resolves only to unguarded files — so the block can come **only** from root (B), and the spec states the kill condition explicitly ("an (A)-only implementation must fail this row"). M33's G6 re-run set is extended to include M66, pinning the allow side under a Verified state. The fixture is hermetic in the existing jest harness (spawn cwd + stdin content + `CLAUDE_PROJECT_DIR` all controllable). This is the complete closure I asked for. |

## Oracle-Mapping Scan (flow branches vs matrix rows)

The FSPEC's contract is that the matrix is the acceptance-test suite for every flow, and it claims to add "exactly one row, M66." I re-derived the row coverage for every decision point. D1–D4, the `mv` resulting-path rule, redirection classification, G1–G10 (incl. M57–M59 fetch-failure/default-false rows), and S01–S06 all map cleanly — with the exceptions below, all of which are decided behaviors in this document's flows that **no matrix row exercises**. CF-1 and CF-2 changed or created decidable behavior but — unlike CF-3 — amended only prose, not the oracle. That is precisely the F4-01 class of gap (a spec disjunct an incorrect implementation can pass green), now sitting in the document that owns matrix amendments, so it can no longer be deferred downstream.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | Medium | Local | **CF-1's corrected `>&` disambiguation is unverifiable from the oracle.** FSPEC-GUARD-02 rule 1 supersedes REQ M62's "followed by a digit" with "word entirely digits or exactly `-`" — but every existing row agrees under **both** rules: M62's word (`docs/f/CROSS-REVIEW-x.md`) starts with a non-digit, and `2>&1`/`>&2` (M56) are all-digits. The discriminating case — a digit-leading non-numeric word resolving to a guarded file — appears only in the edge-case table, rowless. An implementation of the superseded rule (first-char-digit ⇒ fd-duplication) classifies `>& 2024-notes/CROSS-REVIEW-x.md` as fd-duplication, finds no deletion verb, allows via D1 — and passes all 66 M-rows green. Add a matrix row, e.g. M67: fixture adds nested guarded file `docs/f/2024-notes/CROSS-REVIEW-x.md` (nested subdirs are guarded per REQ Definitions); command `cd docs/f && npm test >& 2024-notes/CROSS-REVIEW-x.md` → BLOCK `NOT_COMMITTED`; include in M33's G6 re-run set. Hermetically writable today. | FSPEC-GUARD-02 redirection rule 1; § Carry-Forward CF-1 |
| F-02 | Medium | Local | **CF-2's degraded token set defines three token classes; the matrix pins only one.** M42 exercises the word-verb class via `rm`; M43 pins the no-token allow; nothing pins: (a) the two-word `git clean` token (block) and its normative negative "bare `clean` is **not** a token" (allow — e.g. no interpreter, `./scripts/clean docs/backup`: contains `clean` + `docs/` but must ALLOW); (b) the `>`-character class (e.g. no interpreter, `foo > docs/f/CROSS-REVIEW-x.md` → BLOCK `DEGRADED`); (c) the DG-DP1→DG-DP2 precedence branch (step 3) — no row co-occurs both degradations, so an implementation that tests stdin parseability first and emits `PARSE_ERROR` passes M41 and M42 green while violating the stated precedence (fixture: no interpreter + stdin `not-json{"cmd":"rm docs/f"}` → BLOCK `DEGRADED`, not `PARSE_ERROR`). A degraded matcher grepping only for word verbs passes every current row. All fixtures are hermetic (restricted `PATH` + stdin content, per REQ-GUARD-05). Add rows for (a) positive+negative, (b), and (c). | FSPEC-GUARD-04 business rule + step 3; § Carry-Forward CF-2 |
| F-03 | Low | Local | **Precedence prose contradicts DG-DP1's total rule for the empty-stdin co-occurrence.** Step 3 says "when both degradations co-occur … result is `DEGRADED`", but DG-DP1's decision rule is total: raw text with no token match → ALLOW. For no-interpreter + **empty** stdin, one reading yields BLOCK `DEGRADED`, the other ALLOW — two defensible expected exit codes, so the test is not writable without a ruling. Reword step 3 to "the DG-DP1 flow governs (its matcher decides block-vs-allow; a block carries `DEGRADED`, never `PARSE_ERROR`)" and let the F-02(c) row pin the block side. | FSPEC-GUARD-04 step 3 |
| F-04 | Low | Local | **Step 8 does not anchor guarded-directory enumeration to a root.** "Enumerate guarded directories (top-level `docs/{feature}/` …)" is root-relative but names no root. M66's own fixture makes the wrong choice live: with process cwd `docs/f`, enumeration relative to the process cwd finds no `docs/*` and allows. M66 would catch that implementation, but only accidentally — state the root normatively (repo root: `CLAUDE_PROJECT_DIR`, else `git rev-parse --show-toplevel`) so TSPEC doesn't have to reverse-engineer it from a matrix row. | FSPEC-GUARD-01 step 8 |
| F-05 | Low | Local | **Git-command-failure-inside-a-valid-repo has no oracle row.** The FSPEC-GUARD-03 error table decides "never exit 0 on git error; TSPEC picks the nearest G-state" — a decided fail-closed branch with no matrix row and a deferred reason code. The invariant that *is* fixed (exit 2 + `pdlc-guard[` prefix) is testable now with a hermetic error-injection fixture (e.g. corrupt `.git/HEAD` in the fixture repo). Add a row asserting exit 2 + prefix only, reason code to be pinned by TSPEC. | FSPEC-GUARD-03 error scenarios |
| F-06 | Low | Local | **Scope-check step 1's basename-filter branch has no S-row.** "Basename does not match … → silent exit" is the branch protecting every non-review Write/Edit from warning noise (US-03's core promise), yet S01–S06 all use matching basenames. Add S07: write `notes.md` containing no Scope pattern → silent. | FSPEC-GUARD-05 step 1 |
| F-07 | Low | Local | **`mv` flow is specified for exactly two operands.** "Canonicalize source and destination" leaves multi-source `mv` (`mv docs/f/CROSS-REVIEW-x.md docs/f/CODE_REVIEW-f-v1.md /tmp/`) undecided — a realistic agent spelling. State the generalization (last operand is the destination; each source evaluated independently under the resulting-path rule; any BLOCK blocks) so the decision is derivable rather than implementation-chosen. | FSPEC-GUARD-02 `mv` flow step 1 |
| F-08 | Low | Local | **Recursion depth ≥ 2 is decided but rowless.** The edge-case table decides `bash -c 'eval "rm docs/f/*.md"'` (re-parse "at every level"), but M22/M23 are both single-level; an implementation that unwraps opaque payloads exactly once passes both. Add a depth-2 row (BLOCK `NOT_COMMITTED`). | FSPEC-GUARD-01 edge cases |
| F-09 | Low | Process | **Carry-forward protocol gap the FSPEC's own three CFs demonstrate:** CF-3 (behavioral gap) amended the oracle (M66); CF-1 and CF-2 (also behavior-defining) amended only prose and produced F-01/F-02. Durable rule for the pipeline: a carry-forward resolution that creates or changes decidable behavior must amend the Canonical Matrix in the same edit, not just the flow text — otherwise the declared one-test-per-row oracle silently under-covers exactly the newest behavior. | § Carry-Forward Resolutions |

## Questions

| ID | Question |
|----|---------|
| — | None. Every branch I could not write a test for is filed as a finding with the missing row or wording fix named. |

## Positive Observations

- **M66 is a model closure of F4-01** — not just the row, but the stated kill condition ("an (A)-only implementation must fail this row"), the G6 re-run extension for the allow side, and the hermeticity argument. This is what closing an oracle gap should look like.
- The single-oracle-by-reference design (matrix cited per flow, never duplicated) eliminates the FSPEC/REQ drift class entirely; the Linked Requirements table's per-flow row citations reconciled cleanly against the REQ matrix on re-derivation.
- FSPEC-GUARD-06's mechanical decision-path → reason-code table is exactly the right shape for a substring oracle: every one of the six codes is reachable from a named decision point, and I found no mismatch diffing it against REQ-GUARD-07 and the G-matrix.
- Fixture constructibility is sound throughout: every git state G1–G10 (bare local origin, detached HEAD, stale-ref, unreachable-origin via bogus `file://` URL), degraded mode (restricted `PATH`), and both M64/M66 cwd variants are controllable from the existing jest `runHookScript` harness with no network.
- The behavior-only discipline (regex prose and message text delegated to TSPEC, tests bound to prefix + substrings) keeps the oracle stable against wording churn.

## Recommendation

**Needs revision**

F-01 and F-02 must be fixed before this FSPEC binds the oracle for TSPEC/PROPERTIES: both carry-forward resolutions CF-1 and CF-2 define behavior that no matrix row can falsify, so an implementation of the superseded `>&` rule or a word-verbs-only degraded matcher passes the entire declared acceptance suite green — the exact regression class US-04 exists to catch, in the document chartered to amend the matrix. Required changes: add the discriminating rows named in F-01 and F-02 (with M33 re-run-set updates where applicable) and resolve the F-03 precedence wording. The Low findings (F-04–F-08) can be absorbed in the same edit; F-09 is process signal for harvest, not a change to this document.
