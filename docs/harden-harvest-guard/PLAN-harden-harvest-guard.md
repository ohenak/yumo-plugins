---
Status: Draft
Author: se-author
Version: 1.0
Feature: harden-harvest-guard
---

| Field | Value |
|---|---|
| Upstream | REQ → FSPEC → TSPEC → DECISIONS → **PLAN** |
| Downstream | PROPERTIES, IMPL |
| Cross-Reviews | (none yet — added as reviews land) |
| LEARNINGS | docs/harden-harvest-guard/LEARNINGS-harden-harvest-guard.md |

# PLAN — harden-harvest-guard

Execution plan for the fail-closed rewrite of `pdlc/hooks/scripts/guard-harvest-before-delete.sh` (bash wrapper + embedded Python parsing/verification engine), the anchored `Scope:` detection in `pdlc/hooks/scripts/check-scope-field.sh`, and the matrix-driven test suite binding every row **M01–M90 / S01–S07** of the REQ v1.7 Canonical Block/Allow Matrix.

Sources: [REQ-harden-harvest-guard.md](REQ-harden-harvest-guard.md) v1.7 · [FSPEC-harden-harvest-guard.md](FSPEC-harden-harvest-guard.md) v1.4 · [TSPEC-harden-harvest-guard.md](TSPEC-harden-harvest-guard.md) v1.1 · [DECISIONS-harden-harvest-guard.md](DECISIONS-harden-harvest-guard.md) v1.2 (CFD-1–CFD-8 are **binding** implementation dispositions — se-implement absorbs them without re-review).

**Prior-phase baseline pre-flight:** not applicable — this feature rewrites two self-contained hook scripts and extends an existing jest suite; there are no `BL-PREREQ` symbols from a prior-phase baseline, so no `P2-00` gate task is needed (skip logged here per the PLAN convention).

---

## Summary of what's being built

| Deliverable | File | TSPEC ref |
|---|---|---|
| Shared row-id registry (`M_ROWS`, `S_ROWS`, `M33_RERUN_IDS`) | `pdlc/workflows/__tests__/helpers/guardRowIds.js` (new) | § 6.3 |
| Hermetic git fixture library + runners (`buildFixture` G1–G10/degraded/nested/secondRepo/M77, `runGuard`, `runScopeCheck`, `BASH_ABS`, `expectBlock`/`expectAllow`) | `pdlc/workflows/__tests__/helpers/guardFixtures.js` (new) | §§ 6.1–6.2 |
| Matrix test suite (M01–M90, M33 G6 re-run, dispatch-map bespoke blocks, self-audit meta-tests, P-D1/P-QUOTE tables, self-test binding, CFD-8 gate pin) | `pdlc/workflows/__tests__/guardMatrix.test.js` (new) | §§ 6.3, 6.6 |
| PROP-COMPAT-05 migration + S01–S07 suite (PROP-COMPAT-04 retained) | `pdlc/workflows/__tests__/hookCompatibility.test.js` (modified) | §§ 6.4–6.5 |
| Fail-closed guard rewrite (wrapper + degraded matcher + embedded Python engine + `--self-test`) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` (rewritten, same filename — C16) | §§ 3.0–3.8, 6.6 |
| Anchored scope check (basename filter + P1–P3 EREs) | `pdlc/hooks/scripts/check-scope-field.sh` (modified in place) | § 5 |
| Docs/traceability sweep (hook descriptions now describe fail-closed semantics) | `pdlc/README.md`, `CLAUDE.md`, this PLAN | — |

---

## Task table

Status key: ⬚ Not Started | 🔴 Red | 🟢 Green | 🔵 Refactored | ✅ Done

Batching rules honored: max 5 parallel tasks per batch; **tasks in the same batch never touch the same file**. `guard-harvest-before-delete.sh` is a single-file deployable (DEC-01/DEC-02), so TASK-06 → TASK-08 → TASK-09 → TASK-10 → TASK-11 → TASK-12 form a strict serial chain.

| # | Batch | Task | Test File | Source File | Depends on | Status |
|---|---|---|---|---|---|---|
| TASK-01 | B1 | **[Fake first]** Row-id registry: export `M_ROWS` (`M01`…`M90`), `S_ROWS` (`S01`…`S07`), and `M33_RERUN_IDS` — the maintained copy of the REQ v1.7 M33 G6 re-run enumeration (M01, M02, M04–M24, M44–M54, M60–M64, M66–M67, M73–M74, M79–M80, M82, M84–M90), documented at its declaration as the single named comparison source (TE F-07(b)). Lives in the jest-ignored helpers dir (C14) | n/a (helper; consumed by meta-tests in TASK-03/04) | `pdlc/workflows/__tests__/helpers/guardRowIds.js` | — | ⬚ |
| TASK-02 | B1 | **[Fake first]** Hermetic fixture library (TSPEC §§ 6.1–6.2): default-fixture state builders `G8` (default), `G6`, `G7`, `G4`/`G5` (no initial push), `G2`, `G3` (detach), `G9`, `G10` (stale tracking ref via `update-ref`), `G10-unreachable`, `G1` (plain mkdtemp, never `git init`), `M77` (**corrupt loose object** behind `origin/feat-f` + builder self-check that throws unless the `ls-tree` probe fails nonzero with non-empty stderr — C18/DEC-04), `nested` (M67), `secondRepo` (M86); `degradedEnv()` (empty-mkdtemp-dir `PATH`); `BASH_ABS` resolved once at module load under the parent env, `/bin/bash` fallback (C17); `runGuard(fixture\|null, {command\|stdinRaw, stdinCwd?, spawnCwd?, env?})` — assembles stdin JSON or passes `stdinRaw` verbatim, spawns `spawnSync(BASH_ABS, [scriptPath], …)`, `CLAUDE_PROJECT_DIR` defaults to `repoDir` and is unsettable via `env: {CLAUDE_PROJECT_DIR: undefined}` (C11), **`spawnCwd` defaults to `fixture.repoDir` (null → fresh mkdtemp scratch dir; NEVER `process.cwd()` — TE F-09)**; `runScopeCheck(filePath, content)` companion for the S-rows; `expectBlock(res, reason, substrings)` asserting exit 2 + `pdlc-guard[<REASON>]` prefix + every substring, `expectAllow(res)` asserting exit 0. Hermeticity: all temp trees `mkdtempSync` + `afterEach` cleanup, local identity config, `-c protocol.file.allow=always`, no network anywhere | n/a (helper; exercised by every test in TASK-03/04/05) | `pdlc/workflows/__tests__/helpers/guardFixtures.js` | — | ⬚ |
| TASK-03 | B2 | **Red matrix suite — core.** Literal `MATRIX` table (`{id, state, command\|stdinRaw, stdinCwd, spawnCwd, env, bespoke?, expect:{exit, reason, substrings}}`) driving `it.each`, test title = row ID; the nine base groups **partition `{M01–M32, M34–M90}` with every row in exactly one group** (TSPEC § 6.3 binding table — D2 statics/verbs/compounds; mv destruction M87–M89; D3/D4 indeterminates; D1/NFR-01 allows + opaque recursion; cwd-union rows M17/M64/M65/M66/M80/M85/M86; git-state rows M34–M36/M38–M40/M57–M59/M77; non-repo rows M37/M75/M76/M83; contract rows M41 (both `not-json{` and empty variants in the one row-test)/M78/M81; degraded rows M42–M43/M68–M72 via `degradedEnv()`). Bespoke rows per **CFD-7 dispatch-map rule**: `bespoke: true` entries filtered from `it.each`; dedicated `it()` blocks generated mechanically from an `id → orchestration function` dispatch map. **M33 G6 re-run**: one parameterized `it.each` iterating `M33_RERUN_IDS` directly against `G6` fixtures (+nested/secondRepo variants where the base row needs them), expecting ALLOW. Oracle substrings assembled **mechanically** per the § 6.3 contract table **plus CFD-3 conjuncts** (`NOT_COMMITTED` → `LEARNINGS-f.md`, `/pdlc:harvest-learnings`, `then commit`; `INDETERMINATE` → row's unresolvable-operand raw text + `ailing closed`; `DEGRADED` → `python3` + `interpreter`; `NO_REPO` → `outside a git repository`; `PARSE_ERROR` → `unparseable`; G4 → `git push -u origin`; G7 → `git push`; G10 → `git push` + `git fetch`) **and CFD-4** (M21 asserts the fixed `piped input` marker). Self-audit meta-tests: **(1)** MATRIX id list sorted deep-equals `M_ROWS` minus `M33` AND length === Set size AND dispatch-map key set set-equal to the `bespoke: true` id set (CFD-7); **(2)** `M33_RERUN_IDS ⊆ M_ROWS`, no duplicates. Entire suite is 🔴 against the current script | `pdlc/workflows/__tests__/guardMatrix.test.js` | (tests only — exercises `pdlc/hooks/scripts/guard-harvest-before-delete.sh`) | TASK-01, TASK-02 | ⬚ |
| TASK-04 | B2 | **Red S-row suite + PROP-COMPAT-05 migration.** Delete the PROP-COMPAT-05 block (`hookCompatibility.test.js:156–244`, C12) and replace with a migration comment pointing at TSPEC § 6.5 (assertion-level map: non-repo cell → M37 `NO_REPO`; in-repo no-LEARNINGS → M01; disk-only-LEARNINGS-allows **inverted** → M01 blocks / any-`LEARNINGS-*` globbing retired by M36; allow-side successor = M33/G6 re-run; `rm CODE_REVIEW…` covered by the M06+M01 pair — no new row, TE F-11). Add S01–S07 describe block: seven table-generated tests from a local `S_CASES` table via `it.each` (title = row ID), tmpdir/writeFileSync pattern reused from PROP-COMPAT-04 (C13); oracle: silent = exit 0 + empty stdout; warning = exit 0 + stdout containing `hookSpecificOutput` and `Scope`. **Meta-test (3):** `S_CASES` id set equals `S_ROWS` (TE F-07(a)). **PROP-COMPAT-04 retained untouched** — its "already tagged" fixture is a P1 match and must keep passing. S04–S06 negatives 🔴 against the current substring grep | `pdlc/workflows/__tests__/hookCompatibility.test.js` | (tests only — exercises `pdlc/hooks/scripts/check-scope-field.sh` and the guard) | TASK-01, TASK-02 | ⬚ |
| TASK-05 | B3 | **Red supplementary suite** (same file as TASK-03 → later batch; all titles outside matrix-row accounting): **P-D1 table** (`P-D1-nn`) — verb-free commands over the guarded-token vocabulary (guarded paths in `echo`/`grep`/`git commit -m`/heredoc-body/quoted-string positions × connectors × `>>`/`2>&1` lookalikes), every case `expectAllow`; **P-QUOTE table** (`P-QUOTE-nn`) — for each row in the designated static-operand block list (M01, M04–M07, M47–M51, M53–M54, M60–M63, M90) re-run with the guarded operand rewrapped in `"…"` and `'…'`, verdict + reason must equal the base row's; **self-test binding** — python3-gated test spawning the entrypoint with `["--self-test"]`, asserting exit 0 AND printed case count ≥ the named floor constant `SELF_TEST_MIN_CASES` (declared here, near the § 6.6 coverage-area enumeration; never below 1 — CFD-6); **CFD-8 gate-pinning test** — `runGuard(fixture, {stdinRaw: "--self-test", env: {GUARD_SELF_TEST: "1"}})` → `expectBlock(res, "PARSE_ERROR", ["unparseable"])` (pins the CFD-1 env-clear/argc gate) | `pdlc/workflows/__tests__/guardMatrix.test.js` | (tests only) | TASK-03 | ⬚ |
| TASK-06 | B3 | **Guard rewrite 1/6 — bash wrapper** (TSPEC §§ 3.1–3.2): script header documenting `GUARD_FETCH_BEFORE_CHECK` / `GUARD_FETCH_TIMEOUT_SECS` (BR-03-1), the exit-code contract (0 allow / 2 block), and `--self-test`; step-0 `--self-test` gate — `[ "${1:-}" = "--self-test" ]` → invoke Python with `argv[1] = --self-test` + `GUARD_SELF_TEST=1`; **on the hook path explicitly `unset GUARD_SELF_TEST`/strip it from the Python env (CFD-1)**; stdin read via bash builtins only — `IFS= read -r -d '' input \|\| true` (replaces load-bearing-wrong `$(cat)`, C4); interpreter probe retained verbatim (C6: `python3`/`python`/`py`, executed to skip the Windows Store stub); **degraded coarse matcher** in `[[ … ]]` builtins only over raw `$input` (§ 3.2: word verbs `rm\|unlink\|mv\|truncate\|find` with word-boundary regex, two-word `git clean`, `>` character class; conjunct `docs/`\|`CROSS-REVIEW`\|`CODE_REVIEW`) → exit 2 with the `DEGRADED` message (names missing interpreter + `python3` remedy) or exit 0; interpreter present → `"$PY_BIN" - "$input" <<'PY'` handoff (C5), wrapper propagates the Python exit code. Python engine body is a placeholder failing closed until TASK-08. **Green checkpoint: degraded rows M42–M43, M68–M72 (incl. M71 `DEGRADED`-precedence, M72 empty-stdin allow)** | `pdlc/workflows/__tests__/guardMatrix.test.js` (red rows from TASK-03: M42, M43, M68–M72) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` | TASK-03 | ⬚ |
| TASK-07 | B3 | **check-scope-field.sh rewrite** (TSPEC § 5, minimal diff): **Change 1** — basename filter: `case "$(basename "$fp")" in CROSS-REVIEW-*.md\|CODE_REVIEW-*.md)` (anchored, no leading `*`; replaces the full-path substring match, C8/TE F-08); **Change 2** — replace the case-insensitive substring grep (C7, `cross-feature` alternate dropped) with the three case-sensitive REQ-GUARD-04 EREs: P1 `^[[:space:]]*Scope:` · P2 `^[[:space:]]*\*\*Scope(\*\*:\|:\*\*)` · P3 `\|[[:space:]]*Scope[[:space:]]*\|`; any match → silent. Interpreter probe, `[ -f ]` guard, advisory-JSON emission (C9), always-exit-0 posture, and the warning text all retained. **Green checkpoint: S01–S07 + retained PROP-COMPAT-04** | `pdlc/workflows/__tests__/hookCompatibility.test.js` (red rows from TASK-04: S01–S07, esp. negatives S04–S06) | `pdlc/hooks/scripts/check-scope-field.sh` | TASK-04 | ⬚ |
| TASK-08 | B4 | **Guard rewrite 2/6 — Python intake + tokenizer/segmenter** (TSPEC §§ 3.3–3.5.1): top-level `except Exception` handler installed first (layer 2 of the two-layer discipline — emits the exact § 3.7 `PARSE_ERROR` template + ` (internal parse failure: <ExceptionType>)`, exit 2); intake gate — `json.loads` failure or empty raw → `PARSE_ERROR` (M41); **presence test, not falsiness** — `"command" in tool_input and tool_input["command"] is not None` (M78 blocks, `""` → zero segments → D1 allow, M81); `cwd` field extraction with relative-value resolution (§ 3.4); data model `Token {raw, value, expansion_active, glob_active}` / `Redir {op, target, destructive}` / `Segment {tokens, redirs, connector_in, piped_from}`; `strip_heredocs` (M29); `segment` — quote-aware split at unquoted `;`/`&&`/`\|\|`/`\|` (incl. `\|&`)/`&`/newline, pipe topology, transparent `(`/`{` group openers; `tokenize` — character scanner (normal/single-quote/double-quote/backslash states, DEC-03), `expansion_active` on unescaped non-single-quoted `$` backtick `$(` `<(`, `glob_active` on unquoted `* ? [`; redirection classification per § 3.5.1 (fd-duplication `N>&M`/`>&-` excluded; `>&` lexical rule — fd-form iff word is all digits or exactly `-` (CF-1/M67 class); truncation family `>`, `>\|`, any `N>`, `&>`, `>& word`; `>>`/`N>>` append never destructive; `<`/`<<<` data). **Green checkpoint: M41, M78, M81** | `pdlc/workflows/__tests__/guardMatrix.test.js` (M41, M78, M81; tokenizer rows M44–M46, M14–M15 pinned but green later) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` | TASK-06 | ⬚ |
| TASK-09 | B5 | **Guard rewrite 3/6 — verb identification + operand classification + union-cwd + resolution + enumeration** (TSPEC §§ 3.5.2–3.5.3): `identify_verb` — skip `NAME=value` assignments (RHS recorded into the compound-command-wide `assignments` map from **every** segment — D3 channel 2) and transparent prefixes (`command`, `env`, `sudo`, `nice`, `time`, with their flags); `rm` recursive-capable iff `^-[A-Za-z]*[rR]` or `--recursive`; `git rm`/`git clean` (empty pathspec → D4 candidate); `find` deletion forms (`-delete`, `-exec`/`-execdir` + deletion verb up to `;`/`+`; roots = args before first `-`/`(`/`!` token; **no root → `.`**; always recursive-capable); `xargs` unwrap (effective verb = first non-flag arg, operands = piped stream, always indeterminate); `eval`/`bash -c`/`sh -c` opaque recursion — static payload re-parsed at `depth+1`, **cap 8, over-cap = opaque → D1-eligible, never deletion-shaped** (DEC-08/RR-3; M73 pins depth 2); substitutions in non-deletion-verb arguments NOT re-parsed (RR-6); `classify_operands` (flags until literal `--`; static iff not `expansion_active`); `EffectiveCwd` — `root_A` = stdin cwd else `CLAUDE_PROJECT_DIR` else process cwd (M85), `root_B` = process cwd always, dedup union applied to **every** segment (M80); `cd` static-absolute collapse / static-relative per-root join / indeterminate-or-`-` poisoning (D3 channel 4) / no-operand → `$HOME` else poisoned; `pushd`/`popd` untracked (RR-5); `resolve_static` — normpath per root, `glob.glob(recursive=True)` **plus the literal pattern paths** ; `enumerate_guarded` — `os.walk` of `repo_root/docs/*/`, basename fnmatch `CROSS-REVIEW-*.md`/`CODE_REVIEW-*.md` at any depth, feature = first path segment under `docs/` | `pdlc/workflows/__tests__/guardMatrix.test.js` (rows M04–M08, M17–M25, M47–M51, M60, M63–M66, M73, M80, M85–M86 — green at TASK-11) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` | TASK-08 | ⬚ |
| TASK-10 | B6 | **Guard rewrite 4/6 — git-state verification + message catalog** (TSPEC §§ 3.6–3.7): `verify_feature(repo_root, feature)` with the **failure-vs-absence classifier** (clean absence = exit 0/1 + empty stdout + empty stderr → "absent"; anything else = query failure → deterministic `NOT_COMMITTED`, never exit 0 — M77/DEC-04); idioms `git symbolic-ref --quiet --short HEAD` (detached → G3), `git remote` (no origin → G2), `git ls-tree --name-only HEAD -- <learnings>` (G2/G3 fallback), G-DP2 fetch iff `GUARD_FETCH_BEFORE_CHECK == "true"` exactly AND origin + branch exist (formability), `subprocess.run(timeout=GUARD_FETCH_TIMEOUT_SECS)` (int-parse, unparseable/≤0 → 10; `TimeoutExpired`/nonzero → proceed on local state, M59), `git show-ref --verify --quiet refs/remotes/origin/<branch>` (clean absence → G4 `NOT_PUSHED` + `git push -u origin <branch>` / G5 `NOT_COMMITTED`), `git ls-tree --name-only origin/<branch> -- <learnings>` (present → G6 VERIFIED; absent + in HEAD → G7/G10 `NOT_PUSHED` + `git push` + `git fetch` hint; absent everywhere → G8 `NOT_COMMITTED`); exact top-level filename `docs/f/LEARNINGS-f.md` only (G9/M36); memoized per feature (BR-03-3); env thresholds read at invocation, never baked. **Layer-1 exception wraps** on every `subprocess.run` (G-DP1 exceptions → `NO_REPO`; `verify_feature` exceptions → query-failure `NOT_COMMITTED` — § 3.3). `emit_block` — the exact § 3.7 template catalog (all six reason codes, prefix `pdlc-guard[<REASON>]:`), G4 vs G7/G10 `NOT_PUSHED` variants, query-failure appendix, **CFD-4 `piped input` interpolation** for stream-indeterminate D3 operands (M21) | `pdlc/workflows/__tests__/guardMatrix.test.js` (rows M34–M40, M57–M59, M77 + substring oracles of every BLOCK row — green at TASK-11) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` | TASK-09 | ⬚ |
| TASK-11 | B7 | **Guard rewrite 5/6 — decision engine + mv flow** (TSPEC §§ 3.5.4–3.5.5, BR-01-1 ordering **D1 → G-DP1 → D2 → D3 → D4 → allow**, first match wins): **D1** — no deletion-shaped segment (verb in executable position incl. recursive payload results, or destructive redirection) → exit 0; **G-DP1** eager repo check `git -C <root_A> rev-parse --is-inside-work-tree` once, before D2–D4 (nonexistent dir/nonzero/launch failure → `NO_REPO`; M37, M75, M76, M83, M85 neither-signal pass-through); enumeration anchor `repo_root = CLAUDE_PROJECT_DIR` else `rev-parse --show-toplevel` of root A (M86); **D2** per static operand via both roots — (i) guarded file, (ii) inside unverified guarded dir (path-prefix), (iii) the dir itself, (iv) ancestor for recursive-capable forms only; multiple affected features → sorted, first unverified decides; destructive redirections take the D2/D3 path per target determinacy; **`mv_flow` instead of plain D2 for mv** (BR-02-1): step 0 multi-source independent evaluation (M79), step 1 canonicalize, step 2 static-source guarded-dir/ancestor test before any indeterminacy jump (M13, M74), **step 2b static-destination destruction test** — static dest, not an existing dir, no trailing `/`, names an existing guarded file in an unverified guarded dir → G-state code, source guardedness AND determinacy irrelevant (M87 — a step-3-first implementation emits `INDETERMINATE` and fails; M84/M88 also land here), step 3 indeterminate source/dest → D3 (M82), step 4 resulting-path arm `dest/basename(source)` destruction test (M89) + trailing-slash corner, step 5 guarded source ALLOW iff same-feature subtree + basename pattern kept (M11, M52; else M09/M10/M12); **D3** — indeterminate operand/redir target AND docs-reference via the four channels (own tokens / assignment RHS / piped producer / cd context; segment-scoped, sibling `docs/` never qualifies — M55–M56) AND ≥1 unverified guarded dir → `INDETERMINATE`; **D4** — pathspec-less `git clean` → `INDETERMINATE` iff any unverified guarded dir (M08); fall-through → exit 0 (M03, M25, M32). **Green checkpoint: the entire remaining MATRIX + the M33 G6 re-run set + P-D1/P-QUOTE tables** | `pdlc/workflows/__tests__/guardMatrix.test.js` (all remaining M-rows) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` | TASK-10 | ⬚ |
| TASK-12 | B8 | **Guard rewrite 6/6 — `--self-test` mode** (TSPEC § 6.6, DEC-01, CFD-1/CFD-6): Python-side conjunction gate `sys.argv[1] == "--self-test" and os.environ.get("GUARD_SELF_TEST") == "1"` → `sys.exit(run_self_test())` (unforgeable because the wrapper strips `GUARD_SELF_TEST` on the hook path — TASK-06; the sanctioned argc-gate variant may be substituted, **pick one, don't stack both**); embedded `SELF_TEST_CASES = [(case_id, input_text, expected_structure)]` calling the § 3.5 functions directly (no JSON envelope, no git, no filesystem), covering: tokenizer (quote states, backslash escapes, `expansion_active`/`glob_active` placement, adjacent redirections, `>&` lexical rule incl. digit-leading words, `N>`/`N>>`/`N>&M`), segmenter (every connector incl. `\|&`, quoted operators as data, nested quotes, group openers), heredoc stripper (quoted/unquoted delimiters, `<<-` tabs, operator lookalikes in bodies, `<<<`), verb identifier (assignment/transparent prefixes, `git rm`/`git clean`, `find` forms + root defaulting, `xargs`/`eval`/`bash -c` unwrap, depth-cap opacity); mismatch → failing case ids on stderr, exit 1; success → **case count on stdout** (the CFD-6 floor oracle), exit 0. Case count must meet `SELF_TEST_MIN_CASES` (TASK-05). **Green checkpoint: self-test binding test + CFD-8 gate pin + full suite green (`npm test`)** | `pdlc/workflows/__tests__/guardMatrix.test.js` (self-test binding, CFD-8) | `pdlc/hooks/scripts/guard-harvest-before-delete.sh` | TASK-11, TASK-05 | ⬚ |
| TASK-13 | B9 | **Docs/traceability sweep:** update the hook descriptions in `pdlc/README.md` and root `CLAUDE.md` (guard-harvest-before-delete now: fail-closed, committed-and-pushed verification, D1–D4/degraded/G-matrix semantics, env thresholds; check-scope-field: anchored P1–P3 patterns) — they currently describe the "LEARNINGS exists on the branch" disk-era behavior; verify traceability by grepping test titles against `M_ROWS`/`S_ROWS` (the meta-tests enforce it mechanically — confirm they ran); confirm script headers document `GUARD_FETCH_BEFORE_CHECK`/`GUARD_FETCH_TIMEOUT_SECS` + `--self-test`; update this PLAN's Status column. **Per CFD-2: do NOT open a REQ revision for the M77 fixture parenthetical** — TSPEC § 6.2 is authoritative meanwhile; the correction rides the next REQ touch | n/a | `pdlc/README.md`, `CLAUDE.md`, `docs/harden-harvest-guard/PLAN-harden-harvest-guard.md` | TASK-12 | ⬚ |
| TASK-14 | B10 | **Final gate + refactor pass:** single-command run `npm test` in `pdlc/workflows/` — every row M01–M90 (M33 via the parameterized re-run) and S01–S07 has exactly one asserting test and all pass (the four self-audit meta-tests prove the accounting); confirm zero network I/O (only local bare/invalid-path remotes); refactor both scripts and the test files for clarity with the suite as the safety net (🔴→🟢→🔵 discharge); commit and push | all test files | (refactor only — no behavior change) | TASK-13 | ⬚ |

---

## Batch overview (topological)

| Batch | Tasks | Parallelism | Notes |
|---|---|---|---|
| B1 | TASK-01, TASK-02 | 2 | Test scaffolding — [Fake first]; different files |
| B2 | TASK-03, TASK-04 | 2 | Red acceptance tests; different files |
| B3 | TASK-05, TASK-06, TASK-07 | 3 | TASK-05 extends guardMatrix.test.js (serialized after TASK-03); TASK-06/07 start implementation on disjoint scripts |
| B4–B8 | TASK-08 → TASK-12 | 1 each | Strict serial chain — all touch `guard-harvest-before-delete.sh` (single-file deployable, DEC-01/DEC-02) |
| B9 | TASK-13 | 1 | Docs sweep |
| B10 | TASK-14 | 1 | Full-suite gate, refactor, commit/push |

No batch exceeds 5 parallel tasks; no two same-batch tasks share a file target.

---

## Dependency and TDD-ordering notes (binding for se-implement)

1. **Test-first is structural in this plan:** every implementation task (TASK-06–TASK-12) has its red-test rows written in B2/B3 (TASK-03/04/05) before any implementation batch runs. Named ATs per implementation task are the matrix row IDs listed in its Test File column — e.g. TASK-06's ATs are M42, M43, M68–M72; TASK-07's are S01–S07; TASK-08's are M41, M78, M81.
2. **Green checkpoints on the serial chain:** the guard script cannot pass end-to-end rows until the decision engine exists, so intermediate chain tasks (TASK-08–TASK-10) assert their named intake/degraded rows green and leave engine rows red; **full matrix green is the TASK-11 exit criterion**, self-test/supplementary green is TASK-12's. A chain task must never make a previously-green row red.
3. **Fail-closed from the first commit of the chain:** TASK-06 leaves the Python body failing closed (placeholder exits 2 `PARSE_ERROR` class), and TASK-08 installs the top-level handler before any parsing code — at no intermediate commit does the guard fail open.
4. **CFD-1–CFD-8 are binding, not advisory** (DECISIONS v1.2): env-clear gate (TASK-06/12), CFD-3 substring conjuncts + CFD-4 `piped input` marker (TASK-03/10), CFD-6 case-count floor (TASK-05/12), CFD-7 dispatch-map bespoke convention + set-equality audit (TASK-03), CFD-8 gate pin (TASK-05). CFD-2 constrains TASK-13 (no REQ touch).
5. **Helpers are jest-ignored** (C14) — TASK-01/02 outputs produce no test files of their own; their correctness is exercised transitively (the M77 builder self-check makes fixture drift loud at build time).
6. **hookCompatibility.test.js is touched only by TASK-04** — the PROP-COMPAT-04 retention and S-row addition land in one edit, so no other task may modify that file.

---

## Integration points

| Point | Contract | Cite |
|---|---|---|
| `pdlc/hooks/hooks.json` | **Unchanged.** Wires both scripts by absolute plugin path, passes no argv — script filenames must not change; the `--self-test` argv gate is unreachable from production wiring | C16 |
| `runHookScript` pattern | Reused (cite-and-reuse, REQ-GUARD-05): `spawnSync` child-process shape from `hookCompatibility.test.js:47–59`; degraded rows extend it with `BASH_ABS` absolute-path spawning | C10, C17 |
| `pdlc/workflows/package.json` | `npm test` remains the single command (jest 29, ESM); `testPathIgnorePatterns` already ignores `__tests__/helpers/` — new helper files need no config change | C14, C15 |
| PROP-COMPAT-04 suite | Retained untouched; its `Scope: Local` frontmatter fixture is a P1 match under the new greps | C13 |
| Existing interpreter probe | Retained verbatim from the current guard (executes candidates to skip the Windows Store stub) | C6 |
| Runtime dependencies | bash, git, Python ≥ 3.8 stdlib only (`sys, json, os, re, glob, fnmatch, subprocess`); degraded mode requires zero external binaries before the probe | TSPEC § 3.3 |

---

## Definition of Done

- [ ] Every row M01–M90 and S01–S07 has exactly one asserting test (M33 via the parameterized G6 re-run over `M33_RERUN_IDS`); all pass under a single `npm test` in `pdlc/workflows/`
- [ ] The four self-audit meta-tests pass: base allocation (set-equality + length), re-run integrity, S-row coverage, CFD-7 dispatch-map key equality
- [ ] Block oracle asserts `pdlc-guard[<REASON>]` prefix + all required substrings (incl. CFD-3 conjuncts and CFD-4 `piped input`) — never full prose
- [ ] No test performs network I/O; all git fixtures hermetic (`mkdtempSync`, local bare origin, cleanup in `afterEach`)
- [ ] Degraded rows run under an empty child `PATH` via `BASH_ABS`; wrapper pre-probe section uses bash builtins only
- [ ] PROP-COMPAT-05 block deleted with the § 6.5 migration comment; PROP-COMPAT-04 still passing
- [ ] `--self-test` passes with case count ≥ `SELF_TEST_MIN_CASES`; CFD-8 gate-pin test passes; `GUARD_SELF_TEST` stripped on the hook path
- [ ] Script headers document `GUARD_FETCH_BEFORE_CHECK` (default `false`), `GUARD_FETCH_TIMEOUT_SECS` (default `10`), exit-code contract, `--self-test`
- [ ] `check-scope-field.sh` uses the basename filter + case-sensitive P1–P3 EREs; advisory/always-exit-0 posture retained
- [ ] Hook descriptions in `pdlc/README.md` and `CLAUDE.md` updated to the fail-closed semantics; no REQ revision opened (CFD-2)
- [ ] Every task 🔵/✅ in the table above; work committed and pushed on `feat-harden-harvest-guard`

---

## Revision History

| Version | Date | Change |
|---|---|---|
| 1.0 | 2026-07-02 | Initial PLAN from REQ v1.7 / FSPEC v1.4 / TSPEC v1.1 / DECISIONS v1.2: 14 tasks in 10 topological batches (scaffolding → red matrix/S-row suites → wrapper/scope-check/supplementary → serial 5-step embedded-Python chain → docs sweep → final gate); single-file constraint of DEC-01/DEC-02 drives the serial chain; CFD-1–CFD-8 bound to their implementing tasks |
