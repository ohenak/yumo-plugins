# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** docs/pdlc-review-loop-hardening/REQ-pdlc-review-loop-hardening.md (v1.2)
**Date:** 2026-07-29
**Iteration:** 3
**Scope:** technical feasibility, implementability, integration risk (REQ altitude)
**Mode:** delta re-review. Baseline reviewed at v2 = `66f6174` (v1.1); diff taken against `174ec18` (v1.2). Only changed sections re-read.

## Disposition of v2 findings

| v2 ID | Sev | Status | Evidence in v1.2 |
|---|---|---|---|
| F-01 | High | **Resolved** (one consequence of the fix is contested below — see new F-01/F-02) | §4a A-7 records the harvest deletion with four SKILL citations (verified: `harvest-learnings/SKILL.md:3`, `:10`, `:27`, `:100` all say `CROSS-REVIEW-*` + `CODE_REVIEW-*`), AC-4.2b adds a two-tier evidence model with a harvest-surviving approval record, `harvest-learnings` is in `Targets`, AC-4.2c states the legacy fail-closed case, AC-2.3b's falsified example is replaced by reachable examples A/B, R-6 no longer rests on it, R-8 records the widened scope, O-21 owns the grammar. Verified at HEAD: `docs/pdlc-workflow-distribution/` has zero `CROSS-REVIEW-*`, and its LEARNINGS `Harvested from` row does record 62 cross-reviews + 3 `CODE_REVIEW-*` as filenames/counts with no verdicts. |
| F-02 | Medium | **Resolved** | AC-3.1 restated as "may emit"; AC-3.1a fixes the referent as bytes the call emits, contrasts whole-file write vs. edit-shaped call, and names the required shape (skeleton may be a whole-file write; every later section by edit/append; whole-file rewrite forbidden once the document exceeds the budget). AC-3.6 inherits it. |
| F-03 | Medium | **Resolved** | AC-3.5e + §4a A-8: the three surfacings (value / nothing / throw) are recorded as unmeasured, and the wrapper is required to catch the fault, re-measure from disk, and account the attempt on every path, with a prohibition on an unhandled halt that bypasses the count. O-22 owns the three-way fixture. |
| F-04 | Low | **Resolved** | Re-measured and re-derived. Verified at `9220a20`, `wc -lc`: `REQ-pdlc-workflow-distribution.md` = 1017 / 89,069; TE-v1 = 88 / 12,767; SE-v1 = 48 / 11,933. 89,069 ÷ 12,000 = 7.42 ✓; "67 bytes above 11,933" ✓; "0.94× of 12,767" ✓; the false "rounded down" claim is retracted in-document; the revision rule now demands the producing command. |
| F-05 | Low | **Resolved** | The `Citation baseline` row is narrowed to §H-1–§H-4, §4a and AC-5, and states that short in-line references elsewhere may be bare; O-16's stricter bar still binds the FSPEC. |
| F-06 | Low | **Resolved** | AC-3.2a now states the true cost (N commits = N replay steps = N conflict points, Phase DOD Step 0 halts on conflict) and retracts the "unaffected" claim, drawing the right conclusion (coarse sections). |

All six v2 findings are closed, and SE-v2 Q-01/Q-02/Q-03 are answered in-document (AC-4.2b, AC-1.4a, AC-4.7a).

## Findings (new — changed sections only)

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | Medium | Local | **AC-4.2b's tier-2 record carries no temporal anchor, so AC-4.4 cannot be evaluated from it — yet AC-4.2b claims it can.** The record is enumerated as "per approving round, the document type, the round index, each role and its verdict value", and the sentence concludes this is "enough for AC-4.1a's same-round test **and AC-4.4's staleness test** to be evaluated after the artifacts are gone". It is not. AC-4.4 asks whether "the document has been modified after the approving reviews"; under tier 1 the comparison has a referent (the cross-review file's commit/mtime — O-8), but tier 2 exists precisely because those files were deleted, and none of the four enumerated columns dates anything. `LEARNINGS-{feature}.md`'s own commit is always later than the approvals it records, so it cannot serve. The consequence is a fork with no good branch: evaluate AC-4.4 fail-closed and tier 2 **never grants a skip**, which reinstates the exact inertness AC-4.2b was added to remove; or treat unevaluable staleness as "not stale" and tier 2 **fails open**, laundering post-approval edits — the one outcome R-1 and AC-4.2a's whole design forbid. O-21 half-notices this ("and whatever AC-4.4's staleness comparison needs"), which is an FSPEC-altitude hedge over a REQ-altitude gap: the columns are enumerated normatively here, and the sufficiency claim is made here. Fix at this altitude: name the anchor the record must carry (approving commit sha, or the document's content hash at approval — a hash is rebase-proof, which timestamps are not, per O-8's own rebase caveat), or delete the AC-4.4 clause and state that tier-2 approval is *always* treated as stale when the document changed by any measure the FSPEC can define. Corroborating evidence that harvest-authored records need a checkable derivation: `pdlc-workflow-distribution`'s existing `Harvested from` row asserts its `POSTMORTEM-R-*` was "all now deleted" while that file is present at HEAD and `harvest-learnings/SKILL.md` never instructs its deletion — harvest already mis-records what it did in the very row AC-4.2b builds beside. | AC-4.2b, AC-4.4, O-21, R-1 |
| F-02 | Medium | Local | **AC-2.3b worked example B offers AC-4.6 as a route forward that AC-4.6a — added in the same revision — explicitly forbids, and that is a no-op in that state anyway.** Example B: unresolved `POSTMORTEM-R`, no readable verdict ⇒ "the operator's route forward is AC-2.4 … **or AC-4.6 (force the phase)**". AC-4.6a, twelve lines later: "A forced phase is still subject to AC-2.3 … the force-run is **refused** … **AC-2.4's resolve-then-run is the exclusive route through that state**." Example B *is* that state, so the two clauses give opposite answers to the REQ's only worked recovery scenario. Independently, forcing is inert there: AC-4.6 overrides *recorded approval*, and in example B there is no readable approval — the phase already "would run", which is what makes AC-2.3 refuse. So the sentence names a lever that does nothing and would be refused if it did. One of the two must change; on the merits AC-4.6a's rationale is the stronger position, so example B should name AC-2.4 as the sole route (and, if a bypass is wanted, AC-4.6a must gain an explicit escape with its own operator-visible record rather than leaving the contradiction to the FSPEC). | AC-2.3b worked example B, AC-4.6a |
| F-03 | Medium | Local | **`MAX_AUTHORING_DISPATCHES` reintroduces, for a strictly progressing artifact, the false-positive halt AC-3.5a was written to exclude — and the REQ does not state the counter's reset scope or the halt's classification.** AC-3.5c caps total dispatches per artifact per phase at **6**, "progress or not", terminal. Under AC-3.1's mandated pacing, a dispatch that is stall-killed after writing one or two sections *has* made progress, so AC-3.5a/AC-3.5c reset the consecutive counter every time — and a 10–12-section REQ that advances 1–2 sections per killed dispatch hits the cumulative cap at section 6–12 while every single dispatch was healthy. That is the same shape of false positive AC-3.5a rightly refuses to accept, on the same new halt path. The rationale defends 6 by asserting "a healthy document needs far fewer: one skeleton dispatch plus … a single dispatch that writes every remaining section" — but the witness for that is the *pre-pacing* observation, and under AC-3.1a's per-call byte bound the dispatch count for a large document is unmeasured, so the bound is set by argument on a threshold whose own revision rule ("requires a measured case") disallows exactly that. Two things must be stated at REQ altitude, because both decide whether the run is recoverable: (i) whether the cumulative counter is per invocation and therefore **resets on re-entry**, so an operator re-invoking continues from committed partial progress (AC-3.2/AC-3.3) — nothing in AC-3.5 says; and (ii) whether an AC-3.5d exhaustion writes a `POSTMORTEM-{phase}-{feature}.md`. If it does, AC-2.3 refuses re-entry to that phase and AC-4.6a now makes AC-2.4 the exclusive route — so a merely large document could halt the pipeline into a state that requires a human POSTMORTEM resolution to leave. Alternatively make the cumulative bound progress-aware (cap dispatches *without* progress cumulatively, or cap total dispatches per section) so cost is still bounded without penalising advance. | AC-3.5c, AC-3.5d, `MAX_AUTHORING_DISPATCHES` declaration, AC-2.3, AC-4.6a |
| F-04 | Low | Local | **The `MAX_AUTHORING_DISPATCHES` derivation's per-dispatch arithmetic does not divide.** "§H-3's six killed attempts cost ~71 min and ~1.34 M subagent tokens for zero output — **~5.3 min** and ~223 K tokens per dispatch." 1.34 M ÷ 6 ≈ 223 K ✓, but 71 ÷ 6 ≈ **11.8 min**, not 5.3 (5.3 × 6 ≈ 32 min). The cap's conclusion ("bounds the worst case at the already-observed ceiling") is unaffected, so this is Low — but it is the second consecutive round in which a threshold's *measured* derivation contained an arithmetic slip, and this threshold's revision rule is itself a measurement rule. Recompute or state what the 5.3 figure measures. | `MAX_AUTHORING_DISPATCHES` Derivation row |
| F-05 | Low | Local | **AC-3.1a understates an edit-shaped call's emitted bytes by roughly 2× for a replacement.** "An **edit / append-shaped** call emits only the replacement text, so its emitted bytes equal its new content." A replace-shaped edit must also emit the text it matches on, so replacing a 7 KB section emits ~14 KB and can breach a 12,000-byte budget while adding nothing net; a pure append is the only shape for which the stated equality holds. This matters most for AC-3.6's review/remediation agents, which are the population that edits existing sections rather than appending new ones. One clause fixes it: emitted bytes = match text + replacement text, and prefer append-shaped or narrowly scoped edits so both stay well under the budget. | AC-3.1a, AC-3.6 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | For F-03(i): is `MAX_AUTHORING_DISPATCHES` intended to be per invocation or per artifact-lifetime? A per-lifetime reading would make a document that needed 7 dispatches across two operator invocations unfinishable, which cannot be the intent — but only the REQ can say so. |
| Q-02 | AC-4.2b makes tier 2 consulted "**only** when tier 1 is absent" and treats tier-1/tier-2 disagreement as AC-4.2a's unparseable case. If tier 1 is absent for one role and present for the other (the role-asymmetric branch O-18 now covers), is that "absent" for pairing purposes — i.e. does the pair get completed across tiers, or does mixed provenance fail closed? |

## Positive Observations

- The response to SE-v2 F-01 is the strongest kind of REQ revision: rather than patching the worked example, it identified the missing durable carrier, took the scope (`harvest-learnings`), recorded the mechanism as a measured assumption (A-7, with four SKILL citations and a tree check), recorded the widened scope as a risk (R-8), stated the legacy consequence honestly (AC-4.2c), and replaced one falsified example with two reachable ones. Worked example A's "this is the state run 4 was in" is exactly the kind of grounding that keeps an example falsifiable.
- AC-3.5e + A-8 is the right handling of an unmeasurable dependency for the second time in this document: enumerate the possible surfacings, require identical accounting across all of them, make disk state the authority, and forbid the bypass path. O-22's "this is the path §H-3 actually took, so it is not an optional fixture" is a well-aimed instruction to the test author.
- AC-3.1a converts an ambiguous numeric bound into a behavioral rule about tool-call shape — the correct altitude decision, since it changes what the author SKILLs must do rather than deferring to FSPEC.
- AC-4.7a's refusal to add an unread field to `CODE_REVIEW-*` while keeping the verdict *catalogue* shared is the right seam: one grammar where it is read, no speculative contract where it is not.
- Every re-measured figure in the AC-3.1 threshold table now reproduces exactly at the declared HEAD with the declared command. The self-retracting parentheticals ("v1.1's … was simply false") make the derivation auditable rather than merely corrected.

## Recommendation

**Needs revision**

Must change before Phase F:

1. **F-01 (Medium)** — give AC-4.2b's tier-2 record an anchor AC-4.4 can compare against (commit sha or approval-time content hash), or drop the sufficiency claim and state that tier-2 approval never survives a document change. As written, tier 2 is either inert or fail-open.
2. **F-02 (Medium)** — reconcile AC-2.3b worked example B's "or AC-4.6 (force the phase)" with AC-4.6a's exclusive AC-2.4 route; they contradict, and AC-4.6 is inert in that state regardless.
3. **F-03 (Medium)** — make `MAX_AUTHORING_DISPATCHES` progress-aware or state its reset scope, and say whether an AC-3.5d exhaustion writes a POSTMORTEM (which, with AC-2.3 + AC-4.6a, decides whether a large document can halt the pipeline into a human-only recovery).

F-04 and F-05 are one-clause corrections and would not gate a round on their own.

None of the three blocking findings is an "AC has no oracle/fixture/seam" finding: F-01 is an insufficiency in a normatively enumerated record, F-02 is a direct contradiction between two clauses added in the same revision, F-03 is a false-positive halt path plus two unstated recovery semantics. §8's stopping rule does not answer any of them, and all three land in text introduced at v1.2.

VERDICT: Needs revision
{"high": 0, "medium": 3, "low": 2}
