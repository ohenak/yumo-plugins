# Cross-Review: product-manager — PROPERTIES (round 3, delta)

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-review-loop-hardening/PROPERTIES-pdlc-review-loop-hardening.md` (v1.2, 181,638 B)
**Date:** 2026-07-30
**Iteration:** 3
**Scope:** Delta re-review. Verification of the four findings in `CROSS-REVIEW-product-manager-PROPERTIES-v2.md` (0 High / 3 Medium / 1 Low) against the tree and the approved specs, plus a new-defect scan restricted to the sections v1.1 → v1.2 changed. Unchanged sections are not re-litigated. Upstream REQ v1.6 / FSPEC v1.8 / TSPEC v1.7 / PLAN v1.4 are approved, closed, and treated as fixed contracts. Citation and `file:line` drift corrected silently, never filed (R-6). Genuine upstream defects are upward-facing notes, not blocking findings (R-5).

---

## Method

| Step | What was done |
|---|---|
| Baseline | `git pull --rebase` on `feat-pdlc-review-loop-hardening` — already up to date at `b6773e7` |
| Diff | The brief's range `d6b524d..b6773e7` **omits one commit of v1.2**: `d6b524d` is itself *"§1.3 — withdraw 'no new ledger row' claim"*, i.e. the F-04 fix. The reviewed range was therefore widened to `95529be..b6773e7` (v1.1 → v1.2, ten commits). Reviewing the brief's range alone would have left F-04 unverifiable |
| Prior findings | F-01…F-04 each re-verified **against the tree and the approved specs**, never against §0's claim (DC-02) |
| New-defect scan | Restricted to sections carrying diff hunks: §0, §1.3, §3.1, §4.2, §4.3, §4.4, §5.2, §5.3, §6.1, §6.5, §7.1, §7.2, §8.2, §8.4, §8.5 |
| Suite | `cd pdlc/workflows && npm test` — **1038 passed / 1 failed / 70 skipped, 36 suites, 389.063 s** on this machine. The single red is the pre-existing intentional `coveredViolations (§10, §10.1) › AT-22 [red-until-L-06]` at `documentOracles.test.js:245`. Baseline reproduces exactly; wall clock is machine-dependent and not compared |
| Measurements re-derived | `driftGenerators.js:423` `const BYTES_FLOOR = 64;` and the `"bytes"` arm at `:456–457`; `__tests__/helpers/` = 12 `.js` modules + `bin/`; `__tests__/fixtures/` = 2 entries; `reviewLoop`'s injected parameters (`_agent`, `_parallel`, `_checkFile` — no `recordPhase`); TSPEC §3.7's `parseApprovalHash` signature, §4.1's `HASH_FAILURES`, §4.3's `duplicated` definition, §5.3's pre-count, §6.2 rows 6–7; PLAN §7.3's ledger and §9.2 item 3(c); `docs/_queue/QUEUE.md` row Order 9; `docs/_constraints/DOMAIN-CONSTRAINTS.md` DC-08 |
| Reconciliation | `CROSS-REVIEW-software-engineer-PROPERTIES-v2.md` read in full; SE F-20 ≡ PM F-04 is a single defect, fixed once |

**On document size — re-measured, not accepted.** The brief states ~14,500 B of the +22,966 B growth is the §0 v1.2 changelog. Measured: §0 in total is **24,122 B** (13.3 % of the document) and the **v1.2 block alone is 9,581 B** — 42 % of the growth, not 63 %. The remaining ~13,385 B is body text in §1.3, §4.2, §4.3, §4.4 and §8.

**Is §0 still inert? Yes — verified row by row.** Every one of the ten disposition rows and three question rows in the v1.2 block was checked against the section it points at: SE F-12 → §4.4's decision table and withdrawal paragraph; F-13 → §4.3 and §6.5; F-14 → §4.3 conjunct (i) and its non-vacuity floor; F-15 → §4.4's cardinality paragraph and §8.5 item 5; F-16 → §4.3, §8.4 residual 5, §8.5 item 4; F-17 → §4.3's Generator; F-18 → §6.1; F-19 → §5.2's row; F-20/PM F-04 → §1.3's five-cell table and §7.1's cell; PM F-01 → §3.1, §8.2, §2.3; PM F-02 → §4.2's invariant and non-vacuity; PM F-03 → §4.2's owner table and §8.4 residual 6; Q-01 → §4.4 and §8.5 item 5; Q-02 → §4.3 conjunct (i). Q-03's row records a *non*-change and therefore owns no substance. **No property's only precise statement, and no live disposition, lives only in a changelog row.** No finding is filed on size.

---

## Verification of my round-2 findings

| Prior | Sev | Disposition | Evidence I derived (not the author's claim) |
|---|---|---|---|
| **F-01** | Medium | **Resolved** | Both deletions happened. §3.1's `Used by` cell now reads *"`PROP-DIGEST-01/-02`, `PROP-HASH-01`, `PROP-STALE-01`. **§2.3's table is the sole owner of each one's disposition** and is not restated here"* — the "no-op on every case they generate … their strings are shorter" clause is gone. §8.2's ladder row likewise drops the blanket claim. §2.3 is intact (no hunk) and its table is **accurate**: `PROP-DIGEST-01`'s generator (§4.1) draws `rng.bytes(n).toString("utf8")` for **`n ∈ 0…512`**, so 65 of 513 draws — **≈12.7 %, "roughly an eighth"** — sit at or below `BYTES_FLOOR = 64` (`driftGenerators.js:423`, re-measured) and get `[]`; the other ≈7/8 get the single `slice(0, 64)` rung. The claim is if anything conservative, since the U+FFFD replacement this decode performs *expands* the UTF-8 byte length above `n`. The `PROP-HASH-01` / `PROP-STALE-01` row's "no-op" is separately sound: their trailer/anchor sits at the document end and a 64-byte prefix removes the thing under test. **The deletion left an accurate, sufficient statement behind.** One residue on *pointer* ownership is filed as F-03 below |
| **F-02** | Medium | **Resolved** | §4.2's `PROP-HASH-01` no longer asserts stability over two trailers; the conjunct is explicitly withdrawn and the empty `§6.4 owns which` delegation is gone. Restated: *"a document carrying **two** `APPROVAL-HASH:` lines outside fenced regions returns `{ ok: false, reason: "duplicated" }` — never a hash, and never one of the two arbitrarily chosen"*, plus a membership conjunct over `HASH_FAILURES`. Checked against the approved TSPEC, both directions: §4.1 `HASH_FAILURES = ["absent","duplicated","unparseable"]`; §3.7 `parseApprovalHash(fileText) → { ok:true, hash, reviewedCommit } \| { ok:false, reason: HashFailure }`; §6.2 row 6 routes absent/duplicated/unparseable to `UNEVALUABLE`. A conforming parser returns `{ok:false, reason:"duplicated"}` and **passes**; a parser that returns one of the two hashes fails both the duplicated conjunct and the membership conjunct. **The ≥5 double-trailer floor survives on the rejection shape** and the document says why — it is the only floor that forces the *named* `reason` rather than bare `ok:false`, which the hex-shape totality conjunct cannot reach because totality quantifies over returned hashes and a duplicated document returns none. PM Q-02 is answered in the owning section. One boundary the restatement does not pin is filed new as F-01 below |
| **F-03** | Medium | **Resolved in part — the empty routing is genuinely gone; the residual's successor is mis-scoped** | The routing sentence is withdrawn in §4.2 with the reason stated (`PROP-APPROVE-01`'s three conjuncts are tier discipline, window respect and idempotence; its generator produces no such anchors). The replacement is a **real assignment, not a relabel**: `unparseable` → `PROP-STALE-01` conjunct (i), ≥5 cases in each of four malformed shapes (verified in §4.2, unchanged from v1.1); `duplicated` → `PROP-HASH-01`, named-`reason` conjunct + ≥5 double-trailer floor + a new **two valid trailers** generator draw; `absent` → `PROP-HASH-01`, same conjunct + ≥5 no-trailer floor + a new **no trailer at all** draw. Conjuncts, generator axes and non-vacuity floors are all present for both new classes — this is the fix the finding asked for. `unreadable` is filed as §8.4 residual 6 and the gap is stated honestly (*"has no owner … nobody"*). PM Q-01 is answered in the owning section. **The successor named is a real, tracked queue row** — `QUEUE.md` Order 9 `pdlc-authoring-contract`, status `blocked`, `Depends-On: pdlc-review-loop-hardening`, REQ path recorded, and §8.3 already binds two deferrals to it. **But row 9's stated scope does not cover this item** — filed new as F-02 below |
| **F-04** | Low | **Resolved** | Fixed in `d6b524d`, outside the brief's diff range. §1.3's closing sentence — *"a property that would need a genuinely new row is a defect in the property, and none here does"* — is quoted and **withdrawn**, and replaced by a five-cell table: `PROP-TRAILER-01` \| `__tests__/pacingWrapper.test.js` \| RLH-21 (batch 3) \| green **batch 3** \| permitted red **none**. §7.1's cell now reads *"**own row — a genuinely new §7.3 entry, five cells stated in §1.3**"* and cross-references it. **§1.3 and §7.1 now agree**, and the SE F-20 filing of the same defect is discharged by the same edit. I re-derived the window independently from PLAN §4: RLH-21 (writer) and RLH-05(f) (greener, the five record parsers) both land in batch 3, so the intervening-batch set is empty ⇒ green batch 3, permitted red none. PLAN §7.3's `RLH-AT-35 … -58, RLH-AT-43a, RLH-AT-61-loop` row carries green 7 / red 3–6, so no existing row carries this window: the entry is genuinely new |

**Four of four resolved.** F-03's resolution carries one live residue (F-02 below); F-01, F-02 and F-04 are closed outright and none is carried forward.

### Does the approved PLAN stay closed?

The brief's standing rule is that *"a property needing a new §7.3 row is a defect in the property."* I re-derived and I do **not** file it as a defect, for a reason I want on the record so round 4 does not re-open it:

- §7.3's ledger is **per assertion**, and its window is *derived*, not chosen: green from = the batch of the task that greens the assertion; permitted red = the batches between writing and greening. Applying that rule to `PROP-TRAILER-01` yields batch 3 / none. **No new rule, no new surface, no new content** — only a row the PLAN could not carry because the property did not exist when PLAN v1.4 converged.
- The alternative — riding the `RLH-AT-35 … -58` pacing row on the strength of shared file — would license **four batches of permitted red** for an assertion that can be green from batch 3. That is a real gate loss bought by a co-location argument, and §4.2 states it.

So: `PROP-TRAILER-01` needs one new ledger row, adopting it is a mechanical edit owned by RLH-21, and the PLAN's *content* stays closed. That is what §1.3 now says, and it is the honest reading. **Upward-facing note (R-5, not a finding):** PLAN §7.3 will need that row added at implementation; whoever owns the PLAN should expect it.

---

## Re-judgement: is the seven-property floor still met in substance?

**Yes — seven of seven, unchanged.** Round 2's judgement stands and I checked specifically that no v1.2 edit regressed it. Measured: **§4.1 carries no diff hunk in v1.1 → v1.2** (hunks land at §0, §1.3, §3.1, §4.2 onward), so `PROP-DIGEST-01/-02`, `PROP-SCAN-01`, `PROP-NAME-01`, `PROP-ROUND-01`, `PROP-FORCE-01` and `PROP-COMPLETE-01` are byte-identical to the versions I approved in substance last round. The only v1.2 edit touching a floor property is §5.2's `PROP-DIGEST-02` (3rd) ledger row, which replaced a restated "≥10" with a **citation** of §4.1's floor — and §4.1's floor reads *"≥15 cases must contain a code point above U+FFFF and **≥5** must contain a lone surrogate"* (line 620, re-derived), so the citation is now the only statement of the number and it is correct. That is a strengthening, not a regression.

**On the reporting-upward of the ruling-2/ruling-3 precedence (§8.5 item 5) — I judge it the honest disposition, and it drops no coverage the specs require.** I checked what the approved specs actually oblige. TSPEC §8.5's rulings are *exemptions* from the await requirement, and its table is unordered; PLAN §9.2 item 3(c) instructs the walk to decide *"whichever, if any, applies"*. The obligation the specs place on a property is therefore that **every non-`await`ed seam call site in `S` is exempt under some ruling, and none is unclassified** — not that a particular label be produced for a site two rulings exempt. §4.4's replacement assertion — `classify(fragment)` returns **exactly one** outcome and it is a member of `{returned-promise, awaited-combinator-argument}`, never `unclassified`, never both — discharges that obligation in full for the both-rulings shape. What is given up is a label no artifact defines. Inventing one downstream would have redded a **conforming** classifier on §7.3's row 1, whose permitted red is *none, ever* — the exact failure mode this feature exists to remove. §5.3 gains a matching falsifier (return both exemptions ⇒ cardinality dies), so the weakened assertion is not an unfalsifiable one. This is a spec gap correctly named and correctly left with the spec's owner.

---

## New findings (changed sections only)

| ID | Severity | Scope | Finding | Ref |
|----|----------|-------|---------|-----|
| F-01 | **Medium** | Local | **`PROP-HASH-01`'s `ok:true` `iff` and its new `duplicated` conjunct disagree on a document the generator's own draw list can build.** The `iff` reads *"returns `{ ok: true, hash, … }` **iff** a **single** well-formed approval trailer appears at a position the format permits"*; the next conjunct reads *"a document carrying **two** `APPROVAL-HASH:` lines outside fenced regions returns `{ ok: false, reason: "duplicated" }`"*. Measured against the approved TSPEC, the contract's duplicate test is a **count over `APPROVAL-HASH:` lines, not over well-formed trailers**: §4.3 defines `duplicated` as *"more than one such line outside fenced regions"*, and §5.3's pre-count *"collect the `APPROVAL-HASH:` lines outside fences"* treats `≥ 2` as ambiguous regardless of payload. So on a document carrying one **valid** trailer and one **63-hex** trailer — both drawn from §4.2's own list (*"valid (64 lowercase hex) … 63 and 65 characters"*) — a conforming parser returns `duplicated`, while the `iff` demands `ok:true` because a single *well-formed* trailer does appear. Whether that document is generated is left open: the Generator says *"D3 prose interleaved with trailer **candidates**"*, plural, and never states that a document carries exactly one candidate. Both readings are available to an implementer, and §1.1's own bar for an invariant is *"stated precisely enough that two engineers write the same assertion"* — here they would not, and one of the two assertions reds correct code at the batch-3 gate (`PROP-HASH-01` rides the digest row: green batch 3, permitted red batch 2). This is not a re-file of F-02: F-02's fix is right, and this is the boundary the fix newly exposed | PROPERTIES §4.2, §1.1; TSPEC §4.3, §5.3, §6.2 row 6 |
| F-02 | **Medium** | Cross-Feature | **§8.4 residual 6 binds the unreadable-document gap to a queue row whose stated scope excludes it — DC-08's own named inverse failure.** Residual 6 defers TSPEC §6.2 row 7's *"reviewed document unreadable at comparison time"* to *"**queue row Order 9, `pdlc-authoring-contract`** — the same row §8.3 uses, **whose scope is the read/write contract these seams sit on**". The row is real (`QUEUE.md`, Order 9, `blocked`, `Depends-On: pdlc-review-loop-hardening`), so the *naming* half of DC-08 is satisfied. But `QUEUE.md`'s row-9 note states that row's scope in its own words, and it is not that: *"All three are the **same** underlying gap: the six author/review SKILLs are the authoring interface, and they declare nothing machine-readable about what they produce"*, and *"**Scope this row's REQ to declaring those contracts in the SKILLs, not to re-implementing row 0's mechanism**"*. Its three bound items are heading-template drift (Q-09), `MAX_AUTHORING_WRITE_BYTES` (T-Q-03) and a heading-name question (Q-05). An `_readFile → null` IO-failure path at the reader seam is **mechanism**, which that sentence explicitly excludes — so the item as bound will not be picked up by the REQ that row instructs its author to write. §8.3's two existing uses of row 9 (`MAX_AUTHORING_WRITE_BYTES`, SKILL-template drift) *do* fit its charter, which makes the third the outlier rather than the pattern. `DOMAIN-CONSTRAINTS.md` DC-08 names precisely this failure in its Origin: *"inverse case in §4c, **row existed but description false**"*. The residual is honest about the gap and dishonest only about who will close it — which is the same shape as v1.1's empty routing, moved one level out | PROPERTIES §8.4 residual 6, §8.3; `docs/_queue/QUEUE.md` row 9; DC-08 |
| F-03 | Low | Local | **§3.1's new sentence and §2.3 name different sole owners of the same rule.** §3.1's `Used by` cell now reads *"**§2.3's table is the sole owner of each one's disposition**"*; §2.3 itself reads *"**Each property's `Shrink.` line is the sole owner of its disposition**"*. I checked the content and it does **not** diverge — §2.3's table, §4.1's two `Shrink.` lines (*"Shipped `"bytes"` kind, floor 64 bytes (§2.3)"* / *"Shipped `"bytes"` kind"*) and §4.2's `PROP-HASH-01` `Shrink.` line (*"guaranteed no-op here"*) all agree — so this is a pointer collision, not a third conflicting statement, and it may ship. But "sole owner" asserted twice for one rule is the seed of exactly what F-09 and F-01 were filed for, in a document that has now paid for that shape twice. Per R-5 the fix is deletion: pick one owner. §2.3's table is the better one (it states all four dispositions in one place); §2.3's "Shrink. line is the sole owner" sentence is the one to withdraw, or to soften to "restates its row" | PROPERTIES §2.3, §3.1 |

### Candidates examined and *not* filed — do not re-file these

- **§0's size (24,122 B total, 9,581 B for v1.2).** Verified inert row by row against every owning section, as above. A changelog is not a shadow specification here. Third round with the same result; this should stop being re-derived.
- **§8.2's "not restated here" followed by a restatement.** The ladder row says the disposition *"is not restated here"* and then states that the DIGEST pair's domain is `n ∈ 0…512` and most of its corpus gets the rung. Literally self-undercutting, but the restatement is **accurate** (re-derived above) and its function is to name the withdrawn v1.1 claim, which is this document's stated convention for every fix. Not a finding.
- **`unreadable`'s exclusion from `PROP-HASH-01`.** Correct. It is an `_readFile` seam failure (TSPEC §6.2 row 7), not a parser input class; `parseApprovalHash` never sees it. The three-way owner table's division is sound. Only the successor binding is wrong (F-02).
- **The `absent` conjunct vs. a quoted-only document.** §4.2 states *"no `APPROVAL-HASH:` line outside a fenced region ⇒ `absent`"* as a one-directional implication, not an `iff`, so a parser that returns `absent` for a `> `-quoted-only document does not violate it, and the membership conjunct still binds. Deliberately not the same shape as F-01, which is bidirectional.
- **§4.4's `unclassified` being both "the property fails loudly" and an expected outcome for ≥10 fragments.** Unchanged from v1.1 and already reconciled there: the loud failure is about real sites in `S`, the expectation is about synthetic D8 fragments. §4.4's "total cover, not total partition" paragraph owns the distinction.
- **`PROP-EPISODE-01`(i)'s exhaustion precondition (SE F-14's fix).** Checked from the product side: the fallback for saturated-but-early-converging segments is the inequality, stated in both the conjunct and the floor, so no coverage is dropped by the narrowing. Technical lens; se-review owns it.
- **TSPEC §8.1 / §8.2 count inconsistency.** Already reported upward in §8.1, correctly, with the substance-vs-count distinction stated rather than left to arithmetic. Upward-facing (R-5), not a blocking finding.
- **Wall-clock and `file:line` drift** (`reviewLoop` at `:532` vs the cited `:531–543`, the postmortem dispatch, the 389 s vs 179.795 s figure). Corrected silently, not filed (R-6). §2.5 already states the wall clock as machine-dependent.

---

## Questions

| ID | Question |
|----|---------|
| Q-01 | For F-01: does each generated `PROP-HASH-01` document carry **exactly one** trailer candidate (with "two valid trailers" as its own shape), or may candidates be mixed? If the former, the `iff` needs one clause; if the latter, the `iff` needs restating over the line count. The choice is the author's — but it must be written down, because the generator paragraph currently supports both. |
| Q-02 | For F-02: is there a queue row whose charter actually covers reader-seam IO failures, or does closing residual 6 require row 9's REQ scope to be widened? If the latter, say so as an *action* — "row 9's REQ must be scoped to include the reader seam" — rather than characterising the row as already covering it. |

---

## Positive Observations

- **The F-03 fix is the model answer to a division-with-an-empty-side finding.** It did not reconcile, it did not hand-wave: two of the three classes were given a real home with conjuncts, generator draws and non-vacuity floors, and the third was admitted as owned by *nobody*. Writing "nobody" in a ledger is the sentence a weaker document omits, and it is why the only remaining defect there is the successor's charter rather than the coverage claim.
- **The precedence gap was reported upward instead of being papered over.** §8.5 item 5 names the missing ruling-2/ruling-3 ordering, says where it would belong (TSPEC §8.5's table), states that the strengthening from cardinality to a label follows automatically once it exists, and declines to invent it. A downstream document choosing a weaker assertion over a false-red on a no-permitted-red row is the correct trade, and the matching §5.3 falsifier keeps it from being a free pass.
- **Both of my deletion-based fixes left accurate statements behind.** This is what F-01 caught last round and it did not recur: §2.3's `n ∈ 0…512` arithmetic re-derives to ≈12.7 %, and §1.3's replacement table states five cells that reproduce independently from PLAN §4's batch assignments.
- **Every measurement in this revision measured true against the tree**: `BYTES_FLOOR = 64` at `:423`, twelve helper modules plus `bin/`, two fixture entries, `reviewLoop`'s three injected seams with no `recordPhase`, `HASH_FAILURES`' three members, §6.2 rows 6–7, and the 1038/1/70/36 baseline. The two findings above are about *scope and boundary claims*, not about mis-measured facts — the same improving failure mode se-review noted in round 2.
- **§1.3 answered the ledger question by stating the row rather than by softening the sentence.** Naming the five cells makes RLH-21's mechanical edit executable by someone who never reads this review.

---

## What must change for approval

1. **F-01** — pin `PROP-HASH-01`'s `ok:true` boundary to what the contract counts. Either state the `iff` as *"exactly one `APPROVAL-HASH:` line outside a fenced region, and that line is well-formed"* (citing TSPEC §4.3 / §5.3, not paraphrasing), or state in the Generator that each document carries exactly one trailer candidate. One clause, either way.
2. **F-02** — replace §8.4 residual 6's *"whose scope is the read/write contract these seams sit on"* with row 9's scope as `QUEUE.md` states it, and say plainly what closing the residual requires — a widened row-9 REQ scope, or a different successor. DC-08 wants a row that will pick the item up, not merely a row that exists.
3. **F-03** (Low, may ship) — one of §2.3 and §3.1 must stop claiming sole ownership of the shrink disposition.

Neither Medium touches the approved REQ, FSPEC, TSPEC or PLAN. Both are single-clause repairs in the owning section, in line with R-5.

---

## Recommendation

**Needs revision**

The seven-property floor is still met in substance, seven of seven — §4.1 is untouched by this revision and the one floor-adjacent edit (§5.2's citation of §4.1's ≥5 lone-surrogate floor) strengthens it. All four of my round-2 findings are resolved; §0 is inert for the third round running; the deletion-based fixes did not over-shoot this time; and the reporting-upward of the ruling precedence is the honest disposition rather than a dropped obligation. What remains is two boundary claims — one about which inputs the `iff` covers, one about which queue row will pick a deferral up — and a duplicated ownership pointer. None requires restructuring, none reopens an upstream document, and each is a single clause.

VERDICT: Needs revision
{"high": 0, "medium": 2, "low": 1}
