# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** docs/pdlc-review-loop-hardening/REQ-pdlc-review-loop-hardening.md (v1.1)
**Date:** 2026-07-29
**Iteration:** 2
**Scope:** technical feasibility, implementability, integration risk (REQ altitude)
**Mode:** delta re-review. Baseline reviewed at v1 = `9220a20`; diff taken against `66f6174` (v1.1). Only changed sections re-read.

## Disposition of v1 findings

| v1 ID | Sev | Status | Evidence in v1.1 |
|---|---|---|---|
| F-01 | High | **Resolved** | AC-5.5 now targets `pdlc/workflows/dist/` + `build-runtime.mjs --check`, with the consumer copy asserted separately by `sync-workflows.sh --check` and explicitly named untracked-by-decision (§4a A-6 cites DEC-DIST-02 correctly). |
| F-02 | High | **Resolved** | C-1 extended to "not observable"; §4a A-2/A-3 record the measurement and the *unknown* re-entrancy; AC-3.3 keys "retry" off on-disk structural completeness; AC-3.5 counts a script-owned dispatch-and-verify attempt with a declared `MAX_AUTHORING_ATTEMPTS`; runtime-counter observability deferred as D-RLH-04. Correctly written to hold under both readings of A-3. |
| F-03 | Medium | **Resolved** | AC-1.1a fixes the un-suffixed form as index 1 and makes un-suffixed + `-v1` co-presence an operator-visible error rather than a silent choice. |
| F-04 | Medium | **Resolved as stated**, but its worked example is falsified — see F-01 (new) | AC-2.3a keys refusal on (phase, feature); AC-2.3b makes the AC-4 skip evaluated first; R-6 records the trade-off. |
| F-05 | Medium | **Resolved** | Citation baseline row added; every citation I re-derived is now correct at HEAD. Spot-verified: `:509` dead template, `:511` `recordPhase(… 5)`, `:513` `haltError`, `:562` `if (iteration > 5)`, `:567` prompt literal, `:598` return, `:644` `isPass(verdict1) && isPass(verdict2)`, `:675` slug comment block, `:19/:22/:25/:28` flags + `DOD_MAX_ITERATIONS`, `:322` `parseVerdict`, `:624/:633` call sites, `:572/:580/:583/:586-588` `postmortemFailed`, `:819` harvest read; `orchestrate-queue.js:324/:358/:733/:739/:749-750/:771-773`; `runtime-adapter.js:13-16` (eleven globals, exact); `document-oracles.mjs:77/:81`; `se-review/SKILL.md:143/:199`; `orchestrate-queue/SKILL.md:111`; `pm-author/SKILL.md:29`. The ~60-line `orchestrate-queue.js` miss is corrected. A-4's claim that `CROSS-REVIEW-test-engineer-REQ-v1.md` lacks the trailer is also true. |
| F-06 | Medium | **Resolved** | §4a A-5 + O-1 now require the FSPEC to dispose of `document-oracles.mjs` explicitly and pin one shared "cannot judge" contract (DC-04/DC-11). |
| F-07 | Medium | **Resolved** | §4a A-4 establishes the verdict is a *response* contract only; AC-4.2 takes scope of persisting it; AC-4.2a fails closed; O-17 owns the grammar; R-7 records the widened scope; `Targets` extended to the three review SKILLs. |
| F-08 | Low | **Resolved** | AC-1.6a pins the constant to one meaning (budget); AC-1.6b fixes the reporting surfaces; AC-1.6c reconciles with H-2. |
| F-09 | Low | **Resolved** | Header `Queue row` now reads Order 0 / `in-progress`, and the uncommitted-edit observation is folded into §H-2 as corroboration. |

All nine v1 findings are closed. All four v1 questions are answered in-document (A-3 for Q-01, AC-2.6a for Q-02, AC-3.2a for Q-03, AC-4.7 for Q-04).

## Findings (new — changed sections only)

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **Phase H deletes the artifacts AC-4.2 reads, so AC-4's skip cannot fire for the feature that motivates it — and AC-2.3b's worked example inverts.** AC-4.2 (v1.1) re-bases approval on "a persisted, machine-readable verdict field carried in the cross-review artifact itself"; AC-4.2a fails closed when that field cannot be read. But `harvest-learnings` **deletes every `CROSS-REVIEW-*` file** after LEARNINGS is committed (`pdlc/skills/harvest-learnings/SKILL.md:27, :44, :100`; the `guard-harvest-before-delete` hook exists precisely to sequence that deletion). Verified at HEAD: `docs/pdlc-workflow-distribution/` holds `LEARNINGS-…`, a REQ at v17.1, `POSTMORTEM-R-…` — and **zero `CROSS-REVIEW-*` files**. So for the exact case AC-2.3b names as "the next real operational use": the verdict field is unreadable ⇒ AC-4.2a fails closed ⇒ Phase R **is not skipped** ⇒ it would otherwise run ⇒ AC-2.3 fires on the unresolved `POSTMORTEM-R` ⇒ **the pipeline halts**. The REQ asserts the opposite outcome ("Phase R is skipped, the run continues to Phase F") and builds R-6's risk acceptance on it. More generally, AC-4 as specified is inert for every harvested feature — i.e. every feature that has actually shipped, which is the whole population that re-entry targets — while H-4's motivating example is one of them. The REQ does not mention harvest anywhere in AC-4's vicinity. Requirements-altitude fix needed: state what durable artifact carries approval across harvest (LEARNINGS' `Harvested from` table records filenames, not verdicts), or make AC-4.2's evidence source survive Phase H, or scope AC-4 explicitly to pre-harvest features and correct AC-2.3b's worked example and R-6 accordingly. | AC-4.2, AC-4.2a, AC-2.3b, R-6, H-4 |
| F-02 | Medium | Local | **AC-3.1's new bound is stated over "document content", not over bytes the call emits — and under the Write tool those differ by the whole file.** `MAX_AUTHORING_WRITE_BYTES` is defined as the maximum a "single write or edit call may **author**… of document content". A `Write` call rewrites the file in full: appending §7 to a 40 KB draft emits ~40 KB regardless of how much of it is new. The watchdog A-2 describes counts wall-clock between progress events, so what matters is bytes *emitted per call*, not bytes newly authored. Read literally, AC-3.1 is satisfiable by six 12 KB-of-new-content Write calls that each emit progressively more of a growing file — which is exactly the failure mode H-3 documents (the 84 KB terminal write) reached incrementally. Read as "emitted", AC-3.1 additionally forbids whole-file `Write` for any document over 12 KB and therefore mandates append/`Edit` discipline for every spec document (this REQ is 51,683 bytes at HEAD). Which reading holds decides whether AC-3 works at all, and it is an AC-level behavioral choice, not an FSPEC detail. State the bound over bytes emitted by the call, and say which tool call shape the author SKILLs must use past the first write. | AC-3.1, AC-3.6 |
| F-03 | Medium | Local | **AC-3.5's dispatch-and-verify wrapper is defined only for the path where the dispatch returns; the observed §H-3 failure is the other path.** AC-3.5: "after the dispatch **returns**, the script re-checks the artifact's structural completeness… If completeness did not advance… the script counts that as one failed attempt and re-dispatches." §H-3 records a dispatch that was killed and retried six times by the runtime; the REQ never says what the `agent()` call does at the end of that — returns a value, returns null/empty, or throws/rejects. A-1 and A-2 deliberately establish that nothing about the watchdog is observable, and A-3 leaves re-entrancy unknown, so this cannot be inferred; yet the entire AC-3.5 mechanism, and with it AC-3.3's resume, only runs on one of the two outcomes. If the exhausted dispatch surfaces as a rejection, the wrapper never re-checks, the run halts on an unhandled fault, and "no progress across N attempts" is never reported — i.e. the AC is unimplementable as written for its own motivating scenario. This is the failure path of the new central mechanism, so it belongs in the AC (or in §4a as a measured assumption with the halt/report behavior stated for both outcomes), not deferred to O-19. | AC-3.5, AC-3.3, §4a A-2/A-3, §H-3 |
| F-04 | Low | Local | **`MAX_AUTHORING_WRITE_BYTES`'s derivation does not reproduce with the command and HEAD it cites, and its own arithmetic is self-inconsistent.** The table says all three measurements are `wc -lc` at HEAD `9220a20`. Re-run there: `docs/pdlc-workflow-distribution/REQ-pdlc-workflow-distribution.md` is **1017 lines / 89,069 bytes**, not the cited 970 / 84,671 (plausibly its size at the moment of the failing write — but then the provenance line is wrong). Consequently "7.1× below the failing floor" is 7.42×. Separately, "the default sits at the smaller of the two surviving witnesses, **rounded down**" is false: the smaller witness is 11,933 bytes and the default is **12,000** — above it, not rounded down from it; the 0.94× ratio quoted is against the *larger* witness (12,767). The band conclusion survives either way, so this is Low — but a threshold whose stated derivation cannot be re-run is exactly what the revision rule ("raising the default requires a new surviving-write measurement") depends on. | AC-3.1 threshold table |
| F-05 | Low | Local | **The `Citation baseline` row overstates what v1.1 delivers, and O-16 holds the FSPEC to a stricter bar than the REQ meets.** The header claims "**every** citation also names the enclosing symbol and a distinctive literal", and O-16 declares "a bare `file:line` citation is a defect in that document". Several v1.1 citations are bare: `orchestrate-queue.js:364` (A-2), `orchestrate-dev.js:322`, `:594-595`, `:624`, `:633` (A-4), `:586-588` and `:598` (O-12), `:25` (AC-3.5 threshold table), `orchestrate-queue.js:358` (AC-2.6). All are *correct* — I verified each — so this costs nothing today; it costs the next reviewer if the file moves. Either soften the header claim to the sections it holds for, or finish the pass. | Header `Citation baseline`, §4a A-2/A-4, O-12, O-16 |
| F-06 | Low | Local | **AC-3.2a's rebase claim is not quite right.** "Phase DOD's rebase is unaffected because it rebases the branch, not individual commits' content" — a rebase replays each commit in turn, so N per-section commits are N replay steps and N potential conflict points, and Phase DOD Step 0 halts the pipeline on conflict (`ship-pr`). The cadence is still the right call (a single end-of-attempt commit is precisely what a stall-kill destroys), and squash-merge makes the history cost zero, but the stated cost is understated: the honest trade-off is "more replay steps against a moved base", which argues for coarse sections rather than for the claim of no effect. | AC-3.2a |

## Questions

| ID | Question |
|----|---------|
| Q-01 | For F-01: is there any intended durable carrier of approval that survives Phase H? `LEARNINGS-{feature}.md`'s `Harvested from` row lists the deleted filenames, not their verdicts — if that row is meant to be the evidence, AC-4.2 must say so and O-17 must own *its* grammar too. |
| Q-02 | AC-1.4 makes the script error when the path it is about to instruct already exists. Given AC-1.1 always derives `max + 1`, is AC-1.4 reachable outside AC-1.1a's malformed-duplicate case and a concurrent-run race? If not, say so — an AC with no reachable trigger is a fixture that will be written to pass vacuously (O-11 currently asks for the oracle without asking whether the state is reachable). |
| Q-03 | AC-4.2 amends the three review SKILLs to persist the verdict field. Does the same change apply to `dod-verify`'s `CODE_REVIEW-{feature}-v{N}.md`, which the same harvest step also deletes and which AC-4.7 places out of AC-4's scope? A yes keeps one grammar; a no should be stated so the FSPEC does not have to guess. |

## Positive Observations

- The v1.1 revision is a genuine altitude-preserving fix, not a re-litigation. Every one of the nine v1 findings is answered at requirements altitude, and the two Highs are answered by *changing the requirement* (AC-5.5's tier, AC-3.3/3.5's observable) rather than by asserting the problem away.
- **§4a Assumptions is the strongest addition.** Recording A-3 as *unknown* — with "no measurement in this repo can settle it" — and then writing every AC-3 obligation to hold under both readings is the correct engineering response to an unobservable dependency. Deferring the observability itself as D-RLH-04, coupled to the same successor row as D-RLH-03, is the right shape.
- Citation quality went from "uniformly drifted" to "every reference I re-derived is exact", including the two that were materially wrong at v1. A-4's cross-check of this branch's own two cross-review files (one carries the trailer, one does not) is the kind of falsifying evidence that makes an assumption trustworthy.
- AC-2.3b's decision to evaluate the skip *before* the refusal, while still requiring the skip report to name the open POSTMORTEM, is the right precedence — the mechanism is sound even though the worked example attached to it is wrong (F-01).
- AC-4.2a and AC-1.4's "prompt text alone does **not** satisfy this AC, because that is exactly H-1's root cause one level down" are both correct fail-closed instincts.

## Recommendation

**Needs revision**

Must change before Phase F:

1. **F-01 (High)** — reconcile AC-4.2's evidence source with Phase H's deletion of `CROSS-REVIEW-*`. Verified at HEAD: `docs/pdlc-workflow-distribution/` has no cross-review files, so AC-2.3b's worked example produces a halt, not a skip, and AC-4 is inert for every shipped feature. Either name a harvest-surviving carrier of approval, or scope AC-4 to pre-harvest features and correct AC-2.3b and R-6.
2. **F-02 (Medium)** — restate AC-3.1's bound over bytes **emitted per call**, and name the tool-call shape required past the first write.
3. **F-03 (Medium)** — specify AC-3.5's behavior when the dispatch does not return normally (the §H-3 case), so the wrapper and the "no progress across N attempts" report exist on both outcomes.

F-04/F-05/F-06 are one-line corrections and would not gate a round on their own.

None of the three blocking findings is an "AC has no oracle/fixture/seam" finding: F-01 is a contest of an AC's stated outcome against verified HEAD state, F-02 is an ambiguity in a numeric bound's referent, F-03 is a missing failure path in an AC's own mechanism. §8's stopping rule does not answer any of them.

VERDICT: Needs revision
{"high": 1, "medium": 2, "low": 3}
