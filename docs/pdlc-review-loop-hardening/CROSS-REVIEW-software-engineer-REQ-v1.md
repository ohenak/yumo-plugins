# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** docs/pdlc-review-loop-hardening/REQ-pdlc-review-loop-hardening.md
**Date:** 2026-07-29
**Iteration:** 1
**Scope:** technical feasibility, implementability, integration risk (REQ altitude)

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **AC-5.5 names the wrong artifact tier and, as written, cannot be satisfied.** It requires "the runtime bundles (`.claude/workflows/*.bundle.js`) … rebuilt in the same commit as any change to their sources". `DEC-DIST-02` (`docs/_decisions/DECISIONS-plugin-distribution.md:36-46`) and CLAUDE.md pin `.claude/workflows/` as the **untracked consumer copy — never committed**; the tracked generated tier is `pdlc/workflows/dist/`, and `build-runtime.mjs --check` (which AC-5.5 also invokes) checks `dist/`, not `.claude/`. An AC that mandates committing an untracked-by-decision path contradicts a promoted decision and will either be silently reinterpreted by the implementer or land a `.gitignore` regression. Restate as: `pdlc/workflows/dist/` rebuilt and committed in the same commit; `sync-workflows.sh --check` clean for the consumer copy. | AC-5.5 |
| F-02 | High | Local | **AC-3.3 and AC-3.5 require a retry signal the REQ itself declares unobtainable.** C-1 and §H-3 establish that the 180 s kill *and the six-attempt retry* are runtime-side, that the retry "re-ran a byte-identical prompt", and that no attempt-count constant exists anywhere under `pdlc/` or `.claude/` (independently verified: grep for `180000`/`stall`/`no progress` returns nothing in workflow sources). AC-3.3's "the prompt for a retry differs from the prompt for a first attempt" and AC-3.5's "naming … the attempt count" and "no progress across N attempts" both presuppose that the workflow script (a) is re-entered on retry and (b) can read the runtime's attempt counter. Neither is established, and if the runtime replays the same `agent()` call transparently, no in-script code runs between attempts at all. Either cite the observable seam that makes attempt count visible, or restate AC-3.3/AC-3.5 in terms of state the harness *can* observe — artifact state on disk before dispatch, and a script-owned dispatch-and-verify wrapper that counts its own attempts. As written these are the two ACs most likely to be "implemented" as an unverifiable prompt sentence. | AC-3.3, AC-3.5, C-1 |
| F-03 | Medium | Local | **AC-1.1's next-index rule does not dispose of the un-suffixed first-round filename, which §8 O-2 confirms exists.** The rule is "one greater than the highest existing `CROSS-REVIEW-{role}-{doc-type}-v{N}.md`, or 1 when none exist". On a branch holding only `CROSS-REVIEW-software-engineer-REQ.md` (the v-less form the reviewer SKILL emits — `pdlc/skills/se-review/SKILL.md` Cross-Review File Format uses `[-v{N}]`, optional), no `-v{N}` file exists, so the derived index is **1**, and AC-1.4 then turns a legitimate round-2 dispatch into a pipeline error. This is observable AC behavior, not a fixture question: state whether the un-suffixed form counts as N=1. | AC-1.1, AC-1.4, O-2 |
| F-04 | Medium | Local | **AC-2.3 (refuse) and AC-4.1 (skip) have no stated precedence, and the motivating case satisfies both.** `docs/pdlc-workflow-distribution/POSTMORTEM-R-pdlc-workflow-distribution.md` exists at HEAD *and* §1 states REQ v17 reached dual approval. After this feature ships, re-entering Phase R for that feature matches AC-2.3's refusal condition and AC-4.1's skip condition simultaneously. Which wins determines whether the very next operational use of the harness halts or proceeds. Also unstated: whether an unresolved POSTMORTEM for phase R blocks entry to *later* phases (F, T, …) or only to R — AC-2.3 says "for that phase", but R-2's deadlock risk only bites if the answer is "R only". | AC-2.3, AC-4.1, R-2 |
| F-05 | Medium | Process | **Every line citation in this REQ is drifted, and two are materially wrong — yet O-16 asks reviewers to verify AC-5.1/AC-5.2 by those very line numbers.** Verified against HEAD (`2763eec`): the `orchestrate-dev.js` refs are uniformly off by one (dead template is `:509` not `:508`; the bare `5` literals are `:562`, `:567`, `:598` not `:561/:566/:597`; the halt block is `:509`+`:511-513`; the seven `reviewLoop` call sites are `:1649, :1675, :1701, :1742, :1771, :1797, :1913`, each one greater than cited). Worse, the `orchestrate-queue.js` status-write citation (`:676`, `:680-682`, `:691-693`) is wrong by ~60 lines — line 676-693 is the `runPicked(...)` argument list; the actual writes are `:733` (`in-progress`), `:739` and `:749-750` (`halted`/`awaiting-merge`) and the `rewriteStatus` helper at `:771-773`. The *substance* of H-1/H-2/AC-5.1/AC-5.2 all verified true (no `readdirSync`/glob anywhere in `pdlc/workflows/*.js`; no call site passes `iteration`; `{DOC-TYPE}` reaches prompts verbatim at `:709-710` and `:733`; `updateQueueStatus` returns input unchanged when the row is absent, `orchestrate-queue.js:358` — AC-2.6's premise confirmed; the driver performs zero git operations). But citations that drift on an unchanged file mean O-16's "verify without re-deriving the line references" is not achievable. Cite by symbol name plus a distinctive literal, and record the HEAD sha the refs were taken at. | §H-1, §H-2, AC-5.1, AC-5.2, O-16 |
| F-06 | Medium | Cross-Feature | **O-1 does not cite the sibling that already ships root-parameterised file discovery, risking two divergent listing mechanisms.** O-1 says "`orchestrate-dev.js` has no directory-listing capability today" — true — but the repo already ships `pdlc/workflows/lib/document-oracles.mjs`: pure, root-parameterised, no `process.cwd()`, walking the tree while skipping `.git/` and `node_modules/`, i.e. exactly the shape DC-04 mandates for an oracle and the shape AC-1.1/AC-4.2 need. It is an `.mjs` jest-side module and therefore *cannot* be reached from the runtime bundle (C-2), so the FSPEC will need an injected adapter seam regardless — which is precisely why the reuse-vs-duplicate question must be posed at obligation level now. Two independent implementations of "list this feature's review artifacts" with divergent error contracts would violate DC-11 (sibling oracles called from one checklist share an error contract). Extend O-1 to require the FSPEC to either reuse/extend `document-oracles.mjs` or state why the runtime seam must be separate, and to pin one shared "cannot judge" contract across both. | O-1, AC-1.1, AC-4.2, DC-04, DC-11 |
| F-07 | Medium | Local | **AC-4.2's evidence source has no owner in §8.** AC-4.2 requires approval to be established "from artifacts on the branch — the reviewers' recorded verdicts". The `VERDICT:` trailer is a *response* contract between the reviewer agent and the workflow script (`parseVerdict` in `orchestrate-dev.js`), not a field guaranteed to be present in the cross-review **file**; what the file guarantees is a free-text `## Recommendation` section. No obligation row covers extracting a verdict from a persisted artifact — O-1 covers discovery, O-2 covers *filenames*, and nothing covers file *content* grammar. So AC-4.2 and AC-4.3 (both verdict forms count) will be implemented against an unpinned string format, in the one place where a false positive silently skips a review phase (R-1, DC-07). Add an obligation row for the persisted-verdict grammar, including the ambiguous-or-unparseable case (which must fail closed → phase runs). | AC-4.2, AC-4.3, R-1 |
| F-08 | Low | Local | **AC-1.6 and AC-5.1 need to be reconciled explicitly.** The loop gate is `if (iteration > 5)` (`orchestrate-dev.js:562`) and the postmortem prompt/return hardcode the same `5` as the *count* (`:567`, `:598`). Once the index starts at 14 (AC-1.1) and the budget is counted from the invocation's starting round (AC-1.6), a single named constant (AC-5.1) has to be read as `startIndex + MAX - 1` for the gate and as `MAX` for the reported count — otherwise the resumed loop trips the cap on entry, and the postmortem's "Iterations (5 — limit reached)" line becomes false. State which of the two meanings the constant carries. | AC-1.6, AC-5.1 |
| F-09 | Low | Local | **Header metadata contradicts §2 and HEAD.** The Queue row field says "8 (`blocked` — a human sets it `pending`…)"; §Downstream and §Priority say Order 0, and `docs/_queue/QUEUE.md:22` has the row at Order 0 / `in-progress`. Also worth noting: that `in-progress` edit is sitting uncommitted in the working tree at review time — a live instance of the H-2 defect this REQ fixes, and a useful corroborating citation for AC-2.1. | Header table, §2 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Is the runtime's retry actually re-entrant into the workflow script (does control return to `orchestrate-dev.js` between attempts), or does the runtime replay the `agent()` call opaquely? The entire feasibility of AC-3.3/AC-3.5 (F-02) turns on this, and the answer is a one-run experiment, not a design choice. |
| Q-02 | For AC-2.1's direct-invocation path (O-5): should `orchestrate-dev` invoked on a REQ with **no** queue row halt, or proceed and report "no row to mark"? AC-2.6 says a status write with no matching row is an error, which would make every non-queue direct invocation of a halting phase a double failure. |
| Q-03 | AC-3.2 requires partial sections to be "on disk **and committed**". Does that mean one commit per section (which makes `git log` on a feature branch section-granular and changes what Phase-DOD rebase touches), or one commit at the end of each surviving attempt? |
| Q-04 | Does AC-4.1's skip apply to Phase CR (final codebase review) and Phase DOD, which have no `CROSS-REVIEW-{role}-{doc-type}` artifact pair in the AC-1.1 sense? |

## Positive Observations

- Every substantive claim about existing code that I checked is **true**: no directory-listing capability anywhere in `pdlc/workflows/*.js`; no `reviewLoop` call site passes `iteration` (all seven verified); `{DOC-TYPE}` genuinely reaches agent prompts unsubstituted; `updateQueueStatus` is a silent no-op on a missing row; the driver performs zero git operations; the dead `docs/{feature}/POSTMORTEM-…` template with literal braces is real; no stall/retry constant exists in this repo. The failure analysis is sound — only the line numbers drifted (F-05).
- C-2 correctly pre-empts the two constraints that would otherwise be discovered in TSPEC (runtime restrictions, and the `await`-every-injected-IO-call rule). C-5 correctly keeps index/approval/POSTMORTEM detection out of model calls.
- §8's obligation table is the right instrument at this altitude, and O-10's fixture matrix already names the non-contiguous and non-conforming-filename cases a reviewer would otherwise have to raise.
- R-4's bootstrapping response cites a real, pipeline-independent gate (`cd pdlc/workflows && npm test`).

## Recommendation

**Needs revision**

Must change before Phase F: F-01 (AC-5.5 targets an untracked-by-decision path — contradicts DEC-DIST-02), F-02 (AC-3.3/AC-3.5 depend on a runtime signal C-1 declares out of reach; restate against harness-observable state or cite the seam), F-03 (un-suffixed v1 in the AC-1.1 index rule), F-04 (AC-2.3 vs AC-4.1 precedence), F-05 (citations, incl. the ~60-line `orchestrate-queue.js` miss, which O-16 depends on), F-06 (O-1 must dispose of the existing `document-oracles.mjs` precedent per DC-04/DC-11), F-07 (add an obligation row for persisted-verdict grammar, failing closed).

All seven are contests of observable AC behavior, of a claim about existing code, or of a §8 row's completeness — none is an "AC has no oracle/fixture/seam" finding, so none is answered by §8's stopping rule.

VERDICT: Needs revision
{"high": 2, "medium": 5, "low": 2}
