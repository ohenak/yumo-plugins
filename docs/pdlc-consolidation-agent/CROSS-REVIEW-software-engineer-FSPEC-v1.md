# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-consolidation-agent/FSPEC-pdlc-consolidation-agent.md` (v1.0)
**Date:** 2026-08-06
**Iteration:** 1
**Scope:** Local unless tagged otherwise

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **`resolveAdvisoryRung` reuse is not implementable as §2.6 and step 8 specify.** The exported resolver hardcodes the dispatched skill: `const ADVISORY_RUNG_SKILL = "se-review"` (`pdlc/workflows/orchestrate-dev.js:1797`), used at `:1841` (`_agent(ADVISORY_RUNG_SKILL, prompt, { model })`), and it takes no skill parameter. A consolidation pass that "reuses" it dispatches every model call to the **se-review** skill with a consolidation prompt. Worse, the resolver has **no probe mode** — rung resolution is inseparable from a real agent dispatch (`:1844-1875`), so §2.2's step 8 ("Resolve the advisory model rung", terminating `failed`/`advisory-model-unresolved` *before* step 9 reads any body) cannot exist as specified without a throwaway dispatch the FSPEC does not name. This is load-bearing twice: the AC-1.6 `failed` arm, and the whole O-C1 / E-32 loss analysis, which rests on step 8 failing after step 7 and before any body read. Either state the throwaway-dispatch shape and its prompt/skill, or take baseline §3's sanctioned alternative (restated literals + a named drift observable) — and if the resolver must gain a skill parameter, that is a `pdlc/workflows/` edit §15.3 does not list. | §2.2 step 8, §2.6, §14 T-05, §15.3 |
| F-02 | High | Local | **The resolver's fourth outcome is unmapped.** `resolveAdvisoryRung` returns `{kind: "dispatch-error", err}` whenever the agent fails for a reason that is *not* model resolution (`orchestrate-dev.js:1857`, `:1867`). §2.6's table enumerates exactly three outcomes (primary resolves / fallback resolves / neither resolves) and asserts "There is no third rung and no default-model fall-through" — but says nothing about a dispatch that fails for any other reason. No terminal status, no reason code, and no vocabularies §1 row covers it, so by the FSPEC's own set-equality rule (§10.5, §15.2) it cannot be added downstream without a REQ change. Every agent dispatch the pass makes at steps 9, 12 and 13 has this same unhandled arm. | §2.6, §12.1, §15.2 |
| F-03 | High | Local | **NFR-4 idempotence has no carrier for the two consuming-repo routes.** §6.4 defines the suppression key as the `(failure-mode-id, action)` pair "read from the `PDLC-CONSOLIDATION-PROMOTIONS` trailer of PRs in the target repository". An AC-2.1 / AC-2.2 promotion (§12.2 P-01) never appears on a PR, so its pair is never in the key set and is **never suppressed** — re-running a pass over the same corpus appends the same constraint to `DOMAIN-CONSTRAINTS.md` again. Two sections lean on suppression that structurally cannot fire for that route: §3.4's unreadable-log row ("fail-open toward re-consumption … NFR-4 suppression is what prevents a duplicate proposal") and §4.5's both-observe-absent race row. Name the non-PR carrier (the §8.1 failure-mode records already in the log are the obvious candidate) and state its state set, or the P-01 route is not idempotent at all. | §6.4, §3.4, §4.5, §12.2 |
| F-04 | High | Local | **The delegated AC-5.3 choice is discharged by an undecidable predicate.** §8.5 row 3 selects `revision` when "the promotion's `artifact` still exists **and the recurrence names the same `symptom` the promotion targeted**". `symptom` is declared in §8.1 as "one line the pass's own model writes under no vocabulary" and explicitly **non-keying**, precisely because two passes word it differently — the FSPEC's own argument for excluding it from the id derivation. §8.5 then makes it the discriminator of a reported field, with no matching rule (exact string? normalised? semantic?). Worse, the corpus cannot supply the input: §8.4 says a LEARNINGS carries a `failure-mode-id` line, not a symptom, so "the recurrence names the same symptom" ranges over a field the recurrence evidence does not have. The §1 delegation table claims this question is discharged at §8.5; as written it is not. | §8.5, §8.1, §8.4, §1 |
| F-05 | High | Cross-Feature | **`recurred` is unobservable as specified — the falsifiability loop's only positive-recurrence verdict has no derivation on the producing side.** §8.3 decides `recurred` by "at least one LEARNINGS in the consumed set names this `failure-mode-id`", and §8.4/§15.3 make that possible by "a `failure-mode-id` line added to the LEARNINGS §5 Open Items convention" in `pdlc/skills/harvest-learnings/SKILL.md` (metadata table verified at `:70-78`). Nothing states **how a harvest agent derives that id**: the slug is `{phase}-{artifact-slug}` over a prior *promotion's* recorded phase and canonical path (§8.1), which harvest does not know. An LLM-authored LEARNINGS will not emit a byte-exact match to a prior promotion's slug by chance, so `recurred` never fires, every promotion drifts to `insufficient-evidence`, and §8.7 marks it `unmeasurable` — US-04/US-05 delivered as a machine that always answers "don't know". State the harvest-side obligation (a lookup of open promotions from the log, or a stated derivation harvest can perform) and give it an acceptance test; no AT in §13.7 covers the producing side. | §8.3, §8.4, §15.3 |
| F-06 | Medium | Local | **Two opposite rules for the same observable.** §5.3: "A pass with nothing in any of the three rows writes **no** proposal file: a `no-op` pass opens no PR and writes no proposal file (AC-1.4)." §7.3 + AT-K3 + §12.1 S-08: a pass whose only promotion degraded on an absent credential is terminal `no-op` — and §6.3 requires that degradation to write `CONSOLIDATION-PROPOSAL-{passId}.md` with the full diff. So for S-08 one rule says the file exists and another says it does not. This is testable in both directions and must be reconciled here. (The underlying REQ tension between AC-1.4's exhaustive two causes and AC-4.3's third is routed upstream as an erratum; §5.3's sentence is this document's to fix regardless.) | §5.3, §7.3, §12.1 S-08, AT-K3 |
| F-07 | Medium | Local | **The `failure-mode-id` injectivity claim is false.** §8.1: "injective enough for its purpose (two distinct `(phase, artifact)` pairs cannot collide, because the substitution is reversible up to case)". The substitution maps **both** `/` and `.` to `-`, and then collapses runs of non-`[a-z0-9-]` to a single `-` — it is many-to-one and not reversible. `pdlc/skills/a-b.md`, `pdlc/skills/a/b.md` and `pdlc/skills/a.b.md` all yield `pdlc-skills-a-b-md`. NFR-4's whole safety argument (and §5.5's abandonment recovery) rests on the id being a faithful key: a collision silently suppresses an unrelated promotion. Either use a lossless encoding, or state the collision as an accepted, bounded cost with the consequence spelt out — do not assert an injectivity the derivation does not have. | §8.1 |
| F-08 | Medium | Local | **"The consumed window" is undefined at this layer.** §9.5's two conjuncts range "across the consumed window", while §9.2 defines counting over **entries** of an append-only, non-feature-scoped file with no window at all, and §9.4's dominance test compares seam **totals** (implicitly whole-file). So the two AC-6.2/AC-6.3 tests use different, and in §9.5's case unspecified, populations — is the window the entries whose `Feature` is in this pass's consumed set, or every entry ever appended? AT-A6 does not pin it. §9.2 delegated exactly this ("how `ESCALATIONS.md` entries are counted per `Seam` per `Feature`", §1) and left the windowing half unanswered. | §9.2, §9.4, §9.5 |
| F-09 | Medium | Local | **AC-1.6's principal branch has no acceptance test, contrary to DC-05 and §15.1's own claim.** §2.6 table row 2 (primary fails, fallback resolves ⇒ the pass proceeds, names the fallback rung, and surfaces the `ADVISORY_MODEL_FALLBACK:` line verbatim — §10.4 item 2) is a named behavioural branch. §15.1 maps AC-1.6 → AT-M4, which asserts only that a `failed` pass at step 8 releases its marker and wrote its row. Nothing asserts the non-silent downgrade, which is the entire point of AC-1.6. AC-1.5's "records the rung it actually ran on" is likewise covered only by AT-L5, a vocabulary set-equality test that never observes a rung value. | §13.1, §15.1, §2.6 |
| F-10 | Medium | Process | **AT-Q7 is an absence-only oracle.** "no merge or enable-auto-merge API call exists, on any path, for any PR including its own" asserts only that something does not happen, on a path with no paired positive assertion. Pair it with a set-equality over the PR route's actual API-call set (create branch / push / create PR — exactly those three, per §6.2 and §7.1's two permissions), so that deleting a call, renaming the route, or a pass that makes no API calls at all cannot pass the test vacuously. The same shape would strengthen §6.5 control (b), which is stated as "no such call exists on any code path" and nothing else. | §13.5 AT-Q7, §6.5 |
| F-11 | Low | Local | Three citation off-by-ones, all verified against HEAD. §5.4 cites `commitQueueRow`'s commit as `orchestrate-queue.js:1579-1585`; `:1579` is blank and the call spans `:1580-1586` (the REQ says `:1580-1585`). §5.4 cites the `NOTHING_TO_COMMIT_RE` treatment at `:1628-1633`; the block is `:1631-1635` (`:1628` is the closing `]);`). §2.6 cites the resolver's halt error at `:1866+`; `:1866` is the rejection handler's parameter list and the `throw haltError(` is `:1868`. | §5.4, §2.6 |
| F-12 | Low | Local | §2.6 says the pass threads `rungState` "exactly as `orchestrate-queue` does (`pdlc/workflows/orchestrate-queue.js:1245-1256`)". Verified: the queue initialises `const rungState = { resolved: null }` at `:1120` and `:1245-1256` threads it into `runAdvisorySeamFn`, **not** into `resolveAdvisoryRung` — the only call site of the resolver in the repo is `orchestrate-dev.js:3132`, inside `runAdvisorySeam`. So there is no shipped precedent for a direct-call reuse, and the cited lines do not show one. Cite `:1120` for the state shape and say plainly that the direct call site is new. | §2.6 |


## Questions

| ID | Question |
|----|---------|
| Q-01 | The pass ships as a new workflow bundle (REQ §5, T-02). The runtime allows **no imports** (`build-runtime.mjs:41`), and the queue bundle reaches `orchestrate-dev` only because the builder inlines that whole module (`bundles`, `:448-466`; CLAUDE.md). Does the consolidation bundle likewise inline all of `orchestrate-dev.js` to reach `resolveAdvisoryRung`, and is that acceptable at T-02, or does F-01's alternative (restated literals + drift observable) become the cheaper route? §2.6 asserts reuse without naming the vehicle. |
| Q-02 | §2.2 puts the marker take at step 6 and the consumed-pair append at step 7, but the pass's own writes to `.consolidation-log.md` are appends while the *predicate* reads the same file. Two passes that both observed the marker absent (§4.5 row 2) each append a pair naming an overlapping set. §3.2's disjunction makes that harmless for membership — but §8.3's `recurred` and §8.5's streak count over "this pass's consumed set". Does the second pass's duplicate consumption double-count a verdict for the same promotion in the same window? |
| Q-03 | §5.4's write set is committed once at step 15, but §10.2 appends the effectiveness table (order 3) and the terminal row (order 4) at step 14, and the consumed pair at step 7. If the process dies between step 7 and step 14, the pair is on disk and uncommitted. §3.3 says "the next pass reads the working tree" — is that also true for a pass invoked in a *different* checkout of the same repo (the §6.1 clone is temporary, but an operator's second worktree is not)? |
| Q-04 | §9.5 routes a widening of "the **shipped defaults** in `pdlc/workflows/`" to a PR. The advisory envelope default is `ADVISORY_DEFAULTS`/`envelope` inside `orchestrate-dev.js` (a `pdlc/workflows/` path ✓), but `ADVISORY_SEAMS` is `Object.freeze` (`:1669`) and the envelope is a "four-member literal" per CLAUDE.md. Is a widening proposal an edit to that literal, and does §8.2's one-promotion-one-authored-file rule then force the rebuilt `dist/` bundles to ride it (§8.2 row 2 says yes)? Worth confirming explicitly, since AT-F3 tests that shape only for `orchestrate-dev.js`. |


## Positive Observations

- **Every `file:line` citation in the document was checked against HEAD and all but the three in F-11 are exact.** Verified: `MERGE_GUARD_DEFAULTS` `orchestrate-dev.js:48-53` (frozen four-member array), `MERGE_DEFAULTS`/`mergeMode: "off"` `:60-61`, `effectiveGuardPaths` `:709`, `guardVerdict` `:732`, `MODEL_ADVISORY` `:1652` / `MODEL_ADVISORY_FALLBACK` `:1653` (both module-private, as claimed), `ADVISORY_SEAMS` `:1669`, `parseAdvisoryConfig` `:1682`, the resolver doc comment `:1800` and export `:1833`, `ADVISORY_MODEL_FALLBACK:` `:1859`, `advisorySummaryRows` `:2708`, `renderEscalationEntry` `:2763` with the heading at `:2776` and the `| Feature |` / `| Seam |` rows at `:2782-2783`, `PHASE_DISPATCH` `:3337-3437`, `gitWithLockRetry` `:8617`, `commitPaths` `:8669` with its plain `git commit -m` at `:8690` and the doc-comment rationale at `:8660-8663`, the `recordPhase` literals `:10020` / `:10250` / `:10407` / `:10462` / `:10568`, `commitQueueRow` `orchestrate-queue.js:1576-1577`, `commitAdvisoryRecord` `:1615`, `hooks.json:3/:14/:29`, `nudge-consolidation.sh:25/:28/:32/:36-37/:38-39/:41/:47-48`, `consolidate-learnings/SKILL.md:35/:38/:40/:41/:43/:54`, `harvest-learnings/SKILL.md:70-78`, `.gitignore` (no pattern matching the lock path), `docs/_queue/` holding `QUEUE.md` alone. This is a materially higher standard of existing-code claim than the norm.
- **The 13-member phase catalogue is sourced correctly.** `PHASE_DISPATCH` carries exactly eight keys (R, F, T, D, P, PR, CR, DOD); the remaining five (I, PT, H, PUB, MERGE) come from the `recordPhase` literals §8.1 cites individually. 8 + 5 = 13, and the citation set is complete.
- **The corpus measurement is right.** Five LEARNINGS under `docs/*/` ∪ `docs/completed/*/`, two under `docs/discarded/*/` — so §3.1's insistence that the widening is "exactly one added literal path segment" rather than a depth increase is a real defect avoided, not a stylistic point, and §2.3's first-tick assertion (3 un-consolidated, below the threshold of 5, cadence fires on the empty datum) is verifiable at HEAD.
- **§5.4's precedent selection is correct and non-obvious.** `commitPaths` really does commit without a pathspec (`:8690`) and really does document that as deliberate (`:8660-8663`); `commitQueueRow` really does carry the pathspec on both calls. Choosing the latter for a pass that may run against a mid-pipeline tree with a staged index is the right call, and AT-R3 tests exactly that.
- **Receive-side totality is genuinely total, not gestured at.** §3.4, §9.3, §11.1 and §19.1 each enumerate absent / unreadable / truncated / stray-marker arms, and the two deliberately asymmetric decisions (an unterminated block counts as consumed; an unparseable marker reclaims rather than refuses) are each argued from the failure they prevent.
- **§14.2 and §19.3 record two real losses rather than routing around them.** O-C1 in particular is stated against the FSPEC's own interest.


## Recommendation

**Needs revision**

Five High and five Medium findings. What must change before approval:

1. **F-01 / F-02 — close the model-rung seam.** State what step 8 actually does given that `resolveAdvisoryRung` dispatches `se-review` (`:1797`, `:1841`) and has no probe mode: either name the throwaway dispatch (skill, prompt, and why its output is discarded), or take baseline §3's sanctioned alternative and name the drift observable — and list any `pdlc/workflows/` edit it implies in §15.3. Then map the resolver's `dispatch-error` outcome to a terminal status and reason code; if no vocabularies §1 row exists for it, that is an upstream row to add, not a branch to leave unhandled.
2. **F-03 — give the consuming-repo routes an idempotence carrier.** §6.4's PR-trailer key cannot cover P-01. Name the non-PR carrier and its observed states, and correct §3.4's and §4.5's claims that NFR-4 absorbs re-consumption on those routes.
3. **F-04 — restate §8.5 row 3 over a decidable input.** The condition may not range over `symptom`, which §8.1 declares non-keying and which the recurrence evidence does not carry.
4. **F-05 — specify the producing side of `recurred`.** Without a stated harvest-side derivation or lookup, the only positive-recurrence verdict in the feature is unreachable and the falsifiability loop degenerates to `unmeasurable`. Add the obligation and an acceptance test that ranges over a harvest-authored LEARNINGS.
5. **F-06 — reconcile the `no-op` proposal-file rule** so §5.3, §7.3, S-08 and AT-K3 agree on whether the file exists.
6. **F-07 — drop or correct the injectivity claim**; **F-08 — define the consumed window**; **F-09 — add an AT for AC-1.6's fallback branch**; **F-10 — pair AT-Q7's absence with a positive set-equality**.

F-11 and F-12 are corrections of record, not blockers.

Nothing above contests the REQ's scope, phasing or user need. Two upstream items are routed as errata rather than folded into this verdict: the REQ's two incompatible definitions of `no-op`, and `pdlc-advisory-corpus-baseline.md` §3's "reuse the resolver" instruction, which the resolver's hardcoded skill parameter does not support.

## Verdict

Five High findings (F-01…F-05), five Medium (F-06…F-10) and two Low (F-11, F-12) are open. Per the approval rule — any High or Medium finding means the document is not approved — this iteration does not approve `FSPEC-pdlc-consolidation-agent.md`.

VERDICT: Needs revision

