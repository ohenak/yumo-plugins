# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-consolidation-agent/FSPEC-pdlc-consolidation-agent.md` (v5.0)
**Date:** 2026-08-06
**Iteration:** 5
**Scope:** Local unless tagged otherwise
**Protocol:** delta re-review. Baseline `d0ee225` (the last FSPEC commit `CROSS-REVIEW-software-engineer-FSPEC-v4.md` reviewed); diff `d0ee225..HEAD` — 122 insertions, 33 deletions across 6 commits. Only the changed sections were re-read for new issues.

## Prior findings — disposition

Every v4 finding was re-checked against the revision and, where it made a claim about HEAD, against
the code. **All four are closed as filed.** As in each of the last three rounds, the repairs create
new checkable defects in the sections they rewrote; those are filed below on their own merits, not as
reopenings.

| v4 | Verdict | Evidence |
|---|---|---|
| F-01 (Medium) — §6.5's invoking-tree permitted set `{add, commit}` was red on a conforming pass that reads its branch name through the git seam | **Resolved, and on both git domains rather than the one I named.** §6.5's table grows a fifth column (the Given each domain's obliged conjunct is asserted on), both git rows admit `read-branch` / `read-status` as permitted-but-not-obliged, and a new paragraph states *why* they are permitted and why they are not obliged (AC-3.8b obliges the observation, not a seam — the same shape `fetch` has in the clone row). §5.4 carries the matching clause at `:682-686`, BR-28 and AT-Q7 move with it. I re-verified the precedent: `parseAbbrevRef` is `orchestrate-dev.js:3491-3496` exactly, `readHeadBranch` issues `_git(["rev-parse","--abbrev-ref","HEAD"])` at `:3524`, `gitWithLockRetry` is `:8617` and `commitPaths` `:8669` — all as cited but one (F-04 below) |
| F-02 (Medium) — the `suppressed-by:` grammar diverged from the REQ-owned vocabularies row with no erratum | **Resolved as filed.** ER-5 is routed in §14.4 with the exact argument, and the divergence is stated at all four sites that carry the grammar: §10.3's class table (`:1607` — "one exception is named rather than absorbed"), §10.3's field table (`:1625`), BR-26 and §15.2's lexicon row. I verified the upstream row at HEAD: `docs/_constraints/pdlc-consolidation-vocabularies.md:63` spells `` `{id}:{action} → PR URL` entries, or empty ``, and the change-control clause at `:25-27` is as quoted. ER-5's shipping assumption reuses ER-2's rather than inventing a third |
| F-03 (Medium) — §8.2's "one `target`" merge observable was undetermined across §5.2 kinds | **Resolved for the mixed-kind case, with the rule stated as a total order** (§8.2 `:1191-1224`), three checkable consequences, O-C8 recording the cost, §10.4 item 4 naming the elided kind, BR-33b, and AT-R6b gaining a third fixture. The kind axis is closed. The *subject* axis it depends on is not — filed as F-01 — and the new fixture's Given contradicts itself — filed as F-02 |
| F-04 (Low) — two off-by-one line citations | **Resolved, and verified.** `build-runtime.mjs:448` opens `const bundles = [`, its closing `];` is `:471`, and the `pdlc-cli.mjs` entry runs `:464-470` (opening brace `:464`, `file:` key `:465`). §14.1 T-02 and §15.3 now say exactly that |

## Findings

All findings below are in sections the revision changed. Nothing unchanged since v4 is re-litigated.

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | Medium | Local | **The new precedence rule ranks *kinds*, but for the lowest-precedence kind `target` is a function of the *subject* — so a same-kind process-learning merge over colliding subjects leaves `target` exactly as undetermined as the mixed-kind case just closed.** §8.2's rule (`:1198-1204`) is a total order over the three §5.2 kinds and consequence 3 says so ("on a same-kind merge it returns the kind both proposals already had"). That is sufficient for kinds 1 and 2, whose `target` is a function of the *id* or a constant (`docs/_constraints/DOMAIN-CONSTRAINTS.md`; `docs/_decisions/DECISIONS-{failure-mode-id}.md`, §8.1 `:1085-1087`). It is **not** sufficient for kind 3, whose `target` is "the subject `artifact` itself" — and the merge's whole premise is that the two proposals have **different** subject paths that slug to one id (§8.1's collision table `:1136-1139`; AT-R6b's second fixture, `pdlc/skills/a-b.md` and `pdlc/skills/a/b.md`). Two process learnings over that pair are one promotion by §8.2's uniqueness rule, both are kind 3, precedence returns kind 3, and the merged record has **two** candidate `target` values — the same undetermined "one `target`" observable F-03 was raised about, one axis over. The record's `artifact` field is undetermined on the same fixture and for the same reason (§8.1 obliges "exactly one canonical repository path"), which matters downstream: §8.5's remediation choice is a file-existence test **on the subject** (BR-35a), so the tie-break decides whether AT-F17 or AT-F18's branch is taken two passes later. Note this is the *common* kind — §5.2's own worked collapse says three of fifteen skill directories share one decision file, and process learnings about skill prompts are what the corpus mostly yields. Repair belongs in §8.2 beside the precedence table and is cheap: state a total tie-break over subjects (lexicographic order over the canonical path is the only one that is a pure function of the inputs — "first proposed" is not, since proposal order is a model artifact), say that `artifact` and `target` both follow it, and give AT-R6b's second fixture the assertion — it currently asserts "one `target`" without saying **which**, so an implementation that picks either passes. | §8.2 `:1191-1224`, §8.1 `:1073-1074`/`:1085`, AT-R6b, BR-33b, BR-35a |
| F-02 | Medium | Local | **AT-R6b's new third fixture contradicts itself in its Given, and the row's lead sentence no longer describes it — the fixture cannot be built as written.** The cell opens "two **AC-2.2** promotions in one pass sharing `phase = P`", then adds "in a **third** fixture **the same colliding subjects** proposed under **different §5.2 kinds** — one a process learning about `pdlc/workflows/orchestrate-dev.js` … and one an AC-2.1 domain invariant about **it**". Three things cannot all hold: (i) the lead says every promotion in the row is AC-2.2, while fixture 3 contains no AC-2.2 promotion at all (a process learning and an AC-2.1 invariant); (ii) "the same colliding subjects" names the second fixture's **pair** of distinct paths (`pdlc/skills/a-b.md`, `pdlc/skills/a/b.md`), while "about `orchestrate-dev.js` … and … about **it**" names **one** shared path — those are different merge triggers (slug collision vs. key identity), and under the collision reading the fixture also inherits F-01's undetermined subject; (iii) under the one-shared-path reading, `pdlc/skills/*` never enters fixture 3, so "the same … subjects" is simply false. The assertions the fixture exists for (single `target` = `DOMAIN-CONSTRAINTS.md`, `route: constraints`, no guard-set write, no PR, one `symptom` naming both modes, report body naming the elided kind) are correct and well-chosen under **either** reading — which is why this is a Given defect rather than an oracle defect, and why it will survive into an implementation as two different tests. Pick the one-shared-path reading (it is the minimal fixture for precedence and needs no tie-break), restate the Given as its own sentence, and fix the row's "two AC-2.2 promotions" lead to name the three fixtures' kinds. I verified the guard-set premise the fixture rests on: `pdlc/workflows/` is a member of `MERGE_GUARD_DEFAULTS` (`pdlc/workflows/orchestrate-dev.js:48-53`), so a process learning whose `target` is `orchestrate-dev.js` does take §5.1's PR route absent the merge. | AT-R6b (`:1911`), §8.2, §5.2 |
| F-03 | Medium | Local | **§8.1's new reader-side rule is normative, states a negative three ways, and has no test — while every comparable receive-side rule in this document has one.** The paragraph at `:1101-1112` rules that "a record missing a field the reader indexes is reported as a parse notice and skipped for that contract — never a halt, never a guessed default, and never an in-place repair", and BR-33a restates it. Its AT column cites **AT-F20 and AT-F16**, and neither asserts it: AT-F20's Given is one pass **writing** records (it pins the writer's eight-field set-equality, which the same paragraph says is what makes a short record "a defect at the writer"), and AT-F16's Given is a LEARNINGS carrying an id that matches **no record** — a different failure, on the other side of the boundary. So the rule's positive half (a notice is emitted; §6.4 fails to suppress and the promotion is re-proposed; §8.4 step 1 leaves the id open) is asserted nowhere, and its negative half ("never a halt") is left as an absence-only obligation. The document sets its own bar here and meets it everywhere else — E-12/AT-A7 for a `Feature`-less `ESCALATIONS.md` entry, E-10/AT-C6 for a malformed log row in the §2.5 derivation (`:2411`), AT-F16 for the unmatched id. E-10 is the nearest neighbour and is explicitly scoped to the `passId` derivation, so it does not cover §6.4 or §8.4 step 1. Repair is one E row plus one AT over a constructed `.consolidation-log.md` fixture — exactly AT-F19's shape, which this round already demonstrates is testable at this layer with no agent in it: a record short of `route` read by §6.4's carrier, asserting the notice **and** that the promotion is re-proposed (the positive that pairs the "never a halt"). | §8.1 `:1101-1112`, BR-33a, §19 (no E row), AT-F16/AT-F20 |
| F-04 | Low | Local | **One of this round's new citations points at an error string rather than the read it names.** §6.5 `:925` cites "the branch-guard read at `:3585`"; `:3585` is a line of the halt message's template literal (`` `(git rev-parse --abbrev-ref HEAD failed: ` ``). The read itself is `readHeadBranch` (`orchestrate-dev.js:3520`, issuing `_git(["rev-parse","--abbrev-ref","HEAD"])` at `:3524`), called from the branch guard at `:3580`. The paragraph's *claim* — that the shipped precedent observes the branch through the git seam — is true and is what matters; the pointer is what a TSPEC author will follow. `parseAbbrevRef:3491-3496`, `gitWithLockRetry:8617` and `commitPaths:8669` in the same sentence are exact. | §6.5 `:920-926` |
| F-05 | Low | Local | **§6.5's closing absolute is falsified by the very row it was written to describe.** `:946` ends "equality is asserted on no domain, on any Given" — but AT-Q7c asserts the PR seam and the clone seam **observe `∅`**, which is an equality (and a strictly stronger claim than `∅ ⊆ permitted`, which is vacuous). The intended meaning is plainly "no domain's observed set is asserted equal to its *permitted* set", and that distinction is load-bearing twice over: the `∅` assertions are what make AT-Q7c falsify a pass that quietly clones on a no-guard-set corpus, so a reader who takes the absolute literally would weaken them to containment and lose the row's teeth. Qualify the sentence ("equality **with a domain's permitted set** is asserted on no domain; AT-Q7c's `∅` assertions are equalities with the empty set and are deliberate"). | §6.5 `:944-946`, AT-Q7c |
| F-06 | Low | Local | **§6.5's new fifth column glosses its own condition into a wider one.** The invoking-tree row's Given reads "a pass that makes a §5.4 commit — **i.e. any pass that promotes anything**, PR-opening or not". The two are not the same set: §5.4's fourth consequence (`:712-715`) and AT-R5 describe a pass whose `git add` stages nothing, where "the commit is skipped" — a promoting pass on that path observes `add` and no `commit`, and the obliged conjunct `{add, commit}` is red on it. (The gap is narrow — every pass also appends its terminal row to `.consolidation-log.md`, which is in the same write set, so the stages-nothing path is hard to reach on a promoting pass — but it is the document's own stated path, and the "i.e." is what makes it a contradiction rather than a bound.) Drop the gloss and leave the condition as written, or say that a pass that stages nothing is outside the Given. | §6.5 `:917`, §5.4 `:712-715`, AT-R5 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | §10.4 item 10 now reports the **number** of open promotions, and AT-F19 asserts the number is present in the report body. Is the count asserted **equal to the computed list's cardinality** on that fixture — i.e. `3` for `{B, C, D}` — or only present? Only the first form catches the failure O-C7 added it for: an implementation that computes the list correctly but reports a stale or hard-coded length is indistinguishable from a conforming one under a presence check, and the whole point of item 10 is that the growth is legible *before* it truncates a prompt. AT-F19 already has the fixture; the literal is one word. |
| Q-02 | §8.1's reader-side rule (F-03) says a short record is "skipped **for that contract**". §8.3 is a third reader — it emits one effectiveness row per distinct `failure-mode-id` over every record — and it is not named among the four the rule enumerates (§5.1, §6.4, §8.4 step 1, §10.2 order 2 at `:1063-1064`). If a record short of `artifact` is skipped by §8.3, the promotion silently loses its row on that pass, which reads as `insufficient-evidence` rather than as a notice; if it is not skipped, §8.5's subject file-existence test (BR-35a) has no path to test. Which contract owns it? |
| Q-03 | O-C8 says the elided kind's content survives "in the merged `symptom`", and §8.2 consequence 1 says the merged promotion's single `symptom` "states both failure modes". §8.1 pins `symptom` as **one line, human-readable, explicitly non-keying** (`:1072`). Is one line still the obligation after a mixed-kind merge, and if so, is a two-mode `symptom` bounded anywhere? This is the field an operator re-proposes the elided write from (O-C8's stated compensation), so its legibility is the compensation's whole load — and a three-way merge (two modes plus a re-proposal) is reachable under the same key. |

## Positive Observations

## Recommendation

## Verdict
