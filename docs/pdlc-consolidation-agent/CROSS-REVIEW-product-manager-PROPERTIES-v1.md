# Cross-Review: product-manager — PROPERTIES

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-consolidation-agent/PROPERTIES-pdlc-consolidation-agent.md` (v1.0, 2026-08-09)
**Date:** 2026-08-09
**Iteration:** 1
**Scope:** Product lens only — requirements traceability, scope compliance, acceptance-criteria fidelity. Technical design, test mechanics and code quality are the SE/TE lenses.

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **§12.1's REQ column, and the `AC-…` citation in many property rows, name the wrong criteria — a systematic shift, not a typo.** Measured against REQ v2.1: AC-1.1 is *cadence* and AC-1.2 is *volume*, but §12.1:1453-1454 labels them "volume trigger" / "cadence trigger"; AC-1.3 is the in-progress marker and AC-1.4 is the `no-op` pass, but §12.1:1456 labels AC-1.4 "one pass at a time" and PROP-MRK-01…04 (:812, :826, :840, :847) and PROP-PASS-09 (:1248) each cite `AC-1.4` for marker behaviour that REQ states at AC-1.3; AC-2.4 is the log record, not "process learnings" (:1462); AC-3.4 (PR URL recorded), AC-3.5 (proposal-file fallback + failure classes) and AC-3.6 (never pushed to default branch, branch naming) are shifted by one throughout — §12.1:1466-1468 and PROP-RTE-06 (:907), PROP-PR-03 (:933), PROP-PR-06 (:978); AC-4.2/AC-4.3 are transposed (:1472-1473, PROP-CRED-03 :1034); AC-5.4 is retirement routing, not "late failure reporting" (:1478); NFR-1 is *no promotion applied to the guard set — PR only*, not "append-only" (:1485, PROP-RPT-02 :1067, PROP-PASS-10 :1258); NFR-5 is *never modifies a consumed LEARNINGS + records consumption*, not "traceability" (:1489). The document's completeness claim — "Both directions hold" (:1491) — is exactly what this table is offered as evidence for, so a mis-keyed table means the claim is unverified. It is also the instrument PLAN and the implementer read to decide whether an AC has a falsifier; F-03 and F-04 below are gaps this mis-keying currently hides. Fix: re-key §12.1 and every in-row `AC-…` citation against REQ §3/§4 verbatim, then re-check both directions. | REQ §3 AC-1.1…AC-7.2, §4 NFR-1…NFR-5 |
| F-02 | High | Local | **§12.2 and §12.3 assign properties to files and tasks that contradict the property rows themselves, and one assignment cannot be built.** §12.2:1501 and §12.3:1528 put **PROP-COR-11** in `consolidationHookParity.test.js` under **T04**, but PROP-COR-11 (:362-367) states *"L2 · `consolidationPass.test.js` · T20 → T25/T31"*; T04 owns the hook-parity file in batch 2 (PLAN §5:307), where the pass does not yet exist, so an implementer following §12.3 writes a red block that its own wave cannot green. Symmetrically, the hook-parity rows that *do* exist — **PROP-COR-07** (:383), **PROP-COR-08** (:310), **PROP-COR-12**, **PROP-COR-13** — are filed under `consolidationPredicate.test.js`/T14 by the `PROP-COR-01…10, PROP-COR-13` range at §12.2:1505 and §12.3:1532, and PROP-COR-09/10 (L2, `consolidationPass.test.js`) are swept into that same L1 range. **PROP-PASS-07** has the same defect in the other direction: §12.2:1503 gives it to `consolidationRung.test.js`, the row (:1221) gives it to `consolidationPass.test.js`. Since §12.2/§12.3 are the mapping PLAN's file-ownership manifest is checked against, two documents now disagree about which task owns which property. Fix: derive §12.2/§12.3 mechanically from the file/task named in each §§4–11 row (the authoritative statement), and drop the `PROP-X-01…N` range shorthand where a range crosses two files. | PLAN §4 task table, §5 file-ownership manifest |
| F-03 | Medium | Local | **AC-1.4's positive obligation for a `no-op` pass has no property.** REQ AC-1.4 (`REQ:215-222`) requires that a pass making no new promotion *"still emits the AC-5.2 effectiveness table, restating each prior promotion's **standing** verdict and state (including an `unmeasurable` already reached)"*, and AC-5.5 leans on it (*"AC-1.4 restates it meanwhile"*, `REQ:467`). Searching the document, `no-op` is asserted only for the proposal-file negative (PROP-RTE-06(b), :900), the `pr:`-empty row (PROP-RPT-01, :1058), the terminal-status shape (PROP-CRED-04, :1041) and the streak population (PROP-EFF-06, :710) — none asserts that the table *is* emitted on a `no-op` pass, nor that a standing `unmeasurable` is restated in it. The nearest neighbours are the O-5 pairing at :258 (table present when step 11 completed, absent on `failed`/`refused`) and PROP-PASS-08, neither of which has a `no-op` Given. This is the operator-visible half of US-04/US-05: a quiet week must still report standing verdicts. Add one L2 property with a `no-op` Given, asserting table presence plus a restated `unmeasurable` row. | AC-1.4, AC-5.5, US-04, US-05 |
| F-04 | Medium | Local | **Four AC conjuncts carry no property, each an operator-visible obligation.** (a) **AC-3.4** requires the PR URL in *both* `.consolidation-log.md` **and** `CONSOLIDATION-PROPOSAL-{passId}.md`; PROP-RPT-01 (:1054) covers the `pr:` field only, and no property reads the proposal file for the URL. (b) **AC-3.6** requires that the branch is `consolidation/{passId}`, is never reused, and is **not deleted by the pass** (*"the residue of a half-failed pass stays inspectable"*, `REQ:285-287`); PROP-PR-05's verb bounds (:948) admit `push` without pinning its target away from the default branch, and no property observes the branch surviving the pass. (c) **AC-3.7(c)** requires the PR body to carry the `PDLC-CONSOLIDATION-PASS` trailer so a repo-side control can recognise it; PROP-PR-02 pins the promotions trailer and PROP-PR-08 pins AC-3.2's three body obligations, neither pins this one. (d) **AC-7.1** requires the report to carry *"LEARNINGS consumed by basename"* and *"what it deferred for human judgment"*; `deferred:` and `consumed:` appear only in PROP-RPT-04's **excluded** free-form list (:1080), so no property asserts they are present and populated. Name each of these either as a property or, if deliberately out of layer, in §13.1's gap table — a blank cell is what §12 promises never to leave. | AC-3.4, AC-3.6, AC-3.7, AC-7.1 |
| F-05 | Low | Local | **§12.4 files the configuration properties under the wrong AT family, and the AT-N ids are claimed twice.** §12.4:1559 lists `PROP-CFG-01…03` in the **AT-P** (predicate/corpus) row, but those rows discharge **AT-N1…AT-N4** (:414, :428); the AT-N row (:1569) separately names PROP-RPT-06/PROP-RPT-07, which assert the same four ids (:1110-1123). So AT-N1…AT-N4 are owned by two red tasks (T14 and T24) in two files, and the family table does not show it. Either state the duplication deliberately (L1 reader vs L2 report view) or collapse it to one owner. | AC-7.1, REQ §4a |

## Questions

| ID | Question |
|----|---------|
| Q-01 | §12 states its counts are "the length of the list beside it rather than a transcribed total" (:83-84). Given F-01 and F-02, is §12 intended to be **derived** from the §§4–11 rows at authoring time? If so, the derivation should be stated as the procedure (read each row's file / task / AC citation, group), so a later erratum round regenerates rather than re-transcribes it. |
| Q-02 | AC-3.4 also obliges the URL into `CONSOLIDATION-PROPOSAL-{passId}.md`, while §5.3's proposal file exists only on a degraded route. Is the REQ's conjunct vacuous on the happy path (PR opened, no proposal file), or is a proposal file expected there too? If vacuous, that is worth a §13.1 gap row rather than silence — it reads as a REQ/FSPEC tension this layer noticed and could route. |
| Q-03 | PROP-BLD-02 is stated as "red at today's HEAD by design" and green after T33. Verified at HEAD: `git ls-files pdlc/workflows/dist/` returns **4** tracked files and the manifest carries **3** rows, while `CLAUDE.md:58-62` names three outputs and calls them "those three" — so the pre-existing error is real and the post-T33 arithmetic (5 files, 4 rows) is right. Is the interim red intended to be visible in the wave gate, or carried `describe.skip` until T33? PLAN's wave order decides it; the property does not say. |

## Positive Observations

- **Grounding is re-measured, not inherited, and it is correct.** I re-verified every symbol in §1's table at HEAD: `MERGE_GUARD_DEFAULTS` `orchestrate-dev.js:48-53` (frozen, four members), `ADVISORY_RUNG_SKILL = "se-review"` `:1797`, `resolveAdvisoryRung` `:1833`, `ADVISORY_MODEL_FALLBACK:` `:1859`, `ADVISORY_SEAMS` `:1669`, `gitWithLockRetry` `:8653` (module-private `async function`), `commitPaths` `:8705` with its unscoped `git commit -m` at `:8726`, `commitQueueRow` `orchestrate-queue.js:1576-1595`, `rtListFiles` `runtime-adapter.js:915`/`:929-931`, `seams.js:296-299`, `driftGenerators.js:76`/`:134`, `package.json:18-22`, `runtimeBundle.test.js:215`/`:1040`, `ESCALATIONS.md` absent, and the live corpus at exactly 5 LEARNINGS files. Every one holds. Document versions pin correctly too: FSPEC 11.5, TSPEC 2.0, vocabularies 1.4, and the FSPEC AT register measures 99 distinct ids.
- **Two upstream defects were routed rather than absorbed.** §13.3 emits the stale-locator drift and REQ AC-6.3's "across the consumed window" wording as errata instead of quietly writing properties against the wrong text. I confirmed both: the `:8617`/`:8669`/`:8690` citations are live in FSPEC `:791-792`, TSPEC `:255-258` and PLAN `:107`/`:451-452`, and FSPEC BR-37a (`:2648`) does range over the whole file, contradicting the REQ sentence. This is exactly the discipline the pipeline asks for.
- **The oracle rules answer the product-lens worries directly.** O-1 (no absence-only), O-2 (set-equality wherever a dropped member is invisible) and O-3 (invariance never asserted alone) are stated once and then discharged per row — PROP-RPT-04's fourth leg reading the authority file, PROP-TRC-01's non-vacuity floor, PROP-COR-12's replacement of a byte-identity claim that "passed vacuously on this repo" are all cases where a weaker oracle would have looked fine.
- **Fixtures are constructed, never the live repository (PROP-FIX-02), with the reason stated concretely** — the corpus grows with every delivered feature, this one included, so a Given pinned to HEAD inverts on its own PR. That is the kind of reasoning that keeps a suite honest a year out.
- **The scope boundary is respected.** DEC-LAYER-01 is cited and obeyed: FSPEC §14.5's five layer deferrals (LD-1…LD-5) are each discharged by a named property, set-equal to the five FSPEC records, and no FSPEC/TSPEC rule is re-decided here. I found no scope creep — nothing asserted that REQ does not ask for.

## Recommendation

**Needs revision**

Two High findings, both about the traceability instrument rather than the properties themselves. Exactly what to change:

1. **F-01** — Re-key §12.1's REQ column and every in-row `AC-…`/`NFR-…` citation against REQ v2.1 §3/§4 verbatim (the marker family AC-1.3-vs-AC-1.4, the AC-3.4/3.5/3.6 shift, AC-4.2/4.3, AC-2.4, AC-5.4, NFR-1, NFR-5), then re-run the two-direction check and correct anything it newly reveals.
2. **F-02** — Regenerate §12.2 and §12.3 from the file and task each §§4–11 row names; specifically move PROP-COR-11 out of the hook-parity/T04 cell, restore PROP-COR-07/08/12/13 to it, take PROP-COR-09/10/11 out of the `PROP-COR-01…10` L1 range, and reconcile PROP-PASS-07's file with :1221.

The three non-blocking findings (F-03, F-04, F-05) are recorded and may be addressed in the same revision: they are coverage gaps that the corrected §12 will surface anyway.

## Verdict

VERDICT: Needs revision
{"high": 2, "medium": 2, "low": 1}
