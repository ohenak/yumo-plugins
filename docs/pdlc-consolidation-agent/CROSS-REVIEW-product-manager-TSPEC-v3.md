# Cross-Review: product-manager — TSPEC

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-consolidation-agent/TSPEC-pdlc-consolidation-agent.md` (v1.2)
**Date:** 2026-08-06
**Iteration:** 3
**Scope:** Local (per-finding tags in the table)
**Delta base:** `ea5be5a` (the commit carrying my v2 review) → HEAD `e75feca`; `git diff` over the document is 251 insertions / 62 deletions across §1.1 (T-08), §3.1, §3.2, §4.1, §5.5, §5.6, §7.1, §7.3, §7.6, §8.3, §10.1, §10.2, §10.3, §10.4, §11.1, §11.2, §11.3(e)/(f), §11.4, §12.2, §12.3, §12.4, §13.1 and §13.3. Only changed sections are re-read; unchanged sections approved earlier are not re-litigated.

## Prior findings — disposition

| v2 ID | Sev | Status | Evidence in the revision |
|---|---|---|---|
| F-07 | Medium | **Resolved** | The read-side widening is withdrawn, and withdrawn on measurement rather than by softening the prose. §5.5 now splits the one row into two — `_writeFile` "repo-root-relative today, and that is a blocker", `_readFile` "**already absolute-safe; no change**" — §5.6(a) is retitled "One adapter contract this feature changes, one it deliberately leaves alone", §9.2 states "Only the **write** prompt changes", §13.1 row 11 records widening `rtWriteFile` **alone** with "widen both prompts for symmetry" as the rejected alternative, and §13.3's PLAN hand-off says "§5.6(a)'s **one** prompt widening (`rtWriteFile`; `rtReadFile` is not edited)". Every empirical claim behind that verifies at HEAD: `relative to the repository root` occurs in `pdlc/workflows/runtime-adapter.js` **exactly once**, at `:805`, inside `rtWriteFile` (`:802-811`); `rtReadFile` is `:493` and its transport carries the *cwd* instruction "Run this exact command from the repository root" at `:374`, which resolves an absolute path verbatim. §11.3(e) is re-scoped to `rtWriteFile`'s prompt with two conjuncts — the widened clause verbatim, **and** an occurs-exactly-once count that falsifies the opposite mistake — and states positively why no `rtReadFile` assertion exists ("an assertion there could only pin text that does not exist — which reds on a correct tree and gets 'fixed' by deletion"). That is a stronger repair than the one I asked for. **My v2 sub-claim (iii) was wrong and I withdraw it:** I reported the shipped bytes as `relative to repository root`; they are `relative to the repository root`, exactly as this document quotes. The document's transcription was right and my correction of it was not. |
| F-08 | Low | **Resolved** | All six pointers corrected and re-verified at HEAD: `build-runtime.mjs:44` is `stripModuleSyntax`'s doc comment (`:45` the declaration) and `:54`/`:55` the same for `wrapModule`; the `rtWriteFile` prompt clause is `:805`; the function is `:802-811`; `rtListFiles`'s transport line is `:915` (`:913` is the `__PDLC_NOT_A_DIRECTORY__` arm); the reply validator's separator rejection is `:929-931`; and the `Route` row is `docs/_constraints/pdlc-consolidation-vocabularies.md:57`, inside §1's `:38-65` table, with `Version` `1.4` at `:7`. §13.1 row 10 and §12.4 carry the corrected forms too. |
| F-09 | Low | **Resolved** | §10.1's snippet is `async function finishPass(state)` with all three steps awaited, and a new normative paragraph states that `main()` writes `return await finishPass(state)` at **every** terminating branch; §4.1 and §10.2's two `catch`/`kind` returns are updated to match. Better than the minimum: the revision explains why §11.3(c)'s identifier scan and every sync-double suite are structurally blind to this, and mints T-13 plus `consolidationLifecycle.test.js` and §11.2's `asAsync` wrapper to make it falsifiable — an oracle read **after** `main()`'s promise resolves, with a positive conjunct (terminal row present) beside the negative (marker absent). |

Questions Q-04, Q-05 and Q-06 are all answered. Q-04: §7.1's new paragraph routes the `PDLC_PENDING:` stderr line as **production code** owned by the hook's implementation task, and names the release note. Q-05: §8.3 states that no AC owns the drift-gate interruption and argues why an AC would be the wrong home, leaving §13.3's release-note obligation as the whole discharge — an acceptable answer, since the mechanic is the shipped gate's, not this feature's. Q-06: §7.6 and §12.4 now specify the two-fixture control in `consolidationReport.test.js` (routed propose-only vs. `branch-exists`-degraded, identical `route: "degraded"`, reason code present in one report body and absent in the other, both directions), so the ER-6 discriminator is asserted rather than asserted-about.

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-10 | High | Local | **The revision relaxes a REQ-level guarantee and narrows an FSPEC acceptance test at this layer, and routes neither upstream — in the same round where it routes two smaller gaps as errata.** REQ step 1 states the corpus rule and closes with "Widening makes `nudge-consolidation.sh:28` an in-scope edit (§5), **keeping one enumeration as well as one predicate**" (`REQ-pdlc-consolidation-agent.md:115-116`), and FSPEC AT-P7's *When* is "**both the pass's enumeration** and `pdlc/hooks/scripts/nudge-consolidation.sh` are run over each case", its *Then* "the two un-consolidated sets are **set-equal** on every case" (`FSPEC-…:2026`; BR-09 binds AT-P7 at `:2489`). The revision changes both: §11.3(f) now states that feeding both sides one basename list puts "the enumeration pair … outside AT-P7's reach entirely", §12.2's T-08 row is rewritten to "the *evidence* holding them equal is narrowed deliberately to the **predicate**; the enumeration half is held by inspection, not by a test", §13.1 row 6 restates it, and §10.4 records two divergence classes — one of them operator-visible ("the operator is nudged about a file no pass will consolidate, **and no pass can clear the nudge**") — as accepted exposure. Accepting that is a product decision about what "one enumeration" is worth, and it is being made in a technical spec. The document demonstrates in this same diff that it knows the right channel: §12.2's T-11 and T-12 cells raise two register gaps as **errata** against the FSPEC rather than absorbing them. The enumeration relaxation is larger than either and gets no erratum. It also gets no compensating oracle, where the same revision was willing to mint two non-register tests (T-13's `consolidationLifecycle.test.js`, the dropped-code notice under AT-L5) — so an implementation whose enumeration silently diverges further than these two classes reds nothing. Either close the divergence (see F-11 — the cost of doing so is misstated), or raise it as a REQ/FSPEC erratum so the layer that owns "one enumeration" decides whether it is relaxed, and record here what the accepted residue is. | REQ §3.1 step 1 (`:107-116`), NFR-5, BR-09, AT-P7 |
| F-11 | Medium | Local | **§10.4's reason for calling the divergence unclosable is false by measurement, and contradicts §7.1 of the same document.** §10.4 (`:1604-1607`) says neither class is closable "without making one side ask the other's question: teaching the hook `git ls-files` puts a git invocation in a `SessionStart` hook …, **and dropping `--exclude-standard` re-admits the two `docs/discarded/` directories §7.1's `:(glob)` anchoring exists to exclude**." The second clause is wrong. Measured at HEAD: `git ls-files --cached --others -- ':(glob)docs/*/LEARNINGS-*.md' ':(glob)docs/completed/*/LEARNINGS-*.md'` (i.e. `--exclude-standard` dropped) returns the **same five** paths and **zero** under `docs/discarded/`; it is dropping `:(glob)` that re-admits the two discarded files (seven paths). §7.1 point 2 says exactly this and says it correctly — "Exclusion of `docs/discarded/` is therefore still by *not enumerating* … and not by a filter a later edit can drop" (`:604-609`) — so §10.4 contradicts §7.1 eleven hundred lines later. The consequence is not cosmetic: divergence class (i) (an ignored LEARNINGS file nudged forever and never consolidated) is closable at the cost of §7.1 point 3's "an ignored file never is [corpus]" rule and nothing else. That is a **product** trade — is a `.gitignore`d LEARNINGS file corpus? — presented as a technical impossibility, which is how a product decision gets made without anyone deciding it. State the real cost, then choose. | REQ §3.1 step 1, AC-1.2 |
| F-12 | Medium | Local | **Two obligations are now correctly identified as having no acceptance test, and the revision leaves them with no test at all rather than covering them here.** §12.2's T-11 records that "**AC-3.2's three body citations have no AT in the register**" — verified: the AC→AT map binds AC-3.2 to AT-Q2 (`FSPEC-…:2269`), and AT-Q2's *Then* is "there are three commits, each with a distinct `PDLC-PROMOTION-ID: {id}:{action}`, and `PDLC-CONSOLIDATION-PROMOTIONS` is **set-equal** to those three pairs" (`:2065`), which asserts nothing about the body citing source LEARNINGS by feature name, the failure mode, or the AC-2.3 evidence. §12.2's T-12 records the same for FSPEC §5.3's "when, **and only when**" negative direction — verified: AT-Q6 (`:2069`) and AT-Q8 (`:2073`) assert only the positive, AT-Q9 (`:2074`) is the deleted-branch trailer-survival case and AT-Q11 (`:2076`) is `DOMAIN-CONSTRAINTS.md` byte-identity, so nothing asserts that a pass which enacts everything writes **no** proposal file. Recording the gaps is right, and the re-binding that surfaced them is right. But AC-3.2 is a REQ acceptance criterion describing the artifact an operator actually reads — the PR body — and after this revision it is discharged by §7.9's `renderPrBody` with **no falsifier anywhere in §12**. The register is not the only place a test may come from: §12.3 adds `consolidationLifecycle.test.js` marked "(no FSPEC AT)" and the dropped-code notice is carried under AT-L5 as "a TSPEC-added observable with no register id", both in this diff. Apply the same remedy to AC-3.2's citations and to §5.3's negative — a TSPEC-added case claiming no register id, so §12.3's set equality stays undisturbed — instead of leaving a REQ AC with a documented hole until the errata land. | AC-3.2, AC-3.5, FSPEC §5.3 |
| F-13 | Medium | Local | **The new "enumeration held by no test" claim contradicts §12.3's own assignment of AT-P1, and AT-P1's oracle is never stated.** §12.2's T-08 row and §13.1 row 6 now assert that the enumeration half is "held by inspection, not by a test". But AT-P1's *Given/When/Then* is purely enumeration — "LEARNINGS under `docs/completed/{feature}/` … corpus is enumerated … is in corpus; LEARNINGS under `docs/discarded/{feature}/` is **not**" (`FSPEC-…:2020`) — and §12.3 assigns AT-P1 to `consolidationPredicate.test.js` at **L1** (`:1925`), unchanged from v1.1. Both statements cannot be true. If AT-P1 is run at L1 through the `_git` double, then the `docs/discarded/` exclusion is decided by the fixture the test author wrote rather than by the pathspec, which is an implementation-echo oracle over the one exclusion the REQ calls out by name (`REQ-…:113-114`, "abandoned work is not evidence about a delivered pipeline") — and it is also the only *negative* in that AT with no positive companion in the same fixture. There is a real falsifier available and §7.1 already names its content: assert the **argv** the pass hands `_git` — `["ls-files", "--cached", "--others", "--exclude-standard", "--", ":(glob)docs/*/LEARNINGS-*.md", ":(glob)docs/completed/*/LEARNINGS-*.md"]` — as a literal, both `:(glob)` prefixes included, since §7.1 point 2 makes `:(glob)` load-bearing. Say which of the two claims stands and state AT-P1's oracle. | AC-1.2, REQ §3.1 step 1, AT-P1 |

## Questions

| ID | Question |
|----|---------|
| Q-07 | §7.1's relocated early exit (`nudge-consolidation.sh:29-30` replaced by a `pending = []` fall-through) is correct on the stdout contract — I checked the shipped script: with `pending` empty, `n = 0`, the `n >= THRESHOLD` test at `:43` is false, nothing is printed, `sys.exit(0)` at `:49` runs, so stdout is byte-identical on a zero corpus. But the fall-through also means the log file at `:32-39` is now opened and read on **every** zero-corpus session, where today the hook exits before touching it. That is a new read on a `SessionStart` path in every repository that ships this plugin. Harmless as far as I can see (the read is already `try`-wrapped and `errors="ignore"`), but it is a shipped-behaviour change with no AC, so: is it deliberate, and does the release note's "default-off debug channel" line cover it? |
| Q-08 | §10.4 class (ii) says a staged-but-deleted LEARNINGS file lands in the pass's corpus, `_readFile` returns `null`, and `classifyCorpus` treats it as "an unreadable corpus entry (§7.1) and the pass reports rather than crashing on". Does that entry count toward `|un-consolidated|` for the AC-1.2 volume test, and does it appear in the consumed pair? A file that can never be read but permanently inflates the volume count would trip the threshold and then be consumed-without-consolidating on every pass — I could not find where §7.1 or §6 decides it either way. |
| Q-09 | §13.1 row 12 and §7.1 make the `PDLC_PENDING:` stderr channel the thing that "makes even the predicate half observable", i.e. AT-P7 exists only because a shipped consumer hook now carries a test-affordance. With F-10's narrowing on top, the whole of AT-P7's remaining value is a predicate differential. Is that still worth a permanent edit to a shipped `SessionStart` hook, or would the PM prefer the predicate be extracted once (row 6's rejected alternative) now that the enumeration half is conceded anyway? Not a finding — a question about where the product wants the cost. |

## Positive Observations

## Recommendation

## Verdict
