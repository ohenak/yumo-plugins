# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-consolidation-agent/FSPEC-pdlc-consolidation-agent.md` (v2.0)
**Date:** 2026-08-06
**Iteration:** 2
**Scope:** Local unless tagged otherwise
**Protocol:** delta re-review. Baseline `4b65278` (the commit `CROSS-REVIEW-software-engineer-FSPEC-v1.md` reviewed); diff `4b65278..HEAD` — 399 insertions, 97 deletions across 13 commits. Only the changed sections were re-read for new issues.

## Prior findings — disposition

Every v1 finding was re-checked against the revision and, where it made a claim about HEAD, against
the code. Ten of twelve are closed; one is closed only in part.

| v1 | Verdict | Evidence |
|---|---|---|
| F-01 (High) — `resolveAdvisoryRung` reuse not implementable | **Partially resolved** → re-raised as F-01 below | §2.6 now states the seam correctly: no probe mode (doc comment `orchestrate-dev.js:1811-1813` — verified verbatim), the hardcoded `ADVISORY_RUNG_SKILL = "se-review"` (`:1797`, dispatched `:1841`), the exported signature `({ _agent, _log, _state, prompt })` (`:1833`), and the chosen repair (an optional `skill` parameter). Step 8 is now *the first advisory dispatch*, which is the only reading the seam permits. **But** the two places §2.6 says carry that repair do not carry it — see F-01 |
| F-02 (High) — the resolver's fourth outcome unmapped | **Resolved** | §2.6 row 4 maps `{kind: "dispatch-error", err}` (`:1857`, `:1867` — both verified) to `failed` with no reason code and the message in the report body; S-11b, E-19b and AT-M6 carry it; the missing vocabulary row is routed as ER-2. The claim "rows 1–4 are set-equal to the resolver's return and throw set" is true of `:1833-1875`: two `{kind:"response"}` paths, two `{kind:"dispatch-error"}` paths, one `throw haltError` (`:1868`) |
| F-03 (High) — NFR-4 has no carrier for the consuming-repo routes | **Resolved in structure** | §6.4 now names two carriers per route, and §3.4 / §4.5 withdraw the claims the PR-trailer carrier could not support — including an explicit statement that the §4.5 race admits a duplicate consuming-repo append. Two follow-on defects in the *new* carrier are filed as F-04 and F-05 |
| F-04 (High) — §8.5 row 3 undecidable over `symptom` | **Resolved** | Row 3 is now a file-existence test on `artifact` at the pass's HEAD; the withdrawal is argued explicitly; AT-F17 / AT-F18 assert both arms and AT-F17 asserts determinism across two runs |
| F-05 (High) — `recurred` unobservable on the producing side | **Resolved** | §8.4 adds a four-step harvest-side **lookup** (never a re-derivation), the subset property, the parse-notice arm for an unmatched id, O-C6 for the recall limit, and AT-F15 / AT-F16. AT-F15's Given ranges over a harvest-authored LEARNINGS, which is what v1 asked for. One residue is filed as F-06 |
| F-06 (Medium) — two opposite `no-op` proposal-file rules | **Resolved** | §5.3's four-row table decides the file's existence on the three proposal rows and explicitly *not* on the terminal status; the AC-1.4 / AC-4.3 tension is routed as ER-3 rather than papered over |
| F-07 (Medium) — false injectivity claim | **Resolved** | The claim is withdrawn; the collision is stated with a worked example, a three-row consequence table and an argued refusal of the lossless repair |
| F-08 (Medium) — "the consumed window" undefined | **Resolved** | §9.2 fixes the population as the whole of `ESCALATIONS.md`, §9.4 and §9.5 are re-worded to match, BR-37a states the rule, and AT-A6 pins it **differentially** (disjoint vs matching `Feature` values must give an identical verdict) — a genuinely falsifying oracle, not a restatement |
| F-09 (Medium) — AC-1.6's fallback branch untested | **Resolved** | AT-M7 asserts all three conjuncts of the non-silent downgrade and AT-M8 is its paired positive-rung negative; §15.1 remaps AC-1.5 → AT-M7/AT-M8 and AC-1.6 → AT-M4/AT-M6/AT-M7 |
| F-10 (Medium) — AT-Q7 absence-only | **Resolved in shape, broken in content** → re-raised as F-02 below | The oracle is now a positive set-equality with a paired PR-state assertion and a demoted supplementary source check (AT-Q7b), and §6.5 control (b) and BR-28 are restated positively. The enumeration it compares against is wrong |
| F-11 (Low) — three citation off-by-ones | **Resolved** | Verified at HEAD: `commitQueueRow`'s commit is `orchestrate-queue.js:1580-1586` ✓, `NOTHING_TO_COMMIT_RE` is `:1631-1635` ✓, the resolver's `throw haltError(` is `orchestrate-dev.js:1868` ✓ |
| F-12 (Low) — `rungState` precedent miscited | **Resolved** | §2.6 now cites `orchestrate-queue.js:1120` for the shape ✓, states that `:1245-1256` threads it into `runAdvisorySeam` ✓, that the resolver's only shipped call site is `orchestrate-dev.js:3132` ✓, and that this pass's direct call is therefore a **new** call site |

## Findings

All findings below are in sections the revision changed. Nothing unchanged since v1 is re-litigated.

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **The `orchestrate-dev.js` edit §2.6 depends on is in neither of the two places §2.6 says it is.** §2.6 item 2 states the feature "adds an **optional `skill` parameter defaulting to `ADVISORY_RUNG_SKILL`**… That is an edit to `pdlc/workflows/orchestrate-dev.js` — a guard-set path — and it is listed as such in §15.3 and constrained at §14.1 T-05." Neither is true of the document: §15.3's seven-row edit table names `nudge-consolidation.sh`, `consolidate-learnings/SKILL.md`, `harvest-learnings/SKILL.md`, `.gitignore`, `build-runtime.mjs` + `distribution-manifest.json`, and the two `docs/_constraints/` files — **`pdlc/workflows/orchestrate-dev.js` is absent**, and so is the `pdlc/workflows/dist/` rebuild that CLAUDE.md requires "in the same commit" as any source edit (only `distribution-manifest.json` is listed, not `orchestrate-dev.bundle.js` / `orchestrate-queue.bundle.js`, both of which inline `devModule` wholesale — `build-runtime.mjs:448-466`). §14.1 T-05 still reads "The `resolveAdvisoryRung` call site and its `rungState` threading — §2.6: reuse, never restate, the two constants", saying nothing about a signature widening. §15.3 is the document's edit manifest and feeds the PLAN's file-ownership manifest; a PLAN built from it omits the one change without which §2.6's entire reuse story does not compile. Add the row (source + rebuilt bundles), and extend T-05 to constrain the widening (default-valued, every existing call site unchanged, one ladder). | §2.6 item 2, §15.3, §14.1 T-05 |
| F-02 | High | Local | **AT-Q7's new set-equality oracle is false on a correct implementation, and it is now the sole runtime evidence for a safety control.** The Given puts "**every** git and PR seam behind a spy that records the verb of each call" and asserts the observed multiset is "**set-equal** to exactly `{create-branch, push, create-pr}`". Two of the pass's own mandatory calls are outside that set. (a) §5.4 requires `git add -- {paths}` then `git commit -m {msg} -- {paths}` in the invoking tree, on **both** calls, on every terminal outcome — two git verbs the spy sees. (b) §6.4's PR-route carrier is "the `PDLC-CONSOLIDATION-PROMOTIONS` trailer of PRs in the target repository", and the PR-state table distinguishes `open` / `merged` / `closed`-unmerged / reopened — i.e. the pass **must** read PRs before it opens any, which is at least one more PR verb (AT-Q3/AT-Q4/AT-Q5 all depend on that read happening). So AT-Q7 is red on a conforming pass. Because §6.5 control (b), BR-28, AC-3.7 and NFR-1 now all route their observable through AT-Q7 (AT-Q7b is explicitly demoted to "never the sole evidence"), the predictable repair is an implementer loosening the oracle to containment — which restores exactly the absence-only weakness v1 F-10 raised. Enumerate the full permitted verb set (the two §5.4 git verbs, the §6.4 read, the three write verbs) and keep the equality; alternatively partition the spy into a git seam and a PR seam and state a set-equality over each. | §13.5 AT-Q7, §6.5 (b), §5.4, §6.4, BR-28 |
| F-03 | High | Local | **§5.2's new `{topic}` derivation contradicts the continuity the same table asserts, and its stated rationale is false.** The §5.2 route table gives the AC-2.2 destination as `docs/_decisions/DECISIONS-{topic}.md` "**as today** (`:41`)" — `pdlc/skills/consolidate-learnings/SKILL.md:41`, verified. The derivation added beneath it is `{topic} = failure-mode-id with the artifact's directory segments dropped`, worked as `DECISIONS-p-skill-md.md`. That is not "as today": the three decision files at HEAD are `DECISIONS-plugin-distribution.md`, `DECISIONS-review-severity-bars.md`, `DECISIONS-test-oracle-mechanics.md` — topical names a pass can never re-derive, so every promotion on a topic already recorded starts a **parallel** file rather than appending to the existing one, and the second property row ("An existing file at that path is appended to") therefore almost never fires on this repo. The derivation is also degenerate in the direction it is meant to be precise: dropping directory segments collapses every `SKILL.md` in a phase to one topic (`pdlc/skills/se-author/SKILL.md`, `pdlc/skills/dod-verify/SKILL.md`, `pdlc/skills/te-review/SKILL.md` all ⇒ `p-skill-md`), so unrelated architectural decisions merge into a file whose name is a phase letter and a file extension. Property row 1's justification — "so §6.4's consuming-repo carrier can suppress the second — a model-chosen topic would not be stable enough to key on" — is a non-sequitur: §6.4 keys suppression on `(failure-mode-id, action)`, never on the destination path, so path stability buys the carrier nothing. Either derive a topic from something topical and state how it reconciles with the three files at HEAD, or state plainly that this feature changes the `{topic}` convention and route that as an edit to `SKILL.md:41` in §15.3. | §5.2, §6.4, §15.3 |
| F-04 | Medium | Local | **The new consuming-repo carrier reads a field the record's definitional table does not carry.** §6.4 row 2 names the carrier as "the **§8.1 failure-mode records** already in `docs/_decisions/.consolidation-log.md`, each of which carries its `failure-mode-id` and its `action` (§10.2 order 2)". §10.2 order 2 is "one failure-mode record per promotion (§8.1)", and §8.1's record is defined as a **four-field** table — `failure-mode-id`, `phase`, `symptom`, `artifact` — with **no `action`**. §8.4's new step 1 restates the same four ("Each promotion record carries `failure-mode-id`, `phase`, `artifact` and `symptom` (§8.1)"). Only §8.2's aside — "Log **records** are keyed `(failure-mode-id, passId, action)`" — supplies it, and a keying tuple is not a field list. Since the whole F-03 repair rests on the log record carrying half the suppression key, the record's own table must carry it: add `action` (and `passId`) to §8.1's table, or re-point §6.4 and §8.4 at §8.2 and say which section is normative for the record's shape. | §6.4, §8.1, §8.2, §8.4, §10.2 |
| F-05 | Medium | Local | **`enacted` does not distinguish a promotion that landed from one that only reached a proposal file.** §6.4's carrier table maps *both* the §5.2 consuming-repo route **and** the §5.3 proposal-file route to the log carrier, over the two-member set `enacted` / `absent`, where `enacted` means only "some prior pass's failure-mode record carries the same pair". But §10.2 order 2 writes a failure-mode record for **every** promotion the pass made, and §6.3/§7.3 degrade a promotion to a proposal file precisely when it could **not** be applied (`credential-unavailable`, `branch-exists`, `api-failure`, `repository-unresolved`). A later pass therefore reads `enacted` for a promotion that exists nowhere but in a proposal file, and permanently withholds it — the exact opposite of the PR route's deliberate rule that a `closed`-unmerged PR is **not** in the key set and the proposal is re-proposable (AT-Q4). Either `enacted` must be conditioned on the write having landed (and the record must say so), or the suppression must be stated as deliberate with its cost named. No AT in §13.5 or §13.7 covers a degraded promotion re-proposed on a later pass, so nothing currently falsifies either reading. | §6.4, §6.3, §7.3, §10.2, §13.5 |
| F-06 | Medium | Local | **§8.4 step 1's "open promotion" ranges over a state the document does not define.** "A promotion is **open** when its most recent standing state is not `retired`." There is no `retired` standing state anywhere: vocabularies §1's per-promotion state row is `ineffective` / `unmeasurable` (verified, `pdlc-consolidation-vocabularies.md` §1), `retire` is an **action** and `retirement` a reported field, and §8 elsewhere uses "retired" only as prose ("`retire` is terminal — a retired promotion is gone"). Step 1 is the input set of a procedure this feature must write into `pdlc/skills/harvest-learnings/SKILL.md`, so "open" has to be computable from the log by a harvest agent: say whether it means "no record for this id carries action `retire`", or "no *merged* retire", and note that a `retire` proposal on an unmerged PR is not observable from the log at all. This is the same class of defect as v1 F-04 — a delegated predicate discharged over an undefined term — at a smaller blast radius (a wrong input set adds or drops questions in step 2, never a wrong verdict). | §8.4, §8.5, vocabularies §1 |
| F-07 | Medium | Local | **§10.3's `credential:` disambiguation claims totality and is not total.** The new table splits `credential: absent` by status: `refused` ⇒ "not attempted", "any other status" ⇒ "attempted and found nothing", and asserts the separating observable is that "attempted-and-found-nothing **always** co-occurs with `credential-unavailable`; not-attempted never does". A `failed` row falsifies both halves. S-11 and S-11b terminate at step 8, long before the §7.2 credential resolution or any PR route, so their `credential: absent` is **not attempted** — yet they fall in the "any other status" row; and they cannot carry `credential-unavailable` either, because vocabularies §1 permits that code only with `promoted-degraded` and `no-op` (verified). The same holds for any `failed` row. Add the `failed` statuses to the not-attempted row (or key the reading on "did the pass reach step 12", which is what actually distinguishes them), and widen AT-K6's Given beyond the two rows it names — as written AT-K6 tests only the pair the table gets right. | §10.3, §12.1 S-11/S-11b, §7.2 |
| F-08 | Low | Local | **§2.6's precedent for an empty `reason:` field does not exist as a written row.** "…`skipped-cadence` already ships carrying none — so an empty reason field is a legal row, not an invented value". A `skipped-cadence` tick appends **no log row at all** (§10.1's table, §12.1 S-01, AT-C3), so it is not a precedent for an empty `reason:` on a row. The behaviour is fine regardless — §10.3 defines `reason:` as "zero or more reason codes" on this layer's own authority, and vocabularies §1 lists no code permitted with `skipped-cadence` — but the sentence should cite §10.3, not a row that is never written. | §2.6, §10.1, §12.1 S-01 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | *(carried from v1, still open and now sharper.)* The runtime forbids imports, and `build-runtime.mjs` reaches `orchestrate-dev` from the queue bundle only by inlining the whole module (`bundles`, `:448-466` — `[QUEUE_META, BANNER, adapter, devModule, queueModule, QUEUE_ENTRY].join(…)`, verified). With F-01's `skill` parameter added, does the consolidation bundle likewise inline all ~10k lines of `orchestrate-dev.js` to reach `resolveAdvisoryRung`? §14.1 T-02 delegates only "the bundle's row in `distribution-manifest.json`, entry in `build-runtime.mjs`" and does not name the inlining. If the answer is yes, T-02 should say so, because it also decides whether the consolidation bundle inherits `orchestrate-dev`'s own module-level constants and whether a drift in either bundle is one artifact or three. |
| Q-02 | §2.6 says every later dispatch "goes out through the **same** resolver with the same `rungState`". The one shipped call site does not call it bare: it races the returned promise against a wall-clock deadline and dispositions a fifth shape, `{kind: "preempted"}` (`orchestrate-dev.js:3130-3134`). The pass has no seam budget (`seamBudgetMinutes` is the advisory tier's, not this feature's) — is the intent that the pass calls the resolver with **no** deadline, relying solely on the runtime's 180 s no-progress watchdog? If so, §2.6's "rows 1–4 are set-equal to the resolver's return and throw set" is exactly right, but the absence of a deadline is a decision worth recording next to it, since a hung clustering dispatch then kills the pass with the marker still held (recoverable only through §4.2's stale-lock reclaim). |
| Q-03 | AT-M7 asserts "the report body contains the `ADVISORY_MODEL_FALLBACK:` line **verbatim**". The resolver emits that line through `_log` (`orchestrate-dev.js:1858-1860`), not in its return value, so the pass must capture its own log stream to put it in the report body. Is that TSPEC's to choose (§14.1 has no row for it), and does the same capture obligation extend to row 4's "error's message surfaced verbatim in the report body"? |
| Q-04 | §8.4 step 2 asks the harvest agent one question **per open promotion**. The open-promotion list grows monotonically with every pass (ids repeat across passes by design, §8.2). Is there an intended bound — a cap, a recency window, or a `retired`/`unmeasurable` closure rule — before the harvest prompt's question list becomes the dominant cost of every Phase H? §8.7's `unmeasurable` state does not remove a promotion from the list as written. |

## Positive Observations

- **The seam analysis in the revised §2.6 is exact, and I verified every line of it.** The doc comment really does rule out a probe ("never by a separate probe — the caller's own `prompt` is what goes out", `orchestrate-dev.js:1811-1813`); `ADVISORY_RUNG_SKILL = "se-review"` really is a module constant (`:1797`) dispatched at `:1841`; the export really does take `({ _agent, _log, _state, prompt })` (`:1833`); the memo really does short-circuit the ladder with `_state.resolved` set (`:1844-1849`); the two `dispatch-error` returns are `:1857` and `:1867`; the halt is `:1868`. The document reasoned from the code rather than around it, and it chose the harder repair (widen the signature) over the cheaper one the corpus baseline sanctioned, for a stated reason.
- **Every v1 citation correction landed exactly.** `commitQueueRow`'s commit at `orchestrate-queue.js:1580-1586`, `NOTHING_TO_COMMIT_RE` at `:1631-1635`, `rungState` initialised at `:1120`, threaded into `runAdvisorySeam` at `:1245-1256`, the resolver's only shipped call site at `orchestrate-dev.js:3132` — all four re-verified at HEAD. §2.6 now says outright that the pass's direct call is a **new** call site rather than citing a precedent that does not exist, which is the honest form of the claim.
- **AT-A6 and AT-C8 are differential tests, not restatements.** AT-A6 runs one corpus twice with `Feature` values disjoint from and matching the consumed set and demands an identical verdict; AT-C8 runs one corpus under both triggers and demands set-equal promotion sets. Both fail on the specific wrong implementation they exist to catch, which is a materially stronger oracle shape than the enumerated example they replaced.
- **The §19 preamble is a durable process improvement.** "The AT column is a per-row obligation, not a family citation" — with the six offending rows named (E-02, E-05, E-06, E-09, E-23, E-29) and each given its own test — is the right general rule for an edge-case table, and it is stated where a later editor will read it.
- **Three losses are recorded rather than engineered around.** O-C6's recall limit is argued in the safe direction (a harvest miss yields `prevented`/`insufficient-evidence`, never a false `recurred`); §4.5 now states outright that the consuming-repo route admits a duplicate append in the marker race; §8.1 withdraws the injectivity claim and prices the collision. Each is stated against the document's own interest.
- **§14.4's three errata are correctly scoped and correctly routed.** I independently confirmed all three against `pdlc-consolidation-vocabularies.md` at `Version` 1.4: there is no `rung:` row, there is no reason code for a non-model dispatch failure, and AC-1.4's two causes do not cover AC-4.3's degraded pass. None of them is this layer's to patch.

## Recommendation

*(filled below)*
