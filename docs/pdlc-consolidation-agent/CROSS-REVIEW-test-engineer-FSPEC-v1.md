# Cross-Review: test-engineer — FSPEC

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-consolidation-agent/FSPEC-pdlc-consolidation-agent.md` (v1.0)
**Date:** 2026-08-06
**Iteration:** 1
**Scope:** Testing lens only — testability of §13's acceptance tests, oracle falsifiability, edge-case
totality, and DC-05 branch coverage. Product framing, architecture choice and prose style are out of
scope.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **AT-C1 is pinned to the live repository and this feature's own run falsifies it.** Its Given is "this repository at HEAD — 5 LEARNINGS in corpus, 2 named in the legacy region" and its Then is "the un-consolidated set has **3** members; the volume test does **not** fire". Verified at HEAD: the corpus is exactly the 5 files §3.1 lists. But Phase H of *this* pipeline writes `docs/pdlc-consolidation-agent/LEARNINGS-pdlc-consolidation-agent.md` (REQ metadata table, row `LEARNINGS`), which the `docs/*/LEARNINGS-*.md` glob admits — so by the time this feature's own CI runs, the corpus is 6 and the un-consolidated set is 4, and AT-C1 is red on the PR that ships it. Two more delivered features make it 5, at which point `volume` fires and the `Then` inverts. An acceptance test must be stated over a **constructed corpus fixture** (n files, k named in the legacy region, threshold parameterised); the HEAD measurement belongs in prose, not in a test's Given. | §13.1 AT-C1, §2.3, §3.1 |
| F-02 | High | Local | **Step 8 is not implementable against the seam it reuses, so AT-M4 cannot be written.** §2.2 makes "Resolve the advisory model rung" a discrete step **8**, strictly after the consumed-pair append (7) and strictly before "Read the consumed LEARNINGS bodies" (9), and AT-M4's Given is "a pass that terminates `failed` at step 8". But `resolveAdvisoryRung` (`pdlc/workflows/orchestrate-dev.js:1833`) has signature `({ _agent, _log, _state, prompt })` and *dispatches* that prompt, falling back on a model-resolution error (`:1859`, `:1866`+) — resolution is observable only by performing a real dispatch. So step 8 is either (a) a probe dispatch of a throwaway prompt, or (b) fused with the first real dispatch in step 9 — in which case a `failed` pass **has** read bodies and O-C1's whole loss argument changes shape. The FSPEC names neither. A tester cannot construct the "terminates at step 8" fixture without knowing which. Name the step-8 dispatch (what prompt, what it costs) or restate the ordering claim in terms of the first advisory dispatch. | §2.2 step 8, §2.6, §13.3 AT-M4 |
| F-03 | High | Local | **The fallback-resolves branch — the one AC-1.6 exists to protect — has no acceptance test.** §2.6's table has three rows; row 3 (neither resolves) is AT-M4/S-11, row 1 is untested, and row 2 ("primary fails, fallback resolves; the pass surfaces `ADVISORY_MODEL_FALLBACK:` in its report; the fallback rung is named — never a silent downgrade") appears in **no** AT. §15.1 maps AC-1.5 to "AT-L5 (the `rung:` field)" and AC-1.6 to AT-M4; AT-L5 is a vocabulary set-equality check that asserts nothing about which rung ran, and AT-M4 is the halt path. The result: an implementation that downgrades silently, or records the primary rung while running on the fallback, passes every test in §13. This is a DC-05 breach on a named branch and needs its own AT with a positive oracle (report body contains the verbatim `ADVISORY_MODEL_FALLBACK:` line **and** `rung:` names the fallback). | §2.6, §13, §15.1 |
| F-04 | High | Local | **§8.5's row-3/row-4 choice — the decision AC-5.3 explicitly delegated to this layer — is both untested and non-deterministic.** AT-F10 tests row 2 (a spent `revise`), AT-F11 tests row 1 (a spent `retire`); **rows 3 and 4, the ordinary choice, have no AT at all**. Worse, row 3's condition is "the recurrence names the same `symptom` the promotion targeted", and §8.1 defines `symptom` as "one line the pass's own model writes under no vocabulary", explicitly **non-keying**, precisely because two passes word it differently. A rule whose branch predicate is a free-text match by an LLM is not a rule two runs must agree on — it contradicts §8.3's "no model judgment, so two runs over the same inputs cannot disagree" and makes the `revision`/`retirement` field unfalsifiable. Either state the predicate over a decidable observable (e.g. whether the recurring LEARNINGS' `failure-mode-id` line carries the same id **and** the promotion's `artifact` still exists) or state that the choice is a model judgment and drop the determinism claim — then give rows 3 and 4 ATs. | §8.5, §8.1, §8.3 |
| F-05 | High | Cross-Feature | **AT-Q7 is an absence-only oracle evaluated by source inspection, with no positive conjunct on the same path.** "Given any code path of the pass, When the source is inspected, Then no merge or enable-auto-merge API call exists." Nothing asserts what the pass **does** do to the PR it opened. A grep-shaped oracle also cannot see a merge issued through a generic seam (`_gh(["pr","merge",…])`, a shell string, an API URL built at runtime), so it passes on the exact implementation shape most likely to regress. AC-3.7/§6.5 is the feature's central safety property and deserves a runtime oracle: a spy over the git/PR seam asserting the observed call verb-set is **set-equal** to the expected open-PR set, plus the positive assertion that the PR is in state `open` (not `merged`) after the pass returns. AT-M5's second conjunct ("that path appears in no pathspec of any pass") is the same defect in miniature. | §13.5 AT-Q7, §13.3 AT-M5, §6.5 |
| F-06 | High | Local | **AT-L5's oracle range is ill-defined, so the test is either vacuous or false on a correct implementation.** §10.3 asserts "Each field is a member of a vocabularies §1 category; **no field carries a value with no §1 row**", and AT-L5 asserts set-equality in both directions against §1 at `Version` 1.4. But of §10.3's 13 fields, §1 has rows for only three (`pr:`, `suppressed-by:`, `credential:`) plus the status/reason/trigger/route/verdict/state/action/phase categories. `rung:` carries a model id — at HEAD the ladder's literals are `"fable"` and `"opus"` (`orchestrate-dev.js:1652-1653`) — and `branch:`, `pass:`, `date:`, `consumed:`, `promotions:`, `deferred:` carry free values. All are "values the row emits" with no §1 row, so the "no value without a row" direction is **false** for any correct implementation, and a tester will save the test by narrowing its range to the enumerated fields — at which point the direction that catches a deleted row must be restated explicitly or it is lost. State AT-L5's domain exactly: which fields' values are in the compared set, and which are free-form and excluded. (The missing `rung:` row is an upstream defect — see the erratum below.) | §10.3, §13.9 AT-L5, §15.2 |
| F-07 | High | Local | **AT-C5 cannot fail, and its Given is self-contradictory.** "Given a log whose most recent datum-status row is a `refused` row followed by a `promoted` row … Then the datum is the `promoted` row's date; the `refused` row is skipped." A `refused` row is by definition *not* a datum-status row (§2.3 lists the four), so the Given contradicts itself; and under the natural reading (refused, then promoted) the `promoted` row is the most recent row **regardless of whether the implementation filters on status at all** — an implementation that takes the last row unconditionally passes. To falsify BR-03 the fixture must place the `refused` row **after** the `promoted` row, and the Then must assert the datum equals the earlier `promoted` row's date. | §13.1 AT-C5, §2.3, §18.1 BR-03 |

| F-08 | Medium | Local | **AC-2.2's write target has no acceptance test.** §15.1 maps AC-2.1 and AC-2.2 to the *same single* row, AT-R2, whose Given is "a promotion targeting `docs/_constraints/DOMAIN-CONSTRAINTS.md`" — the AC-2.1 target. AC-2.2 (REQ `:212`) is a write to `docs/_decisions/DECISIONS-{topic}.md`, a **different path with a derived `{topic}` segment**, and no AT in §13 names it. The topic derivation is the interesting part (what is `{topic}`? is an existing file appended or a new one created? is the name stable across passes?) and §5.2/§5.4 never state it either, so the AT cannot be written today. Give AC-2.2 its own AT and state the `{topic}` derivation it asserts. | §15.1, §13.4 AT-R2, §5.2 |
| F-09 | Medium | Local | **NFR-3 is mapped to a test that cannot observe it.** §15.1 row `NFR-3` cites "AT-C2 (the bar is unchanged under a volume trigger)". AT-C2's entire Then is "the pass runs with trigger `volume`" — it asserts nothing about the pattern-vs-coincidence bar. REQ `:499` states NFR-3 as "pattern-vs-coincidence bar (AC-2.3) unchanged; running on cadence [does] not [lower] it", which is a *comparative* property: the same corpus must yield the same promotion set under a `cadence` trigger and a `volume` trigger. That is testable — and is the only shape that can fail — but no AT states it. The parenthetical in the mapping cell is the assertion the test is missing. | §15.1 NFR-3, §13.1 AT-C2 |
| F-10 | Medium | Local | **`promoted-degraded` is a terminal status no acceptance test asserts.** §12.1 S-07 is the only row producing it; §15.1 routes AC-3.5 to AT-Q6/AT-K2 and AC-4.3 to AT-K2/AT-K3. AT-Q6's Then names reason `branch-exists`; AT-K2's Then names `credential: absent`, reason `credential-unavailable`, "the promotion appears under the `degraded` route" and "the pass does **not** halt`; AT-K3 asserts terminal `no-op`. **No Then in §13 contains the token `promoted-degraded`.** So the partial-success status — the one that distinguishes "two landed, one failed" from both `promoted` and `no-op` — is carried only by AT-L5's vocabulary set-equality, which sees the string, not the behaviour. Add an AT whose Given is ≥2 promotions with exactly one §6.3 failure and whose Then asserts the status verbatim plus the landed promotions' observables. | §12.1 S-07, §13.5, §13.6, §15.1 |
| F-11 | Medium | Local | **§19's own totality claim is falsified by its AT column: six distinct input states are mapped to a test that constructs a different state.** §19 opens "no two distinct input states are silently collapsed into one", then maps E-01 (log **absent**) and E-02 (log present but **unreadable**) both to AT-P4, whose Given is "an absent log file"; E-03 and E-06 both to AT-P3; E-05 (a **closing** marker with no opener) and E-07 (a stray basename) both to AT-P2, whose Given is only the latter; and E-23 (`api-failure`) and E-24 (`branch-exists`) both to AT-Q6, whose Then names only `branch-exists`. Each pair differs in the branch it exercises, so the shared AT proves one arm and leaves the other unwritten — exactly the collapse §19 forbids. Either add the missing arms as ATs or drop the AT column's claim to cover them. | §19.1, §19.2, §13.2, §13.5 |
| F-12 | Medium | Local | **Two edge rows cite an AT that asserts nothing about the row's stated behaviour.** E-09 (basename collision — "the collision is **reported** in the AC-7.1 report, never silently resolved") cites AT-P1, whose Then is about `docs/completed/` inclusion and `docs/discarded/` exclusion and never reads the report. E-29 (the invoking branch carrying the log record is abandoned — "the merged PR is what survives") cites AT-Q3, which is duplicate suppression on an open PR and never abandons a branch. Both behaviours are stated as observable and both are untested; the citations make the DC-05 audit read green when it is not. | §19.1 E-09, §19.2 E-29 |
| F-13 | Medium | Cross-Feature | **Four parameterisable parsers, and §14.1 obliges no property strategy for any of them.** The feature specifies four components whose input space is parameterisable and whose invariants are statable: the two-region consumed predicate (§3.2 — invariant: a basename inside any `<!-- pdlc:consumed -->` block ⇒ consolidated, for any interleaving of openers/closers/strays), the `passId` derivation (§2.5 — invariant: minted id is strictly greater than every parseable id for `{today}` in the log, for any row multiset), the config parse (§11.2 — invariant: per-key independence, for any subset of keys corrupted), and the `ESCALATIONS.md` count (§9.2 — invariant: total attributed count ≤ entries with a `Feature` row). §13 covers all four with hand-picked examples only, and §14.1's obligation table (T-01…T-08) hands TSPEC eight mechanism choices, **none of which is a property strategy**. Per the project standard, example-only coverage of a parser is a Medium; add a T-row obliging ≥1 property per parameterisable component so TSPEC cannot silently ship examples. | §14.1, §13, §2.5, §3.2, §9.2, §11.2 |
| F-14 | Medium | Local | **`credential: absent` is overloaded across two states §19 says are never collapsed.** §12.1 S-09 (marker held and fresh) records the row as "one, `credential: absent`" for a pass that terminates at step 6 and therefore **never resolves a credential at all**; AT-K2 uses the same literal for "neither a credential variable nor `gh` auth" — a pass that *did* resolve and found nothing. AT-K5 then asserts "the row carries exactly one `credential:` value from the closed set", which both states satisfy, so no test can tell them apart, and an operator reading a `refused` row sees a credential finding that was never made. Either add a `not-attempted` member (a vocabularies change, hence an erratum) or state explicitly in §10.3 that `absent` on a `refused` row means "not attempted" and give that reading an AT. | §12.1 S-09, §13.6 AT-K2/AT-K5, §10.3 |

_(Low)_

| F-15 | Low | Local | **AT-C6 tests only the same-day increment.** Its Given is "a log already carrying a row with `passId` `{today}-1`". §2.5's derivation is per-date, so the interesting boundary — a log whose newest rows are all from a **previous** date, where the counter must restart at `-1` rather than continue — is untested, and E-10's "unparseable row contributes no `m`" is tested only through the same single-row fixture. Add the date-rollover Given. | §13.1 AT-C6, §2.5 |
| F-16 | Low | Local | **AT-P7's parity assertion has no stated oracle, and T-08 permits the shape that makes it hardest.** T-08 leaves open whether the hook and the pass share code or are "two implementations held equal by test", but AT-P7's Then ("its predicate is the §3.2 two-region test and its glob includes `docs/completed/*/`") reads like a source inspection — verified at HEAD, the hook's predicate is a whole-file containment at `pdlc/hooks/scripts/nudge-consolidation.sh:41` (`os.path.basename(p) not in logtext`) with the glob at `:28`, i.e. a Python heredoc inside bash, which no JS test imports. If T-08 picks two implementations, AT-P7 must be a **differential** test over a shared corpus-and-log fixture table, asserting the two un-consolidated sets are set-equal. Say which. | §13.2 AT-P7, §14.1 T-08 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | §14.4 states this FSPEC records no erratum against the vocabularies file at `Version` 1.4. But §10.3's terminal row carries a `rung:` field and §15.1 routes AC-1.5 to "AT-L5 (the `rung:` field)", while `docs/_constraints/pdlc-consolidation-vocabularies.md` §1 — verified at HEAD, `Version` 1.4 (`:7`) — has rows for `pr:`, `suppressed-by:` (`:63`) and `credential:` (`:64`) and **none** for `rung:`. AT-L5 as written is therefore red on a correct implementation. §15.3 says this feature *owns* that file, so is the fix a row addition inside this feature's scope (in which case §14.4 stands and §15.2 needs the row), or an erratum against the REQ §4b pin? An erratum is filed below on the conservative reading; say which. |
| Q-02 | AT-C3's Then includes "no `passId` minted" and "no git call made". Both are absence assertions on a path whose only positive observable is that nothing happened at all — the terminal `skipped-cadence` status is not written anywhere (S-01: no log row). What **is** the positive artefact a test reads to know the tick ran and chose this branch rather than crashing before the decision? If it is the returned report body, say so in §10.1; if a `skipped-cadence` tick returns nothing, the AT needs a call-count oracle over the git/IO seams plus a positive assertion on the returned value. |
| Q-03 | §12.1 S-03's status cell is "`promoted` / `no-op` per outcome" and AT-C1's Then asserts trigger `cadence` and reason `no-cadence-datum` but no status. For a bootstrap tick on an empty corpus (E-08 ∧ E-01 together — the true first-run state), which of the two is it? The two edge rows are individually specified but their conjunction is the state every consuming repo starts in. |
| Q-04 | §8.3's effectiveness table is "one row per distinct `failure-mode-id`" over *all prior* promotions, so it grows without bound across passes. AT-F5 fixes N and asserts exactly one row per distinct id. Is there an upper bound, a retirement rule that removes retired ids from the table, or is unbounded growth accepted? A test for the retirement case cannot be written without the answer. |

## Positive Observations

- **§12 → §13 is a genuine coverage contract, not a formality.** §12.1 enumerates fourteen terminal
  scenarios with the marker/log-row/consumed-pair/commit columns filled per row, §12.4 states seven
  cross-row invariants, and §13 is organised to range over them. Most of the findings above were
  findable *because* the document made its own coverage claim checkable — that is the right failure
  mode for a spec to have.
- **The oracle discipline in §13 is unusually strong for a first draft.** AT-R1 and AT-Q2 and AT-L5
  demand **set-equality** rather than containment in so many words; AT-K4 explicitly refuses to
  collapse two fields; AT-F14 asserts a field is "**absent**, not empty-valued"; AT-R5 asserts "no
  failure **and** no `writes-uncommitted`" rather than a bare non-failure. These are the shapes that
  survive a deleted case.
- **DC-01's three arms are actually enumerated per input, not asserted in the abstract.** §19.1's
  E-01…E-15 give the log, the marker, `ESCALATIONS.md` and the config an absent, malformed and
  truncated row each, and E-04's "a partially flushed pair never *loses* consumption" states the
  fail-direction rather than leaving it to the implementer.
- **The accepted losses are stated as losses.** §14.2 O-C1…O-C5 and §19.3 E-32/E-33 name what is
  given up, why the repair would breach an upstream obligation, and where the deferral lives. A spec
  that says "accepted, not repaired" is testable; one that omits the case is not.
- **§15.1's both-directions claim held under audit.** Every REQ criterion `AC-1.1`…`AC-7.2`,
  `NFR-1`…`NFR-5` and `NFR-3a` appears exactly once, and no row names a criterion the REQ does not
  carry. The defects found (F-08, F-09) are in what the cited ATs *assert*, never in the mapping's
  completeness — the coarser property is sound.

## Recommendation

**Needs revision**

Seven High and seven Medium findings are open, so the Challenger bar forbids approval. The document is
substantively strong — the coverage architecture of §12/§13/§15/§19 is better than most TSPECs — and
the revision is bounded. What must change:

1. **F-01** — restate AT-C1 over a constructed corpus fixture parameterised on (n files, k in the
   legacy region, threshold). As written it is falsified by this feature's own Phase H output.
2. **F-02** — name what step 8 dispatches, or restate the ordering claim over the first advisory
   dispatch. AT-M4's fixture is unbuildable until then, and O-C1's loss argument depends on it.
3. **F-03, F-10** — give the fallback-resolves branch (§2.6 row 2) and the `promoted-degraded`
   terminal status their own ATs with positive oracles.
4. **F-04** — make §8.5's row-3/row-4 predicate decidable, or drop §8.3's determinism claim; then
   give rows 3 and 4 ATs.
5. **F-05** — replace AT-Q7's source-inspection oracle with a runtime seam spy asserting the observed
   call verb-set is set-equal to the expected open-PR set, plus a positive `open` state assertion.
   Same for AT-M5's second conjunct.
6. **F-06, Q-01** — state AT-L5's exact domain (enumerated fields in, free-form fields out) and
   resolve the missing `rung:` row.
7. **F-07** — reorder AT-C5's fixture so the `refused` row is *after* the `promoted` row, and fix the
   self-contradictory Given.
8. **F-08, F-09, F-11, F-12** — add the missing ATs (AC-2.2's `DECISIONS-{topic}.md` write with its
   `{topic}` derivation; NFR-3's comparative bar; the unreadable-log, dangling-closer and
   `api-failure` arms) and repair the two §19 rows whose cited AT asserts nothing about the row.
9. **F-13** — add a §14.1 obligation requiring ≥1 property strategy per parameterisable component, so
   the four parsers cannot reach implementation on examples alone.
10. **F-14** — disambiguate `credential: absent` on a `refused` row, in §10.3 and in an AT.

F-15 and F-16 are Low and may be folded into the same revision or deferred to TSPEC.
