# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-consolidation-agent/REQ-pdlc-consolidation-agent.md`
**Date:** 2026-08-05
**Iteration:** 1
**Scope:** Local (with two Cross-Feature findings, tagged inline)

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **AC-3.7's guard inheritance describes no code path, and states no oracle.** `guardVerdict` has exactly two call sites at HEAD — Phase MERGE's decision ladder (`pdlc/workflows/orchestrate-dev.js:900`, via `effectiveGuardPaths` at `:899`) and the advisory-envelope check (`:2143`). Both run *inside* an `orchestrate-dev` pipeline over the branch that run owns; nothing in the repo evaluates a pull request raised against a **different** repository. `pdlc-merge-phase` REQ-MERGE-03 AC-3.7 (`docs/completed/pdlc-merge-phase/REQ-pdlc-merge-phase.md:190-199`) is explicitly about *this repo's own queue PRs*, and Phase MERGE ships `mergeMode: "off"` (`orchestrate-dev.js:61`, refusal at `:838`), so even the local guard is unreachable by default. There is therefore no observable state a test can drive or assert for "inherits the guard": no status value, no escalation, no artifact. Restate AC-3.7 as a property of *this* feature with a named observable (e.g. the credential AC-4.1 holds no merge scope, the agent never sets auto-merge, and the PR carries a named label/marker), or delete it. | REQ-CONS-03 AC-3.7 |
| F-02 | High | Local | **The trigger predicate is defined two incompatible ways at HEAD, and the hook AC-1.2 names cannot trigger anything.** `hooks/scripts/nudge-consolidation.sh` computes "un-consolidated" as *basename not appearing as a substring of `docs/_decisions/.consolidation-log.md`* (`pending = [p for p in learnings if os.path.basename(p) not in logtext]`, THRESHOLD = 5), while `pdlc/skills/consolidate-learnings/SKILL.md:35` defines the pass boundary as *`Date Completed` after the last logged pass*, and AC-1.4 introduces a third phrasing ("no LEARNINGS accumulated since the last logged pass"). These disagree on a LEARNINGS file that is dated after the last pass but named in an earlier log line, and vice versa — so no fixture can be written for AC-1.2 or AC-1.4 without picking one. Separately, the hook only prints `hookSpecificOutput.additionalContext` and exits 0 ("Never blocks (always exit 0)", script header); it has no capability to start a pass, so "that threshold also triggers a pass" asserts a behavior of a component that structurally cannot have it. Name the single predicate, and name the component that does the triggering. | REQ-CONS-01 AC-1.2, AC-1.4 |
| F-03 | High | Local | **AC-1.5's advisory rung is unobservable, and its known fallback branch is uncovered (DC-05).** `MODEL_ADVISORY = "fable"` is a module-local `const` in `orchestrate-dev.js:1652`, not exported and not reachable from any other workflow; the only rung-resolution path is `resolveAdvisoryModel`'s two-step dispatch (`:1851-1870`), which on non-resolution substitutes `MODEL_ADVISORY_FALLBACK` (`:1653`) and emits `ADVISORY_MODEL_FALLBACK: …` (`:1859`), and on double failure produces **no agent output at all** (`:1869`). `pdlc/workflows/` contains only `orchestrate-dev.js`, `orchestrate-queue.js` and `runtime-adapter.js` — there is no consolidation workflow, so the REQ does not say where the pass runs or how the rung is observed. AC-1.5 states only the happy rung: a pass that silently ran on the Opus fallback is neither a required pass nor a required failure, and a pass whose rung failed to resolve entirely has no stated outcome. Add ACs for the fallback (recorded/reported, per `pdlc-advisory-tier`'s "never a silent downgrade") and for total non-resolution. | REQ-CONS-01 AC-1.5 |
| F-04 | High | Local | **AC-1.3's refusal is an absence-only requirement — no status, no reason, no fate for the refused pass.** "Then the second is refused" gives a test nothing to assert except "the second did not run", which passes for any accidental non-run (crash, missing config, empty LEARNINGS set). The repo's own comparable gate does better: the queue's drift gate returns `outcome: "blocked", reason: "Drift gate row N: …"` (CLAUDE.md, §queue) and `orchestrate-queue.js` treats `in-progress` as the active-run marker via `QUEUE_STATUSES`/`selectNextPending`. Per `docs/_decisions/DECISIONS-test-oracle-mechanics.md`, a blocked invariant needs three positive conjuncts: exact status value, named reason code, and the retention/audit assertion. Also unstated: is the refused pass **dropped** or **deferred to the next cadence tick**, and what is the mutual-exclusion artifact (lock file? log row? `in-progress` status?) whose presence a test can plant. | REQ-CONS-01 AC-1.3 |
| F-05 | High | Cross-Feature | **AC-6.1's named inputs do not survive into LEARNINGS in machine-readable form, and AC-6.2 names the wrong source.** The advisory record has a strict schema — five fields per entry (`Seam`, `Confidence`, `Envelope`, `Disposition`, `Model`) rendered by `renderAdvisoryEntry` (`orchestrate-dev.js:2644-2675`) — and `advisorySummaryRows` (`:2690+`) emits exactly five per-seam rows driven by `ADVISORY_SEAMS`. But Phase H2's distil dispatch asks an agent to "**Append a summary of its entries** to … LEARNINGS" with **no schema at all** (`advisoryDistilPrompt`, `:7586-7594`), and the pipeline then deletes `ADVISORY-{feature}.md` through the guarded channel (`:10499`). So the structured per-seam counts AC-6.1 wants to consume are destroyed and replaced by free prose — "invocations, resolutions, escalations, by seam" is not a parseable field set in any LEARNINGS file, and AC-6.2's cross-feature comparison ("escalates disproportionately") has no numeric input. Meanwhile the one durable, non-distilled, non-deleted per-seam escalation record — `docs/_queue/ESCALATIONS.md` (`ESCALATIONS_PATH`, `:2750`; per CLAUDE.md "never distilled and never deleted") — is not mentioned anywhere in this REQ. Either name the schema the distil step must emit (and file it as an erratum-shaped dependency on `pdlc-advisory-tier`) or state `ESCALATIONS.md` as the input. | REQ-CONS-06 AC-6.1, AC-6.2 |
| F-06 | High | Local | **NFR-4's idempotence has no key, and AC-3.5's `{date}` filename collides under the dual trigger AC-1.2 mandates.** "No duplicate promotions and no duplicate PR" names no identity: is a duplicate PR detected by head-branch name, by title, by a marker in the body, or by the log URL from AC-3.4? Without that, the negative test ("second run opens no second PR") is unfalsifiable — it also passes when the second run opened nothing because it crashed. Worse, AC-1.2 makes **two passes on the same calendar day** an expected case (cadence tick plus threshold crossing), yet both AC-3.5 and AC-3.4 identify the fallback artifact solely as `CONSOLIDATION-PROPOSAL-{date}.md`, matching today's skill (`consolidate-learnings/SKILL.md:49`). Two same-day passes therefore either overwrite one another's proposals or append ambiguously, and no AC says which. Give the artifact a disambiguator and give NFR-4 a stated dedup key. | NFR-4, AC-3.4, AC-3.5, AC-1.2 |
| F-07 | Medium | Local | **AC-5.2's tri-state classifier has no decision rule and no completeness obligation.** `prevented` / `recurred` / `insufficient-evidence` is an enumerated output over a parameterisable input space (the set of LEARNINGS consumed since, × the recorded failure mode) — i.e. a classifier, which by project standard needs at least one property strategy, not examples. Nothing states what distinguishes `prevented` (the failure mode was *observably absent from a population where it could have appeared*) from `insufficient-evidence` (no LEARNINGS in scope could have exhibited it), so two implementations can disagree on every input and both satisfy the AC. Per the enumerated-contract rule, the pass's effectiveness table also needs a **set-equality** obligation over the full prior-promotion set — a promotion silently dropped from the table must fail, which a containment check would not catch. | REQ-CONS-05 AC-5.2 |
| F-08 | Medium | Local | **AC-5.3's "two consecutive passes" is undefined across the pass classes the REQ itself introduces.** AC-1.4 defines a `no-op` pass, and AC-5.2 can return `insufficient-evidence`. Does a `no-op` pass count as one of the two consecutive passes? Does an `insufficient-evidence` verdict break the `recurred` streak, or leave it intact? Under one reading a promotion is flagged `ineffective` after two real recurrences; under another, two quiet weeks reset the counter forever. The boundary decides whether a test's third-pass fixture expects `ineffective` or not, so it must be stated. | REQ-CONS-05 AC-5.3 |
| F-09 | Medium | Local | **Every "configured" value in this REQ is unnamed, undefaulted, and has no malformed-value behavior.** Four appear: the cadence (AC-1.1), the plugin repository (AC-3.1, AC-4.1), the secret store (AC-4.2), and the pass-count horizon (AC-5.5). The repo has an established surface — `.claude/pdlc.config.json`, whose `parseAdvisoryConfig` demonstrates the expected contract shape: per-key independent fallback with a stated default, so one bad key never retunes the rest (CLAUDE.md §Advisory tier). DC-01 requires a contract crossing a component boundary to be closed and total. Without key names, defaults and malformed-value outcomes there is no config fixture to write and no negative test to write against it. | AC-1.1, AC-3.1, AC-4.1, AC-4.2, AC-5.5 |
| F-10 | Medium | Cross-Feature | **Three absence-only requirements carry no positive conjunct.** AC-4.2/NFR-2 ("never logged, never written into a PR body, never persisted") and NFR-5 ("never modifies a LEARNINGS file it consumed") can only be tested as "X is not present" / "bytes unchanged". `docs/_decisions/DECISIONS-test-oracle-mechanics.md` rejects preservation/byte-identity oracles lacking a positive-presence conjunct, and DC-03 requires every load-bearing assertion to be falsified before it is trusted: a grep-for-the-token test passes trivially when the code path never ran at all. State what **is** written on the same path — e.g. the credential's *presence* is recorded as a fixed redaction literal in the log row, and the consumed-LEARNINGS assertion is paired with a positive "the log names exactly these consumed files" conjunct on the same run. | AC-4.2, NFR-2, NFR-5 |
| F-11 | Medium | Local | **AC-3.5's "for any reason" collapses distinct failure classes into one untestable catch-all.** At minimum these are separable and behave differently: credential absent/invalid (already carved out by AC-4.3, which adds "records the credential failure" — a *different* obligation from AC-3.5's), network/API failure, plugin repo not found or renamed, head branch already exists from a prior pass (see F-06), and rate limiting. DC-05 requires every named behavioural branch to have its own acceptance test; as written, one fixture (`gh` unavailable) discharges the entire clause. Enumerate the classes and state, per class, whether the fallback fires and what the record says. AC-4.3's "does not halt the whole pass, and does not silently skip the promotion" also needs its positive conjunct — what the reporting surface (AC-7.1) shows for a degraded promotion. | AC-3.5, AC-4.3 |
| F-12 | Low | Local | **Two artifacts are cited without their directory, and the REQ's paths are the only spec a test fixture has.** AC-2.4 and AC-3.4 say `.consolidation-log.md`; the file is `docs/_decisions/.consolidation-log.md` (`consolidate-learnings/SKILL.md:35`, `nudge-consolidation.sh`). AC-3.4/AC-3.5 say `CONSOLIDATION-PROPOSAL-{date}.md`; it is written under `docs/_decisions/` (`SKILL.md:49`). §1 gives the full path once, so this is inconsistency inside one document rather than an unknown. | AC-2.4, AC-3.4, AC-3.5 |
| F-13 | Low | Local | **§1 understates the existing proposal table by one column.** The REQ describes it as "a markdown table with columns 'Target skill / Proposed change / Rationale'"; the shipped template is four columns — `Source LEARNINGS \| Target skill \| Proposed change \| Rationale` (`pdlc/skills/consolidate-learnings/SKILL.md:54`). Since AC-3.5 preserves this artifact as the fallback path, a fixture built from the REQ's description would be structurally wrong. | §1, AC-3.5 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Where does an unattended pass actually execute? D-CONS-04 defers the cloud-Routine vehicle, and `pdlc/workflows/` holds only `orchestrate-dev.js`, `orchestrate-queue.js` and `runtime-adapter.js`. If the vehicle is a new workflow script, its inputs/report shape are the testable surface and belong in the REQ; if it is the SessionStart hook, F-02 applies. |
| Q-02 | AC-3.3 lets several promotions share one PR with one commit each. What is the test's oracle for "revertible independently" — one commit per promotion by count, or a stated commit-message/trailer convention that maps a commit to a promotion id? Commit count alone is satisfied by an unrelated chore commit. |
| Q-03 | AC-5.1 requires the failure mode to be "stated concretely enough to be observed: which phase, which symptom, which artifact". Is that a **structured** record (three named fields, machine-checkable) or prose? AC-5.2's recurrence check is only mechanisable against the former. |
| Q-04 | AC-5.4 routes a *retirement* through AC-3.1's PR path — but AC-3.1 is scoped to promotions targeting `pdlc/skills/**` or `pdlc/workflows/**`. Retiring a promotion that landed in `docs/_constraints/DOMAIN-CONSTRAINTS.md` (AC-2.1, consuming repo) is not a cross-repo edit at all. Which path does that retirement take, and who reviews it? |
| Q-05 | NFR-3 says cadence must not lower the promotion bar. What observable distinguishes a cadence-triggered pass from a threshold-triggered one in the log, so a test can assert the bar held on both? |
| Q-06 | AC-7.2's "single notification" — through which channel, and is its content asserted (PR URL present when and only when a PR was opened) or only its existence? |

## Positive Observations

- The REQ's own diagnosis is the strongest thing in it: §1 separates *propose-only* from *hand-transcribed* precisely, and that distinction is exactly what makes AC-3.1 testable in principle (a PR either contains a diff or it does not).
- AC-3.5 is the right shape for a degradation path — it names today's behavior as the fallback, so the negative case has a concrete positive outcome rather than "the promotion is lost".
- AC-5.3's `ineffective` flag plus AC-5.4's symmetric retirement path is a genuinely falsifiable improvement loop: it gives the effectiveness table a consequence, which is what stops it from being decoration. D-CONS-03 correctly refuses the age-based proxy for it.
- Upstream citations check out against the cited files: `DEC-E2` (`docs/design/MASTER-PLAN-engineering-loop.md:195`), `DEC-E4` (`:216`), `DEC-E5` (`:228`), `OQ-E3` (`:304`), and `pdlc-merge-phase` REQ-MERGE-03 (`docs/completed/pdlc-merge-phase/REQ-pdlc-merge-phase.md:163`) all exist and say what the REQ says they say. The nonexistent-authority failure mode this checklist watches for did not recur here — only F-01's *scope* reading of REQ-MERGE-03 is wrong, not its existence.
- The AC-1.2 threshold claim is accurate to the code: `THRESHOLD = 5` in `pdlc/hooks/scripts/nudge-consolidation.sh`. The defect in F-02 is the predicate behind the number, not the number.
- §5's out-of-scope list is unusually disciplined — "retiring the manual `/pdlc:consolidate-learnings` entry point" being explicitly out of scope preserves the one entry point every test fixture can drive deterministically.

## Recommendation

**Needs revision** — 6 High, 5 Medium.

What must change before this REQ is testable:

1. **F-01** — restate or delete AC-3.7. The guard it claims to inherit runs only inside a local
   `orchestrate-dev` Phase MERGE (`orchestrate-dev.js:900`) over that run's own PR, and ships
   `mergeMode: "off"` (`:61`). Give the "never auto-merged" claim an observable that belongs to
   *this* feature.
2. **F-02** — pick one definition of "un-consolidated" (the hook's substring test or the skill's
   date boundary) and name the component that actually starts a pass; the hook cannot.
3. **F-03** — state how the advisory rung is observed, and add the fallback and
   total-non-resolution branches as their own ACs.
4. **F-04** — give AC-1.3 an exact status value, a named reason code, the mutual-exclusion
   artifact, and the fate of the refused pass.
5. **F-05** — name the machine-readable per-seam input AC-6.1/AC-6.2 consume, or point them at
   `docs/_queue/ESCALATIONS.md`; free prose appended by `advisoryDistilPrompt` cannot serve.
6. **F-06** — give NFR-4 a dedup key and give the proposal artifact a same-day disambiguator.
7. **F-07/F-08** — state the tri-state decision rule, its set-equality obligation over the full
   prior-promotion set, and how `no-op` / `insufficient-evidence` passes affect the consecutive
   count.
8. **F-09/F-10/F-11** — name every config key with its default and malformed-value behavior; pair
   each absence-only requirement with a positive conjunct on the same path; enumerate AC-3.5's
   failure classes.

F-12 and F-13 are corrections, not blockers.

No upstream defects were found: every `MASTER-PLAN` and `pdlc-merge-phase` citation resolves to a
real authority saying what the REQ attributes to it. No ERRATUM lines are emitted.

## Verdict

VERDICT: Needs revision
