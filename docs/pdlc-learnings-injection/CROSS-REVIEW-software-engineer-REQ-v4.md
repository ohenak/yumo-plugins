# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-learnings-injection/REQ-pdlc-learnings-injection.md`
**Date:** 2026-08-19
**Iteration:** 4

**Delta scope.** Reviewed `git diff 04ba6aac..218debf3` on the REQ only (one commit: 218debf3,
+45/−34). v3's F-01…F-05 checked for resolution; unchanged sections not re-litigated.

## Prior-round disposition

| v3 finding | Severity | Status |
|---|---|---|
| F-01 — "same JS bundle" claim false; `consolidate-learnings.js` unreachable from the shipped engine | High | **Resolved, and verified against HEAD.** All three sites now say restate-and-pin, not import. `MODULE_NAMES = ["orchestrate-dev.js", "orchestrate-queue.js"]` is exactly `pdlc/engine/scripts/prepack.mjs:20` as cited. `LS_FILES_ARGV` is a frozen literal at `pdlc/workflows/consolidate-learnings.js:1338-1346`, consumed by `enumerateCorpus` at `:1350`. The proposed evidence form already ships: `pdlc/workflows/__tests__/consolidationPredicate.test.js:23` transcribes the argv literally and asserts `git.calls[0]` equals it (`:43`) — so O-7's pin costs no packaging change, exactly as claimed. DEC-CONS-05's own text backs "one predicate, two enumerations, enumerations pinned not equal" (`docs/completed/pdlc-consolidation-agent/DECISIONS-pdlc-consolidation-agent.md:431-439`). |
| F-02 — AC-5.1a self-compares; no baseline recorded anywhere | Medium | **Resolved.** AC-6.2 now names the baseline once — a composition captured at pre-feature HEAD, committed, never regenerated by the branch under test — and AC-5.1a points at that committed fixture rather than a second branch of the same run. The oracle no longer closes on itself. |
| F-03 — AC-1.2 delegates to AC-6.1's fixtures, which did not cover it | Low | **Resolved.** AC-6.1 now enumerates "AC-1.2's dispatch-set equality and non-authoring byte-identity" explicitly. |
| F-04 — `RSN-TRUNCATED` gloss collided with AC-2.3's bounding | Low | **Resolved.** Re-glossed as source-side truncation with the explicit contrast to AC-2.3. |
| F-05 — AC-4.1 pointed at a per-document id for a corpus-level state | Low | **Resolved.** AC-4.1 now records the corpus-level `RSN-EMPTY` outcome and cites AC-3.2. |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | Medium | Local | **`delta` `local` — "`docs/discarded/` excluded by pathspec" describes a mechanism the shipped predicate does not have, and an implementer who honours it literally turns the pin red.** §1.2 claim 2 and C-3 both state the exclusion as an active property of the enumeration. At HEAD there is no exclusion pathspec: `LS_FILES_ARGV` (`pdlc/workflows/consolidate-learnings.js:1338-1346`) carries two *include* globs only, and `:(glob)docs/*/LEARNINGS-*.md` does not cross `/`, so `docs/discarded/{feature}/LEARNINGS-*.md` (the real layout — `docs/discarded/` holds feature directories) is simply never reached. The outcome the REQ wants is correct; the stated cause is not. Risk is concrete because O-7 pins the restatement to the shipped argv *literally*: an implementer who adds `:(exclude)docs/discarded/**` to satisfy this sentence produces a restatement that fails the very pin the REQ mandates. Reword both sites to "not reached by either pathspec (discarded features live one level deeper, `docs/discarded/{feature}/`)". | §1.2 claim 2, C-3, O-7 |
| F-02 | Low | Local | **`delta` `local` — AC-5.1a quantifies over *every* dispatch; AC-6.2 supplies *a* composition.** AC-5.1a: "every composed dispatch is byte-identical to AC-6.2's recorded baseline"; AC-6.2 records "a fixture composition captured from the pre-feature HEAD" — singular. Since C-1's authoring set spans several roles and phases, the baseline has to be a fixture *set*, one member per authoring dispatch kind in scope, or AC-5.1a's quantifier exceeds its oracle and a test engineer must guess the cardinality. One word fixes it: "a fixture composition **per authoring dispatch kind in C-1's set**". | AC-5.1a, AC-6.2 |
| F-03 | Low | Local | **`delta` `local` — corpus-level outcomes are "recorded once per run", but selection runs per dispatch.** AC-3.2 fixes corpus-level `RSN-UNLISTABLE`/`RSN-EMPTY` at one record per run, while AC-3.1 records per authoring dispatch. Nothing states why one record suffices when selection is re-evaluated at each dispatch — a transient listing failure at dispatch 3 has no stated home. The answer is probably "the corpus is stable within a run because the run's own LEARNINGS is self-excluded (C-2)", but a listing failure is not a corpus-content change. Either say the corpus-level outcome is evaluated at each selection and recorded per dispatch, or state the stability argument and say the first differing evaluation wins. | AC-3.2, AC-3.1, AC-4.2 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | AC-6.2's baseline is "committed and never regenerated by the branch under test". After this feature ships, any *later* legitimate change to dispatch composition makes the frozen fixture red for reasons unrelated to injection. Is the intended lifecycle "regenerate deliberately, in a commit that says so" (fine, and worth one clause) or "frozen forever" (which will produce a confusing red for the next feature that touches composition)? |
| Q-02 | v3's Q-02 stands unanswered: C-1's rule admits the erratum land-proof retry dispatch (`pdlc/workflows/orchestrate-dev.js:12915`, tagged `dispatchKind: "authoring"`). Rule-over-tag says it is in scope and AC-1.2's set equality will pick it up automatically; one clause in C-1 would stop a test engineer counting sites by hand and reading it as a leak. |

## Positive Observations

- The F-01 fix is the right kind of fix: rather than defending the packaging claim, the REQ replaced it with the cheaper mechanism that was available all along, and cited the file and line that proves it. Every citation added this round checks out against HEAD — `prepack.mjs:20`, `LS_FILES_ARGV`, DEC-CONS-05's "pinned, not equal" — and the precedent test the fix depends on already exists (`consolidationPredicate.test.js:23,43`), so O-7 is now costed, not hoped.
- AC-6.2's baseline is named exactly once and everything else points at it. That is what F-02 asked for and it removes the last self-closing oracle in Group 5/6.
- C-3 now reads as a definition with a provenance note rather than a dependency claim, which is what the altitude wants: the REQ states the corpus, the TSPEC states the pin's form.

## Recommendation

**Approved with minor changes**

No High findings remain. F-01 is Medium because it can steer an implementer into a red pin, but it is a two-clause wording fix in the same two places, and it does not block FSPEC authoring — fold it into the next edit rather than opening a round for it. F-02 and F-03 are one clause each.

## Verdict

VERDICT: Approved with minor changes
{"high": 0, "medium": 1, "low": 2}

APPROVAL-HASH: sha256:0110298fd9f864a67213a3aa816da70c6295de63d93e68915dfed89ab832cedb
APPROVAL-HASH-NORMALIZED: sha256:5f47f71fca53a6c4d3e2614be631b1a9cdfc19744f2065402c258eedc6971ea7
REVIEWED-COMMIT: 218debf3cc6541c320242c11c1d8f6433a324084
