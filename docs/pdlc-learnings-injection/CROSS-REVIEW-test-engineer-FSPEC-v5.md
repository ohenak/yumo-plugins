# Cross-Review: test-engineer — FSPEC

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-learnings-injection/FSPEC-pdlc-learnings-injection.md`
**Date:** 2026-08-19
**Iteration:** 5
**Scope:** delta re-review of `f005e6ed..HEAD` under decision freeze; testing lens only. The FSPEC delta is three lines; the material event of this round is that the **upstream REQ moved underneath it** (v0.4 → v0.6, erratum rounds `1bac1663..bc603aa0`). Sections unchanged since v4 are not re-litigated except where a REQ change falsifies a claim they make.

## Disposition of v4 findings

| ID | Severity | Status | Evidence in the revision |
|---|---|---|---|
| F-01 | Medium | **Open, carried** | AT-15's first clause still reads "*Given* a fixture whose only LEARNINGS documents lie under `docs/discarded/`" (FSPEC:800-802) and E-07 still reads "Corpus contains only documents under `docs/discarded/`" (FSPEC:663), while E-35's direct-path case sits on the same AT (FSPEC:664). The fixture-defining sentence still describes E-35's fixture verbatim. Not touched by the delta; re-filed below as F-02, non-gating. |
| F-02 | Low | **Open, carried** | AT-20's disjointness conjunct still reads "neither catalogue's ids appear in the other's position" (FSPEC:822-823) — two catalogues, while BR-9 binds three. Re-filed below as F-03, non-gating. |

Nothing approved in v4 was broken by the FSPEC delta itself. The delta is correct as far as it goes: `AC-5.1c` is now carried in the FSPEC-LRN-15 traceability row (FSPEC:90), given its own coverage row `AC-5.1c | BR-14 | AT-32` (FSPEC:118), and added to BR-14's heading (FSPEC:585). That matches the REQ's new split of AC-5.1b (malformed section) from AC-5.1c (wrong-typed declared key) at REQ:368-376, and AT-32 does carry both conjuncts, so the new row is not a promissory mapping.

The problem is what the delta stopped short of. The same REQ erratum cycle that created AC-5.1c also landed **every other divergence the FSPEC declares an erratum against**, and none of those declarations was retracted.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **All four `ERRATUM: REQ` routes the FSPEC declares are now false against the REQ at HEAD; each states, as fact, a divergence the REQ v0.5/v0.6 erratum cycle already closed.** The FSPEC carries four live erratum declarations, and every one asserts the REQ says something it no longer says. (1) FSPEC:300 — "REQ AC-3.2's catalogue lists `RSN-TRUNCATED` and omits `RSN-NO-MATERIAL`; BR-9 differs on both — `ERRATUM: REQ` rides." REQ AC-3.2 now enumerates `RSN-NO-MATERIAL` outright and rules truncation out in the same breath: "`RSN-NO-MATERIAL` (eligible, carrying none of the material the bounding rule takes). Truncation is **not** a member: a truncated file is either still a LEARNINGS document, so eligible, or `RSN-UNPARSEABLE`" (REQ:318-320). BR-9 and AC-3.2 now agree member for member. (2) FSPEC:272 — the direct `docs/discarded/LEARNINGS-x.md` class, "None exists at HEAD; `ERRATUM: REQ` rides." REQ AC-2.6 now legislates exactly the FSPEC's outcome: "*and given* one directly at `docs/discarded/LEARNINGS-*.md`, it is a corpus member on ordinary terms" (REQ:302-303), with C-3 adding "`docs/discarded/` gets **no exclusion rule of its own** (AC-2.6)" (REQ:171-172). (3) FSPEC:366-369 — "REQ AC-2.1 still asserts the count *equals* the threshold for any corpus above it, which the measurement above falsifies … `ERRATUM: REQ` rides this round." REQ AC-2.1 now says the opposite: "It reaches that cap only where the byte bounds (AC-2.3, AC-2.4) do not bind first; under §4.1's values they bind on measured corpora, so equality above it is **not** claimed" (REQ:273-275) — the FSPEC's own measurement, adopted. (4) FSPEC:606-607 — "REQ AC-5.1b's example makes that typo the detectable case and no longer holds — `ERRATUM: REQ` rides." REQ AC-5.1b now carries the FSPEC's decision verbatim: "A **misspelt section name** reads as absent and is AC-5.1a's state; no unknown-key registry is required" (REQ:371-372). This is load-bearing rather than cosmetic on two counts. Mechanically, `ERRATUM: {DOCTYPE}` is a routed instruction, not commentary: leaving four of them standing asks the orchestrator to open erratum rounds against a REQ that has already made every correction, which is a phantom round on a frozen document. For the testing lens specifically, each declaration is a *factual claim about upstream text* that a traceability test would assert — a reviewer or a spec-consistency check reading "AC-3.2 lists `RSN-TRUNCATED`" against REQ:314-320 gets a contradiction, and the FSPEC's REQ-divergence claims are precisely the class TSPEC authors trust when deciding which side to pin a test to. Fix: drop all four `ERRATUM: REQ` clauses and restate each as agreement — BR-3's catalogue matches AC-3.2, BR-2's direct-path outcome matches AC-2.6, BR-5's count-bound note matches AC-2.1, BR-14's misspelt-section rule matches AC-5.1b — leaving the substantive prose (the measurement at FSPEC:360-366, the glob reasoning at FSPEC:262-271) untouched, since the REQ now agrees with all of it. | BR-2 (FSPEC:272), BR-3 (FSPEC:300), BR-5 (FSPEC:369), BR-14 (FSPEC:607) vs REQ AC-2.1, AC-2.6, AC-3.2, AC-5.1b, C-3 |
| F-02 | Medium | Local | **Carried from v4 F-01, unchanged: AT-15's fixture sentence still describes E-35's case, so one clause of the AT can falsify the other.** AT-15's first clause is "*Given* a fixture whose only LEARNINGS documents lie under `docs/discarded/`, *then* nothing is selected, the report carries corpus-level `RSN-EMPTY`" (FSPEC:800-802); its second is "*And given* a one-file fixture holding exactly `docs/discarded/LEARNINGS-x.md`, *then* it is a corpus member, is selected" (FSPEC:802-804). The second fixture satisfies the first clause's description literally, so a test author building the first fixture at the direct path writes a passing `RSN-EMPTY` assertion for the case E-35 requires to be selected. E-07 carries the same phrase (FSPEC:663). BR-2 disambiguates correctly — the never-candidate class is `docs/discarded/{feature}/`, one directory deeper than the shipped globs reach, confirmed at `pdlc/workflows/consolidate-learnings.js:1344-1345` — so this is wording sitting in the fixture-defining sentence, not a design gap. Now that REQ AC-2.6 states both halves explicitly (REQ:300-303), the FSPEC can simply mirror its two-clause phrasing. Fix: narrow AT-15's first clause and E-07 to `docs/discarded/{feature}/LEARNINGS-*.md` (nested), leaving the direct path to E-35. | AT-15, E-07, E-35, BR-2 |
| F-03 | Low | Local | **Carried from v4 F-02, unchanged: AT-20's cross-catalogue disjointness oracle ranges over two catalogues while BR-9 binds three.** BR-9's rule is "no id is used in another catalogue's position" over all three sets (FSPEC:509), but its only oracle is AT-20's "neither catalogue's ids appear in the other's position" (FSPEC:822-823), which by construction covers the per-document and corpus-level sets alone. Nothing fails if `NTC-KEYTYPE` were emitted in a per-document reason position. The per-catalogue set-equality tests do not close this: they assert membership of a declared set, not exclusivity across sets. REQ:323-325 now names the third catalogue as a peer of the other two ("Three set-equality tests, one per catalogue"), which makes the two-way oracle the odd one out. Fix: extend AT-20's second conjunct to all three catalogues, or add the conjunct to AT-32. | BR-9, AT-19, AT-20, AT-32 |

## Questions

## Positive Observations

## Recommendation

## Verdict
