# Cross-Review: product-manager — Implementation (Phase CR)

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-learnings-injection/` — the feature's full diff against `main`
**Date:** 2026-08-21
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Cross-Feature | The shipped runnable artifact is stale against the reviewed source: `node pdlc/workflows/build-runtime.mjs --check` prints `STALE pdlc/workflows/dist/pdlc-cli.mjs`, and `__tests__/consolidationBuild.test.js`'s `T32 — build-runtime.mjs --check is clean` is **red on the branch as committed**. The drift is LI-22's refactor of `isLearningsSelfPath` (`pdlc/workflows/orchestrate-dev.js:2568-2570` reflowed to one line) never re-bundled. Behaviourally it is whitespace only — I rebuilt, diffed (1 insertion, 3 deletions, formatting), and reverted — but the branch does not pass its own required check, and the artifact an operator actually executes is not the artifact this review read. Fix: run `node pdlc/workflows/build-runtime.mjs` and stage `pdlc/workflows/dist/` (the wave gate's `postWaveCommand`/`postWavePathspecs`, `CLAUDE.md` DEC-08) | Repo constraint DEC-08; blocks AC-6.1's "runs in CI" |
| F-02 | High | Local | **AC-4.3's oracle is vacuous.** `LI-AT-29` (`__tests__/learningsDispatchSet.test.js:380-393`) compares `observe(enabledRun)` to `observe(disabledRun)` over `report.verdicts`, `report.completenessScores`, `report.roundWindows`, `report.approvalAnchors`, `report.erratumRoutes` — **none of those five keys exists on the report**. `buildFinalReport`'s object literal (`pdlc/workflows/orchestrate-dev.js:15925-15955`) carries `feature, outcome, phases, artifactPaths, testSummary, harvestStatus, dodVerifiedCommit, dodHeadUnverified, provenance, notices, haltPhase, postmortemStatus, postmortemPath, queueRow, mergeStatus, mergeSha, mergeMethod` plus four conditional spreads, and no grep in `orchestrate-dev.js` finds any of the five names as a report field. Every `?? null` therefore resolves to `null` on both arms and the assertion is `{5 nulls} === {5 nulls}`. I proved it by mutation: adding `expect(Object.values(observe(enabledRun)).some(v => v !== null)).toBe(true)` to that test reds it (`Expected: true, Received: false`), suite otherwise green. AC-4.3 is the falsifiable form of "the corpus changes what an author reads, never whether the pipeline converges" — its only non-vacuous conjunct today is the per-index prompt comparison below it. Fix: assert the five gate inputs where they actually live (the `phases[]` rows' iteration counts, the parsed verdict records, `learningsRecord`'s round-window arithmetic), or observe them through a recording seam, so a deleted case reds | REQ AC-4.3; FSPEC AT-29 |
| F-03 | High | Local | **The committed pre-feature baseline is never used as an oracle — the builder is not wired.** AC-5.1a requires every composed dispatch on a disabled run to be byte-identical to "AC-6.2's recorded baseline — *that committed pre-feature fixture, not a second branch of this run*", and PLAN §DoD item 4 (`PLAN-pdlc-learnings-injection.md:517`) widens the claim to four states. The fixture ships (`pdlc/workflows/__tests__/fixtures/learnings-baseline/{PHASE-F-AUTHORING-PROMPT,PHASE-R-REVIEW-PROMPTS}/*.txt`, `MANIFEST.json`), the capture script ships (`scripts/capture-learnings-baseline.mjs`), and a drift guard ships (`__tests__/learningsBaselineGuard.test.js`) — but the guard only re-hashes the fixture against hand-transcribed digests. Grepping `learnings-baseline` / `PHASE-F-AUTHORING-PROMPT` across `pdlc/workflows/__tests__/*.js` returns **only** `learningsBaselineGuard.test.js` and `learningsCaptureScript.test.js`; no test reads a fixture file and compares it to a composed prompt. The three tests whose titles say "byte-identical to the recorded pre-feature baseline" (`learningsDispatchSet.test.js:297`, `:405`, `:426`) all compare an enabled run to a **disabled run of the same branch** — the comparison AC-5.1a explicitly excludes — and `LI-AT-29`'s own comment concedes it ("here approximated as byte-identical between the two runs", `:394-397`). The regression AC-6.2 names (injected text leaking into a disabled run through a path the disabled arm also takes) is therefore undetectable. Fix: read `fixtures/learnings-baseline/{caseId}/{i}.txt` and assert equality against the corresponding composed prompt of the disabled/empty/unlistable/admits-nothing runs | REQ AC-5.1a, AC-6.2; PLAN §DoD item 4 |
| F-04 | Medium | Local | **AC-3.4's oracle is vacuous and absence-only.** `LI-AT-23` (`learningsDispatchSet.test.js:440-442`) asserts `JSON.stringify(enabledRun.result.report.erratumRoutes ?? [])` does not contain the injected document's path. `report.erratumRoutes` does not exist (F-02), so the subject is the literal string `"[]"` and the assertion can never red. It is also negative-only: nothing asserts what the report *does* carry for that path instead. AC-3.1's rows naming the source document are the positive half AC-3.4 itself names ("the report's AC-3.1 rows name the source document — the trace an operator follows"); pair the negative with that positive on the same run | REQ AC-3.4; FSPEC AT-23 |
| F-05 | Medium | Local | **AC-5.2's write half is unasserted, and its `_constraints`/`_decisions` clause is tautological.** BR-15 (`FSPEC:714-716`) states "Nothing under `docs/_constraints/` or `docs/_decisions/` is written, no LEARNINGS document or skill prompt is written, and no index, cache or state file is created anywhere (NG-1, NG-4)" — R-5's observational boundary. `runScenario` collects a `created` set from `_writeFile`/`_appendFile` (`learningsDispatchSet.test.js:177,190,194,238`) and **no test ever reads it**; the AT-33/34 test supplies `_writeFile: async () => ({ ok: true })` and asserts nothing about writes. Separately, `learningsDispatchSet.test.js:707-708` asserts no member of `observedSet` starts with `docs/_constraints/` or `docs/_decisions/`, but `observedSet` was built by `.filter(p => isCorpusPath(p))` at `:702-705`, where `isCorpusPath` admits only `priorA`/`priorB` — the two clauses are true by construction of the filter, not by behaviour of the code. Fix: assert the write set (`created`) equals the expected non-corpus write set, and lift the prefix assertions onto the unfiltered read/write log | REQ AC-5.2; FSPEC BR-15, AT-33 |
| F-06 | Medium | Local | **The `RSN-COUNT` / `RSN-BYTES` attribution for overflow documents keys on a condition no upstream document states.** `selectLearnings` (`orchestrate-dev.js:2456-2459`) computes `propagateBytes = firstByteFailIndex !== -1 && firstByteFailIndex < window.length - 1` and labels every document past the `maxDocuments` window `RSN-BYTES` when it holds, `RSN-COUNT` otherwise. The guard `< window.length - 1` appears nowhere in FSPEC, TSPEC, PLAN or PROPERTIES (grepped). Its effect is a split an operator cannot predict from the spec: with `maxDocuments: 5`, a total-byte failure at window index 2 labels documents 6+ `RSN-BYTES`, while the same failure at window index 4 labels them `RSN-COUNT` — though in both cases those documents were cut by the count bound, which BR-5 says is "applied first" and whose cut documents "carry `RSN-COUNT`" (`FSPEC:436-442`), and which AT-08 states as "every unselected one carries `RSN-COUNT`" (`FSPEC:843-845`). AC-3.2's ids are defined by cause (`RSN-COUNT` = "below the count threshold's cut"; `RSN-BYTES` = "dropped by the total byte bound"), so the reason an operator reads is wrong in one arm regardless of which reading is chosen. The catalogue closure is unaffected — both ids are members — so this is accuracy, not conformance. Upstream owes the disambiguation (see the two ERRATUM lines in my final message); the implementation owes a rule that matches whichever upstream lands, plus a fixture that reds when the arm flips | REQ AC-3.2, AC-2.4; FSPEC BR-5, BR-6, AT-08 |
| F-07 | Medium | Local | **The per-dispatch `mode` field ships permanently `undefined`.** TSPEC §D.2's record shape (`TSPEC:717`) carries `phaseId: "T", docType: "TSPEC", mode: "creator"` as "per-dispatch context that lets an operator find the dispatch" (`TSPEC:767-769`). `buildLearningsInjector`'s closure destructures `{ feature, docType, phaseId, mode }` and writes `mode` into the record (`orchestrate-dev.js:2555, 2578`), but the sole production call site passes only three of the four: `await _injectLearnings({ feature, docType, phaseId })` (`orchestrate-dev.js:9448`). No test asserts `mode` (grep for `mode` in `learningsDispatchSet.test.js` returns nothing), so nothing reds. The operator-facing consequence is that a report with five optimizer rounds and one creator dispatch for the same `docType` gives no way to tell them apart — which is exactly the disambiguation §D.2 says the field exists for, and which matters because C-1 makes injection recur per round. Fix: pass the episode's mode at `:9448` (it is computed at `:9454-9477` in the same function) and add it to the per-dispatch context assertion | TSPEC §D.2; REQ AC-3.1 (locus), AC-3.3 |
| F-08 | Medium | Local | **AC-1.1's erratum arm has no runtime oracle.** AC-1.1 names three dispatch shapes — "creator, optimizer round, or erratum". The composition-site set-equality test (`learningsDispatchSet.test.js:459-514`) drives `main()` and observes `{REQ, FSPEC, TSPEC, PLAN, DECISIONS, PROPERTIES, null, LEARNINGS}`, but that run reaches creator and optimizer dispatches only; grepping `erratum` across `learnings*.test.js` finds no test that drives an erratum round and asserts the composed prompt carries the block. The erratum arm is covered only by a **static source scan** — `learningsPremises.test.js:119-145` counts `dispatchKind: "authoring"` occurrences inside `erratumRound`'s function body. That establishes the site is tagged, not that a block reaches it. The risk is mitigated (both erratum sites at `orchestrate-dev.js:13494` and `:13588` go through `wrappedDispatch` → `dispatchAndVerify`, the one chokepoint where `injectHere` lives, and `wrapperSeams` carries `_injectLearnings` at `:13063`), which is why this is Medium and not High — but a behavioural claim in a P0 AC deserves a behavioural oracle. Fix: one scenario that routes an erratum round and asserts the block on the erratum-author prompt | REQ AC-1.1; FSPEC AT-01 |
| F-09 | Medium | Process | **A new operator-facing lint ships with no behavioural test.** `pdlc/hooks/scripts/check-finding-grammar.sh` is 97 new lines that decide whether an author is warned about untagged erratum findings, and it is live: registered in `pdlc/hooks/hooks.json` as a `PostToolUse: Write|Edit` hook. Its only coverage is inventory — `consumerCleanup.test.js:76` checks the file is tracked and mode `100755`, `hookCompatibility.test.js:387-392` checks the `{event, script}` pair is registered. Nothing executes the script against a `CROSS-REVIEW-*` fixture with and without line-leading `FINDING:` lines. Since this hook exists precisely because two rounds on this branch halted on missing tags (POSTMORTEM-D item 8), a false negative in it reinstates the failure it was written to prevent, silently. Fix: two cases on the sibling script-execution pattern already in `consumerCleanup.test.js` — one erratum-round doc that must warn, one that must not | POSTMORTEM-D item 8; DC-01 (catalogued operator strings) |
| F-10 | Medium | Process | **Out-of-scope pipeline behaviour shipped on this branch.** The diff carries four changes with no REQ acceptance criterion and no PLAN task row (the PLAN's task table runs LI-01…LI-23 and covers only the injection region): (1) the erratum **restatement retry** — a new re-dispatch loop at `orchestrate-dev.js:13814-13911` that spends an extra model turn per non-approving zero-`FINDING:` confirmation; (2) the rewritten `findingGrammarClause` (`:10151-10177`); (3) the new `check-finding-grammar.sh` hook; (4) `Delta-Confirmation Findings` sections added to `pdlc/skills/{pm,se,te}-review/SKILL.md` (+17 lines each). These are POSTMORTEM-D remediations and I agree they are good changes — (1) in particular is well-reasoned and well-tested (`erratumProtocol.test.js` RT-1g-a…e). But REQ G-5 states as a goal that "erratum routing … [is] unchanged. This feature changes what an author is told, never what the pipeline requires of what they produce", and (1) changes how many dispatches an erratum round makes. Shipping them inside this feature's diff means the product decision was made in engineering artifacts rather than in the REQ (which is what the scope rule exists to prevent), and it makes this branch's blast radius wider than its REQ describes. Fix: either add a REQ/FSPEC amendment naming the POSTMORTEM-D remediations as in-scope for this feature, or split them into their own queue row | REQ G-5, NG-7 |
| F-11 | Low | Local | **`orderKeys` is recorded over eligible documents only, not "per corpus document".** TSPEC §D.2 words BR-10 locus 1 as "ordering key per corpus document, as observed by THIS dispatch" (`TSPEC:724-726`), and AC-3.3 requires "every input the rule used" to be in the report. `selectLearnings` builds `orderKeys` from `ordered`, which is the *eligible* set only (`orchestrate-dev.js:2434-2436`); documents rejected `RSN-SELF`, `RSN-UNREADABLE`, `RSN-UNPARSEABLE` or `RSN-NO-MATERIAL` never appear there. Reproduction is not actually harmed — `rejected[]` accounts for each of them with its reason, and an ineligible document has no ordering key to record — so this is a wording/shape mismatch rather than a reproducibility gap. Fix: either narrow §D.2's wording to "per eligible document" (an upstream edit) or record `orderKey: null` rows for rejected documents | REQ AC-3.3; TSPEC §D.2 |

**Scope legend.** `Local` — addressed in this loop and discarded with the file. `Cross-Feature` — promoted to `docs/_constraints/` or a DECISIONS doc at harvest. `Process` — routed to process learnings at harvest.

**Not counted against this document.** `documentOracles.test.js` `AT-22` and `consumerCleanup.test.js` also red in my full-suite run, but both are the known local-environment artifact (`coveredViolations` walks the whole tree under `root`, so untracked `.claude/worktrees/agent-*` checkouts pollute it — `CLAUDE.md` debugging note). `consumerCleanup` passes in isolation. Neither is a defect of this feature. `pdlc/workflows` full suite otherwise: **3956 passed, 3 failed, 70 skipped**; the 12 `learnings*.test.js` suites are **106/106 green**.

## Questions

_(pending)_

## Positive Observations

_(pending)_

## Recommendation

_(pending)_
