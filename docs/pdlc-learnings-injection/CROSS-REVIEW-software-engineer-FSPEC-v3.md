# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-learnings-injection/FSPEC-pdlc-learnings-injection.md` (v0.3)
**Date:** 2026-08-19
**Iteration:** 3

Delta re-review against `CROSS-REVIEW-software-engineer-FSPEC-v2.md`, over
`git diff 81df67cc..HEAD` on the FSPEC (62 insertions, 44 deletions, v0.2 → v0.3). Only changed
sections were re-read for new defects. Every measured claim touched this round was re-measured
against HEAD.

## Prior findings disposition

| Prior | Severity | Status | Evidence in the revision |
|---|---|---|---|
| F-01 | High | **Resolved** | BR-14 now carries five states: wrong-typed declared key takes its default, the run stays **enabled**, and a notice names the key — matching the shipped per-key fallback plus `invalidKeys` notice (`pdlc/workflows/orchestrate-dev.js:1986-2031` builds `invalidKeys` from `boolField`/`positiveInt`/`positiveNumber`/`envelope` fallbacks and returns `sectionMalformed: false`). `sectionMalformed: true` remains reserved for the section-present-but-not-an-object guard (`:1982`), which is exactly what BR-14's malformed row now says. E-23 narrowed, E-34 added, AT-32 rewritten with a positive assertion for the wrong-typed case (run enabled, key at default, notice names it). No divergence from precedent left to justify. |
| F-02 | Medium | **Resolved** | BR-5 now states plainly that REQ AC-2.1's "count equals the threshold" is falsified under §4.1's declared values by the measurement, and declares `ERRATUM: REQ` (line 365). |
| F-03 | Medium | **Resolved** | BR-2's note now names the residual class — a document *directly* at `docs/discarded/LEARNINGS-x.md` matches the first glob, the case REQ C-3/AC-2.6 legislate against — and declares `ERRATUM: REQ` (line 270). "None exists at HEAD" checks out: `git ls-files 'docs/discarded/*'` returns only `README.md` and depth-2 feature directories, no `docs/discarded/LEARNINGS-*.md`. |
| F-04 | Low | **Resolved** | `### Group 4 — fail-open under corpus state` restored above AT-24 (line ~823). Branch-coverage line correspondingly re-stated for E-01 … E-34 less retired E-05, and D-5's exercise list corrected to AT-04/16 (AT-15 is a corpus-membership case, not a D-5 branch — the drop is right). |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **BR-5's new measurement qualifier states a basis under which the measurement yields the opposite result, and contradicts BR-6.** The added parenthetical reads "(max 41,180, under strict title matching on BR-6's five names)" and carries "87 of the 89 exceed `maxBytesPerDocument` alone". BR-6 says the opposite about matching: these names identify sections "by the conventional titles the harvest skill writes, **where they carry numeric prefixes** — `## 2. Cross-Feature Patterns`" (FSPEC:391-393), and every corpus document at HEAD writes them that way (`docs/completed/pdlc-advisory-tier/LEARNINGS-pdlc-advisory-tier.md:13,32,52`; `docs/pdlc-plugin-retirement/LEARNINGS-pdlc-plugin-retirement.md:14,24,36`). I re-measured this repo's 9 corpus documents both ways. Strict literal matching of the five bare titles selects **zero** sections in all 9 documents: 0 bytes each, 0 of 9 above the 6,000-byte bound — under that basis the paragraph's conclusion ("at most three can contribute") inverts to "the count bound never binds because nothing is injectable". Prefix-tolerant matching reproduces the stated numbers: max 41,022 bytes (`docs/completed/pdlc-review-loop-hardening/LEARNINGS-pdlc-review-loop-hardening.md`, against the quoted 41,180 — same document, small accounting difference), 9 of 9 above 6,000. So the measurement was taken prefix-tolerantly and the qualifier misnames its own basis. This is load-bearing twice over: BR-5's `ERRATUM: REQ` against AC-2.1 rides on the number, and F-O-1 hands TSPEC the heading-form pin — a TSPEC author who takes "strict title matching" literally pins a predicate that injects nothing from the real corpus and ships a vacuously green feature. Fix: state the basis BR-6 states — prefix-tolerant matching of the five conventional titles in `## N. Title` form — or, if strict matching is genuinely meant, correct the numbers and BR-6 with it. | BR-5 §measurement paragraph, BR-6 (FSPEC:391-393), F-O-1 |
| F-02 | Medium | Local | **The per-document reason catalogue now diverges from REQ AC-3.2's closed catalogue in two directions with no erratum declared.** REQ AC-3.2 enumerates its closed set as `RSN-COUNT`, `RSN-BYTES`, `RSN-SELF`, `RSN-UNREADABLE`, `RSN-UNPARSEABLE`, `RSN-TRUNCATED` (`REQ-…md:312-318`), and AC-4.2 restates a truncated document as skipped "with its per-document reason id recorded" (`:341-344`). BR-9's catalogue has six members, and they are not those six: `RSN-TRUNCATED` is retired (hardened this round: "the v0.2 edge retires with it, E-05 not reused") and `RSN-NO-MATERIAL` is added — a member REQ never mentions. Both catalogues are declared closed and each is asserted by a set-equality test (BR-9 rule 1; AT-19), so as the two documents stand, a set-equality test written from REQ fails against the behaviour FSPEC specifies. The FSPEC routes its three other REQ divergences explicitly (BR-2:270, BR-5:365, BR-14:597); this one is the same class and is silent. Add the one-line `ERRATUM: REQ` note beside the BR-3 retirement paragraph and BR-9's catalogue naming both the deletion and the addition. Erratum line rides with this review. | BR-3 (FSPEC:293-298), BR-9, REQ AC-3.2, AC-4.2 |
| F-03 | Medium | Local | **The configuration notices are called "catalogued" but sit in no catalogue and carry no id, and this round added a second one.** BR-14 now emits two distinct notices — "a catalogued notice naming the malformed configuration" (row 3) and "a catalogued notice naming the key" (row 4, new) — and E-23/E-34/AT-32 repeat the wording. BR-9 closes and tests the two `RSN-*` catalogues, but neither notice appears in either, and neither has an id. REQ C-9 is explicit that "every report line and notice this feature emits is a registered catalogue entry **with an id a test can assert on**", and that closure of the set is a requirement while only registration is TSPEC's (`REQ-…md:204-210`). F-O-3 defers registration, not enumeration. As written, AT-32's oracle can only assert that some notice text mentions the key — a containment check, not the set-equality discipline the rest of the document holds itself to. Give the two notices ids and a third closed catalogue in BR-9 (or state them as members of an existing one), so the notice set gets the same completeness test the reason sets get. | BR-14 rows 3-4, BR-9, E-23/E-34, AT-32, REQ C-9 |
| F-04 | Low | Local | **AT-11 was hardened against implementation echo; its two siblings were left as they were.** AT-11 now requires the expected byte count to be "committed with the fixture as a literal integer, recomputed by hand when the fixture changes, never derived in the test" — exactly right. AT-12 still reads "its contributed bytes equal the bound **as computed from the fixture**", and AT-13's total-bound assertion is phrased the same way. The bound in AT-12 is a declared threshold, so the literal is available and cheaper than the phrasing suggests. Apply AT-11's wording to AT-12 and AT-13 so the whole byte-accounting group is pinned by transcription rather than by computation. | AT-11, AT-12, AT-13 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | E-33 asserts "(measured: occurs at HEAD)" for a LEARNINGS document carrying none of BR-6's priority sections. It does not occur in this repository — all 9 corpus documents carry between 13,038 and 41,022 bytes of BR-6 material under prefix-tolerant matching, none under strict. Is the measurement from `regime-ledger`? If so, say which repository the parenthetical measured, as BR-4's table does for its own counts; if it was taken under strict title matching, it is the same artefact as F-01 and the row is really "occurs nowhere at HEAD". |
| Q-02 | With F-01 settled, is the intended F-O-1 predicate "heading text, after stripping an optional leading `N.` ordinal, equals one of the five titles", or something looser (prefix match, case-insensitive)? BR-6 correctly leaves the form to TSPEC, but the measurement basis and the pin have to agree, and the difference between them is the whole feature's material. |

## Positive Observations

- The BR-14 rewrite is the right resolution of v2's High and it landed as a behaviour change, not as prose: the wrong-typed key moved out of the malformed row into its own state with its own edge (E-34), its own AT clause, and its own stated rationale against turning a feature off over one mistyped threshold (DC-08). The shipped precedent it now matches is real — `parseAdvisoryConfig` pushes the key onto `invalidKeys`, substitutes `ADVISORY_DEFAULTS[key]`, and returns `sectionMalformed: false` (`orchestrate-dev.js:1986-2031`) — so an operator who mistypes `maxDocuments` gets the same treatment here as everywhere else in the pipeline.
- AT-32's wrong-typed clause is a positive oracle, not an absence one: it asserts the run is enabled, the block is selected under the key's default, and the notice names the key. The misspelt-section clause keeps its paired positive ("indistinguishable from AT-31's absent-section case") rather than only asserting no notice fires.
- Retiring `RSN-TRUNCATED` was carried through consistently across every surface it touched in one edit — BR-3's outcome table and paragraph, E-05 deleted rather than left dangling, AT-28 rewritten to the single remaining branch, the branch-coverage line updated to "E-01 … E-34, less retired E-05", and D-12's exercise pointer preserved. The only loose end is upstream (F-02), not internal.
- The AT-11 hardening names the failure mode it prevents rather than gesturing at it: expected value committed as a literal, recomputed by hand, never derived in the test. That is the anti-echo rule stated in a form an implementer cannot satisfy accidentally.
- The v2 findings were answered by narrowing claims rather than by widening them: BR-2 names the residual `docs/discarded/` class instead of quietly re-scoping AC-2.6, BR-5 says outright that AC-2.1 is falsified under §4.1's values, and both route an erratum instead of leaving REQ and FSPEC to disagree in silence.
