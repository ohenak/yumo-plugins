# Cross-Review: test-engineer — FSPEC (delta re-review, frozen round)

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-learnings-injection/FSPEC-pdlc-learnings-injection.md` (v0.9)
**Date:** 2026-08-19
**Iteration:** 11

## Scope

Delta re-review of `cbb0a63e..HEAD` for this document (+8/-5, one commit: `523e2df9`, "v0.9
follow-through"). I re-read only the changed regions — the revision-note block at FSPEC:20-48 and
the AC-6.2 traceability row at FSPEC:149 — plus the material those regions make claims about
(header at FSPEC:9-18, `## Acceptance Tests` preamble at FSPEC:748-754, AT-20/AT-21/AT-22 at
FSPEC:854-867, AT-31/AT-32 at FSPEC:899-912). Sections untouched by the delta were approved at v10
and were not re-litigated. Frozen round: only a defect this delta introduced, or a contradiction
with HEAD or upstream REQ v0.9, can block.

## Disposition of v10 findings

v10 was **Approved** with zero findings (`CROSS-REVIEW-test-engineer-FSPEC-v10.md:150-152`,
`{"high": 0, "medium": 0, "low": 0}`), so there is no open finding of mine to resolve. The one
`DEFERRED:` item I carried — BR-9's notice catalogue leaving its locus unstated, routed to TSPEC —
is untouched by this delta and stays deferred; it was never a blocking item.

## What the delta changed, and whether it broke anything

The delta is two edits, both bookkeeping. Neither touches a rule, a flow step, an edge case or an
acceptance-test oracle — I diffed the whole file to confirm that (`git diff cbb0a63e..HEAD --
docs/pdlc-learnings-injection/FSPEC-pdlc-learnings-injection.md`, 8 insertions / 5 deletions,
confined to lines 29-48 and 149).

**1. The v0.9 revision note moved and grew (FSPEC:41-47).** It previously sat *above* the v0.8
erratum note; it now sits below it, so the note block reads oldest-to-newest (v0.6 → v0.7 → v0.8 →
v0.9) instead of interleaving. Every claim the expanded note makes is true of the document it
prefaces, which is the only thing that can break here:

- "recorded **per authoring dispatch** (REQ AC-3.2/AC-3.3)" — BR-9's per-document catalogue
  (FSPEC:503-513) and BR-10's locus table (FSPEC:548-551) still say exactly that; upstream REQ
  AC-3.2/AC-3.3 still say it too (`REQ-pdlc-learnings-injection.md:327-328, 336-345`).
- "§4.1 thresholds stay run-level" — BR-10's second locus row is still "Once per run"
  (FSPEC:548-551), and E-26 still routes all three thresholds to that record (FSPEC:724).
- "BR-10 closes at two loci with one completeness test each" — AT-22 still says "**two**
  completeness tests assert set equality, one per BR-10 locus" (FSPEC:862-864). Set equality, not
  containment, so deleting a member of either locus still reds.
- "run-level mirrors are additive, not oracles" — BR-9:537-538 and BR-10:554-555 unchanged.
- "AT-20/AT-21/AT-22 name the locus and exercise AT-18's changing-corpus fixture" — AT-20
  (FSPEC:854-858) and AT-22 (FSPEC:859-867) both invoke AT-18's changing-corpus run and state the
  falsifier ("one run-level field fails" / "one run-level set reproduces at most one of them and
  fails"); AT-21 (FSPEC:857-858) names "BR-8's rows **for that dispatch**". AT-22's anti-echo clause
  survives verbatim: expected selection "transcribed literally by hand and committed in the
  fixture", "neither calls the production selector nor reimplements it".
- "Locus corrections only; no new behaviour" — confirmed by the diff itself.

**2. AC-6.2's rules column renamed (FSPEC:149).** `§Acceptance-test preamble` → `§Acceptance Tests
preamble`. This is a dangling-reference repair, not a content change: the document's heading is
`## Acceptance Tests` (FSPEC:748) and its preamble (FSPEC:750-754) is where the disabled-run
execution condition actually lives ("Byte-identity tests compare against a **recorded baseline**: a
composition captured from pre-feature HEAD, committed, and never regenerated by the branch under
test"). That preamble is what REQ AC-6.2 asks for (`REQ:415`, disabled configuration asserts against
the recorded baseline), and the row's tests AT-31/AT-32 still exist and still carry the oracle —
AT-31 asserts byte-identity to the recorded baseline *and* the positive "no injection key … absent,
not present-and-empty" conjunct (FSPEC:899-902); AT-32 keeps its positive-presence conjunct that
stops its equality from being vacuously green (FSPEC:903-912). AC-6.1's row on the line above uses
the same corrected anchor, so the two rows agree.

Nothing else moved: BR-9 still carries three catalogues with three set-equality tests
(AT-19/AT-20/AT-32), the notice catalogue's set-equality test still lands at AT-32, and the header's
Upstream field still pins REQ v0.9 (FSPEC:11), which is the version at HEAD.

## Findings

## Questions

## Positive Observations

## Recommendation

## Verdict
