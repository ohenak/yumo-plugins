# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-learnings-injection/REQ-pdlc-learnings-injection.md`
**Date:** 2026-08-18
**Iteration:** 1

Scope of this review is the testing lens only: whether each acceptance criterion is precise
enough to derive a failing black-box test from, whether the enumerated contracts are closed and
set-equality-checkable, and whether every existing-behaviour claim the REQ makes is true at HEAD.
Product framing (§1–§3) and technical approach are their reviewers' lenses, not mine.

All HEAD claims below were checked against the tree, not inferred.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **C-1 names a REQ authoring dispatch that does not exist at HEAD.** `PHASE_DISPATCH.R` sets `creator: null` (`pdlc/workflows/orchestrate-dev.js:3629`), and the pipeline dispatches nothing for it: `if (dispatch.creator)` guards the only creator dispatch (`orchestrate-dev.js:13505-13508`, comment "`creator: null` (Phase R) reviews a document this pipeline did not write, so there is nothing to dispatch"), reaffirmed at the Phase R call site — "No creator (`PHASE_DISPATCH.R.creator` is null — the REQ arrives authored)" (`orchestrate-dev.js:13760-13761`). One of the six members of C-1's closed set therefore has no dispatch to inject into, and AC-1.1 ("any of the six authoring dispatches named in C-1 is composed") cannot be exercised for it. A TE deriving the AC-1.1 test suite gets five cases and one that cannot be written. Either drop REQ from C-1 and say where a REQ author is grounded instead (the REQ is authored outside `orchestrate-dev`), or state the separate integration point that would carry it and scope it explicitly. | C-1, AC-1.1, §1.2.1 |
| F-02 | High | Local | **"Exactly the six authoring dispatches" is not a closed, run-invariant set at HEAD, so AC-1.2's set-equality check cannot be written.** Three separate facts break the count. (a) Phase D is conditional: it runs only when the `DECISIONS_WARRANTED` trailer says so, defaulting true on absent/malformed input (`orchestrate-dev.js:5204-5239`, prompt at `9411-9412`) — a run with `DECISIONS_WARRANTED: false` has five, not six. (b) Every phase adds one **optimizer** dispatch per failing review round (`reviewLoop` step (g), `orchestrate-dev.js:7655-7660`), and the optimizer is an author writing the same document, sharing the creator's session (`M-2`, `:7657`). (c) **Erratum** author dispatches and their land-proof retries are themselves tagged `dispatchKind: "authoring"` (`orchestrate-dev.js:12821`, `:12915`), alongside the creator (`:13515`). AC-1.2 asserts a cardinality of six and byte-identity for "every other dispatch"; against HEAD it is either false or vacuous depending on the run, and the failure is invisible under a fixture that converges on iteration 1 with no DECISIONS. State the rule over the dispatch taxonomy that already exists (`dispatchKind === "authoring"` and phase membership), not over a hand-counted six, and say explicitly whether optimizer and erratum dispatches for an in-scope document are in or out — NG-5 excludes reviews, implementation, DoD and harvest but is silent on both. | C-1, AC-1.2, NG-5 |
| F-03 | High | Local | **AC-3.4's only testable half is the absence half, and its positive half has no trigger or output to assert on.** The AC pairs "the report surfaces it as a note against the named source document" with "**no** erratum round is opened". The negative conjunct already holds at HEAD for free: `parseErrata` ignores any line whose doc type is outside `ERRATUM_DOC_TYPES` rather than erroring (`orchestrate-dev.js:6945` regex, `:6971-6975`), so a `LEARNINGS` erratum line is dropped today, with or without this feature — a test asserting only "no erratum round opened" passes against the pre-feature baseline and proves nothing. The positive conjunct is what would make the oracle falsifiable, and the REQ defines neither what the author emits to raise the note (there is no such channel; `ERRATUM:`, `VERDICT:` and `DECISIONS_WARRANTED:` are the parsed trailers) nor what the report field looks like. It also asks the author to produce something new, which contradicts G-5 ("this feature changes what an author is told, never what the pipeline requires an author produces") and NG-3. Resolve the contradiction in one direction: either drop the positive conjunct and make AC-3.4 purely a preservation statement over the erratum mechanism, or define the emit-side grammar as a registered catalogue entry per C-9/DC-01 and amend G-5. | AC-3.4, G-5, NG-3, C-6 |
| F-04 | High | Local | **The report's enumerated contents contradict AC-2.3, so the two cannot both be satisfied.** AC-3.1 enumerates what the report carries per authoring dispatch — "the source document paths selected, in the order used, and the total bytes injected" — and AC-3.2 enumerates the unselected rows with a reason "drawn from a closed set (below the count threshold's cut, dropped for the total byte bound, excluded as the authored feature's own, unreadable, unparseable)". AC-2.3 separately requires that "the report states for each such document that it was bounded" — a per-**selected**-document fact. It fits neither enumeration: AC-3.1's selected rows have no per-document field for it, and AC-3.2's five-member closed reason set is about documents that were *not* selected and contains no bounding/truncation member. A completeness test over these contracts must be a set-equality check over the full enumeration (a silently deleted case is exactly what containment misses), and today set-equality against AC-3.1/AC-3.2 falsifies AC-2.3. Add the per-selected-document bounded flag to AC-3.1's enumeration and re-close both sets. | AC-2.3, AC-3.1, AC-3.2 |
| F-05 | High | Cross-Feature | **The REQ does not carry its own stopping rule, which `DC-09` requires of every REQ.** `docs/_constraints/DOMAIN-CONSTRAINTS.md:245` (DC-09, "Applies to: REQ authoring, `pm-review`/`se-review`/`te-review` of REQ") makes the obligation explicit and singles it out: "Paste the stopping rule into the artifact under review. This is the part that made the difference and the part that is easy to get wrong. A rule living only in a constraints file or a method document nobody loads does nothing." Grepping the REQ for "stopping rule", "DC-09", "fixed point" and "non-decreasing" returns nothing, and §7's obligations are feature obligations, not loop-termination conditions. This is load-bearing for the round this document is in: without it neither reviewer has the cited authority to route implementability findings downstream and approve, which is the failure mode DC-09 was promoted from (16 rounds, blocking counts `9→12→11→12→7→7→10→10→8→9→9`). Paste the DC-09 stopping rule verbatim into the REQ, near §5. | Whole document, DC-09 |
| F-06 | Medium | Local | **AC-4.3's oracle cannot fail honestly as stated.** It compares a run against "the same run with `learningsInjection.enabled` set to `false`" and asserts every verdict, every round count and the halt/complete outcome are unchanged. Against live dispatches the comparison is model-nondeterministic, so a red result would be noise rather than a defect; against the fixture harness AC-6.1 mandates ("no live model calls") the comparison is vacuous, because scripted responses are identical by construction whatever the prompt contained. The invariant that is actually falsifiable sits one level down and is worth stating instead: no injected byte reaches any gate input — verdict parsing, completeness scoring, round-window arithmetic, approval anchors and erratum routing consume no injection-derived value — plus AC-1.2's byte-identity on non-authoring dispatch prompts. That version fails when someone feeds an injection count into a convergence decision, which is the regression AC-4.3 exists to catch. | AC-4.3, G-4, G-5 |
| F-07 | Medium | Local | **AC-2.2 defers its expected value, so no selection test has one.** The AC requires the selected documents be "the highest-ordered ones under a **stated total ordering**" with "a defined, documented fallback for any document not carrying" the harvest metadata — but the ordering and the fallback are both deferred to O-2/BL-02, and no ordering is stated in the document. A fixture corpus mixing documents with and without harvest metadata therefore has no derivable expected selection, and AC-2.4's "dropping whole documents from the low end of the ordering" inherits the same gap. Per DC-09 the oracle itself is legitimately deferrable, and O-2 does bind it before FSPEC authoring, so this is not blocking — but the REQ can carry the falsifiable part today without leaving its altitude: state the ordering's *negative* invariants as testable now (permuting file mtimes and re-running yields the identical selection; renaming a file's directory without changing its metadata does not change its rank), so AC-2.2's "never wall-clock time, file mtime, or a model's judgement" is a property with a test rather than a promise. | AC-2.2, AC-2.4, O-2 |
| F-08 | Medium | Cross-Feature | **The receive-side triple DC-01 requires is missing its third member.** `DOMAIN-CONSTRAINTS.md:20` (DC-01, "Applies to: REQ authoring, FSPEC, all reviews") states: "The REQ must state the behaviour for **absent, malformed, and truncated** input, with an exact fallback and an observable log signal. A parser specified only for well-formed input is an incomplete requirement." AC-4.1 covers absent, AC-4.2 covers unreadable and unparseable, AC-5.1 covers absent-or-malformed configuration — nothing covers **truncated**: a LEARNINGS document cut mid-section, which by construction still parses as Markdown and may still present some of its numbered sections. Whether that document is eligible, and whether it is reported, is currently undefined, and truncation is a live state here because the harvest step writes these documents under a pacing contract. Add the truncated case to Group 4 with its outcome and its AC-3.2 reason-set member (which also feeds F-04's re-closing). | AC-4.2, C-7, C-9, DC-01 |
| F-09 | Low | Local | **AC-5.1's "no corpus file is opened" is not observable from outside the process.** The other conjuncts of AC-5.1 are black-box (byte-identical dispatches; the injection key absent from the report — a shape HEAD already precedents, `orchestrate-dev.js:15006` emits `advisory: ... : undefined` for exactly this reason). "No corpus file is opened" is not: proving it needs process tracing or a seam the REQ should not be naming. State the observable instead — the report's injection key is absent and the run's recorded corpus reads are zero — and let TSPEC decide which seam records the reads. | AC-5.1 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Are **optimizer** (review-round revision) and **erratum** dispatches for an in-scope document inside or outside C-1? Both are `dispatchKind: "authoring"` at HEAD (`orchestrate-dev.js:7655-7660`, `:12821`, `:12915`), and the answer changes both the AC-1.2 oracle and the realised prompt-budget measurement O-1 obliges — a five-round phase pays the injection cost five more times than the six-dispatch framing suggests. |
| Q-02 | AC-2.3's per-document bound and O-4's undecided "which part of the document is injected" interact with determinism: if O-4 lands on "a named subset of its sections", does the bound apply before or after subsetting? AC-2.5's byte-identity is testable either way, but the AC-3.2 reason set and F-04's bounded flag differ between them. |
| Q-03 | AC-2.6 excludes `docs/discarded/` — verified as an established convention (`pdlc/workflows/consolidate-learnings.js:1345` scopes the corpus to `:(glob)docs/completed/*/LEARNINGS-*.md`, and `consolidationHookParity.test.js:440` asserts no `docs/discarded/` path appears). Should this feature reuse that same pathspec so the two consumers cannot drift, and should a shared-corpus-definition test assert they agree? |

## Positive Observations

- Group 4 is a genuinely first-class set of states rather than an error appendix: AC-4.1 through AC-4.4 each name an outcome *and* a report obligation, and AC-4.4's "thresholds that admit nothing behave exactly as the disabled case and record that they did" is the kind of case specs usually leave to an implementer's judgement.
- AC-3.2's reason set is stated as **closed**, which is what makes a set-equality completeness test possible at all; F-04 asks for one more member, not for a different shape.
- AC-5.1's "the key is absent, not present-and-empty" is precise, falsifiable, and matches a shape already in the codebase (`advisory: ... : undefined`, `orchestrate-dev.js:15006`) — a test can assert key absence rather than value emptiness, and the distinction is exactly the one that catches a leak.
- AC-2.5 and AC-6.1 together specify determinism as a comparison of two compositions rather than by inspection, and AC-6.2 pins the disabled case to a recorded baseline — both are stated as tests that can fail, not as intentions.
- The §1.2 "what exists at HEAD" section is a real premise list rather than framing, and it checked out: no authoring dispatch composed anywhere in `orchestrate-dev.js` references another feature's LEARNINGS (the only reads are the harvest phase and the Tier-2 approval anchor at `:8778`), and both corpus locations are pre-existing conventions this feature reads rather than invents.
- Every threshold in §4.1 carries an owner and a named consuming AC, and O-1 obliges re-deriving all three from a live measurement before they are treated as settled — the defaults are declared as a starting point rather than smuggled in as facts.
