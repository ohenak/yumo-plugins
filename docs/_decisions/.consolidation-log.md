# Consolidation Log

One entry per `consolidate-learnings` pass. The most recent entry's date is the **boundary**: a
`docs/*/LEARNINGS-*.md` with a `Date Completed` after it is in scope for the next pass.

---

## Pass 1 — 2026-07-29

**Boundary before this pass:** none. First pass; both existing LEARNINGS files were in scope.

**Consumed (2 files, both read in full):**

| LEARNINGS | Date Completed |
|---|---|
| `docs/orchestrate-dev-workflow/LEARNINGS-orchestrate-dev-workflow.md` | 2026-06-02 |
| `docs/pdlc-workflow-distribution/LEARNINGS-pdlc-workflow-distribution.md` | 2026-07-29 |

**Created (the directories did not exist — this was TE REQ v13 F-03 / post-mortem R-5, now closed):**

- `docs/_constraints/DOMAIN-CONSTRAINTS.md` — DC-01 … DC-13
- `docs/_decisions/DECISIONS-plugin-distribution.md` — DEC-DIST-01 … 04
- `docs/_decisions/DECISIONS-test-oracle-mechanics.md` — DEC-ORACLE-01 … 04
- `docs/_decisions/CONSOLIDATION-PROPOSAL-2026-07-29.md` — P-1 … P-11, plus 3 deferrals
- this log

**Promoted — constraints.** Ten of thirteen cleared the ≥2-feature bar; three were promoted on a
single feature's evidence as standing invariants, marked below.

| ID | Constraint | Features |
|---|---|---|
| DC-01 | Boundary contracts are closed (emit) and total (receive) | both |
| DC-02 | A stated platform/runtime fact must be measured, per platform | both |
| DC-03 | Every load-bearing assertion is falsified before it is trusted | both |
| DC-04 | An oracle is a pure function of an injected root | pdlc-workflow-distribution, corroborated by orchestrate-dev-workflow |
| DC-05 | Every named behavioural branch has an AT at FSPEC | both |
| DC-06 | A remediation is verified by mutation, not by reading the diff | pdlc-workflow-distribution |
| DC-07 | Work that skips a pipeline phase inherits zero review coverage | both |
| DC-08 | An unresolved item needs a named successor surface | both |
| DC-09 | A REQ stays at requirements altitude and carries its own stopping rule | pdlc-workflow-distribution ¹ |
| DC-10 | The PLAN owns what downstream phases would otherwise invent | both |
| DC-11 | Sibling oracles in one checklist share an error contract | pdlc-workflow-distribution ¹ |
| DC-12 | A drift false positive is fixed by rephrasing the document | pdlc-workflow-distribution ¹ |
| DC-13 | Scope-tag accurately — an untagged repo-wide finding reaches nothing | pdlc-workflow-distribution ¹ |

¹ Single-feature promotion, justified as a standing invariant. DC-09: the loop hit the iteration
ceiling twice, 16 rounds, and the rule was verified working once written into the artifact under
review. DC-11: mechanically obvious and cheap to state. DC-12: an anti-regression rule upheld against
four separate attempts to weaken it. DC-13: an invariant of this consolidation mechanism itself — the
under-tagging is precisely what suppresses the second occurrence that would otherwise justify a
two-feature promotion.

**Promoted — decisions.** `DECISIONS-plugin-distribution.md` is the pass's cleanest promotion:
`orchestrate-dev-workflow` raised the plugin→consumer workflow-sync problem as `OQ-05` and carried it
unresolved through all 5 FSPEC iterations, flagging that it would recur for every future
workflow-bearing plugin; `pdlc-workflow-distribution` answered it. The answer is now project-level.
`DECISIONS-test-oracle-mechanics.md` records the tool behaviours (jest's per-file module registry,
istanbul and subprocesses) that make an assertion vacuous regardless of discipline.

**Proposed, not applied — 11 skill-prompt rows.** P-1 (hand the stopping rule to reviewers at
dispatch) has the largest measured cost behind it. P-2/P-3/P-4 overlap queue row 8
`pdlc-review-loop-hardening` — **which the operator set `pending` and moved to `Order 0` on 2026-07-29,
making it the queue's next pickup** — so apply them there rather than twice. P-9 is new to this pass, not from either
LEARNINGS: five shipped skill prompts cite `DC-07`/`DC-08`/`DC-09` of a *different consuming repo's*
constraint file, and this repo now has its own DC-07/08/09 with unrelated content.

**Deferred, with reasons (3):** the additive caller-scoped trailer convention (one feature, no
recurrence — its deciding criterion was promoted as part of DC-01); the Low-carry threshold as an
enforced gate (promoted as guidance in DC-08 only, because mechanical escalation of unresolved
findings is exactly what produced the 16-round fixed point); the booked G-01…G-07 code defects
(feature-local, not cross-feature — belong in a follow-up REQ or row 8, now `Order 0`; **G-02 first**).

**Not deleted:** `docs/pdlc-workflow-distribution/FALSIFICATION-LEDGER.md`. PLAN §3.1 declares it
deliberately retained as the standing falsification record; it was read as input to DC-03.

**Branch note.** This pass was authored on `feat-pdlc-workflow-distribution`, because
`LEARNINGS-pdlc-workflow-distribution.md` existed only there while PR #19 was open. #19 was
**squash-merged** as `1fb6cbe` on 2026-07-29, so `chore-consolidate-learnings-2026-07-29` was rebased
onto `main` and now carries this pass's five files and nothing else.
