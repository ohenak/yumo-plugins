# Consolidation Log

One entry per `consolidate-learnings` pass. The most recent entry's date is the **boundary**: a
`docs/*/LEARNINGS-*.md` with a `Date Completed` after it is in scope for the next pass.

---

## Pass 1 — 2026-07-29

**Boundary before this pass:** none. First pass; both existing LEARNINGS files were in scope.

**Consumed (2 files, both read in full):**

| LEARNINGS | Date Completed |
|---|---|
| `docs/orchestrate-dev-workflow/LEARNINGS-orchestrate-dev-workflow.md` | 2026-06-02 |
| `docs/pdlc-workflow-distribution/LEARNINGS-pdlc-workflow-distribution.md` | 2026-07-29 |

**Created (the directories did not exist — this was TE REQ v13 F-03 / post-mortem R-5, now closed):**

- `docs/_constraints/DOMAIN-CONSTRAINTS.md` — DC-01 … DC-13
- `docs/_decisions/DECISIONS-plugin-distribution.md` — DEC-DIST-01 … 04
- `docs/_decisions/DECISIONS-test-oracle-mechanics.md` — DEC-ORACLE-01 … 04
- `docs/_decisions/CONSOLIDATION-PROPOSAL-2026-07-29.md` — P-1 … P-11, plus 3 deferrals
- this log

**Promoted — constraints.** Ten of thirteen cleared the ≥2-feature bar; three were promoted on a
single feature's evidence as standing invariants, marked below.

| ID | Constraint | Features |
|---|---|---|
| DC-01 | Boundary contracts are closed (emit) and total (receive) | both |
| DC-02 | A stated platform/runtime fact must be measured, per platform | both |
| DC-03 | Every load-bearing assertion is falsified before it is trusted | both |
| DC-04 | An oracle is a pure function of an injected root | pdlc-workflow-distribution, corroborated by orchestrate-dev-workflow |
| DC-05 | Every named behavioural branch has an AT at FSPEC | both |
| DC-06 | A remediation is verified by mutation, not by reading the diff | pdlc-workflow-distribution |
| DC-07 | Work that skips a pipeline phase inherits zero review coverage | both |
| DC-08 | An unresolved item needs a named successor surface | both |
| DC-09 | A REQ stays at requirements altitude and carries its own stopping rule | pdlc-workflow-distribution ¹ |
| DC-10 | The PLAN owns what downstream phases would otherwise invent | both |
| DC-11 | Sibling oracles in one checklist share an error contract | pdlc-workflow-distribution ¹ |
| DC-12 | A drift false positive is fixed by rephrasing the document | pdlc-workflow-distribution ¹ |
| DC-13 | Scope-tag accurately — an untagged repo-wide finding reaches nothing | pdlc-workflow-distribution ¹ |

¹ Single-feature promotion, justified as a standing invariant. DC-09: the loop hit the iteration
ceiling twice, 16 rounds, and the rule was verified working once written into the artifact under
review. DC-11: mechanically obvious and cheap to state. DC-12: an anti-regression rule upheld against
four separate attempts to weaken it. DC-13: an invariant of this consolidation mechanism itself — the
under-tagging is precisely what suppresses the second occurrence that would otherwise justify a
two-feature promotion.

**Promoted — decisions.** `DECISIONS-plugin-distribution.md` is the pass's cleanest promotion:
`orchestrate-dev-workflow` raised the plugin→consumer workflow-sync problem as `OQ-05` and carried it
unresolved through all 5 FSPEC iterations, flagging that it would recur for every future
workflow-bearing plugin; `pdlc-workflow-distribution` answered it. The answer is now project-level.
`DECISIONS-test-oracle-mechanics.md` records the tool behaviours (jest's per-file module registry,
istanbul and subprocesses) that make an assertion vacuous regardless of discipline.

**Proposed, not applied — 11 skill-prompt rows.** P-1 (hand the stopping rule to reviewers at
dispatch) has the largest measured cost behind it. P-2/P-3/P-4 overlap queue row 8
`pdlc-review-loop-hardening` — **which the operator set `pending` and moved to `Order 0` on 2026-07-29,
making it the queue's next pickup** — so apply them there rather than twice. P-9 is new to this pass, not from either
LEARNINGS: five shipped skill prompts cite `DC-07`/`DC-08`/`DC-09` of a *different consuming repo's*
constraint file, and this repo now has its own DC-07/08/09 with unrelated content.

**Deferred, with reasons (3):** the additive caller-scoped trailer convention (one feature, no
recurrence — its deciding criterion was promoted as part of DC-01); the Low-carry threshold as an
enforced gate (promoted as guidance in DC-08 only, because mechanical escalation of unresolved
findings is exactly what produced the 16-round fixed point); the booked G-01…G-07 code defects
(feature-local, not cross-feature — belong in a follow-up REQ or row 8, now `Order 0`; **G-02 first**).

**Not deleted:** `docs/pdlc-workflow-distribution/FALSIFICATION-LEDGER.md`. PLAN §3.1 declares it
deliberately retained as the standing falsification record; it was read as input to DC-03.

**Branch note.** This pass was authored on `feat-pdlc-workflow-distribution`, because
`LEARNINGS-pdlc-workflow-distribution.md` existed only there while PR #19 was open. #19 was
**squash-merged** as `1fb6cbe` on 2026-07-29, so `chore-consolidate-learnings-2026-07-29` was rebased
onto `main` and now carries this pass's five files and nothing else.

---

## Pass 2 — 2026-08-19

**Pass id:** `2026-08-19-1`. **Trigger:** manual (`direct`). **Route:** constraints + decisions,
committed on the invoking branch; no PR. **Status:** `promoted`.

**Boundary before this pass:** Pass 1 (2026-07-29), which consumed `orchestrate-dev-workflow` and
`pdlc-workflow-distribution`. Everything below is the un-consolidated remainder. The block that
follows is this file's first `<!-- pdlc:consumed -->` pair, so all text above it is the frozen legacy
region.

<!-- pdlc:consumed 2026-08-19-1 -->
LEARNINGS-pdlc-review-loop-hardening.md
LEARNINGS-pdlc-merge-phase.md
LEARNINGS-pdlc-advisory-tier.md
LEARNINGS-pdlc-consolidation-agent.md
LEARNINGS-pdlc-headless-engine.md
LEARNINGS-pdlc-engine-distribution.md
LEARNINGS-pdlc-plugin-retirement.md
<!-- /pdlc:consumed -->

**Consumed (7 files, all read in full):**

| Feature | Date Completed |
|---|---|
| `pdlc-review-loop-hardening` | 2026-07-30 |
| `pdlc-merge-phase` | 2026-08-02 |
| `pdlc-advisory-tier` | 2026-08-05 |
| `pdlc-consolidation-agent` | 2026-08-11 |
| `pdlc-headless-engine` | 2026-08-12 |
| `pdlc-engine-distribution` | 2026-08-16 |
| `pdlc-plugin-retirement` | 2026-08-18 |

**How this pass ran.** By hand, per the contract in
`pdlc/skills/consolidate-learnings/SKILL.md`. `pdlc-plugin-retirement` (REQ O-8) retired the
workflow-runtime host that loaded `pdlc/workflows/consolidate-learnings.js`; no surviving host runs
it today, and the re-host is tracked as `pdlc-consolidation-rehost` (`docs/_queue/QUEUE.md` Order 24,
`ready: false`). Running by hand bypasses the machinery this skill exists to drive — the log-boundary
read, deterministic `failure-mode-id` derivation, NFR-4 duplicate suppression, the guard-set PR route
and the in-progress marker were all performed by judgement rather than by code. Recorded as a known
degradation of this pass, not as a property of the promotions.

**Promoted — constraints (9).** `docs/_constraints/DOMAIN-CONSTRAINTS.md`, DC-14 … DC-22. Every one
cleared the ≥2-feature bar; feature counts are per-constraint evidence, not mentions.

| ID | Constraint | Features |
|---|---|---|
| DC-14 | An oracle never sources its expected value from the code under test | 6 |
| DC-15 | An oracle that walks a live tree measures the host, not the diff | 5 |
| DC-16 | A gate that decides a phase reads committed state, never a transcript | 4 |
| DC-17 | One section owns each normative question; a restatement is not an amendment | 4 |
| DC-18 | A claim carried by N documents needs an N-document guard | 4 |
| DC-19 | A wave gate proves the suite ran; no PLAN wave ends red | 5 |
| DC-20 | Repo-wide state edited inside one feature's window needs a review lane | 4 |
| DC-21 | An ordering obligation needs a gate at the moment it binds | 3 |
| DC-22 | Two writers on one branch need a lock, not an inference | 2 |

**Promoted — decisions (4 entries across 4 files).**

- `docs/_decisions/DECISIONS-model-availability.md` (new) — `DEC-MODEL-01` (model availability is a
  declared per-seat property with a fallback tier; a 529 degrades the round rather than halting the
  run) and `DEC-MODEL-02` (model fallback is not transport failover; `DEC-ENG-01`/`DEC-ENG-02` stand).
  The second entry exists because `pdlc-headless-engine` deliberately *rejected* runtime transport
  failover, and `DEC-MODEL-01` would otherwise read as reopening it.
- `docs/_decisions/DECISIONS-wave-gates.md` (new) — `DEC-WAVE-01` (`implementation.testCommand` is
  repo-wide state; editing it is the terminal act of a phase, transcribed token-for-token from HEAD,
  with `--testPathIgnorePatterns` line-terminal), `DEC-WAVE-02` (the gate proves the suite ran; the
  un-skip check is per-file), `DEC-WAVE-03` (a red and its green land in the same wave, or the red
  lives outside the gated suite).
- `docs/_decisions/DECISIONS-seam-defaults.md` (new) — `DEC-SEAM-01` (a seam's default is chosen for
  what the consumer does with it, not for reading inert), the direct promotion of
  `pdlc-advisory-tier`'s candidate 7, corroborated by `pdlc-headless-engine` and `pdlc-merge-phase`.
- `docs/_decisions/DECISIONS-review-severity-bars.md` (appended) — `DEC-ERR-04` (an erratum is
  complete only when the reverse grep finds no surviving statement of the corrected claim).

**Proposed, not applied — 12 skill-prompt rows.**
`docs/_decisions/CONSOLIDATION-PROPOSAL-2026-08-19-1.md`, Q-1 … Q-12, plus 5 recorded deferrals.
Q-1/Q-2/Q-3 carry the largest measured cost. Q-11 is the notable one: DC-13 has existed since Pass 1
and under-tagging was filed as a finding in **all seven** LEARNINGS consumed here, so the row
proposes the structural half (per-finding column plus a blocking hook) rather than re-proposing P-10's
guidance, which remains unapplied.

**Suppressed as duplicates (NFR-4) — 4 clusters.** Each recurred in this corpus and each is already
promoted; no new DC or DEC was minted for them.

| Cluster | Suppressed by | Note |
|---|---|---|
| Machine-read output grammar drift (`VERDICT:` missing "with", non-approving verdicts with no `FINDING:` line) | `DC-01` (closed catalogue on emit, total function on receive) | The rule is right and already promoted; what is missing is *emission-side validation*, so this went to proposal row Q-4 instead of a new constraint. |
| Approval carry-forward, severity bars, spec-layer boundary, absorb-don't-route, file-side verdict on the erratum path, superset delta confirmation, cite content not line number, frozen matured rounds | `DEC-CONV-01`, `DEC-SEV-01/02/03`, `DEC-LAYER-01`, `DEC-ERR-01/02/03`, `DEC-DOC-01`, `DEC-FRZ-01` | The `pdlc-consolidation-agent` corpus minted these mid-feature; §5 row 9 asked consolidation to promote them and they are already at project level. Only the genuinely new erratum rule (`DEC-ERR-04`) was added. |
| Scope-tag under-use | `DC-13` | Recurred in all seven. Re-stating it would not change anything — see Q-11. |
| "No `crypto`, no `import()`/`fs`/`process`/`fetch` in the workflow runtime" | `DEC-DIST-01`, `DC-02` | Rediscovered per feature. `pdlc-advisory-tier` sharpened it usefully (no `import(` **token** may survive in a bundle, reachable or not; no C0 control byte except tab/LF/CR) — that sharpening is now enforced by `build-runtime.mjs` and `runtimeBundle.test.js` (RLH-AT-65), so it is mechanised rather than re-promoted. |

**Deferred, with reasons (5).** All recorded in the proposal file's second table: the workflow-script
and engine fixes (`DEC-ERR-02` landing, DoD version derivation, erratum de-duplication,
`appendApprovalAnchors` ordering, orchestrator lock lease) belong to `docs/_queue/QUEUE.md` row 22
`pdlc-halt-hardening-followups`, which is `blocked` and should be confirmed to still charter them;
normalised approval hashing and first-class freeze mode are operator decisions about what "approved"
means, not consolidation promotions; the runtime-primitive prohibition is already mechanised; and
P-9 from Pass 1 is not re-proposed but should be **applied**, since DC-14 … DC-22 lengthen the range
a bare `DC-NN` citation can collide with.

**Open successors this pass did not touch.**
- `pdlc-consolidation-rehost` (queue Order 24, `ready: false`) — the host for this very pass.
- `pdlc-retirement-operator-verification` (queue Order 25) — the `PENDING-OPERATOR` criteria.
- `POSTMORTEM-R-pdlc-review-loop-hardening.md` still carries no `RESOLVED:` marker; the marker is
  human-written only, and Phase R fails closed on that feature until it is set.
- `pdlc-review-loop-hardening` DoD F-2/F-3 (dynamic `import()` in the `checkPrCi` and `defaultGit`
  seams) still have no chartering queue row.

**Branch note.** Authored and committed on `main`, per the skill's Git Workflow section: the pass
writes in the invoking tree, on the branch it is already on, and opens no PR for promotions.

## Pass 3 — 2026-08-27

**Pass id:** `2026-08-27-1`. **Trigger:** manual (`direct`). **Route:** skill contract, since no
workflow host survives the `pdlc-plugin-retirement` sweep. **Status:** `promoted`.

**Boundary before this pass:** Pass 2 (2026-08-19-1). Everything below is the un-consolidated
remainder as of this pass.

<!-- pdlc:consumed 2026-08-27-1 -->
LEARNINGS-pdlc-advisory-wave-gate.md
LEARNINGS-pdlc-learnings-injection.md
LEARNINGS-pdlc-wave-resume.md
LEARNINGS-pdlc-engineering-loop.md
LEARNINGS-pdlc-review-convergence.md
LEARNINGS-pdlc-rcv-budget-stop.md
<!-- /pdlc:consumed -->

**Consumed (6 files):**

| Feature | Date Completed |
|---|---|
| `pdlc-advisory-wave-gate` | 2026-08-20 |
| `pdlc-learnings-injection` | 2026-08-21 |
| `pdlc-wave-resume` | 2026-08-24 |
| `pdlc-engineering-loop` | 2026-08-26 |
| `pdlc-review-convergence` | 2026-08-01 (discarded) |
| `pdlc-rcv-budget-stop` | 2026-08-02 (discarded) |

**Promoted — decisions (3 files).**

- `docs/_decisions/DECISIONS-erratum-routing.md` (new) — `DEC-ERRROUTE-01` (a confirmation round's
  findings mechanically mint erratum items; a finding recorded but unrouted is a routing defect, not
  reviewer discretion), `DEC-ERRROUTE-02` (the erratum channel's round budget scales with the
  altitude/size of the change routed into it), `DEC-ERRROUTE-03` (partial re-grounding — absorbing
  only the named items and not the full interval — is worse than none).
- `docs/_decisions/DECISIONS-loop-termination.md` (new) — `DEC-TERM-01` (convergence is a derivative
  signal — no new ≥Medium finding for N rounds — not verdict-at-cap) and `DEC-TERM-02` (a
  staleness-only round is not a review round; re-filing must deduplicate).
- `docs/_decisions/DECISIONS-test-oracle-mechanics.md` (appended) — `DEC-ORACLE-06` (byte-identical
  text from two review lenses is a generator signature, not independent agreement; synthesized
  findings must be marked machine-synthesized).

**Proposed, not applied — 7 skill/engine rows.**
`docs/_decisions/CONSOLIDATION-PROPOSAL-2026-08-27-1.md`, R1–R7, plus 1 recorded deferral.

**Suppressed as duplicates (NFR-4) — 4 clusters, naming the suppressing DC/DEC in one line each.**

| Cluster | Suppressed by |
|---|---|
| Restated literal counts across N sites drift out of sync | `DC-18` (claim carried by N documents needs N-document guard) |
| Mutation-verification as the DoD floor | `DC-06` (remediation verified by mutation, not diff) |
| Scope-tag under-use / mis-tagging (constraint itself; the fail-closed enforcement mechanism went to proposal row R1) | `DC-13` |
| Unbound deferrals lack a named successor | `DC-08` |

**Deferred: none.**

**Branch note.** Authored and written on `main`, per the skill contract; no workflow host exists to
run this pass end to end after `pdlc-plugin-retirement`, so it was run by hand against the LEARNINGS
files directly.
## Pass 4 — 2026-08-27

**Pass id:** `2026-08-27-2`. **Trigger:** manual (`direct`). **Route:** skill contract, since no
workflow host survives the `pdlc-plugin-retirement` sweep. **Status:** `promoted`.

**Input.** This pass consumes an **external corpus**: the regime-ledger repo's own consolidation
findings, relayed cross-session on 2026-08-27. Nothing in this repo's `docs/` was consumed by this
pass — it therefore carries **no `<!-- pdlc:consumed -->` block**, deliberately. The boundary this log
tracks for *this* repo's LEARNINGS remains Pass 3 (`2026-08-27-1`); no `docs/*/LEARNINGS-*.md` file was
read or should be treated as read for this pass.

**Promoted (2) — one line each, cross-repo bar cleared (≥1 occurrence in each of two unrelated repos).**

- `docs/_decisions/DECISIONS-erratum-routing.md` — `DEC-ERRROUTE-04` (a confirmation round with an
  unchanged document hash pin-checks, it does not re-review).
- `docs/_decisions/DECISIONS-anchor-provenance.md` (new) — `DEC-ANCHOR-01` (a dispatch's quoted hash is
  harness-computed, never agent-transcribed).

**Corroborated, not re-promoted (recorded so the second occurrence isn't lost).** Each of these already
cleared its promotion bar in an earlier pass; this pass's external corpus supplies a second, independent
occurrence and the id is left as-is rather than re-minted.

- `DEC-ERRROUTE-01` — regime-ledger `paper-book-strategy-generalization`: six ERRATUM items routed, none
  amended upstream, the same findings re-filed across four consecutive rounds.
- `DEC-ERRROUTE-03` — regime-ledger: an erratum dispatch naming a subset of items claimed the full
  absorbed version range.
- `DEC-TERM-02` — regime-ledger `iv-snapshot-store-postgres`: PLAN v4–v7+v10–v15 and TSPEC v7–v9 were
  entirely provenance bookkeeping; one squash invalidated 17 in-prose short SHAs at once.
- `DEC-ORACLE-06` — regime-ledger: the pm-review `FINDING:`-grammar gate fail-closed silently twice in
  one feature, invisible to the reviewer that caused it.

**Proposal rows corroborated (still proposals).** R6-remainder — regime-ledger
`regime-swing-confirmation`: DoD dispatched "v3" while v1..v5 already existed on disk. R7 —
same feature: the 5g split trigger fired in Phase R round 3, was answered with in-place revisions,
and the feature ran to 188 cross-review files with six documents at or near the 15-round cap; its
REQ hit 92% of the byte ceiling by round 2.

**Origin.** The regime-ledger findings were relayed cross-session on 2026-08-27 from that repo's own
consolidation pass; their durable home is regime-ledger's harvest summaries (the surviving primary
source post-harvest) and its own same-day consolidation proposal file.

**Branch note.** Authored and written on `main`, per the skill contract.

## Pass 5 — 2026-09-01

**Pass id:** `2026-09-01-1`. **Trigger:** manual (`direct`). **Route:** skill contract, by hand —
no workflow host survives the `pdlc-plugin-retirement` sweep. **Status:** `promoted`.

**Boundary before this pass:** Pass 4 (`2026-08-27-2`). Pass 4 consumed an **external** corpus and
carries no `<!-- pdlc:consumed -->` block by design, so the boundary it set for *this repo's*
LEARNINGS is the one Pass 3 left. Three `docs/*/LEARNINGS-*.md` files carry a `Date Completed` after
it, and all three are consumed below.

<!-- pdlc:consumed 2026-09-01-1 -->
LEARNINGS-pdlc-loop-economics.md
LEARNINGS-pdlc-stats.md
LEARNINGS-pdlc-decision-ledger.md
<!-- /pdlc:consumed -->

**Consumed (3 files, all read in full):**

| LEARNINGS | Date Completed |
|---|---|
| `docs/completed/pdlc-loop-economics/LEARNINGS-pdlc-loop-economics.md` | 2026-08-28 |
| `docs/completed/pdlc-stats/LEARNINGS-pdlc-stats.md` | 2026-09-01 |
| `docs/pdlc-decision-ledger/LEARNINGS-pdlc-decision-ledger.md` | 2026-09-01 |

**Promoted — constraints (5).** `docs/_constraints/DOMAIN-CONSTRAINTS.md`, DC-24 … DC-28. Each names
≥2 unrelated features; feature counts are per-constraint evidence, not mentions.

| ID | Constraint | Features |
|---|---|---|
| DC-24 | A completion claim is discharged by a measured tree transition, not by a report | pdlc-stats, pdlc-loop-economics |
| DC-25 | A recommendation for a mechanical guard is built or explicitly declined — "recorded" is not a state | pdlc-decision-ledger, pdlc-loop-economics (corroborated by pdlc-stats §5.2) |
| DC-26 | An assertion pinned to an untracked file measures the operator's machine, and is vacuous in CI | pdlc-loop-economics, pdlc-stats |
| DC-27 | A numeric literal in a specification carries its provenance class and its measurement basis | pdlc-decision-ledger, pdlc-stats |
| DC-28 | A prose contract about where code may be constructed owes a source-census oracle | pdlc-stats, pdlc-loop-economics |

Three of the five are the strongest shape available to this mechanism — **one feature paid for a
mechanism's absence while another had already shipped it or written the general rule**. DC-26 is the
sharpest: `pdlc-loop-economics` and `pdlc-stats` reached the *same pin*
(`waveResumePreflight.test.js:45-46`) from opposite ends, one finding it red-and-CI-vacuous and the
other finding it red only on the operator's machine, with neither aware of the other. DC-24 and
DC-25 are the two that cost the most measured pipeline time this interval.

**Promoted — decisions (2 entries across 2 existing files).** No new decision file was created.

- `docs/_decisions/DECISIONS-test-oracle-mechanics.md` (appended) — `DEC-ORACLE-07` (a paired
  conjunct, and any model standing in for the production structure, must be shown red on the arm it
  runs on; discharged by mutation to a stub, not by inspection). Promoted because both instances
  **satisfied DC-14's existing checklist and still had zero power** — the tautological cross-arm
  conjunct (`X == X` on the flag-off run) and the `Map`-based model stand-in that deduplicates by
  construction. The entry names the residue DC-14 and DC-03 leave open rather than restating them.
- `docs/_decisions/DECISIONS-plugin-distribution.md` (appended) — `DEC-DIST-08` (adding to the
  vendored `MODULE_NAMES` module class is a spec-level change; a feature pays the co-change sweep
  deliberately or lands in-file, and records which). Three features hit the same coupling from three
  directions: `pdlc-loop-economics` and `pdlc-decision-ledger` independently declined the `lib/`
  module and took the serial-wave / sentinel-block cost, `pdlc-stats` paid the sweep and produced
  DC-23. DC-23 governs the sweep's *scope*; DEC-DIST-08 governs the decision to take it.

**Corroborated, not re-promoted (NFR-4 duplicate suppression, applied by hand).** Each already
cleared its bar in an earlier pass; this pass's corpus supplies further independent occurrences and
the id is left as-is rather than re-minted.

| Existing id | Corroborating occurrence |
|---|---|
| `DC-03` / `DC-06` | Mutation as the verification floor, twice: `pdlc-loop-economics` mutation-tested the classifier down to a no-op stub to prove F-9's property non-falsifying; `pdlc-decision-ledger` mutation-verified DoD F-1…F-3. |
| `DC-08` | `pdlc-loop-economics` §5.1 (three vestigial SKILL.md sentences — a declared-but-unbound deferral, no queue row) and `pdlc-stats` §5.2 (the dispatch-side TSPEC pin `f2261510…` carried `DEFERRED` for four rounds, closed on the document's side only, retired by nothing in the pipeline). |
| `DC-13` | Third and fourth consecutive pass. `pdlc-decision-ledger`: **exactly one** `Cross-Feature` tag across 129 harvested reviews. `pdlc-stats`: ~22 of 127 artifacts mention it at all, nearly all in the first six DECISIONS rounds — the signal degrades exactly when the loop gets long enough to need it. Both features' §2 had to be re-routed by inference. The *diagnosis* is new and went to proposal row S-3, not to a re-promotion. |
| `DC-14` | `pdlc-stats` §2 — two independent reviewers, two artifacts: te-FSPEC F-05 on AT-10/AT-13, and a PROPERTIES round on PROP-PBT-04 (`statsProperties.test.js:262-264` re-derives byte totals with `stats.mjs:210-212`'s own formula). This is DC-14's implementation-echo bullet verbatim. AT-11 is the counter-example that pins the literal `2`. |
| `DC-18` | `pdlc-decision-ledger` §2 (the erratum channel routes loci, not claims — a claim at two sites routed once leaves the twin asserting retired text; and a baseline citation-anchor bumped v1.0 → v1.2 mid-feature obliges re-derivation by every citing claim, not a `Version` line edit) and `pdlc-loop-economics` §2 F-6 (README/CLAUDE.md disclose only `learningsInjection` while OPERATIONS.md carries `cascade.pinCheck` and `review.derivativeStop`). Mechanisms went to S-6 and S-9. |
| `DC-19` / `DEC-WAVE-02` | `pdlc-decision-ledger` §4.7 — wave 3's gate went red on an orphan-freedom oracle because a `test.skip` landed in a swept module without a sink registration. The gate proved the suite ran; the *invisible skip sink* interaction went to S-12. |
| `DC-20` | `pdlc-decision-ledger` §2 — `learningsInjection` cited as precedent on the wrong axis: FSPEC BR-9 promised "once per review dispatch" while the shipped precedent computes once per *episode*, outside the dispatch loop. Re-derive at HEAD at a verified file and line; the axis-specificity refinement is noted in the proposal, not minted. |
| `DC-21` | `pdlc-decision-ledger` §4.8 — T-11 (wave 4) and T-18 (wave 10) failed the wave gate on the same multi-name-ownership-cell defect, and T-18's commit message records that T-11 had **predicted** it. Prediction written, not gated. |
| `DC-23` | `pdlc-stats` §2 — raised as `Cross-Feature` in te-DECISIONS v1, v2, v3, v4 and v6, five rounds circling one rule; landed in-feature as DC-23. The generalisation question is deferred (D-3), not re-minted. |
| `DEC-ERRROUTE-02` | `pdlc-decision-ledger` §4.3 — two instances of a budget sized by items rather than by coupled chains: Phase D's one re-derivation performed in three instalments across rounds 5–7, each instalment minting the next round's finding; Phase PR round 9 landing two verbatim substitutions and dropping the one item that required designing an oracle. The dimension-sharpening is deferred (D-6). |
| `DEC-ERRROUTE-03` | `pdlc-stats` §3 — the null-round shortcut ("the erratum landed and was routed") was proposed and rejected **twice**, by two roles, on DEC-ERR-03 grounds; in one round the only finding was reachable *only* by re-reading upstream at HEAD. LEARNINGS calls it "the single most load-bearing rejected shortcut in the feature". |
| `DEC-ERRROUTE-04` | `pdlc-stats` §4 — a third independent feature. FSPEC 12 / DECISIONS 12 / TSPEC 11 rounds were driven not by disagreement but by four erratum routes (`ERRATUM: REQ` ×2, `ERRATUM: TSPEC` ×2), each re-opening every pinning downstream document whether or not its own bytes moved. **`pdlc-loop-economics` is deliberately not counted here:** it is the feature that *built* `cascade.pinCheck`, so its account is self-reference, not corroboration. |

**Proposed, not applied — 13 rows.** `docs/_decisions/CONSOLIDATION-PROPOSAL-2026-09-01-1.md`,
S-1 … S-13, plus 15 recorded deferrals. S-1/S-2 (the measured tree transition, and who may cause it)
carry the largest measured cost — two features lost review rounds to a delivery step. S-3 is the
notable one: DC-13 has been re-recorded in **every pass since Pass 1**, Pass 2's Q-11 and Pass 3's R1
both proposed structural fixes and neither landed, and this pass finally has the *diagnosis* —
`Scope:` names two different fields, a document-level one that `check-scope-field` warns about and a
per-finding one that harvest routing depends on, so a reviewer can satisfy the hook while starving
the harvest. S-7 is DC-25's own founding evidence and would be the first mechanical gate on the
erratum protocol.

No `pdlc/skills/*/SKILL.md` file was edited by this pass. Every skill-prompt, hook and engine change
appears only as a proposal row.

**Deferred, with reasons (15).** All recorded in the proposal file's second table, D-1 … D-15. The
three worth naming here: **D-3**, widening DC-23's phrasing to cover every enumeration in a co-change
set, is deferred deliberately as an amendment to an already-promoted constraint — per DC-17 that
belongs to the section that owns the question, not to a drive-by edit mid-pass, and the reading is
recorded for the next pass. **D-2**, the finding-identity collision between `REQ-DECLEDGER-06` and
`DEC-LOOPECON-06`, is an open question with no canonical identity declared; promoting it would mint
a decision nobody has taken, and it happens to name a collision *between two of this pass's own
in-scope features*. **D-4**, the three vestigial SKILL.md sentences, is a textbook DC-25 instance
that this pass cannot close, because it may not edit `SKILL.md`.

**Open promotions across the whole log: 0.** No entry in this file — Pass 1 through Pass 5 — carries
a `failure-mode-id:` line, so there is no open promotion to hand forward. `pdlc-decision-ledger` §5
independently checked this against the log and its own FSPEC §8.3/§8.4 list and reports the same
empty result.

**Open successors this pass did not touch.** Carried from Pass 2 and still unclosed:
`pdlc-consolidation-rehost` (queue Order 24, `ready: false`) — the host for this very pass;
`pdlc-retirement-operator-verification` (queue Order 25); the missing `RESOLVED:` marker on
`POSTMORTEM-R-pdlc-review-loop-hardening.md`. New this pass: D-4's three SKILL.md files have no
queue row, and S-7's routed-locus diff gate has none either.

**Branch note.** Authored and written on `main`, per the skill's Git Workflow section: the pass
writes in the invoking tree, on the branch it is already on, and opens no PR for promotions. Run
**by hand**, per the skill contract — `pdlc-plugin-retirement` (REQ O-8) retired the workflow-runtime
host that loaded `pdlc/workflows/consolidate-learnings.js` and the re-host is still queued but not
ready, so the log-boundary read, the deterministic `failure-mode-id` derivation and the NFR-4
duplicate suppression were all performed by judgement rather than by code. Recorded, as in Passes 2
and 3, as a known degradation of *this pass*, not as a property of the promotions.
