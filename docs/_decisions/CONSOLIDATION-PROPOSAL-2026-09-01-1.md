# Consolidation Proposal — 2026-09-01-1

Pass 5 of `consolidate-learnings`. Consumed `pdlc-loop-economics`, `pdlc-stats` and
`pdlc-decision-ledger`. Constraints and decisions were **promoted** in the same pass
(`docs/_constraints/DOMAIN-CONSTRAINTS.md` DC-24 … DC-28;
`docs/_decisions/DECISIONS-test-oracle-mechanics.md` DEC-ORACLE-07;
`docs/_decisions/DECISIONS-plugin-distribution.md` DEC-DIST-08). This file carries the rows that
would change a **skill prompt, hook or engine behaviour** — those are *proposed only*. No
`pdlc/skills/*/SKILL.md` file was edited by this pass.

Rows are `S-1 … S-13`, continuing the per-pass id convention (`P-*` Pass 1, `Q-*` Pass 2, `R-*`
Pass 3).

---

## Proposed changes

| # | Source LEARNINGS | Target skill | Proposed change | Rationale |
|---|---|---|---|---|
| S-1 | `pdlc-stats` §4; `pdlc-loop-economics` §4.5, §5.5 | `se-implement`, `tech-lead`, `tech-lead-python`, `dod-verify` | Before an agent may report a remediation or a wave complete, require it to assert and quote a **measured tree transition**: `git status --porcelain` empty over the task's pathspec, the branch tip SHA before and after, and (where a downstream check reads the remote) that the tip is pushed. A verifier is re-dispatched only after that assertion. | DC-24. `pdlc-stats` spent one whole DoD round *and* one whole CR round on a delivery step with zero content change — `CODE_REVIEW-v3`'s only finding was that round 2's remediation (9 modified files, `+65/−80`, plus an untracked 178-line new oracle) had never been committed. `pdlc-loop-economics` filed the same shape as F-8 at **both** DoD rounds. This is the cheapest available guard and both LEARNINGS name it. |
| S-2 | `pdlc-loop-economics` §4.5 | `tech-lead`, `tech-lead-python`, `orchestrate-dev` (subagent dispatch templates) | Add a standing "do **NOT** commit or push; report a measured tree state instead" clause to every implementation-subagent dispatch in a multi-agent/gatekeeper flow, with the gatekeeper owning the commit. | One agent committed and pushed `146e2b3ba` without authorization mid-feature. LEARNINGS records that this "recurs across features (see prior LEARNINGS' equivalent note) and is not yet a standing template clause" — i.e. it is exactly the DC-25 "recorded candidate" state. Pairs with S-1: S-1 says *measure the transition*, S-2 says *who is allowed to cause it*. |
| S-3 | `pdlc-decision-ledger` §4.6, §5.5; `pdlc-stats` §4 | `pm-review`, `se-review`, `te-review`, `harvest-learnings`, `hooks/scripts/check-scope-field.sh` | Resolve the **`Scope:` name collision**. Two different fields share the token: a *document-level* `**Scope:**` header, which is what `check-scope-field` asks for and warns about, and a *per-finding* scope tag (`Local` / `Cross-Feature` / `Process`), which is what harvest routing depends on and which nobody emits. Rename the per-finding tag, and emit it **inside the `FINDING:` grammar the engine already parses fail-closed**, so the reviewer cannot satisfy the hook while starving the harvest. | This is the missing *diagnosis* behind DC-13, which every pass since Pass 1 has re-recorded and which Pass 2's Q-11 and Pass 3's R1 both proposed structural fixes for without landing one. The measurements are now stark and independent: `pdlc-decision-ledger` carried **exactly one** `Cross-Feature` tag across **129** harvested reviews; `pdlc-stats` had ~22 of **127** artifacts mention it at all, nearly all in the first six DECISIONS rounds. Everything in both features' §2 had to be re-routed by inference at harvest. |
| S-4 | `pdlc-stats` §4, §5.3 | `harvest-learnings` | Reconcile `SKILL.md` on what it harvests: `:77` lists `CROSS-REVIEW`, `CODE_REVIEW`, `POSTMORTEM`, while `:126` and `:59` disagree, and NG-6 (`:79-80`) sets a trap. Fix at the source. | The self-inconsistency produced **two falsified premises across rounds 6 and 7** of one feature's review loop. Every feature pays this again until the source is fixed. |
| S-5 | `pdlc-stats` §4, §5.4 | `se-review`, `harvest-learnings`, `orchestrate-dev` | Pick **one** Phase CR doc-type token, or teach every sweep both. Today software-engineer writes `CROSS-REVIEW-{role}-IMPLEMENTATION-v{N}.md` while product-manager and test-engineer write `CROSS-REVIEW-{role}-REVIEW-v{N}.md` — for the same phase over the same artifact. | Two tokens for one phase means any count, sweep, harvest or guard keyed on doc-type must know both, and a hook that knows one silently under-scopes. Both consumed features exhibit it (`pdlc-decision-ledger` carries `CROSS-REVIEW-{pm,te}-REVIEW-v{1,2}.md` *and* `CROSS-REVIEW-software-engineer-IMPLEMENTATION-wave3-v1.md`). |
| S-6 | `pdlc-decision-ledger` §2, §5.3 | `orchestrate-dev` erratum dispatch; `pm-review`, `se-review`, `te-review` | Give each routed erratum item an optional **`retired-text:`** field, grepped over the **whole document** and failing closed when the retired text survives anywhere. | The erratum channel routes **loci**, not **claims**. A claim stated at two sites and routed once leaves the twin asserting retired text — observed at PLAN TE-2 and again across DoD F-6 → F-7 → F-8, where the round's sweep covered the "disclosure family" and missed the sibling site. DC-18 already states the invariant (a claim carried by N documents needs an N-document guard); what is missing is the mechanism. Locus-shaped routing under-scopes *systematically*, and worse the larger the document. |
| S-7 | `pdlc-decision-ledger` §4.2, §5.2 | `orchestrate-dev` / engine | Build the **routed-locus diff gate** (`erratum-routed-item-unlanded-halt`): at `REVISION-COMPLETE`, halt when a routed locus is byte-unchanged. The routed list already carries loci, so the input exists. | DC-25's founding evidence. Named in `POSTMORTEM-PR` edition 1, recorded as a "standing pipeline-improvement candidate" in a memory note, and then the identical failure recurred five rounds later — two confirmer dispatches, a second halt, a second post-mortem edition on the same phase. The pipeline has a document-oracle harness, a fail-closed wave gate and a finding-grammar hook; this one erratum-protocol defect, observed twice in one feature, has no gate at all. |
| S-8 | `pdlc-decision-ledger` §2, §5.4 | `te-review`, `se-review`, `pm-review`, `pdlc/OPERATIONS.md` | Add a **cross-arm falsifiability** entry to the oracle checklist: a conjunct paired with an absence assertion must be shown red on the arm it runs on, and a property that models the production structure must be mutation-verified against a stub. Ship T-10a conjunct 3 as the worked example. | DEC-ORACLE-07. The existing checklist was *obeyed* and the oracle still had zero power — `report`'s key set asserted `set-equal` to the flag-off key set, on the flag-off run, is `X == X`. A checklist that stops one step short of falsifiability licenses vacuous conjuncts. |
| S-9 | `pdlc-loop-economics` §2 F-6, §5.3 | `se-implement`, `ship-pr`, `CLAUDE.md` / `pdlc/README.md` | Require that any new `.claude/pdlc.config.json` block lands in the `README.md`/`CLAUDE.md` config disclosure **in the same commit** as its `pdlc/OPERATIONS.md` documentation — or state explicitly in both files that `pdlc/OPERATIONS.md` is the sole config catalogue and the summaries are non-normative. | `cascade.pinCheck` and `review.derivativeStop` shipped documented in OPERATIONS.md and the example config, and named in neither README nor CLAUDE.md. Accepted as advisory rather than blocking, correctly — but LEARNINGS records the README/CLAUDE.md-lags-OPERATIONS.md pattern as having "now recurred across features". DC-18 covers the invariant; this row supplies the guard. |
| S-10 | `pdlc-loop-economics` §4.1 | `dod-verify` | Require a DoD finding to state whether the defect fires on **every** instance of the code path or only some, and to make severity follow from that. | F-1 earned High because it was defect-by-construction: pin-check eligibility is literally `c.record.hash === c.docHash`, the identical condition the PASS path's re-anchor guard short-circuits on, so the append is skipped on **100%** of PASS results. That framing is what distinguishes it from a narrower conditional bug, and today the reader has to infer it from a code excerpt. |
| S-11 | `pdlc-decision-ledger` §4.5, §5.10 | `dod-verify`, `tech-lead` | Adopt a **delta-coverage floor scoped to the feature's own introduced lines**, and apply it to **remediation diffs** as well as to original work. | The feature's ~530 new lines were measured only inside `orchestrate-dev.js`'s ~15k-line whole under `--per-file --branches 85` — a floor that a large host file dilutes to nothing. Phase CR Q-02 stood unanswered across two rounds. Independently: DoD v1 opened on a fully green branch (166/166 suites, 5,258 passing, `build-runtime --check` in sync) and still found three unwired integrations, and the **remediation itself** introduced two never-taken `??` fallbacks that only a delta gate caught. |
| S-12 | `pdlc-decision-ledger` §4.7, §4.8 | `se-author` (PLAN), `tech-lead` | (a) A PLAN task that places a `test.skip` in a **swept surface** must enumerate its blast radius in the placement instruction. (b) A task's File Ownership Manifest cell must name exactly one file per pathspec row, since the wave gate's `git add` pathspec derivation does not consume a multi-name cell. | (a) Wave 3's gate went red on the orphan-freedom oracle because a `test.skip` landed in a swept module without a sink registration — an invisible skip sink. (b) T-11 (wave 4) and T-18 (wave 10) failed the wave gate on the **same** defect, both recovered by hand-committing a verified tree; T-18's commit message records that T-11 had already *predicted* it. Prediction written, not gated. |
| S-13 | `pdlc-stats` §4 | operator decision on `.claude/pdlc.config.json` → `cascade.pinCheck.enabled` | Turn `cascade.pinCheck` **on**, or record the decision not to and the measurement that would change it. | The three highest-iteration loops (FSPEC 12, DECISIONS 12, TSPEC 11) were not driven by disagreement about the feature — they were driven by four erratum routes (`ERRATUM: REQ` ×2, `ERRATUM: TSPEC` ×2) each re-opening every pinning downstream document. This is the exact workload `cascade.pinCheck` was built for and it ships off by default. LEARNINGS calls this feature "the strongest available argument for turning it on". Corroborates `DEC-ERRROUTE-04` with a third independent feature. |

---

## Deferred, with reasons

| # | Item | Source | Why not promoted |
|---|---|---|---|
| D-1 | **Cloning a wired analogue silently drops the wiring** — observability disclosure fields (`sink.ruleInputs`, `_log`) are part of the contract being copied, not decoration; `dod-verify`'s "unwired integration" criterion should be checked *against the analogue*. | `pdlc-decision-ledger` §2 | Single feature. Three DoD findings of one shape is real recurrence *within* a feature, which the bar does not count. Not invariant-grade (no security/data-integrity/regulatory standing). A second feature that clones a composition root promotes it; until then it is a `dod-verify` criterion change needing a human decision. |
| D-2 | **Standing shipped contracts cannot be silently re-keyed** — `REQ-DECLEDGER-06`'s finding-identity triple vs `DEC-LOOPECON-06`'s shipped identity (`orchestrate-dev.js:6746-6780`, open-finding `:9625-9668`). Suggested home: `DECISIONS-review-severity-bars.md`. | `pdlc-decision-ledger` §2, §5.8 | This is an **open question**, not a settled decision: LEARNINGS §5.8 asks someone to "name one consumer of the identity" and neither identity is declared canonical. Promoting it would mint a decision no one has taken. Operator adjudication. Note it names a collision *between two of this pass's own in-scope features*, so it will not go stale quietly. |
| D-3 | **Widen DC-23's phrasing to cover *every* enumeration in a co-change set**, not the four instances that happened to be caught. | `pdlc-stats` §2, §5.6; te-DECISIONS v2/v3 | Deferred deliberately as an **amendment to an already-promoted constraint**, which per DC-17 belongs to the section that owns the question rather than to a drive-by edit mid-pass. Reading recorded for the next pass: DC-23's *Applies to* line already generalises ("co-change sweeps over any vendored/packaged module class") and its membership test is already a rule, not an instance list — but its title and worked example are module-scoped, and widening "module class" to "any enumeration" changes its meaning materially. `DEC-DIST-08` (promoted this pass) covers the decision side of the same coupling and is the cheaper half. |
| D-4 | **Three vestigial "append/transcribe anchor verbatim" sentences remain live in review `SKILL.md` files**, describing a dispatch instruction the engine no longer issues (per DEC-LOOPECON-07's own finding). | `pdlc-loop-economics` §4.3, §5.1 | Deletion was explicitly scoped out by REQ NG-2 as "a permitted follow-up outside this feature", with no queue row and no successor REQ — DoD v1 §3 and v2 §4 both flag it as a **declared-but-unbound deferral**. This pass may not edit `SKILL.md`, so it cannot be closed here. It is a textbook DC-25 instance and needs a queue row naming the three files. |
| D-5 | **Decide what the advisory tier records, or stop paying for it** — `ADVISORY-pdlc-decision-ledger.md` and `docs/_queue/ESCALATIONS.md` record four escalations (A6 at waves 1, 3, 9; one A4), all `Disposition: escalated / budget-exhausted`, `Root cause: unclassified`, empty **Diagnosis**, `(none)` **Evidence**. Three of the four coincide with real wave halts that were diagnosed by hand. | `pdlc-decision-ledger` §4.9 | Single feature, and the decision is about whether a shipped tier earns its cost — an operator/product call, not a constraint. The concrete sub-proposal (capture the wave's failing output, or suppress the record) is worth taking to a REQ. |
| D-6 | **Erratum-round budgets should be sized by *independent items*, not by coupled chains, and not at all for items needing *invention*.** | `pdlc-decision-ledger` §4.3 | Corroborates `DEC-ERRROUTE-02` (the budget scales with the altitude/size of the change routed) rather than replacing it; this sharpens the *dimension* from size to independence. Single feature, two instances (Phase D rounds 5–7, where one re-derivation was performed in three instalments and each instalment minted the next round's finding; Phase PR round 9, where two verbatim substitutions landed and the one item requiring an *oracle to be designed* was dropped). Re-propose as an amendment on a second feature's evidence. |
| D-7 | **May a Phase CR loop be terminated by the DoD channel?** `CROSS-REVIEW-software-engineer-IMPLEMENTATION-v3.md` shipped `VERDICT: Needs revision` with an open High and no v4 was ever written; the finding *was* closed, by `CODE_REVIEW-v4`. | `pdlc-stats` §1, §5.5 | Undecided by design, not by omission. Today the CR record reads unresolved while the defect is closed, which is a **provenance** problem the next reader inherits. Operator decision about the phase graph; a consolidation pass should not invent it. |
| D-8 | **A constraint that cites its own dispatch as authority is unverifiable** — C-2 grounded the `docs/completed/{feature}/` lookup in "the REQ's own dispatch input", which no later reader or test can open, while verifiable authorities existed and went uncited. | `pdlc-stats` §4 (pm F-12) | Single feature, one occurrence, and adjacent to both DC-14's "a claim with no citable oracle has no expiry date" and DC-20's re-derive-at-HEAD rule. A genuinely good invariant ("an authority a later reader cannot open is not an authority") — record and promote on a second occurrence. |
| D-9 | **Where a feature substitutes direct authoring for the engine's review loop, red-test coverage of the exact scenario the headline outcome depends on is the only safety net.** | `pdlc-loop-economics` §4.2 | Single feature, and the direct-authoring/gatekeeper mode is itself rare — the constraint would be promoted against a mode that may not recur. The evidence is strong (DEC-LOOPECON-10's collision with the 2026-08-08 high-only shortcut would have made `converged-by-derivative-stop` dead code, and it was T-09's red test, not any review round, that caught it), so this is worth re-checking the next time a feature runs without cross-review rounds. |
| D-10 | **Documents grew during the phase meant to tighten them** — PLAN 67 KB → 83 KB, TSPEC 129 KB → 150 KB, PROPERTIES 96 KB → 110 KB; single-siting discipline was applied to the payload but not to the prose asserting the payload (three PLAN sites say "stated elsewhere" *while restating the arithmetic in the same sentence*). | `pdlc-decision-ledger` §4.11 | Single feature, and adjacent to DC-17 (one section owns each normative question; a restatement is not an amendment). The novel half — that "single-sited" was read as a property of the payload and not of the prose about it — is a sharpening of DC-17 worth one more occurrence before it is minted. |
| D-11 | **Full-suite runs are load-flaky, and the flake is in nested-jest children** — 1 red in 4 full-suite runs at `decisionLedgerLoop.test.js:404`, green on re-run; wave-3 independently recorded a jest worker process that "failed to exit gracefully" at 118.8 s. | `pdlc-decision-ledger` §4.10 | Test-infrastructure defect, not a constraint or a decision. Belongs in a queue row against the suite, and needs a measurement (is it the nested-jest child, or the skip-join?) before anything can be written down as a rule. |
| D-12 | **`documentOracles.test.js`'s namespace-exclusion mechanism generalises to a third namespace** (`learnings*`, `waveResume*`, now `loopEconomics*`). | `pdlc-loop-economics` §4.7 | A positive precedent, not a constraint — nothing failed. Recorded here so the next feature adding a batch of new top-level test files cites it rather than re-inventing an exclusion. |
| D-13 | **Phase CR's `reviewLoop` call site (`orchestrate-dev.js:17616`) is a declared, not accidental, omission from M3's derivative-stop threading** — `recordPhase` hardcodes `Approved` for that phase. | `pdlc-loop-economics` §5.4 | No action needed; recorded verbatim so a future feature touching Phase CR's review model does not mistake the omission for an oversight. Not a promotion. |
| D-14 | **No silent tool-denial evidence appears anywhere in the corpus** — all 127 `pdlc-stats` artifacts searched for permission-denial / silent-denial / tool-denial language; none found. | `pdlc-stats` §4 | An absence, and LEARNINGS correctly reads it as ambiguous: either no denials occurred, or they occurred and the process left no trace. Nothing promotable until the two are distinguishable; the sub-proposal (reviewers record a denial when one occurs) is speculative without a single observed instance. |
| D-15 | **One subagent reported disregarding an injected "stop using tools" instruction found inside compaction/expansion artifact content**; investigated, the instruction was untrusted embedded content, the subagent treated it as data, no repo-level impact. | `pdlc-loop-economics` §4.6 | Recorded as a positive signal (injection resistance held), not a defect. Carried forward so the trace is not lost at harvest. |

---

## Corroborated, not re-promoted

Listed in full in `docs/_decisions/.consolidation-log.md`'s Pass 5 entry. Summary: `DC-03`, `DC-06`,
`DC-08`, `DC-13`, `DC-14`, `DC-18`, `DC-19`, `DC-20`, `DC-21`, `DC-23`, `DEC-ERRROUTE-02`,
`DEC-ERRROUTE-03`, `DEC-ERRROUTE-04`, `DEC-WAVE-02`. Each already cleared its bar in an earlier
pass; this pass's corpus supplies further independent occurrences and the ids are left as-is rather
than re-minted (NFR-4).
