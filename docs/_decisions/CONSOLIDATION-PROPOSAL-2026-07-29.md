# Consolidation Proposal — 2026-07-29

Skill-prompt changes proposed by the first `consolidate-learnings` pass over
`LEARNINGS-orchestrate-dev-workflow.md` (2026-06-02) and
`LEARNINGS-pdlc-workflow-distribution.md` (2026-07-29).

**Nothing here has been applied.** Agents proposing changes to the prompts that govern agents pass
through human judgment. Review each row and apply or reject it.

**Read P-1 through P-4 first.** They are the rows with measured cost behind them — 15 rounds of correct
diagnosis producing zero code change, and a 16-round REQ loop — and P-2/P-3/P-4 overlap with queue row
8 (`pdlc-review-loop-hardening`), which already has an authored REQ.

| # | Source LEARNINGS | Target skill | Proposed change | Rationale |
|---|---|---|---|---|
| **P-1** | pdlc-workflow-distribution §1a, §2 | `pm-review`, `se-review`, `te-review` | Hand the REQ-scope stopping rule to reviewers **at dispatch**, in the prompt itself — not by pointing at `DOMAIN-CONSTRAINTS.md` DC-09. Add to each review skill's REQ section: a round whose blocking findings are all implementability/oracle defects means approve-and-route-downstream; "this AC has no oracle" is closable by deferring the oracle to TSPEC; two consecutive rounds of non-decreasing blocking count is a fixed point, and a growing document with a flat count is stronger evidence; distinguish plateau from churn explicitly and pre-commit to escalating. | The single highest-value row. The rule **demonstrably worked** — but only after a human pasted it into the REQ under review, where both reviewers then cited it by name in all four converging rounds. A rule in a constraints file the reviewer may or may not load did nothing for ten rounds. Cost of its absence: ~10 of 16 REQ rounds and over half the feature's entire review budget. |
| **P-2** | pdlc-workflow-distribution §1b, §2 | `orchestrate-dev` SKILL.md + `pdlc/workflows/orchestrate-dev.js` | Derive the review iteration index from the artifacts: index = `1 + max(N)` over `CROSS-REVIEW-{role}-{doc}-v{N}.md` present on the branch; derive the reviewer's delta baseline from the same computation; **refuse to write a cross-review path that already exists** (hard error, not a warning). A version-bump-only edit is not a revision round. | `reviewLoop` today always starts at 1 and does no directory listing at all. Round 16 was dispatched as "iteration 4" telling the reviewer to read `-v3` and write `-v4` while `v1..v15` sat committed. Both reviewers detected the skew **every single round** and refused; had any complied, committed review history would have been destroyed. Diagnosed at round 3 with correct routing attached, escalated by two post-mortems, `git log` shows no commit through round 16. **Overlaps queue row 8 H-1** (that row became `Order 0` on 2026-07-29). |
| **P-3** | pdlc-workflow-distribution §1a(iii), §2 | `orchestrate-dev` SKILL.md + `pdlc/workflows/orchestrate-dev.js` | Make a post-mortem an **input**, not just an output. On writing `POSTMORTEM-{phase}-{feature}.md`: set the queue row to `halted` **and commit it** in the same commit; refuse to re-enter that phase while an unresolved post-mortem exists on the branch; read any existing post-mortem at phase entry and surface its Recommendation section to the phase's agents. | Three mechanisms failed at once and the cost was five wasted rounds: the queue row was never set `halted`, so `orchestrate-queue` saw `pending` + `ready: true` and re-picked the feature; **no skill reads post-mortems at phase entry**, so all five P0/P1 recommendations were invisible to the run that followed; and the harness could not tell how many rounds it had run. "A post-mortem no agent consumes is not an escape hatch — it is one more artifact the loop writes on its way around again." **Overlaps queue row 8 H-2** (now `Order 0`). |
| **P-4** | pdlc-workflow-distribution §4d, queue row 8 H-3/H-4 | `pm-author`, `se-author`, `te-author`, `orchestrate-dev` | Make document authoring **incremental and resumable**, and add an **approved-phase skip**. | The runtime's 180 s no-progress stall watchdog is not configurable from this repo. Phase F of the 2026-07-28 run spent ~71 minutes and ~1.34 M subagent tokens on six identical `pm-author` attempts, each killed mid-`Write`, producing **zero bytes**. Separately, re-entering the pipeline to reach Phase F would have re-run all four already-approved Phase-R rounds at Opus rates, risking a `needs revision` on a settled document. **Overlaps queue row 8 H-3/H-4 — as of 2026-07-29 that row is `pending` at `Order 0`, i.e. the queue's next pickup.** |
| **P-5** | orchestrate-dev-workflow §4a, §6a | `te-review` | In the FSPEC review checklist, replace the general AT-presence check with: "For every named behavioural branch in §7 (error flows) and every explicitly conditional path (PASS/FAIL, skip/full, crash/non-crash, timeout, enabled/disabled), verify an acceptance test exists in §8. A branch with no AT is a **High** finding." | The only pattern in the corpus that recurred *verbatim* on every lapse. It dominated all 5 FSPEC rounds of `orchestrate-dev-workflow`, then reproduced exactly as predicted when Phase PUB was added — 9–10 branches, zero `AT-SHIP-*` tests. Now DC-05, but the checklist is where a reviewer actually looks. |
| **P-6** | orchestrate-dev-workflow §4b | `se-author` (TSPEC) | For any loop with a cap, the pseudocode MUST show (a) where the counter increments, (b) where the cap check fires, and (c) a counter-value-to-action table — mutually consistent. | Off-by-one in loop boundary conditions is the most common pseudocode ambiguity in iterative algorithms. TSPEC-LOOP-03 said 4 optimizer calls before cap, FSPEC §1.8 said 5; the contradiction survived to a v2 High finding purely because the positions were implied by prose. |
| **P-7** | orchestrate-dev-workflow §4c, §4d, §4e; pdlc-workflow-distribution §4g, §4b | `se-author` (DECISIONS + PLAN), `te-review` (DECISIONS) | Add the DC-10 obligations to `se-author`: a `Testability:` field on every DECISIONS entry; a named fixture/harness for every Integration-level property; path + export signature + enforcement AC for every canonical test double; a one-line lifecycle disposition for every tracked artifact the PLAN introduces; path-scoped halt-state recovery; a "why" for any coverage floor knowingly unmet. In `te-review`, flag a decision that constrains a testing approach with no testability note as **Medium**. | Six independent findings across both features, each of the same shape: the PLAN or DECISIONS omitted something, and the next phase invented it inconsistently or dropped it silently. `orchestrate-dev-workflow` alone lost 3 High-severity test-infrastructure items at implementation to this. |
| **P-8** | pdlc-workflow-distribution §4a, §4f; orchestrate-dev-workflow §4a | `se-implement`, `te-author`, `dod-verify` | Make `FALSIFICATION-LEDGER.md` a standing pdlc convention with its domain stated as **every load-bearing assertion**, not the enumerated property set (DC-03), and add to `dod-verify`: verify a remediation by mutation, recording the mutation and the red test ids — never by reading the diff (DC-06). | The ledger discipline worked on all 63 `PROP-*` properties and **every dead oracle found in Phase CR and DoD was outside that enumeration** — four of them, including the artifact-freshness gate the whole `dist/` discipline rests on. Separately, mutation-verification is how DoD-11 was caught: a fix for a broken oracle that was itself a broken oracle. Reading the diff would have passed it. |
| **P-9** | this pass | `pm-review`, `se-review`, `te-review`, `se-implement`, `se-author` | Disambiguate the six existing `DOMAIN-CONSTRAINTS.md DC-07 / DC-08 / DC-09` citations across five shipped skill prompts. They refer to a **different consuming repo's** constraint file and were written when no such file existed here; this repo now has its own DC-07/08/09 with unrelated content. Either qualify the citation (name the repo) or drop the number and cite the constraint by title. | A reader in this repo following "`docs/_constraints/DOMAIN-CONSTRAINTS.md` DC-07" now lands on an unrelated constraint. Cheap to fix, silently misleading if not. Sites: `pm-review:105`, `te-review:131,133`, `se-review:118`, `se-implement:250`, `se-author:67`. |
| **P-10** | pdlc-workflow-distribution §4f, §1b | `pm-review`, `se-review`, `te-review`, `harvest-learnings` | Strengthen the `Scope:` guidance: a finding naming a repo-wide mechanism, a sibling skill, or the pipeline itself is `Process` or `Cross-Feature`, **never** `Local`. State the consequence in the prompt — an untagged repo-wide finding reaches nothing. | Of 62 cross-reviews, ~13 carried `Cross-Feature` and ~18 `Process`; the REQ rounds tagged almost everything `Local`. The iteration-index defect is the clearest instance and the most expensive one. Now DC-13, but the tagging decision is made in the review prompt. |
| **P-11** | orchestrate-dev-workflow §6d; pdlc-workflow-distribution §4c | `orchestrate-dev`, `dod-verify` | Add a DC-07 check: at DoD, detect a commit landed on the feature branch outside the pipeline during the run (compare the round's recorded provenance header against the branch tip) and require an explicit disposition — attribute-and-review, or move to the owning queue row. Never resolve the doc falsification by deleting the change. | One out-of-band commit made 11 statements across 5 documents false in the present tense, partially discharged a deferral whose successor row still carried the old justification, invalidated the previous round's "working tree clean" provenance header, and changed the next phase's gate under the feature's feet. Two of three DoD rounds were spent on this and on remediation-of-remediation rather than on the feature. |

---

## Not proposed — deferred, with reasons

| Item | Source | Why not promoted |
|---|---|---|
| The **additive caller-scoped trailer convention** (`DECISIONS_WARRANTED:` — a workflow-injected structured return value that does not modify the skill for other callers), with DEC-ODW-02/DEC-ODW-04 as canonical examples | orchestrate-dev-workflow §2 | **One feature, no recurrence.** A genuinely reusable pattern, but `pdlc-workflow-distribution` needed nothing like it, so there is no second data point on whether the shape generalises. Its *deciding criterion* — universality of benefit determines SKILL.md-native vs. caller-injected — is promoted as part of DC-01, which is the transferable half. Revisit when a third workflow-bearing feature needs structured output from a worker skill. |
| A **"Low carry threshold"** as an enforced pipeline rule (escalate any Low surviving 3+ rounds) | orchestrate-dev-workflow §4f | Promoted as **guidance** in DC-08, not proposed as a skill-enforced gate. Both features show the underlying problem is real, but neither shows that automatic escalation is the right response — `pdlc-workflow-distribution` §1a is direct evidence that mechanically escalating unresolved findings is what produced a 16-round fixed point. Human judgment on the threshold, please. |
| Booked follow-up work items from Phase CR round 2 / DoD round 3 (G-01 through G-07 and the poisoned capability-probe cache) | pdlc-workflow-distribution §5 | Feature-local code defects, not cross-feature patterns — out of this skill's scope. They belong in a follow-up REQ or queue row 8 (now `Order 0`). **G-02 first**: the hook's `EXIT` trap arm has no detector, one is constructible with a one-fixture addition, and until it lands a P0-absolute guarantee rests on reasoning alone. |
