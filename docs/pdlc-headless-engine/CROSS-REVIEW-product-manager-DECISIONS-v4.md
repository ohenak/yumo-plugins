# Cross-Review: product-manager — DECISIONS

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-headless-engine/DECISIONS-pdlc-headless-engine.md` (v1.5)
**Date:** 2026-08-11
**Iteration:** 4
**Scope:** Delta re-review against v1.3 (`4c89a75a`) and my own `CROSS-REVIEW-product-manager-DECISIONS-v3.md`. Product lens only — traceability to REQ v0.10 / FSPEC v1.7, scope compliance, acceptance-criteria and upstream-summary fidelity. Changed sections only; sections approved in rounds 1–3 are not re-litigated.

## Prior findings disposition

Diff reviewed: `git diff 4c89a75a..HEAD -- docs/pdlc-headless-engine/DECISIONS-pdlc-headless-engine.md`, seven commits (`75d67334`, `0a1739a9`, `a8626ee2`, `db6f5693`, `a575fd0f`, `4be38958`, `449d49e2`). Changed: the header pin block, two new change notes (v1.4, v1.5), §0's TSPEC pin, DEC-ENG-03's authority paragraph and both deferred bullets, DEC-ENG-05's rule block and its "why structural" body, DEC-ENG-05's re-evaluation trigger, DEC-ENG-10's new pairing paragraph, DEC-ENG-13's new boundary paragraph, and four §7/§8 rows.

| Prior ID | Severity | Status | Evidence |
|---|---|---|---|
| F-01 | Medium | **Addressed in direction, but the replacement text is stale — see new F-01** | §8's unqualified "every skill-identifier literal is a member (no exemption list)" is gone; the row now scopes containment by syntactic position and states explicitly that `meta.name` (`orchestrate-dev.js:3316`, `orchestrate-queue.js:45`) and the reviewer-role map (`:6229-6231`) are out of scope rather than exempted. Both anchors re-verified at HEAD. The finding I raised is closed; what replaced it transcribes TSPEC **v1.7**, which TSPEC v1.8 superseded before these commits were written. |
| F-02 | Low | **Closed** | DEC-ENG-13 now draws its boundary in one paragraph: suite-runner diagnostics are outside the catalogue, carry no id, owe no once-per-suite emit obligation, and the rule generalises ("a string is a catalogue member if it can be read by an operator who never runs the suite"). DEC-ENG-10's filtered-run skip line and its new step-4-ran line are named as the worked case. This is the one-clause answer I asked for, and it is answered in the direction that costs PLAN least. |
| Q-01 | — | **Fully closed at HEAD** | The FSPEC half landed. I re-ran the grep the entry itself used as its evidence: `grep -inE "python\|interpreter"` over `FSPEC-pdlc-headless-engine.md` returns **11** hits at HEAD, exactly as the entry now claims. All five new citations verify line-for-line: BR-GUARD-6 (`FSPEC:921-943`), rung 4a's ladder row (`FSPEC:307`), EC-START-10/11 (`FSPEC:416-417`), `AT-ENG-11a` (`FSPEC:977`), C-11 traceability row (`FSPEC:1386`). |
| Q-02 | — | **Closed** | The v1.4/v1.5 change notes state the dedupe and the re-syncs as deliberate; no content I flagged as possibly-lost is missing. |

Both round-1 Highs remain closed. The revision resolves every finding I carried, and introduces one new High in the material it added.

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | **High** | Local | **DEC-ENG-05's new rule block and §8 row transcribe TSPEC v1.7's site predicate, which TSPEC v1.8 superseded as a High before these commits were written — and the superseded clause is red on correct code at HEAD.** The quoted rule now reads "A site the extractor cannot resolve to a literal or a module-level constant is a **failure**, never a skip" (`DECISIONS:415-419`), and the census reads "7 / 28 / 1 / 11 = 47 sites at HEAD after the edit, enumerated in TSPEC §3.3" (`DECISIONS:435-437`). TSPEC §3.3 at HEAD says neither. TSPEC v1.8's F-44 (rated High there) measured that clause against the two modules and found **11 indirect positions** that resolve to neither a literal nor a module-level constant — `skill: reviewers[0]`/`[1]` (`orchestrate-dev.js:5909`, `:5910`), `skill: authorSkill` (`:9288`), `skill: dispatch.creator` (`:9528`), and seven variable-argument dispatches (`:5573`, `:5579`, `:5585`, `:5876`, `:7124`, `:7463`, `:9244`). I re-read all four `skill:` anchors at HEAD; they are as described. Under the DECISIONS clause every one of them is a failure, so the guard is **permanently red on correct code** — the same failure mode one layer down that DEC-ENG-05's own v1.4 revision existed to remove. TSPEC's fix is a **third outcome**: indirect dispatch is *neither a site nor a failure*, and its count (11) is asserted separately (`TSPEC:570-593`). The census moved with it — **7 / 28 / 1 / 12 = 48 direct plus 11 indirect**, the twelfth direct site being `_agent(ADVISORY_RUNG_SKILL, …)` at `orchestrate-dev.js:1841`, which I verified exists and resolves to the module-level constant at `:1797`. DECISIONS carries the pre-fix arithmetic and the pre-fix rule. Ordering matters for whether this is drift or a missed re-ground: TSPEC v1.8's §3.3 rescope landed `a9e17ee2` at **17:00**; DECISIONS' four re-sync commits are **17:11–17:13**. The corrected upstream was on the branch when this text was written. This is not a competing engineering judgement I am second-guessing — DECISIONS §3.3's own supersession sentence says "DEC-ENG-05 moves with it" (`TSPEC:567`), and it has not moved. PLAN and PROPERTIES derive the guard's obligations from DEC-ENG-05's rule block and §8 row, so shipped as written they specify a guard that cannot go green. | REQ AC-3.5 (`REQ:599`-adjacent startup contract); FSPEC BR-START-4; summary-fidelity of the §7/§8 hand-off tables |
| F-02 | Medium | Local | **§8's re-synced row labels the enumeration "four enumerated site classes" and then lists five.** The row reads "a `DISPATCHABLE_SKILLS` member declaration, a module-level `SKILL_*`/`ADVISORY_RUNG_SKILL` constant, a `PHASE_DISPATCH` role field, a `skill:` object field, or the skill argument of a dispatch call" (`DECISIONS:932`) — five items under a "four" quantifier. The entry body's own block lists four (constant declaration, role field, `skill:` field, dispatch-call first argument, `DECISIONS:415-419`), and TSPEC §3.3's census is per-class over four. A reader building the extractor from §8 gets a different class count from the one the census is closed over, and the "a **fifth site class** escapes it" re-evaluation trigger in the same row becomes ambiguous about which class is the fifth. One-clause fix, but it lands in the same edit as F-01. | FSPEC BR-START-4; DEC-ENG-05 |
| F-03 | Medium | Local | **The upstream pin says TSPEC v1.7; HEAD is TSPEC v1.8** (`TSPEC:16`). The header block (`DECISIONS:9`) and §0's opening sentence ("TSPEC v1.7 fixes the mechanism", `DECISIONS:72`) both pin v1.7. This is the mechanical cause of F-01: the document re-grounded its FSPEC citations meticulously this round and did not re-ground its TSPEC ones. Worth noting the asymmetry as evidence the pin is doing real work — every FSPEC anchor in DEC-ENG-03 verifies exactly, and the one document whose pin went stale is the one whose transcribed rule went stale with it. Fixing the pin without fixing F-01 would make it worse, not better; they are one edit. | Traceability; DECISIONS header contract |

## Questions

| ID | Question |
|----|---------|
| Q-01 | When F-01 is fixed, does DEC-ENG-05 want to **carry** TSPEC's census arithmetic at all? The entry now transcribes a figure that has changed twice in two rounds (47 → 48 direct + 11 indirect), and each change costs a DECISIONS round that settles no decision. The decision this entry owns is "containment, not absence, over a structurally scoped site set, conjoined with a census so it cannot pass vacuously" — that survives every recount. Citing TSPEC §3.3 for the numbers, rather than restating them, would make the entry stable under recount while losing nothing a later reader needs. Author's call; I would not spend a round moving numbers that a round after will move again. |
| Q-02 | DEC-ENG-13's boundary rule is "a string is a catalogue member if it can be read by an operator who never runs the suite" (`DECISIONS:826`). Does the **CI log** count as such a reader? An operator who never runs the suite locally still reads CI output when a run goes red, and DEC-ENG-10's step-4 skip line is exactly what they would see. I read the intended answer as no — the rule's worked case names those very lines as outside — and I am not raising it as a finding because the entry states the case explicitly rather than leaving it to inference. But PLAN will write the runner's diagnostics as plain prose on the strength of it, so if the answer is "CI logs are operator-visible", the boundary needs one more clause before PLAN, not after. |

## Positive Observations

- **DEC-ENG-03's re-ground is the best upstream-tracking work I have reviewed on this feature.** Five new citations, all exact at HEAD: `FSPEC:921-943` (BR-GUARD-6's four fixed terms), `FSPEC:307` (rung 4a's ladder row), `FSPEC:416-417` (EC-START-10/11), `FSPEC:977` (`AT-ENG-11a`), `FSPEC:1386` (C-11's traceability row). The entry even re-ran its own falsifying grep and reported the new count — "every earlier version of this paragraph reported zero hits … at HEAD the same grep returns eleven". I re-ran it: eleven. A document that keeps the evidence it was once right about and shows the number that moved is a document a reviewer can check cheaply.
- **The deferred bullets were resolved rather than deleted, and the entry says so.** "Recorded here as resolved rather than deleted, so the next reader can see the decision did not grow to cover what it had deferred" (`DECISIONS:257-259`). This is the honest shape: the entry did not quietly absorb authority that landed upstream, it kept its own boundary and cited across it. It also names the outcome it did *not* predict — rung **4a** rather than the rung 6 both the entry and I expected — and explains why 4a is better than either option it had filed ("keeps rung 5 as billing posture and lets `doctor` name the failing capability — the two properties the hedge was protecting"). Recording that your own filed alternatives were both beaten is rarer than it should be.
- **DEC-ENG-10's new pairing is a genuine positive-assertion counterpart, not a restatement.** The added paragraph identifies the exact failure the one-directional obligation permits — an over-matching detector reporting skipped-with-reason on *every* run, leaving the three set-equality assertions permanently unexecuted — and pairs it with "an unfiltered run must assert step 4 actually *ran* — a pass, not a skip and not silence". That is the no-absence-only-oracles bar met on the merits: one direction proves the detector fires, the other proves it does not over-fire. It also names which invocation carries the positive half (`npm test` with no arguments), so PLAN cannot satisfy it by asserting nothing anywhere.
- **DEC-ENG-13's boundary generalises instead of ruling on the instance.** I asked which side of the catalogue DEC-ENG-10's skip line falls on. The answer settles that case *and* states the rule that decides the next twenty ("a string is a catalogue member if it can be read by an operator who never runs the suite"), so PLAN can write suite-runner prose without a catalogue edit and without asking again. Answering the class rather than the instance is what keeps this document from needing a round per string.
- **The change notes remain checkable.** v1.5's scope claim — four edits, no decision reversed — matches the diff exactly across seven commits, including the two round-2 TE questions it explicitly leaves open as TE's to settle. Three rounds running, the change note has let me scope the delta review from the note and confirm it from the diff rather than re-reading the document. That is worth more to review cost than any single finding I have raised.

## Recommendation

**Needs revision** — one High (F-01), in material this revision added.

Every finding I carried into this round is resolved, three of them well: DEC-ENG-03's authority is now cited rather than promised and every anchor verifies at HEAD; DEC-ENG-10 gained a real positive counterpart; DEC-ENG-13 drew the boundary I asked for and generalised it. Nothing that was approved in earlier rounds broke — I checked each changed section against HEAD and found no reversed decision and no lost consequence.

The blocking finding is narrow and mechanical, and it is not a disagreement about the decision. DEC-ENG-05's decision — containment over structurally scoped sites, conjoined with a census — is right, and TSPEC agrees with it. What is wrong is the transcription: the rule block and §8 row carry TSPEC **v1.7**'s wording and arithmetic, which TSPEC v1.8 replaced as its own High (F-44) at 17:00, eleven minutes before these commits. As written, "cannot resolve ⇒ failure" makes the guard red on eleven positions that exist in correct code at HEAD, and the census is short by one direct site (`_agent(ADVISORY_RUNG_SKILL, …)`, `orchestrate-dev.js:1841`). PLAN and PROPERTIES read their obligations from exactly these two places, so this cannot be folded into the hand-off the way last round's carry-forwards could.

Exactly what to change:

1. **F-01** — Re-sync DEC-ENG-05's rule block (`DECISIONS:415-419`) and §8 row (`DECISIONS:932`) with TSPEC §3.3 at HEAD: classes 3 and 4 require a string literal or an identifier bound to a module-level constant; **indirect dispatch (parameter, local, or member expression) is neither a site nor a failure**, and its count is asserted separately; the census is **7 / 28 / 1 / 12 = 48 direct plus 11 indirect**.
2. **F-02** — Make §8's enumeration and its "four classes" quantifier agree (four items, or four named classes with the `DISPATCHABLE_SKILLS` member declaration folded into class 1).
3. **F-03** — Move the TSPEC pin to **v1.8** in the header block and §0.

All three are one edit to two locations plus the pin. No decision changes, no alternative is re-opened, and I would expect this to be a single-commit round.

No errata against REQ or FSPEC this round — both verify at HEAD. One erratum is emitted against TSPEC in the response trailer: TSPEC's own upstream anchors for FSPEC's rung-4a material are stale by roughly the same insertion that moved them (`FSPEC:299` for the ladder row, which is `:307` at HEAD and is a table header at `:299`; `:406-407` for EC-START-10/11, which are `:416-417`; `FSPEC:302-303` for BR-START-1's no-probe sentence; "§5's ladder" for what is §4.1; pin "FSPEC v1.6" against FSPEC v1.7). That is TSPEC's to fix, not this document's — DECISIONS' own FSPEC anchors are correct.

## Verdict

VERDICT: Needs revision
{"high": 1, "medium": 2, "low": 0}
