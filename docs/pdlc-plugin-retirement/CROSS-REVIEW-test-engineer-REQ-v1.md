# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-plugin-retirement/REQ-pdlc-plugin-retirement.md` (v0.4, 2026-08-10)
**Date:** 2026-08-17
**Iteration:** 1
**Scope:** testability, edge-case completeness, oracle falsifiability, traceability

## Findings

All existing-behaviour claims below were verified against the tree at HEAD on
`feat-pdlc-plugin-retirement`, not against the documents that describe it.

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **The retirement inventory is not set-equal to the tree it enumerates, and AC-1.1 asserts the gap as if it were true.** `pdlc/workflows/dist/` at HEAD holds five artifacts, not four: `consolidate-learnings.bundle.js` (418 KB) sits beside the two bundles, the manifest and the probe CLI, and `CLAUDE.md`'s "Workflow scripts and the runtime build" section lists it as a shipped artifact of the same build script. It appears nowhere in §1.2's M-table, so it is in neither the deletion set nor the survivor set — yet AC-1.1 requires `pdlc/workflows/dist/` to contain "M-9 alone". As written, AC-1.1 is a containment check over a hand-written enumeration that a third bundle already falsifies. Fix: add the bundle as an M-row with an explicit disposition (delete with M-4/M-5, or survive with M-9), and restate AC-1.1 as a **set-equality** over `dist/`'s actual entries so a bundle added between now and the sweep fails the criterion rather than slipping through. | §1.2, AC-1.1, G-1 |
| F-02 | High | Cross-Feature | **Deleting the two CI jobs turns the engine suite red, and no criterion covers the engine suite at all.** `pdlc/engine/__tests__/ci-arrangement.test.js` is a mechanical set-equality oracle over the PR gate: `GATE_JOB_IDS` hard-codes `artifact-freshness` and `fresh-clone-bootstrap` (`:47-53`); a further block asserts `CLAUDE.md`'s CI table set-equals the rendered check column (`:614-633`) **and** that its prose **count word** equals the row count (`:636-646`, the `**N checks** must pass` form); the file header (`:12-16`) also asserts `publish.yml`'s gate-command set equals the PR-gate files'. AC-1.3 scopes greenness to "the workflow test suite" and AC-1.4 to CI jobs — nothing requires `cd pdlc/engine && npm test` to pass after the sweep, even though that suite is the one that provably breaks first. R-2's dependent list names CI jobs and CLAUDE.md but not this oracle, the count word, or the engine suite. Fix: add an AC requiring the engine suite green at each C-5 commit, and name `ci-arrangement.test.js` (with its CLAUDE.md count-word conjunct) in R-2 and O-5. | AC-1.3, AC-1.4, R-2 |
| F-03 | High | Local | **`publish.yml` invokes two of the deleted artifacts and is absent from the whole document.** The tag-triggered `gate` job runs `node pdlc/workflows/build-runtime.mjs --check` (`.github/workflows/publish.yml:41`), a rebuild-and-diff (`:45`, `:57`), `pdlc/hooks/scripts/sync-workflows.sh` as the documented bootstrap (`:62`, with an explicit exit-126 branch at `:66`) and `sync-workflows.sh --check` (`:75`). §1.2's prose counts "two CI jobs … plus index-mode assertions inside a third" and stops there; `publish.yml` appears in no M-row, no AC, and no risk. A sweep that satisfies every criterion in §6 still leaves the release path calling deleted scripts, and the failure only surfaces at the next `engine-v*` tag — after merge, outside any PR gate. Fix: add `publish.yml`'s gate steps to the inventory and to AC-1.4 (or a sibling AC), and require the tag path exercised or statically asserted before the sweep closes. | §1.2, AC-1.4, R-2 |
| F-04 | High | Local | **AC-1.2's repo-wide search has no enumerated allow-list, and tracked fixtures already falsify it.** Five tracked files carry the retired names outside any document: `pdlc/engine/__tests__/fixtures/consumer-ac12/.claude/workflows/{orchestrate-dev.bundle.js,orchestrate-queue.bundle.js,distribution-manifest.json,pdlc-cli.mjs,.pdlc-drift-state.json}` and `pdlc/workflows/__tests__/fixtures/covered-violations/.claude/workflows/orchestrate-dev.bundle.js` (`git ls-files | grep '\.claude/workflows'`). None is a `docs/completed/` doc, a LEARNINGS, a post-mortem or queue prose, so none is a "historical record permitted by NG-4" — the AC is unsatisfiable until their disposition is decided, and "the only surviving matches are historical records" is a judgement call per match, not a check a test can run. Fix: state the allow-list as an explicit **path-glob set** (`docs/completed/**`, `LEARNINGS-*.md`, `POSTMORTEM-*.md`, `docs/_queue/QUEUE.md`), so the oracle is `matches − allowlist == ∅`, and give the two fixture trees a named disposition (delete, rename, or add to the allow-list with a reason). | AC-1.2, NG-4 |
| F-05 | High | Local | **AC-1.3's third conjunct derives its expected value from the change under test, so it cannot fail.** "its file count has dropped by the number of dedicated test modules removed" is true of any deletion whatsoever — the expectation is computed from the diff, not transcribed from the spec. The related §1.2 figure is also stale in a way that matters to whoever writes the test: M-8's "83 test files in the suite" was exactly right at `ec55ba9d` (2026-08-08, `git ls-tree -r ec55ba9d -- pdlc/workflows/__tests__ \| grep -c '\.test\.js'` → 83) and is **119** at HEAD. Fix: at C-6 re-measurement time, transcribe the **literal** expected post-sweep file count (better: the literal list of removed test module basenames) into the FSPEC, and have AC-1.3 assert against that literal, plus a positive conjunct that a named surviving test module is still present. | AC-1.3, §1.2 M-8 |
| F-06 | Medium | Local | **Three of §6.3's criteria already pass at HEAD, and the REQ does not say so.** C-10's handshake is shipped: `pdlc/engine/package.json:18` declares `"pdlcPluginCompat": "^0.23.0"`, and `pdlc/engine/lib/handshake.mjs:1-10` documents the fail-closed refusal that names both versions, the range and the remedy (`:152` carries the no-range message). AC-3.2, AC-3.5 and AC-3.6 therefore go green before a single line of this feature is written, and NG-5 forbids changing the engine anyway. That is legitimate as a **regression** guard, but the REQ presents C-10 as something this feature establishes, which invites the FSPEC to re-author shipped behaviour. Fix: mark these three explicitly as inherited-and-re-asserted (citing `handshake.mjs`), so their pass carries information — e.g. pair each with a post-sweep conjunct (the refusal path still fires **after** the plugin's workflow machinery is gone). | AC-3.2/3.5/3.6, C-10, NG-5 |
| F-07 | Medium | Local | **AC-5.2's baseline cannot be produced after the sweep, and nothing requires capturing it before.** The criterion compares the post-sweep run report "against a pre-sweep run report of the engine path", but no constraint or prerequisite obliges anyone to generate, commit and pin that baseline (path + commit) before the first deletion lands. BL-03's cited reports are adoption evidence, possibly from another repo and not necessarily field-comparable. Once the sweep merges, the comparison is unexecutable and the AC degrades to memory. Fix: add a prerequisite (or a C-6 clause) requiring the baseline run report captured, committed and cited by path+commit **before** the first deletion commit, and state the comparison as field-set equality with an enumerated allowed-difference list (feature name, timestamps, ids), so an added *or* removed field fails. | AC-5.2, BL-03, C-6 |
| F-08 | Medium | Local | **AC-1.8's per-commit greenness names no mechanism, so it is unverifiable as stated.** GitHub Actions runs on the PR head, not on each intermediate commit, so "each of those commits is independently green in CI" is not observable from a normal PR run — and C-5/C-7's bisectability guarantee rests entirely on it. Fix: name the mechanism and the exact command set (e.g. `git rebase --exec '<gate commands>' <base>..<head>` over the sweep range, with the gate commands enumerated: `npm run test:coverage`, `cd pdlc/engine && npm test`, `bash -n` sweep), so the criterion is a command an operator can run and paste output from. | AC-1.8, C-5, C-7 |
| F-09 | Medium | Local | **The cleanup criteria carry an unnamed exit contract and an absence-only oracle.** AC-4.3 asks the step to exit "in a way that makes the refusal visible" — no exit code, no stream, no message shape, so two implementations that disagree both pass. AC-4.4 asserts only absences ("nothing reads them and no warning about them is emitted"); any run that fails for an unrelated reason also emits no such warning. Fix: AC-4.3 → exact non-zero exit code + the refused path named on stderr + the file byte-identical afterwards; AC-4.4 → pair the absences with a positive conjunct on the same path (the run reaches its configured final phase and its report set-equals the report of the same run in a repo with no leftovers). | AC-4.3, AC-4.4, C-9 |
| F-10 | Medium | Local | **AC-3.3 checks containment where the risk is deletion.** "when they invoke each interactive skill, then each loads and runs" enumerates nothing, so a skill lost during the sweep satisfies it vacuously — exactly the regression NG-1 exists to prevent. Fix: define the set mechanically (every `pdlc/skills/*/SKILL.md` at HEAD, minus the two delegators) and require **set-equality** against the pre-sweep listing, plus the named hook assertions already present. | AC-3.3, NG-1 |
| F-11 | Medium | Local | **BL-05 and O-7 state queue facts that are false at HEAD, and one of them is a hard gate.** The `pdlc-install-mechanism` row was **removed** from `docs/_queue/QUEUE.md` on 2026-08-13, "closed as superseded, not delivered" — so BL-05's "operator decision on the `pdlc-install-mechanism` and `pdlc-release-ci` queue rows" has no subject for half its scope, and can never read "satisfied" as written. O-7's "Queue rows 6 and 7 … both describe the retired copy channel and both are `blocked`" is wrong twice over: there is no row 7, and row 6 is `pdlc-engineering-loop`, status **`pending`** (`docs/_queue/QUEUE.md:78`); the blocked row that names the retired channel is row 8, `pdlc-release-ci` (`:79`). Fix: retarget both by feature name (v0.4's changelog says this was already attempted), record `pdlc-install-mechanism` as discharged with its closure citation, and restate O-7 against the live table so AC-2.3 has a checkable row set. | BL-05, O-7, AC-2.3 |
| F-12 | Low | Local | **§1.2's measurements are stale enough to mislead a plan built on them.** At HEAD: M-4 402 KB (table: 304 KB), M-5 401 KB (303 KB), M-6 1.5 KB (1.1 KB), M-7 831 lines / 34 KB (572 lines / 22.6 KB), M-9 680 KB (476 KB). C-6 already requires re-measurement, so this is not a correctness hole — but the table carries no **measuring command**, which is what makes re-measurement mechanical rather than another hand count. Fix: record the exact command beside the table (and the commit it was run at), so C-6's re-derivation is reproducible by anyone. | §1.2, C-6 |
| F-13 | Low | Local | **AC-1.5 treats the `.gitignore` row as single-purpose; it is not.** `/.claude/workflows/` at `.gitignore:33` is deliberately **anchored**, and the comment above it (`:16-19`, `:30`) explains why: an unanchored pattern would also swallow the covered-violations fixture's nested `.claude/workflows/` tree, whose tracked file (`pdlc/workflows/__tests__/fixtures/covered-violations/.claude/workflows/orchestrate-dev.bundle.js`) the document-drift oracle depends on seeing. Removing the row is probably right, but it is exactly R-4's hazard in concrete form. Fix: have AC-1.5 name the fixture interaction, and have AC-1.6 assert the drift scan's result on a **clean tracked-files-only tree** before and after. | AC-1.5, AC-1.6, R-4 |
| F-14 | Low | Local | **AC-1.6's "the remaining oracles pass on this repo" is environment-dependent.** `coveredViolations` walks the entire tree under `root`, skipping only `.git/` and `node_modules/` (CLAUDE.md states this explicitly as a known local-red cause), so an untracked cache or editor backup can flip the result independently of the diff. Fix: state the invocation the criterion means — a clean checkout or a tracked-files-only root — so the oracle's pass is attributable to the change. | AC-1.6 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Does `dist/consolidate-learnings.bundle.js` retire with M-4/M-5, or survive with M-9? `/pdlc:consolidate-learnings` is a live skill with a runtime bundle, so the answer decides whether AC-1.1's "M-9 alone" is the intended end state or a third survivor is owed a G-5-style clause (F-01). |
| Q-02 | Is editing `pdlc/engine/__tests__/ci-arrangement.test.js` and the engine's fixture trees in scope, or does NG-5 ("Changing the engine") forbid it? The sweep cannot leave both the engine suite green and NG-5 untouched under the strict reading — one of the two needs a stated carve-out (F-02, F-04). |
| Q-03 | Which repo and which commit produce AC-5.2's pre-sweep baseline report, and where is it committed? Naming it in the REQ is the difference between a checkable comparison and an unexecutable one (F-07). |
| Q-04 | For AC-1.8, what command set constitutes "green" per commit — the PR gate's five checks, or a reduced local subset? The answer determines whether C-5's bisectability claim is testable at all (F-08). |
| Q-05 | AC-4.1 says "the repo's tracked files are unchanged" after cleanup. In a repo where `.claude/workflows/` was tracked rather than ignored, is the cleanup expected to refuse (per C-9's conservatism) or to delete tracked files? The AC currently presumes the untracked case (F-09 adjacent). |

## Positive Observations

- **Every criterion is Who/Given/When/Then and tree-checkable.** §6's preamble states the bar — "none depends on an agent reporting success" — and the body largely honours it. That is the single most important testability property a demolition REQ can have, and it is present from the first draft.
- **C-5's per-artifact-class commits plus C-7's per-commit greenness are a genuinely testable bisectability contract**, not a slogan. The pairing (one commit per class; each independently green) is what makes R-1's revert control real. F-08 asks only for the mechanism, not for the idea.
- **C-8's no-skip rule is exactly right and rarely written down.** "Removed, never skipped, marked pending, or left asserting a vacuous truth against an empty directory" forecloses the three ways a deletion sweep normally launders lost coverage.
- **R-8 anticipates test-corpus loss and prescribes re-homing surviving assertions before their host file is deleted.** That is the correct order of operations, and it is the finding I most expected to have to raise myself.
- **G-5/AC-5.3/R-5 keep the probe CLI's *generation* a criterion, not just its existence** — "still produced by a build step rather than maintained by hand" is a falsifiable oracle, and it closes the exact failure mode where a generated artifact silently becomes a checked-in fossil.
- **AC-4.2's idempotence criterion has three conjuncts** (succeeds, changes nothing, says so), which is the shape absence-only oracles usually lack. F-09's request is that AC-4.3 and AC-4.4 be brought up to this same standard.
- **NG-3 states the inertness claim as provable on the engine path** rather than as an intention, which gives §6.5 something to actually assert against.

## Recommendation

**Needs revision**

Five High findings must be closed before this REQ is a sound basis for FSPEC authoring:

1. **F-01** — add `dist/consolidate-learnings.bundle.js` to the inventory with a disposition, and restate AC-1.1 as a set-equality over `dist/`.
2. **F-02** — add an acceptance criterion covering the **engine** test suite, and name `ci-arrangement.test.js` (job-id set, CLAUDE.md table, CLAUDE.md count word, `publish.yml` gate-command equality) in R-2.
3. **F-03** — bring `.github/workflows/publish.yml` into the inventory and into a criterion; it calls `build-runtime.mjs` and `sync-workflows.sh` today and is currently invisible to the whole document.
4. **F-04** — replace AC-1.2's judgement-based allow-list with an enumerated path-glob set, and decide the disposition of the two tracked fixture trees that already carry the retired names.
5. **F-05** — replace AC-1.3's self-derived file-count conjunct with a literal transcribed at C-6 re-measurement time, plus a positive survivor conjunct.

The Medium findings (F-06 … F-11) are what turn §6 from "checkable in principle" into "a test an engineer can write without asking a question" — F-07 and F-11 in particular decay with time: AC-5.2's baseline becomes uncapturable once the sweep starts, and BL-05's subject row is already gone.

No upstream errata: this REQ is the root of its own document chain, and its cross-feature citations (`pdlc-engine-distribution` O-3/O-4/R-2, `pdlc-headless-engine` NG-4/C-5) resolve to real, live text.

## Verdict

VERDICT: Needs revision
{"high": 5, "medium": 6, "low": 3}
