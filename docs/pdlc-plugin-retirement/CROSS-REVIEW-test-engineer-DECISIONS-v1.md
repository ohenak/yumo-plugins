# Cross-Review: test-engineer — DECISIONS

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-plugin-retirement/DECISIONS-pdlc-plugin-retirement.md` (v0.1)
**Date:** 2026-08-17
**Iteration:** 1
**Scope:** testing lens only — testability of each decision, falsifiability of the oracle that would prove it, and whether the decision leaves PROPERTIES/PLAN a contract they can write a failing test against. Architecture choice, product framing and code style are out of lens.
**Verification base:** HEAD `65a23537` on `feat-pdlc-plugin-retirement`. `git diff --stat 2017c6f9 HEAD` is one file, +185 lines (this DECISIONS document), so the document's `2017c6f9` claims and HEAD are the same code tree.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **Decision rule 2 licenses an implementation echo.** "Set-equality claims are re-measured, never inherited … counts are re-derived from the tree at implementation time" makes the expected value of a set-equality AT a function of the tree the AT is testing. A count re-derived from the post-sweep tree greens by construction: delete one file too many and the re-measured literal moves with it. Expected values in ATs must be literal transcriptions of the spec; when the transcribed literal disagrees with the tree, the resolution is an erratum against the owning document (which rule 2's second half already says), never a fresh measurement fed back into the assertion. State the rule as: *implementation asserts the spec's literal; a disagreement halts for an erratum; re-measurement is evidence for the erratum, not the oracle's input.* As written PROPERTIES cannot tell which of the two halves binds it. | `## Decision`, cross-cutting rule 2 |
| F-02 | High | Local | **DEC-07 leaves the suite-size literal ambiguous with no interim rule, and rule 2 then resolves it the unfalsifiable way.** FSPEC L-5 pins post-sweep `pdlc/workflows/__tests__/*.test.js` at **97** (119 − 22, `hookCompatibility` inside the deleted 21); TSPEC §6.1 erratum 6 says the correct post-sweep literal is **99** with the module moved into class 6's reduction set. DEC-07 chooses reduction but records no rule for what implementation asserts if erratum 6 has not landed when class 6 is built — and TSPEC's own risk T-6 says the disposition can flip. Combined with rule 2 the implementer re-measures and greens whatever the tree contains, so AC-1.3 can pass over a suite of any size. The decision needs an explicit blocking clause (e.g. "class 6 does not start until erratum 6 lands; until then the pinned literal is 97 and the reduction is red") so exactly one literal is assertable at any moment. | DEC-07; FSPEC L-5; TSPEC §6.1 erratum 6, §6.4 T-6 |
| F-03 | High | Local | **DEC-09's version bump has no oracle that can fail.** The only mechanical enforcement in the tree is `advertisedVersionViolation()` (`pdlc/workflows/lib/document-oracles.mjs:571`+), and it is doubly unavailable here: (a) it compares working tree against HEAD and returns `{ skipped: S_NOTHING_STAGED }` when `git status --porcelain -- pdlc/workflows/dist/` is clean, so once the sweep commit lands it verifies nothing; (b) FSPEC class 9 puts "the packaging and advertised-version checks over `dist/`" (M-11g) inside the sweep's own edit set — the sweep edits its only witness. DEC-09 must name a surviving positive oracle (e.g. a class-9 assertion that `pdlc/.claude-plugin/plugin.json`'s `version` equals the literal `0.23.2` **and** satisfies `pdlc/engine/package.json:18`'s `^0.23.0` via `satisfiesRange`), otherwise BR-VER-1's user-visible signal can silently not ship. Note the JSON-literal check alone is a dead-config assertion; pair it with the `satisfiesRange` call so the range semantics are exercised, not just transcribed. | DEC-09; FSPEC class 9 (M-11g); DC-03 |
| F-04 | High | Local | **DEC-03's "do not edit" is unenforced — the guard array's membership has no falsifiable oracle today.** `pdlc/workflows/__tests__/mergeGuard.test.js:45`–`:48` builds `MERGE_GUARD_DEFAULTS_SNAPSHOT` by `JSON.parse(JSON.stringify(MERGE_GUARD_DEFAULTS))` and then asserts the array equals that snapshot — the expected value is read from the subject, so any membership change passes. `mergeConfig.test.js:67`–`:70` only checks frozen / non-empty / trailing-slash. Dropping `.claude/workflows/` from `orchestrate-dev.js:48` today reds nothing, and since `prepack.mjs:20` vendors `orchestrate-dev.js` verbatim the edit would reach the published engine unobserved — precisely the REQ NG-5 blast radius DEC-03 rejects option B to avoid. A decision whose whole content is "this stays byte-identical" needs a set-equality assertion over the four literal members, transcribed in the test, plus a mutation check (drop a member, expect RED). | DEC-03; `orchestrate-dev.js:48`; DC-03 |
| F-05 | Medium | Local | **DEC-08 contradicts FSPEC class 10 without citing the erratum that reconciles them, and the surviving oracle is containment, not set-equality.** FSPEC line 162 lists `.claude/pdlc.config.example.json`'s `postWaveCommand` / `postWavePathspecs` **values** among class 10's retired surface; DEC-08 keeps both values. TSPEC §6.1 erratum 5 already routes exactly this ("the wave-gate config values do not retire with `dist/`"), but DEC-08 neither cites it nor appears in the Re-evaluation triggers, which carry errata 2 and 6 only — an unresolved item without a named successor surface (DC-08). Also, the assertion DEC-08 relies on, `consolidationPreflight.test.js:208`, is `expect(postWavePathspecs).toContain("pdlc/workflows/dist/")`: containment over an enumerated one-element contract. Post-sweep it stays green if a second, wrong pathspec is added. Cite erratum 5, add the matching trigger, and require the class-10 assertion to be a set-equality over the pathspec array. | DEC-08; FSPEC class 10; TSPEC §6.1 erratum 5 |
| F-06 | Medium | Local | **DEC-06 mis-scopes the 509 test lines it prices, so the cost it weighs is not the cost the tests carry.** The document groups `__tests__/adapterProbe.test.js` (306) and `__tests__/helpers/adapterHarness.js` (203) as coverage of the orphaned `runtime-adapter.js`. `adapterProbe.test.js:2`–`:3` states its subject as "`pdlc-cli.mjs`'s probe transport (`rtCliQuery`) and the three seams built on it" — `pdlc-cli.mjs` is the artifact DEC-01 deliberately keeps. Whatever erratum 2 decides about the module, those 509 lines are the surviving CLI's coverage and cannot be discarded with it. DEC-06 should split the two costs and state which surviving production path the probe tests guard, so erratum 2's options can be priced without silently proposing to delete live coverage. | DEC-06; `pdlc/workflows/__tests__/adapterProbe.test.js:2` |
| F-07 | Medium | Local | **DEC-04's chosen option under-describes the contract it is deciding, so PROPERTIES cannot write the negative tests.** Option A names exits `0` / `3` / `4`, but TSPEC §3.2's contract has five rows: `4a` (argument-parse error — nothing touched, the only arm assertable as "removes nothing") and `4b` (runtime failure — partial removal stays) are distinct, and row 5 (`--dry-run`, exit `0`/`3`, removes nothing) is an operator-facing flag that TSPEC §6.1 erratum 7 flags as owned by no upstream criterion. DEC-04 is the natural place to decide whether `--dry-run` ships and whether 4a/4b split; as written the decision is silent on both, and neither the flag nor the merged `4` is testable as stated. | DEC-04; TSPEC §3.2 rows 4a/4b/5, §6.1 erratum 7 |
| F-08 | Low | Local | **No decision row names its owning criterion.** The `## Decision` table's third column states net effect on the tree but no AT / AC / property id, and only DEC-06 and DEC-07 appear under Re-evaluation triggers. Adding an "Owning oracle" column (AT or PROP id per row, `none yet` where F-03/F-04 apply) would make the traceability from decision to failing test mechanical for PROPERTIES rather than reconstructed. | `## Decision` |

## Questions

| ID | Question |
|----|---------|
| Q-01 | If erratum 6 does not land before class 6 is implemented, does DEC-07 block the class or ship the reduction against FSPEC L-5's 97? One of the two must be written down; today rule 2 answers "re-measure", which is the answer that cannot fail. |
| Q-02 | After class 9 edits the advertised-version check (M-11g), what remains that would go red if `pdlc/.claude-plugin/plugin.json` were left at `0.23.1`? |
| Q-03 | Does DEC-04 intend `--dry-run` to ship? If yes it needs an upstream criterion (erratum 7's AC-4.5); if no, TSPEC §3.2 row 5 and TT-2 come out with it. |
| Q-04 | DEC-03 rejects option B partly because the vendored engine copy must stay byte-identical. Is any test asserting that byte-identity, or is the property carried only by `prepack.mjs:20` reading the file at pack time? |

## Positive Observations

- Every code citation I re-measured at HEAD `65a23537` holds: `git ls-files pdlc/workflows/dist/` is exactly the five named entries; `build-runtime.mjs` is 831 lines with `OUT_DIR` at `:32`, `CLI_SOURCES = ["orchestrate-dev.js", "cli.mjs"]` at `:530`, the adapter read at `:97`, `QUEUE_SOURCES`/`DEV_SOURCES`/`CONS_SOURCES` at `:690`–`:692` and `bundles` at `:694`; `MERGE_GUARD_DEFAULTS` is the four-member frozen array at `orchestrate-dev.js:48` with no `pdlc/engine/` member; `prepack.mjs:20` and `startup.mjs:52` say what the document says they say; `runtime-adapter.js` is 1,209 lines / 56,713 bytes; the SKILL.md pair is 97 + 250; `hookCompatibility.test.js` is 371 lines and its only drift-hook-dependent block is `C7` (`:246`, `:251`), the other three being `PROP-COMPAT-04/05/06`; `plugin.json` is `0.23.1` and `pdlc/engine/package.json:18` is `^0.23.0`. A decisions document whose numbers all survive independent re-measurement is rare and made this review cheap.
- Every spec id cited resolves in the named upstream document — no phantom-authority citations, a failure mode this repo has shipped before.
- DEC-05 is the model the other rows should follow: the chosen option is stated as observable surface (unchanged frontmatter `name`/`description`, delegation statement, invocation contract, relay and refusal rules), and that surface is already pinned by `skillFiles.test.js`'s `RLH-SKILL-08`/`RLH-SKILL-09` and `orchestrateDevSkill.test.js`, so the decision arrives with a test that can fail.
- Rejecting option B in DEC-01 on the ground that relocation trades a *measured* set-equality for a TSPEC literal no test asserts is exactly the right currency for comparing alternatives — oracle strength, not effort.
- The Reversibility section's hard/easy split and the explicit "one-way door: none" are usable by PLAN as-is.

## Recommendation

**Needs revision**

Four High findings. Three of them (F-02, F-03, F-04) share one shape: the decision is load-bearing, and nothing in the tree or in the downstream contract can go red if implementation does the opposite. F-01 is the rule that makes that shape reproducible across the whole feature, so fix it first. Concretely, the next revision should: (1) restate cross-cutting rule 2 so specs' literals are transcribed and disagreements halt for an erratum; (2) give DEC-07 an explicit interim literal and a blocking condition on erratum 6; (3) name a surviving positive oracle for DEC-09's bump that outlives class 9's edit; (4) require a transcribed set-equality plus mutation check over `MERGE_GUARD_DEFAULTS`' four members. The Medium findings (F-05 erratum 5 citation and set-equality over `postWavePathspecs`, F-06 the mis-scoped 509 lines, F-07 DEC-04's missing `4a`/`4b`/`--dry-run` rows) are cheap edits in the same pass.

## Verdict

VERDICT: Needs revision
{"high": 4, "medium": 3, "low": 1}
