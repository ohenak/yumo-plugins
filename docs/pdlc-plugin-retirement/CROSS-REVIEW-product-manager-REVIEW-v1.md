# Cross-Review: product-manager — Final Codebase Review

**Reviewer:** product-manager
**Document reviewed:** diff `main...HEAD` (30+ implementation commits, T01–T33, PLAN v0.1) against REQ v0.16 / FSPEC v0.10 / TSPEC v0.11 / DECISIONS v0.5 / PLAN v0.1 / PROPERTIES v0.2
**Date:** 2026-08-18
**Iteration:** 1 (final codebase review)
**Scope:** diff

## Findings

| ID | Severity | Scope | Finding | Requirement |
|---|---|---|---|---|
| F-01 | High | Local | `pdlc/OPERATIONS.md:108` (new file, authored in this feature by splitting content out of CLAUDE.md) still fully documents the retired **queue drift gate** as a live mechanism: *"Before `QUEUE.md` is read, drift gate consults recorded drift-state record (the `check-workflow-drift` SessionStart hook writes)... refuse run for the whole invocation... operator brings consumer copy back in sync (plain `sync-workflows.sh`, `--force`...)... repo can opt out of gate per `.claude/pdlc.config.json` → `distribution.checkEnabled: false`."* This is factually false post-sweep: `pdlc/hooks/hooks.json` no longer registers `check-workflow-drift.sh` (confirmed — only `guard-harvest-before-delete`, `check-scope-field`, `check-req-size`, `nudge-consolidation` survive, matching FSPEC L-4), `pdlc/hooks/scripts/check-workflow-drift.sh` and `pdlc/hooks/scripts/sync-workflows.sh` no longer exist, and `orchestrate-queue.js`'s drift-gate logic is gone (only a stray code comment — `// once drift gate blocked...` — remains). A reader of `pdlc/OPERATIONS.md` cannot tell the pipeline runs a single execution path; they are told a gate exists that does not. Violates REQ AC-2.1 ("no instruction to... check drift"), BR-DOC-2 ("Removed, not deprecated"), BR-GATE-1 ("gate is removed, not disabled" — the doc describes it as merely configurable-off), and G-3 ("exactly one described way to run the pipeline"). | REQ AC-2.1, G-3; FSPEC BR-DOC-2, BR-GATE-1 |
| F-02 | High | Local | `pdlc/README.md`'s "Local development" section gained **new** prose in this feature's own edit (not carried over unedited — this paragraph did not exist on `main`): *"`pdlc/workflows/*.js` are the ES module sources; `node pdlc/workflows/build-runtime.mjs` compiles them into the runnable artifacts under `pdlc/workflows/dist/` (`pdlc-cli.mjs`, `distribution-manifest.json`)."* This is newly-introduced incorrect content: `pdlc/workflows/dist/` contains only `pdlc-cli.mjs` at HEAD (confirmed by directory listing, matching REQ AC-1.1's `{M-9}` set-equality), and `build-runtime.mjs` no longer references `distribution-manifest.json` anywhere in its source (confirmed by search). `distribution-manifest` is one of L-2's seven retired search terms (FSPEC §4.2) — reintroducing it in freshly-authored prose is a regression the sweep itself created, not a pre-existing miss. Violates REQ AC-2.1/G-3 and reintroduces a retired identifier the FSPEC explicitly forbids surviving ("No surviving identifier is a member," FSPEC L-2). | REQ AC-2.1, G-3; FSPEC L-2, BR-DOC-2 |
| F-03 | High | Local | `pdlc/RELEASE-CHECKLIST.md §1 "AC-6.2a — published package really carries `workflows/dist/`"` (lines 17–65) is untouched by this feature's diff and still instructs the release operator to verify that the **published, installed** package's `workflows/dist/` contains **both** `orchestrate-dev.bundle.js` and `orchestrate-queue.bundle.js` **and** `distribution-manifest.json`, including a runnable `node -e` snippet that greps for exactly those three retired filenames. This is precisely the row category REQ AC-2.2 names for removal: *"row for the retired machinery (package-carries-`dist/`, `dist/`-changed-without-version-bump...) removed or rewritten against the engine's release artifact; no row instructs a check that can no longer be performed."* The row as written now instructs a check that will always fail post-sweep (the artifacts it checks for no longer exist), and it is not in `docs/_constraints/pdlc-retirement-baseline.md`'s A-1 allow-list. `pdlc/RELEASE-CHECKLIST.md` is one of the four paths M-11k explicitly names as swept documentation (baseline row M-11k), and PLAN task T28 [green] claims class-12 documentation sweep landed against `pdlc/RELEASE-CHECKLIST.md`, but this section survived. | REQ AC-2.2; baseline M-11k |

## Positive Observations

- `dist/` entry set, hook manifest, CI required-check count (four, across the same two files), `plugin.json` version (`0.23.2`, inside the published engine's declared `^0.23.0` range), and `.worktreeinclude`/`.gitignore` row removal all verify correctly against FSPEC's pinned literals (L-1, L-4, L-7) — the mechanical, oracle-covered surface of the sweep is solid.
- CLAUDE.md's and `pdlc/OPERATIONS.md`'s oracle-covered `### Continuous integration` sections were correctly updated to "four checks" with the right two workflow files named, and the CI workflow files themselves gate on the correct surviving job set with no reference to deleted artifacts.
- The three findings above share one root cause the REQ itself predicted: R-7 names documentation retirement as "most likely half-done, least mechanical" because free-form prose outside the document-oracle's fixed heading checks isn't exhaustively swept by a keyword grep the way code is. All three are outside `documentOracles.test.js`'s asserted surface, which is exactly the blind spot R-7 flags — this is a process-shaped finding, not a one-off author slip, worth a Process-scope note for harvest even though it's tagged Local here per the individual findings' evidence.

## Recommendation

Needs revision. All three findings are High-severity documentation-fidelity gaps against explicit, testable acceptance criteria (AC-2.1, AC-2.2) — not stylistic nits. F-01 and F-03 leave operator-facing instructional text describing mechanisms (`check-workflow-drift.sh`/`sync-workflows.sh`/`distribution.checkEnabled`, and the release-checklist bundle-presence check) that no longer exist and cannot be run; F-02 is a fresh regression this feature's own edit introduced. Each is mechanically fixable — delete the stale prose (F-01), correct or delete the newly-added paragraph (F-02), and remove/rewrite the "AC-6.2a" release-checklist section against the engine's release artifact the way `pdlc/RELEASE-CHECKLIST.md §7` already models for the engine channel (F-03) — none require re-litigating scope or reopening the deletion sweep itself.

## Verdict

VERDICT: Needs revision
{"high": 3, "medium": 0, "low": 0}
