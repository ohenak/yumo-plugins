# Cross-Review: product-manager — DECISIONS

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-plugin-retirement/DECISIONS-pdlc-plugin-retirement.md` (v0.1)
**Date:** 2026-08-17
**Iteration:** 1
**Scope:** product lens only — requirements traceability, scope compliance, acceptance-criteria fidelity. Verification base: HEAD `65a23537` on `feat-pdlc-plugin-retirement`.

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **No decision is recorded for `consolidate-learnings`'s execution host — the one user-visible capability the sweep removes.** DEC-05 (`:66`–`:72`) prices the *two* orchestration skills; the third skill in M-11n is never given an option set. `pdlc/skills/consolidate-learnings/SKILL.md:11` names `pdlc/workflows/dist/consolidate-learnings.bundle.js` as the pass's host ("The pass is performed in `pdlc/workflows/consolidate-learnings.js` (shipped as …bundle.js)"), and class 7 deletes that bundle (M-10). The engine does not replace it: its command surface is `dev`/`queue`/`doctor` only, and `consolidate-learnings` sits in `OPERATOR_ONLY_SKILLS` (`pdlc/engine/lib/startup.mjs:52`) with no workflow module dispatching it. TSPEC §6.1 erratum 3 and T-5 already flag this as blocking, and SUCC-2 routes the replacement out of scope — but DECISIONS is the document that is supposed to price that choice, and it does not name it, list alternatives, or record the price. The surviving `nudge-consolidation` SessionStart hook (kept by AC-1.7 / O-1) will keep telling humans to run a pass with no host. Add a DEC row: options are (a) rewrite the SKILL.md to instruct the interactive pass, (b) block class 7/11 on the upstream disposition, (c) accept the capability loss explicitly — each with its REQ NG-3 / US-04 price. | REQ NG-1, NG-3, US-04, O-2; AC-2.1; TSPEC §6.1 erratum 3 |
| F-02 | High | Local | **Decision rule 1 states the engine carve-out as absolute — "No task in Phase P may write under `pdlc/engine/`" (`:128`) — which contradicts both this document's own Context (`:22`, "may edit `pdlc/engine/**` only inside two named carve-outs") and REQ NG-5, which puts the engine-side tests and fixture trees (M-11c, M-11d, M-11e, M-11m) and the compat declaration explicitly *in scope* "because the sweep cannot leave the engine's own required check green otherwise".** TSPEC §2.9 class 2 is exactly such a commit: it edits `pdlc/engine/__tests__/smoke.test.js`, `fs-observation.test.js` and deletes `fixtures/consumer-ac12/`. An implementer reading rule 1 as written, in the document whose job is to bind implementation (`:126`, "bind implementation"), leaves those files asserting deleted artifacts and reds a required check — the outcome REQ C-7 forbids. Restate rule 1 as "no engine **runtime behaviour** change; writes under `pdlc/engine/` only within NG-5's two named carve-outs". | REQ NG-5, C-7; FSPEC class 2; TSPEC §2.9 |
| F-03 | High | Local | **DEC-09 rejects option B on a scope ground the REQ explicitly opens.** `:103` rejects the minor/major bump because "`^0.23.0` does not admit `0.24.0`, so the bump alone would break the handshake and force an engine edit — REQ NG-5 again". The caret arithmetic is right (`pdlc/engine/package.json:18` declares `^0.23.0`; `pdlc/.claude-plugin/plugin.json:4` is `0.23.1`), but the scope claim is not: NG-5 names "the engine's declared compatible-plugin range (BL-07)" as **carved out and in scope**, and BL-07 is the prerequisite that exists precisely to gate a sweep whose plugin version can leave the installed window. The honest price of option B is an engine release cut plus BL-07 re-gating (operator cost, release latency) — not a non-goal violation. As written, the record teaches downstream readers that the compat range is untouchable, which will mis-shape the PLAN's BL-07 handling. Option A may well still win; re-state its rejection on the true cost. | REQ NG-5, BL-07, R-3; DEC-09 |
| F-04 | Medium | Local | **DEC-02's artifact count is internally inconsistent, in a document whose own Context makes counts load-bearing (`:23`, C-6 "pinned literals are re-measured, never loosened").** `:43` says `build-runtime.mjs` "emits four artifacts" and then enumerates five (three bundles, the CLI, the manifest); `:142` repeats "one emitter instead of four". The tree has five tracked entries under `pdlc/workflows/dist/` (`git ls-files`), and DEC-01 correctly says "five entries to one" (`:116`). Fix the literal to five so the two rows agree and PLAN sizes the class-7 commit from a correct number. | REQ C-6; AC-1.1 |
| F-05 | Medium | Local | **DEC-04's evidence for the NG-6 discharge is vacuous, and the criterion that actually keeps it true is not cited.** The Decision row (`:119`) offers "**no** entry added to `pdlc/hooks/hooks.json` (verified: zero references today)" — a script that does not exist yet cannot be referenced, so nothing is verified. The property that makes the structural discharge durable is AC-1.7's post-sweep hook-set set-equality over the four surviving entries (and TSPEC's five-script mode-bit enumeration), which deliberately excludes `cleanup-consumer-workflows.sh`. Cite that instead, so a later reader cannot satisfy DEC-04 by inspection alone. | REQ NG-6, C-9, AC-1.7 |
| F-06 | Low | Local | **DEC-04's option A under-describes the operator-facing surface it decides.** TSPEC §3.2 gives the invocation as `cleanup-consumer-workflows.sh [--dry-run] [<repo-root>]`; the decision text (`:61`) records only the three exit statuses. `--dry-run` is the operator's preview affordance for a destructive step whose whole product justification is conservatism (G-4, "refuses anything it did not expect"), so it belongs in the recorded choice rather than only in the technical spec. | REQ G-4, AC-4.1, AC-4.2 |
| F-07 | Low | Local | **DEC-06's citation count double-counts.** `:79` says "Four surviving modules cite the file by name … — `orchestrate-dev.js:6743`, `orchestrate-queue.js:1259` and `:2259`, `consolidate-learnings.js:316`, **plus** `pdlc/engine/lib/adapter.mjs`" — four modules *plus* a fifth. All five anchors verify at HEAD, so only the arithmetic is wrong; the list is also incomplete (`consolidate-learnings.js:1331` cites `runtime-adapter.js:915`/`:929-931`), which strengthens rather than weakens the decision. State "four surviving modules, of which one is the engine's adapter" or make it five. | DEC-06; REQ C-6 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Re-evaluation trigger 1 (`:170`) says DEC-06 "is superseded by whatever the upstream document decides" about `runtime-adapter.js`. If the upstream **declines** erratum 2 and states no disposition, does DEC-06 A stand permanently, or does the sweep re-open it? The Consequences row (`:152`) accepts "reviewers will read it as dead weight **until** erratum 2 lands", which reads as if landing is assumed. |
| Q-02 | DEC-08 keeps `postWaveCommand`/`postWavePathspecs` because later waves edit `CLI_SOURCES` (`build-runtime.mjs:530` — verified: `["orchestrate-dev.js", "cli.mjs"]`). Post-sweep, is the CLI's *behaviour* still exercised anywhere, or does AC-5.3's "answers exactly as before" rest solely on an operator invoking it by hand? A survivor whose only proof is manual invocation is the artifact most likely to rot next. |
| Q-03 | DEC-05's thin delegators must surface a C-10 refusal to the human session (REQ O-2). Is the refusal's *user-visible wording* owned by the delegator (plugin, in scope) or by the engine banner (NG-5, out of scope)? AC-3.6 pins the terminal block; the delegator's relay of it is not obviously covered by either document. |

## Positive Observations

- Every code claim I sampled verifies at HEAD, at the line cited: `MERGE_GUARD_DEFAULTS`' four frozen members (`pdlc/workflows/orchestrate-dev.js:48`), `MODULE_NAMES` vendoring only two modules (`pdlc/engine/scripts/prepack.mjs:20`), `OPERATOR_ONLY_SKILLS` (`pdlc/engine/lib/startup.mjs:52`), `OUT_DIR`/`CLI_SOURCES`/`cliArtifact`/`bundles`/`manifestRows` (`build-runtime.mjs:32`, `:530`, `:532`, `:694`, `:773`), the sole `runtime-adapter.js` import (`:97`), `pdlcPluginCompat` (`pdlc/engine/package.json:18`), and the two config-example values verbatim. Line counts check out too: 831 / 1,209 / 56,713 bytes / 97+250 = 347 / 371 / 306+203 = 509. A DECISIONS doc whose alternatives are priced against measured code rather than intuition is exactly what the task asked for, and it is rare.
- DEC-01's rejection of option B is the strongest product argument in the set: trading AC-1.1's *measured* set-equality branch for a fresh TSPEC-named literal is a weaker oracle, and naming that as the deciding cost — rather than "we prefer `dist/`" — is the right currency. It also matches the branch TSPEC §2.2 pinned, so no downstream reader has to reconcile two pins.
- DEC-07 refuses to delete passing `PROP-COMPAT-*` assertions to satisfy an inherited count, and routes the count as erratum 6 instead. That is C-6 applied in the direction that is easy to get wrong (deleting files until a number matches), and Decision rule 2 generalises it correctly.
- The Consequences section prices what got *worse* in four rows with named owners, and the Reversibility split (easy / hard / one-way: none) gives PLAN a real basis for commit ordering. The closing Downstream obligations paragraph — class-7 must land the DEC-01 deletion and DEC-02 reduction together or `--check` fails — is a concrete, testable hand-off rather than a platitude.

## Recommendation
