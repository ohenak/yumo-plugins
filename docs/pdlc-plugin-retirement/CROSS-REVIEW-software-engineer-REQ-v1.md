# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-plugin-retirement/REQ-pdlc-plugin-retirement.md`
**Date:** 2026-08-17
**Iteration:** 1
**Scope:** Technical review — feasibility, cost, implementability, existing-code claim verification

## Verification Basis

Every existing-behaviour claim in the REQ was checked against the tree at HEAD on
`feat-pdlc-plugin-retirement`. Measurements re-taken 2026-08-17.

| REQ claim | HEAD | Verdict |
|---|---|---|
| M-1 `sync-workflows.sh` 33 KB | 32,939 B / 725 lines | ✅ |
| M-2 `lib/pdlc-drift.sh` 76 KB | 75,617 B / 1,955 lines, non-executable | ✅ |
| M-3 `check-workflow-drift.sh` 19 KB | 19,240 B / 381 lines | ✅ |
| M-4 `orchestrate-dev.bundle.js` 304 KB | **401,716 B (392 KB)** | ❌ 29% low |
| M-5 `orchestrate-queue.bundle.js` 303 KB | **401,020 B (392 KB)** | ❌ 29% low |
| M-6 `distribution-manifest.json` 1.1 KB | 1,464 B / 46 lines | ⚠️ 33% low |
| M-7 `build-runtime.mjs` 572 lines / 22.6 KB | **831 lines / 33,664 B** | ❌ 45% low |
| M-8 "83 test files in the suite" | **121 files in `pdlc/workflows/__tests__/`**; 179 `*.test.js` across workflows+engine | ❌ |
| M-9 `pdlc-cli.mjs` 476 KB | **679,956 B (664 KB)** | ❌ 39% low |
| §1.2 = the artifacts existing only for the runtime host | omits `pdlc/workflows/dist/consolidate-learnings.bundle.js` (present; emitted by M-7 at `build-runtime.mjs:715`) | ❌ |
| `.worktreeinclude`'s only row is `.claude/workflows/` | confirmed, single row | ✅ |
| `.gitignore` row ignoring the consumer copy | `.gitignore:33` (plus explanatory block `:13-32`) | ✅ |
| Two CI jobs + index-mode assertions in a third | `pr-tests.yml:122` `Generated artifacts are in sync`, `:148` `Fresh-clone bootstrap works`, `:206`/`:228-250` `Shell scripts parse` | ✅ |
| `SessionStart` drift-reporter hook entry | `pdlc/hooks/hooks.json:38-45` | ✅ |
| Queue drift gate + `distribution.checkEnabled` | `orchestrate-queue.js:1257-1320`, `:2285-2330`, `DRIFT_STATE_PATH` `:79` | ✅ |
| Document oracles police `dist/` | `document-oracles.mjs:177-178` (`DIST_REL`), `:102-104` (generated-tree exemption), `:593-594` (`git status -- pdlc/workflows/dist`) | ✅ |
| Four `RELEASE-CHECKLIST.md` sections | hits at `:12, :17, :22-23, :35, :60, :94, :102, :139, :148, :203-206, :226` | ✅ (≥4) |
| Phase MERGE guard names `pdlc/workflows/`, `.claude/workflows/` (O-4) | confirmed | ✅ |
| C-10 handshake is engine behaviour | **already shipped**: `pdlc/engine/package.json:19` `pdlcPluginCompat: "^0.23.0"`, `lib/handshake.mjs:144` `checkCompat`, `:159-168` missing-plugin refusal, `:176-177` out-of-range refusal, `:197-213` banner, `lib/report.mjs:54-79` version fields | ⚠️ pre-satisfied |
| O-7 "queue rows 6 and 7 … both `blocked`" | **row 6 = `pdlc-engineering-loop`, `pending`** (`QUEUE.md:78`); **row 7 `pdlc-install-mechanism` removed from the table 2026-08-13** (`QUEUE.md:45-46`) | ❌ |
| BL-01/BL-02 resolution = queue row `done` | both rows **removed** from the table (`QUEUE.md:34`, `:64`), never set `done` | ❌ |
| Versions in play | engine `0.2.1`, plugin `0.23.1`, compat `^0.23.0` ⇒ `>=0.23.0 <0.24.0` (`handshake.mjs:110-128`) | context for F-02 |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **`publish.yml`'s `gate` job is a hidden dependent that no goal, AC or risk names, and it makes C-7 unsatisfiable.** `.github/workflows/publish.yml:76-111` runs `build-runtime.mjs --check`, the rebuild-diff and `sync-workflows.sh`, and `:135-153` asserts `check_mode` for all three deleted scripts. Worse, `pdlc/engine/__tests__/ci-arrangement.test.js:746-782` (T49) asserts **set-equality** between the `run:` commands of every `pr-tests.yml` gate job and `publish.yml`'s `gate` job commands — so deleting the two PR jobs *without* editing `publish.yml` in the same commit reds a required check. AC-1.4 scopes the CI obligation to `pr-tests.yml` jobs only, and §1.2's inventory says "two CI jobs … plus index-mode assertions inside a third", never naming the tag-triggered publish gate. Add `publish.yml`'s gate to §1.2, to AC-1.4 and to C-5's artifact-class list. | §1.2, AC-1.4, C-5, C-7, R-2 |
| F-02 | High | Cross-Feature | **The retirement cannot ship without an engine-side `pdlcPluginCompat` widening, which NG-5 places out of scope with no named successor.** `pdlc/engine/package.json:19` declares `^0.23.0`; `handshake.mjs:110-128` pins the caret to the leftmost non-zero component, so the accepted range is `>=0.23.0 <0.24.0`. The plugin is at `0.23.1` (`pdlc/.claude-plugin/plugin.json:4`), and CLAUDE.md requires a version bump for *any* change under `pdlc/skills/`, `pdlc/hooks/` or `pdlc/workflows/` — this feature changes all three. Shedding the entire workflow-execution machinery is at minimum a minor bump to `0.24.0`, which puts the plugin **outside the installed engine's range**: `handshake.mjs:176-177` then refuses every run, loudly, by design. So the very handshake C-10 relies on turns the retirement commit into a total outage until a new engine is published with a widened range and a re-cut `engine-v*` tag. Either add a BL row gating the sweep on a published engine whose range admits the post-retirement plugin version, or state explicitly that the bump stays inside `0.23.x` — and say which. | C-10, NG-5, R-3, AC-3.5 |
| F-03 | High | Local | **The retirement inventory omits a third `dist/` bundle, and AC-1.1's enumeration is therefore incomplete as authored.** `pdlc/workflows/dist/consolidate-learnings.bundle.js` exists at HEAD and is emitted by M-7 (`build-runtime.mjs:715`), yet §1.2 lists only M-4 and M-5, and AC-1.1 requires that "M-1, M-2, M-3, M-4, M-5 and M-6 do not exist" while `dist/` "contains M-9 alone". A tree passing AC-1.1's named set literally still carries the consolidate-learnings bundle. C-6's re-measurement is the right control but does not repair an enumeration that is wrong at authoring time — the REQ's own §1.2 preamble asserts these *are* the artifacts. This is the failure mode C-6 exists to prevent, present in the document that declares C-6. | §1.2, AC-1.1, C-6 |
| F-04 | High | Local | **G-5/AC-5.3 preserve the probe CLI while the sweep deletes both its only delivery path and its only consumer, making AC-5.3 unverifiable.** The only invoker of the probe CLI is the retiring runtime adapter: `pdlc/workflows/runtime-adapter.js:664` `RT_CLI_PATH = ".claude/workflows/pdlc-cli.mjs"`, queried at `:697-699`. The only thing that ever writes that consumer path is `sync-workflows.sh` via the manifest row (`dist/distribution-manifest.json:34-36`). The engine channel has **no** consumer — grep over `pdlc/engine/**` finds `pdlc-cli` only in a fixture tree (`__tests__/fixtures/consumer-ac12/.claude/workflows/pdlc-cli.mjs`) and an unrelated `cwd` string. So after the sweep AC-5.3's "when the probe CLI is invoked in a consumer repo, then it answers exactly as it did before" has no defined invocation: nothing installs it and nothing calls it. O-3 asks *where its build lives* but never *who invokes it and how it reaches them*. Either add that to O-3 and give AC-5.3 a concrete post-sweep invocation path, or reclassify M-9 as retired with NG-2 amended. | G-5, AC-5.3, O-3, R-5, NG-2 |
| F-05 | High | Cross-Feature | **Engine-channel code must change, which collides with NG-5 and C-3, and `Engine tests` is one of the six required checks.** `pdlc/engine/__tests__/smoke.test.js:329-357` asserts "`pdlc queue` is blocked by the drift gate in a repo with no `.claude/workflows`", and `:491-493`, `:530`, `:565` set `distribution.checkEnabled: false` to clear the gate; `ci-arrangement.test.js:47-53, 69-87` hardcodes both deleted job ids and their rendered names; `__tests__/fixtures/consumer-ac12/.claude/workflows/` models the retired consumer copy including the drift-state record and the manifest. C-3 says the gate "and its tests" leave "the workflow modules" together — but a material share of its coverage is **engine-side**, and NG-5 forbids authoring engine changes here. Also note `prepack.mjs:4` vendors `pdlc/workflows/*.js` sources into the published package, so editing `orchestrate-queue.js` changes the engine's shipped payload. Resolve the ownership boundary explicitly: either narrow NG-5 to engine *runtime capability* (letting this feature edit engine tests/fixtures/vendored sources) or add a BL/successor row that owns them. | C-3, NG-5, C-7, AC-1.3 |
| F-06 | High | Local | **O-7 and BL-05 cite queue rows that do not exist as described, and AC-2.3 gates on them.** O-7 says "Queue rows 6 and 7. Both describe the retired copy channel and both are `blocked`." At HEAD, row 6 is `pdlc-engineering-loop`, status `pending`, about the engineering loop (`QUEUE.md:78`); row 7 (`pdlc-install-mechanism`) was **removed from the table on 2026-08-13, closed as superseded** (`QUEUE.md:45-46`). BL-05 likewise gates on "the `pdlc-install-mechanism` and `pdlc-release-ci` queue rows" — only the latter still exists (`QUEUE.md:79`, `blocked`). This is exactly the defect v0.4's changelog claims to have fixed ("queue-row references repointed to feature names (stale Order numbers…)"), so the fix was applied to §2 but not to §8. Since AC-2.3 refuses to pass "while an open row still mandates the retired channel", the criterion currently points at rows that do not make that claim. Re-derive both citations from `QUEUE.md` at HEAD by feature name. | O-7, BL-05, AC-2.3 |
| F-07 | High | Local | **A surviving document-oracle test *requires* CLAUDE.md to keep naming the two deleted scripts, in direct contradiction with G-3/AC-2.1.** `pdlc/workflows/__tests__/documentOracles.test.js:747-749` asserts `CLAUDE.md` **contains** `check-workflow-drift.sh` and `sync-workflows.sh`. Sweeping the docs per AC-2.1 reds that assertion; keeping the prose to satisfy it violates G-3's "a reader who never saw the old path must not be able to find instructions for it in a tracked file". The same file's `coveredViolations` suite survives the sweep, so this is not covered by AC-1.6's "the packaging and advertised-version checks … are gone with their tests" — an assertion inside a surviving test module must be deleted individually. Name it in §1.2/R-2 and in R-8's re-homing step. | G-3, AC-2.1, AC-1.6, R-8 |
| F-08 | Medium | Process | **Every non-trivial measurement in §1.2 is 29–45% understated, and the test-suite figure is wrong by 46%.** See the Verification Basis table: M-4/M-5 are 392 KB not 304/303 KB, M-7 is 831 lines / 33.7 KB not 572 / 22.6 KB, M-9 is 664 KB not 476 KB, and "83 test files in the suite" is 121 in `pdlc/workflows/__tests__/` alone. Nine days of drift. The direction favours the argument (the cost of keeping is larger than stated), so this does not threaten the rationale — but AC-1.3's oracle is computed from the file count, and a reader cannot tell a stale number from a wrong one. Re-measure at FSPEC time and date-stamp each row. | §1.2, M-4…M-9, AC-1.3 |
| F-09 | Medium | Local | **AC-1.3's file-count clause is tautological and its skip clause is an absence-only oracle.** "its file count has dropped by the number of dedicated test modules removed" is true of any number of removals — the expected value is defined by the actual, so nothing can fail it. Replace with a set-equality over the *retained* test-module enumeration (a deleted-by-accident module then fails), plus a literal expected count transcribed from the FSPEC. Separately, "contains no skipped or pending test belonging to M-8" asserts only absence; pair it with the positive assertion R-8 already implies — the re-homed assertions about surviving behaviour (queue triage, hook-manifest compatibility) exist at named paths and pass. | AC-1.3, C-8, R-8 |
| F-10 | Medium | Local | **C-10 and three of §6.3's six ACs are already satisfied at HEAD; unlabelled, they will read as delivered work.** `pdlcPluginCompat` (`engine/package.json:19`), the pre-dispatch check (`handshake.mjs:144`, refusing before the workflow import — `run.mjs:504-505`, `:547-548`), the missing-plugin refusal (`handshake.mjs:159-168`; also `skills.mjs:293`), dispatch-time skill reads with no on-disk snapshot (`skills.mjs:369-375`), the banner (`handshake.mjs:197-213`) and both version fields on success, halt and refusal paths (`report.mjs:54-79`, `cli.mjs:610-617, 655-660, 694`) are all shipped, with tests (`__tests__/handshake.test.js:102-140`, `cli.test.js:86-114`, `run.test.js:388-400`). AC-3.2, AC-3.5 and AC-3.6 will therefore be green before any work starts. Given this project's recorded vacuous-green failure, label them as pre-satisfied regression guards with the HEAD citation, so "green" is not misread as "delivered". | C-10, AC-3.2, AC-3.5, AC-3.6 |
| F-11 | Medium | Local | **AC-3.6's "names both versions … in its refusal" is not satisfied by the refusal message at HEAD, and the AC does not say whether that matters.** `handshake.mjs:176-177` interpolates the *found plugin* version and the required range; the **engine's own** version appears only in the banner (`handshake.mjs:208`), printed immediately above the refusal (`cli.mjs:655`), and in the JSON `engine.engineVersion`. Read strictly, AC-3.6 fails at HEAD and mandates an engine-side message change — which NG-5 forbids. State the observable precisely: whether "in its refusal" means the refusal string, or the banner-plus-refusal block on stdout, or the emitted report. | AC-3.6, NG-5 |
| F-12 | Medium | Local | **BL-01/BL-02's resolution form is unsatisfiable as written.** Both gate on "queue row `done`", but `pdlc-headless-engine`'s row was removed from the table on 2026-08-12 and `pdlc-engine-distribution`'s on 2026-08-16 (`QUEUE.md:34`, `:64`) — merged features are *removed*, not marked `done` (the only `done` row is 23, a release row). A Phase-R checker looking for a `done` row finds none and must conclude the gate is unmet. Restate the resolution form as what the repo actually records: the `docs/completed/{feature}/` directory plus the removal note in `QUEUE.md`'s prose, cited by commit. | BL-01, BL-02 |
| F-13 | Medium | Local | **`.claude/pdlc.config.example.json` names the retired build and `dist/` in the wave-gate keys, and this feature's own Phase I runs under them.** That file's line 1 carries `"postWaveCommand":"node pdlc/workflows/build-runtime.mjs"` and `"postWavePathspecs":["pdlc/workflows/dist/"]` (documented at `CLAUDE.md:102`). Neither appears in §1.2's reference list nor in AC-1.2's enumerated search set ("the three scripts, the two bundles, the manifest, the drift-state record, the `distribution.checkEnabled` key"). This is self-referential: the sweep's own implementation waves will run a post-wave command that rebuilds the bundles being deleted and commit a pathspec that no longer exists. Add both keys to the inventory, to AC-1.2's search set, and to C-5's artifact classes; note that `ci-arrangement.test.js:799-820` reads this file but asserts only `implementation.testCommand`, so nothing catches a stale value. | §1.2, AC-1.2, C-5 |
| F-14 | Medium | Local | **AC-1.2's exemption set is not path-enumerated, so the search is not falsifiable by a machine.** "the only surviving matches are historical records permitted by NG-4" requires a human judgement per hit across a repo where these names appear in hundreds of places (LEARNINGS, post-mortems, completed docs, queue prose, `.consolidation-log.md`, and the bundles' own text until deleted). Give NG-4's permitted set as path globs (`docs/completed/**`, `**/LEARNINGS-*.md`, `**/POSTMORTEM-*.md`, `docs/_queue/QUEUE.md`, `docs/_decisions/.consolidation-log.md`) so AC-1.2 reduces to a grep with an exclude list and a required-empty result. | AC-1.2, NG-4, O-5 |
| F-15 | Medium | Local | **AC-5.2 compares against a "pre-sweep run report" that no prerequisite captures or pins.** By the time the deletions are merged, producing a pre-sweep baseline is impossible — it must be captured *before* the first deletion commit and stored at a fixed path. BL-03 pins adoption evidence for C-1 but does not pin a baseline report for AC-5.2. Add a BL row (or extend BL-03) requiring the engine-path baseline report to be committed at a named path before the first deletion, and have AC-5.2 cite that path. | AC-5.2, BL-03 |
| F-16 | Low | Process | **§1.2's measured facts are inlined in the REQ rather than cited from a constraints baseline.** Per the spec-layer boundary, a REQ should carry shipped-behaviour facts as measured-fact ids cited from a constraints file; this repo already keeps such files (`docs/_constraints/pdlc-engine-baseline.md`, `pdlc-wave-gate-baseline.md`). Relocating §1.2 to `docs/_constraints/pdlc-retirement-baseline.md` and citing `M-*` would let C-6's re-measurement update one file that the plan and the oracles can both read, instead of editing the REQ. | §1.2, C-6 |
| F-17 | Low | Local | **AC-4.3's visible-refusal clause declares no observable.** "exits in a way that makes the refusal visible" pins neither an exit code nor a stream. The retired tooling it must be "at least as conservative as" (C-9) uses a specific convention — non-zero exit with a warning line per row (`sync-workflows.sh`, `--check`). Name the exit code and the stream so the AC is checkable without reading the implementation. | AC-4.3, C-9 |
| F-18 | Low | Local | **AC-1.5 will leave an orphaned rationale block.** `.gitignore:33` is the row to delete, but `:13-32` is a 20-line comment block explaining it and citing `lib/document-oracles.mjs`'s tree walk. AC-1.5 speaks only of rows and of deleting a file "left with no remaining rows"; say that a row's explanatory comment goes with it. | AC-1.5 |

## Questions

_pending_

## Positive Observations

_pending_

## Recommendation

_pending_
