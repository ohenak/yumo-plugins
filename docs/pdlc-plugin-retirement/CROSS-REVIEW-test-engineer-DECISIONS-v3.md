# Cross-Review: test-engineer — DECISIONS

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-plugin-retirement/DECISIONS-pdlc-plugin-retirement.md` (v0.3)
**Date:** 2026-08-18
**Iteration:** 3
**Scope:** testing lens only — is each decision falsifiable, does a named oracle exist that can go red, can PLAN/PROPERTIES derive a contract from it

Delta re-review. Base of comparison: `1053b7fd` (the commit reviewed at v2) → HEAD. Six commits touch the document (`29717bec`, `d1f77ad2`, `2078ad97`, `9c59a676`, `8863ef43`, `875c67cf`); the diff is +26/−12 lines and touches DEC-06's option B, DEC-09's option A, DEC-10's price paragraph, five Decision-table oracle cells, new cross-cutting rule 5, the Consequences gated-class row, and a new "What a gated merge looks like" paragraph. Only those sections were scanned for new issues; unchanged prose already reviewed at v1/v2 is not re-litigated.

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **The restated closure is still one class short: class 12 is bound after the gated classes, so the erratum-3 gate holds six classes, not five.** v0.3 correctly walks FSPEC's ordering column for classes 8, 9 and 10 and concludes "five of the thirteen classes FSPEC §3.1 enumerates — 7, 8, 9, 10 and 11". It stops one row early. FSPEC's class 12 (Documentation) carries the ordering constraint **"Last of the deletion classes"** (`FSPEC-pdlc-plugin-retirement.md:164`), and class 11 is a deletion class held by DEC-10's own gate; class 12 therefore cannot land while 7–11 are held. Class 13's ordering row adds that its documentation "is part of the one story class 12 tells" (`FSPEC:165`), so the doc half of 13 rides 12. Three places now carry the short count and each has a testing consequence: (i) the price paragraph's "five of the thirteen"; (ii) the Consequences row "Five of thirteen classes (7, 8, 9, 10, 11) cannot land on engineering's schedule"; (iii) the new gated-merge paragraph, which partitions the classes exhaustively — "Classes 1–5 are ungated and land on engineering's schedule; class 6 waits on erratum 6 (DEC-07) and classes 7–11 on erratum 3 (DEC-10)" — leaving class 12 in no bucket at all, and then instructs PLAN and PROPERTIES to place edges and ATs for "classes 7–11" only. That is the batch-column understatement my role treats as blocking: PLAN's dispatcher reads the `Deps` column, and a class-12 task without a class-7-closure edge is schedulable in an earlier batch, which lands CLAUDE.md / `OPERATIONS.md` deletion prose while the bundles those docs describe still exist — reddening (or, worse, silently re-fixturing away) the document oracles class 9 owns. Fix: restate the closure as classes **7–12** on erratum 3 (seven of thirteen gated counting DEC-07's class 6), correct all three sites from the single stated place, and extend the PLAN/PROPERTIES obligation sentence to "classes 7–12". | DEC-10 "Price of A"; Consequences gated-class row; "What a gated merge looks like" |
| F-02 | Medium | Local | **A pinned count literal contradicts the enumeration directly beneath it: "Four cross-cutting rules follow from the set and bind implementation" (`:172`) is followed by five numbered rules (`:174`, `:185`, `:196`, `:199`, `:203`).** This is the same failure shape as v2's F-03 ("two of eleven classes"), in the section the revision added. It matters beyond tidiness because these rules are the document's binding surface for PLAN: a downstream reader or oracle that transcribes the enumeration by its stated size drops rule 5 — the newest and least-guessable rule — and rule 5 is the one that decides whether the next unowned surface may ship ahead of its criterion. Cross-cutting rule 2's own transcribe-the-literal discipline applies to this document too. Fix: change "Four" to "Five". | Decision, preamble to the cross-cutting rules |
| F-03 | Low | Local | **DEC-06's new excluded-references sentence mis-cites the surviving suite it rests on, in both line anchors and subject matter.** The text reads: "`consolidationBuild.test.js:88`, `:94`, `:136`, `:141` read it via `readWorkflowSource(\"runtime-adapter.js\")` to assert on `rtWriteFile`'s prompt." Measured at HEAD, the four `readWorkflowSource("runtime-adapter.js")` calls sit at `:88`, **`:95`**, `:136` and `:141` — `:94` is the `it(...)` title line, not the read. And only `:88` belongs to the `rtWriteFile` prompt assertion (`describe("T12 adapter prompt …")`); `:95` asserts a one-occurrence count of the string "relative repository root", while `:136`/`:141` sit under `describe("T12 rtConsInjections …")` and assert `rtConsInjections`'s declaration and its set-equal key set. The rejection of option B is unaffected — it is strengthened, since three separate surviving describes red on deletion, not one — but a reader who follows the anchor to check the claim lands on the wrong line and the wrong assertion. Fix: `:88`, `:95`, `:136`, `:141`, and say the reads back `rtWriteFile`'s prompt **and** `rtConsInjections`'s key set. | DEC-06 option B (ii) |
| F-04 | Low | Local | **The three FSPEC ordering strings quoted in DEC-10's price paragraph are paraphrases presented as quotations.** The document writes: class 8 lands "at the same time as class 7", class 9 "in the same commit as class 7", class 10 after class 7. FSPEC's actual ordering cells read "Any time after class 7" (`FSPEC:161`), "Same commit as, or after, class 7; CLAUDE.md prose guards move in the same commit" (`FSPEC:162`) and "After class 7 (the retired value names a deleted output)" (`FSPEC:163`). The conclusion is unchanged — all three are bound behind class 7 — but two of the three quotes tighten a permissive constraint into a stricter one, and cross-cutting rule 2 tells implementers to transcribe literals from the spec rather than re-word them. A PROPERTIES author transcribing "in the same commit as class 7" would over-constrain class 9's AT placement relative to what FSPEC actually permits. Fix: quote the cells verbatim or drop the quotation marks and cite the anchors. | DEC-10 "Price of A" |

## Questions

| ID | Question |
|----|---------|
| Q-01 | With class 12 folded in (F-01), is any class ungated other than 1–5 and 13? Class 13 is marked "Independent; may land any time" but its documentation is tied to class 12's story — does 13's script half ship ungated while its doc half waits, and if so which criterion owns the split? |
| Q-02 | The gated-merge paragraph says AC-1.1's `dist/` set-equality "stays red while classes 7–11 are held". Red on the branch across a multi-week gate is a standing failure in the suite the gate command runs — is that intended to be an expected-failure marker, a skip with a registered `SKIP_INVENTORY` record (the mechanism TT-1b uses), or a genuinely red gate that blocks every intermediate commit under REQ C-7? The three have very different consequences for whether ungated classes can land at all. |
| Q-03 | Rule 5 splits surfaces into additive-and-conservative versus subtractive. Which side does the class-9 version assertion sit on — it is additive (a new assertion) but it rides a subtractive class. If additive assertions may ship ahead of their class, could DEC-09's `satisfiesRange(...).ok` oracle plus its `0.24.0` negative arm land in `consolidationPreflight.test.js` **now**, ungated, asserting `0.23.1` today and re-pinned to `0.23.2` by class 9? That would give the gate period a live oracle instead of an empty cell. |

## Positive Observations

- placeholder

## Recommendation

placeholder

