# Cross-Review: test-engineer — Codebase Review

**Reviewer:** test-engineer
**Document reviewed:** Full diff `main...HEAD` (T01–T33, PLAN-pdlc-plugin-retirement.md v0.1) plus evidence artifacts REPLAY, POSTSWEEP-RUN, OPERATOR-OBSERVATIONS
**Date:** 2026-08-18
**Iteration:** 1 (Phase CR — final codebase review)
**Scope:** full diff

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **CLAUDE.md's own T19/T28-landed edit ships a factually wrong claim about the reduced builder's output, and no oracle in the suite catches it because the only oracle covering that line (D-1) is a pre-existing, unmodified test that mandates the stale claim rather than verifying it against the real build.** `CLAUDE.md:55` (post-sweep text, part of this diff — `git diff main...HEAD -- CLAUDE.md` line ~50) reads: *"`node pdlc/workflows/build-runtime.mjs` generates the runnable artifacts into `pdlc/workflows/dist/` (`pdlc-cli.mjs` and `distribution-manifest.json`)."* This is false at HEAD: `pdlc/workflows/build-runtime.mjs` contains zero references to `manifest` anywhere in its source (`grep -n manifest pdlc/workflows/build-runtime.mjs` — no hits), and `pdlc/workflows/dist/` on a clean checkout holds exactly one file, `pdlc-cli.mjs` (confirmed by `ls`), matching PROP-BUILD-1's own claim that the builder's emitted set equals `{pdlc-cli.mjs}` and DoD item 4 ("`pdlc/workflows/dist/` holding exactly `pdlc-cli.mjs`"). `CLAUDE.md:61` (also part of this diff) correctly says "`pdlc/workflows/dist/` still holds the regenerated `pdlc-cli.mjs`" (singular) two paragraphs later — the document self-contradicts. The pre-existing oracle `documentOracles.test.js:253` (`D-1`, untouched by this diff) *requires* the "Workflow scripts and the runtime build" section to contain the string `distribution-manifest.json`, so it currently passes precisely because the stale claim was left in place — the oracle is enforcing the wrong thing rather than verifying build-output parity, and would in fact fail (correctly) if someone fixed the prose without updating D-1. No task in §2 (T19, T20, T24, T26, T28 — all of which write CLAUDE.md, per §3's multi-writer manifest for this exact file) closes this gap, and no assertion anywhere in the suite cross-checks CLAUDE.md's dist/-output prose against `build-runtime.mjs`'s actual `CLI_SOURCES`/emitted-file set. This is exactly the "verify any 'X at HEAD' claim against the real production artifact, not the doc" check the review skill calls for, and it is squarely in this feature's scope: it is the shipped state of PROP-BUILD-1's own subject matter. | `CLAUDE.md:55` vs `CLAUDE.md:61`; `pdlc/workflows/__tests__/documentOracles.test.js:253` (D-1); PROP-BUILD-1, PLAN §7 DoD-4 |
| F-02 | Medium | Local | **PROP-CLEAN-8's "property-based… over arbitrary subsets" claim is implemented as exactly one fixed-seed draw per exit-code branch, not an iterated property.** `consumerCleanup.test.js`'s `TT-4: property — the classifier is name-only and all-or-nothing` describe block (`__tests__/consumerCleanup.test.js:293`) uses `seeded(SEED)`/`seeded(SEED+1)` to draw a single subset once per `it()` (one for the "subset alone → exit 0" case, one for "subset + one unexpected name → exit 3"). There is no loop, no `it.each`, no shrinking, and no re-draw across multiple seeds — the two tests are two deterministic example cases whose inputs happen to be RNG-derived rather than hand-picked, not a property exercised over the input space PROPERTIES describes ("arbitrary subsets… not only… three hand-built fixtures"). Per the review skill's property-based-testing guidance, single-draw coverage of a parameterisable component is Medium by default; a genuine property run (e.g. looping `Array.from({length: N})` over distinct seeds, or a real property library) would substantiate the "arbitrary subsets" claim PROP-CLEAN-8 makes and PROPERTIES §1's rules require ("Property-based" carrier cells should mean iterated, not single-seed). | `pdlc/workflows/__tests__/consumerCleanup.test.js:293-330`; PROPERTIES `PROP-CLEAN-8`, §4 (AT-4.1 traceability), TSPEC §5.2 TT-4 |
| F-03 | Low | Local | **`consolidationBuild.test.js`'s two "T33 CLAUDE.md ↔ manifest" tests (guarded by `if (!existsSync(MANIFEST_PATH)) return;`) are now permanently vacuous, and the comment says so, but the tests remain in the suite as if still load-bearing.** Since T19 deletes `distribution-manifest.json` from the build output for good (confirmed above — F-01), `MANIFEST_PATH` will never exist again on any future commit that respects DoD-4, so these two `it()` blocks (`__tests__/consolidationBuild.test.js` ~line 417-460) can never execute their assertion bodies again; they always take the early-return path. The comment above them acknowledges this explicitly ("these two now guard on the manifest's absence — vacuously… does not speculate about post-T28 shape"), so it is not a hidden false-green, but per PROPERTIES §1's own anti-vacuity rule ("no property is satisfied because a search found nothing") and the review skill's "a test that only can pass is not yet a test" principle, dead assertions that can structurally never fire again should be deleted rather than left as permanent no-ops once their guarding condition is known to be permanent (as the comment itself states it is). | `pdlc/workflows/__tests__/consolidationBuild.test.js:417-460` |
| F-04 | Low | Process | **PLAN-pdlc-plugin-retirement.md's own Status column (§2) still reads `⬚ Not Started` for all 33 rows, even though every task in the range T01–T30 is landed on `feat-pdlc-plugin-retirement` (confirmed by REPLAY's 30-commit sweep and `git log`) and this is the Phase-CR final review.** §1.4 defines the status key (`⬚/🔴/🟢/🔵/✅`) precisely so a reader can tell landed work from pending work at a glance; leaving every row at `⬚` makes the PLAN itself misleading about the branch's true state, undermining the "everything traces" principle this review lens exists to protect (a reviewer or successor reading PLAN in isolation would believe none of the 33 tasks have started). | `PLAN-pdlc-plugin-retirement.md` §2 (all 33 rows), §1.4 |

## Questions

None — all findings above are concrete and actionable without further clarification from the author.

## Positive Observations

- `git diff main...HEAD` and `npm test` were run directly rather than trusted from prose: `pdlc/engine` is green (846 tests, 844 pass — the remainder are documented `it.skip`/`test.skip`), and `pdlc/workflows` is 3843/3915 passing with the only two failing suites (`documentOracles.test.js`'s `coveredViolations` walk and `consumerCleanup.test.js`'s tracked-file-diff assertion) both traceable to this reviewer's own dirty local tree (`M .claude/pdlc-wave-state.json`, `?? .claude/workflows/`), exactly the failure mode PLAN §5 point 5 and DoD item 1 warn readers to expect on an unclean checkout — not a code defect.
- `RLH-SKILL-10`'s two conjuncts (`skillFiles.test.js:258-284`) are live and asserting for real, not vacuously: `consolidate-learnings/SKILL.md`'s content does not contain `consolidate-learnings.bundle.js` at HEAD, so the early-return guard is not taken, and both the "no retired host survives" and "every named path exists" conjuncts execute and pass against the real file.
- T13's erratum-6 disposition gate (`preflight-baseline.test.js`) and `documentOracles.test.js`'s post-sweep suite-count assertion both correctly transcribe TSPEC §4.4's corrected **99** rather than FSPEC L-5's stale **97** literal, and the live count (`Test Suites: … 99 total`) matches — the DEC-07-mandated "assert the spec's literal, halt the erratum on disagreement" discipline was followed rather than an implementer silently picking a number.
- DEC-08's wave-gate assertions (`consolidationPreflight.test.js:167-210`) are live, checking both `.claude/pdlc.config.example.json`'s `postWaveCommand`/`postWavePathspecs` values and CLAUDE.md's corresponding prose, and both pass.
- The BL-08 pre-sweep-report gap and the AC-5.1/AC-5.2/AT-4.4 live-engine-run items are already honestly disclosed as High/process and PENDING-OPERATOR in POSTSWEEP-RUN and OPERATOR-OBSERVATIONS rather than papered over — consistent with the orchestrator's own deviation ledger.
- REPLAY's tooling-defect self-correction (the `grep -qE "Test Suites:.*0 failed"` false-negative on runs where Jest omits the "N failed" token entirely) is diagnosed, fixed, and every affected row re-judged rather than silently re-labelled — a good instance of "verify, don't trust the harness."

## Recommendation

**Needs revision** — F-01 is a High finding: a shipped, in-diff documentation claim about this feature's own centerpiece deliverable (the reduced build's output set) is factually wrong, and the one oracle that touches it is asserting the wrong thing rather than verifying it.

## Verdict

VERDICT: Needs revision
{"high": 1, "medium": 1, "low": 2}
