# Cross-Review: product-manager — TSPEC

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-plugin-retirement/TSPEC-pdlc-plugin-retirement.md` (v0.1)
**Date:** 2026-08-17
**Iteration:** 1
**Scope:** Product lens only — requirements traceability, scope compliance, acceptance-criteria fidelity. Technical design quality, test strategy depth and code quality are the SE/TE reviewers' lenses.

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **The cleanup step the TSPEC designs reds AC-1.2 permanently.** §4.2 claims "Nothing it decides introduces a new retired identifier: the cleanup script's name … contains none of the seven, so the script's own source cannot red AT-1.2." The name claim is true; the *source* claim is contradicted by the TSPEC's own §4.3, which requires the nine L-11 names to be **transcribed into `cleanup-consumer-workflows.sh` as a literal list** (BR-CLN-3a). Three of those literals match L-2's search terms verbatim: `consolidate-learnings.bundle.js` / `orchestrate-dev.bundle.js` / `orchestrate-queue.bundle.js` match `\.bundle\.js`, and `.pdlc-drift-state.json` matches both `pdlc-drift-state` and `pdlc-drift` (FSPEC §4.2 L-2 term table). The script is a **new tracked file at `pdlc/hooks/scripts/`**, and A-1's allow-list covers only `docs/completed/**`, `docs/discarded/**`, `docs/_decisions/**`, the baseline file, `**/LEARNINGS-*.md` / `**/POSTMORTEM-*.md`, two test-fixture corpora and `QUEUE.md` (`docs/_constraints/pdlc-retirement-baseline.md:87`–`:95`) — nothing under `pdlc/hooks/scripts/`. The new `consumerCleanup.test.js` (§5.2) hits the same wall, since AT-4.1/AT-4.3 must construct those names. As specified, the P0 criterion AC-1.2's required-empty search returns non-empty at HEAD after the sweep. Needs an explicit resolution in this TSPEC: either route an A-1 extension upstream (baseline + FSPEC L-3's expected-empty literal), or state a name-construction rule that keeps the predicate literal-equivalent without carrying the terms — and say which, because BR-CLN-3a's "transcribed as a literal list" forecloses the second option today. | REQ AC-1.2; FSPEC L-2, L-3, L-11, BR-CLN-3a |
| F-02 | High | Local | **§2.2's "`dist/` survives, still build-generated" decision is not reconciled with the retirement of the wave-gate rebuild.** §2.2 keeps `pdlc/workflows/dist/pdlc-cli.mjs` **tracked** and generated by the reduced build step, and §3.1 keeps `--check`'s STALE/exit-1 contract. Meanwhile class 10 (§2.9) retires the wave-gate *values* `implementation.postWaveCommand` = `node pdlc/workflows/build-runtime.mjs` and `postWavePathspecs` = `["pdlc/workflows/dist/"]` (`.claude/pdlc.config.example.json:1`, baseline M-11h), and class 1 removes the `Generated artifacts are in sync` CI check (FSPEC L-7). But the staleness gate does **not** leave the repo: `consolidationBuild.test.js:156`–`:159` runs `build-runtime.mjs --check` inside the surviving workflow suite, which §5.3 lists as protected. Post-sweep, any wave that edits `pdlc/workflows/cli.mjs` or `orchestrate-dev.js` (both `CLI_SOURCES`, `build-runtime.mjs:530`) leaves the tracked artifact stale with no step regenerating it — the suite reds mid-run, and G-5/AC-5.3's "still produced by the build step rather than maintained by hand" degrades to hand-maintenance in practice. §2.2's consequence list stops at `.gitignore` and never reaches this. The TSPEC must decide it: keep the two wave-gate values (routing an erratum against REQ C-5 / M-11h, which assumed `dist/` retires), or state which surviving step regenerates the artifact after a source edit. | REQ G-5, AC-5.3, C-5, C-7; FSPEC L-7, M-11h |
| F-03 | High | Cross-Feature | **Erratum 3 is named but nothing gates the sweep on it, so the sweep may ship a surviving skill that cannot run.** §6.1 erratum 3 correctly establishes that `consolidate-learnings` loses its only execution host — verified: `pdlc/skills/consolidate-learnings/SKILL.md:9`–`:11` names `pdlc/workflows/dist/consolidate-learnings.bundle.js` as where the pass is performed; the engine's command surface is `dev` / `queue` / `doctor` only (`pdlc/engine/bin/cli.mjs:115`–`:128`); and `consolidate-learnings` sits in `OPERATOR_ONLY_SKILLS` (`pdlc/engine/lib/startup.mjs:52`–`:58`), i.e. it is not an engine-dispatched skill. §2.1 states the module "survives … loses its only execution host", and §2.9 class 11 still schedules the SKILL.md rewrite. But §6.3's obligation table carries only O-A, O-B and T-1…T-3 — **no obligation blocks a deletion commit on erratum 3 being answered upstream**. That leaves a live, human-invocable capability removed by a sweep whose REQ says it removes none (NG-1 keeps the skills; NG-3 forbids pipeline-semantics change), decided inside an engineering artifact rather than in REQ/FSPEC. Add a blocking obligation in §6.3 tying the class-7/class-11 commits to the upstream disposition (restore a host, or an explicit REQ statement that the capability is knowingly parked under SUCC-2). | REQ NG-1, NG-3; FSPEC §3.1 class 11, M-11n |
| F-04 | Medium | Local | **The `driftGenerators.js` consumer measurement is wrong and self-contradictory, in a document whose §1.3 stakes its authority on measurement.** §2.6 item 3 says "eleven `*.test.js` modules import it, of which seven survive"; §4.7 says "eighteen `*.test.js` modules import the helper. Seven are deleted … eleven survive", listing `advisoryConfig`, `advisoryEnvelope`, `advisoryEscalationLog` and `consolidationProperties` as importers. They are not: those four only *mention* the helper in comments (`advisoryConfig.test.js:45`, `advisoryEnvelope.test.js:38`, `advisoryEscalationLog.test.js:59`, `consolidationProperties.test.js:4`), and `driftRepoRoot.test.js` — listed in §4.7 among the deleted importers — imports nothing from it either. Measured at HEAD: twelve `*.test.js` modules plus `helpers/mergeDoubles.js:14` import it; six are deleted (`driftBackups:46`, `driftBaseline:56`, `driftFault:37`, `driftHook:69`, `driftOrdering:36`, `queueDriftGate:60`) and six survive (`approvalHash:39`, `completeness:55`, `forcePhases:30`, `pacingWrapper:60`, `roundDerivation:36`, `scanLines:28`). This also contradicts the C-6 baseline, which records **eight** surviving consumers (`docs/_constraints/pdlc-retirement-baseline.md:45`). The decided outcome — surviving import set `{seeded, resolveSeed, shrink}` — happens to be right and §2.6 mandates a fresh scan anyway, but two "measured at `2cd0d6b1`" claims that disagree with each other and with the baseline are exactly what C-6 exists to prevent. | REQ C-6, R-8; FSPEC M-11p |
| F-05 | Medium | Local | **`--dry-run` is new operator-facing surface no criterion asks for, and no oracle covers it.** §3.2's usage line is `cleanup-consumer-workflows.sh [--dry-run] [<repo-root>]` and contract row 5 specifies its behaviour (print rows 1–3, exit `0`/`3`). No REQ criterion (AC-4.1…AC-4.4) or FSPEC rule (BR-CLN-1…6, §3.5's five outcomes) mentions a dry-run mode, and §5.2's AT table has no row exercising it — so a documented operator behaviour ships untested. Either drop the flag from the interface, or route it upstream as an addition to G-4's step and add the covering assertion in §5.2. | REQ G-4, AC-4.1…AC-4.3; FSPEC §3.5, BR-CLN-1…6 |
| F-06 | Medium | Local | **Retaining `hookCompatibility.test.js` changes M-8's membership, but only the L-5 *count* is routed for correction.** §2.6 operation 2 retains the module (a sound O-E resolution, and the module is verifiably self-contained — its only drift dependency is the `C7` block at `hookCompatibility.test.js:246`–`:294`, and it imports no drift helper), and §4.4 re-derives the post-sweep literal as 99, correcting FSPEC L-5 at C-6 re-measurement per ASM-2. But FSPEC L-5 also **enumerates** `hookCompatibility` among M-8's 21 modules, and the baseline's M-8 row does too (`docs/_constraints/pdlc-retirement-baseline.md:45`, `:114`). C-6 requires every path to sit in exactly one class; this decision moves one path from M-8 to the class-6 reduction set. §4.4 routes the number and not the membership, so a re-measurement that transcribes L-5's list faithfully would still delete the module. State the class reassignment as an explicit upstream correction alongside the count. | REQ C-6, AC-1.3, R-8; FSPEC L-5, ASM-2, §7 O-E |
| F-07 | Low | Local | **§4.6's version table cites the published engine without noting the repo is already ahead of it.** The table records `@kaneho/pdlc-engine@0.2.0` declaring `pluginCompat: ^0.23.0`, which is the right gate for BR-VER-1 — but `pdlc/engine/package.json:3` is at `0.2.1` and the newest tag is `engine-v0.2.0`, i.e. an unpublished engine version sits in the tree. BL-07 is satisfied either way (`^0.23.0` admits `0.23.2`), so this is not a gate failure; naming the unreleased `0.2.1` in the table keeps the "published range" citation unambiguous for the operator who reads it at release time. | REQ BL-07, C-10; FSPEC BR-VER-1 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Does F-01's resolution belong to A-1 (allow-list the cleanup script and its test module) or to BR-CLN-3a (permit the expected names to be assembled at runtime from fragments)? The first keeps the script readable and costs an upstream baseline + FSPEC L-3 edit; the second keeps AC-1.2's search literally empty but makes the refusal predicate harder for an operator to audit by reading, which BR-CLN-4's "checkable without reading the implementation" spirit argues against. Whichever is chosen, AC-1.2's set-equality means the choice cannot be left to the implementer. |
| Q-02 | For F-02: was REQ C-5's "the configured pathspec retires" written on the assumption that `dist/` itself retires (the branch §2.2 has now closed the other way)? If so, the erratum belongs against REQ C-5 / M-11h rather than being absorbed as a class-10 deletion. |
| Q-03 | AC-4.4 asks that a consumer who never cleans up still reaches the configured final phase with a set-equal report. §5.2's AT-4.4 says the leftovers are placed and an engine run compared — is that run the same run BL-08 captures pre-sweep, or an additional post-sweep pair? The obligation table (§6.3) budgets one operator-captured report, so the reviewer cannot tell whether AT-4.4 costs a second engine run. |

## Positive Observations

- §1.1's routed-item table is the single best thing in this document: every FSPEC/REQ open item is answered in one row with the deciding section named, so a reader can check "did the TSPEC decide only what was routed to it" in one pass. §1.2's matching "what this document does not settle" closes the other half.
- §2.2's decision to keep `pdlc/workflows/dist/pdlc-cli.mjs` is argued from what the criteria actually measure — AC-1.1's set-equality and AC-1.2's term set — rather than from taste, and the rejected alternative (`pdlc/workflows/bin/`) is named with its cost. That is exactly the shape a PM can check against G-5.
- §4.5's comparison model is faithful to AC-5.2 and does not quietly widen it: the excluded set maps one-to-one onto REQ's allowed classes (provenance versions, paths, ids, timestamps, the eight run-variable collections), and the "value-compared, explicitly" list — including `engine.startupAuth.row` — turns AC-5.2's exhaustiveness clause into something a test can enforce. Verified against `pdlc/engine/lib/report.mjs:54`–`:95`: every key the model names exists, and `buildEngineBlock()` does default every field to a present value, so the "missing key is always a real difference" premise holds.
- §4.3 transcribes L-11's nine names exactly, including the `.pdlc-backups/`-as-a-whole-directory rule and the deliberate exclusion of `distribution-manifest.json`, with the reason (E-16) preserved rather than paraphrased.
- §4.6 resolves FSPEC O-G in the direction that keeps BL-07 satisfiable without a release on the critical path, and the `0.24.0`-is-an-outage reasoning is checked against the real handshake semantics rather than asserted.
- §5.5 and §5.4 hold the line on C-8 and C-7 without softening: deleted-never-skipped is asserted repo-wide, and the replay loop makes AC-1.8's per-commit greenness something the operator pastes rather than claims.
- §6.1's three errata are real, well-evidenced, and correctly left unfixed here rather than silently absorbed — erratum 3 in particular found a live-capability loss that three prior review rounds had not.

## Recommendation

**Needs revision.**

Exactly what to change:

1. **F-01** — Resolve the AC-1.2 collision the cleanup step creates. Add a subsection under §4.3 stating either the A-1 extension to be routed upstream (naming `pdlc/hooks/scripts/cleanup-consumer-workflows.sh` and `pdlc/workflows/__tests__/consumerCleanup.test.js`) or the name-construction rule, and correct §4.2's claim, which is false as written.
2. **F-02** — Reconcile §2.2 with class 10. Either keep `postWaveCommand` / `postWavePathspecs` (and route the erratum against REQ C-5 / M-11h), or name the surviving step that regenerates `dist/pdlc-cli.mjs` after a source edit, and record the consequence in §2.2 next to the `.gitignore` note.
3. **F-03** — Add an obligation in §6.3 that blocks the class-7 and class-11 commits on erratum 3's upstream disposition, so a surviving skill with no execution host cannot ship unnoticed.
4. **F-04** — Re-measure `driftGenerators.js`'s consumers and make §2.6 item 3, §4.7 and the baseline's count agree, or state which is authoritative.
5. **F-05** — Drop `--dry-run` from §3.2 or route it upstream and give it an AT row in §5.2.
6. **F-06** — Route `hookCompatibility`'s M-8 → class-6 reassignment as an upstream correction alongside §4.4's count correction.
7. **F-07** — Note the unpublished `0.2.1` engine version in §4.6's table.

## Verdict

VERDICT: Needs revision
{"high": 3, "medium": 3, "low": 1}
