# Cross-Review: test-engineer — REQ

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-advisory-tier/REQ-pdlc-advisory-tier.md`
**Date:** 2026-08-03
**Iteration:** 1
**Scope:** testability of REQ-pdlc-advisory-tier v1.1 — black-box acceptance-criterion precision, edge-case completeness, falsifiability of the prohibitions. Not product strategy, not architecture.

## Verification Log

Every existing-behavior claim in the REQ, checked against HEAD (not against the master plan).

| REQ claim | Verified at | Result |
|---|---|---|
| `MODEL_DEFAULT`, `MODEL_IMPLEMENTATION`, `MODEL_QUEUE` exist as constants (AC-1.1) | `pdlc/workflows/orchestrate-dev.js:39`, `:40`; `pdlc/workflows/orchestrate-queue.js:64` | Confirmed. All three are **bare aliases** (`"opus"`, `"sonnet"`) — see F-04 |
| A1: Phase-0 triage returns `needs-human` → skip the candidate, try next (§1) | `pdlc/workflows/orchestrate-queue.js:596-604` (`continue` after pushing to `skipped`) | Confirmed |
| Triage verdict is parsed from a `TRIAGE:` trailer, defaulting to `needs-human` | `pdlc/workflows/orchestrate-queue.js:264-283` | Confirmed |
| A2: a distinct "stale-REQ re-grounding gate" exists | `pdlc/skills/orchestrate-queue/SKILL.md:135-144` — prose **inside the triage prompt**, emitting the same `TRIAGE: needs-human` verdict; no separate gate in the workflow script | **Partially false as stated** — see F-01 |
| A3: Phase DOD verify→remediate is capped at 3 iterations, then halts | `pdlc/workflows/orchestrate-dev.js:24` (`DOD_MAX_ITERATIONS = 3`), loop at `:1218-1261`, halt at `:1953-1961` | Confirmed |
| A4: `ship-pr` reports `REBASE_STATUS: conflict`; pipeline halts, branch unchanged | `pdlc/skills/ship-pr/SKILL.md:54-55`; parser `pdlc/workflows/orchestrate-dev.js:974-990`; halt `:1938-1946` | Confirmed |
| A5: Phase PUB CI red → pipeline halts | `pdlc/workflows/orchestrate-dev.js:1315-1317` (`haltError` on `status === "failed"`) | Confirmed |
| `ciStatus` is derived from the GHA rollup, not from an agent (AC-4.3) | `pdlc/workflows/orchestrate-dev.js:1280`, `:1308-1343` — `_checkCi(prUrl)`, no agent in the poll loop | Confirmed |
| Harvest deletes `CROSS-REVIEW-*` / `CODE_REVIEW-*` after LEARNINGS lands (AC-9.3) | `pdlc/skills/harvest-learnings/SKILL.md:27,33,95-100`; guard `pdlc/hooks/scripts/guard-harvest-before-delete.sh:35,43` | Confirmed — but the guard's token regex matches **only** `CROSS-REVIEW`/`CODE_REVIEW`; `ADVISORY-*` is not covered. See F-07 |
| `REQ-MERGE-03` exists and defines self-modification paths (AC-3.4, NFR-5) | `docs/pdlc-merge-phase/REQ-pdlc-merge-phase.md:106` (AC-3.1 names `pdlc/workflows/` and `pdlc/skills/` as the two non-removable defaults) | Confirmed — citation is to a real authority. See Q-03 |
| `docs/_queue/ESCALATIONS.md` does not exist yet (BL-04) | `docs/_queue/` contains only `QUEUE.md` | Confirmed |
| Upstream `docs/design/MASTER-PLAN-engineering-loop.md` exists | present | Confirmed |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **Seams A1 and A2 are not distinguishable by any observable signal, so AC-5.1 and AC-5.2 cannot both be tested.** The REQ treats them as two seams with different advisory behaviors (A1 → `ready`/`blocked`/`escalate` adjudication; A2 → a re-grounding proposal). At HEAD they are one code path: the re-grounding gate is prose *inside the triage prompt* (`pdlc/skills/orchestrate-queue/SKILL.md:135-144`) and emits the same `TRIAGE: needs-human <free-text reason>` trailer as every other ambiguity (`pdlc/workflows/orchestrate-queue.js:264-283`, consumed at `:596-604`). Nothing in the trailer says *which* seam fired. A black-box test cannot set up "given the re-grounding gate fires" without asserting on free-text prose. **Resolution:** state the observable discriminator as an acceptance criterion — e.g. the triage trailer carries a machine-readable seam token, and `needs-human` without a recognised token routes to the A1 adjudicator. Without it, AC-5.2/AC-5.3 have no testable precondition. | §1 seam table; AC-5.1, AC-5.2 |
| F-02 | High | Local | **The prohibitions (REQ-ADV-04) are absence-only oracles: the REQ never states what happens *instead* when a forbidden action is proposed.** AC-4.1–AC-4.5 and AC-3.2 say the action is "refused by the workflow script", but no AC names the positive outcome on that path: is the refusal an escalation (REQ-ADV-10)? is it recorded in the advisory record (AC-9.1)? does the run continue, skip, or halt? does the refusal itself carry a reason code? AC-4.6/NFR-2 mandate a failing test per prohibition, but a test written from this text can only assert "the merge did not happen" / "`ready: true` was not written" — which any accidental state also satisfies (project standard: every negative assertion needs a paired positive assertion on the same path). **Resolution:** add one AC giving the refusal path its observable triple — exact outcome (escalated / refused-and-continued / halted), a named refusal reason, and the record written — so each prohibition test has a positive conjunct. | AC-3.2, AC-4.1–4.6, NFR-2 |
| F-03 | High | Local | **The envelope predicates in AC-3.3/AC-3.4 have no operational definition, so the in-code enforcement they mandate cannot be specified or tested.** Four undefined terms carry the entire control: (a) "files the feature branch itself created" — created relative to what base? merge-base with the default branch, the PR's changed-file list, or the branch point at Phase DOD's rebase? These differ after AC-8.3's re-verify; (b) "a lint, format, or type error **introduced by** the feature branch" — introduced vs. pre-existing is exactly the distinction AC-8.4 says must be drawn, but the rule for drawing it is stated nowhere; (c) "any change outside the feature's **declared scope**" — the REQ never says where scope is declared (REQ §5? FSPEC? the PLAN's touched files?); (d) "re-running a **flaky** check" — no definition of flaky, and no cap on re-runs distinct from AC-8.2's 3 attempts. Each of these is an outcome-level decision, not a seam design detail, so it belongs here. **Resolution:** give each envelope entry a single-sentence decidable rule (e.g. "branch-created = absent at the merge-base tree"), leaving only the mechanism to the TSPEC. | AC-3.3, AC-3.4, AC-7.1, AC-8.4 |
| F-04 | High | Local | **AC-1.1 pins a literal model string the REQ itself says is probably wrong, which forces the AC-1.1 test into an implementation echo.** AC-1.1 fixes the default to `claude-fable-5`, while BL-01 records that the alias is unverified and notes the three existing constants are bare aliases — confirmed: `MODEL_DEFAULT = "opus"`, `MODEL_IMPLEMENTATION = "sonnet"` (`pdlc/workflows/orchestrate-dev.js:39-40`), `MODEL_QUEUE = "sonnet"` (`pdlc/workflows/orchestrate-queue.js:64`). A test transcribed literally from AC-1.1 will fail on day one and be "fixed" by importing `MODEL_ADVISORY` and asserting it equals itself — the exact implementation echo the project forbids. **Resolution:** restate AC-1.1 as the observable property (a single constant, resolvable by the runtime, naming the Fable 5 rung) and let the TSPEC pin the literal once BL-01 is resolved; keep AC-1.4's unresolvable-value failure as the falsifiable oracle. | AC-1.1, BL-01 |
| F-05 | Medium | Local | **Attempt and time budgets are "configured" but never valued, so AC-2.4 and NFR-4 have no test.** AC-8.2 gives 3 attempts for A5 only. AC-2.4 ("exceeds its configured attempt budget"), NFR-4 ("more than its configured budget", no unit) and the A1/A2/A3/A4 seams give none. NFR-4 additionally lacks the observable: is the budget wall-clock per seam, per invocation, or per pipeline, and what is measured to detect the overrun? **Resolution:** state a shipped default per seam and the unit for NFR-4, or explicitly declare one global default that every seam inherits. | AC-2.4, NFR-4 |
| F-06 | Medium | Local | **AC-4.5 ("the original deterministic gate re-runs") is not satisfiable at every seam, and the REQ does not say which gate re-runs where.** At A2 the "original gate" is Phase-0 triage — an *agent* verdict (`pdlc/workflows/orchestrate-queue.js:586-589`), not a deterministic gate; re-running it after applying a re-grounding patch also interacts with AC-5.4's one-pick-per-invocation guarantee, which the REQ leaves unaddressed. At A4, AC-7.4 substitutes "the branch's tests re-run", which is a different gate from the one that fired (`ship-pr` rebase). **Resolution:** replace the blanket AC-4.5 with a per-seam table naming the gate that re-runs and the state it must reach, so each row is one test. | AC-4.5, AC-5.4, AC-7.4 |
| F-07 | Medium | Local | **AC-9.3's "harvested exactly like `CROSS-REVIEW-*` and `CODE_REVIEW-*`" is falsified by the guard at HEAD, and the REQ does not say whether the guard extends.** `pdlc/hooks/scripts/guard-harvest-before-delete.sh:35,43` matches only the literal tokens `CROSS-REVIEW` and `CODE_REVIEW`, so an `ADVISORY-{feature}.md` deletion is *not* blocked when LEARNINGS is absent — "exactly like" is untrue unless the guard is extended, and the REQ owns that decision because it is an observable protection, not an implementation detail. AC-9.2 has the same absence-only shape as F-02 ("action taken without a record ... asserted against by test") with no statement of what the pipeline does when the record write fails. **Resolution:** state whether the LEARNINGS-precedes-delete protection covers `ADVISORY-*`, and give AC-9.2 a positive outcome for a failed record write (escalate / halt / retry). | AC-9.2, AC-9.3 |
| F-08 | Medium | Local | **`ESCALATIONS.md` is declared a cross-feature data contract (BL-04: "its format is defined here and consumed by `pdlc-engineering-loop`") but only its field *names* are given.** A downstream parser needs: append order (newest first or last), the entry delimiter/heading grammar, whether an escalation for the same feature+seam appends again or updates in place across re-invocations, and what "pipeline state at the time" means as a value set. Without these, neither the producer test nor a `pdlc-engineering-loop` consumer test can be written, and set-equality over the field list is unenforceable. **Resolution:** specify the entry grammar and the repeat-escalation rule. | AC-10.1, AC-10.2, BL-04 |
| F-09 | Medium | Local | **NFR-3's "byte-identical to today's behavior" is not a testable oracle, and AC-1.6 is absence-only.** Byte-identical over what artifact — the final report text, the git diff, the phase table? Report text already varies (timestamps, iteration counts). AC-1.6's "no model resolution is attempted" is unobservable from outside without a paired positive ("the run completes with the same phase outcomes and the report carries no advisory summary"). **Resolution:** restate NFR-3 as a concrete equality on a named artifact (e.g. the report's phase table and outcome are unchanged, and no `ADVISORY-*` file is created), and pair AC-1.6 with the positive outcome. | NFR-3, AC-1.6 |
| F-10 | Medium | Local | **AC-3.4's test-tampering exclusion is stated as a single predicate ("any change to a test assertion") and misses the equivalent evasions.** Deleting a test file, renaming a test out of the collected set, adding a skip/xfail marker, narrowing a parametrised case list, or lowering a coverage threshold all achieve exactly the failure mode AC-3.5 calls "the single most dangerous", and none is literally "a change to a test assertion". Because AC-3.5 demands this exclusion be asserted by test, the enumeration must be closed and checkable by set-equality — a deleted case must fail the test. **Resolution:** enumerate the excluded operations explicitly. | AC-3.4, AC-3.5 |
| F-11 | Low | Local | **`confidence ∈ {high, medium, low}` has no observable difference between `medium` and `low`.** AC-2.2 gates action on `high` only; nothing else in the REQ reads the value. As written the enum is a two-valued contract with three labels, and no test can distinguish a `medium` verdict from a `low` one. Either state where the distinction is observable (advisory record? escalation text? retry policy?) or collapse the enum. | AC-2.1, AC-2.2 |
| F-12 | Low | Local | **AC-9.4's advisory summary is not closed under the seam set.** "count of invocations, count resolved, count escalated, by seam" leaves open whether all five seams A1–A5 appear with zero counts or only those invoked. Only the former admits a set-equality assertion that fails when a seam is dropped. | AC-9.4 |
| F-13 | Low | Local | **AC-8.2's re-poll loop has no stated interaction with Phase PUB's existing timeouts.** Phase PUB already owns a 10-minute no-checks window and a completion cap measured from first check registration (`pdlc/workflows/orchestrate-dev.js:1308-1343`). After an AC-8.2 push, is the completion budget restarted or inherited? A test of "3 attempts then escalate" cannot be written without knowing whether a timeout during attempt 2 counts as an attempt or as a separate escalation. | AC-8.2 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | AC-8.3 says a post-DOD push makes Phase DOD **re-verify**. Does re-verification restart the 3-iteration `DOD_MAX_ITERATIONS` budget (`pdlc/workflows/orchestrate-dev.js:24`) or continue the earlier count? The answer decides whether an A5 fix loop can, in the worst case, run 3 CI attempts × 3 DoD iterations, and it is the precondition for any test of AC-8.3. |
| Q-02 | AC-5.3 permits applying a re-grounding proposal to a REQ. Who commits that edit, on which branch, and does the amended REQ then re-enter triage in the *same* invocation or the next one (AC-5.4 says at most one candidate is picked per invocation)? The observable difference matters for the queue's serial guarantee. |
| Q-03 | AC-3.4 excludes "anything under REQ-MERGE-03's self-modification paths" — verified as `pdlc/workflows/` and `pdlc/skills/` (`docs/pdlc-merge-phase/REQ-pdlc-merge-phase.md:106`). In *this* repo essentially every feature's diff is under those paths, so the advisory tier is excluded from acting on almost every self-hosted run. Is that intended (the tier is for consuming repos), and if so should the REQ say so, since otherwise the A4/A5 seams have no reachable in-envelope case here and their acceptance tests would only ever exercise the escalation branch? |
| Q-04 | AC-2.3 treats a malformed verdict as an escalation. Does a malformed verdict consume an AC-2.4 attempt, or escalate immediately? A parser test needs the answer to assert attempt counts. |
| Q-05 | AC-7.4 reverts the resolution when the branch's tests fail. Which command defines "the branch's tests", and what is the observable post-revert state (rebase aborted, branch at pre-rebase SHA, `ADVISORY-*` and `ESCALATIONS.md` both written)? |

## Positive Observations

- The seam table in §1 is the strongest part of the document for testing purposes: five named seams, each with a stated *current* behavior. Every one of those current behaviors checked out against HEAD (see Verification Log) — that is unusually good grounding for a REQ, and it means the "before" half of every regression test is already writable.
- AC-4.5 ("the advisory tier fixes causes; gates decide outcomes") is the right invariant and is stated as an invariant rather than a procedure. Once F-06 gives it a per-seam gate table, it becomes a small, mechanical test matrix.
- AC-8.4 is grounded in a recorded real occurrence and names the failure mode precisely (attributing a pre-existing default-branch failure to a feature). That is exactly the kind of edge case that is usually discovered in production.
- AC-1.3's requirement that a fallback run be *distinguishable* from an intended-rung run, and AC-9.4's requirement that the report name the model actually used, together give the fallback path a positive oracle rather than an absence-only one — the pattern F-02 asks for elsewhere in the document.
- AC-3.5's insistence that test-assertion editing be "structurally impossible rather than discouraged", and NFR-1's "a prompt instruction is not a control", place enforcement in the workflow script where it can be tested. This is the correct altitude for a REQ to take a position on.
- The deferral table (D-ADV-01…05) binds each deferral to a named successor or explicitly to none, so nothing is silently dropped.

## Recommendation

**Needs revision**

Four High findings must be closed before this REQ can carry a test suite:

1. **F-01** — give seams A1 and A2 an observable discriminator, or merge them into one seam with one behavior. As written, AC-5.2/AC-5.3 have no testable precondition.
2. **F-02** — give every prohibition in REQ-ADV-04 (and the AC-3.2 refusal path) a positive outcome: exact end state, named refusal reason, and the record written. AC-4.6/NFR-2 currently mandate tests that can only be absence-only oracles.
3. **F-03** — give each envelope entry in AC-3.3/AC-3.4 a decidable rule ("branch-created", "introduced by the feature branch", "declared scope", "flaky"). These four terms are the whole control surface; undefined, they cannot be enforced in code as NFR-1 requires.
4. **F-04** — restate AC-1.1 as an observable property rather than the literal `claude-fable-5`, which BL-01 already flags as unverified. As written the AC forces its own test into an implementation echo.

The six Medium findings (F-05…F-10) are each a missing value or a missing enumeration rather than a design change, and should be closable in the same revision.

## Verdict

VERDICT: Needs revision
{"high": 4, "medium": 6, "low": 3}

