# Cross-Review: test-engineer — PLAN

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-advisory-tier/PLAN-pdlc-advisory-tier.md` (v1.1)
**Date:** 2026-08-03
**Iteration:** 1
**Scope:** testing lens only — testability, TDD ordering, batch-DAG mechanics, oracle falsifiability, coverage strategy.

## Verification performed

Every claim below was executed or grepped against the working tree at HEAD `e7ffd1d` (the PLAN cites
`ca55bb6`; only doc commits separate them, and every code line the PLAN cites still resolves).

| Check | Result |
|---|---|
| §2.2 `BL-PREREQ` line numbers | **all 18 rows confirmed** — `guardVerdict` `orchestrate-dev.js:731`, `effectiveGuardPaths` `:708`, `commitPaths` `:6905` (no `export`), `gitWithLockRetry` `:6862`, `parseImplementationConfig` `:181`, `rebaseOntoDefault` `:6254`, `raisePrAndVerifyCi` `:6337`, `checkPrCi` `:5927`, `defaultAppendFile` `:6805`, `dodVerifyLoop` `:6273`, `parseDodStatus` `:6059`, `parsePlanTasks` `:2039`, `buildFinalReport` `:8595` (no `export`), `MERGE_ESCALATIONS` `:1321`, `MODEL_IMPLEMENTATION` `:1621`; `orchestrate-queue.js` `parseTriageVerdict:302`, `triagePrompt:653`, `precheckDependencies:630`, `parseQueue:116`, `buildQueueReport:1221`, `runPicked:961`; guard-script tokens at `:35`, `:43`, `:57`–`:59` |
| §2.1 rebase pins | `git merge-base --is-ancestor 26c3f1c HEAD` ⇒ true; same for `5d66c48`. A-01 replacing the rebase task is justified |
| §7 integration-point line numbers | all resolve within ±2 lines (`:8282`–`:8288` rebase halt, `:8294`–`:8302` DoD halt, `:8341`–`:8350` guard literal + regex, `:6371` `status === "failed"`, `queue:890`–`:897` precheck skip, `queue:912`–`:920` needs-human, `queue:758` `main` params — confirmed to lack `_appendFile` today, `build-runtime.mjs:87` export array / `:96`–`:103` prelude) |
| §9.1 parse claim | **executed.** `parsePlanTasks` ⇒ 37 tasks, `parsePlanOwnership` ⇒ 37 rows, `validatePlanContract` ⇒ `{"ok":true}`, `computeTopologicalBatches` ⇒ **20** batches, no cycle, no label warning. §5.2's 20-row transcription matches the executor's grouping exactly |
| Batch-DAG re-derivation | **re-derived all 37 rows** from the `Deps` column: every `Batch` cell equals `max(dep batch) + 1`. No desync, no cycle, no unresolved dependency, no duplicate id |
| Same-batch same-new-file | **no collision.** Every file in §4 has exactly one owner; §3 batch 4 (A-16/A-17/A-28), batches 10–12 (dev+queue pairs) and batch 17 (A-34/A-35) are path-disjoint |
| §5.1 gate defect (A-00's premise) | **reproduced.** `npm test -- --testPathIgnorePatterns=documentOracles` at HEAD ⇒ `Test Suites: 23 failed, 69 passed, 92 total`. The restated form ⇒ `--listTests` returns exactly **68** files, as claimed |
| Existing test-infra citations | `__tests__/helpers/mergeDoubles.js`, `helpers/seams.js`, `helpers/guardFixtures.js`, `fixtures/tmpGitFixture.js` all exist; `helpers/advisoryDoubles.js` and `fixtures/created-files-26c3f1c.json` are correctly declared new; no `advisory*.test.js` exists yet |
| TSPEC §14.1 symbol set | exists and is per-component, but see F-06 |

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Cross-Feature | **The RED-terminal wave gates are unsatisfiable against the script-owned gate, and will halt Phase I at executor batch 3.** §5.2 describes executor batches 1, 3–5 and 6 as "RED-terminal" — the new tests "must fail for the specified reason". But Phase I's script-owned gate runs the *whole* configured suite after every wave and **throws `haltError` on failure** (`pdlc/workflows/orchestrate-dev.js:8111-8118`: `const gate = await runCommandFn(implConfig.testCommand); if (!gate \|\| gate.ok !== true) { throw haltError(...) }`). There is no per-wave scoping seam — `testCommand` is one string with no task/file filter. So the wave that lands A-03…A-07 fails its gate and the pipeline halts before A-08 is dispatched. The same defect makes §5.2's batches 7–17 wording ("the whole suite is green") false by construction: `advisoryDriver.test.js` is red from executor batch 3 until A-22 (batch 11), `advisoryDisabled.test.js` from batch 6 until A-33 (batch 18), `advisoryBundle.test.js` from batch 5 until A-32 (batch 17). **Resolution:** fold each 🔴 row into its 🟢 successor as one TDD task (red-then-green inside the task, which is what `se-implement` already does), or have each 🔴 task author its cases as `test.todo`/`describe.skip` and have the 🟢 task un-skip them. Whichever is chosen, §5.2's gate wording must describe a gate the executor can actually pass. | §5.2; §3 rows A-03…A-16 |
| F-02 | High | Local | **A-00 cannot repair the wave gate from inside batch 1 — the config is read once, before the wave loop.** `implConfig` is parsed at `orchestrate-dev.js:8040-8042` (`readMergeConfigSafely` → `parseImplementationConfig`), *outside* and above the wave loop at `:8094`. Every wave in the run then uses that one cached `implConfig.testCommand` (`:8112`). A-00's edit to `.claude/pdlc.config.json` therefore changes nothing for any gate in the run that contains it, and §5.1's mitigation ("batch 1's gate is re-run by hand once after A-00 lands") does not rescue it: `:8113` has already thrown `haltError` and the pipeline is halted, not paused. The premise is right and verified — the shipped command yields `Test Suites: 23 failed, 69 passed, 92 total` at HEAD — which makes this worse, not better: **wave 1 halts unconditionally**. **Resolution:** land the `.claude/pdlc.config.json` repair as a pre-Phase-I operator step (or in the PLAN-authoring commit itself) and delete A-00 from the task table, renumbering nothing else; or state explicitly that Phase I must be re-invoked after the halt so the second invocation reads the repaired command. | §5.1, §3 A-00, §5.2 batch 1 |
| F-03 | High | Local | **T-02-1, T-02-2 and T-02-3 are assigned to two different test files by two sections of this PLAN, and the §3 assignment strands them across five batches.** §3's A-05 row claims "T-02-1…T-02-5" for `advisoryVerdict.test.js`; §8.1 assigns T-02-1/2/3 to `advisoryDriver.test.js` (A-07) and only T-02-4/5 to `advisoryVerdict.test.js`. §8.1 is the correct one — FSPEC:284-286 shows T-02-1/2/3 are whole-lifecycle cases (apply→gate→`resolved`; `low-confidence` escalation; envelope disagreement), provable only against `runAdvisorySeam`, which does not exist until A-22 (batch 9). Under §3's assignment those three cases are authored into a file whose 🟢 owner (A-19, batch 6) implements only `parseAdvisoryVerdict`/`budgetExceeded` and structurally cannot make them pass. §8.1's own promise — "Every case has exactly one home" — is violated. **Resolution:** correct A-05's row to `T-02-4, T-02-5` and add `T-02-1, T-02-2, T-02-3` to A-07's row. | §3 A-05/A-07; §8.1 T-02 row |
| F-04 | Medium | Cross-Feature | **No task calls for a property-based / generator-driven test, though the feature is almost entirely parsers, classifiers and validators and the repo already ships the library.** `parseAdvisoryConfig`, `parseAdvisoryVerdict`, `parseTriageVerdict`, `parseA3Classification`, `classifyEnvelope`, `refusalReasonFor`, `governingClass`, `budgetExceeded`, `renderAdvisoryEntry` and `renderEscalationEntry` are all parameterisable over an input space with statable invariants, and TSPEC §13.2 itself files ten of them as "Pure-function unit". Every §3 red row enumerates *example* cases only. The precedent is in-repo, not hypothetical: `pdlc/workflows/__tests__/helpers/driftGenerators.js:1-30` is a seeded, dependency-free property-generation library (xorshift32 `seeded(seed)`, `resolveSeed` honouring `PDLC_PROP_SEED`, exhaustive `enumerateLeaves()`), consumed by ten shipped suites (`approvalHash.test.js`, `completeness.test.js`, `driftOrdering.test.js`, `forcePhases.test.js`, …), and it was introduced by a prior PLAN with its own single-writer ownership row. **Resolution:** name at least one property per parameterisable leaf — e.g. `parseAdvisoryVerdict` is total (never throws) over arbitrary strings and its five malformedness rules partition the input space; `budgetExceeded` is monotone in elapsed time and invariant under the `waitMs` carve-out; `classifyEnvelope` is idempotent and its result is always a member of `ADVISORY_REFUSAL_REASONS ∪ {null}` — and either reuse `driftGenerators.js` (adding an ownership edge) or give A-02 the generator export and say so in §6.1's signature block. | §3 A-03…A-09; §6 |
| F-05 | Medium | Local | **A-13's task row omits T-08-4b, which §8.1 assigns to the file A-13 owns.** §8.1 puts `T-08-3 … T-08-6, T-08-4b, T-08-8, T-08-9` in `advisoryHarvest.test.js`; A-13's row lists only "T-08-3…T-08-6, T-08-8, T-08-9". FSPEC:746 shows T-08-4b is a distinct unit-scoped guard case ("a direct delete of `ADVISORY-{feature}.md` with no sibling `LEARNINGS-{feature}.md` … is refused, the refusal names the artifact class, and the file still exists") that T-08-4 does not subsume. This PLAN's own §6.2 warns that obligations visible only outside the task row "are routinely dropped when an agent sees only a one-line task row" — this is that failure mode, in the PLAN itself. **Resolution:** add T-08-4b to A-13's row. | §3 A-13; §8.1 T-08 row |
| F-06 | Medium | Local | **The coverage floor has no falsifiable measurement procedure, and it is filed under "a machine can decide each of these".** §6.4 sets "90% statements / 85% branches over the advisory surface (every symbol TSPEC §14.1 names)" and gives an invocation whose reporting granularity is the *file*: `--collectCoverageFrom='orchestrate-dev.js'` yields one percentage for an 8,642-line file whose pre-existing coverage dominates any advisory-only signal, in either direction. Three further problems: (a) TSPEC §14.1's "symbol set" is not a set of symbols — rows include `structural non-calls + SeamOps.verifyGate`, `the post-PUB distil step`, `the guard-script edit`, `the single enabled early return`, `the D-6 fixture`; (b) it also includes five *pre-existing* symbols marked `(reused)` — `guardVerdict`, `checkPrCi`, `commitPaths`, `rebaseOntoDefault`, `_runCommand` — which this feature does not own and whose coverage it should not be held to; (c) §6.4 says the number "is read against the §14.1 symbol set by a human or by the DoD reviewer", yet §9.1 lists the floor under **Mechanical — a machine can decide each of these**. As written the checkbox cannot be ticked or refuted. **Resolution:** enumerate the advisory symbol set in this PLAN (not by reference), and state the mechanical procedure — e.g. `--coverage --coverageReporters=json-summary` plus a named script that reads per-function/per-line ranges for exactly those symbols — or move the floor to §9.2 (behavioural) and say who reads it against what. | §6.4; §9.1 |
| F-07 | Medium | Local | **The `ciStatus`-provenance oracle is grep-shaped, with no runtime conjunct.** A-11 names "the grep-shaped `ciStatus`-provenance assertion" and §9.3 restates it as "`ciStatus` is still assigned only from `checkPrCi`'s return; no advisory value reaches it". A source grep is not falsifiable by the failure it guards: it passes against a runtime path that assigns around `checkPrCi` through a variable, a spread, or a helper. The behavioural oracle is available and cheap — A-24's `verifyGate` re-polls `checkPrCi` (`orchestrate-dev.js:5927`), so a test can drive the A5 seam to a `resolved` disposition with a **call-count spy** on `checkPrCi` and assert (i) the spy ran ≥1 after `apply`, and (ii) the reported `ciStatus` is byte-equal to the spy's last return value — a positive assertion on the same path, not "no advisory value reaches it". **Resolution:** keep the grep as a cheap secondary, and add the call-count + value-identity oracle to A-11's row and §9.3's checkbox. | §3 A-11; §9.3 |
| F-08 | Medium | Local | **A-34 is a hard gate on A-36 but has no procedure an implementing agent can execute, and no fallback.** A-34 ("Dispatch one trivial advisory agent on `\"fable\"` in a real workflow runtime") is a batch-17 task that A-36 depends on. The workflow runtime is the consumer's Claude Code runtime loading `.claude/workflows/`, which no task in this PLAN populates — `sync-workflows.sh` is explicitly excluded (§4.2: the consumer copy "is refreshed by `pdlc/hooks/scripts/sync-workflows.sh`, never by a task here"). A Phase-I wave agent therefore has no way to satisfy A-34, and the wave gate cannot distinguish "recorded a real observation" from "wrote a plausible sentence". **Resolution:** either mark A-34 an operator task explicitly excluded from the wave DAG (and drop A-36's edge to it), or give it a runnable procedure plus a stated fallback record ("runtime unavailable in this environment — BL-01 remains open, recorded as such"), so the artifact is honest either way. | §3 A-34; §10.1 item 1 |
| F-09 | Low | Local | §4.1's prose says "**Nine** tasks own `orchestrate-dev.js`", but its own table lists twelve (A-17, A-18, A-19, A-20, A-21, A-22, A-23, A-24, A-25, A-26, A-27, A-33) and the paragraph two lines below correctly says "twelve distinct batches for twelve owners". Fix the count. | §4.1 |
| F-10 | Low | Local | §8.1's total row says "**13 files** (11 above + `advisoryPreflight.test.js`, `advisoryBundle.test.js`)". The table names **12** distinct files (config, rung, verdict, driver, envelope, queueSeams, dodSeams, pubSeam, record, harvest, escalationLog, disabled), so the total is 14, matching §4's manifest. | §8.1 |
| F-11 | Low | Local | A-00 changes the wave gate but no task pins the repaired `implementation.testCommand`, so a later edit can silently reintroduce the 23-failing-suite state. Cheapest oracle: one case in `advisoryPreflight.test.js` (A-01) reading `.claude/pdlc.config.json` and asserting `implementation.testCommand` contains all four ignore patterns — set-equality against a transcribed literal, not a substring match. (Applies only if A-00 survives F-02.) | §5.1; §3 A-00/A-01 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | A-15 must "capture the created-file set of a run at `26c3f1c`", but no task says how a tree at `26c3f1c` is obtained inside Phase I — checkout is not available to a wave agent (the orchestrator owns the branch), and §2.3 explicitly forbids `git checkout -- .`. Is the intent a `git worktree` at the pin, a `git stash`-free detached read, or an operator pre-step like F-02's? The `scenario` header records the `command` after the fact; the PLAN needs to state it before. |
| Q-02 | §6.2 assigns "Tree-state invariants … `fixtures/tmpGitFixture.js` — compare `git status --porcelain` and `git rev-parse HEAD` before and after" to A-07, A-10, A-11. A-07 is the driver unit test against a *fake* `SeamOps`; no real git tree is touched on that path. Is A-07 in that row deliberately (the `revert`-on-`{ok:false}` case), or should the row list A-10/A-11 only? |
| Q-03 | A-01 asserts the guard script's tokens "by reading the hook" at HEAD, and A-28 (batch 4) then extends that hook's refusal message. If A-01's assertion is byte-exact on the message it goes red at batch 4 — the first casualty of F-01's gate problem. Can A-01's guard assertions be pinned to the *stable* substrings (`CROSS-REVIEW`, the bracketed-directory shape) that A-28 promises not to change, and can that promise be stated in A-01's row rather than only in §5.4(3)? |
| Q-04 | §9.2 requires every one of the 81 cases to be "failing before its green task and passing after". Under any resolution of F-01 that folds red into green, that per-case red evidence is no longer a batch boundary the executor records. What artifact carries it — a per-task commit pair, or an entry in the DoD `CODE_REVIEW`? |

## Positive Observations

- **The batch column is mechanically correct.** All 37 rows re-derive to `max(dep batch) + 1` from the declared `Deps`; the graph is acyclic, ids unique, every dependency resolves. `computeTopologicalBatches` reproduces §5.2's 20-row transcription exactly, including the size-cap splits `A-03…A-07 / A-08…A-12 / A-13…A-15`. This is the first PLAN I have reviewed here where the executor numbering was actually run rather than asserted.
- **§4's file-ownership manifest is genuinely disjoint per batch**, and §4.1 does the harder check — disjointness in the *executor's* batches, not just §3's labels. The "only the 🔴 row owns the test file; the 🟢 row repeats it but does not own it" convention (§3 preamble) is exactly the rule that stops last-writer-wins coverage loss.
- **§5.1's gate defect is a real, reproduced defect**, found by the author rather than by review, with the reproduction transcript in the document. It fails at HEAD as described (23 suites), and the restated command collects exactly the 68 files claimed. F-02 is about *where* the repair lands, not whether it is right.
- **§6.1's canonical-doubles module with named export signatures, plus AC-INFRA-1 prohibiting per-test ad-hoc equivalents**, is the correct answer to driver-vs-seam contract drift, and it correctly composes with the shipped `mergeDoubles.js` / `seams.js` / `guardFixtures.js` / `tmpGitFixture.js` rather than re-authoring them — all four verified present.
- **§6.4's final paragraph names the four obligations no percentage can express** — seven X-a operations as seven named tests, the positive V-8 triple alongside every prohibition, closed sets compared as sets, and the D-6 expected value transcribed rather than derived. §8.2 then expands each into a task. That is the anti-absence-only, anti-implementation-echo discipline stated at PLAN altitude, which is where it is cheapest to enforce.
- **§5.3's "A-15 gates A-16, not the reverse"** is precisely the right reasoning: a test that generates its own expected value is incapable of failing. Naming that as a dependency-edge justification is better than leaving it as a convention.
- **§8.3 note 1** correctly refuses to let the phase-integration test subsume the seam-unit test for the no-`testCommand` path, and says why neither subsumes the other. That is the distinction most PLANs collapse.
- **§6.3's per-artifact lifecycle table** closes the gap where Phase H deletes only `CROSS-REVIEW-*`/`CODE_REVIEW-*` — including the observation that the manual-verification file's pattern is *not* guard-watched, so its deletion will not be stopped.

## Recommendation

**Needs revision**

Three High findings block approval, and the first two are the same class of problem: the PLAN's
verification story is stated in prose the executor never reads.

1. **F-01** — reconcile §5.2's gate wording with `orchestrate-dev.js:8111-8118`. RED-terminal waves
   cannot exist under a full-suite gate that halts on failure. Fold 🔴 into 🟢, or ship the red cases
   skipped and un-skip them in the green task.
2. **F-02** — move the `.claude/pdlc.config.json` repair out of batch 1. `implConfig` is read at
   `:8040-8042`, before the wave loop at `:8094`; A-00 cannot affect its own run's gate, and wave 1
   halts unconditionally as things stand.
3. **F-03** — correct the T-02-1/2/3 assignment so A-05's row and §8.1 agree, and so those three
   lifecycle cases land in the file whose green owner (A-22) can satisfy them.

Then the five Mediums: name the property strategies (F-04, with `driftGenerators.js` as the in-repo
precedent), add T-08-4b to A-13 (F-05), make the coverage floor measurable or move it out of the
mechanical list (F-06), pair the `ciStatus` grep with a `checkPrCi` call-count + value-identity oracle
(F-07), and give A-34 an executable procedure or an explicit operator carve-out (F-08).

The Lows (F-09, F-10, F-11) are counts and a cheap regression pin; fold them into the same revision.

Nothing in the analysis, the dependency graph or the ownership manifest needs rework — the DAG is
correct and the doubles strategy is right. The revision is about making the gates and the case
assignments say what the machine will actually do.

