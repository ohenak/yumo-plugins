# Cross-Review: product-manager — PLAN

**Reviewer:** product-manager
**Document reviewed:** docs/pdlc-advisory-tier/PLAN-pdlc-advisory-tier.md
**Date:** 2026-08-04
**Iteration:** 9 (delta re-review)
**Scope of this round:** delta only. Base `279bc38` (PLAN v1.8, the bytes reviewed in this
reviewer's v8) → head `06040a4` (PLAN v1.9). One content commit, `06040a4`, touching one §3 cell
(the A-07 row), the §10 changelog, and the header version stamp — `git diff 279bc38 --stat` reports
`5 +++--` on the PLAN, of which two lines are the version stamp swap. Sections outside that diff are
not re-reviewed.

## Prior findings — disposition

One prior finding, the v8 Medium. Checked against the repository at HEAD, not against the
changelog's claim about itself.

| Prior | What it asked for | Disposition |
|---|---|---|
| **v8 F-01** (Medium, Local) — §3's A-07 row (`PLAN:258`) still stated the per-seam gate-exclusivity cases "each need that seam's `verifyGate` to exist" and named **A1** as the sole gateless exception, contradicting §8.2 (`PLAN:869`) after DEC-ADV-11. The ask was, in one cell and with no structural change: cover both gate *representations*, extend the parenthetical to **A1 and A3**, cite FSPEC §5.4 / `TSPEC:657` / DEC-ADV-11, and leave the block assignment (A3+A4 ⇒ `A-23`) untouched | rewrite clause (b) of §3's A-07 row only | ✅ **Resolved** by `06040a4`, and resolved as specified rather than approximately. `PLAN:258` clause (b) now reads "each need that seam's gate *representation* to exist — its `verifyGate` for A2/A4/A5, its `verifyGate: null` for A1 **and A3**", and the parenthetical opens "**A1 and A3** declare no gate" and carries all four asserted conjuncts the finding named: `verifyGate === null`, `resolved` unreachable on every path, termination in `escalated`/`no-action` with the seam's own O-1 triple, and *installing* `async () => ({ passed: true })` failing the case. All three citations verify at HEAD: FSPEC §5.4's A3 row is `FSPEC:378` — "**none.** A3's product is a classification only: its `permittedActions` is `[]` … *(Decided at the Phase PR erratum round — see DEC-ADV-11…)*" — inside the `361-380` range §8.2 already cites; `TSPEC:657` is "A3 \| **`null`** — same shape as A1: `permittedActions: []`, step 6 unreachable, `resolved` never reached"; `DECISIONS:698` is the DEC-ADV-11 heading. Block assignment is unchanged: A3+A4 ⇒ **A-23**, A5 ⇒ A-24, A1+A2 ⇒ A-31, exactly as at v1.8. |

The cell also went one sentence further than the finding required, and correctly: it names the
failure mode rather than only the rule — "A3's case therefore takes the **install-the-stub** form,
not the replace-the-gate form — authoring it the other way would stub a gate A3 never reaches and
would fail against a correct build in this very RED batch, undiagnosed until A-23, per
`PROPERTIES:570-572`". That citation verifies: `PROPERTIES:570` begins "Asserting conjunct 1 at A3
would require stubbing a gate A3 never reaches…". §3 is what the A-07 authoring task is dispatched
with, so stating the consequence at the point of authorship — not only in §8.2's reference cell — is
the difference between a rule the author must reconcile and one they cannot misread.

## Findings

**None.** No High, no Medium, no Low.

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| — | — | — | No findings this round. | — |

The delta was scanned for new issues in the three places it touched:

- **§3's A-07 row (`PLAN:258`).** The revised clause is consistent with §8.2 in both directions now.
  §8.2's registry (`PLAN:869`) assigns A3 to `A-23 — A3/A4 gate exclusivity` and states "A3's case
  takes the same install-the-stub form as A1's"; §3 now says the same thing in the same words. The
  A-31 clause in §8.2 still describes A1 alone, which is correct rather than stale — A1 is the only
  gateless seam in that block. No acceptance criterion is restated or narrowed: AC-4.5's five-row
  gate table is still quantified over in full (the case is generated by iterating the in-file
  registry, `gate` column and all), and the set-equality driver against `ADVISORY_SEAMS` is
  untouched, so a deleted case still fails the suite and a sixth seam with no row still fails it.
- **§10's v1.9 changelog row.** Every factual claim in it verifies: `FSPEC:378`, `DECISIONS:698`,
  `TSPEC:657`, `PROPERTIES:570-572`, `PLAN:869`, and "te `-v8` verdict was **Approved**, no
  findings" (`CROSS-REVIEW-test-engineer-PLAN-v8.md:77` reads `VERDICT: Approved`). The row states
  what did *not* change rather than leaving it to be diffed, and the claim holds — see below.
- **The header stamp.** 1.8 → 1.9, matching the changelog row added.

Structural invariants re-checked at HEAD, not assumed: the task↔manifest bijection is still 36/36
(72 `| A-NN` rows in the document, unchanged from v8's count), and no dependency edge, batch label,
phase boundary, requirement or acceptance criterion moved — the whole PLAN-side diff is five lines.
Files named by the changed row are correct: `pdlc/workflows/__tests__/advisoryDriver.test.js` is
declared new by A-07's 🔴 RED authoring role, and `ls pdlc/workflows/__tests__/` confirms no
`advisory*` file exists yet, so nothing in the row claims an existing artifact that is absent.

## Questions

| ID | Question |
|----|---------|
| Q-01 | Carried forward from v8, unchanged and still informational — not a blocker and not something this round should reopen. §8.2's registry names "A3's `SeamOps`" as A-23's un-skipper while §3's A-23 row (`PLAN:274`) lands both A3's `SeamOps` and A4's `verifyGate` in that one task. Because one task lands both symbols the block assignment is unambiguous either way; the note is only that a *future* split of A-23 would move the un-skip point under §3's stated rule ("the last symbol the block's cases exercise") without any text needing to change. No edit requested. |
| Q-02 | Also carried from v8, and also not a PLAN matter — recorded here only so harvest does not read it as a live disagreement. `PROPERTIES:559-561` still frames the A1 gate row as "the one place the upstream documents disagree", and `PROPERTIES:565-568` still cites `TSPEC:638`/`TSPEC:640` where §5.5 now begins at `TSPEC:648` with A3 at `:657`. DEC-ADV-11 closed the disagreement; PROPERTIES is in its own review loop and is the right place for both repairs. |

## Positive Observations

- **The finding was applied where it was raised, not near it.** v8's F-01 existed precisely because
  the previous round fixed §8.2 and left §3 — the "same defect one cell over" pattern. This round
  changed exactly the cell named, kept the block assignment the finding said must not change, and
  did not opportunistically re-touch §8.2 while it was in the neighbourhood. That is the behaviour
  that makes a delta review cheap to run.
- **The fix carries the *consequence*, not just the corrected rule.** Adding "authoring it the other
  way … would fail against a correct build in this very RED batch, undiagnosed until A-23, per
  `PROPERTIES:570-572`" turns a contract statement into a decision the A-07 author can check their
  own work against. The wasted-RED-batch failure mode v8 warned about is now written into the row
  that would have caused it.
- **Both gate representations are named symmetrically.** "its `verifyGate` for A2/A4/A5, its
  `verifyGate: null` for A1 **and A3**" enumerates all five seams rather than describing a rule plus
  an exception list that a sixth seam could quietly fall outside of. The gate contract is stated the
  same way in §3 and §8.2, over the same enumeration, with the same mutant.
- **The changelog row is auditable rather than self-congratulatory.** It names the prior finding, the
  document version it was raised against, every upstream anchor it relies on, and the invariants it
  did *not* disturb (36 tasks, 36 ownership rows, 20 batches) — each of which was independently
  confirmed above rather than taken on the row's word.
- **Three rounds of scope discipline in a row.** v1.7, v1.8 and now v1.9 are each a single prose cell
  plus a changelog row. Nothing in the executable contract — tasks, edges, ownership, batches, ACs —
  has moved since v1.6.

## Recommendation

**Approved.**

The single Medium that blocked v8 is closed, in the cell it named, in the form it specified, with
every citation verifying against the file and line it names. The delta breaks nothing: no task,
dependency edge, ownership row, batch label, phase boundary, requirement or acceptance criterion
changed; the task↔manifest bijection still holds at 36/36; the file the changed row names is
correctly declared new. No new finding of any severity was found in the changed sections.

From the product lens, the question this reviewer has tracked since v6 — *does the PLAN state A3's
gate contract the same way the approved product contract states it* — is now answered consistently
in all four places it appears: FSPEC §5.4 (`FSPEC:378`, "none"), DEC-ADV-11 (`DECISIONS:698`),
TSPEC §5.5 (`TSPEC:657`, `null`), and both PLAN sites (`PLAN:258` and `PLAN:869`). AC-4.5's five-row
gate table is quantified over in full, by set-equality against `ADVISORY_SEAMS`, with the
trivially-passing stub carrying one meaning — the mutant — across the whole document set.

No erratum is emitted this round.

## Verdict

VERDICT: Approved
{"high": 0, "medium": 0, "low": 0}
