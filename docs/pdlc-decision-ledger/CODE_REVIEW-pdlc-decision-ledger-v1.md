# CODE REVIEW — pdlc-decision-ledger (v1)

| Field | Detail |
|---|---|
| Feature | pdlc-decision-ledger |
| Branch | feat-pdlc-decision-ledger |
| Review version | 1 |
| Date | 2026-09-01 |
| Verdict | Findings |
| Branch coverage (lowest new module) | 89.00% (`pdlc/workflows/orchestrate-dev.js`, per-file over the feature's introduced ranges; whole-file table reports 88.99%) |
| Requirements traced | 37/38 |

Scope: Local (all findings), except where a row says otherwise.

Evidence base: `npm run test:coverage` run fresh on this branch — 166/166 suites, 5258 passed / 70 skipped / 5328 total, exit 0; `node pdlc/workflows/build-runtime.mjs --check` in sync; `pdlc/workflows/dist/pdlc-cli.mjs` current; delta-coverage gate (`scripts/check-wave-resume-delta-coverage.mjs`) run against merge-base `origin/main` (`8f298525f99c`).

## §1 Code Quality Findings

| # | Criterion | Severity | File:Line | Problem | Required fix | Scope |
|---|---|---|---|---|---|---|
| F-1 | Unwired integration | medium | `pdlc/workflows/orchestrate-dev.js:15694–15701` (dead consumer at `:2894`) | `buildDecisionLedgerInjector` is constructed in `main()` **without** `_log`, so the per-dispatch observability line at `:2894` (`if (typeof _log === "function") _log({ feature, phaseId, docType, corpusOutcome })`) is live only under test doubles and never on a real run. TSPEC:1247 lists `_log` explicitly so that "the per-dispatch observability line is live in production, not only under doubles", citing the shipped CODE_REVIEW F2 lesson from the learnings injector — whose remediated wiring sits ten lines above at `:15645–15649` with that lesson in a comment. The identical defect has therefore been reintroduced in the clone. | Pass a `_log` at the construction site that emits through the run's own `emit`, mirroring `:15645–15649` (`decision-ledger: phase … doc … (feature …) — corpus …`). | Local |
| F-2 | Unwired integration (dead record fields) | medium | `pdlc/workflows/orchestrate-dev.js:9991–9992` vs `:2843`/`:2874–2885` | The only production call site is `await _injectDecisionLedger({ feature })`; the closure destructures `{ feature, phaseId, docType, round }` and writes all four onto the dispatch record. On every production run `record.phaseId`, `record.docType` and `record.round` are therefore `undefined`, contradicting TSPEC §5.1's `DecisionLedgerDispatchRecord` (`phaseId: string \| null`, `docType: string \| null`, `round: number`). No test pins the production-path shape — the injector suites supply these fields directly. Note the divergence is also internal to TSPEC: §4.5 declares the seam as `(args: { feature: string })`, which the call site does satisfy. | Either thread `phaseId`/`docType`/`round` from `reviewLoop`'s in-scope values into the call (preferred — the record's `null` legs then mean "absent", not "never wired") or route an erratum narrowing TSPEC §5.1 to `feature`-only and drop the three fields. Do not leave the declared shape and the shipped shape disagreeing. | Local |
| F-3 | Dead config / unpopulated declared field | low | `pdlc/workflows/orchestrate-dev.js:15693` vs TSPEC §5.1 (`:1044–1047`) | `DecisionLedgerSink.ruleInputs?: { thresholds: { maxEntries, maxBytes } }` is declared but never assigned anywhere in production or in tests. The learnings analogue populates its `ruleInputs` once per run at `:15657–15663` precisely so an enabled run with zero dispatches still discloses the thresholds actually in force. As shipped, an enabled decision-ledger run that never dispatches discloses nothing. | Populate `decisionLedgerSink.ruleInputs = { thresholds: { maxEntries, maxBytes } }` after the enabled check, mirroring `:15657–15663`, or delete the field from TSPEC §5.1 via erratum. | Local |
| F-4 | Coverage gap (delta gate red) | medium | `pdlc/workflows/orchestrate-dev.js:15589–15596` and `:2890` | `check-wave-resume-delta-coverage.mjs` reports **FAIL — 9 uncovered lines inside this feature's introduced ranges**: (a) `:15589–15596`, the run-level `NTC-DECLEDGER-KEYTYPE` notice push in `main()` — its sibling `NTC-DECLEDGER-MALFORMED` at `:15581–15588` *is* covered by `decisionLedgerMain.test.js:528`, so only the wrong-typed-key leg is unexercised end-to-end (see §2 gap row, PROP-CFG-08's F-5 leg); (b) `:2890`, the `if (!Array.isArray(sink.dispatches)) sink.dispatches = []` initialisation inside the object-sink branch, unreachable in production because `main()` pre-initialises the sink at `:15693` and every suite passes a pre-initialised object or a bare array. Whole-file branch coverage is 89.00%, i.e. above the 85% bar, so the failure is the delta gate, not the threshold. | Add a `main()`-driven case in `decisionLedgerMain.test.js` alongside the `:528` malformed case that feeds a wrong-typed `decisionLedger` key and asserts the emitted notice set is set-equal to `{NTC-DECLEDGER-KEYTYPE}` with the key named in the detail (PROP-CFG-08's F-5 leg). For `:2890`, either add an injector case passing an object sink without `dispatches`, or delete the defensive initialisation now that the union's object arm is always pre-initialised. | Local |

## §2 Requirements Traceability

| # | Source | Criterion / AC | Implementation path | Test path | Gap? | Severity | Scope |
|---|---|---|---|---|---|---|---|
| 1 | REQ-DECLEDGER-01 | Index rendered when enabled, one line per in-scope decision, sourced at dispatch-construction time | `orchestrate-dev.js:2546–2596` (corpus argv, `DECISION_HEADING_RE`, `recogniseDecisionRecords`), `:2621–2653` (render), `:2696–2772` (selection), `:2843–2897` (injector), `:9981–9992` (per-round call) | `decisionLedgerCorpus.test.js`, `decisionLedgerRecognise.test.js`, `decisionLedgerRender.test.js`, `decisionLedgerInjector.test.js` | No | — | — |
| 2 | REQ-DECLEDGER-02 | Disabled path byte-identical to the pre-feature baseline | `orchestrate-dev.js:15692–15701` (null when not enabled), `:15708`, `:18991–18994`, `:19033` (conditional spread) | `decisionLedgerBaselineGuard.test.js`, `decisionLedgerMain.test.js`, `decisionLedgerLoop.test.js` | No | — | — |
| 3 | REQ-DECLEDGER-03 | Rule text requires High severity **and** new evidence to re-open | `orchestrate-dev.js:2598–2620` (`DECISION_LEDGER_PREAMBLE`, `DECISION_LEDGER_RULE_TEXT`) | `decisionLedgerRender.test.js` | No | — | — |
| 4 | REQ-DECLEDGER-04 | Index construction fails open, two legs, never silently stale | `orchestrate-dev.js:2773–2842` (`gatherDecisionCorpus`, per-path try/catch), `:2773` `DECISION_LEDGER_CORPUS_OUTCOMES` | `decisionLedgerInjector.test.js` | No | — | — |
| 5 | REQ-DECLEDGER-05 | Three config keys fail open independently | `orchestrate-dev.js:2468–2545` (`DECISION_LEDGER_DEFAULTS`, `DECISION_LEDGER_NOTICES`, `parseDecisionLedgerConfig`), `:15575–15596` (run-level notices) | `decisionLedgerConfig.test.js`, `decisionLedgerMain.test.js:528` | No (unit + malformed leg); run-level wrong-typed leg gapped — see row 39 | — | — |
| 6 | REQ-DECLEDGER-06 | Decision id is the reviewer's reopening dedupe key; driver identity unchanged | `orchestrate-dev.js:2598–2653` (rule text + row id rendering) | `decisionLedgerRender.test.js`, `decisionLedgerLoop.test.js` | No | — | — |
| 7 | REQ-DECLEDGER-07 | Rendered index stays within both declared bounds | `orchestrate-dev.js:2654–2772` (omit reasons, oversized-line pre-pass, tail-drop) | `decisionLedgerBounds.test.js` | No | — | — |
| 8 | REQ-DECLEDGER-08 | Driver-side scoring identical whether the flag is on or off | `orchestrate-dev.js:9981–9992` (prompt-only injection; no scoring seam touched) | `decisionLedgerLoop.test.js` | No | — | — |
| 9 | FSPEC AT-01 | Frozen corpus renders the expected index | `orchestrate-dev.js:2621–2772` | `decisionLedgerCorpus.test.js`, `decisionLedgerRender.test.js` | No | — | — |
| 10 | FSPEC AT-02 | Statement/citation round-trip from source heading to rendered line | `orchestrate-dev.js:2562–2596` | `decisionLedgerCorpus.test.js` | No | — | — |
| 11 | FSPEC AT-03 | Re-gather on every dispatch (freshness) | `orchestrate-dev.js:2843–2897` | `decisionLedgerInjector.test.js` | No | — | — |
| 12 | FSPEC AT-04 | Flag-off run byte-identical to committed baseline | `orchestrate-dev.js:15692–15708` | `decisionLedgerBaselineGuard.test.js`, `decisionLedgerMain.test.js` | No | — | — |
| 13 | FSPEC AT-05 | Flag-off reviewer prompt unchanged in the loop | `orchestrate-dev.js:9991–9992` | `decisionLedgerLoop.test.js` | No | — | — |
| 14 | FSPEC AT-06 | Rule text present with the index | `orchestrate-dev.js:2598–2620` | `decisionLedgerRender.test.js` | No | — | — |
| 15 | FSPEC AT-07 | Cited record, not index line, carries the evidence bar | `orchestrate-dev.js:2598–2620` | `decisionLedgerRender.test.js` | No | — | — |
| 16 | FSPEC AT-08 | Total fail-open leg (`RSN-UNLISTABLE`) | `orchestrate-dev.js:2773–2842` | `decisionLedgerInjector.test.js` | No | — | — |
| 17 | FSPEC AT-09 | Partial fail-open leg (one unreadable source) | `orchestrate-dev.js:2792–2842` | `decisionLedgerInjector.test.js` | No | — | — |
| 18 | FSPEC AT-10 | Whole-source degradation recorded, not swallowed | `orchestrate-dev.js:2792–2842`, `:2883–2884` | `decisionLedgerInjector.test.js` | No | — | — |
| 19 | FSPEC AT-11 | Config matrix: three keys × {valid, wrong-typed, absent} | `orchestrate-dev.js:2491–2545` | `decisionLedgerConfig.test.js` | No | — | — |
| 20 | FSPEC AT-12 | Decision-id dedupe key across rounds | `orchestrate-dev.js:2621–2653` | `decisionLedgerLoop.test.js`, `decisionLedgerRender.test.js` | No | — | — |
| 21 | FSPEC AT-13 | `maxEntries` bound honoured | `orchestrate-dev.js:2654–2772` | `decisionLedgerBounds.test.js` | No | — | — |
| 22 | FSPEC AT-14 | `maxBytes` bound honoured, no partial line | `orchestrate-dev.js:2696–2772` | `decisionLedgerBounds.test.js`, `decisionLedgerLoop.test.js` | No | — | — |
| 23 | FSPEC AT-15 | Empty in-scope set renders no block at all (E-6) | `orchestrate-dev.js:2654–2772` | `decisionLedgerBounds.test.js` | No | — | — |
| 24 | FSPEC AT-16 | Recorded reviewer round scores identically flag-on/flag-off | `orchestrate-dev.js:9981–9992` | `decisionLedgerLoop.test.js` | No | — | — |
| 25 | FSPEC AT-17 | Driver-side identity under injection | `orchestrate-dev.js:9981–9992` | `decisionLedgerLoop.test.js` | No | — | — |
| 26 | FSPEC AT-18 | Same id in project- and feature-level files; project-level wins | `orchestrate-dev.js:2578–2596`, `:2696–2772` | `decisionLedgerCorpus.test.js` | No | — | — |
| 27 | PROPERTIES PROP-CFG (10) | Config parser: independence, malformation, `0`-valid, `invalidKeys` set-equality, totality, block isolation, single read | `orchestrate-dev.js:2468–2545`, `:15575–15596` | `decisionLedgerConfig.test.js`, `decisionLedgerMain.test.js` | No (except PROP-CFG-08's F-5 leg — row 39) | — | — |
| 28 | PROPERTIES PROP-REC (12) | Recognition: carrier markup, id grammar, last-record-wins, cross-file precedence | `orchestrate-dev.js:2562–2596` | `decisionLedgerRecognise.test.js` | No | — | — |
| 29 | PROPERTIES PROP-REND (9) | Render shape and row grammar | `orchestrate-dev.js:2621–2653` | `decisionLedgerRender.test.js` | No | — | — |
| 30 | PROPERTIES PROP-TEXT (6) | Preamble / rule-text literals | `orchestrate-dev.js:2598–2620` | `decisionLedgerRender.test.js` | No | — | — |
| 31 | PROPERTIES PROP-BND (12) | Bounds invariants, tail-drop, omit reasons | `orchestrate-dev.js:2654–2772` | `decisionLedgerBounds.test.js` | No | — | — |
| 32 | PROPERTIES PROP-FAIL (12) | Fail-open legs, corpus outcomes, degraded sources | `orchestrate-dev.js:2773–2842` | `decisionLedgerInjector.test.js` | No | — | — |
| 33 | PROPERTIES PROP-INV (12) | Injector invariants: re-gather per call, purity, sink append | `orchestrate-dev.js:2843–2897` | `decisionLedgerInjector.test.js` | No | — | — |
| 34 | PROPERTIES PROP-WIRE (12) | Wiring: seam shared, block appended last, report field conditional | `orchestrate-dev.js:9981–9992`, `:15688–15709`, `:18991–18994` | `decisionLedgerLoop.test.js`, `decisionLedgerMain.test.js` | No (PROP-WIRE-08's *locus* is stale in TSPEC §4.5 — see Notes) | — | — |
| 35 | PROPERTIES PROP-OFF (6) | Flag-off obligations, four not-enabled spellings, empty notice set | `orchestrate-dev.js:15692–15708` | `decisionLedgerBaselineGuard.test.js`, `decisionLedgerMain.test.js` | No | — | — |
| 36 | PROPERTIES PROP-DIS-06 | Destructured-expression source-text discipline (ADVISORY sentinel) | `orchestrate-dev.js:15688–15709` (sentinel-bounded block) | `advisoryDisabled.test.js` | No | — | — |
| 37 | PROPERTIES PROP-DISC (10) | Config-example / docs disclosure family | `.claude/pdlc.config.example.json`, `pdlc/OPERATIONS.md:24–33`, `pdlc/README.md:137`, `CLAUDE.md:108` | `decisionLedgerConfig.test.js`, `pdlc/engine/__tests__/decision-ledger-config-example.test.js` | No | — | — |
| 38 | PROPERTIES PROP-PRE (6) | Preflight / census guards over the new source region | `orchestrate-dev.js:2462–2899` | `decisionLedgerPreflight.test.js`, `decisionLedgerCensus.test.js`, `decisionLedgerFixtureGuard.test.js` | No | — | — |
| 39 | PROPERTIES PROP-CFG-08 (F-5 leg) | Run-level notice set **set-equal** to `{NTC-DECLEDGER-KEYTYPE}` for a wrong-typed key, with the key named in the detail | `orchestrate-dev.js:15589–15596` | Not found (only the F-4 `NTC-DECLEDGER-MALFORMED` leg is covered, `decisionLedgerMain.test.js:528`) | YES | medium | Local |

## Notes

- Criteria 1 and 3 are clean: no `TODO`/`FIXME`/`throw new Error("not implemented")`/placeholder returns in the new production region (`orchestrate-dev.js:2462–2899` plus the wiring loci), and no fixture or mock data on a production path — the decision-corpus fixtures live only under `pdlc/workflows/__tests__/` and are already absorbed into A-1's sweep allow-list.
- Criterion 6 is clean. Adjacent surfaces are all updated in-branch (`.claude/pdlc.config.example.json`, `pdlc/OPERATIONS.md`, `pdlc/README.md`, `CLAUDE.md`) and oracle-covered (`pdlc/engine/__tests__/decision-ledger-config-example.test.js`). The two sibling economies (`cascade.pinCheck`, `review.derivativeStop`) keep their per-key fail-open semantics unchanged — PROP-CFG-07 pins that a malformed `decisionLedger` block leaves them byte-identical. The REQ's deferrals (NG-1, NG-3) have queued successors at `docs/_queue/QUEUE.md:136–138` (rows 30/31/32), so no unbound deferral.
- **F-1 and F-3 are the same lesson twice.** Both are places where the learnings-injection clone dropped a remediation the original already carries (`:15645–15649` for `_log`, `:15657–15663` for `ruleInputs`). Remediate them together at the `main()` construction site; they are a handful of lines and share a test.
- **F-4 and §2 row 39 share a root cause** — one missing `main()`-driven wrong-typed-key case. Fixing PROP-CFG-08's F-5 leg closes eight of the nine delta-coverage lines; `:2890` is the ninth and is independent.
- **Upstream staleness, not a code defect (no fix owed here).** TSPEC §4.5 still declares `reviewerPrompt`'s ninth `ledgerBlock` parameter and PROPERTIES PROP-WIRE-08 (`PROPERTIES:344`) still names `orchestrate-dev.js:11483`/`:11506` as the append locus, but the parameter was deleted in the Phase CR round (TE F-02) and the block is now threaded through `dispatchAndVerify` (`orchestrate-dev.js:11909–11917`, `:11610`). The property's *intent* — block appended last in the delivered prompt — is implemented and tested at the new locus, so this is not a delivery gap; it is an already-routed document erratum (`CROSS-REVIEW-test-engineer-REVIEW-v2.md` F-02 / Q-01) still open at HEAD. Flagging so a later reader does not "restore" the deleted parameter to satisfy the stale spec text.
- Remediation ordering suggestion: F-1 + F-3 (one edit at `:15693–15701`), then F-2 (decide thread-vs-erratum first), then F-4's test, then `:2890`.
- Gate state at review time: full suite green (166/166, exit 0), `build-runtime.mjs --check` in sync, whole-file branch coverage above the 85% bar. The only red mechanical gate is the delta-coverage script.
