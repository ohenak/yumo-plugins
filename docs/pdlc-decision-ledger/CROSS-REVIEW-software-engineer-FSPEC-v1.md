# Cross-Review: software-engineer — FSPEC

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-decision-ledger/FSPEC-pdlc-decision-ledger.md` (v1.0, draft)
**Date:** 2026-08-28
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **The two fail-open legs do not partition the failure space, so §5's "rendering is total" claim is false.** §3.3's leg conditions are *Total* = "**every** source is unavailable" and *Partial* = "**one decision of several** fails to render". A whole source that is missing/unreadable/unparseable while other sources survive — e.g. `docs/_decisions/` readable but the feature's `DECISIONS-*.md` unreadable — matches neither: it is not *every* source, and it is not *one decision* (it is every decision of that source, count unknown, since an unparseable file yields no decision list at all). An engineer must invent the outcome, which is exactly what §1 says an FSPEC exists to prevent. Fix: restate the Partial condition over the *rendered set* rather than over a single decision — "any proper subset of the in-scope set fails to render, for any reason including a whole source being unavailable" — leaving Total as the degenerate case where the surviving subset is empty. | §3.3 table; §5 preamble; E-2/E-3 |
| F-02 | High | Local | **BR-8 / E-4 / AT-10 has no observable consequence anywhere, so it cannot be tested, only asserted.** §1's table fixes the driver's sole observable as "the dispatch prompt's bytes". Under that observable an ordinary-empty source and an unreadable source are indistinguishable in **both** directions: (a) *only source* — empty ⇒ E-6 (no index block; rule text does not stand alone), unreadable ⇒ E-2 total leg (no index, no rule text): identical bytes; (b) *one of several* — the empty file contributes no line and the failed source's lines are omitted (BR-7): identical bytes again. AT-10's *Then* is therefore purely negative ("contributes nothing and neither fail-open leg is taken") with no positive assertion on the same path, and its stated discriminator — "E-6 follows rather than E-2's total leg" — names two cases with the same output. Per `DEC-LAYER-01` the FSPEC must state the observable that pins this. Fix: either give the two paths a distinct observable at the FSPEC's own altitude (the surviving-lines assertion in a mixed corpus where the empty file is *not* the only source *and* one real source is unreadable, so the ordinary-empty reading renders lines the total-leg reading would not), or state plainly that BR-8 is a construction rule with no dispatch-visible consequence and route its coverage to a driver-internal observable the TSPEC must expose. | BR-8, E-4, AT-10, §1 observable table |
| F-03 | High | Local | **AT-01's expected value is indeterminate, and the reading its words carry blows the default bound.** AT-01 asserts the rendered line set *equals* "the Baseline's enumeration transcribed by id: `M-1d` project-level, `M-2e` per feature directory". `M-2e` (Baseline v1.1) enumerates every feature directory, not one; taken literally the expected set is 41 + 100 ≈ 141 lines, which (i) contradicts §3.2 step 2 and REQ G-1, where the set is the project's decisions **plus those of the feature whose document is under review**, and (ii) exceeds `maxEntries` `70` on the very fixture AT-01's *Given* says is "within bounds" — the cap was sized from `M-6b` (`41 + 22` = 63) precisely on the one-feature reading. As written the expected value cannot be transcribed without an unstated choice, so the sole oracle for REQ-DECLEDGER-01 is not a literal transcription from the spec. Fix: AT-01 must name the feature-under-review its fixture dispatch reviews and pin the expected set as `M-1d` ∪ the `M-2e` row for that feature. (The REQ carries the same ambiguity; raised upstream as an erratum, and the fix here does not depend on it.) | AT-01; §3.2 step 2; C-5 |
| F-04 | Medium | Cross-Feature | **The `learningsInjection` precedent is mis-cited on both the timing axis and the site axis — an engineer who implements "as `learningsInjection` does" violates BR-9.** Verified at HEAD: the injected block is computed **once per episode**, before the dispatch loop (`pdlc/workflows/orchestrate-dev.js:11074-11077`, outside the `for(;;)` at `:11098`, with the block re-used in every iteration's prompt at `:11143`), and only for **authoring** dispatches (`injectHere = dispatchKind === "authoring" && LEARNINGS_TARGET_DOCTYPES.includes(docType)`, `:11021-11022`). Review dispatches carry no injected block today, and the shipped behavior is a per-episode snapshot — the exact thing BR-9 forbids ("no snapshot taken earlier in the round window is rendered, and no index is reused across dispatches"). What *is* an accurate precedent, and should be cited instead for the injection site, is `reviewerPrompt`'s config-gated clause appends (`freezePart` / `findingGrammarPart`, `orchestrate-dev.js:11446-11453`), which already demonstrate a flag-gated, byte-identical-when-off addition to a **review** prompt. Keep `learningsInjection` as the precedent only where it is true: per-key independent config fallback (`:2272-2296`) and fail-open-to-silence. | §1 "Precedent"; BR-9; §3.3 closing sentence |
| F-05 | Medium | Local | **BR-1's biconditional is falsified by its own edge cases.** BR-1 says an index block appears "exactly when resolved `enabled` is `true` **and** at least one in-scope decision survives §3.3". E-6 and E-7 give two cases where a decision survives §3.3 and no index block appears anyway — all lines omitted by the bounds (E-6's "or because all were omitted") and `maxEntries` `0` (E-7). Fix: add the third conjunct — "and at least one line survives the bounds of §3.2 step 5" — so BR-1, E-6 and E-7 state one rule rather than a rule and two counterexamples. | BR-1; E-6, E-7 |
| F-06 | Medium | Local | **AT-14 asserts only an absence, and drops the half of E-6 that says what happens instead.** E-6 has two clauses: no index block, *and* "rule text does not stand alone without an index". AT-14's *Then* asserts only the first, plus two negations of shapes the block might have taken. So a build that emits the rule text with no index passes AT-14 while violating E-6. Fix: pair the negative with a positive on the same path — assert that the dispatch equals AT-04's committed baseline stream in both the empty-set and `maxEntries: 0` cases, which pins index absence, rule-text absence and whitespace in one comparison. | AT-14; E-6, E-7 |
| F-07 | Low | Local | **"Config is read for the dispatch" is a stronger claim than the precedent it invokes and may mint an unintended per-dispatch I/O obligation.** The shipped loader reads the config file once per run and threads the result (`readLearningsConfigSafely`, `pdlc/workflows/orchestrate-dev.js:2286-2290`: "Read once per run and the result threaded, never re-read"). §3.1's wording, read alongside BR-9's genuinely per-dispatch freshness rule, invites a TSPEC that re-reads `.claude/pdlc.config.json` on every dispatch for no behavioral gain. Fix: say config is *resolved* for the dispatch, and state that only the record corpus (BR-9) is re-read per dispatch. | §3.1; BR-9 |
| F-08 | Low | Local | **AT-03 and the §6 preamble read as contradictory about what may be mutated.** The preamble forbids asserting against the live repository "which grows, on this branch included"; AT-03's *Given* is "a decision record changes between two dispatches". Nothing says the change happens inside the frozen fixture copy, and the obvious naive implementation edits a real record. Fix: state that AT-03 mutates the frozen fixture corpus, not the repository. | §6 preamble; AT-03 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | §3.2 step 2 scopes the set to "the project's closed decisions plus those of the feature whose document is under review". Under the feature this pipeline is running now (`pdlc-decision-ledger`) that feature contributes **zero** records — there is no `DECISIONS-pdlc-decision-ledger.md`. Is a dispatch whose feature contributes nothing simply the project-level set (BR-1 satisfied, index renders), and is that the same resolution Q-2 gives to a document with no feature directory? Stating the two together would make the in-scope function total without adding a rule. |
| Q-02 | E-3's Partial leg says "one decision of several fails to render". What makes an *individual* decision fail once its file has parsed — a record whose heading has an id but no statement, an unrepresentable byte, an overlong statement? If the only realistic per-decision failure is "the whole file did not parse", then E-3 and F-01's missing middle case are the same case and §3.3 collapses to two legs stated over the surviving subset. |
| Q-03 | BR-14 and AT-16 enumerate five driver-side outcomes that must be identical across the two flag settings. Is that enumeration meant to be exhaustive of driver-side accounting (asserted by set equality against a named list, so a sixth mechanism added later fails), or a representative sample? AT-11 takes the set-equality discipline explicitly for C-3's keys; AT-16 does not say either way. |
| Q-04 | Q-3 asks whether the block is disclosed in `.claude/pdlc.config.example.json`. Confirmed at HEAD: both shipped gated blocks are disclosed there (`cascade.pinCheck`, `review.derivativeStop`), each with an engine-side disclosure test (`pdlc/engine/__tests__/loop-config-example.test.js`, `learnings-config-example.test.js`). Since NG-6 explicitly preserves that precedent, is there any live reason *not* to disclose — i.e. is Q-3 a genuine open question, or a decision the precedent has already made that TSPEC only has to record? |

## Positive Observations

- The **two-moving-parts split in §1** (driver-side index / reviewer-side rule) with an explicit observable per part is the single most useful thing in this document: it is what makes NG-4 falsifiable rather than aspirational, and §3.4's "a reviewer who files anyway is not intercepted" closes the loop honestly.
- **Every claim about the config precedent checks out at HEAD.** Per-key independent fallback is really shipped in the shape BR-10 describes (`pdlc/workflows/orchestrate-dev.js:2272-2296`: absent ⇒ default, wrong type ⇒ default + `invalidKeys`, other keys untouched), the config path is `.claude/pdlc.config.json` (`:2194`), and `DEC-LOOPECON-06`'s exact-match triple that AT-12 promises to leave alone exists as cited (`docs/completed/pdlc-loop-economics/DECISIONS-pdlc-loop-economics.md:163`), as does `DEC-ERRROUTE-01` (`docs/_decisions/DECISIONS-erratum-routing.md:12`).
- **Feasibility is not in doubt.** `reviewerPrompt` (`pdlc/workflows/orchestrate-dev.js:11433`) is a single, already-parameterised seam for review dispatch construction, and it already carries two flag-gated conditional clause appends that are the empty string when off — so BR-4's byte-identical disabled path is cheap and structurally proven, and the prompt is rebuilt inside the dispatch loop (`:11143`), so BR-9's per-dispatch freshness is available without restructuring anything.
- The **`maxBytes`/`maxEntries` interaction at the single-oversized-line case (E-8)** and the N-1/N-2 non-cases are the right things to have written down: they are exactly where an implementer would otherwise invent an abort path.
- **§2's traceability table earns its place** — every AC has a row, every row names both rules and tests, and G-4's explicit *no row, by design* is better than silence.

## Recommendation

**Needs revision**

Three High findings, all of them repairable inside this document without touching the REQ:

1. **F-01** — restate §3.3's Partial leg over the *surviving subset* rather than over "one decision", so the leg conditions partition the failure space and §5's totality claim holds.
2. **F-02** — give BR-8 / E-4 an observable, or say plainly that it has none at this altitude and route its coverage obligation accordingly. As written, AT-10 is an absence-only oracle whose stated discriminator names two cases with identical output.
3. **F-03** — pin AT-01's expected set to `M-1d` ∪ the `M-2e` row of the feature its fixture dispatch reviews, so the sole oracle for REQ-DECLEDGER-01 is a determinate transcription and does not exceed the `maxEntries` default it is asserted under.

The Medium findings (F-04 precedent mis-citation, F-05 BR-1's biconditional, F-06 AT-14's missing positive) should land in the same revision; the Lows are wording.

Nothing here questions the shape of the feature: the split of concerns is right, the routing to TSPEC is correctly drawn, and the disabled-path guarantee is cheap to honour at the seam that already exists.

## Verdict

VERDICT: Needs revision
{"high": 3, "medium": 3, "low": 2}
