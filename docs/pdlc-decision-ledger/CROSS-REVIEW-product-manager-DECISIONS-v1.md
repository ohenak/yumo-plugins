# Cross-Review: product-manager — DECISIONS

**Reviewer:** product-manager
**Document reviewed:** docs/pdlc-decision-ledger/DECISIONS-pdlc-decision-ledger.md
**Date:** 2026-08-28
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **DEC-DECLEDGER-15 rejects an alternative that no longer exists upstream, and its re-evaluation trigger describes an already-closed gap.** The decision reads *"Rejected: positive-integer validators, **as REQ C-5's type label reads**"* (line 207) and *"Rejected: edit the REQ to match. The type label is REQ-owned; it is routed as an erratum instead"* (line 212); the trigger row adds *"REQ C-5's type label is corrected upstream, closing the gap this decision currently spans"* (line 261). At HEAD `REQ-pdlc-decision-ledger.md:172-173` types **both** keys `non-negative integer` and states verbatim *"**Non-negative**: `0` is a valid admits-nothing value, not a malformed one falling back to `70`"*. That erratum landed in commit `4e197abe5` (*"REQ erratum §4 C-5 — non-negative types, measured maxBytes"*, 2026-08-28 **18:50:07**); this document's sections were committed at **19:21:52–19:22:49** (`8e28a016f`, `463ca16a1`, `dffc62b7f`) — i.e. after. The decision is still *correct* in its outcome, but its entire stated justification is a reading of a REQ clause that no longer says that, so a later agent checking the rationale against the REQ finds a contradiction and may reopen a settled question. Restate DEC-DECLEDGER-15 as *aligned with* REQ C-5 (citing the v1.8 wording and FSPEC E-7 together), delete the "routed as an erratum" clause, and replace the trigger row with the closed state. | REQ C-5, FSPEC E-7 |
| F-02 | High | Local | **The byte arithmetic carried by DEC-DECLEDGER-03, -12 and -13 is derived from the retired `maxBytes` default of 8,000, and one decision's rejection reasoning inverts under the shipped value.** The document asserts a *"6,800-byte allowance"* (line 192) and *"~495 bytes of headroom"* / *"about three more promoted decisions"* (lines 189-190, 181) — figures that are exactly `8000 − 1200 = 6,800` and `6,800 − 6,305 = 495` (TSPEC §3.6, `TSPEC-…:435-436`). REQ C-5 now ships `maxBytes` **12,500** (`REQ-…:173`), so the allowance is **11,300** and the headroom **4,995** — an order of magnitude out, which also makes the DEC-DECLEDGER-13 trigger's *"passes ~44 promoted records"* (line 256) wrong by roughly 27 records. The material consequence is in **DEC-DECLEDGER-03**, which rejects *"relying on the order being inert"* on the ground that *"`maxBytes` binds first in every case"* (lines 84-86): per **REQ G-1** a dispatch's in-scope set is the project's closed decisions **plus the one feature under review**, whose largest standing instance is 41 + 22 = 63 records rendering **10,859** bytes (TSPEC §3.6 table, `TSPEC-…:422`). `10,859 ≤ 11,300`, so under the shipped default the omission order **never fires** on the standing corpus — the inertness the decision says two reviewers falsified is now true again for a different reason. Re-derive every figure against 12,500, and re-state DEC-DECLEDGER-03's justification on a ground that survives the new default (the order must still be specified and falsifiable even when it is inert at the Baseline commit). | REQ C-5, REQ G-1, REQ-DECLEDGER-01 |
| F-03 | Medium | Local | **The header pins Baseline v1.1, but the Baseline is v1.2 and the ids this document's budget reasoning depends on were added in v1.2.** Line 12 pins *"`docs/_constraints/pdlc-decision-corpus-baseline.md` v1.1, cited by `M-*` id, never restated"*; the Baseline is `Version | 1.2 · 2026-08-28` (`pdlc-decision-corpus-baseline.md:7`), and REQ (`:15`) and FSPEC (`:11`) both pin **v1.2**. Because REQ C-5's shipped 12,500 is now *"derived from Baseline **v1.2**'s `M-7b`/`M-7c`"* (`REQ-…:26`, `FSPEC-…:21`), and this document's own header rule is that measurements are *"cited, never restated"* (lines 53-55), the pinned version cannot resolve the two ids F-02's corrected arithmetic must cite. Move the pin to v1.2. | REQ C-5 |
| F-04 | Low | Local | **The "Risks accepted" bullet misstates both the count and the status of the open upstream errata.** Lines 273-274 read *"**Two errata are open** against upstream documents and are the TSPEC's to carry (`ERR-1`…`ERR-4`, §9.2)"* — the sentence says two while enumerating four ids, and at HEAD `ERR-1` (C-5 typing) and `ERR-2` (the `maxBytes` default) are both **resolved** in REQ v1.8, leaving only the two FSPEC-owned items `ERR-3`/`ERR-4`. The trailing conditional *"If the `maxBytes` default moves…"* (line 275) is likewise written in a future tense the repository has already overtaken. State the count and the ids consistently, and record ERR-1/ERR-2 as closed. | REQ C-5 |
| F-05 | Low | Local | **DEC-DECLEDGER-08 cites REQ NG-6 more broadly than NG-6 is written.** The decision says adding a `lib/` module *"means editing `pdlc/engine/`, which REQ NG-6 forbids"* (lines 133-135). NG-6 forbids engine **runtime** changes and explicitly carves out engine-side tests: *"No engine **runtime** changes under `pdlc/engine/`… This does **not** forbid the shipped per-block config-disclosure test precedent"* (`REQ-…:134-140`). The file that would actually need the edit, `pdlc/engine/scripts/prepack.mjs:20` (`export const MODULE_NAMES = [` — verified: a frozen four-entry vendoring list), is a pack-time build script, not runtime. The refusal itself is sound and `DEC-LOOPECON-08` is a real precedent, but the citation should rest on the frozen vendoring list plus the NG-6 *spirit*, not on a prohibition NG-6's text does not carry — otherwise a later reader who reads NG-6 literally concludes the decision was over-constrained. | REQ NG-6 |

## Questions

_(pending)_

## Positive Observations

_(pending)_

## Recommendation

_(pending)_

