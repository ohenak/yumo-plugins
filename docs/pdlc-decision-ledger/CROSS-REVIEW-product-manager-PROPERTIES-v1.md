# Cross-Review: product-manager — PROPERTIES

**Reviewer:** product-manager
**Document reviewed:** `docs/pdlc-decision-ledger/PROPERTIES-pdlc-decision-ledger.md`
**Date:** 2026-08-29
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Requirement ref |
|----|----------|-------|---------|----------------|
| F-01 | High | Local | **The `FSPEC` acceptance test → property map misattributes the properties that state the criterion.** Three rows point at properties that do not state their AC, while the property that does state it is filed under a different AC. (a) `FSPEC:482` is **AT-15 — a single oversized line is omitted whole**; the property stating exactly that is **PROP-BND-10** ("A **single line alone** exceeding `maxBytes`, among other lines that fit, must be **absent in full** … no fragment of it present"), yet the map files PROP-BND-10 under **AT-14** and gives AT-15 `PROP-BND-05, -06, -11` (one-loop disjunction, determinism, no-abort) — none of which mention an oversized line. The BND section's own header contradicts the map: it calls the boundary block "`FSPEC` AT-13/AT-15 regression anchors". (b) `FSPEC:506` is **AT-17 — a filed reopening is scored as any other High finding**; **PROP-INV-04** states that verbatim ("A **High** finding re-opening an indexed decision that a reviewer files anyway must be scored, deduped and routed **as any other High finding**"), yet the map files PROP-INV-04 under **AT-16** and gives AT-17 `PROP-INV-05…10` (suppressed-finding absence, source census, round-constant pinning) — none of which score a filed reopening. (c) **AT-02** is given `ORC-02; PROP-REND-06`, but ORC-02's own **Decides** line reads "PROP-REND-03, PROP-FAIL-05, and `FSPEC` AT-02"; PROP-REND-06 is the project-before-feature ordering property and has no citation-resolution content. **Fix:** move PROP-BND-10 to the AT-15 row, move PROP-INV-04 to the AT-17 row, and replace PROP-REND-06 with PROP-REND-03 and PROP-FAIL-05 on the AT-02 row — or, where the pairing is deliberate, add the missing property to each row so no AC is left with only properties that do not state it. This map is the artifact an auditor uses to check acceptance-criteria fidelity, and an implementer building "AT-15's regression anchor" from its row builds the wrong test. | `REQ-DECLEDGER-07` (AT-15), `REQ-DECLEDGER-08` (AT-17), `REQ-DECLEDGER-01` (AT-02) |
| F-02 | Medium | Local | **The upstream-obligation table routes `BR-11` / `NG-4` to a property that does not discharge it.** The row reads "**BR-11 / NG-4** — no second source of decision text | `FSPEC`/`REQ` | PROP-DISC-07 — the census, cloning `loopEconomicsAnchorGuard.test.js`". PROP-DISC-07 is the `documentOracles.test.js` `*.test.js` namespace-exclusion and `102` count — a repo-hygiene oracle with no bearing on where decision text may originate. The census that *does* discharge BR-11/NG-4 is **PROP-INV-06** (zero occurrences of `DECISION_LEDGER_CENSUS_TOKENS` outside the four owned regions), supported by PROP-INV-07 (token set equality) and PROP-INV-08 (non-empty slices) — and the INV family's own `Traces` line already cites `FSPEC` BR-11 and `REQ` NG-4. `loopEconomicsAnchorGuard.test.js` is likewise named as PROP-INV-06's model, not PROP-DISC-07's. **Fix:** change the row's discharge to PROP-INV-06…08. Coverage exists; the pointer is wrong, and a reader auditing NG-4 against this table concludes the non-goal is protected by a file-count assertion. | `REQ` NG-4; `FSPEC` BR-11 |
| F-03 | Medium | Local | **Two `PLAN` tasks have no row in §Coverage Matrix's Family → PLAN task table, and one of them is scheduled against `PLAN`'s explicit decision.** `PLAN` lists 24 tasks; `T-00a` and `T-20` appear nowhere in this document (zero occurrences of either id). `T-20`'s substance *is* covered — PROP-DISC-08 asserts `build-runtime.mjs --check` exits 0, `pdlc/workflows/dist/` staged in the same commit, and the `0.23.6` → `0.23.7` bump — but the DISC row's Red-task column reads `T-00, T-03, T-11, T-12, T-12a`, so PROP-DISC-08 has no owning task. More consequentially, `T-00a` is `PLAN`'s **batch-1** census-exclusion task, titled "**a batch-1 obligation, not a batch-9 one (TE F-01)**" precisely because batch 1 adds three `decisionLedger*` modules and would redden `documentOracles.test.js`'s saturated `102` literal before any production code exists. PROP-DISC-07 states that same rationale in its own prose ("batch 1 alone would redden a required check before any production code exists") — yet the DISC row assigns its green task as **T-19**, batch 9. **Fix:** add `T-00a` to the DISC row's Red-task column with PROP-DISC-07 attributed to it, and add `T-20` as PROP-DISC-08's owner. A one-directional check ("every task id here is one of `PLAN`'s 24") passes today; the converse — every `PLAN` task is traced — does not. | `FSPEC` Q-3; `REQ` NG-6 |
| F-04 | Medium | Local | **§Coverage Matrix's stated reconciliation does not reconcile.** The text asserts "The count reconciles: 98 properties over 11 families" and then "**Pyramid promised in `## Overview`:** 47 pure-unit properties needing no fixture, 11 under a generator, 37 integration properties … **zero** end-to-end." 47 + 11 + 37 = **95**, not 98. Summing the table's own Count column: CFG 10 + REC 11 + REND 9 + TEXT 6 = 36 pure-unit, BND 11 generator, FAIL 11 + PRE 5 + INV 10 + WIRE 11 = 37 integration, OFF 6 recorded byte-identity, DISC 8 repo/document oracle — total 98, with the pure-unit-and-other bucket at **50**, not 47. The `OFF` and `DISC` families have no level in the pyramid at all, so their 14 properties are unaccounted for in the level breakdown a reader uses to check the suite is runnable per wave (`PLAN` T-18). **Fix:** restate the pyramid as 36 pure-unit + 11 generator + 37 integration + 6 recorded byte-identity + 8 repo/document oracle = 98, or give OFF and DISC an explicit bucket, and drop the "reconciles" claim until the arithmetic holds. | `REQ-DECLEDGER-01`…`-08` (coverage audit) |
| F-05 | Low | Local | **`TSPEC` §6.1's fourteenth failure row is cited by no family.** The Overview states "`TSPEC` §6.1 fourteen failure rows", and the family `Traces` lines cite F-1…F-5 (CFG), F-6…F-10 (FAIL) and F-11…F-13 (BND) — thirteen. **F-14** (`TSPEC-pdlc-decision-ledger.md:935`) is cited nowhere. Its substance is covered — PROP-FAIL-11 handles the no-directory and zero-record feature cases — but that property cites only `FSPEC` Q-2 and `TSPEC` §3.1. **Fix:** add `F-14` to the FAIL family's `Traces` line so the "audit coverage … mechanically" promise in §Overview holds against §6.1 as well as against `REQ` §5 and `FSPEC` §4/§5/§6. | `REQ-DECLEDGER-04` |
| F-06 | Low | Local | **ORC-01's `Decides` list and the AT-01 map row disagree on which properties the oracle discharges.** ORC-01 reads "**Decides:** PROP-REC-01…08, PROP-PRE-01…05, and `FSPEC` AT-01"; the AT-01 map row reads "ORC-01; PROP-REC-01…**11**, PROP-REND-01…09". The two ranges cannot both be right: PROP-REC-09 is a `fast-check` property over generated text and PROP-REC-10/11 are pure-unit null/CRLF cases, none of which the corpus oracle runs. **Fix:** narrow the AT-01 row to ORC-01's own range, or widen ORC-01's `Decides` line and say which of PROP-REC-09…11 the corpus oracle actually decides. | `REQ-DECLEDGER-01` |

## Questions

_(pending)_

## Positive Observations

_(pending)_

## Recommendation

_(pending)_
