# Cross-Review: software-engineer — REQ

**Reviewer:** software-engineer
**Document reviewed:** `docs/pdlc-decision-ledger/REQ-pdlc-decision-ledger.md`
**Date:** 2026-08-28
**Iteration:** 1

## Findings

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | The index line's mandated fields do not exist in the records this REQ says it sources. G-1 / REQ-DECLEDGER-01 require each line to carry "the phase/round it closed in, and its citation", while G-1 also forbids minting a new decision-record file type. Neither shipped record shape carries those two fields: project-level entries are `## DEC-…` heading + `**Decision.**` + `**Origin.**` (`docs/_decisions/DECISIONS-erratum-routing.md:12-22`, `docs/_decisions/DECISIONS-loop-termination.md:11-25`, whose `Origin` is a promotion date and a LEARNINGS list, not a phase/round), and feature-level entries are `**Context.** / **Decision.** / **Rationale.** / **Alternatives considered:**` with no round and no citation field (`docs/completed/pdlc-loop-economics/DECISIONS-pdlc-loop-economics.md:163-180`). As written the P0 criterion is unsatisfiable on today's corpus. State per field whether it is required (and therefore requires new record data, contradicting G-1) or best-effort/omitted-when-absent. | G-1, REQ-DECLEDGER-01 |
| F-02 | High | Local | REQ-DECLEDGER-03's discount is stated as an outcome of "the review-loop driver, accounting for a completed round's findings", but the driver has neither the data nor a decidable predicate for it, and the REQ never says whose observable behaviour changes. Convergence today is computed from the reviewer-emitted `VERDICT:` value and counts (`pdlc/workflows/orchestrate-dev.js:8390-8394`, `:9676-9695`); the only per-finding data the driver holds comes from `FINDING:` lines parsed at `pdlc/workflows/orchestrate-dev.js:6643-6689`, whose fields are severity, provenance, locality, section, free text — no decision id and no citation. "Cites evidence not present in the decision's original citation" is a semantic comparison no parse can make. Either the rule is reviewer-side (a reviewer who files it anyway self-reports a lower High count — observable in the cross-review artifact) or driver-side (the driver approves a document whose reviewer emitted `Needs revision`, an outcome that contradicts the verdict/anchor contract and needs saying out loud). Pick one and state the observable. | G-2, REQ-DECLEDGER-03 |
| F-03 | High | Cross-Feature | REQ-DECLEDGER-06 makes the decision id "the dedupe key for this purpose" across rounds, but the shipped, decision-recorded dedupe key for the same finding stream is the exact-match identity triple (severity, section anchor, normalised free text) — `DEC-LOOPECON-06` (`docs/completed/pdlc-loop-economics/DECISIONS-pdlc-loop-economics.md:163-173`), implemented at `pdlc/workflows/orchestrate-dev.js:6746-6780` and consumed by the open-finding ledger at `:9625-9668`. Two findings naming the same decision id in different prose are two identities under the shipped rule and one under REQ-DECLEDGER-06; the REQ neither acknowledges the conflict nor says which key governs which consumer (open-finding ledger vs. this REQ's reopening accounting). A standing shipped contract cannot be silently re-keyed. | REQ-DECLEDGER-06 |
| F-04 | Medium | Cross-Feature | The interaction with `review.derivativeStop` is unspecified. Its flat-round test asks whether the round `added` any new finding (`pdlc/workflows/orchestrate-dev.js:9643`, `:9683-9684`, `DEC-LOOPECON-10`). The REQ says a discounted finding "does not by itself block approval" but never says whether it still counts as a *new* finding for the derivative signal. If it does, a stream of discounted re-litigation findings keeps the loop non-flat forever — the exact burn this feature exists to kill; if it does not, that is a change to a shipped convergence path NG-4 currently claims is untouched. | G-2, NG-4, REQ-DECLEDGER-03 |
| F-05 | Medium | Local | "Closed decisions **relevant to the document under review**" (G-1, REQ-DECLEDGER-01, REQ-DECLEDGER-07) carries the whole gated set, yet the relevance rule is neither stated nor routed. O-1 routes only the over-budget *omission* rule, which is a different question: omission decides what is dropped from an already-selected set. Since a decision is gate-eligible under REQ-DECLEDGER-03 only if it was in that dispatch's index, relevance decides what a reviewer may still freely challenge — an outcome-bearing choice. Either state the observable (e.g. all project-level decisions plus this feature's own) or add it to Obligations explicitly. | G-1, REQ-DECLEDGER-01, O-1 |
| F-06 | Medium | Local | G-4's measurable outcome is not observable as specified. It is derived "by parsing `FINDING:` lines against the rendered index", but G-3 / REQ-DECLEDGER-04 require the index to be derived fresh at dispatch-construction time and no criterion requires it to be retained anywhere — the rendered index leaves no artifact on the branch, unlike the round counts and erratum events the proposal's §5 measures from committed files. Name where the index is observable after the run (an emitted line, a recorded dispatch record) or restate the signal in terms of something the branch already keeps. | G-4, G-3 |
| F-07 | Medium | Local | NG-6 ("No changes under `pdlc/engine/`") is asserted from the vendoring convention, but both shipped config blocks this REQ names as precedent landed an engine-side disclosure test per block: `pdlc/engine/__tests__/learnings-config-example.test.js` (asserts the `learningsInjection` section's key set and declared defaults in `.claude/pdlc.config.example.json`) and `pdlc/engine/__tests__/loop-config-example.test.js:62-84`. Those files also carry the stated reason they are per-block files rather than rows on `ci-arrangement.test.js`. NG-6 as written forbids the shipped disclosure precedent for a new operator-facing config block; either soften it to "no engine *runtime* changes" or accept the added test file. | NG-6, C-3 |
| F-08 | Low | Local | §1 attributes `DEC-ERR-01` to `docs/_decisions/DECISIONS-erratum-routing.md` ("its `DEC-ERR-01` absorb-don't-route rule (referenced there …)"). `DEC-ERR-01` is defined in `docs/_decisions/DECISIONS-review-severity-bars.md:88` and is not referenced anywhere in `DECISIONS-erratum-routing.md` (that file defines only `DEC-ERRROUTE-01..04`). The `pdlc/OPERATIONS.md:29` half of the claim is correct. Re-point the citation. | §1 |
| F-09 | Low | Local | §1 lists `structure-directional-options-scoring` beside `pdlc-engineering-loop` and `pdlc-wave-resume` as if all three were this repo's corpus. The proposal's own §2 places it in the `regime-ledger` corpus ("the regime-ledger corpus records 9 rounds on structure-directional-options-scoring"); no such feature exists under `docs/` or `docs/completed/` here. The numbers (159 `FINDING:` lines, anchor re-filed across four reviews) transcribe the proposal correctly — only the provenance reads wrong. Say which repo it comes from. | §1 |

## Questions

| ID | Question |
|----|---------|
| Q-01 | Does "every review dispatch" (G-1) mean every document review round (pm/se/te across REQ→PROPERTIES), or also the non-document review dispatches — DoD verification, advisory, erratum/delta-confirmation rounds? The three have different finding grammars, and REQ-DECLEDGER-02's byte-identity claim is only checkable once the set is named. |
| Q-02 | With `decisionLedger.enabled` defaulting to `false`, should `.claude/pdlc.config.example.json` disclose the block at its declared defaults (the `loop`/`merge`/`cascade` default-off precedent) or omit it? This decides whether F-07's engine-side disclosure test is in scope. |
| Q-03 | Is a decision "closed" the moment its record exists, or only once the owning document's round is approved? For a feature's own in-flight `DECISIONS-{feature}.md`, those differ, and REQ-DECLEDGER-01's freshness rule makes the distinction observable within a single phase. |

## Positive Observations

- C-5 declares every threshold with a default, a type, a named config owner and a rationale, and R-4 says plainly that the two budget numbers are analogy-derived rather than measured. That is exactly the disclosure that keeps TSPEC from inventing a number and an operator from trusting one.
- C-2 pins the disabled path against a *committed fixture baseline* rather than a same-branch before/after run, naming the shipped `learningsBaselineGuard.test.js` precedent (`pdlc/workflows/__tests__/learningsBaselineGuard.test.js:80-83` records why the same-branch comparison was insufficient). Reusing the precedent instead of reinventing the oracle is the right call.
- C-1 / REQ-DECLEDGER-05 reuse the per-key independent-fallback shape that is genuinely shipped (`parseLearningsConfig`, `pdlc/workflows/orchestrate-dev.js:2252-2290`), so the config work is a copy of a proven parser rather than a new one.
- NG-1..NG-7 draw a scope boundary that survives contact with the proposal: M5/Phase G is named as a possible future *source* rather than a dependency, which keeps this feature independently shippable.

## Recommendation

**Needs revision**

Three High findings. Concretely: (1) F-01 — say which index fields are required versus omitted-when-absent, because "phase/round it closed in" and "citation" are not in any shipped decision record; (2) F-02 — name the actor whose observable behaviour implements the never-re-litigate discount, since the driver holds neither the decision id nor a decidable evidence-comparison; (3) F-03 — reconcile REQ-DECLEDGER-06's decision-id dedupe key with the shipped exact-triple identity of `DEC-LOOPECON-06`, or say which consumer uses which. The Medium findings (F-04 derivative-stop interaction, F-05 relevance rule, F-06 measurability of G-4, F-07 NG-6's engine claim) are cheap to close in the same revision.

## Verdict

VERDICT: Needs revision
{"high": 3, "medium": 4, "low": 2}
