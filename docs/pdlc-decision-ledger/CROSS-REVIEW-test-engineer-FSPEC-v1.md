# Cross-Review: test-engineer — FSPEC

**Reviewer:** test-engineer
**Document reviewed:** `docs/pdlc-decision-ledger/FSPEC-pdlc-decision-ledger.md` (v1.0, Draft)
**Upstream:** `docs/pdlc-decision-ledger/REQ-pdlc-decision-ledger.md` v1.7 (approved; my `CROSS-REVIEW-test-engineer-REQ-v7.md` carried 3 non-gating Mediums)
**Date:** 2026-08-28
**Iteration:** 1

## Verification performed

Every repo path and every cross-document citation the FSPEC names was checked against HEAD on this
branch, not against the documents that name them.

| Claim in FSPEC | Checked | Result |
|---|---|---|
| Baseline v1.1 exists and carries the `M-*` ids cited (`M-1d`, `M-2e`, `M-3a`, `M-3c`, `M-4a`, `M-4b`, `M-4d`, `M-4e`, `M-5a`, `M-5c`, `M-6b`, `M-6c`) | `docs/_constraints/pdlc-decision-corpus-baseline.md` | ✓ all twelve present; `Version` 1.1 |
| E-9's arithmetic — `M-4d` carries 4 records alongside 8 `DEC-`-bearing non-records | Baseline `:85` | ✓ verbatim: 4 records (`DEC-A6-01…04`), 4 no-namespace ids + 4 back-references = 8 |
| E-4's two standing-corpus empty files are `M-4a` and `M-4b` | Baseline `:82`, `:83` | ✓ `DECISIONS-advisory-wave-gate-questions.md` (bullets, no ids) and `DECISIONS-pdlc-plugin-retirement.md` (ten `DEC-NN` headings, no namespace) |
| E-10 / BR-2's twice-opened block has `M-3c` as sole HEAD witness | Baseline `:75` | ✓ `DEC-LOOP-01` opened at `:237` and `:363`, second stating the outcome |
| E-11 has no HEAD instance (`M-5a`) | Baseline `:92`–`:93` | ✓ zero cross-file duplicate ids; `M-5c` states intent only |
| `maxEntries` floor arithmetic (`M-6b` 63, `M-6c` 70 clears it by 7) | Baseline `:101`, `:102` | ✓ `41 + 22 = 63` under the directory-glob reading |
| `DEC-LOOPECON-06`'s triple is (severity, section anchor, normalised text) — AT-12, AT-16, BR-11 | `docs/completed/pdlc-loop-economics/DECISIONS-pdlc-loop-economics.md:163` | ✓ exact match, incl. the unevaluable-round clause |
| `DEC-ERRROUTE-01` exists and is about erratum minting — BR-14, AT-16 | `docs/_decisions/DECISIONS-erratum-routing.md:12` | ✓ "A confirmation round's findings must mechanically mint erratum items" |
| `REQ-LOOPECON-01b` recompute-at-dispatch precedent — §3 preamble, BR-9 | `docs/completed/pdlc-loop-economics/REQ-pdlc-loop-economics.md` | ✓ shipped and tested |
| Q-3's two disclosure tests exist | `pdlc/engine/__tests__/learnings-config-example.test.js`, `loop-config-example.test.js` | ✓ both; a third (`advisory-config-example.test.js`) exists, strengthening the precedent Q-3 cites |
| Q-3's claim that both shipped gated blocks are disclosed today | `.claude/pdlc.config.example.json` | ✓ `cascade.pinCheck` and `review.derivativeStop` are both present; `decisionLedger` is absent, as expected pre-feature |
| C-2's committed-fixture-baseline precedent (BR-4, AT-04) | `pdlc/workflows/__tests__/learningsBaselineGuard.test.js` | ✓ shipped; it pins the capture with **hand-transcribed digest literals per `caseId`** (DC-14) and a **set-equality** check over the case-id set, with a three-step mutation proof recorded in its header |

The `learningsBaselineGuard.test.js` header is worth reading before TSPEC: it is the exact shape
AT-04/AT-05 need, and it already answers half of REQ O-4.

## Findings

Leading with the three Highs. All three are the same shape — a named behavioural branch whose
stated outcome no acceptance test asserts — which `docs/_constraints/DOMAIN-CONSTRAINTS.md` **DC-05**
(`:143`–`:156`) makes a High by standing constraint: *"For every named behavioural branch in the
FSPEC — each error flow, each explicitly conditional path (PASS/FAIL, skip/full, enabled/disabled) —
an acceptance test must exist in the FSPEC's test section. A branch with no AT is a High finding."*
DC-05's `Applies to` line names `te-review` of FSPEC specifically, and its origin note records that
this is the one pattern that has recurred verbatim every time the discipline lapsed.

This is a strong document — §5 is genuinely total where most FSPECs are illustrative, and §6 is
mostly well-oracled. The Highs are three gaps in an otherwise closed enumeration, and each closes in
one or two clauses.

| ID | Severity | Scope | Finding | Section ref |
|----|----------|-------|---------|------------|
| F-01 | High | Local | **E-9 is a named behavioural branch with no acceptance test — DC-05.** §5 states an outcome for the mixed-content file ("the non-records contribute no line; the file's real records render normally"), names the HEAD witness (`M-4d`), and even partitions ownership ("which headings qualify is the recognition rule's (O-1); that a mixed file still renders its real records is this spec's"). No AT in §6 cites E-9. It is *near*-covered by AT-01's whole-line set equality, but only if the frozen fixture's in-scope set happens to include `pdlc-advisory-wave-gate` — which F-04 shows AT-01 does not pin. So the branch this spec explicitly claims as its own has no test that fails when a build renders the 8 non-record headings as 8 extra lines. Cheapest fix: one conjunct on AT-01 ("…and the fixture's corpus includes `M-4d`'s mixed file, whose 8 non-record headings contribute no line"), or a dedicated AT-18. | §5 E-9; §6 |
| F-02 | High | Local | **E-11 is a named behavioural branch with no acceptance test — DC-05.** E-11 states a definite outcome this spec owns — **"One line renders"** — and correctly routes *which* line to O-1 and the fixture to O-5. But O-5 is an obligation on PROPERTIES to build a fixture; it is not an acceptance test, and §6 states no expected outcome for the two-file case at all. The result is that the one assertion the FSPEC can make here (cardinality: exactly one line for an id recorded twice across files, never two, never zero) is written in §5 and asserted nowhere. A build that renders both records passes every AT in §6. Fix: an AT that takes O-5's synthetic two-file corpus as its *Given* and asserts the cardinality conjunct only, leaving the winner to TSPEC — the same split E-11 itself already makes. | §5 E-11; §7 O-5; §6 |
| F-03 | High | Local | **AT-14 is an absence-only oracle, and it drops the one conjunct E-6 exists to state.** E-6 states two things: no index block, **and** "Rule text does not stand alone without an index." AT-14's *Then* asserts only the first — "in both cases **no index block at all** — not an empty block, not a header without rows". Nothing asserts what the dispatch *does* contain. A build that emits the rule text with no index above it satisfies AT-14, AT-06 (which requires an index in its Given), and every other AT — yet it ships the reviewer a never-re-litigate instruction referring to an index that is not there, which is precisely the "wrong is worse than absent" failure BR-7 is written to prevent. The document already knows the right pattern: AT-08's *Then* pairs "no index and no rule text" with the positive conjunct "the dispatch matches AT-04's byte-identical stream". AT-14 needs the same pairing. This also disposes of the open question of whether an empty in-scope set is byte-identical to the disabled path — say so, and the oracle becomes falsifiable in one clause. | §6 AT-14; §5 E-6, E-7 |
| F-04 | Medium | Local | **AT-01's expected set is still two-valued, and its own *Given* is unsatisfiable under one of the two readings.** This is my `CROSS-REVIEW-test-engineer-REQ-v7.md` F-31 carried forward unresolved. AT-01 says the expected value is "the Baseline's enumeration transcribed by id: `M-1d` project-level, `M-2e` per feature directory". `M-1d` is 41 ids; `M-2e` is **100** ids across twelve directories (Baseline `:67`). But §3.2 step 2 scopes the set to project-level decisions plus **the one feature whose document is under review** — at most `41 + 22 = 63`, exactly `M-6b`'s floor. Read literally, AT-01 expects 141 lines, which exceeds `maxEntries` 70 and so contradicts AT-01's own *Given* ("the set is within bounds") and hands the case to REQ-DECLEDGER-07 instead. I am filing this against the FSPEC rather than as an erratum because the REQ was approved with this as an open non-gating carry, and §6 is where it closes: name the fixture's feature directory in AT-01's Given ("`M-2e`'s entry for the fixture's feature directory"). Six words. | §6 AT-01; §3.2 step 2 |
| F-05 | Medium | Local | **AT-01 compares three fields and pins a transcribable expected value for one — the implementation-echo hazard.** Carried from REQ-v7 F-30, unresolved here. AT-01 correctly requires "equality of rendered lines, not containment and not equality over ids alone", and correctly explains why (`M-3c`). But `M-1d`/`M-2e` enumerate **ids**; the Baseline enumerates no statements and no citations, and only `M-3c` has a verbatim heading (`:75`). A te-author with a three-field equality and a one-field expected value will reach for the only cheap filler available — run the renderer over the fixture and freeze its output — which derives the expectation from the code under test and produces a test that can never fail for a wrong statement. Q-1 correctly keeps the line *format* out of this document, but the *provenance* of the expected statement and citation values is not format and belongs here: state that they are transcribed from the frozen fixture's own heading text and record location (data, not code), with `M-3c`'s verbatim as the pinned discriminating case. Two documents have now left this open; TSPEC/PROPERTIES is where it becomes irreversible. | §6 AT-01; §7 Q-1 |
| F-06 | Medium | Local | **AT-16 compares the system to itself and pins nothing to a literal.** Every conjunct is "identical across the two runs": convergence decision, dedupe ledger, flat/non-flat classification, minted erratum items, fail-closed read. A driver broken identically in both runs — the overwhelmingly likely failure mode, since the flag is meant not to reach the accounting leg at all — passes AT-16 green. The invariance claim is right and worth asserting; it just needs one anchor conjunct so the test can fail for a reason other than divergence: pin at least one of the five outcomes for the recorded fixture to a value transcribed from the fixture itself (e.g. "and for this fixture the open-finding ledger is exactly {…}"). Note the same fixture already carries the anchor AT-16 needs — it is a *recorded* set of reviewer outputs, so its expected accounting is transcribable, not derivable. | §6 AT-16 |
| F-07 | Medium | Local | **§2 asserts a set-equality property over §3–§6 that §5 falsifies.** §2's preamble states "nothing in §3–§6 exists without a row here". Five named items have no row in that table: **E-9, E-10, E-11, N-1, N-2**. E-10 and N-1/N-2 are substantively covered elsewhere (AT-01 names `M-3c`; AT-13/AT-15 carry N-1; AT-17 carries N-2), so this is traceability rather than coverage for three of the five — but E-9 and E-11 are untraced *and* untested (F-01, F-02), which is exactly what a traceability table exists to surface. A table that claims completeness and is not complete is worse than one that claims nothing, because a reader stops checking. Fix: add the five rows, or weaken the preamble's claim. | §2; §5 E-9, E-10, E-11, N-1, N-2 |
| F-08 | Medium | Local | **AT-07 names no executor, so it has no oracle.** *"Who: a reader applying the rule text to two constructed candidate findings"* — a reader is not a test runner, and "they classify oppositely" has no asserting party. As written AT-07 is either (a) a human review step, in which case say so and give it a home (a PROPERTIES manual-check row, or an obligation alongside O-5/O-6), or (b) a mechanical test, in which case what is asserted is a property of the **rule text**, not of a classification: that the text names both exemplars, each labelled with its side, and directs the reader to the cited record. (b) is assertable today and is very close to what AT-06 already does. AT-02's "the statement field says what was decided rather than what was asked" has a milder version of the same problem, but it is rescued by F-05's fix — a transcribed expected statement makes it mechanical. | §6 AT-07, AT-02 |
| F-09 | Medium | Cross-Feature | **The FSPEC mints ten new Baseline citations but is not on the Baseline's propagation path.** The Baseline's `Cited by` row (`docs/_constraints/pdlc-decision-corpus-baseline.md:6`) names only the REQ, and states its own contract: *"This list is the propagation path for a `Version` bump, so a new citation is added here in the same edit that mints it."* The FSPEC newly cites `M-3a`, `M-4a`, `M-4b`, `M-4d`, `M-6b`, `M-6c` (and re-cites `M-1d`, `M-2e`, `M-4e`, `M-5a`, `M-5c`) and pins **v1.1** in its header — so when the Baseline bumps to v1.2, nothing notifies this document, and AT-01's expected value silently loses its pin. This is the same version-skew class the repo has been bitten by before. The FSPEC author should not edit the Baseline unilaterally; route the one-line `Cited by` addition with the FSPEC's own section anchors. Tagged Cross-Feature because the propagation contract spans documents and the lesson outlives this feature. | Header (`Baseline` row); Baseline `:6` |
| F-10 | Low | Local | **"All four not-enabled spellings" and E-5's `{wrong type, malformed, absent}` cross product do not agree, so neither can be asserted as set equality.** AT-05 enumerates four (absent, `false`, wrong-typed, whole block missing); §3.1's table and E-5 use a three-way `{wrong type, malformed, absent}` per key; E-5 adds a fifth path for `enabled` ("with `enabled` the malformed key, the fallback is `false`"). For a boolean, "wrong-typed" and "malformed" are not distinguished anywhere in the document. AT-11 asks for set equality "over C-3's enumeration ... so a fourth key or a different spelling fails" — an admirable bar, but it needs the malformation vocabulary to be closed too. Define the two terms once (e.g. wrong-typed = parses as JSON, wrong type; malformed = does not parse) and make AT-05's four and AT-11's 3×3 agree. | §6 AT-05, AT-11; §5 E-1, E-5; §3.1 |
| F-11 | Low | Local | **The bounds invariant is stated only as two examples, with no property obligation recorded.** BR-12/BR-13 state a universally quantified invariant — for *any* in-scope set and *any* resolved bounds, the rendered index is ≤ `maxEntries` lines and ≤ `maxBytes` bytes, with no partial line — and AT-13 exercises it with exactly two examples ("an in-scope set exceeding `maxEntries`, and separately one exceeding `maxBytes`"). This is the canonical shape for a property: the input space is parameterisable (set size × line sizes × two bounds) and the invariant is stateable in one line. §7 already carries coverage obligations to PROPERTIES (O-5, O-6); one more, naming the bounds invariant as a property obligation rather than an example, costs a line here and forecloses an example-only suite later. Low, not Medium, because the property's home is PROPERTIES and nothing here blocks it. | §6 AT-13; §4 BR-12, BR-13; §7 |

**No erratum items this round.** F-04 and F-05 originate in REQ v1.7 (my REQ-v7 F-31 and F-30), but
both were dispositioned as open non-gating carries at REQ approval, and both close inside §6 of this
document without a REQ edit. Re-routing a knowingly-carried Medium would re-litigate a settled
disposition and spend a round to move text upstream that reads better downstream. REQ-v7 F-32
(the frozen fixture rendering the recompute-at-dispatch clause unfalsifiable) is **closed by this
FSPEC** — see Positive Observations.

## Questions

| ID | Question |
|----|---------|
| Q-01 | On F-03: is an empty in-scope set (E-6) byte-identical to the disabled dispatch of BR-4, or does it differ in some whitespace? AT-08 answers this for the total fail-open leg by pointing at AT-04's stream; E-6 is the one remaining path where a `true` flag produces no index and the document does not say what the bytes are. Whichever answer, stating it converts AT-14 from an absence-only oracle into a falsifiable one. |
| Q-02 | On F-02: is E-11's cardinality claim ("One line renders") genuinely this spec's, or is it TSPEC's along with the precedence rule? I read it as this spec's — it is stated in the indicative, and BR-2's "exactly once" is a per-decision invariant that does not stop at a file boundary. If that reading is right, it is assertable now against O-5's fixture and F-02 stands. If the intent was that cardinality follows from whatever precedence rule TSPEC picks, say so in E-11 and F-02 becomes a routing note rather than a missing AT. |
| Q-03 | On F-06: does the AT-16 fixture already exist in some shipped form (a recorded round of reviewer outputs), or is it a further constructed-fixture obligation of the O-6 class? §7 does not list it. If it is owed, it belongs beside O-6 for the same reason the frozen corpus copy does. |
| Q-04 | On Q-3 (yours): `advisory-config-example.test.js` exists alongside the two disclosure tests you cite, so the shipped precedent is three-for-three, not two. Does that settle the antecedent enough to answer Q-3 in this document rather than deferring it — i.e. is there a reading under which a gated block ships undisclosed? |

## Positive Observations

- **REQ-v7 F-32 is closed, and closed properly.** I raised at REQ that a frozen fixture corpus makes the recompute-at-dispatch clause structurally unfalsifiable — over a corpus that cannot move, a build that snapshots once per round window passes every leg. AT-03 fixes it exactly as the shipped `loopEconomicsAnchorFreshness` precedent does: mutate the record *between two dispatch constructions in the same round window* and assert the second index moved, with the explicit negative ("a snapshot taken at the first dispatch and rendered again fails"). This is the hardest oracle in the feature and it is right.
- **§5 is total, and says so.** "Rendering is **total**: every case below has one stated outcome, and no case is left to an engineer's discretion" is the claim I most often have to *ask* for at FSPEC. Eleven edges plus two explicit non-cases, each with a stated outcome and a rule citation, and the `(no HEAD instance)` marks tell a fixture author exactly which four need construction before they start.
- **N-1 and N-2 are the right kind of writing.** Stating the failure classes that do *not* exist ("there is no 'index too large to send' failure"; "there is no suppressed-finding state") pre-empts the invented-branch failure mode where an implementer adds a state no test covers because no document forbade it. N-2 in particular is what makes NG-4 falsifiable rather than aspirational, and §3.4's observation that the driver is never given a decision id to consult is the mechanism behind it, not a restatement of it.
- **The fail-open path is split into two legs that degrade differently, with the third case pulled out of the failure taxonomy entirely.** §3.3's separation of the empty-but-valid file from both legs — and its grounding in `M-4e`, with two real HEAD files (`M-4a`, `M-4b`) in exactly that position — closes the empty-vs-unreadable conflation that is the classic absence-only oracle in this shape. AT-10 asserts the boundary in the direction that matters ("where it is the only source, E-6 follows rather than E-2's total leg").
- **Set equality is demanded where containment would hide a deletion.** AT-01 (whole rendered lines, not ids), AT-11 ("a fourth key or a different spelling fails"), and BR-10's cross product are all specified as equality over a closed enumeration. This matches the shipped `learningsBaselineGuard.test.js`, whose mutation proof records that a containment check would have let a whole deleted case pass.
- **The frozen-fixture choice honours DC-15.** Asserting against a frozen copy at the Baseline's `Verified at` commit rather than the live repository — which grows on this very branch — is the difference between an oracle that measures the change and one that measures the host.
- **Q-1, Q-2 and Q-3 are the right three to have raised**, and each names its owner and why the answer belongs downstream. Q-2 in particular (a document with no feature directory) is a totality hole in the in-scope set that would otherwise have surfaced as an implementer's coin-flip.

## Recommendation

**Needs revision**

Three High findings, all of one kind: a named behavioural branch whose outcome this document states
and no acceptance test asserts (DC-05). What must change:

1. **F-01** — give E-9 an assertion. Either a conjunct on AT-01 naming `M-4d`'s mixed file in the fixture and asserting its 8 non-record headings contribute no line, or a dedicated AT.
2. **F-02** — give E-11 an assertion. An AT over O-5's synthetic two-file corpus asserting the cardinality conjunct only ("exactly one line for an id recorded in both files"), leaving the winner to TSPEC.
3. **F-03** — pair AT-14's absence assertion with a positive one, on the model AT-08 already sets: state what the dispatch *does* contain when the in-scope set is empty, so that a build emitting standalone rule text fails.

The six Mediums are recorded and not gating, but F-04 and F-05 are the two that get materially more
expensive downstream: both are about AT-01's expected value, both are one clause each, and both are
on their second document. Closing them here is the last cheap moment.

## Verdict

VERDICT: Needs revision
{"high": 3, "medium": 6, "low": 2}
