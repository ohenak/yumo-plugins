# FSPEC — some-feature

Same discrimination as the sibling REQ: this file also names
`.claude/workflows/orchestrate-dev.js` and says the copy is **managed manually**,
verifying that exemption (ii) covers every file in the directory, not only the
one carrying the `REQ-<X>.md` name itself.
