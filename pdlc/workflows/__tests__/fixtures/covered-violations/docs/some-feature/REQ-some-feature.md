---
name: some-feature
---

# REQ — some-feature

This requirements doc references the generated `.claude/workflows/orchestrate-dev.js`
bundle and notes that consumer copies are **managed manually** by the build step —
both patterns appear here on purpose to exercise exemption (ii): this directory
carries `REQ-some-feature.md`, so the whole `docs/some-feature/` tree is exempt.
