/**
 * orchestrate-dev.bundle.js — fixture stand-in for the generated runtime
 * artifact. References .claude/workflows/orchestrate-dev.js and is
 * managed manually only via the build step, not by hand. Exempt via (i):
 * this whole tree is a generated tree.
 */

export const meta = { name: "orchestrate-dev" };
