/**
 * orchestrate-queue.js — fixture stand-in.
 *
 * The runtime copy under .claude/workflows/ is a generated artifact and is
 * managed manually only in the sense that a human triggers the build step.
 */

export const meta = { name: "orchestrate-queue" };
