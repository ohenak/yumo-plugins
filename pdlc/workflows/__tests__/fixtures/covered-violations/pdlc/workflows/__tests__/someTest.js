/**
 * someTest.js — fixture stand-in for a test file that happens to mention a
 * covered pattern. References .claude/workflows/orchestrate-dev.js and says
 * the copy is managed manually. Exempt via (iv): any __tests__/ path.
 */

test("placeholder", () => {
  expect(true).toBe(true);
});
