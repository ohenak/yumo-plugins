/**
 * orchestrate-queue.bundle.js — fixture stand-in for the generated dist
 * artifact. References .claude/workflows/orchestrate-queue.js and notes the
 * copy is managed manually only via the build step. Exempt via (i):
 * pdlc/workflows/dist/ is a generated tree.
 */

export const meta = { name: "orchestrate-queue" };
