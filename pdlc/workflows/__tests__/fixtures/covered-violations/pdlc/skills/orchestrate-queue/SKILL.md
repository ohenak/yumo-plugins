---
name: orchestrate-queue
description: Serial queue driver.
---

# orchestrate-queue

Copying the bundle into a consumer repo happens automatically the first time
`build-runtime.mjs` runs — there is no manual step for the operator.
