// Stand-in for `pdlc/workflows/dist/orchestrate-queue.bundle.js` (TSPEC §7.7, PROP-READ-4).
//
// Same role as the neighbouring `orchestrate-dev.bundle.js` stand-in: content is illustrative,
// never executed. It exists so this fixture's `.claude/workflows/` tree is populated with more
// than one entry, matching what a real `sync-workflows.sh` run leaves behind.
export const STAND_IN = "orchestrate-queue.bundle.js";
